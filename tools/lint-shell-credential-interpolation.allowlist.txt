# SPDX-License-Identifier: FSL-1.1-ALv2
# Phase 31 LOCKER-06 — shell-credential-interpolation allowlist.
# Format: one `path:line` per non-blank, non-`#`-prefixed line. Trailing
# `# rationale` is permitted (everything after `#` is stripped).
#
# Adding an entry here REQUIRES a tracking issue and one-line rationale.
#
# Phase 36.a (CRIT-FIX-07): the 3 original seed entries (audit-archive.ts
# lines 106/115/127) have been REMOVED — the corresponding call sites in
# `apps/worker/src/jobs/audit-archive.ts` were rewritten in the SAME
# commit to use argv-array `spawn()` + PG* env vars (no bash, no dbUrl
# in argv). LOCKER-06 is now BLOCKING for production code (apps/**/src,
# packages/**/src).
#
# The 11 entries below are PRE-EXISTING test + tooling debt that the
# WARN-only flag was hiding. They are scoped to `tests/`, `tools/`, and
# `packages/data/migrations/__tests__/` — NOT production paths. They
# interpolate test-fixture passwords (`ownerPassword`) or non-secret
# operator hostnames (`BACKEND_URL`). Tracked for a dedicated test-tooling
# follow-up phase; see `.planning/phases/36-worker-bundle/36-a-DECISIONS.md`
# §D-1 for rationale. NOT blocking the Phase 36.a closure (LOCKER-06's
# regression target is the production audit-archive call sites; those
# are clean and the BLOCKING flip prevents their re-introduction).
packages/data/migrations/__tests__/0017-setup-state.test.ts:81  # pre-existing test-fixture password — Phase 36.a DECISIONS §D-1
packages/data/migrations/__tests__/0017-setup-state.test.ts:84  # pre-existing test-fixture password — Phase 36.a DECISIONS §D-1
packages/data/migrations/__tests__/0017-setup-state.test.ts:95  # pre-existing test-fixture password — Phase 36.a DECISIONS §D-1
tests/e2e/compose-helper.ts:139  # pre-existing e2e helper BACKEND_URL — non-secret — Phase 36.a DECISIONS §D-1
tests/e2e/compose-helper.ts:150  # pre-existing e2e helper BACKEND_URL — non-secret — Phase 36.a DECISIONS §D-1
tests/e2e/helpers/phase6-compose.ts:316  # pre-existing e2e helper BACKEND_URL — non-secret — Phase 36.a DECISIONS §D-1
tests/e2e/helpers/phase6-compose.ts:782  # pre-existing e2e helper BACKEND_URL — non-secret — Phase 36.a DECISIONS §D-1
tests/e2e/helpers/phase6-compose.ts:801  # pre-existing e2e helper BACKEND_URL — non-secret — Phase 36.a DECISIONS §D-1
tests/self-tests/rls-introspection.test.ts:40  # pre-existing self-test password — Phase 36.a DECISIONS §D-1
tests/self-tests/rls-introspection.test.ts:58  # pre-existing self-test password — Phase 36.a DECISIONS §D-1
tools/lint-rls.test.ts:67  # pre-existing lint-rls test-fixture password — Phase 36.a DECISIONS §D-1
