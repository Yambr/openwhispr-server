# SPDX-License-Identifier: FSL-1.1-ALv2
# Phase 31 LOCKER-06 — shell-credential-interpolation allowlist.
# Format: one `path:line` per non-blank, non-`#`-prefixed line. Trailing
# `# rationale` is permitted (everything after `#` is stripped).
#
# Adding an entry here REQUIRES a tracking issue and one-line rationale.
# Removing all entries is the WARN→BLOCKING flip trigger (Phase 36.a).
#
# Seed = 3 entries from `apps/worker/src/jobs/audit-archive.ts` — the
# CR-5 source. Phase 36.a (CRIT-FIX-05) rewrites those call sites to
# argv-array form AND clears this file in the same closing commit.
apps/worker/src/jobs/audit-archive.ts:106  # CR-5 — fixed in Phase 36.a; remove from allowlist when locker flips to BLOCKING
apps/worker/src/jobs/audit-archive.ts:115  # CR-5 — fixed in Phase 36.a; remove from allowlist when locker flips to BLOCKING
apps/worker/src/jobs/audit-archive.ts:127  # CR-5 — fixed in Phase 36.a; remove from allowlist when locker flips to BLOCKING
