{
  "name": "@openwhispr/load-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "k6 load-test workspace for OpenWhispr Server (Phase 08).",
  "main": "./src/main.ts",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/k6": "^1.3.0",
    "@vitest/coverage-v8": "4.1.5",
    "tsup": "8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
