# SPDX-License-Identifier: FSL-1.1-ALv2
# Phase 31 LOCKER-05 — secret-shape-in-error allowlist.
# Format: one `path:line` per non-blank, non-`#`-prefixed line. Trailing
# `# rationale` is permitted (everything after `#` is stripped).
#
# Adding an entry here REQUIRES a tracking issue and one-line rationale.
# An empty allowlist file is the BLOCKING-mode steady state.
#
# History:
#   * Phase 31-05 seeded 3 entries (CR-9 source + 2 siblings).
#   * Phase 37 (CRIT-FIX-09) landed the fix on all 3 sites and cleared
#     this file in the same closing commit that drops `--warn-only`
#     from `package.json` `lint:secret-shape-in-error`.
