# Stub. Real content authored in Phase 14 / Plan 02 / Task 2 (next commit).
# Exists at this point so tools/bootstrap.sh --print-template can resolve
# its default path without tripping the existence guard.
