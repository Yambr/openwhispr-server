# v2.5 — Post-publish security-alert remediation

**Opened:** 2026-05-23, after GitHub security scanning fired on the freshly
published `github.com/Yambr/openwhispr-server`.

## Alert inventory

### Secret scanning — 1, RESOLVED
- Alert #1 `ASIAIOSFODNN7EXAMPLE` in `redact-url-shapes.test.ts` — AWS's own
  synthetic documentation example, a test fixture for the byok-guard URL
  redactor. Resolved as `false_positive` via API (not a real credential).

### Code scanning — 37 alerts

**Production code — REAL, fix (8):**
| # | Rule | Location |
|---|------|----------|
| 21 | js/clear-text-logging | apps/api/src/config/auth.ts:104 |
| 20 | js/request-forgery | apps/api/src/routes/__test/fetch.ts:100 |
| 33 | js/missing-rate-limiting | apps/api/src/routes/setup-admin.ts:160 |
| 14 | js/polynomial-redos | apps/api/src/middleware/dual-auth.ts:225 |
| 15 | js/polynomial-redos | apps/api/src/routes/test-only.ts:124 |
| 17 | js/polynomial-redos | packages/byok-guard/src/redact-url.ts:116 |
| 19 | js/polynomial-redos | packages/litellm-client/src/index.ts:300 |
| 36,37 | actions/missing-workflow-permissions | e2e-cjm.yml, conformance-axe.yml |

**Test-file noise — suppress via CodeQL config / inline (16):**
- js/disabling-certificate-validation ×7 — all `tests/e2e/*` + `contract-tests`
  (dev self-signed certs; `rejectUnauthorized:false` is legitimate for the
  hermetic e2e stack against `*.localhost`).
- js/incomplete-sanitization ×3 — `locale-parity-sweep.test.tsx`,
  `redact-url-shapes.test.ts`, `compose-overlays.test.ts`.
- js/insecure-randomness ×2 — `seed.ts`, `probe.ts` test fixtures.
- js/polynomial-redos ×2 in tests — `test-only-seed-tenant.test.ts`,
  `conformance.ts` (seed).
- js/clear-text-logging — `log-scrub-sentinel.test.ts` (a test that DELIBERATELY
  logs to verify the scrubber).

**Dependency CVEs surfaced as code-scanning (13)** — overlap with Dependabot;
fixed by the dependency bumps below.

### Dependabot — 28 open alerts, 8 packages
| Package | Alerts | Max severity |
|---|---|---|
| protobufjs | 8 | medium (several high in advisories) |
| fastify | 7 | high |
| nodemailer | 7 | medium |
| @opentelemetry/sdk-node | 2 | high |
| esbuild / postcss / qs / uuid | 1 each | medium |

## Remediation plan (v2.5)

- **v2.5-A — dependency bumps:** `pnpm update` the 8 flagged packages to
  patched versions; re-run the full suite; verify CVEs clear.
- **v2.5-B — production code-scanning fixes:** the 8 real findings, TDD-paired.
- **v2.5-C — CodeQL noise suppression:** a `.github/codeql/codeql-config.yml`
  query-filter / path-filter for the legitimate test-file patterns, so future
  scans stay signal-rich. NOT blanket-disabling — narrowly scoped to
  `tests/**` for the specific rules that are legitimate there.
