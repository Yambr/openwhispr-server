---
phase: quick-260528-o73
plan: 01
subsystem: helm-chart
tags: [chart, litellm, anti-drift, notes, incident-64]
requires: []
provides:
  - "charts/openwhispr/examples/values-embedded-litellm.yaml litellm.config.model_list (12 canonical aliases)"
  - "charts/openwhispr/templates/NOTES.txt empty-model_list-on-embedded WARNING (fingerprint 'model_list is empty')"
affects: []
tech-stack:
  added: []
  patterns: ["helm NOTES.txt conditional guard on empty config", "chart example mirrors compose source-of-truth verbatim"]
key-files:
  created: []
  modified:
    - charts/openwhispr/examples/values-embedded-litellm.yaml
    - charts/openwhispr/templates/NOTES.txt
decisions:
  - "No chart version/appVersion bump: only an example overlay + install-time NOTES text changed; no deployed Kubernetes resource changes (default values.yaml model_list stays [])."
metrics:
  duration: "~25m"
  completed: "2026-05-28"
  tasks: 2
  files: 2
  commit: 5afe0f2e
requirements: [CHART-DRIFT-64]
---

# Quick 260528-o73: Chart Canonical LiteLLM model_list Example + Empty-model_list NOTES Warning Summary

Closed chart anti-drift incident #64 by shipping the 12 canonical server-baked LiteLLM aliases in `values-embedded-litellm.yaml` (mirroring `compose/litellm/litellm_config.yaml` verbatim) plus a Helm `NOTES.txt` install-time WARNING that fires when an embedded install leaves `model_list` empty — so operators copy the RIGHT alias names instead of full provider paths that trigger HTTP 400 "Invalid model name" and a silent broken agent chat.

## What Was Done

### Task 1 — Populate `values-embedded-litellm.yaml` with 12 canonical aliases
Added a `config:` child (`model_list` + `router_settings: {}`) immediately under the existing `litellm: { embedded: true }` block (NOT appended at EOF — that would have nested it under the trailing `worker:` block and rendered 0 models). All 12 entries copied verbatim from the source-of-truth:

| # | model_name | model (backing path) | extras |
|---|---|---|---|
| 1 | qwen3.6-plus | openrouter/qwen/qwen3.6-plus | (agent default) |
| 2 | gemini-3-flash | openrouter/google/gemini-3.1-flash-lite | `-lite` GA (not -preview) |
| 3 | gpt-4o-mini | openrouter/openai/gpt-4o-mini | |
| 4 | qwen3.6-cleanup | openrouter/qwen/qwen3.6-35b-a3b | api_base: os.environ/REASONING_CLEANUP_API_BASE |
| 5 | whisper-large-v3 | groq/whisper-large-v3 | LITERAL api_base https://api.groq.com/openai/v1 |
| 6 | realtime-default | openai/gpt-realtime | mode: realtime |
| 7 | gpt-realtime | openai/gpt-realtime | mode: realtime |
| 8 | gpt-realtime-mini | openai/gpt-realtime-mini | mode: realtime |
| 9 | gpt-4o-realtime-preview | openai/gpt-4o-realtime-preview | mode: realtime (legacy comment DROPPED) |
| 10 | openwhispr-default | groq/whisper-large-v3 | LITERAL api_base https://api.groq.com/openai/v1 |
| 11 | openwhispr-reason | openrouter/openai/gpt-4o-mini | |
| 12 | openwhispr-realtime | openai/gpt-realtime | mode: realtime |

Every `model_name:` line is CLEAN (alias is the last token; the canonical source's trailing `# legacy alias for backward compat` comment on `gpt-4o-realtime-preview` was dropped). Added a comment block above `model_list` explaining model_name (sent verbatim) vs model (retargetable backing path), the HTTP-400 failure mode, and citing incident #64 + the source-of-truth chain.

### Task 2 — Empty-model_list WARNING in `NOTES.txt`
Added a guarded block `{{- if and .Values.litellm.embedded (eq (len .Values.litellm.config.model_list) 0) }}` containing the exact lowercase fingerprint `model_list is empty`, the consequence (agent route returns "Invalid model name" / "no healthy deployments"), the fix (point at `values-embedded-litellm.yaml`, qwen3.6-plus default), and the #64 reference. ASCII/English-only, no emojis.

## Verification (run from worktree root, helm v4.1.4)

`helm lint charts/openwhispr` — clean: `1 chart(s) linted, 0 chart(s) failed`.

**Task 1 — 12 aliases present (clean lines) + rendered:**
```
ALL 12 ALIASES PRESENT (clean lines)
RENDERED model_name COUNT = 12
TASK1 OK: 12 aliases present + rendered
```

**Alias-set exact diff (generated.json vs example):**
```
NO DIFF -> alias sets identical (12==12, no extras)
```

**Provider-path fidelity (most-guessable):**
```
gemini path OK (-lite GA)
cleanup path OK (qwen3.6-35b-a3b)
```

**Task 2 — NOTES warning both directions (`helm install --dry-run=client`):**
```
DEFAULT (embedded+empty) WARNING FIRES OK
POPULATED EXAMPLE SUPPRESSES WARNING OK
```

**Rendered NOTES warning text (bare-defaults dry-run):**
```
==================================== WARNING ====================================

  The embedded LiteLLM Proxy model_list is empty -- no models are registered.

  Consequence: the OpenWhispr agent route will return "Invalid model name" /
  "no healthy deployments for this model" for every request until you populate
  litellm.config.model_list with the canonical server aliases. The agent chat
  will appear broken (empty reply bubble).

  Fix: set litellm.config.model_list with the canonical aliases. A ready-to-use
  example with all 12 server-baked aliases ships at:

    charts/openwhispr/examples/values-embedded-litellm.yaml

  The agent default model is qwen3.6-plus (DEFAULT_AGENT_MODEL = the first
  alias). The model_name values are sent VERBATIM to LiteLLM -- do NOT replace
  them with full provider paths.

  Reference: chart-drift guard for incident #64.

=================================================================================
```

**Scope (git status --short):** exactly 2 files modified — `charts/openwhispr/examples/values-embedded-litellm.yaml`, `charts/openwhispr/templates/NOTES.txt`. No version bump; no compose/litellm or packages/litellm-client edits; no deletions.

**Hooks:** committed WITHOUT `--no-verify`. gitleaks (no leaks), english-only check (passed), commitlint (passed; 1 non-blocking footer-leading-blank warning). Commit `5afe0f2e`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Verify commands hardcoded the MAIN-repo path; ran them from the worktree root instead**
- **Found during:** Task 1 verify.
- **Issue:** The plan's verify blocks all prefix `cd /Users/nick/openwhispr-server` — that is the MAIN repo, not this execution worktree (`/Users/nick/openwhispr-server/.claude/worktrees/agent-ad393c399cb8f8772`). The Edits landed correctly in the worktree, but helm/grep against the main repo read a stale copy of the file → `MISSING ALIAS` false-negative. Fix: ran every verify command from `$(git rev-parse --show-toplevel)` (the worktree root) instead of the hardcoded path. No file content changed.
- **Files modified:** none (verify-invocation path only).

**2. [Rule 3 - Blocking] Built helm subchart dependencies in the worktree**
- **Found during:** Task 1 render (`helm template`).
- **Issue:** `helm template` failed with `missing in charts/ directory: valkey, minio, cert-manager` — the `.tgz` dependency archives are gitignored build artifacts present in the main repo but absent in the fresh worktree. Fix: copied the three pinned `.tgz` archives (`cert-manager-v1.16.4.tgz`, `minio-17.0.21.tgz`, `valkey-5.6.5.tgz`) from the main repo into `charts/openwhispr/charts/`. They are gitignored (confirmed via `git status`), so they do NOT enter the commit and leave no untracked debris.
- **Files modified:** none tracked (gitignored deps only).

**3. [Style] Replaced em-dashes in ADDED comment lines with ASCII `--`**
- **Found during:** post-edit non-ASCII scan.
- **Issue:** My added comment block in the example used em-dashes (`—`). Per the English-only / ASCII discipline, replaced the 3 introduced em-dashes with ` -- ` for added lines (pre-existing em-dashes in untouched context lines were left as-is). The repo's `english` pre-commit hook subsequently passed clean.
- **Files modified:** charts/openwhispr/examples/values-embedded-litellm.yaml (within the same task, pre-commit).

## Chart Bump Decision

No `Chart.yaml` version/appVersion bump (per the plan's flagged recommendation). Only an example overlay file and install-time NOTES text changed; neither alters any deployed Kubernetes resource and the default `values.yaml` `model_list` stays `[]` (external-LiteLLM operators unaffected). A bump would imply a rendered-manifest behavior change for default installs — there is none. If the user wants a release, bump `charts/openwhispr/Chart.yaml` `version` + `appVersion` together — out of scope for this quick task unless explicitly requested.

## Known Stubs

None. The example ships all 12 fully-specified aliases; no placeholder/empty values.

## Self-Check: PASSED
- `charts/openwhispr/examples/values-embedded-litellm.yaml` — FOUND (12 model_name aliases, alias-set diff clean).
- `charts/openwhispr/templates/NOTES.txt` — FOUND (warning fires on bare defaults, suppressed when populated).
- Commit `5afe0f2e` — FOUND on HEAD (`git log --oneline -1`).
- Working tree clean; exactly 2 tracked files changed; no deletions.
