---
phase: 02.4
plan: 06
type: execute
wave: 3
depends_on: [02.4-01, 02.4-02, 02.4-03, 02.4-04, 02.4-05]
files_modified:
  - .planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md
autonomous: true
requirements: [TDD-01, TDD-01b]
must_haves:
  truths:
    - "Aggregate coverage on all targeted production files reaches ≥90% lines/branches per CONTEXT acceptance gate"
    - "Each of the six test groups has documented reverse-patch evidence proving the test fails when production fix is reverted (D-02)"
    - "Repo-wide `pnpm test --coverage` exits 0 (constitutional 85/80 floor preserved)"
    - "No production code was modified in this phase (test-only per D-06)"
  artifacts:
    - path: ".planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md"
      provides: "Phase summary with reverse-patch evidence per G1/G2/G3/G4/G5a/G5b and final coverage report"
      contains: "Reverse-patch evidence"
  key_links:
    - from: "02.4-SUMMARY.md"
      to: "all six new test files + the five Phase 02.x source-of-record commits"
      via: "evidence table mapping each test group → commit → reverse-patch result"
      pattern: "Reverse-patch evidence"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Final verification + atomic commit + SUMMARY for Phase 02.4.

Purpose: Prove the acceptance gate from CONTEXT.md is met (≥90% coverage on the six targeted production files, repo-wide CI green, reverse-patch evidence per test group), then land everything in one atomic commit per CLAUDE.md TDD-01.

Output: One SUMMARY.md documenting evidence + one atomic git commit containing all six test files plus the SUMMARY. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-CONTEXT.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-01-PLAN.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-02-PLAN.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-03-PLAN.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-04-PLAN.md
@.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run aggregate coverage report and capture results</name>
  <files>(no files written — output captured for SUMMARY)</files>
  <read_first>
    - All six new test files (must exist before this task — depends_on enforced)
  </read_first>
  <action>
Run the CONTEXT.md acceptance-gate command:

```bash
pnpm vitest run --coverage \
  --coverage.include="tools/bootstrap.sh" \
  --coverage.include="packages/data/migrations/init/00-roles.sql.tpl" \
  --coverage.include="apps/api/Dockerfile" \
  --coverage.include="apps/api/tsup.config.ts" \
  --coverage.include="apps/api/src/routes/better-auth-handler.ts" \
  --coverage.include="apps/api/src/auth.ts" \
  tests/unit/bootstrap-interpolate.test.ts \
  tests/integration/postgres-roles-idempotent.test.ts \
  tests/self-tests/api-bundle-externals.test.ts \
  tests/integration/observability-stack-up.test.ts \
  apps/api/src/routes/__tests__/better-auth-handler.test.ts \
  apps/api/src/__tests__/auth-trusted-origins.test.ts
```

Capture the coverage table output. Note: shell scripts (bootstrap.sh), SQL templates (00-roles.sql.tpl), Dockerfiles, and YAML configs are NOT v8-instrumented. The acceptance gate from CONTEXT.md D-02 was authored knowing this — for those targets, the per-test assertion count + reverse-patch evidence IS the coverage proof. Document this explicitly in SUMMARY.

For the two .ts files that ARE v8-instrumented (`apps/api/src/routes/better-auth-handler.ts` and `apps/api/src/auth.ts`), record the actual lines/branches percentages from the v8 report. ≥90% on better-auth-handler.ts is required; auth.ts may legitimately be lower than 90% on the WHOLE FILE because the OIDC/email-service paths are out of scope for this phase — record the specific trustedOrigins-block coverage if v8's per-line report supports it (use lcov or json reports), and document the OIDC/email gap as deferred to Phase 02.5.

Then run repo-wide:

```bash
pnpm test --coverage
```

Capture pass/fail and overall coverage percentages.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/bootstrap-interpolate.test.ts tests/integration/postgres-roles-idempotent.test.ts tests/self-tests/api-bundle-externals.test.ts tests/integration/observability-stack-up.test.ts apps/api/src/routes/__tests__/better-auth-handler.test.ts apps/api/src/__tests__/auth-trusted-origins.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six test files run; aggregate exit 0
    - Coverage percentages captured for the two .ts targets (better-auth-handler.ts + auth.ts)
    - Coverage caveats for non-instrumented targets (sh/sql/Dockerfile/yaml) noted
    - Repo-wide `pnpm test --coverage` exits 0
  </acceptance_criteria>
  <done>Coverage data + repo-wide green status captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Execute reverse-patch verification for each test group</name>
  <files>(no files written — output captured for SUMMARY)</files>
  <read_first>
    - All six test docstrings (each declares its `Reverts:` block — execute exactly that)
  </read_first>
  <action>
For each test group, perform a temporary inverse-patch + re-run + revert sequence to PROVE the test goes RED when the production fix is removed. Use `git stash` or apply-and-restore-from-commit; do NOT commit any inverse patches.

Procedure per group:

1. Apply the inverse patch (e.g. `git apply <<EOF ...EOF` with a reversed diff, OR comment out the production fix block).
2. Run that group's test(s); record the failure message + line.
3. `git checkout -- <file>` to restore the production code.
4. Re-run the test; confirm it goes back to GREEN.

Concrete inverse patches per group:

- **G1 (interpolate + three-way)**: in `tools/bootstrap.sh`, change line 197's `RESULT["${key}"]="$(interpolate "${example_value}")"` to `RESULT["${key}"]="${example_value}"` (kills interpolation). Expect: `tests/unit/bootstrap-interpolate.test.ts` test 4 ("interpolates ${SECRET_A} inside DATABASE_URL") fails.
- **G1 (URL-default preservation)**: in `tools/bootstrap.sh`, change the else-branch (lines 213-223) so the empty-current case calls `gen_secret` instead of using `example_value`. Expect: test 2 ("preserves URL-shaped real defaults") fails because OPENWHISPR_API_URL becomes a base64 token.
- **G2 (postgres roles idempotency)**: in `packages/data/migrations/init/00-roles.sql.tpl`, replace the DO-block (lines 27-40) with plain `CREATE ROLE openwhispr_owner WITH LOGIN BYPASSRLS PASSWORD ...; CREATE ROLE openwhispr_app WITH LOGIN PASSWORD ...;`. Expect: `tests/integration/postgres-roles-idempotent.test.ts` test 3 ("second apply does NOT error") fails with SQLSTATE 42710.
- **G3 (Dockerfile no pnpm deploy)**: insert `RUN pnpm --filter @openwhispr/api --prod deploy /out` anywhere in `apps/api/Dockerfile`. Expect: `tests/self-tests/api-bundle-externals.test.ts` test "does NOT contain pnpm deploy" fails.
- **G3 (tsup external)**: in `apps/api/tsup.config.ts`, remove `"pg"` from the `external` array. Expect: test "externalizes pg" fails.
- **G4 (otel-collector loki exporter)**: in `compose/otel-collector/config.yaml`, rename `otlphttp/loki:` to `loki:` (deprecated exporter type). Expect: `tests/integration/observability-stack-up.test.ts` test "otel-collector running" fails (container exits) and the log-sentinel test also fails.
- **G5a (handler guard)**: in `apps/api/src/routes/better-auth-handler.ts`, comment out the `if (typeof handler !== 'function') throw ...` block. Expect: `apps/api/src/routes/__tests__/better-auth-handler.test.ts` test "throws when auth.handler is not a function" fails.
- **G5b (trusted origins parse)**: in `apps/api/src/auth.ts`, replace the entire `process.env.AUTH_TRUSTED_ORIGINS_EXTRA ?? "").split(...).map(...)` block with `[process.env.AUTH_TRUSTED_ORIGINS_EXTRA]`. Expect: `apps/api/src/__tests__/auth-trusted-origins.test.ts` "comma-separated list" test fails.

Record each (group → patch → failure message → restore success) row in a table for SUMMARY.

CRITICAL: after every inverse patch, run `git checkout -- <file>` to restore. Verify with `git status` clean before proceeding to the next group. Any inverse patch that accidentally lands in the final commit is a critical failure.
  </action>
  <verify>
    <automated>git status --porcelain | grep -E "^( M|MM|A )" | grep -vE "(02\.4-SUMMARY\.md|tests/(unit|integration|self-tests)/.*\.test\.ts|tests/fixtures/.*\.txt|apps/api/src/(routes/)?__tests__/.*\.test\.ts)$" && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 reverse-patch scenarios above ran, failed the expected test, and was successfully restored
    - `git status` shows ONLY new test files + fixture + SUMMARY as modified — no production file deltas
    - Failure messages captured for SUMMARY
  </acceptance_criteria>
  <done>All 8 reverse-patch scenarios verified; repo clean of accidental production diffs.</done>
</task>

<task type="auto">
  <name>Task 3: Write 02.4-SUMMARY.md</name>
  <files>.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-CONTEXT.md (acceptance gate verbatim)
    - $HOME/.claude/get-shit-done/templates/summary.md (template structure)
  </read_first>
  <action>
Create `02.4-SUMMARY.md` documenting:

1. **Phase outcome:** test-only phase, all six test groups landed, repo-wide CI green.
2. **Test artifacts table:**
   | Group | File | Tests | Source commit |
3. **Acceptance gate evidence (verbatim from CONTEXT.md):**
   - Gate 1: `pnpm test --coverage` exit code (must be 0)
   - Gate 2: targeted-files coverage report (numbers per file)
   - Gate 3: CI green (link to GHA run after push)
   - Gate 4: reverse-patch evidence (table from Task 2)
4. **Reverse-patch evidence table:**
   | Group | Inverse patch | Test that went RED | Failure excerpt | Restored clean |
5. **Coverage caveats** (D-02 compliance for non-v8 targets):
   - bootstrap.sh, 00-roles.sql.tpl, Dockerfile, otel-collector/config.yaml: shell/SQL/Dockerfile/YAML not v8-instrumented; coverage proven via reverse-patch + assertion density.
   - auth.ts: full-file coverage may be <90% because OIDC + email paths are out of scope; trustedOrigins block (lines 149-155) IS ≥90% covered (record exact lines/branches from v8 lcov report).
6. **Out of scope reaffirmed (D-06):** no production code changed; e2e contract-test path is Phase 02.5; mutation testing on these files is not in TEST-MUTATION-01 scope.
7. **Next phase pointer:** Phase 02.5 (better-auth drizzle schema) now safe to start.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md && grep -q "Reverse-patch evidence" .planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md && grep -q "Acceptance gate" .planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Contains a "Reverse-patch evidence" table with one row per test group (8 rows)
    - Contains acceptance-gate verbatim from CONTEXT.md with PASS/FAIL per gate
    - Contains coverage caveats section explaining non-v8 targets
    - References Phase 02.5 as next-step
  </acceptance_criteria>
  <done>SUMMARY written with full evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit (per CLAUDE.md TDD-01)</name>
  <files>(git operation only)</files>
  <read_first>
    - git status output (verify only test files + SUMMARY are dirty)
  </read_first>
  <action>
Verify `git status` shows ONLY:
- `tests/unit/bootstrap-interpolate.test.ts` (new)
- `tests/fixtures/bootstrap-env-template.txt` (new)
- `tests/self-tests/api-bundle-externals.test.ts` (new)
- `tests/integration/postgres-roles-idempotent.test.ts` (new)
- `tests/integration/observability-stack-up.test.ts` (new)
- `apps/api/src/routes/__tests__/better-auth-handler.test.ts` (new)
- `apps/api/src/__tests__/auth-trusted-origins.test.ts` (new)
- `.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-SUMMARY.md` (new)
- `.planning/phases/02.4-backfill-tdd-test-coverage-for-phase-02-x-yolo-cascade-6-pro/02.4-0[1-6]-PLAN.md` (new — committed as part of this phase)
- Possibly `.planning/STATE.md` + `.planning/ROADMAP.md` (gsd transition updates)

If anything else is dirty (especially production source), STOP and surface as a defect.

Stage explicit files (NOT `git add .`), then commit:

```
test(02.4): backfill TDD coverage for Phase 02.x yolo cascade

Closes the constitutional ≥90% per-phase coverage gap (PROJECT.md TDD-01b)
opened by the Phase 02.x yolo cascade (commits 451e9b3 / 26eaa69 / 7ccb8bb /
059b948 / 5f274e6 shipped without per-fix tests).

Test-only phase per CONTEXT D-06 — zero production code changes. Each test
group includes reverse-patch evidence in its docstring proving the test
fails when the corresponding production fix is reverted (CONTEXT D-02).

Test groups:
- G1: tools/bootstrap.sh interpolate + three-way value semantics
- G2: 00-roles.sql.tpl idempotent postgres role init (testcontainer)
- G3: api Dockerfile no-pnpm-deploy + tsup external pg/pg-native/better-auth
- G4: docker compose --profile obs-only stack-up (tempo/mimir/loki/otel-collector)
- G5a: better-auth-handler bridge plugin (Web Request/Response, multi-Set-Cookie)
- G5b: AUTH_TRUSTED_ORIGINS_EXTRA env parsing
```

Single atomic commit per TDD-01.
  </action>
  <verify>
    <automated>git log -1 --format="%s" | grep -q "test(02.4): backfill TDD coverage" && git diff HEAD~1 --stat | grep -vE "(\.test\.ts|02\.4-.*\.md|bootstrap-env-template\.txt|STATE\.md|ROADMAP\.md)" | grep -v "^$" | wc -l | grep -q "^[[:space:]]*0$"</automated>
  </verify>
  <acceptance_criteria>
    - Commit created with exact subject `test(02.4): backfill TDD coverage for Phase 02.x yolo cascade`
    - Commit touches ONLY test files, fixture, SUMMARY/PLAN files, and possibly STATE/ROADMAP
    - Zero production source files in the commit diff
    - `git log -1` shows the commit on the current branch
  </acceptance_criteria>
  <done>Atomic commit landed; phase 02.4 is closed.</done>
</task>

</tasks>

<verification>
  <automated>pnpm test --coverage && git log -1 --format="%s" | grep -q "test(02.4): backfill TDD coverage"</automated>
</verification>

<success_criteria>
- All four tasks complete
- Repo-wide `pnpm test --coverage` green
- SUMMARY documents reverse-patch evidence per group + coverage numbers
- Single atomic commit on the branch
- Phase 02.5 unblocked
</success_criteria>

<output>
Phase 02.4 closed. Next: `/gsd-plan-phase 02.5` (better-auth drizzle schema).
</output>
