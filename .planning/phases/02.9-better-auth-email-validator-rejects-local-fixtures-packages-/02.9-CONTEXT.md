---
phase: 02.9
status: locked
date: 2026-05-10
mode: yolo-mechanical
---

# Phase 02.9 — CONTEXT

## Goal

Close cascade tail #9: `packages/data/src/seed/conformance.ts:36` uses `rotation-test@local` (no TLD); Better Auth v1.6.9's email validator (`z.email()` from Zod v4) rejects per RFC 5321/5322. Surfaced by Phase 02.8 contract-test E2E after UUID mismatch closed.

After this phase: signup pipeline reaches verification step for ALL 5 fixtures, Phase 02.7-06 contract suite re-runs cleanly.

## Empirical confirmation (Zod v4 email validator)

Tested `z.email().safeParse(...)` against installed `zod@4.4.3`:
- `fixture@conformance.test` → **OK** (RFC 2606 reserved TLD, valid)
- `verified@conformance.test` → **OK**
- `pending@conformance.test` → **OK**
- `poll@conformance.test` → **OK**
- `rotation-test@local` → **FAIL** ← only this one
- `rotation-test@example.com` → **OK** (replacement)

So fix is strictly one line.

## Locked Decisions

### D-01 — Single-line fixture rewrite

`packages/data/src/seed/conformance.ts:36`:
```ts
{ email: "rotation-test@local", name: "Rotation Test", verified: true },
```
→
```ts
{ email: "rotation-test@example.com", name: "Rotation Test", verified: true },
```

Use `@example.com` not `@conformance.test` for variety + because the fixture is for token-rotation testing, not conformance-suite proper. Both are RFC-valid.

### D-02 — TDD discipline

1. **Failing test FIRST** — `apps/api/src/__tests__/fixture-emails-rfc-compliance.test.ts` (or extend existing seed-signup test):
   - Import `CONFORMANCE_FIXTURES` from `@openwhispr/data/seed/conformance` (export already added in Phase 02.7-04)
   - For each fixture, assert `z.email().safeParse(f.email).success === true`
   - Currently RED on `rotation-test@local`
   - After fix → GREEN
2. Reverse-patch: revert the email change → test goes RED again
3. Coverage: assertion density on the fixtures array

### D-03 — Out of scope

- Verifying check-user / OAuth / contract-test conformance for token-rotation specifically — that's the scope of Phase 02.7 plans 06+07 to verify after this fix
- Adding RFC-compliance lint to the broader codebase — defer

## Files modified

Production:
- `packages/data/src/seed/conformance.ts` — one fixture email change

Tests:
- `apps/api/src/__tests__/fixture-emails-rfc-compliance.test.ts` (new) — RFC-compliance assertion per fixture

## Acceptance gate

1. New test RED before fix, GREEN after
2. `pnpm typecheck` clean (no type changes anyway)
3. `make contract-test` advances PAST the rotation-test signup step (was failing 400; now succeeds OR surfaces yet ANOTHER cascade tail)
4. Single atomic commit
