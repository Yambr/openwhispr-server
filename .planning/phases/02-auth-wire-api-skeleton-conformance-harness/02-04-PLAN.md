---
phase: 02-auth-wire-api-skeleton-conformance-harness
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - apps/api/src/email.ts
  - apps/api/src/email.test.ts
  - apps/api/src/__tests__/email-mailpit.test.ts
  - apps/api/src/plugins/rate-limit.ts
  - apps/api/src/__tests__/rate-limit-check-user.test.ts
  - apps/api/src/__tests__/rate-limit-verification-status.test.ts
  - apps/api/src/__tests__/rate-limit-health-exempt.test.ts
  - apps/api/src/auth.ts
  - apps/api/src/index.ts
  - apps/api/package.json
  - compose/traefik/dynamic.yml
  - compose/traefik/static.yml
autonomous: true
requirements: [WIRE-20, AUTH-06, PROVIDER-04]
must_haves:
  truths:
    - "HTTP requests to Traefik web entrypoint redirect to HTTPS (websecure) with 308; HTTPS is the only externally reachable scheme"
    - "@fastify/rate-limit registered globally (60/min/IP default) with errorResponseBuilder returning {error:'Too many requests'} ONLY (no statusCode/code/message keys)"
    - "Per-route limits applied: /api/check-user 10/min/IP; /api/auth/verification-status 30/min/(ip,email); /api/auth/delete-account 5/min per req.user.id (post-auth keyGen); /api/health rateLimit:false"
    - "Fastify trustProxy:true so req.ip resolves the X-Forwarded-For client IP behind Traefik (not Traefik's own IP)"
    - "makeEmailService() returns a no-op stub when SMTP_HOST unset (logs warn 'email.smtp_not_configured'); returns nodemailer transport when set; throws on transport errors (does NOT swallow)"
    - "When SMTP_HOST=mailpit, sending an email lands a message in mailpit's /api/v1/messages with the expected subject"
    - "Better Auth's emailAndPassword.sendVerificationEmail hook calls makeEmailService.send() with subject 'Verify your OpenWhispr account' + the BA-supplied verification URL"
    - "x-openwhispr-source header logged as structured field on every request (already wired in Plan 03; this plan adds an integration test asserting log emission)"
  artifacts:
    - path: apps/api/src/email.ts
      provides: "EmailService interface + makeEmailService factory with nodemailer + dev fallback"
      contains: "makeEmailService"
    - path: apps/api/src/plugins/rate-limit.ts
      provides: "@fastify/rate-limit plugin with envelope-conformant errorResponseBuilder + Valkey backend"
      contains: "errorResponseBuilder"
    - path: compose/traefik/static.yml
      provides: "HTTPS-only entrypoint config (web → websecure 308 redirect)"
      contains: "redirectScheme"
  key_links:
    - from: apps/api/src/auth.ts
      to: apps/api/src/email.ts
      via: "emailAndPassword.sendVerificationEmail = (args) => email.send(...)"
      pattern: "sendVerificationEmail"
    - from: apps/api/src/index.ts
      to: apps/api/src/plugins/rate-limit.ts
      via: "app.register(rateLimitPlugin)"
      pattern: "rate-limit"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Wire the three convention-and-infrastructure layers Plan 03's routes depend on: HTTPS-only at Traefik (WIRE-20), per-route rate limiting with envelope-conformant 429 body (D-28), and the SMTP email transport with dev-fallback (D-26 + PROVIDER-04). Land x-openwhispr-source observability assertion (AUTH-06).

Purpose: Plan 03 provided the routes; without this plan, rate limit responses violate the global envelope (CONTRACT-01 fails), HTTP requests bypass HTTPS, and email verification is broken.

Output: Traefik HTTPS-only enforcement, rate-limit plugin + 3 integration tests, email module + mailpit integration test, Better Auth wired to send verification emails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-CONTEXT.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-CONTAINER.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md
@apps/api/src/auth.ts
@apps/api/src/index.ts
@compose/traefik/static.yml
@compose/traefik/dynamic.yml
@docker-compose.yml

<interfaces>
From Plan 01 (this phase):
- `apps/api/src/auth.ts` exports `buildAuth({db, log})`; emailAndPassword.sendVerificationEmail is a placeholder — this plan replaces with real impl

From Plan 02 (this phase):
- mailpit dev service available on `mailpit:1025` (SMTP) and `mailpit:8025` (HTTP API at /api/v1/messages)
- valkey reachable at `valkey:6379`; password via VALKEY_PASSWORD env

From @fastify/rate-limit@10.3.0:
- Default 429 body is `{statusCode, error, message}` — MUST be overridden via errorResponseBuilder
- Per-route override: `route.config.rateLimit = { max, timeWindow, keyGenerator }`
- Disable per-route: `route.config.rateLimit = false`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Email service (nodemailer + dev fallback) + Better Auth wiring + mailpit integration test</name>
  <files>apps/api/src/email.ts, apps/api/src/email.test.ts, apps/api/src/__tests__/email-mailpit.test.ts, apps/api/src/auth.ts, apps/api/package.json</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-CONTAINER.md § Pattern 4 (email.ts), § Pitfall #4 (Better Auth swallowing nodemailer errors)
    - apps/api/src/auth.ts (Plan 01 output — sendVerificationEmail placeholder is replaced here)
  </read_first>
  <behavior>
    - SMTP_HOST unset: makeEmailService() returns stub; .send() resolves with `{delivered:true, reason:'smtp-not-configured'}`; logs `event:'email.smtp_not_configured'` warn at construction
    - SMTP_HOST=mailpit, SMTP_PORT=1025: nodemailer transport with secure:false (port != 465); .send() actually delivers; integration test sends and asserts message lands in `http://mailpit:8025/api/v1/messages`
    - SMTP_HOST set + transport throws (e.g., bad host): .send() RE-THROWS — does NOT log-and-swallow (Pitfall #4); Better Auth then leaves account unverified
    - secure: port === 465 → true; else false (auto-derived per Pitfall: don't hardcode)
    - auth: only set when both SMTP_USER and SMTP_PASSWORD present
  </behavior>
  <action>
    Add deps: `pnpm --filter @openwhispr/api add nodemailer@8.0.7` and `pnpm --filter @openwhispr/api add -D @types/nodemailer`.

    Create `apps/api/src/email.ts` verbatim from 02-RESEARCH-CONTAINER.md § Pattern 4. Critical: `.send()` throws on transport failure (NOT log-and-return). The dev-fallback path (SMTP_HOST unset) is the ONLY path that returns delivered:true without actual delivery — log line MUST include `event:'email.smtp_not_configured'` so operators can grep.

    Update `apps/api/src/auth.ts` to wire `emailAndPassword.sendVerificationEmail`:
    ```typescript
    sendVerificationEmail: async ({ user, url }) => {
      await email.send({
        to: user.email,
        subject: "Verify your OpenWhispr account",
        text: `Click to verify: ${url}`,
        html: `<p>Click to verify: <a href="${url}">${url}</a></p>`,
      });
    }
    ```
    The `email` instance is constructed inside `buildAuth({db, log, email})` — pass it in. For backward compat, default `email = makeEmailService(log)` if not provided.

    Tests:
    - `email.test.ts` (unit): three cases — SMTP_HOST unset (stub path), SMTP_HOST set + mock transport.sendMail success → delivered:true, mock throws → re-throws.
    - `__tests__/email-mailpit.test.ts` (integration, gated by `describe.skipIf(!dockerAvailable || !mailpitReachable)`): start mailpit via testcontainers OR connect to docker-compose dev profile; instantiate transport with SMTP_HOST=mailpit, SMTP_PORT=1025; call email.send({to:"int@test.local", subject:"Hi", text:"body"}); poll `http://mailpit:8025/api/v1/messages` for up to 10s; assert exactly 1 message exists with subject "Hi".

    TDD pair as before.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test --run src/email.test.ts src/__tests__/email-mailpit.test.ts</automated>
  </verify>
  <done>email.test.ts green (mocked); email-mailpit.test.ts green when mailpit available (skipped cleanly otherwise); Better Auth verification email path wired (apps/api/src/auth.ts updated).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rate limit plugin + per-route configs + 3 integration tests + Fastify trustProxy</name>
  <files>apps/api/src/plugins/rate-limit.ts, apps/api/src/__tests__/rate-limit-check-user.test.ts, apps/api/src/__tests__/rate-limit-verification-status.test.ts, apps/api/src/__tests__/rate-limit-health-exempt.test.ts, apps/api/src/index.ts, apps/api/package.json</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-CONTAINER.md § Pattern 5, § Pitfall #1 (envelope mismatch), § Pitfall #2 (req.ip behind Traefik)
    - apps/api/src/routes/check-user.ts (Plan 03 — already references config.rateLimit; this plan registers the plugin)
    - apps/api/src/routes/verification-status.ts, delete-account.ts (Plan 03 — keyGenerator on (ip,email) and req.user.id respectively)
  </read_first>
  <behavior>
    - Global default: 60/min keyed on req.ip with envelope-conformant errorResponseBuilder
    - Per-route configs ALREADY supplied by Plan 03 in route definitions; this plan ensures they are honored
    - 11th call to /api/check-user from same IP within 60s → 429 with body `{error:"Too many requests"}` EXACTLY (no statusCode, no code, no message)
    - 31st call to /api/auth/verification-status with same (ip, email) → 429 with envelope; 30 calls succeed
    - 1000 calls to /api/health within 60s → ZERO 429 (rateLimit:false honored)
    - Fastify built with `trustProxy: true` so req.ip behind Traefik resolves the X-Forwarded-For client (Pitfall #2). Verified: two simulated requests with different X-Forwarded-For values land in different rate-limit buckets.
  </behavior>
  <action>
    Add deps: `pnpm --filter @openwhispr/api add @fastify/rate-limit@10.3.0 @redis/client@5`.

    Create `apps/api/src/plugins/rate-limit.ts` verbatim from § Pattern 5. Critical: `errorResponseBuilder: () => ({ error: "Too many requests" })` — body has EXACTLY one key. NameSpace `owrl:` to avoid Valkey key collisions across services.

    **Plan 04 is the sole author of `apps/api/src/index.ts`'s `buildApp()`** (Plan 03 ships route plugin modules + `routes/index.ts` barrel `allRoutes` array; Plan 04 wires them in). Construct/finalize `buildApp()` in this exact order — ordering is load-bearing:
    1. Construct Fastify with `trustProxy: true`
    2. `registerErrorHandler(app)` FIRST (Plan 03's setErrorHandler — so any plugin error during register is enveloped)
    3. Register `@fastify/cookie`
    4. Register zod-type-provider plugin (Plan 03)
    5. Register request-log plugin (Plan 03 — preserves x-openwhispr-source)
    6. Register `rateLimitPlugin` (this plan's Task 2 plugin) BEFORE any route registration so per-route `config.rateLimit` overrides apply
    7. Mount Better Auth: `app.all("/api/auth/*", ...)` per § Pattern: Fastify integration in 02-RESEARCH-AUTH.md
    8. Register `dualAuthHook` as `onRequest` (routes opt out via config.auth=false; Plan 03's middleware)
    9. Iterate Plan 03's `allRoutes` from `apps/api/src/routes/index.ts` and register each plugin
    10. Export `buildApp`; the built artifact is what `node dist/index.js` invokes

    **Import statement at top of `apps/api/src/index.ts`**: `import { allRoutes } from './routes/index.js';` — this is the handoff contract from Plan 03.

    Tests (in-process via app.inject; faster than docker compose):
    - `rate-limit-check-user.test.ts`: build app with rate-limit + check-user route; 12 sequential POSTs from same simulated ip; assert first 10 are 200, 11th is 429, body === `{error:"Too many requests"}` STRICTLY (zod ErrorEnvelope.parse(body) succeeds AND no other keys via Object.keys(body).length === 1).
    - `rate-limit-verification-status.test.ts`: sign in fixture user; 31 GETs with same email; first 30 succeed (200), 31st is 429. Then change email → first call after the boundary succeeds (separate bucket per email).
    - `rate-limit-health-exempt.test.ts`: 100 rapid GETs to /api/health (well above default 60/min) → all 200, zero 429.
    - For trustProxy verification (cross-task): in `rate-limit-check-user.test.ts` add a final test case that sets `X-Forwarded-For: 1.1.1.1` for first 10 requests, then `X-Forwarded-For: 2.2.2.2` for the 11th — assert 11th still succeeds (different bucket).

    Use Vitest + @fastify/inject. The Valkey backend is mocked via `@redis/client` in-memory mode OR a testcontainers Redis — recommended testcontainers for fidelity.

    TDD pair per test file.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test --run src/__tests__/rate-limit-check-user.test.ts src/__tests__/rate-limit-verification-status.test.ts src/__tests__/rate-limit-health-exempt.test.ts</automated>
  </verify>
  <done>All 3 rate-limit tests green; 429 body is exactly `{error:"Too many requests"}`; trustProxy bucketing verified per X-Forwarded-For; health exempt confirmed; integrates with Plan 03's route configs without changes (or with documented minor amendments).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Traefik HTTPS-only redirect + WIRE-20 self-test + AUTH-06 log-emission test</name>
  <files>compose/traefik/static.yml, compose/traefik/dynamic.yml, tests/self-tests/traefik-https-only.test.ts, apps/api/src/__tests__/openwhispr-source-log.test.ts</files>
  <read_first>
    - compose/traefik/static.yml (existing Phase 1 — find the `entryPoints` block)
    - compose/traefik/dynamic.yml (existing — find `routers`)
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-WIRE.md § HTTPS-only convention
  </read_first>
  <behavior>
    - HTTP request to http://api.localhost/api/health → 308 Permanent Redirect to https://api.localhost/api/health (Traefik http→websecure scheme redirect)
    - All routers in dynamic.yml that previously listened on `web` (HTTP) entrypoint now listen on `websecure` only; `web` is configured purely as the redirect entrypoint
    - x-openwhispr-source header → captured in pino log child as field `openwhisprSource`; assertion test captures stdout, parses JSON log lines, finds the field
  </behavior>
  <action>
    Edit `compose/traefik/static.yml`:
    ```yaml
    entryPoints:
      web:
        address: ":80"
        http:
          redirections:
            entryPoint:
              to: websecure
              scheme: https
              permanent: true   # 308
      websecure:
        address: ":443"
    ```
    (Preserve any existing entryPoints.web block — augment, don't blindly overwrite.)

    Audit `compose/traefik/dynamic.yml`: every router that says `entryPoints: [web]` → change to `entryPoints: [websecure]` and add `tls: {}`. Mailpit router added in Plan 02 already uses websecure — verify.

    Create `tests/self-tests/traefik-https-only.test.ts`: docker-compose up; HTTP GET to http://api.localhost/api/health; assert status in [301,302,308]; assert Location header starts with `https://`. Skip cleanly if Docker unavailable.

    Create `apps/api/src/__tests__/openwhispr-source-log.test.ts`: build app with the request-log plugin (Plan 03); use Fastify's `logger` configured with a custom destination (memory stream); inject GET /api/health with header `x-openwhispr-source: desktop`; assert at least one log line in the stream contains `"openwhisprSource":"desktop"`.

    TDD pair for the AUTH-06 log test (compose YAML edits don't TDD cleanly; the self-test asserts behavior end-to-end and that's sufficient).
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test --run src/__tests__/openwhispr-source-log.test.ts && pnpm vitest run tests/self-tests/traefik-https-only.test.ts</automated>
  </verify>
  <done>Traefik static.yml has http→https redirect; all dynamic.yml routers on websecure; HTTP request to api.localhost gets 308 to https; x-openwhispr-source preserved in structured logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik :80 | Plaintext requests crossed here only to be 308'd to HTTPS |
| Untrusted client → rate-limit bucket key | req.ip + (email|user.id) — must not be spoofable |
| Email transport → external SMTP | Operator's relay; STARTTLS via nodemailer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Plaintext HTTP downgrade attack | mitigate | Traefik http→https permanent (308) redirect; no service exposes :80 directly; conformance test asserts |
| T-02-04-02 | Information Disclosure | Default 429 body leaks framework details | mitigate | errorResponseBuilder returns `{error:"Too many requests"}` only — no statusCode/code/message |
| T-02-04-03 | Tampering | X-Forwarded-For spoofing to bypass rate limit | accept (v1) | trustProxy:true is acceptable inside the closed Docker network; Phase 6 ops hardening reduces to CIDR allowlist |
| T-02-04-04 | Information Disclosure | Email-relay abuse via misconfigured SMTP | mitigate | Operator owns SMTP; nodemailer doesn't retry on 4xx (no amplification); .send() throws on transport error so operator sees logs |
| T-02-04-05 | Denial of Service | Better Auth swallows email errors → user stuck | mitigate | email.send() RE-THROWS on transport failure; Better Auth leaves account unverified; verification-status keeps returning false until operator fixes SMTP |
| T-02-04-06 | Information Disclosure | mailpit accessible in production | mitigate | profiles:[dev] in compose (Plan 02); never instantiated by `docker compose up` default |
</threat_model>

<verification>
- All Task verifies green
- pnpm test (full root) green
- pnpm exec biome check . + lint-english.ts green
- compose config still validates (default + dev profiles)
- The CONTRACT-01 contract test for /api/check-user 11th-call (Plan 06) will use the rate-limit infra established here
</verification>

<success_criteria>
- WIRE-20 satisfied: HTTP → 308 HTTPS at Traefik
- D-28 honored: per-route rate limits with envelope-conformant 429 body
- D-26 + PROVIDER-04 satisfied: SMTP via nodemailer with dev fallback; mailpit integration verified
- AUTH-06 satisfied: x-openwhispr-source preserved in structured logs
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-04-SUMMARY.md`. Note any deviations from the research doc and the open question CONTAINER-A2 mailpit /livez resolution if Plan 02 didn't already settle it.
</output>
