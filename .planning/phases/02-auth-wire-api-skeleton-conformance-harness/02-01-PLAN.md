---
phase: 02-auth-wire-api-skeleton-conformance-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/auth.ts
  - apps/api/src/lib/scheme-allowlist.ts
  - apps/api/src/lib/scheme-allowlist.test.ts
  - apps/api/src/lib/cookie-domain.ts
  - apps/api/src/lib/cookie-domain.test.ts
  - apps/api/src/lib/token-rotation.ts
  - apps/api/src/lib/token-rotation.test.ts
  - apps/api/src/lib/default-tenant.ts
  - packages/data/src/schema/accounts.ts
  - packages/data/src/schema/verifications.ts
  - packages/data/src/schema/oauth_state.ts
  - packages/data/src/schema/users.ts
  - packages/data/src/schema/sessions.ts
  - packages/data/src/schema/index.ts
  - packages/data/migrations/0001_better_auth.sql
  - packages/data/migrations/0002_oauth_state.sql
  - packages/data/src/__tests__/0001_better_auth.test.ts
  - packages/data/src/__tests__/0002_oauth_state.test.ts
autonomous: true
requirements: [AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Better Auth instance boots with email+password enabled and (when OIDC_* env set) generic OIDC provider configured"
    - "0001_better_auth.sql adds account + verification tables, extends users (email_verified_at, name, image, password_hash) and sessions (previous_token_hash, previous_token_expires_at, ip_address, user_agent)"
    - "0002_oauth_state.sql creates oauth_state table; both migrations pass tools/lint-rls.ts (FORCE RLS + tenant policies)"
    - "lookup_session_by_previous_token(bytea) SECURITY DEFINER function is callable by openwhispr_app and returns rows only when previous_token_expires_at > now()"
    - "validateScheme() rejects javascript/data/file/control-chars/uppercase/non-allowlist; accepts openwhispr/openwhispr-dev/openwhispr-staging plus OPENWHISPR_PROTOCOL override"
    - "cookieDomainConfig() returns single-host omit, eTLD+1 for shared parent, and throws for unrelated hosts"
  artifacts:
    - path: apps/api/src/auth.ts
      provides: "Better Auth instance + buildAuth(opts) factory"
      contains: "betterAuth"
    - path: packages/data/migrations/0001_better_auth.sql
      provides: "Better Auth tables + sessions/users extensions + SECURITY DEFINER function"
      contains: "lookup_session_by_previous_token"
    - path: packages/data/migrations/0002_oauth_state.sql
      provides: "oauth_state table for OAuth shim"
      contains: "CREATE TABLE oauth_state"
  key_links:
    - from: apps/api/src/auth.ts
      to: packages/data/src/client.ts (makeAppDb)
      via: drizzleAdapter(db, { provider: 'pg', schema })
      pattern: "drizzleAdapter"
    - from: packages/data/migrations/0001_better_auth.sql
      to: tools/lint-rls.ts
      via: "FORCE ROW LEVEL SECURITY + tenant_isolation policies"
      pattern: "FORCE ROW LEVEL SECURITY"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Wire Better Auth 1.6.9 into the API process bound to Phase 1's `appDb`, ship migrations 0001 (Better Auth tables + extends users/sessions for AUTH-04 overlap) and 0002 (oauth_state), and land the three pure-function libraries that subsequent Wave 2/3 plans depend on: scheme allow-list, cookie-domain resolver, token-rotation overlap helpers.

Purpose: Establishes the auth substrate. Every later plan (wire endpoints, OAuth shim, conformance suite) imports from `auth.ts` or the migrations created here.

Output: Working `betterAuth(...)` instance, two migrations passing `tools/lint-rls.ts`, three unit-tested libraries, schema files for new tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-CONTEXT.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md
@packages/data/src/client.ts
@packages/data/src/tenant-context.ts
@packages/data/src/schema/users.ts
@packages/data/src/schema/sessions.ts
@packages/data/src/schema/index.ts
@packages/data/migrations/0000_initial.sql
@tools/lint-rls.ts

<interfaces>
From packages/data/src/client.ts:
```typescript
export function makeAppDb(): { db: NodePgDatabase<schema>, pool: Pool };
export function makeOwnerDb(): { db: NodePgDatabase<schema>, pool: Pool };
export type AppDb = ReturnType<typeof makeAppDb>['db'];
```

From packages/data/src/tenant-context.ts:
```typescript
export async function withTenant<T>(db: AppDb, tenantId: string, fn: (tx) => Promise<T>): Promise<T>;
```

From packages/data/src/schema/users.ts (existing — Phase 1):
```typescript
export const users = pgTable('users', {
  id: uuid('id').primaryKey().defaultRandom(),
  tenantId: uuid('tenant_id').notNull().references(() => tenants.id),
  email: text('email').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
}, /* unique (tenantId, email) */);
```

Phase 2 adds via 0001 migration: `name text`, `email_verified boolean NOT NULL DEFAULT false`, `email_verified_at timestamptz`, `image text`, `password_hash text`. Drizzle TS schema MUST be updated to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-function libraries (RED→GREEN, no infra)</name>
  <files>apps/api/src/lib/scheme-allowlist.ts, apps/api/src/lib/scheme-allowlist.test.ts, apps/api/src/lib/cookie-domain.ts, apps/api/src/lib/cookie-domain.test.ts, apps/api/src/lib/token-rotation.ts, apps/api/src/lib/token-rotation.test.ts</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md § Channel-Scheme Allow-List, § Cookie Host Scoping, § Token Rotation Overlap
    - .planning/research/PITFALLS.md (entries #4, #5, #8)
  </read_first>
  <behavior>
    - validateScheme:
      - empty / >32 chars / control chars / non-RFC-3986 → `{ok:false, reason}`
      - `javascript`, `data`, `file`, `vbscript`, `about`, `chrome`, `chrome-extension` → reject (deny-list)
      - any prefix `ms-*` → reject
      - uppercase scheme → reject (we are stricter than RFC)
      - `openwhispr` / `openwhispr-dev` / `openwhispr-staging` → accept
      - `mycorp-whispr` only when `OPENWHISPR_PROTOCOL=mycorp-whispr` set → accept
      - other custom scheme without env override → reject ("not in allow-list")
    - buildProtocolRedirect("openwhispr", "abc+/=") → `openwhispr://?bearer_token=abc%2B%2F%3D`
    - findSharedParentDomain:
      - ("api.example.com", "api.example.com") → "example.com"
      - ("auth.example.com", "api.example.com") → "example.com"
      - ("auth.foo.com", "api.bar.com") → null
      - single-label TLDs ("a.localhost", "b.localhost") → "localhost" rejected (need ≥2 labels)
    - cookieDomainConfig (env-driven):
      - AUTH_URL or OPENWHISPR_API_URL unset → `{enabled:false}`
      - same host → `{enabled:false}` (omit domain)
      - shared eTLD+1 → `{enabled:true, domain:".example.com"}`
      - unrelated hosts → throws Error containing both hostnames
    - hashToken: SHA-256, 32-byte Buffer; same input → same output
  </behavior>
  <action>
    Implement the three pure-function modules verbatim from 02-RESEARCH-AUTH.md (§ Channel-Scheme Allow-List source, § Cookie Host Scoping source, § Token Rotation Overlap `hashToken` only — DB-touching helpers are Task 3). Per D-06: regex `/^[a-z][a-z0-9+\-.]*$/`, max 32 chars, dangerous-scheme deny-list per the research doc. Per AUTH-A1 (case sensitivity): reject uppercase explicitly even though RFC permits — research doc rationale stands. Tests precede implementation; commit pair `test(02-01): RED — scheme/cookie/token-rotation helpers` then `feat(02-01): GREEN — scheme-allowlist + cookie-domain + token hash`.
    NO infra deps in this task: no DB, no Better Auth import, no Fastify import. These three files compile + test against Vitest in-process only.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test --run src/lib/scheme-allowlist.test.ts src/lib/cookie-domain.test.ts src/lib/token-rotation.test.ts</automated>
  </verify>
  <done>All three test files green; coverage on the three .ts files ≥95% lines; biome + lint-english pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drizzle schema files + migrations 0001 + 0002 (with RLS lint compliance)</name>
  <files>packages/data/src/schema/accounts.ts, packages/data/src/schema/verifications.ts, packages/data/src/schema/oauth_state.ts, packages/data/src/schema/users.ts, packages/data/src/schema/sessions.ts, packages/data/src/schema/index.ts, packages/data/migrations/0001_better_auth.sql, packages/data/migrations/0002_oauth_state.sql, packages/data/src/__tests__/0001_better_auth.test.ts, packages/data/src/__tests__/0002_oauth_state.test.ts</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md § Migrations
    - packages/data/migrations/0000_initial.sql (Phase 1 pattern reference for FORCE RLS + policy shape)
    - tools/lint-rls.ts (rules every new tenant_id-bearing table must satisfy)
    - packages/data/src/__tests__/migration-rollback.test.ts (Phase 1 test pattern)
  </read_first>
  <behavior>
    - 0001 forward+rollback applies cleanly against a fresh testcontainers Postgres 17
    - tools/lint-rls.ts passes after 0001 + 0002 land
    - account / verification / oauth_state all have ENABLE + FORCE RLS + tenant_isolation USING+WITH CHECK policy
    - lookup_session_by_previous_token(bytea) returns 0 rows when previous_token_expires_at <= now(); returns 1 row otherwise
    - openwhispr_app has EXECUTE on the function; public is REVOKEd
    - users gains email_verified, email_verified_at, name, image, password_hash; sessions gains previous_token_hash (bytea), previous_token_expires_at (timestamptz), ip_address, user_agent; partial index on sessions(previous_token_hash) WHERE NOT NULL
  </behavior>
  <action>
    Per D-22: hand-author both SQL migrations following the templates in 02-RESEARCH-AUTH.md § Migrations verbatim (account, verification, oauth_state tables — all with tenant_id FK to tenants(id), FORCE RLS, tenant_isolation policy referencing `current_setting('app.tenant_id', true)::uuid`, indexes per research doc).

    Mirror the schema in TS: create `accounts.ts`, `verifications.ts`, `oauth_state.ts` (Drizzle pgTable definitions matching the SQL columns exactly — these are CONSUMED by the Drizzle adapter in Better Auth via `schema` arg). Extend `users.ts` (add `name`, `email_verified`, `email_verified_at`, `image`, `password_hash` columns) and `sessions.ts` (add `previous_token_hash` bytea, `previous_token_expires_at` timestamptz, `ip_address`, `user_agent`). Update `schema/index.ts` to re-export new tables.

    Add the SECURITY DEFINER function exactly as specified in 02-RESEARCH-AUTH.md § Token Rotation Overlap (REVOKE FROM public, GRANT EXECUTE TO openwhispr_app).

    Tests (testcontainers, Phase 1 pattern): `0001_better_auth.test.ts` applies migration on a fresh Postgres, asserts (a) tables present, (b) FORCE RLS via pg_class.relforcerowsecurity, (c) policy exists, (d) function exists + permissions correct, (e) extension columns on users/sessions present. `0002_oauth_state.test.ts` same shape for oauth_state.

    Resolves AUTH-A3 open question: confirms previous_token machinery is required (Better Auth 1.6.9 stock rotation invalidates immediately — verified during this task by reading `node_modules/better-auth/dist/plugins/bearer/*` after `pnpm install` in Task 3; if Better Auth supports overlap natively, document the finding in SUMMARY.md and DELETE the previous_token columns + function in a follow-up commit).

    NO `drizzle-kit generate` — Phase 1 established that drizzle-kit doesn't emit FORCE RLS; we hand-author. Run `tools/lint-rls.ts` locally before commit; commit fails if lint reports any new unguarded table.

    TDD pair: `test(02-01): RED — 0001+0002 migration tests` → `feat(02-01): GREEN — Better Auth + oauth_state migrations + RLS policies`.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/data test --run src/__tests__/0001_better_auth.test.ts src/__tests__/0002_oauth_state.test.ts && pnpm tsx tools/lint-rls.ts</automated>
  </verify>
  <done>Both tests green against testcontainers Postgres; tools/lint-rls.ts exits 0; pnpm typecheck green; pnpm test:migration (Phase 1's existing migration roundtrip) green.</done>
</task>

<task type="auto">
  <name>Task 3: Better Auth instance + Drizzle adapter binding (apps/api/src/auth.ts)</name>
  <files>apps/api/package.json, apps/api/src/auth.ts, apps/api/src/lib/default-tenant.ts</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md § Standard Stack, § Pattern: Better Auth instance bound to Phase 1 `appDb`, § Pattern: Fastify integration
    - apps/api/src/auth.ts — does NOT exist; this task creates it
  </read_first>
  <action>
    Add deps: `pnpm --filter @openwhispr/api add better-auth@1.6.9 @fastify/cookie@11.0.2`. Add devDep `@better-auth/cli@1.6.9` to root.

    Create `apps/api/src/auth.ts` with `buildAuth({ db, log })` factory that returns a Better Auth instance configured per 02-RESEARCH-AUTH.md (§ Pattern: Better Auth instance):
    - `database: drizzleAdapter(db, { provider: 'pg', schema })` — db is `appDb` (PgBouncer/RLS-subject) per D-01; explicitly NOT ownerDb
    - `secret: process.env.BETTER_AUTH_SECRET` — let Better Auth's own validation throw on missing
    - `baseURL: process.env.AUTH_URL ?? 'http://localhost:3000'`
    - `trustedOrigins: [process.env.OPENWHISPR_API_URL, process.env.AUTH_URL].filter(Boolean)`
    - `emailAndPassword: { enabled: true, requireEmailVerification: true, sendVerificationEmail }` — `sendVerificationEmail` is a placeholder hook here (`async () => {}`); plan 04 wires nodemailer
    - `session: { expiresIn: 60*60*24*30, updateAge: 60*60*24, cookieCache: { enabled: true, maxAge: 300 } }` per D-03 (≥30-day TTL)
    - `advanced: { cookiePrefix: 'openwhispr', crossSubDomainCookies: cookieDomainConfig(), useSecureCookies: process.env.NODE_ENV === 'production' }` — calls Task 1 helper
    - `plugins: [bearer(), ...oidcProviders.length > 0 ? [genericOAuth({ config: oidcProviders })] : []]` — OIDC silently disabled if any of OIDC_ISSUER_URL/OIDC_CLIENT_ID/OIDC_CLIENT_SECRET unset (D-02)
    - genericOAuth `config[0]` follows the 02-RESEARCH-AUTH.md shape (providerId: "oidc", discoveryUrl built from issuer)

    Create `apps/api/src/lib/default-tenant.ts` exporting `resolveDefaultTenantId(): Promise<string>` that SELECTs the row from `tenants` where `slug = 'default'` (Phase 1 seeded this) and returns its UUID. Memoize after first lookup.

    Per AUTH-A1: this task does NOT verify genericOAuth.onSuccess redirectTo rewriting — that is Plan 05's concern. Document in code comment that the resolution belongs to Plan 05.

    Per D-02: when `OIDC_ISSUER_URL` is unset, `oidcProviders = []` and genericOAuth is NOT registered. Test via a smoke unit test: `OIDC_ISSUER_URL=undefined` → `auth.options.plugins.length === 1` (bearer only).

    No tests for the auth instance lifecycle in this task (that lands with Plan 03's wiring); a smoke test (`apps/api/src/auth.test.ts`) asserts the factory returns an object with the expected plugin count given env permutations.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api typecheck && pnpm --filter @openwhispr/api test --run src/auth.test.ts</automated>
  </verify>
  <done>`buildAuth({db, log})` returns a Better Auth instance; smoke test green for both env permutations (OIDC unset / set); pnpm install reproducibly resolves `better-auth@1.6.9`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migrations → DB DDL | Owner role applies DDL; bypass-RLS — must not be reachable through PgBouncer |
| Drizzle adapter → DB queries | RLS-subject `openwhispr_app`; cross-tenant query is denied at row level |
| `lookup_session_by_previous_token` | SECURITY DEFINER — runs as function owner; explicit GRANT EXECUTE controls callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | account/verification tables (cross-tenant leak via Better Auth queries) | mitigate | FORCE RLS + tenant_isolation policy on every new table; tools/lint-rls.ts blocks regression; Drizzle adapter bound to appDb (not ownerDb) per D-01 |
| T-02-01-02 | Elevation of Privilege | SECURITY DEFINER function lookup_session_by_previous_token | mitigate | REVOKE ALL FROM public; GRANT EXECUTE only to openwhispr_app; function returns only (user_id, tenant_id) tuples (no row data) |
| T-02-01-03 | Tampering | DDL via PgBouncer transaction-mode | mitigate | Migrate runner uses DATABASE_URL_OWNER (direct 5432, established Phase 1); Plan 02's migrate service explicitly bypasses pgbouncer |
| T-02-01-04 | Information Disclosure | Hand-rolled scheme regex bypass (e.g. `JavaScript:`) | mitigate | validateScheme rejects uppercase explicitly; deny-list checks lowercase form; unit tests cover case-bypass attempts |
</threat_model>

<verification>
- pnpm test (full root suite, including migration roundtrip) green
- tools/lint-rls.ts exits 0
- biome check + lint-english pass
- packages/data and apps/api typecheck both green
</verification>

<success_criteria>
- 0001 + 0002 migrations applied on a fresh Postgres yield: account, verification, oauth_state tables with FORCE RLS + tenant_isolation policy; users + sessions extended with the listed columns; lookup_session_by_previous_token function present with correct grants
- buildAuth() returns a working Better Auth instance with email+pwd always; OIDC conditional on env (D-02)
- All three pure-function libraries unit-tested with ≥95% line coverage
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-01-SUMMARY.md`. Document AUTH-A3 resolution (does Better Auth 1.6.9 stock support overlap natively? — finding goes here).
</output>
