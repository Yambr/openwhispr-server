---
phase: 02-auth-wire-api-skeleton-conformance-harness
plan: 07
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - docs/operations.md
  - docs/auth.md
  - docs/oidc-operator-config.md
  - docs/channel-scheme-override.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/01-core-infra-multi-tenant-data/deferred-items.md
autonomous: true
requirements: [PROVIDER-03, CONTRACT-01]
must_haves:
  truths:
    - "docs/auth.md documents email+password sign-in, OIDC plug-in, channel-scheme handling, dual auth (bearer+cookie), token rotation overlap, cookie-host scoping — operator can wire any IdP without reading source"
    - "docs/oidc-operator-config.md walks through env config (OIDC_ISSUER_URL/CLIENT_ID/CLIENT_SECRET) and validates against a Keycloak / Authentik / Google Workspace / Azure AD example each"
    - "docs/channel-scheme-override.md explains OPENWHISPR_PROTOCOL env override, the allow-list rules, the deny-list, why javascript:/data:/file:/uppercase rejected"
    - "docs/operations.md gains an Auth section: troubleshoot 401 cascades, cookie-host pitfalls, SMTP fallback behavior, mailpit dev profile usage"
    - "Phase 1 deferred-items.md SC#1 partial entry is DELETED (closed by Plan 02)"
    - "REQUIREMENTS.md status flips to Complete for AUTH-01..07, WIRE-01..04, WIRE-17..20, PROVIDER-03, PROVIDER-04, CONTRACT-01"
    - "ROADMAP.md Phase 2 plan list shows all 7 plans complete; phase progress 7/7; Coverage Map row for Phase 2 stays green"
    - "STATE.md updated: phase 2 complete; phase 3 next; current position table reflects 3/11 phases done"
    - "Final integration smoke (full quality gate): `pnpm test && make up && make contract-test && make down` runs end-to-end and exits 0"
  artifacts:
    - path: docs/auth.md
      provides: "Operator-facing auth documentation (DOCS-06 v1 starter; full version in Phase 10)"
      contains: "OIDC"
    - path: docs/oidc-operator-config.md
      provides: "Per-IdP env-config walkthroughs"
      contains: "OIDC_ISSUER_URL"
    - path: docs/channel-scheme-override.md
      provides: "Channel scheme allow-list rules + override path"
      contains: "OPENWHISPR_PROTOCOL"
  key_links:
    - from: docs/auth.md
      to: apps/api/src/auth.ts (Plan 01)
      via: "narrative refers to env vars consumed by buildAuth()"
      pattern: "BETTER_AUTH_SECRET"
    - from: docs/operations.md (Auth section)
      to: tests/self-tests/api-entrypoint-default-secrets.test.ts (Plan 02)
      via: "operator troubleshooting reference"
      pattern: "default-secrets"
---

<objective>
Land Phase 2's documentation surface (operator-facing), update planning state files (ROADMAP, REQUIREMENTS, STATE) to reflect completion, close the Phase 1 SC#1 partial entry, and run the full quality gate end-to-end as a final integration smoke.

Purpose: PROVIDER-03 (Identity provider interface) needs documented IdP plug-in steps to be considered satisfied; the planning state files must be authoritative; Phase 1's deferred-items.md must reflect that the SC#1 partial is now closed (by Plan 02); subsequent phases need a clean STATE/ROADMAP to consume.

Output: 4 doc files (3 new + 1 augmentation), 4 planning files updated, integration smoke green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-CONTEXT.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-WIRE.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-CONTAINER.md
@.planning/phases/01-core-infra-multi-tenant-data/deferred-items.md
@docs/operations.md
@docs/storage.md

<interfaces>
From all preceding plans:
- Plan 01: better-auth@1.6.9 wired; migrations 0001+0002 applied; allow-list/cookie-domain/token-rotation libs
- Plan 02: API container + compose services + Phase 1 SC#1 partial CLOSED
- Plan 03: 4 wire endpoints + zod schemas + setErrorHandler + dual auth + cookie-only middleware
- Plan 04: HTTPS-only + rate limit + email + AUTH-06 logs
- Plan 05: OAuth shim + callback + token rotation overlap + AUTH-07 cookie-host
- Plan 06: CONTRACT-01 conformance suite + GHA contract-test job

Phase 2 satisfies all 18 phase requirement IDs:
- WIRE-01..04 (4 endpoints) → Plan 03
- WIRE-17 (envelope) → Plan 03
- WIRE-18 (401 not 200) → Plan 03
- WIRE-19 (x-openwhispr-source) → Plan 03 + Plan 04 test
- WIRE-20 (HTTPS-only) → Plan 04
- AUTH-01 (email+pwd) → Plan 01 + Plan 03 integration
- AUTH-02 (channel-scheme echo) → Plan 05
- AUTH-03 (≥30-day bearer + dual auth) → Plan 01 + Plan 03 + Plan 05
- AUTH-04 (rotation overlap) → Plan 01 lib + Plan 05 wiring
- AUTH-05 (OIDC pluggable) → Plan 01 (silently disabled if env unset)
- AUTH-06 (x-openwhispr-source) → Plan 03 plugin + Plan 04 log assertion
- AUTH-07 (cookie scoping) → Plan 01 lib + Plan 05 wiring + Plan 06 test
- PROVIDER-03 (IdP interface) → THIS PLAN documents the operator path
- PROVIDER-04 (SMTP) → Plan 04
- CONTRACT-01 (conformance suite) → Plan 06 + this plan finalizes traceability
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 documentation files (auth.md, oidc-operator-config.md, channel-scheme-override.md, operations.md Auth section)</name>
  <files>docs/auth.md, docs/oidc-operator-config.md, docs/channel-scheme-override.md, docs/operations.md</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-RESEARCH-AUTH.md (entire — most content here can be repurposed for operator docs)
    - apps/api/src/auth.ts (final shape after Plans 01+05)
    - apps/api/src/lib/scheme-allowlist.ts (allow-list rules)
    - docs/operations.md (existing Phase 1 storage section — append, do not rewrite)
  </read_first>
  <action>
    Create `docs/auth.md` (operator-facing; DOCS-06 starter; full version in Phase 10):
    - § Overview: Better Auth 1.6.9, email+password first-class, OIDC pluggable, opaque bearer tokens
    - § Quick Start (email+password only): set BETTER_AUTH_SECRET, AUTH_URL, OPENWHISPR_API_URL → done
    - § Plugging an OIDC Provider: env vars, silent-disable behavior, link to oidc-operator-config.md
    - § Sign-in Flow (desktop): /api/desktop-signin/{provider} → IdP → /api/auth/callback → channel-scheme redirect
    - § Token Lifecycle: ≥30-day TTL, set-auth-token rotation, ≥5-min overlap (AUTH-04 explanation)
    - § Dual Auth: when bearer is required vs cookie-only endpoints
    - § Cookie Host Scoping: single-host vs split-host (eTLD+1) vs unsupported topology
    - § Troubleshooting: 401 cascade → check rotation overlap; cookie not reaching API → check eTLD+1 config; OIDC silent-disable → check all 3 env vars set

    Create `docs/oidc-operator-config.md` with worked examples:
    - Generic OIDC (env vars + .well-known URL discovery)
    - Keycloak / Authentik (realm URL pattern)
    - Google Workspace (Admin Console steps + OAuth credentials)
    - Azure AD (App Registration steps)
    - Okta (custom auth server)
    Each section: env var values + dashboard config steps + verification command (`curl ${OIDC_ISSUER_URL}/.well-known/openid-configuration | jq`).

    Create `docs/channel-scheme-override.md`:
    - Why channel scheme matters (desktop dev/staging/prod builds)
    - Built-in allow-list: openwhispr, openwhispr-dev, openwhispr-staging
    - OPENWHISPR_PROTOCOL env override (single additional scheme)
    - Allow-list rules: RFC 3986 grammar, 32-char cap, lowercase only
    - Deny-list: javascript, data, file, vbscript, about, chrome, chrome-extension, ms-* prefix
    - Reject behavior: 400 with `{error:"invalid callback scheme"}` — never 302 to a rejected scheme
    - Examples: valid mycorp-whispr override; invalid JavaScript: (uppercase) → 400

    Append to `docs/operations.md` a new top-level § Auth section linking to the three new docs and covering:
    - Configuring SMTP (with mailpit dev fallback explained)
    - Rotating BETTER_AUTH_SECRET (revokes all sessions; document the operator runbook)
    - Default-secrets entrypoint check (links to apps/api/scripts/check-default-secrets.ts and the deny-list file)
    - Troubleshooting common 401 patterns

    All files English-only (constitutional rule); pass `tools/lint-english.ts`. No code blocks reference unimplemented features.
  </action>
  <verify>
    <automated>pnpm tsx tools/lint-english.ts docs/auth.md docs/oidc-operator-config.md docs/channel-scheme-override.md docs/operations.md && test -s docs/auth.md && test -s docs/oidc-operator-config.md && test -s docs/channel-scheme-override.md && grep -q "## Auth" docs/operations.md</automated>
  </verify>
  <done>4 doc files committed; English-only lint passes; operations.md has new Auth section; cross-references between the docs work.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning state files (REQUIREMENTS + ROADMAP + STATE) and close Phase 1 deferred entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-core-infra-multi-tenant-data/deferred-items.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md § Traceability table (rows for AUTH-01..07, WIRE-01..04+17..20, PROVIDER-03, PROVIDER-04, CONTRACT-01)
    - .planning/ROADMAP.md § Phase 2 (Plans: TBD currently); § Progress Table; § Coverage Map (already correct)
    - .planning/STATE.md (Current Position, Performance Metrics rows for Phase 02)
    - .planning/phases/01-core-infra-multi-tenant-data/deferred-items.md (the SC#1 partial entry)
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md`:
    - For each of AUTH-01..07, WIRE-01..04, WIRE-17..20, PROVIDER-03, PROVIDER-04, CONTRACT-01: change Status from `Pending` to `Complete` in BOTH the per-section list (use `[x]`) and the Traceability table (last column)
    - Add a new § "Phase-Level Plan Traceability" subsection for Phase 2 (mirror Phase 1's format) listing each requirement → plan(s) → primary artifacts. Reference: WIRE-01 → Plan 03 (apps/api/src/routes/check-user.ts, packages/contract-tests/src/check-user.test.ts), etc.

    Update `.planning/ROADMAP.md`:
    - § Phase 2 → replace `**Plans**: TBD` with full plan list (7 plans across 4 waves):
      - 02-01-PLAN.md — Better Auth wiring + migrations + libs (Wave 1)
      - 02-02-PLAN.md — API container + compose + migrate one-shot (Wave 1)
      - 02-03-PLAN.md — Wire endpoints + envelope + dual auth (Wave 2)
      - 02-04-PLAN.md — HTTPS-only + rate limit + email (Wave 2)
      - 02-05-PLAN.md — OAuth shim + callback + token rotation (Wave 3)
      - 02-06-PLAN.md — CONTRACT-01 conformance suite + GHA (Wave 3)
      - 02-07-PLAN.md — Docs + finalization (Wave 4)
    - § Progress Table → Phase 2 row: 7/7 Complete with completion date
    - List checkbox each plan as [x]

    Update `.planning/STATE.md`:
    - Top frontmatter: `completed_phases: 3` (was 2); `total_plans: 19` (12 + 7); `completed_plans: 19`
    - Current Position table: Phase 2 COMPLETE; Plan: 02-07 complete; Phase progress: 3/11 phases complete; 19/19 plans complete (100%)
    - Progress bar `[X][X][X][ ][ ][ ][ ][ ][ ][ ][ ]`
    - Append the 7 new plan rows to Performance Metrics table (placeholder values; exec-time will fill)
    - Accumulated Context: add 8 new Key Decisions Logged from Phase 2 (Better Auth 1.6.9 pin, custom previous_token_hash machinery for AUTH-04 overlap, channel-scheme allow-list grammar, eTLD+1 cookie-domain logic, single zod source of truth, centralized setErrorHandler, contract-test as required GHA check, fixture-idp under contract-test compose profile)
    - Open Todos: remove "Decide token TTL + rotation overlap policy in Phase 2" (decided)
    - Session Continuity: Next entry point `/gsd-plan-phase 3`; Last session stopped at "Completed 02-07-PLAN.md (docs + finalization). Phase 2 closed."

    Update `.planning/phases/01-core-infra-multi-tenant-data/deferred-items.md`:
    - DELETE the "## SC#1 partial: API entrypoint defense-in-depth orphaned" section in its entirety (closed by Plan 02 of Phase 2; Plan 02's SUMMARY.md authoritatively records the closure)
    - Leave the biome-warnings section if still relevant; if Phase 2 work already cleaned them up via `pnpm exec biome check --write --unsafe`, delete that section too
    - If both sections gone, replace the file with a minimal "All Phase 1 deferred items resolved as of Phase 2 completion (2026-05-09)" stub
  </action>
  <verify>
    <automated>grep -q "AUTH-01.*Phase 2.*Complete" .planning/REQUIREMENTS.md && grep -q "02-07-PLAN.md" .planning/ROADMAP.md && grep -q "completed_phases: 3" .planning/STATE.md && ! grep -q "SC#1 partial" .planning/phases/01-core-infra-multi-tenant-data/deferred-items.md</automated>
  </verify>
  <done>All four planning files updated; SC#1 partial removed; Phase 2 status reflected as Complete; ROADMAP plan list authoritative.</done>
</task>

<task type="auto">
  <name>Task 3: Final integration smoke (full quality gate end-to-end) + Phase 2 SUMMARY.md</name>
  <files>.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-07-SUMMARY.md</files>
  <read_first>
    - All 02-0X-SUMMARY.md files from Plans 01-06 (read each; aggregate findings)
    - Makefile (existing targets)
  </read_first>
  <action>
    Run the full quality gate locally (or in a Docker-enabled CI session):
    1. `pnpm install --frozen-lockfile`
    2. `pnpm test` (unit + integration; expects all green)
    3. `pnpm exec biome check .` (clean)
    4. `pnpm tsx tools/lint-english.ts` (clean)
    5. `pnpm tsx tools/lint-rls.ts` (clean)
    6. `pnpm typecheck` (clean across workspace)
    7. `docker compose --profile default --profile contract-test up -d --wait`
    8. `pnpm -F @openwhispr/data run seed:conformance`
    9. `make contract-test` (8 conformance tests green)
    10. `docker compose down -v`

    Capture metrics: total runtime of each step, peak resource usage, any flake. Document in 02-07-SUMMARY.md.

    Aggregate findings from all six prior SUMMARYs:
    - Plan 01 SUMMARY: AUTH-A3 resolution (Better Auth stock rotation behavior — was the previous_token_hash machinery actually needed?)
    - Plan 02 SUMMARY: CONTAINER-A1 (`pnpm --prod deploy`) + A2 (mailpit /livez) findings
    - Plan 03 SUMMARY: WIRE-Q1 (withTenant inside preHandler)
    - Plan 04 SUMMARY: any rate-limit or email surprises
    - Plan 05 SUMMARY: AUTH-A1 (genericOAuth onSuccess redirectTo) chosen path
    - Plan 06 SUMMARY: fixture-idp implementation choice and size

    02-07-SUMMARY.md sections:
    - Phase 2 Outcome (success criteria 1-7 from ROADMAP each marked PASS/PARTIAL/FAIL with evidence pointers)
    - Aggregated open-question resolutions
    - Integration smoke results (timing, any flakes, action taken)
    - Plan-by-plan summary digest (1 paragraph each)
    - What landed vs what's deferred:
      - Landed: all 18 requirement IDs
      - Deferred: real-IP CIDR allow-list refinement (Phase 6); rate-limit response shape standardization (settled in Plan 04 — note); per-tenant signup (Phase 5/6)
    - Next phase: Phase 3 (LiteLLM Integration + Bundled OSS Models) — link to .planning/ROADMAP.md § Phase 3

    Commit message: `docs(02-07): land Phase 2 docs + final integration smoke + planning state update`.
  </action>
  <verify>
    <automated>test -s .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-07-SUMMARY.md && grep -q "Phase 2 Outcome" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-07-SUMMARY.md</automated>
  </verify>
  <done>Quality gate steps 1-10 all green (or any failure root-caused, fixed, and re-run); 02-07-SUMMARY.md authored with aggregated findings; Phase 2 ready to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator reading docs → operator config decisions | Mis-documentation can cause production misconfig (e.g., wrong SMTP fallback understanding) |
| Planning state files → downstream phases | Stale REQUIREMENTS/ROADMAP/STATE leads to duplicated work in Phase 3+ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Information Disclosure | Doc snippets accidentally include real secrets | mitigate | Doc examples use placeholder values (REPLACE_ME, your-key-here); explicit warnings in operations.md against committing real .env |
| T-02-07-02 | Tampering | Phase 1 deferred-items.md SC#1 entry left stale → Phase 3 verifier blocks | mitigate | This plan deletes the entry; verification command grep asserts removal |
| T-02-07-03 | Spoofing | Stale plan count in ROADMAP/STATE → next phase gets wrong baseline | mitigate | Self-test in Phase 0 self-tests verifies plan count consistency between ROADMAP and per-phase directory file counts; runs in CI |
</threat_model>

<verification>
- All Task verifies green
- Full integration smoke (Task 3 steps 1-10) green
- pnpm test full green
- contract-test green against compose-up
- ROADMAP/REQUIREMENTS/STATE all reflect Phase 2 completion
- Phase 1 deferred-items.md entries cleaned up (SC#1 partial gone)
</verification>

<success_criteria>
- All 18 Phase 2 requirement IDs (WIRE-01..04, WIRE-17..20, AUTH-01..07, PROVIDER-03, PROVIDER-04, CONTRACT-01) marked Complete in REQUIREMENTS.md
- Phase 2 success criteria 1-7 from ROADMAP all PASS with evidence in 02-07-SUMMARY.md
- 4 doc files committed and discoverable
- Phase 1 SC#1 partial gap → CLOSED
- Phase 3 ready to plan: STATE.md next-entry-point set to `/gsd-plan-phase 3`
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-07-SUMMARY.md` with the aggregated findings per Task 3 action.
</output>
