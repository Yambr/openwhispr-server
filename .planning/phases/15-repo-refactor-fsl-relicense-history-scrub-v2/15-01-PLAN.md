---
phase: 15-repo-refactor-fsl-relicense-history-scrub-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/migrate-tests.ts
  - tools/migrate-tests.test.ts
  - docs/conventions.md
  - .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md
  - eslint-rules/no-colocated-tests.ts
  - eslint-rules/no-colocated-tests.test.ts
  - package.json
autonomous: true
requirements:
  - STRUCT-01
must_haves:
  truths:
    - "`docs/conventions.md` contains a `## Test layout` section codifying `apps/<app>/tests/{unit,integration}/` and `packages/<pkg>/tests/unit/` as canonical, declaring root `tests/e2e-cjm/` + `tests/e2e/` + `tests/conformance/` + `tests/infra/` exempt, and explicitly exempting `tools/load-test/`."
    - "`tools/migrate-tests.ts` exists, is ts-morph based (NOT regex), exposes `--dry-run`, `--apply`, and `--inventory` modes via a `tsx`-runnable CLI shape mirroring `tools/spdx-header.ts`."
    - "`tools/migrate-tests.test.ts` covers path computation, ts-morph import rewriter, idempotency on re-run, exempt-path filtering — authored RED→GREEN with coverage ≥ 90/90/90/90 on the codemod surface."
    - "`Phase15-MOVE-INVENTORY.md` exists in the phase dir and was generated by `tools/migrate-tests.ts --dry-run --inventory`; the file enumerates every source→target path pair the codemod will execute in 15-02."
    - "A custom ESLint rule `no-colocated-tests` is wired into the repo's discovered ESLint config at error severity; allow-list exempts `tools/load-test/` and the root `tests/` directory."
    - "ts-morph is added as a root devDependency; `pnpm install` is clean (no peer warnings introduced)."
  artifacts:
    - path: "tools/migrate-tests.ts"
      provides: "ts-morph based test-layout codemod CLI; --dry-run prints inventory, --inventory writes to PATH, --apply executes moves + rewrites imports."
      contains: "Project, SourceFile, getImportDeclarations, moveToDirectory"
    - path: "tools/migrate-tests.test.ts"
      provides: "Unit coverage for path mapping + import-rewrite + exempt-path filter + idempotency."
    - path: "eslint-rules/no-colocated-tests.ts"
      provides: "ESLint rule preventing future `*.test.ts` siblings of source files outside the exemption allow-list."
    - path: "eslint-rules/no-colocated-tests.test.ts"
      provides: "Rule tester for no-colocated-tests with positive + negative cases."
    - path: "docs/conventions.md"
      provides: "Test-layout convention section + exemption list."
      contains: "## Test layout"
    - path: ".planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md"
      provides: "Generated source→target table consumed by 15-02 codemod run."
  key_links:
    - from: "tools/migrate-tests.ts"
      to: "tools/spdx-header.ts"
      via: "shared CLI shape (audit/fix → dry-run/apply); copy invocation-guard pattern"
      pattern: "invokedDirect|process\\.argv"
    - from: "eslint-rules/no-colocated-tests.ts"
      to: "discovered ESLint config (root or workspace)"
      via: "rule registration in `plugins` + `rules` block"
      pattern: "no-colocated-tests"
    - from: "docs/conventions.md"
      to: "tools/migrate-tests.ts target paths"
      via: "documented layout matches codemod's path-mapper output"
---

<objective>
Establish the test-layout convention + tooling required to execute the Phase 15
structural reorg without ad-hoc decisions inside 15-02. This is pure pre-flight:
no `git mv` happens here. By the end of this plan the repo MUST own (a) the
codified convention, (b) a tested ts-morph codemod that knows how to perform the
move, (c) a generated MOVE-INVENTORY committed as a tracked artifact, and (d) an
ESLint rule that prevents regression.

Purpose: 15-02 is a single atomic-commit big-bang. If the codemod is authored
inline in 15-02 the diff becomes unreadable and the inventory cannot be reviewed
before execution. Splitting the inventory out lets reviewers approve the move
table BEFORE any file shifts on disk.

Output: A ts-morph CLI, its test suite, a committed inventory artifact, a
convention doc section, and a lint rule blocking future co-location.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-CONTEXT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-RESEARCH-test-layout.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-PATTERNS.md

@tools/spdx-header.ts
@tools/lint-tdd.ts
@docs/conventions.md
@vitest.config.ts
@apps/api/vitest.config.ts
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 0: Discover ESLint config location + ts-morph dependency state</name>
  <files>
    - (read-only scouting; output committed in Task 1)
  </files>
  <action>
    Discover before authoring:
    1. `ls -la eslint.config.{js,mjs,ts,cjs} .eslintrc* 2>/dev/null` at root + every workspace.
    2. `grep -R "eslint" package.json apps/*/package.json packages/*/package.json | grep -v node_modules` to find which workspace owns lint config.
    3. `pnpm why ts-morph 2>/dev/null` — confirm ts-morph is NOT yet installed.
    4. `pnpm why @typescript-eslint/utils 2>/dev/null` — confirm rule-tester dependency state.
    Capture findings as an inline note at the top of Task 1's commit body (NOT a separate doc). If NO ESLint config file is found anywhere, pivot: the rule is authored as a `tools/lint-colocated-tests.ts` standalone CLI (mirroring `tools/lint-tdd.ts`) instead of an ESLint plugin, and the must_have's "wired into ESLint config" truth is reinterpreted as "wired into `pnpm lint` script chain at error severity". Document the pivot in `docs/conventions.md` Task 4 prose.
  </action>
  <verify>
    <automated>ls eslint.config.* 2>/dev/null; pnpm why ts-morph 2>/dev/null; pnpm why @typescript-eslint/utils 2>/dev/null; true</automated>
  </verify>
  <done>Pivot decision recorded: ESLint plugin path OR `tools/lint-colocated-tests.ts` standalone CLI path. Subsequent tasks branch on this outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: RED — author migrate-tests codemod test suite</name>
  <files>
    - tools/migrate-tests.test.ts (new)
  </files>
  <behavior>
    - Test 1: `computeTargetPath("apps/api/src/lib/foo.test.ts")` returns `"apps/api/tests/unit/lib/foo.test.ts"`.
    - Test 2: `computeTargetPath("apps/api/src/routes/__tests__/health.test.ts")` returns `"apps/api/tests/unit/routes/__tests__/health.test.ts"` (preserves `__tests__` harness dir).
    - Test 3: `computeTargetPath("packages/byok-guard/src/__tests__/guard.test.ts")` returns `"packages/byok-guard/tests/unit/__tests__/guard.test.ts"`.
    - Test 4: `computeTargetPath("tools/load-test/src/scenario.test.ts")` returns `null` (exempt — load-test out-of-scope per CONTEXT).
    - Test 5: `computeTargetPath("tests/e2e-cjm/foo.test.ts")` returns `null` (root tests dir exempt).
    - Test 6: Rewriting a test file's relative imports: given a test at `apps/api/src/lib/foo.test.ts` with `import {x} from "./foo"`, after move to `apps/api/tests/unit/lib/foo.test.ts` the import becomes `import {x} from "../../../src/lib/foo"`. Use ts-morph `Project` against an in-memory FS fixture (no real filesystem writes).
    - Test 7: Idempotency — running the codemod twice on the same already-moved file produces no diff and no error.
    - Test 8: Dry-run mode writes no files; returns the planned-move table.
    - Test 9: Inventory mode writes the planned-move table to the path passed via `--inventory` flag.
    - Test 10: Apply mode invokes `moveToDirectory()` on each source file (asserted via ts-morph project introspection in fixture).
  </behavior>
  <action>
    Create `tools/migrate-tests.test.ts` using Vitest. Use ts-morph `Project` with `useInMemoryFileSystem: true` so tests touch ZERO real files. Import the codemod's exported functions (`computeTargetPath`, `rewriteImports`, `planMoves`, `applyMoves`). Run the test; it MUST fail because `tools/migrate-tests.ts` does not yet exist. Commit as `test(15-01): RED migrate-tests codemod suite`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/migrate-tests.test.ts</automated>
  </verify>
  <done>10 assertions RED with "Cannot find module './migrate-tests'" or equivalent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement migrate-tests codemod + add ts-morph dep</name>
  <files>
    - tools/migrate-tests.ts (new)
    - package.json (root, devDependencies += ts-morph)
    - pnpm-lock.yaml
  </files>
  <behavior>
    All 10 assertions from Task 1 pass. Coverage on `tools/migrate-tests.ts` ≥ 90/90/90/90.
  </behavior>
  <action>
    Mirror `tools/spdx-header.ts` shape:
    1. SPDX line-1 header (Apache-2.0 — 15-03 flips to FSL).
    2. Export pure functions: `computeTargetPath(srcPath: string): string | null`, `rewriteImports(file: SourceFile, oldPath: string, newPath: string): void`, `planMoves(roots: string[]): Move[]`, `applyMoves(moves: Move[], opts: {dryRun: boolean, inventoryPath?: string}): Promise<void>`.
    3. Top-level `EXEMPT_PREFIXES = ["tools/load-test/", "tests/", "tools/test-probe/tests/"]` mirroring SPDX `SKIP_DIRS`.
    4. Path mapper: `apps/<app>/src/<rest>.test.ts` → `apps/<app>/tests/unit/<rest>.test.ts`; preserve `__tests__/` segments; same for `packages/<pkg>/src/...`.
    5. ts-morph `Project` instantiated with the workspace tsconfig; `moveToDirectory()` for moves; iterate `getImportDeclarations()` and rewrite `moduleSpecifier` to recompute relative paths via `path.relative()`.
    6. CLI guard at bottom: `if (invokedDirect) { ... parse argv for --dry-run|--apply|--inventory <path>; print JSON inventory on dry-run; exit 0/1 like spdx-header.ts }`.
    7. Add `ts-morph` to root `package.json` devDependencies; run `pnpm install` and commit `pnpm-lock.yaml`.
    Run tests until GREEN. Commit as `feat(15-01): migrate-tests ts-morph codemod`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/migrate-tests.test.ts --coverage</automated>
  </verify>
  <done>10 assertions GREEN; coverage ≥ 90/90/90/90 on tools/migrate-tests.ts; ts-morph resolvable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED+GREEN — no-colocated-tests lint rule (or CLI per Task 0 pivot)</name>
  <files>
    - eslint-rules/no-colocated-tests.ts (new) — OR tools/lint-colocated-tests.ts per Task 0 pivot
    - eslint-rules/no-colocated-tests.test.ts (new) — OR tools/lint-colocated-tests.test.ts
    - (lint config wire-up — location determined by Task 0)
  </files>
  <behavior>
    - RED Test 1: A `*.test.ts` placed at `apps/api/src/foo.test.ts` triggers the rule (positive).
    - RED Test 2: A `*.test.ts` placed at `apps/api/tests/unit/foo.test.ts` does NOT trigger (negative).
    - RED Test 3: A `*.test.ts` placed at `tools/load-test/src/foo.test.ts` does NOT trigger (exempt allow-list).
    - RED Test 4: A `*.test.ts` placed at `tests/e2e-cjm/foo.test.ts` does NOT trigger (root tests exempt).
    - GREEN: Implement rule using `@typescript-eslint/utils` `createRule` (ESLint path) OR pure CLI glob + path-prefix check (CLI pivot path).
  </behavior>
  <action>
    Path depends on Task 0:
    - ESLint plugin path: author `eslint-rules/no-colocated-tests.ts` exporting an ESLint rule; test via `RuleTester` from `@typescript-eslint/utils`; wire into discovered config with severity `error`; allow-list `["tools/load-test/**", "tests/**"]`.
    - CLI pivot path: author `tools/lint-colocated-tests.ts` mirroring `tools/lint-tdd.ts`; exit 1 on violations; wire into `pnpm lint` script chain in root `package.json`.
    RED commit: `test(15-01): RED no-colocated-tests rule`.
    GREEN commit: `feat(15-01): no-colocated-tests lint rule`.
  </action>
  <verify>
    <automated>pnpm vitest run eslint-rules/no-colocated-tests.test.ts || pnpm vitest run tools/lint-colocated-tests.test.ts</automated>
    Also: `pnpm lint` exits 0 on current tree (no false positives against the still-co-located legacy tests — temporarily add a TODO-allow-list entry referencing 15-02 if needed; remove entry as last step of 15-02).
  </verify>
  <done>4 assertions GREEN; coverage ≥ 90/90/90/90; rule wired into lint chain; current tree passes lint via documented temporary allow-list of legacy paths to be cleared in 15-02.</done>
</task>

<task type="auto">
  <name>Task 4: Author docs/conventions.md test-layout section</name>
  <files>
    - docs/conventions.md
  </files>
  <action>
    Append (or extend existing relevant section) a `## Test layout` section to `docs/conventions.md`. The section MUST state:
    1. Canonical layout: `apps/<app>/tests/{unit,integration}/<mirror-of-src>.test.ts`; `packages/<pkg>/tests/unit/<...>.test.ts`.
    2. Root-level exempt directories: `tests/e2e/`, `tests/e2e-cjm/`, `tests/conformance/`, `tests/infra/`.
    3. Tooling exempt: `tools/load-test/` (rationale: dev-tooling, not an app/library; revisit deferred-items #4).
    4. Enforcement: ESLint rule `no-colocated-tests` (or `tools/lint-colocated-tests.ts` per Task 0 pivot) at error severity.
    5. Migration tooling: `tsx tools/migrate-tests.ts [--dry-run|--apply|--inventory <path>]`.
    6. Cross-reference: link to `Phase15-MOVE-INVENTORY.md` for the one-time migration record (relative path from `docs/`).
    Commit as `docs(15-01): codify test-layout convention`.
  </action>
  <verify>
    <automated>grep -q '^## Test layout' docs/conventions.md && grep -q 'tools/load-test' docs/conventions.md && grep -q 'migrate-tests' docs/conventions.md</automated>
  </verify>
  <done>All four grep gates pass.</done>
</task>

<task type="auto">
  <name>Task 5: Generate Phase15-MOVE-INVENTORY.md via dry-run codemod</name>
  <files>
    - .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md
  </files>
  <action>
    Run: `tsx tools/migrate-tests.ts --dry-run --inventory .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md`.
    The inventory file MUST be a markdown table with columns: `Source | Target | Workspace | Notes`. The codemod writes this format directly (implemented in Task 2). Spot-check that every co-located test from the PATTERNS.md inventory appears (apps/api/src/**/*.test.ts, apps/web/src/**/*.test.ts, packages/data, packages/wire-schemas, packages/byok-guard, packages/email, packages/contract-tests, apps/worker/src/i18n/__tests__).
    Confirm the file lists ZERO entries under `tools/load-test/`, `tests/e2e/`, `tests/e2e-cjm/`, `tests/conformance/`, `tests/infra/`.
    Commit as `docs(15-01): generate Phase15-MOVE-INVENTORY`.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md && ! grep -E '^\| tools/load-test/' .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md && grep -c '\.test\.ts' .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md | awk '$1 > 30 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Inventory exists, contains > 30 test-file entries, contains no exempt-prefix entries.</done>
</task>

</tasks>

<test_strategy>
- Unit (Vitest, in-memory FS via ts-morph): `tools/migrate-tests.test.ts` (10 cases), `eslint-rules/no-colocated-tests.test.ts` (4 cases) — coverage gate 90/90/90/90 on diff.
- Integration: dry-run codemod against real tree produces a non-empty inventory file (Task 5).
- E2E: N/A — this plan does not touch user-visible routes. The phase-level E2E lives in 15-02 (traefik-host-split.feature).
</test_strategy>

<deviation_handling>
- If Task 0 finds NO ESLint config file anywhere → pivot to standalone CLI per Task 3 prose; record pivot in conventions.md.
- If ts-morph install introduces peer warnings from another workspace → file an issue but do NOT downgrade; CLAUDE.md "no workarounds" applies.
- If the dry-run inventory produces fewer than ~30 entries → the path-mapper has a bug; do NOT proceed to 15-02 until Task 1 RED suite is extended to cover the miss.
- If a co-located test legitimately needs to stay (e.g. a `__tests__/` harness shared between two source files) → document as an exemption in conventions.md AND skip in the codemod via an allow-list comment; flag for orchestrator review.
</deviation_handling>

<success_criteria>
1. `pnpm vitest run tools/migrate-tests.test.ts` — 10 GREEN.
2. `pnpm vitest run eslint-rules/no-colocated-tests.test.ts` (or CLI sibling) — 4 GREEN.
3. `Phase15-MOVE-INVENTORY.md` exists and lists > 30 test files, none in the exempt prefixes.
4. `docs/conventions.md` has a `## Test layout` section linking to the inventory + the codemod.
5. `pnpm lint` exits 0 on current tree.
6. Coverage ≥ 90/90/90/90 on diff (`tools/migrate-tests.ts` + lint rule).
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-01-SUMMARY.md` per get-shit-done summary template: list the ESLint vs CLI pivot outcome from Task 0, the count of inventory rows, the temporary lint allow-list entries to be removed in 15-02, and the ts-morph version pinned in package.json.
</output>
