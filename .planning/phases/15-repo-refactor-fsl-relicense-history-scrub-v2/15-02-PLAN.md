---
phase: 15-repo-refactor-fsl-relicense-history-scrub-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - docker-compose.embedded-litellm.yml
  - docker-compose.load-test.yml
  - docker-compose.load-test.realistic.yml
  - compose/docker-compose.embedded-litellm.yml
  - compose/docker-compose.load-test.yml
  - compose/docker-compose.load-test.realistic.yml
  - compose/traefik/dynamic.dev.yml
  - Makefile
  - .github/workflows/conformance-axe.yml
  - .github/workflows/e2e-cjm.yml
  - docs/operations.md
  - docs/conventions.md
  - apps/api/src/auth.ts
  - .env.example
  - .env.slim.example
  - tests/e2e-cjm/playwright.config.ts
  - tests/e2e-cjm/features/traefik-host-split.feature
  - tests/e2e-cjm/features/step_definitions/host-split.steps.ts
  - vitest.config.ts
  - apps/api/vitest.config.ts
  - apps/web/vitest.config.ts
  - apps/worker/vitest.config.ts
  - packages/data/vitest.config.ts
  - packages/email/vitest.config.ts
  - packages/litellm-client/vitest.config.ts
  - packages/byok-guard/vitest.config.ts
  - packages/contract-tests/vitest.config.ts
  - tools/test-probe/vitest.config.ts
  - compose/mock-litellm/vitest.config.ts
  - tests/e2e/vitest.config.ts
  - apps/web/public/.gitkeep
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-04
  - STRUCT-05
  - STRUCT-06
  - STRUCT-07
must_haves:
  truths:
    - "All remaining root compose files (`docker-compose.embedded-litellm.yml`, `docker-compose.load-test.yml`, `docker-compose.load-test.realistic.yml`) live in `compose/`; base `docker-compose.yml` remains at root."
    - "Every reference (Makefile, GitHub Actions workflows, `docs/operations.md`) is updated to the new paths; grep at repo root finds zero orphan references to the old root-level overlay paths."
    - "`compose/traefik/dynamic.dev.yml` defines a `web@docker` router on `Host(\\`web.localhost\\`)` → `web:3001` AND retains/declares the `api@docker` router on `Host(\\`api.localhost\\`)` → `api:3000`."
    - "Better Auth `trustedOrigins` (apps/api/src/auth.ts + .env examples) lists both `https://web.localhost` and `https://api.localhost` (or http forms for non-TLS dev) via the existing env-driven `AUTH_TRUSTED_ORIGINS_EXTRA` chain."
    - "`tests/e2e-cjm/playwright.config.ts` `baseURL` is `https://web.localhost` (or http for dev); every Gherkin step regex referencing the prior `app.localhost` literal is updated to `web.localhost`."
    - "A new Gherkin scenario `@cjm-traefik-host-split` (RED→GREEN) asserts `GET /api/locale` against `api.localhost` returns localized JSON (not 404), closing TD-15.g."
    - "`tools/migrate-tests.ts --apply` was executed; every co-located `*.test.ts` listed in `Phase15-MOVE-INVENTORY.md` has moved to its target path; ts-morph rewrote relative imports; the full test suite is GREEN post-move."
    - "Every workspace `vitest.config.ts` `include` is `['tests/**/*.test.ts']`; root `vitest.config.ts` uses Vitest 3.2+ `projects` config (NOT deprecated `workspace`)."
    - "Route-group audit recorded in `docs/conventions.md`: either `(public)/(authed)/(admin)` convention documented as-is OR vestigial groups removed; `(auth)` vs `(authed)` naming disambiguated."
    - "`apps/web/public/.gitkeep` exists and is committed."
    - "`pnpm vitest run` passes across every workspace; e2e-cjm Playwright + Cucumber suites pass; `pnpm lint` passes (the temporary legacy-tests allow-list entries from 15-01 are removed)."
  artifacts:
    - path: "compose/docker-compose.embedded-litellm.yml"
      provides: "Embedded LiteLLM overlay co-located with the other 6 overlays from Phase 14."
    - path: "compose/docker-compose.load-test.yml"
      provides: "Load-test overlay relocated."
    - path: "compose/docker-compose.load-test.realistic.yml"
      provides: "Realistic-profile load-test overlay relocated."
    - path: "compose/traefik/dynamic.dev.yml"
      provides: "Dev-profile Traefik dynamic config implementing the host split (web.localhost + api.localhost)."
    - path: "tests/e2e-cjm/features/traefik-host-split.feature"
      provides: "Gherkin scenario closing TD-15.g."
    - path: "apps/web/public/.gitkeep"
      provides: "Tracks the otherwise-empty public dir (STRUCT-06)."
    - path: "vitest.config.ts"
      provides: "Root Vitest config migrated to `projects` array."
  key_links:
    - from: "compose/traefik/dynamic.dev.yml (web router)"
      to: "tests/e2e-cjm/playwright.config.ts baseURL"
      via: "Host literal `web.localhost` declared in router, consumed by Playwright"
      pattern: "web\\.localhost"
    - from: "apps/api/src/auth.ts (trustedOrigins)"
      to: ".env.example AUTH_TRUSTED_ORIGINS_EXTRA"
      via: "comma-separated env consumed at server boot"
      pattern: "AUTH_TRUSTED_ORIGINS_EXTRA"
    - from: "tools/migrate-tests.ts --apply (from 15-01)"
      to: "every workspace tests/unit/ tree"
      via: "ts-morph moveToDirectory + import rewrite"
      pattern: "tests/unit/"
    - from: "every workspace vitest.config.ts include"
      to: "tests/**/*.test.ts"
      via: "post-move config update"
      pattern: "tests/\\*\\*/\\*\\.test\\.ts"
---

<objective>
Execute the structural reorganization in one atomic plan: relocate the last three
root-level compose files, close TD-15.g by formalizing the Traefik host split
(web.localhost vs api.localhost) with a new Gherkin oracle, run the 15-01
codemod with `--apply` to move every co-located test file and rewrite its
imports, switch every Vitest config to the new include + migrate root to the
Vitest 3.2+ `projects` config, audit route-groups, and add the `apps/web/public/.gitkeep`
sentinel.

Purpose: This is the only plan in Phase 15 that physically rearranges files. By
the end, the repo structure matches ROADMAP success criteria for STRUCT-01,
STRUCT-02, STRUCT-04, STRUCT-05, STRUCT-06, STRUCT-07. The plan is sequenced
internally so that each concern lands as its own RED→GREEN→commit pair, but the
overall plan is a single executor wave (no parallelism — file conflicts and
shared `Phase15-MOVE-INVENTORY.md` consumption forbid it).

Output: Reorganized repo, host-split Traefik, host-split Better Auth origins,
host-split Playwright baseURL, host-split Gherkin coverage, moved tests with
GREEN suite, updated Vitest projects config, route-group audit doc, gitkeep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-CONTEXT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-PATTERNS.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-01-PLAN.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/Phase15-MOVE-INVENTORY.md

@docker-compose.embedded-litellm.yml
@docker-compose.load-test.yml
@docker-compose.load-test.realistic.yml
@compose/traefik/dynamic.yml
@apps/api/src/auth.ts
@tests/e2e-cjm/playwright.config.ts
@Makefile
@.github/workflows/e2e-cjm.yml
@.github/workflows/conformance-axe.yml
@vitest.config.ts
@apps/api/vitest.config.ts
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Gherkin scenario for Traefik host split (TD-15.g closure)</name>
  <files>
    - tests/e2e-cjm/features/traefik-host-split.feature (new)
    - tests/e2e-cjm/features/step_definitions/host-split.steps.ts (new or extended)
  </files>
  <behavior>
    - Scenario `@cjm-traefik-host-split`: Given the dev compose stack is up with the ingress overlay, When the client GETs `https://api.localhost/api/locale` with `Accept-Language: ru`, Then the response is HTTP 200 with `content-type: application/json` and the body parses to a localized payload (NOT 404, NOT routed to web:3001).
    - Scenario `@cjm-traefik-host-split-web`: Given the same stack, When the client GETs `https://web.localhost/`, Then the response is HTTP 200 with `content-type: text/html` and the body contains the web app shell marker (NOT routed to api:3000).
  </behavior>
  <action>
    Create the .feature file with both scenarios. Add step definitions in `step_definitions/host-split.steps.ts` using the existing Playwright `request` fixture pattern from prior Gherkin tests. Verify the test FAILS today (RED) because either (a) `web.localhost` is not declared in Traefik or (b) Playwright cannot resolve the host. Tag scenarios `@cjm @cjm-traefik-host-split`. Commit as `test(15-02): RED traefik host-split gherkin`.
  </action>
  <verify>
    <automated>cd tests/e2e-cjm && pnpm cucumber --tags '@cjm-traefik-host-split' || true</automated>
    Expected: FAIL with 404 or DNS/host error.
  </verify>
  <done>Both scenarios authored, executed, observably RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Traefik host split + Better Auth trustedOrigins + Playwright baseURL</name>
  <files>
    - compose/traefik/dynamic.dev.yml (new or extension of dynamic.yml)
    - apps/api/src/auth.ts (verify trustedOrigins env chain — likely doc-only edit)
    - .env.example
    - .env.slim.example
    - tests/e2e-cjm/playwright.config.ts
    - tests/e2e-cjm/features/*.feature (any app.localhost → web.localhost)
    - /etc/hosts wiring note (documented in compose comment, not code)
  </files>
  <action>
    1. Author `compose/traefik/dynamic.dev.yml` with two routers:
       - `api-router`: `rule: "Host(\`api.localhost\`)"`, `service: api`, `entryPoints: [web, websecure]`.
       - `web-router`: `rule: "Host(\`web.localhost\`)"`, `service: web`, `entryPoints: [web, websecure]`.
       Wire services with loadBalancer endpoints `api:3000` and `web:3001` mirroring the shape in `compose/traefik/dynamic.yml` (lines 1-138).
    2. Confirm `apps/api/src/auth.ts` lines 244-248 already concatenate `AUTH_TRUSTED_ORIGINS_EXTRA` — NO code change required. Update `.env.example` and `.env.slim.example`: set `AUTH_TRUSTED_ORIGINS_EXTRA=https://web.localhost,https://api.localhost,http://web.localhost,http://api.localhost`.
    3. `tests/e2e-cjm/playwright.config.ts`: change `baseURL` from `https://app.localhost` to `https://web.localhost` (or `http://` for non-TLS dev — match prior protocol).
    4. `grep -lR 'app\.localhost' tests/e2e-cjm/` → for every match, replace `app.localhost` with `web.localhost`. Same for any Cucumber step literal.
    5. Re-run the @cjm-traefik-host-split scenarios — they MUST go GREEN.
    Commit as `feat(15-02): traefik host split web/api.localhost`.
  </action>
  <verify>
    <automated>cd tests/e2e-cjm && pnpm cucumber --tags '@cjm-traefik-host-split'</automated>
    Also: `! grep -R 'app\.localhost' tests/e2e-cjm/ apps/ compose/ docs/ .env* 2>/dev/null`.
  </verify>
  <done>Both scenarios GREEN; no `app.localhost` literal remains; .env examples list both hosts.</done>
</task>

<task type="auto">
  <name>Task 3: Move remaining root compose files into `compose/` + patch every reference</name>
  <files>
    - compose/docker-compose.embedded-litellm.yml (moved)
    - compose/docker-compose.load-test.yml (moved)
    - compose/docker-compose.load-test.realistic.yml (moved)
    - Makefile
    - .github/workflows/e2e-cjm.yml
    - .github/workflows/conformance-axe.yml
    - docs/operations.md
    - tools/lint-compose-chart-parity.ts (only if path-list hard-coded — inspect first)
  </files>
  <action>
    1. `git mv docker-compose.embedded-litellm.yml compose/`, same for the two load-test overlays.
    2. `grep -RnE '(\./)?docker-compose\.(embedded-litellm|load-test|load-test\.realistic)\.yml' Makefile .github/ docs/ tools/ apps/ tests/ scripts/ 2>/dev/null` — for every hit, rewrite the path to `compose/...`.
    3. Inspect `tools/lint-compose-chart-parity.ts` — if it hard-codes compose paths, update the array; if it globs `compose/`, no change.
    4. Run `docker compose -f compose/docker-compose.embedded-litellm.yml config -q` to lint the moved file (still references same base).
    5. Run `make help` and exercise the relocated targets (`make up-embedded`, `make load-test`, `make load-test-realistic` — whatever the existing target names are) on a smoke path.
    Commit as `refactor(15-02): move embedded-litellm + load-test overlays into compose/`.
  </action>
  <verify>
    <automated>! grep -RnE '(^|[^/])docker-compose\.(embedded-litellm|load-test|load-test\.realistic)\.yml' Makefile .github/ docs/ tools/ apps/ tests/ scripts/ 2>/dev/null | grep -v '^compose/'</automated>
    Also: `docker compose -f compose/docker-compose.embedded-litellm.yml config -q`.
  </verify>
  <done>Three compose files relocated; zero orphan references; docker compose config -q passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Apply migrate-tests codemod + update vitest configs</name>
  <files>
    - (~100+ test files moved — exact list from Phase15-MOVE-INVENTORY.md)
    - vitest.config.ts (root — migrate to `projects`)
    - 13 workspace vitest.config.ts files (include → tests/**/*.test.ts)
    - (lint allow-list cleanup — remove temp entries from 15-01 Task 3)
  </files>
  <action>
    1. RED check: `pnpm vitest run` against current tree records the baseline pass/fail state — capture in commit body.
    2. Execute `tsx tools/migrate-tests.ts --apply` (uses Phase15-MOVE-INVENTORY.md as the move plan via 15-01's codemod).
    3. Update every workspace `vitest.config.ts` `test.include` to `["tests/**/*.test.ts"]`. Inventory of the 13 files in PATTERNS.md (note `tools/load-test/vitest.config.ts` is exempt — leave as-is).
    4. Migrate root `vitest.config.ts` from any deprecated `workspace` field to Vitest 3.2+ `projects: [...]` array per CONTEXT Q4.
    5. Remove the temporary legacy-paths allow-list entries that 15-01 Task 3 introduced in the no-colocated-tests rule/CLI (legacy paths no longer exist post-move).
    6. Run the full suite — it MUST be GREEN. If any test fails because ts-morph missed a dynamic-template import, fix at codemod level (extend 15-01 tests + re-run) — do NOT hand-patch the moved file.
    Commit as `refactor(15-02): apply migrate-tests codemod + switch to tests/ layout`.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm lint</automated>
    Also: `find apps packages -path '*/src/*' -name '*.test.ts' -not -path '*/tools/load-test/*' | wc -l` → 0 (no co-located tests remain outside exempt paths).
  </verify>
  <done>Full suite GREEN; zero co-located tests outside exempt paths; lint GREEN with no temp allow-list entries.</done>
</task>

<task type="auto">
  <name>Task 5: Route-group audit + .gitkeep + conventions update</name>
  <files>
    - docs/conventions.md
    - apps/web/public/.gitkeep (new)
    - apps/web/src/app/(authed)/... (only if renaming `(auth)` → `(authed)`)
  </files>
  <action>
    1. `ls apps/web/src/app/` — enumerate route groups (currently `(admin)/`, `(auth)/`, `(public)/`).
    2. Decision: per CONTEXT, document the `(public)/(authed)/(admin)` convention. The current `(auth)` naming is ambiguous (auth-namespace vs authed-user). Pick: rename `(auth)/` → `(authed)/` if it contains protected post-login routes, OR keep `(auth)/` if it contains login/signup flows AND introduce `(authed)/` separately if needed. Inspect the actual route content before deciding.
    3. If a rename happens: `git mv apps/web/src/app/\(auth\) apps/web/src/app/\(authed\)` AND grep for any literal path references (`/app/(auth)/` in tests, docs, middleware) and rewrite.
    4. Add `## Route groups` section to `docs/conventions.md` documenting the chosen convention + when to introduce a new group.
    5. `touch apps/web/public/.gitkeep` + `git add -f apps/web/public/.gitkeep` (force-add in case `.gitignore` excludes empty files).
    Commit as `refactor(15-02): route-group audit + public/.gitkeep`.
  </action>
  <verify>
    <automated>test -f apps/web/public/.gitkeep && git ls-files apps/web/public/.gitkeep | grep -q '.gitkeep' && grep -q '^## Route groups' docs/conventions.md</automated>
  </verify>
  <done>Route groups documented; `.gitkeep` tracked; any rename is grep-clean.</done>
</task>

</tasks>

<test_strategy>
- Unit: every existing Vitest suite still passes after the codemod (per-workspace `pnpm vitest run`); coverage on net-new code (`compose/traefik/dynamic.dev.yml` is config, not code — N/A for coverage).
- Integration: `docker compose -f compose/docker-compose.embedded-litellm.yml config -q`; Makefile targets exercised in Task 3.
- E2E: `@cjm-traefik-host-split` Gherkin scenarios — RED in Task 1, GREEN in Task 2. Full e2e-cjm suite re-run at end of plan.
</test_strategy>

<deviation_handling>
- If `apps/api/src/auth.ts` `trustedOrigins` env-chain does NOT include `AUTH_TRUSTED_ORIGINS_EXTRA` (PATTERNS.md says it does, lines 244-248) → make the minimal additive code change AND add a unit test for it; do NOT inline the host literals in auth.ts.
- If `tools/lint-compose-chart-parity.ts` hard-codes the moved compose paths → update path list; if the parity gate now fails because the compose-Chart invariant breaks during the move → halt; this is a real regression, not a workaround target.
- If a moved test file fails because ts-morph missed an import → root-cause at codemod level (extend 15-01 Task 1 RED suite + re-run); do NOT patch by hand.
- If `(auth)` route-group inspection reveals it is used for BOTH login flows and protected routes mixed → propose a split in conventions.md AND defer the rename to deferred-items + record TD-15.h; do not silently merge.
- If `pnpm lint` fails after the migrate-tests run because the temp allow-list still has entries → ensure they were removed in Task 4 step 5; do not suppress.
</deviation_handling>

<success_criteria>
1. Three compose files live in `compose/`; zero orphan path references.
2. `compose/traefik/dynamic.dev.yml` declares both `web.localhost` and `api.localhost` routers.
3. `@cjm-traefik-host-split` Gherkin scenarios GREEN.
4. `pnpm vitest run` GREEN across all workspaces post-move.
5. `find apps packages -path '*/src/*' -name '*.test.ts' -not -path '*/tools/load-test/*'` → 0 hits.
6. Root `vitest.config.ts` uses `projects: [...]` (not `workspace`).
7. `apps/web/public/.gitkeep` is git-tracked.
8. `docs/conventions.md` has `## Test layout` (from 15-01) AND `## Route groups` (Task 5) sections.
9. `pnpm lint` GREEN with no temporary allow-list entries.
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-02-SUMMARY.md` per get-shit-done template: list count of moved test files, count of vitest configs touched, route-group decision (rename or document-only), Makefile/CI files patched, and the final HEAD SHA (needed by 15-03 for SPDX sweep base).
</output>
