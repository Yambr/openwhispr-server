---
phase: 15-repo-refactor-fsl-relicense-history-scrub-v2
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - LICENSE
  - NOTICE
  - MIGRATING.md
  - README.md
  - CONTRIBUTING.md
  - REUSE.toml
  - .github/dco.yml
  - docs/adrs/0013-fsl-relicense.md
  - docs/adrs/0004-apache-2-0-licensing.md
  - .github/workflows/reuse-lint.yml
  - .github/workflows/chart-release.yml
  - .github/workflows/spdx.yml
  - .github/workflows/ci.yml
  - charts/openwhispr/Chart.yaml
  - charts/openwhispr/artifacthub-repo.yml
  - tools/spdx-header.ts
  - tools/__tests__/spdx-header.test.ts
  - (every workspace package.json — ~12 files)
  - (every Dockerfile under apps/, packages/, images/, tools/ — ~12 files)
  - (~675 source files — SPDX header rewrite via tools/spdx-header.ts --fix)
autonomous: true
requirements:
  - FSL-01
  - FSL-02
  - FSL-03
  - FSL-04
  - FSL-05
  - STRUCT-03
must_haves:
  truths:
    - "Root `LICENSE` file contains the FSL-1.1-ALv2 text sourced from https://fsl.software/ (SHA recorded in ADR-0013)."
    - "Annotated tag `pre-fsl-relicense-2026-05-14` exists locally and is pushed; its commit is the LAST Apache-2.0 commit."
    - "`MIGRATING.md` exists at repo root with the 7-day notice, a short pointer to ADR-0013, and a placeholder section for the post-scrub HEAD SHA (filled in 15-04)."
    - "`docs/adrs/0013-fsl-relicense.md` follows `docs/adrs/0000-template.md` shape; sets `**Status:** accepted`; supersedes ADR-0004; contains a Recovery section (the same content 15-04 will execute) and a Retroactive consent section linking to the contributor consent thread."
    - "`docs/adrs/0004-apache-2-0-licensing.md` is patched to `**Status:** superseded by ADR-0013`."
    - "`REUSE.toml` exists at repo root with `version = 1` and `[[annotations]]` blocks covering every SPDX-managed pattern (.ts, .tsx, .js, .mjs, .cjs, .sh, .py, .sql, .yaml, .yml, Dockerfile, *.md)."
    - "`reuse lint` exits 0 locally and as a CI job (new workflow `reuse-lint.yml` OR a job appended to `spdx.yml`)."
    - "Every workspace `package.json` declares `\"license\": \"FSL-1.1-ALv2\"` (BASELINE — previously absent everywhere)."
    - "Every Dockerfile in `apps/`, `images/`, `tools/`, `packages/` (~12 files) has `LABEL org.opencontainers.image.licenses=\"FSL-1.1-ALv2\"`."
    - "Root `README.md` shows a shields.io license badge for FSL-1.1-ALv2."
    - "`CONTRIBUTING.md` has a `## Developer Certificate of Origin (DCO)` section mandating `git commit --signoff` on every commit; references the retroactive consent issue."
    - "`.github/dco.yml` (or equivalent DCO bot config) exists with a placeholder `cutoff_sha` to be filled by 15-04 once the new HEAD is known."
    - "`.github/workflows/chart-release.yml` uses `helm/chart-releaser-action@v1.6.0` pinned by SHA, `charts_dir: charts/`, triggers ONLY on `chart-v*` tags, publishes to `gh-pages`."
    - "`charts/openwhispr/Chart.yaml` is bumped to `chart-v1.0.0` semver lane (independent of server `v*` tags); `artifacthub-repo.yml` carries required metadata (`repositoryID`, `owners`, `ignore`)."
    - "`tools/spdx-header.ts` is EXTENDED (not replaced): `HEADER` constant flips to FSL-1.1-ALv2; the codemod's `--fix` mode has rewritten every in-scope source file's line-1 SPDX header; coverage on the extension remains ≥ 90/90/90/90."
    - "The SPDX sweep is committed as ONE commit per directory area (apps/, packages/, tools/, scripts/, compose/, .github/, root) — NOT a single mega-commit."
    - "`pnpm vitest run`, `pnpm test:spdx-header`, `helm unittest charts/openwhispr`, `biome check`, `reuse lint` are ALL GREEN."
  artifacts:
    - path: "LICENSE"
      provides: "FSL-1.1-ALv2 license text."
    - path: "REUSE.toml"
      provides: "REUSE 3.3+ annotation source-of-truth."
    - path: "docs/adrs/0013-fsl-relicense.md"
      provides: "Decision record for the relicense, including recovery runbook + retroactive consent record."
    - path: "MIGRATING.md"
      provides: "Downstream-user migration guide with 7-day notice and post-scrub SHA placeholder."
    - path: ".github/workflows/chart-release.yml"
      provides: "Helm chart-releaser-action workflow on `chart-v*` tag prefix."
    - path: "charts/openwhispr/artifacthub-repo.yml"
      provides: "ArtifactHub repo metadata."
    - path: "tools/spdx-header.ts (extended)"
      provides: "Codemod retargeted to FSL-1.1-ALv2; sweeps all in-scope source files."
  key_links:
    - from: "tools/spdx-header.ts HEADER constant"
      to: "every source file line-1 SPDX comment"
      via: "--fix mode (codemod)"
      pattern: "SPDX-License-Identifier: FSL-1\\.1-ALv2"
    - from: "REUSE.toml [[annotations]]"
      to: "reuse-lint.yml CI gate"
      via: "reuse lint reads REUSE.toml + headers"
      pattern: "reuse lint"
    - from: ".github/workflows/chart-release.yml"
      to: "charts/openwhispr/Chart.yaml version chart-v*"
      via: "chart-releaser-action publishes to gh-pages"
      pattern: "chart-releaser-action"
    - from: "docs/adrs/0013-fsl-relicense.md"
      to: "MIGRATING.md + LICENSE + CONTRIBUTING.md DCO + .github/dco.yml"
      via: "ADR cross-references the operational artifacts"
      pattern: "ADR-0013"
---

<objective>
Execute the license migration from Apache-2.0 to FSL-1.1-ALv2 across every
surface (LICENSE, ~675 SPDX headers, every workspace `package.json` `license`
field, every Dockerfile license LABEL, README badge), author the ADR-0013
decision record and supersede ADR-0004, install REUSE 3.3+ tooling and a
`REUSE.toml` source-of-truth + `reuse lint` CI gate, mandate DCO in
`CONTRIBUTING.md` with a DCO bot config, and stand up the
`helm/chart-releaser-action`-based chart release workflow per the locked
STRUCT-03 decision (Option 3: monorepo + `chart-v*` tag prefix).

Purpose: This plan delivers FSL-01..05 + STRUCT-03 in their entirety. It is the
last plan that lands via normal PR review before 15-04's force-push event. By
doing the relicense codemod FIRST (this plan) and the history scrub SECOND
(15-04), contributors absorb one force-push, not two — matching the
Homebrew/Rails precedent locked in CONTEXT.

Output: A fully FSL-1.1-ALv2-licensed working tree with REUSE compliance,
DCO enforcement wired, and an operational Helm chart release pipeline on the
`chart-v*` tag namespace.

This plan does NOT execute any `git filter-repo` or force-push — those belong
to 15-04. This plan also does NOT fill in the DCO `cutoff_sha` — that becomes
known only after 15-04 force-pushes the rewritten history.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-CONTEXT.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-PATTERNS.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-RESEARCH-helm-location.md

@tools/spdx-header.ts
@tools/__tests__/spdx-header.test.ts
@docs/adrs/0000-template.md
@docs/adrs/0004-apache-2-0-licensing.md
@.github/workflows/spdx.yml
@.github/workflows/ci.yml
@.github/workflows/helm-release.yml
@CONTRIBUTING.md
@images/cnpg-postgres-17-pgpartman/Dockerfile
@charts/openwhispr/Chart.yaml
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 1: Pre-relicense annotated tag + LICENSE swap + NOTICE recompose</name>
  <files>
    - LICENSE
    - NOTICE
  </files>
  <action>
    1. `git tag -a pre-fsl-relicense-2026-05-14 -m "Last Apache-2.0 HEAD before FSL relicense (ADR-0013)"` — push tag: `git push origin pre-fsl-relicense-2026-05-14`.
    2. Replace `LICENSE` with the canonical FSL-1.1-ALv2 text from https://fsl.software/. Capture the source URL + SHA-256 of the fetched body for ADR-0013 references section.
    3. Update `NOTICE` to align with FSL-1.1-ALv2 patent-grant + attribution requirements; preserve existing third-party attributions verbatim.
    Commit as `feat(15-03): swap LICENSE Apache-2.0 → FSL-1.1-ALv2 (ADR-0013)` with the tag SHA in the body.
  </action>
  <verify>
    <automated>git tag -l 'pre-fsl-relicense-2026-05-14' | grep -q . && grep -q 'Functional Source License' LICENSE && grep -qi 'FSL-1.1-ALv2' LICENSE</automated>
  </verify>
  <done>Tag exists + pushed; LICENSE is FSL-1.1-ALv2; NOTICE patent-grant section present.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0013 + supersede ADR-0004 + MIGRATING.md</name>
  <files>
    - docs/adrs/0013-fsl-relicense.md (new)
    - docs/adrs/0004-apache-2-0-licensing.md (status patch)
    - MIGRATING.md (new, root)
  </files>
  <action>
    1. Copy `docs/adrs/0000-template.md` to `docs/adrs/0013-fsl-relicense.md`. Fill sections:
       - **Status:** accepted
       - **Date:** 2026-05-14
       - **Phase:** 15
       - **Context:** Why we're moving off Apache-2.0 (commercial-protection rationale per CONTEXT decisions).
       - **Decision:** FSL-1.1-ALv2 across all surfaces.
       - **Consequences:** What downstream users must do; the 7-day notice window; the upcoming history scrub (15-04).
       - **Recovery:** Full one-liner for downstream users to rebase off the pre-scrub tag (mirrored in MIGRATING.md).
       - **Retroactive consent:** Link to a GitHub issue with explicit per-contributor sign-off; record the cutoff date.
       - **Alternatives considered:** Apache-2.0 retention, BUSL, SSPL — why FSL won.
       - **References:** https://fsl.software/, https://developercertificate.org/, fetched LICENSE SHA, ADR-0004 supersession.
    2. Patch `docs/adrs/0004-apache-2-0-licensing.md` header: `**Status:** superseded by ADR-0013 (2026-05-14)`.
    3. Author `MIGRATING.md` (root): short pointer (≤80 lines) with the 7-day notice, a `## Recovery (one-liner)` section, and a placeholder section `<!-- POST-SCRUB-HEAD-SHA: filled by 15-04 -->`. Full runbook lives in ADR-0013.
    Commit as `docs(15-03): ADR-0013 FSL relicense + MIGRATING + supersede ADR-0004`.
  </action>
  <verify>
    <automated>test -f docs/adrs/0013-fsl-relicense.md && grep -q '^\*\*Status:\*\* accepted' docs/adrs/0013-fsl-relicense.md && grep -q 'superseded by ADR-0013' docs/adrs/0004-apache-2-0-licensing.md && test -f MIGRATING.md && grep -q 'POST-SCRUB-HEAD-SHA' MIGRATING.md</automated>
  </verify>
  <done>All three files present with correct headers + placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tools/spdx-header.ts for FSL + author REUSE.toml + sweep all source files</name>
  <files>
    - tools/spdx-header.ts
    - tools/__tests__/spdx-header.test.ts
    - REUSE.toml (new, root)
    - (~675 source files — line-1 rewrite via codemod)
  </files>
  <behavior>
    - RED: Extend `tools/__tests__/spdx-header.test.ts` with a new test asserting `HEADER === "// SPDX-License-Identifier: FSL-1.1-ALv2"` and that `--fix` rewrites a fixture file whose line 1 is `// SPDX-License-Identifier: Apache-2.0`. Run — RED.
    - GREEN: Flip `HEADER` constant in `tools/spdx-header.ts` to FSL-1.1-ALv2; extend `applyHeader` to detect-and-replace pre-existing Apache-2.0 headers (current shape only writes if absent); update existing test fixtures.
    - GREEN: Author `REUSE.toml` with `version = 1` and `[[annotations]]` blocks covering: `**/*.ts`, `**/*.tsx`, `**/*.js`, `**/*.mjs`, `**/*.cjs`, `**/*.sh`, `**/*.py`, `**/*.sql`, `**/*.yaml`, `**/*.yml`, `**/Dockerfile`, `**/*.md` — with `SPDX-FileCopyrightText` + `SPDX-License-Identifier = "FSL-1.1-ALv2"`. Exclude `LICENSE`, `NOTICE`, third-party vendored code, `node_modules`, `dist`.
    - Run codemod sweep: `tsx tools/spdx-header.ts --fix` over each directory tree. Commit per area: `apps/`, `packages/`, `tools/`, `scripts/`, `compose/`, `.github/`, `tests/`, root — ONE commit per area. Commit messages: `refactor(15-03): SPDX header sweep apps/`, `refactor(15-03): SPDX header sweep packages/`, etc.
    - Run `pipx install reuse && reuse lint` — MUST be GREEN.
  </behavior>
  <action>
    Implement RED→GREEN→codemod sweep with per-area atomic commits as described. Verify with each commit that `pnpm test:spdx-header` passes and `reuse lint` is GREEN incrementally.
  </action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/spdx-header.test.ts && reuse lint && grep -L 'SPDX-License-Identifier: FSL-1.1-ALv2' $(find apps packages tools -name '*.ts' -not -path '*/node_modules/*' -not -path '*/dist/*') | head -5</automated>
    Expected: vitest GREEN; reuse lint exit 0; final grep returns empty (no files missing FSL header).
  </verify>
  <done>Codemod test GREEN; REUSE.toml present; reuse lint GREEN; per-area sweep commits land cleanly; coverage on spdx-header.ts extension ≥ 90/90/90/90.</done>
</task>

<task type="auto">
  <name>Task 4: package.json license + Dockerfile labels + README badge</name>
  <files>
    - package.json (root + every workspace — ~12 files)
    - apps/api/Dockerfile, apps/web/Dockerfile, apps/worker/Dockerfile
    - packages/*/Dockerfile (if any)
    - images/*/Dockerfile (existing label updated)
    - tools/*/Dockerfile (if any)
    - README.md
  </files>
  <action>
    1. For every workspace `package.json` (root + apps/api, apps/web, apps/worker, packages/data, packages/email, packages/litellm-client, packages/byok-guard, packages/contract-tests, packages/wire-schemas, tools/test-probe, tools/load-test, compose/mock-litellm — confirm via `find . -name package.json -not -path '*/node_modules/*'`): ADD `"license": "FSL-1.1-ALv2"` field. This is BASELINE establishment — field was absent.
    2. For every Dockerfile (`find apps packages images tools compose -name Dockerfile -not -path '*/node_modules/*'`): ADD `LABEL org.opencontainers.image.licenses="FSL-1.1-ALv2"` near other OCI labels. The existing label on `images/cnpg-postgres-17-pgpartman/Dockerfile:39` is updated in place.
    3. Update `README.md`: add (or update) license badge near top: `![License: FSL-1.1-ALv2](https://img.shields.io/badge/license-FSL--1.1--ALv2-blue)` linking to LICENSE.
    Commit as `chore(15-03): license field + OCI labels + README badge → FSL-1.1-ALv2`.
  </action>
  <verify>
    <automated>find . -name package.json -not -path '*/node_modules/*' -exec grep -L '"license"' {} + ; find apps packages images tools compose -name Dockerfile -not -path '*/node_modules/*' -exec grep -L 'image.licenses="FSL-1.1-ALv2"' {} + ; grep -q 'FSL--1.1--ALv2' README.md</automated>
    Expected: first two `find` commands return empty (every file has the field/label); README grep succeeds.
  </verify>
  <done>Every package.json has license; every Dockerfile has the label; README has badge.</done>
</task>

<task type="auto">
  <name>Task 5: DCO requirement — CONTRIBUTING.md + dco.yml + reuse-lint CI gate</name>
  <files>
    - CONTRIBUTING.md
    - .github/dco.yml (new)
    - .github/workflows/reuse-lint.yml (new) OR appended to .github/workflows/spdx.yml
    - .github/workflows/ci.yml (add reuse-lint to required job matrix)
  </files>
  <action>
    1. Append to `CONTRIBUTING.md` a `## Developer Certificate of Origin (DCO)` section: reference https://developercertificate.org/, require `git commit --signoff` on every commit, point at the retroactive consent issue from ADR-0013.
    2. Author `.github/dco.yml` (or whatever path the chosen DCO bot expects — common choice: Probot DCO at `.github/dco.yml` with `require: { members: false }`). Add a placeholder `# cutoff_sha: TBD-from-15-04` comment for the grandfather SHA.
    3. Create `.github/workflows/reuse-lint.yml` mirroring the shape of `.github/workflows/spdx.yml` (harden-runner, pnpm setup or apt install for reuse). Job runs `pipx install reuse && reuse lint`. Concurrency group + cancel-in-progress per CI convention. Pin actions to SHAs.
    4. Update `.github/workflows/ci.yml` job dependency matrix so the `reuse-lint` job is a required check (add to the `needs` chain of any gating job, or list it independently).
    Commit as `feat(15-03): DCO requirement + reuse lint CI gate`.
  </action>
  <verify>
    <automated>grep -q 'Developer Certificate of Origin' CONTRIBUTING.md && test -f .github/dco.yml && test -f .github/workflows/reuse-lint.yml && grep -q 'reuse lint' .github/workflows/reuse-lint.yml && grep -q 'cutoff_sha' .github/dco.yml</automated>
  </verify>
  <done>CONTRIBUTING has DCO section; dco.yml exists with placeholder; reuse-lint workflow runs `reuse lint` in CI; ci.yml requires the job.</done>
</task>

<task type="auto">
  <name>Task 6: Helm chart-releaser workflow + ArtifactHub metadata (STRUCT-03 Option 3)</name>
  <files>
    - .github/workflows/chart-release.yml (new)
    - charts/openwhispr/Chart.yaml
    - charts/openwhispr/artifacthub-repo.yml (new)
  </files>
  <action>
    1. Bump `charts/openwhispr/Chart.yaml` `version` to `1.0.0` (first release on the new `chart-v*` lane); leave `appVersion` aligned to current server semver.
    2. Author `charts/openwhispr/artifacthub-repo.yml` with: `repositoryID` (UUID — generate via `uuidgen`), `owners: [name, email]`, `ignore: []`. Reference ArtifactHub Helm repo metadata spec.
    3. Author `.github/workflows/chart-release.yml`:
       - Trigger: `on.push.tags: ['chart-v*']` ONLY.
       - Concurrency: `group: chart-release-${{ github.ref }}, cancel-in-progress: false`.
       - Permissions: `contents: write, pages: write`.
       - Steps: harden-runner (pinned SHA per existing convention), checkout with `fetch-depth: 0`, configure-pages, install helm, `helm/chart-releaser-action@v1.6.0` (pinned by SHA) with `charts_dir: charts/` + `CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}`.
       - Output: publishes packaged chart to `gh-pages` branch + creates a GitHub Release tagged `chart-v*`.
    4. Document gh-pages branch bootstrap in 15-04 runbook (since `chart-releaser-action` requires the branch to pre-exist on first run; this is a one-shot operator op).
    Confirm `helm-release.yml` (existing OCI push on `v*`) does NOT conflict — different tag namespace, no overlap. Leave as-is.
    Commit as `feat(15-03): chart-releaser workflow on chart-v* (STRUCT-03)`.
  </action>
  <verify>
    <automated>test -f .github/workflows/chart-release.yml && grep -qE "'chart-v\\*'|\"chart-v\\*\"" .github/workflows/chart-release.yml && grep -q 'chart-releaser-action' .github/workflows/chart-release.yml && test -f charts/openwhispr/artifacthub-repo.yml && grep -q 'repositoryID' charts/openwhispr/artifacthub-repo.yml</automated>
    Also run `helm lint charts/openwhispr` and `helm unittest charts/openwhispr` if present.
  </verify>
  <done>Workflow file syntactically valid, tag trigger correct, action pinned; ArtifactHub metadata present; helm lint GREEN.</done>
</task>

</tasks>

<test_strategy>
- Unit: extended `tools/__tests__/spdx-header.test.ts` covers FSL HEADER constant + Apache→FSL replacement path; coverage gate 90/90/90/90 on the diff.
- Integration: `reuse lint` GREEN across the tree; `helm lint charts/openwhispr` + `helm unittest charts/openwhispr` GREEN.
- E2E: this plan does NOT touch user-visible routes — no new Gherkin scenarios required. Verifier re-runs the full Phase 13 + Phase 14 e2e suites against this plan's HEAD to confirm no regression from the SPDX sweep.
- Workflow lint: `actionlint` against the new `chart-release.yml` + `reuse-lint.yml`.
</test_strategy>

<deviation_handling>
- If `reuse lint` flags files NOT covered by the codemod (e.g., binary fixtures, .png, .ico) → add them to `REUSE.toml` `[[annotations]]` with explicit `SPDX-License-Identifier` rather than skipping; do NOT add wildcard suppressions.
- If a workspace `package.json` is missing or malformed → fix the package, don't suppress the lint.
- If `chart-releaser-action` first run fails because `gh-pages` branch is missing → flag in 15-04 runbook as the operator's bootstrap step (`git checkout --orphan gh-pages && git commit --allow-empty -m "init gh-pages" && git push origin gh-pages`).
- If the FSL text fetched from https://fsl.software/ has a fingerprint mismatch vs the version pinned in research → halt + escalate; do NOT proceed with a different LICENSE body.
- If existing `helm-release.yml` (OCI push on `v*`) shares any infrastructure with the new chart-release.yml that could race → document in chart-release.yml header comment + keep separate concurrency groups; do not merge the two workflows.
- If `reuse annotate` (Python tool) interacts oddly with the in-house `tools/spdx-header.ts` codemod's output → prefer the in-house codemod for writes; use `reuse lint` only for verification, NOT writes. This avoids two writers fighting.
</deviation_handling>

<success_criteria>
1. `pre-fsl-relicense-2026-05-14` tag pushed; LICENSE is FSL-1.1-ALv2.
2. ADR-0013 exists with `**Status:** accepted`; ADR-0004 marked superseded.
3. MIGRATING.md has the 7-day notice + POST-SCRUB-HEAD-SHA placeholder.
4. REUSE.toml + `reuse lint` GREEN.
5. Every workspace package.json has `"license": "FSL-1.1-ALv2"`.
6. Every Dockerfile has the OCI license LABEL.
7. README has the FSL-1.1-ALv2 badge.
8. CONTRIBUTING.md mandates DCO; .github/dco.yml has cutoff_sha placeholder.
9. `.github/workflows/chart-release.yml` triggers on `chart-v*`, uses pinned chart-releaser-action; `charts/openwhispr/artifacthub-repo.yml` present.
10. `pnpm vitest run`, `pnpm test:spdx-header`, `helm unittest`, `biome check`, `reuse lint`, `actionlint` ALL GREEN.
11. Coverage ≥ 90/90/90/90 on the spdx-header.ts extension diff.
</success_criteria>

<output>
After completion, create `.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-03-SUMMARY.md` per get-shit-done template: list the LICENSE source URL + SHA, count of files swept per area (7 commits), count of package.json files patched, count of Dockerfiles labeled, chart-release.yml action SHA pin, and the pre-relicense tag SHA. Flag the gh-pages branch bootstrap as a precondition for 15-04.
</output>
