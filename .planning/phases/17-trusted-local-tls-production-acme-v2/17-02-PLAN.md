---
phase: 17-trusted-local-tls-production-acme-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - tools/lint-dockerfile-tls.ts
  - tools/__tests__/lint-dockerfile-tls.test.ts
  - tools/__tests__/fixtures/dockerfile-tls/good/Dockerfile
  - tools/__tests__/fixtures/dockerfile-tls/bad/Dockerfile
  - tools/lint-dockerfile-tls.allowlist.txt
  - package.json
  - lefthook.yml
  - .github/workflows/ci.yml
  - .dockerignore
  - compose/traefik/.dockerignore
  - tests/e2e-cjm/features/phase17-tls.feature
  - tests/e2e-cjm/steps/tls.steps.ts
  - docs/operations.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - tools/spdx-header.ts
autonomous: true
requirements:
  - TLS-05
  - TLS-06
must_haves:
  truths:
    - "`tools/lint-dockerfile-tls.ts` exists and mirrors `tools/lint-phase-tag-comments.ts:1-100` verbatim: line 1 shebang `#!/usr/bin/env -S pnpm exec tsx`, line 2 SPDX `FSL-1.1-ALv2`, argv shape `tsx tools/lint-dockerfile-tls.ts [rootDir]` (bare positional, no subcommands), exit codes 0 (clean) / 1 (violations on stderr) / 2 (internal error / usage), entry guard `invokedDirect` at bottom mirroring `tools/spdx-header.ts:411-414`."
    - "Lint CLI exports `findViolations(rootDir: string): Promise<Violation[]>` for direct-import vitest, mirroring `lint-phase-tag-comments.ts` `findViolations` export."
    - "Glob is narrowed to `**/Dockerfile` ONLY (NOT `**/Dockerfile*`, NOT `.dockerignore`) per CONTEXT Q3-B1 + 17-PATTERNS risk callout line 226. IGNORE list includes `node_modules`, `.git`, `coverage`, `dist`, `.next`."
    - "Forbidden patterns scanned per-line in each Dockerfile: `rootCA*.pem`, `root-ca.crt`, `root-ca.key`, `mkcert` (case-insensitive token), `compose/traefik/certs/`, `*.localhost.pem`, `*.localhost.key`, `local.crt`, `local.key`. Match scope is `COPY` / `ADD` source operands AND any `RUN` invocation containing `mkcert`."
    - "`tools/__tests__/fixtures/dockerfile-tls/good/Dockerfile` + `.../bad/Dockerfile` exist as test fixtures; the good fixture mirrors `compose/traefik/Dockerfile` shape (single `COPY fd-probe.sh` line); the bad fixture contains at least one violation per forbidden pattern."
    - "`tools/__tests__/lint-dockerfile-tls.test.ts` covers: positive (good fixture → 0 violations), positive (bad fixture → ≥7 violations, one per forbidden pattern), allowlist reader strips `#` + blank lines, allowlist entry suppresses a violation for the allowlisted POSIX Dockerfile path, files outside the glob (e.g. `node_modules/.../Dockerfile`) are NOT scanned, main() returns 0 on clean tree and 1 on dirty tree. Coverage ≥ 90/90/90/90 on `tools/lint-dockerfile-tls.ts`."
    - "`tools/lint-dockerfile-tls.allowlist.txt` exists with a header-only body: `# Phase 17 TLS-05 dev-CA-isolation lint allowlist — entries require justification in commit body.` plus the standard one-POSIX-path-per-line format comment; zero path entries on creation."
    - "Wiring triad lands in ONE atomic commit per Phase 16 16-01 `4771e3d` precedent: `package.json` adds `\"lint:dockerfile-tls\": \"tsx tools/lint-dockerfile-tls.ts\"` IMMEDIATELY after `\"lint:phase-tag-comments\"` (around line 24); `lefthook.yml` appends a sibling `dockerfile-tls:` block IMMEDIATELY after `phase-tag-comments:` with `glob: \"**/Dockerfile\"` + `run: pnpm lint:dockerfile-tls`; `.github/workflows/ci.yml` appends `- run: pnpm lint:dockerfile-tls` as a single line to the `lint-english` job IMMEDIATELY after the existing `lint:phase-tag-comments` step. NO new CI job."
    - "`pnpm lint:dockerfile-tls` runs end-to-end against the real tree and exits 0 (current Dockerfile inventory has ZERO violations per 17-CONTEXT `<code_context>` lines 64-67)."
    - "Root `.dockerignore` extended with the 9-line TLS-05 block per CONTEXT Q3 lines 146-158 (header comment + `**/rootCA*.pem` + `**/root-ca.*` + `**/*mkcert*` + `**/*.localhost.pem` + `**/*.localhost.key` + `**/local.crt` + `**/local.key` + `compose/traefik/certs/` + `.certs/`)."
    - "NEW `compose/traefik/.dockerignore` exists per CONTEXT Q3 lines 160-168, containing SPDX hash-style header + `certs/` + `*.pem` + `*.crt` + `*.key` + `*.srl`. This is the FIRST per-context `.dockerignore` in the repo (17-PATTERNS gap §1)."
    - "NEW `tests/e2e-cjm/features/phase17-tls.feature` exists with 3 scenarios tagged `@phase-17 @tls`: (1) `@cjm-tls-trusted-localhost @after-docker-up @expected-red` — browser-trust check; (2) `@cjm-tls-no-dev-ca-in-prod-image` — runnable in CI without docker compose (uses `docker create + docker export | tar -t` per CONTEXT line 176); (3) `@cjm-tls-acme-staging @after-docker-up @expected-red` — ACME staging issuance via Traefik prod profile."
    - "NEW `tests/e2e-cjm/steps/tls.steps.ts` exists with step definitions for all 3 scenarios; scenario 2 step uses `docker create <image>` + `docker export <container> | tar -t | grep -E '(rootCA|local\\.(crt|key)|mkcert)'` and asserts ZERO matches (TLS-05 enforcement); scenario 2 also asserts the bootstrap-minted cert SAN list contains no wildcard entries (closes Q1 corollary)."
    - "`docs/operations.md` has a NEW `## Air-gap mkcert installation` H2 section (anchor `#air-gap-mkcert`) inserted between `## Bootstrap prerequisites` (around L34-43) and the next H2 section (`## BYOK Environment Matrix` around L44); section content covers the 5 items from CONTEXT Q5 (macOS binary mirror URL, Linux binary mirror URL, checksum verification, PATH installation, `mkcert -install` air-gap caveat with manual trust-store fallback)."
    - "`.planning/ROADMAP.md` Phase 17 SC #1 + SC #3 (or wherever `PITFALLS §16` is cited) are corrected to `PITFALLS §13`; same correction applied to `.planning/REQUIREMENTS.md` if it cites §16."
    - "`tools/spdx-header.ts` HASH_PATTERNS scope verified to cover the new file types. If `.feature` and/or `.dockerignore` are NOT already in HASH_PATTERNS, they are added in the same commit as the isolation evidence so the new files lint cleanly under `pnpm spdx:audit-hash`. If already covered, NO change to spdx-header.ts."
    - "Two atomic commits land per CONTEXT Q4 lines 191-194: (a) lint CLI tooling triad atomic (lint + tests + fixtures + allowlist + pnpm + lefthook + ci.yml); (b) isolation evidence atomic (.dockerignore root + per-context + Gherkin feature + step defs + air-gap docs + ROADMAP/REQUIREMENTS §16→§13 fix + optional spdx-header.ts HASH_PATTERNS extension)."
    - "ZERO `--no-verify` across both commits per CONTEXT line 203."
  artifacts:
    - path: "tools/lint-dockerfile-tls.ts"
      provides: "Standalone tsx lint CLI scanning `**/Dockerfile` for dev-CA / mkcert leakage."
      contains: "findViolations, readAllowlist, invokedDirect"
    - path: "tools/__tests__/lint-dockerfile-tls.test.ts"
      provides: "RED→GREEN coverage of all forbidden patterns + allowlist + glob narrowing."
    - path: "tools/__tests__/fixtures/dockerfile-tls/{good,bad}/Dockerfile"
      provides: "Fixture Dockerfiles for vitest."
    - path: "tools/lint-dockerfile-tls.allowlist.txt"
      provides: "Transitional allowlist; zero entries initially."
    - path: "package.json"
      provides: "`lint:dockerfile-tls` script row."
    - path: "lefthook.yml"
      provides: "`dockerfile-tls:` pre-commit block."
    - path: ".github/workflows/ci.yml"
      provides: "Single `- run: pnpm lint:dockerfile-tls` line appended to `lint-english` job."
    - path: ".dockerignore"
      provides: "Root-context dev-CA exclusions (9-line block)."
    - path: "compose/traefik/.dockerignore"
      provides: "Per-context dev-CA exclusions (FIRST per-context `.dockerignore` in repo)."
    - path: "tests/e2e-cjm/features/phase17-tls.feature"
      provides: "3 Gherkin scenarios — trust + isolation + ACME staging."
    - path: "tests/e2e-cjm/steps/tls.steps.ts"
      provides: "Step definitions for the 3 scenarios; filesystem-scan impl for scenario 2."
    - path: "docs/operations.md"
      provides: "`## Air-gap mkcert installation` section (anchor `#air-gap-mkcert`)."
    - path: ".planning/ROADMAP.md"
      provides: "PITFALLS §16 → §13 reference correction."
    - path: ".planning/REQUIREMENTS.md"
      provides: "PITFALLS §16 → §13 reference correction (if needed)."
    - path: "tools/spdx-header.ts"
      provides: "HASH_PATTERNS extension for `.feature` / `.dockerignore` (only if not already covered)."
  key_links:
    - from: "tools/lint-dockerfile-tls.ts"
      to: "tools/lint-phase-tag-comments.ts"
      via: "CLI shape inheritance — argv, exit codes, findViolations, readAllowlist, invokedDirect"
      pattern: "findViolations|readAllowlist|invokedDirect"
    - from: "package.json + lefthook.yml + .github/workflows/ci.yml"
      to: "tools/lint-dockerfile-tls.ts"
      via: "wiring triad atomic — mirrors Phase 16 `4771e3d`"
      pattern: "lint:dockerfile-tls"
    - from: "tests/e2e-cjm/features/phase17-tls.feature"
      to: ".dockerignore + compose/traefik/.dockerignore"
      via: "Scenario 2 (`@cjm-tls-no-dev-ca-in-prod-image`) regression-guards both dockerignores via filesystem-scan of built prod image"
      pattern: "rootCA|mkcert|local\\.crt"
    - from: ".planning/ROADMAP.md + .planning/REQUIREMENTS.md"
      to: ".planning/research/PITFALLS.md §13"
      via: "Reference correction (§16 → §13) closes CONTEXT lines 14-22 directive"
      pattern: "PITFALLS §13"
---

<objective>
Enforce dev-CA isolation from production images and document the air-gap path
for offline operators. This plan delivers three concerns split into two atomic
commits:

1. **Lint CLI tooling triad atomic (commit A)** — `tools/lint-dockerfile-tls.ts`
   standalone tsx CLI mirroring `tools/lint-phase-tag-comments.ts` shape,
   accompanied by Vitest RED→GREEN coverage ≥ 90/90/90/90, test fixtures,
   transitional allowlist file, and the three wiring edits (pnpm script +
   lefthook block + ci.yml line). One commit. Phase 16 16-01 `4771e3d`
   template applies verbatim.

2. **Isolation evidence atomic (commit B)** — extends root `.dockerignore` with
   the 9-line TLS-05 block, authors the FIRST per-context `.dockerignore` at
   `compose/traefik/.dockerignore`, authors the Phase 17 Gherkin feature file
   and step defs (3 scenarios under `@phase-17 @tls`), inserts the
   `## Air-gap mkcert installation` section into `docs/operations.md`,
   corrects `PITFALLS §16` → `§13` references in ROADMAP + REQUIREMENTS, and
   (if needed) extends `tools/spdx-header.ts` HASH_PATTERNS to cover
   `.feature` / `.dockerignore` so the new files audit clean.

Depends on 17-01 (Wave 1) because the lint CLI predicate keys on the
`compose/traefik/certs/` path 17-01 establishes as the cert location (Q1-D1
status quo).

Predicted ZERO `--no-verify` per CONTEXT line 203. If lefthook fires
unexpectedly, HALT — do NOT silently apply `--no-verify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/17-trusted-local-tls-production-acme-v2/17-CONTEXT.md
@.planning/phases/17-trusted-local-tls-production-acme-v2/17-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@tools/lint-phase-tag-comments.ts
@tools/lint-phase-tag-comments.test.ts
@tools/lint-phase-tag-comments.allowlist.txt
@tools/spdx-header.ts
@package.json
@lefthook.yml
@.github/workflows/ci.yml
@.dockerignore
@compose/traefik/Dockerfile
@tests/e2e-cjm/features/traefik-host-split.feature
@docs/operations.md
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — author lint-dockerfile-tls test suite + fixtures</name>
  <files>
    - tools/__tests__/lint-dockerfile-tls.test.ts (new)
    - tools/__tests__/fixtures/dockerfile-tls/good/Dockerfile (new)
    - tools/__tests__/fixtures/dockerfile-tls/bad/Dockerfile (new)
  </files>
  <behavior>
    Test-suite assertions:

    - F1: `findViolations(root)` on a tmpdir containing ONLY the good fixture
      → zero violations.
    - F2: `findViolations(root)` on a tmpdir containing ONLY the bad fixture
      → ≥7 violations, one per forbidden token: `rootCA*.pem`, `root-ca.crt`,
      `root-ca.key`, `mkcert`, `compose/traefik/certs/`, `*.localhost.pem`,
      `*.localhost.key`, `local.crt`, `local.key`.
    - F3: `findViolations(root)` ignores files outside the glob — fixture at
      `tmpdir/node_modules/some/Dockerfile` containing `COPY rootCA.pem /`
      → zero violations.
    - F4: `findViolations(root)` does NOT scan `.dockerignore` files even if
      they contain the forbidden tokens (glob is `**/Dockerfile` exact).
    - F5: Allowlist suppresses — fixture `compose/x/Dockerfile` with `COPY
      rootCA.pem /` AND allowlist entry `compose/x/Dockerfile` → zero
      violations.
    - F6: `readAllowlist(rootDir)` strips `#`-prefixed and blank lines; returns
      `Set<string>` of POSIX paths.
    - F7: `main(["", root])` returns 1 on dirty tree, 0 on clean tree, 2 on
      internal error (e.g. unreadable root).

    Fixture content:

    - Good fixture `tools/__tests__/fixtures/dockerfile-tls/good/Dockerfile`:
      mirrors `compose/traefik/Dockerfile` minimal shape — `FROM
      traefik:3.6` + `COPY fd-probe.sh /` + `HEALTHCHECK …`. No mkcert, no
      cert COPYs.
    - Bad fixture `tools/__tests__/fixtures/dockerfile-tls/bad/Dockerfile`:
      a synthetic Dockerfile containing ONE forbidden token per line so each
      F2 sub-assertion has a clean target line. Example:
      ```
      FROM debian:stable-slim
      COPY rootCA.pem /
      COPY root-ca.crt /
      COPY root-ca.key /
      RUN mkcert -install
      COPY compose/traefik/certs/ /certs/
      COPY api.localhost.pem /
      COPY api.localhost.key /
      COPY local.crt /
      COPY local.key /
      ```
  </behavior>
  <action>
    Create the three files. The test file mirrors
    `tools/lint-phase-tag-comments.test.ts` structure: vitest +
    `mkdtempSync(join(tmpdir(), "lint-dockerfile-tls-"))` + per-`it`
    `beforeEach`/`afterEach` cleanup + `touch(rel, content)` helper writing
    fixture files.

    Imports — `findViolations`, `readAllowlist`, `main` from
    `../lint-dockerfile-tls.ts`. Module does NOT exist yet → RED with
    module-not-found error.

    The two fixture Dockerfiles MUST have an SPDX hash-style header line
    immediately after `FROM`:
    `# SPDX-License-Identifier: FSL-1.1-ALv2` — verify via
    `pnpm spdx:audit-hash` after fixture creation. If `.spdx-header.ts`
    HASH_PATTERNS doesn't cover `Dockerfile`, that audit currently skips
    them — see Task 8 for the optional extension if needed.

    Commit at end of Task 1: `test(17-02): RED lint-dockerfile-tls + fixtures`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/lint-dockerfile-tls.test.ts</automated>
  </verify>
  <done>≥7 assertions RED with module-not-found error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement tools/lint-dockerfile-tls.ts + allowlist</name>
  <files>
    - tools/lint-dockerfile-tls.ts (new)
    - tools/lint-dockerfile-tls.allowlist.txt (new)
  </files>
  <behavior>
    All ≥7 Task 1 assertions GREEN. Coverage on `tools/lint-dockerfile-tls.ts`
    ≥ 90/90/90/90 on lines/branches/functions/statements.
  </behavior>
  <action>
    Mirror `tools/lint-phase-tag-comments.ts:1-100` verbatim. Order:

    1. Line 1: `#!/usr/bin/env -S pnpm exec tsx`
    2. Line 2: `// SPDX-License-Identifier: FSL-1.1-ALv2`
    3. Module-level constants:
       - `const ALLOWLIST_FILE = "tools/lint-dockerfile-tls.allowlist.txt"`
       - `const PATTERNS = ["**/Dockerfile"]` — NOT `**/Dockerfile*` (17-PATTERNS
         risk callout #226 narrows the glob explicitly).
       - `const IGNORE_DIRS = ["node_modules", ".git", "coverage", "dist", ".next"]`
       - `const FORBIDDEN: { regex: RegExp; label: string }[]` — one entry per
         forbidden token (10 entries):
         - `/rootCA[A-Za-z0-9._-]*\.pem/i` "rootCA*.pem"
         - `/root-ca\.crt/i` "root-ca.crt"
         - `/root-ca\.key/i` "root-ca.key"
         - `/\bmkcert\b/i` "mkcert"
         - `/compose\/traefik\/certs\//` "compose/traefik/certs/"
         - `/[A-Za-z0-9_.-]*\.localhost\.pem/i` "*.localhost.pem"
         - `/[A-Za-z0-9_.-]*\.localhost\.key/i` "*.localhost.key"
         - `/\blocal\.crt\b/` "local.crt"
         - `/\blocal\.key\b/` "local.key"
    4. Exported `readAllowlist(rootDir: string): Promise<Set<string>>` — copy
       `lint-phase-tag-comments.ts` shape verbatim with the renamed
       `ALLOWLIST_FILE`.
    5. Exported `findViolations(rootDir: string): Promise<Violation[]>` —
       iterates via `node:fs/promises` `glob` with `PATTERNS` + `IGNORE_DIRS`
       skip. For each Dockerfile, reads, splits on `\n`, and for each line
       checks every entry in `FORBIDDEN`. On match, if file POSIX path NOT in
       allowlist → push `{file, lineNumber, lineText, label}`. Returns
       `Violation[]`.
    6. `main(argv: string[]): Promise<number>` — accepts optional `[rootDir]`,
       returns 0 (clean) / 1 (dirty; per-file summary on stderr) / 2 (caught
       error). Mirror `lint-phase-tag-comments.ts:120-143` shape.
    7. Entry guard at bottom — copy `tools/spdx-header.ts:411-414`
       `invokedDirect` pattern verbatim.

    Create `tools/lint-dockerfile-tls.allowlist.txt` with header-only body:
    ```
    # SPDX-License-Identifier: FSL-1.1-ALv2
    # Phase 17 TLS-05 dev-CA-isolation lint allowlist — entries require
    # justification in commit body. Format: one POSIX path per line; blank
    # lines and `#`-prefixed lines ignored. Add a path here ONLY if a
    # forbidden token in that Dockerfile is intentional (extremely rare).
    ```

    Run vitest until GREEN with coverage gate. Defensive branches uncovered
    → add fixtures, never lower the threshold.

    Commit: `feat(17-02): lint-dockerfile-tls tsx CLI + transitional allowlist`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/lint-dockerfile-tls.test.ts --coverage</automated>
  </verify>
  <done>≥7 assertions GREEN; coverage ≥ 90/90/90/90 on lint-dockerfile-tls.ts; `pnpm exec tsx tools/lint-dockerfile-tls.ts` against the real repo exits 0 (per CONTEXT lines 64-67 the current 12 Dockerfiles contain ZERO violations).</done>
</task>

<task type="auto">
  <name>Task 3: Wiring triad atomic — pnpm + lefthook + CI</name>
  <files>
    - package.json
    - lefthook.yml
    - .github/workflows/ci.yml
  </files>
  <action>
    Three wiring edits in ONE commit per Phase 16 16-01 `4771e3d` template.

    1. **package.json** — add `"lint:dockerfile-tls": "tsx tools/lint-dockerfile-tls.ts"`
       IMMEDIATELY after `"lint:phase-tag-comments": …` (around line 24).
       Preserve trailing-comma JSON syntax.

    2. **lefthook.yml** — append a sibling block IMMEDIATELY after the
       `phase-tag-comments:` block (currently lives around lines 23-26 per
       Phase 16 wiring):
       ```yaml
         dockerfile-tls:
           glob: "**/Dockerfile"
           run: pnpm lint:dockerfile-tls
       ```
       Use the NARROW glob `**/Dockerfile` (NOT `**/Dockerfile*`) per
       17-PATTERNS risk callout line 226 — avoids accidentally triggering on
       `Dockerfile.bak` or, critically, on the new `compose/traefik/.dockerignore`
       (which is OUT of lint scope; the Gherkin scenario is its regression
       guard).

    3. **.github/workflows/ci.yml** — append a single line
       `      - run: pnpm lint:dockerfile-tls` to the `lint-english` job
       IMMEDIATELY after the existing `- run: pnpm lint:phase-tag-comments`
       step. NO new job; NO new runner config.

    Smoke locally BEFORE committing:
    - `pnpm lint:dockerfile-tls` → exit 0 (no violations in current tree).
    - `lefthook run pre-commit --files compose/traefik/Dockerfile` → the new
      block triggers + passes.

    Commit (Task 1+2+3 land as ONE atomic commit per CONTEXT Q4 "lint CLI
    tooling triad atomic"; the test/impl/allowlist files from Tasks 1-2 plus
    the three wiring edits): `feat(17-02): wire lint-dockerfile-tls into pnpm + lefthook + CI`.

    **Combine Tasks 1, 2, 3 into a single atomic commit** at this step —
    CONTEXT Q4 lines 191-193 prescribe "Lint CLI tooling triad atomic". Stage
    all files from Tasks 1+2+3 together, run `pnpm vitest run
    tools/__tests__/lint-dockerfile-tls.test.ts --coverage` one last time,
    then `git commit`.
  </action>
  <verify>
    <automated>grep -q 'lint:dockerfile-tls' package.json && grep -q 'dockerfile-tls:' lefthook.yml && grep -q 'pnpm lint:dockerfile-tls' .github/workflows/ci.yml && pnpm lint:dockerfile-tls; test $? -eq 0</automated>
  </verify>
  <done>One atomic commit (commit A) bundles RED test + GREEN impl + fixtures + allowlist + pnpm script + lefthook block + ci.yml line. `pnpm lint:dockerfile-tls` exits 0 on the real tree.</done>
</task>

<task type="auto">
  <name>Task 4: Extend root `.dockerignore` + author per-context `compose/traefik/.dockerignore`</name>
  <files>
    - .dockerignore
    - compose/traefik/.dockerignore (new)
  </files>
  <action>
    Per CONTEXT Q3-A2 (lines 146-168):

    1. **Root `.dockerignore`** — append the following block (after the
       existing 23-line content; preserve existing entries):
       ```
       # TLS-05 / Phase 17 — dev-CA isolation (PITFALLS §13)
       **/rootCA*.pem
       **/root-ca.*
       **/*mkcert*
       **/*.localhost.pem
       **/*.localhost.key
       **/local.crt
       **/local.key
       compose/traefik/certs/
       .certs/
       ```

    2. **NEW `compose/traefik/.dockerignore`** — Docker per-context dockerignore
       resolution does NOT inherit from the root file. This is the FIRST
       per-context `.dockerignore` in the repo (17-PATTERNS gap §1):
       ```
       # SPDX-License-Identifier: FSL-1.1-ALv2
       # TLS-05 / Phase 17 — per-context guard (root .dockerignore does NOT
       # cover this context; PITFALLS §13).
       certs/
       *.pem
       *.crt
       *.key
       *.srl
       ```

    3. Smoke that `docker build compose/traefik/` does NOT include any of the
       cert leaves: `docker build -t openwhispr-traefik:tls-test compose/traefik/`
       then `docker run --rm --entrypoint sh openwhispr-traefik:tls-test -c
       'ls /certs 2>/dev/null; find / -name "rootCA*" 2>/dev/null; find / -name
       "local.crt" 2>/dev/null'` → empty. (If docker build is unavailable on
       the executor host, defer the smoke to the Gherkin scenario 2 below;
       the per-context dockerignore semantics are well-documented + the lint
       CLI + the Gherkin scan form the regression guard triad.)

    This task is part of commit B (isolation evidence atomic); do NOT commit
    yet — bundle with Tasks 5-7 below.
  </action>
  <verify>
    <automated>grep -q '\*\*/rootCA\*\.pem' .dockerignore && grep -q '\*\*/local\.crt' .dockerignore && grep -q 'compose/traefik/certs/' .dockerignore && test -e compose/traefik/.dockerignore && grep -q '^certs/$' compose/traefik/.dockerignore && grep -q '^\*\.pem$' compose/traefik/.dockerignore</automated>
  </verify>
  <done>Root `.dockerignore` has 9 new entries; per-context `compose/traefik/.dockerignore` exists with SPDX header + 5 ignore entries.</done>
</task>

<task type="auto">
  <name>Task 5: Author Gherkin feature `tests/e2e-cjm/features/phase17-tls.feature` + step defs</name>
  <files>
    - tests/e2e-cjm/features/phase17-tls.feature (new)
    - tests/e2e-cjm/steps/tls.steps.ts (new)
  </files>
  <action>
    Per CONTEXT Q3-C1 (lines 171-176) + 17-PATTERNS analog §4 (lines 152-167):

    Feature file shape — mirror `tests/e2e-cjm/features/traefik-host-split.feature`.
    Line 1: `# SPDX-License-Identifier: FSL-1.1-ALv2`. Top-level tags:
    `@phase-17 @tls`. Three scenarios:

    1. **`@cjm-tls-trusted-localhost @after-docker-up @expected-red`**
       (ROADMAP §17 SC #5):
       ```gherkin
       Scenario: mkcert dev cert is trusted by browser on first run
         Given the developer has run `make tls-trust` on this host
         When they curl https://api.localhost/healthz --cacert compose/traefik/certs/root-ca.crt
         Then the response is 200 with no TLS warning
         And the served leaf cert SAN list contains exactly api.localhost web.localhost app.localhost grafana.localhost mailpit.localhost
         And the served leaf cert SAN list contains no wildcard entries
       ```

    2. **`@cjm-tls-no-dev-ca-in-prod-image`** (TLS-05 enforcement, runnable
       in CI without `docker compose` — see step impl below):
       ```gherkin
       Scenario: production image contains no dev CA artefacts
         Given the api production image has been built (tag openwhispr-api:tls-test)
         When the image filesystem is scanned via docker-export + tar
         Then no path matches rootCA*.pem
         And no path matches local.crt or local.key
         And no path contains mkcert
         And no path matches compose/traefik/certs/
         And the bootstrap.sh SAN block in the image (if present) contains no wildcard entries
       ```

    3. **`@cjm-tls-acme-staging @after-docker-up @expected-red`** (TLS-02-prod
       / TLS-03 enforcement):
       ```gherkin
       Scenario: ACME staging endpoint issues cert via Traefik prod profile
         Given the operator has set LETSENCRYPT_EMAIL and LETSENCRYPT_STAGING=1
         And the stack is up via `docker compose -f docker-compose.yml -f compose/docker-compose.acme.yml up -d --wait`
         When they curl https://api.example.com/healthz
         Then the served leaf cert is issued by "(STAGING) Let's Encrypt"
         And the cert chain validates against the staging root
       ```

    Step defs file `tests/e2e-cjm/steps/tls.steps.ts` — mirror
    `tests/e2e-cjm/steps/locale.steps.ts` shape. Critical step impl details:

    - **Scenario 2 filesystem-scan step** uses CONTEXT line 176 recipe:
      ```ts
      const container = execFileSync("docker", ["create", imageTag]).toString().trim();
      try {
        const tar = execFileSync("docker", ["export", container]);
        const listing = execFileSync("tar", ["-tf", "-"], { input: tar }).toString();
        expect(listing).not.toMatch(/rootCA[^/]*\.pem/);
        expect(listing).not.toMatch(/\blocal\.(crt|key)\b/);
        expect(listing).not.toMatch(/mkcert/i);
        expect(listing).not.toMatch(/compose\/traefik\/certs\//);
      } finally {
        execFileSync("docker", ["rm", "-f", container]);
      }
      ```
      NO `docker run` required — works on distroless. Faster than full
      stack-up.
    - **Scenario 2 SAN-list assertion** (closes Q1 corollary): also extracts
      the openssl SAN list of any bootstrap-minted cert that did happen to
      ship and asserts no `DNS:*.…` entries. If no cert ships, step passes
      vacuously.
    - **Scenarios 1 + 3 steps** are stubbed with `pending('@after-docker-up — runs in GHA CI after stack-up step')`
      bodies for now (per Phase 15-16 precedent — live execution deferred to
      GHA CI; tagging makes the deferral explicit). Step skeletons MUST be
      syntactically complete (the cucumber-tsx runner imports them at parse
      time even when the tag filter excludes them).

    Bundle with commit B (do NOT commit yet).
  </action>
  <verify>
    <automated>test -e tests/e2e-cjm/features/phase17-tls.feature && grep -q '@phase-17' tests/e2e-cjm/features/phase17-tls.feature && grep -q '@cjm-tls-trusted-localhost' tests/e2e-cjm/features/phase17-tls.feature && grep -q '@cjm-tls-no-dev-ca-in-prod-image' tests/e2e-cjm/features/phase17-tls.feature && grep -q '@cjm-tls-acme-staging' tests/e2e-cjm/features/phase17-tls.feature && test -e tests/e2e-cjm/steps/tls.steps.ts</automated>
  </verify>
  <done>Feature file with 3 tagged scenarios + step defs both exist; cucumber-tsx parse passes (`pnpm cucumber-js --dry-run tests/e2e-cjm/features/phase17-tls.feature`).</done>
</task>

<task type="auto">
  <name>Task 6: Author `docs/operations.md` `## Air-gap mkcert installation` section</name>
  <files>
    - docs/operations.md
  </files>
  <action>
    Per CONTEXT Q5 (line 212) + 17-PATTERNS analog §6 (lines 188-195):

    Insert a NEW H2 section `## Air-gap mkcert installation` (anchor
    `#air-gap-mkcert` derives automatically from the title via markdown
    convention) between `## Bootstrap prerequisites` (around docs/operations.md:34-43)
    and the next H2 section (around docs/operations.md:44, likely
    `## BYOK Environment Matrix`).

    Section MUST contain (in order):

    1. **Intro paragraph** — explain that operators without internet access
       cannot pull mkcert from upstream; this section documents the manual
       binary mirror + checksum + trust-store install path.
    2. **macOS binary mirror URL** — `https://github.com/FiloSottile/mkcert/releases/latest`
       (placeholder; operator overrides with their own mirror). Include
       `darwin-amd64` + `darwin-arm64` filename pattern.
    3. **Linux binary mirror URL** — same upstream + `linux-amd64` /
       `linux-arm64` filename pattern.
    4. **Checksum verification** — recommend `sha256sum -c` against the
       upstream `.sha256` file (or operator-provided checksum). Example
       block.
    5. **PATH installation** — `chmod +x mkcert-*` + `mv mkcert-* /usr/local/bin/mkcert`
       (Linux) or `~/Library/.../bin/mkcert` (macOS). Cross-reference
       `make tls-trust` discovery in 17-01.
    6. **`mkcert -install` air-gap caveat** — explain that without internet
       access mkcert's `-install` step may fail to write to the system trust
       store on some platforms; document the manual fallback:
       - macOS: `security add-trusted-cert -d -r trustRoot -k
         /Library/Keychains/System.keychain "$(mkcert -CAROOT)/rootCA.pem"`
       - Linux: `cp "$(mkcert -CAROOT)/rootCA.pem"
         /usr/local/share/ca-certificates/mkcert-rootCA.crt && update-ca-certificates`
       - Note that Firefox uses its own NSS store; `apt install libnss3-tools`
         + `mkcert -install` may still be required.

    All section content in English. Bundle with commit B.
  </action>
  <verify>
    <automated>grep -q '^## Air-gap mkcert installation' docs/operations.md && grep -q 'update-ca-certificates' docs/operations.md && grep -q 'security add-trusted-cert' docs/operations.md && grep -q 'sha256' docs/operations.md</automated>
  </verify>
  <done>The section exists with all 5 sub-items + the anchor `#air-gap-mkcert` referenced from 17-01's `make tls-trust` discovery branch resolves.</done>
</task>

<task type="auto">
  <name>Task 7: Correct `PITFALLS §16` → `§13` references in ROADMAP + REQUIREMENTS; optionally extend `tools/spdx-header.ts` HASH_PATTERNS</name>
  <files>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - tools/spdx-header.ts (optional — only if HASH_PATTERNS doesn't cover `.feature` / `.dockerignore`)
  </files>
  <action>
    Per CONTEXT pitfalls_correction (lines 14-22) + Q5 line 210:

    1. **Reference fix in ROADMAP** — grep for `PITFALLS §16` in
       `.planning/ROADMAP.md`. For each match within the Phase 17 entry
       block, replace with `PITFALLS §13`. Verify that §16 references
       OUTSIDE the Phase 17 block are NOT touched (those legitimately
       reference visual-regression / a11y per CONTEXT line 18).

    2. **Reference fix in REQUIREMENTS** — same operation against
       `.planning/REQUIREMENTS.md`. If `PITFALLS §16` does NOT appear there,
       skip this sub-step and remove `.planning/REQUIREMENTS.md` from the
       commit file list. Document the skip in the commit body.

    3. **`tools/spdx-header.ts` HASH_PATTERNS extension** — run
       `pnpm spdx:audit-hash` against the staged tree. If it reports the
       new `tests/e2e-cjm/features/phase17-tls.feature` or
       `compose/traefik/.dockerignore` as un-audited (i.e. HASH_PATTERNS
       glob doesn't match them), extend the HASH_PATTERNS array in
       `tools/spdx-header.ts` to include them.

       Per 17-PATTERNS line 184: `.dockerignore` is SCOPE-IN ONLY IF the
       audit complains. If it passes silently, do NOT add `.dockerignore`
       to HASH_PATTERNS — that scope creep is rejected upstream.

       For `.feature` files: cucumber Gherkin `#` comments tolerate the
       SPDX header (the feature file already has `# SPDX-License-Identifier:
       FSL-1.1-ALv2` from Task 5); if HASH_PATTERNS doesn't yet glob
       `**/*.feature`, add that pattern.

       If `tools/spdx-header.ts` is modified, re-run `pnpm vitest run
       tools/__tests__/spdx-header.test.ts` (if it exists) and confirm
       coverage holds ≥ 90/90/90/90.

    Bundle with commit B.
  </action>
  <verify>
    <automated>! grep -B2 -A2 'Phase 17' .planning/ROADMAP.md | grep -q 'PITFALLS §16' && grep -q 'PITFALLS §13' .planning/ROADMAP.md && pnpm spdx:audit-hash</automated>
  </verify>
  <done>No Phase 17 reference to §16 remains; §13 reference is present; `pnpm spdx:audit-hash` exits 0 on the staged tree.</done>
</task>

<task type="auto">
  <name>Task 8: Atomic commit B — isolation evidence (.dockerignore + Gherkin + docs + ref-fix)</name>
  <files>
    - .dockerignore
    - compose/traefik/.dockerignore
    - tests/e2e-cjm/features/phase17-tls.feature
    - tests/e2e-cjm/steps/tls.steps.ts
    - docs/operations.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md (if needed per Task 7)
    - tools/spdx-header.ts (if needed per Task 7)
  </files>
  <action>
    Per CONTEXT Q4 (lines 191-194) — Tasks 4-7 land in ONE atomic commit
    ("isolation evidence atomic").

    1. Stage all files from Tasks 4-7.
    2. Run `lefthook run pre-commit --files <staged>` — ZERO `--no-verify`
       expected. If lefthook fires, HALT and document as ME-02 per CONTEXT
       line 203.
    3. Run `pnpm spdx:audit-hash` + `pnpm spdx:audit-ts` + `pnpm
       lint:dockerfile-tls` + `pnpm cucumber-js --dry-run
       tests/e2e-cjm/features/phase17-tls.feature` as a final pre-commit
       sweep.
    4. Commit message (HEREDOC):

       ```
       feat(17-02): dev-CA isolation evidence (dockerignore + Gherkin + air-gap docs)

       Delivers TLS-05 + TLS-06 (Phase 17 isolation enforcement):

       - Root `.dockerignore` extended with 9-line TLS-05 block excluding
         all dev-CA artefacts (`rootCA*.pem`, `root-ca.*`, `mkcert`,
         `*.localhost.{pem,key}`, `local.{crt,key}`, `compose/traefik/certs/`,
         `.certs/`).
       - NEW `compose/traefik/.dockerignore` — Docker per-context resolution
         requires sibling dockerignore for sub-contexts. FIRST per-context
         dockerignore in the repo; Gherkin scenario 2 is the regression
         guard.
       - NEW `tests/e2e-cjm/features/phase17-tls.feature` — 3 scenarios under
         `@phase-17 @tls`: browser-trust check, prod-image filesystem scan,
         ACME staging issuance. Step defs in `tests/e2e-cjm/steps/tls.steps.ts`;
         live execution gated by `@after-docker-up` (deferred to GHA CI per
         Phase 15-16 precedent).
       - `docs/operations.md#air-gap-mkcert` section authored — macOS / Linux
         binary mirror URLs, checksum verification, PATH installation,
         `mkcert -install` air-gap fallback.
       - `.planning/ROADMAP.md` (+ REQUIREMENTS.md if applicable) PITFALLS
         §16 → §13 reference correction within Phase 17 entry block.
       - [optional] `tools/spdx-header.ts` HASH_PATTERNS extended to cover
         `.feature` so new file audits clean under `pnpm spdx:audit-hash`.

       Refs TLS-05, TLS-06; PITFALLS §13.
       ```

    Predicted ZERO `--no-verify`. If lefthook surprises, escalate.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^feat(17-02): dev-CA isolation' && pnpm lint:dockerfile-tls && pnpm cucumber-js --dry-run tests/e2e-cjm/features/phase17-tls.feature</automated>
  </verify>
  <done>Commit B lands with all Task 4-7 files; both atomic commits (A from Task 3 + B from Task 8) clean lefthook with ZERO `--no-verify`.</done>
</task>

</tasks>

<test_strategy>
- Unit (Vitest, tmpdir): `tools/__tests__/lint-dockerfile-tls.test.ts` ≥ 7
  cases; coverage ≥ 90/90/90/90 on `tools/lint-dockerfile-tls.ts`.
- Integration: `pnpm lint:dockerfile-tls` against real tree exits 0 (current
  12 Dockerfiles have zero violations per CONTEXT lines 64-67).
- E2E: `tests/e2e-cjm/features/phase17-tls.feature` 3 scenarios authored.
  - Scenario 2 (`@cjm-tls-no-dev-ca-in-prod-image`) is runnable in CI WITHOUT
    full docker compose stack-up via `docker create + docker export | tar -t`;
    this is the ONE Phase 17 scenario the verifier executes end-to-end.
  - Scenarios 1 + 3 are `@after-docker-up @expected-red` per Phase 15-16
    precedent — `cucumber-js --dry-run` validates the Gherkin parses; live
    execution deferred to GHA CI stack-up job.
- Coverage waiver: NONE for 17-02 (all new TS code is covered).
</test_strategy>

<deviation_handling>
- **Lint CLI reports >0 violations against the real tree on first run.**
  CONTEXT lines 64-67 assert the current 12 Dockerfiles contain zero
  violations. If the lint trips on, e.g., a comment line `# never COPY
  rootCA.pem`, EXTEND the FORBIDDEN regex with a `^\s*COPY|^\s*ADD|^\s*RUN`
  prefix gate so only command directives match (NOT freeform comments).
  Re-run RED tests, then GREEN, then re-commit. Do NOT add legitimate
  Dockerfile paths to the allowlist.
- **`docker create` not available on executor host (Task 5 scenario 2 step
  impl).** Scenario 2 step impl falls back to `tar -tf <image-tarball>`
  against a pre-exported image. If neither approach works locally, the
  scenario is tagged `@after-docker-up` instead of CI-runnable; document
  the downgrade in 17-SUMMARY.
- **`tests/e2e-cjm/features/phase17-tls.feature` cucumber-js --dry-run fails
  to parse.** Inspect step undefined-snippet output; ensure every Given /
  When / Then in the .feature has a matching step impl in `tls.steps.ts`.
  Cucumber-tsx parse-time requires the step file imports cleanly. If a
  step lookup misses, add the missing step skeleton — do NOT delete
  scenarios.
- **`tools/spdx-header.ts` HASH_PATTERNS extension breaks existing
  hash-header tests.** Read the existing HASH_PATTERNS and the audit test
  fixture first; add `.feature` only if the existing tests do not exercise
  it. If the existing tests would fail, extend the test fixture in the same
  commit. Do NOT silently lower the coverage threshold.
- **ROADMAP `PITFALLS §16` references appear OUTSIDE Phase 17 block (e.g.
  in Phase 5 or Phase 12 entries — legitimately referencing visual
  regression).** Only correct §16→§13 within the Phase 17 block. Verify
  scope via `grep -nB10 'PITFALLS §16'` and require the preceding 10 lines
  contain `Phase 17` heading before applying the substitution.
- **lefthook fires on commit A or B unexpectedly.** HALT, do NOT apply
  `--no-verify`. Document as ME-02 follow-up per CONTEXT line 203. The
  Phase 17 invariant is ZERO `--no-verify`.
</deviation_handling>

<success_criteria>
1. `pnpm vitest run tools/__tests__/lint-dockerfile-tls.test.ts --coverage`
   — ≥ 7 GREEN, coverage ≥ 90/90/90/90 on `tools/lint-dockerfile-tls.ts`.
2. `pnpm lint:dockerfile-tls` exits 0 on the real repo tree.
3. Wiring triad (`package.json`, `lefthook.yml`, `.github/workflows/ci.yml`)
   landed in ONE atomic commit A together with lint CLI + tests + fixtures
   + allowlist.
4. CI's `lint-english` job count is unchanged (line-append only).
5. Root `.dockerignore` contains all 9 new TLS-05 exclusion lines.
6. `compose/traefik/.dockerignore` exists with SPDX header + 5 ignore
   entries.
7. `tests/e2e-cjm/features/phase17-tls.feature` exists with 3 scenarios
   under `@phase-17 @tls` tags; `pnpm cucumber-js --dry-run` parses clean.
8. `tests/e2e-cjm/steps/tls.steps.ts` exists with step defs for all 3
   scenarios; scenario 2 step uses `docker create + tar` recipe.
9. `docs/operations.md#air-gap-mkcert` section exists with all 5 sub-items.
10. `.planning/ROADMAP.md` Phase 17 entry references `PITFALLS §13` (not §16).
11. Atomic commit B landed all .dockerignore + Gherkin + docs + ref-fix
    edits together.
12. ZERO `--no-verify` across both atomic commits A + B.
</success_criteria>

<output>
After completion, create `.planning/phases/17-trusted-local-tls-production-acme-v2/17-02-SUMMARY.md`
per get-shit-done summary template. Include:
- Commit SHAs for atomic commit A (wiring triad) and atomic commit B
  (isolation evidence).
- Coverage figures for `tools/lint-dockerfile-tls.ts`.
- Baseline `pnpm lint:dockerfile-tls` exit code on the real tree (expected 0).
- `pnpm cucumber-js --dry-run tests/e2e-cjm/features/phase17-tls.feature`
  parse result.
- Confirmation of ZERO `--no-verify` on both commits (empirical signal for
  Phase 17 close).
- Note whether `tools/spdx-header.ts` HASH_PATTERNS was extended (and, if
  yes, which globs were added).
- Confirmation that PITFALLS §16 → §13 corrections were applied only within
  the Phase 17 block.
</output>
