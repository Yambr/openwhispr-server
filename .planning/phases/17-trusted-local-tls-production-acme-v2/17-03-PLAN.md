---
phase: 17-trusted-local-tls-production-acme-v2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - compose/traefik/traefik.yml
  - compose/traefik/dynamic.prod.yml
  - compose/docker-compose.acme.yml
  - charts/openwhispr/Chart.yaml
  - charts/openwhispr/values.yaml
  - charts/openwhispr/values.schema.json
  - charts/openwhispr/templates/issuer.yaml
  - charts/openwhispr/templates/certificate-api.yaml
  - charts/openwhispr/templates/certificate-web.yaml
  - charts/openwhispr/tests/issuer_test.yaml
  - charts/openwhispr/tests/tls_test.yaml
  - charts/openwhispr/tests/subcharts_test.yaml
autonomous: true
requirements:
  - TLS-02
  - TLS-03
must_haves:
  truths:
    - "`compose/traefik/traefik.yml` has a NEW top-level `certificatesResolvers:` block inserted AFTER the existing `providers:` block (around line 101-122) and BEFORE the `log:` block (around line 124). The block declares a single resolver `letsencrypt` with HTTP-01 challenge on the `web` entryPoint; `email` reads from `${LETSENCRYPT_EMAIL}` env; `caServer` reads from `${LETSENCRYPT_CA_SERVER:-https://acme-v02.api.letsencrypt.org/directory}` (operator overrides to staging URL by setting `LETSENCRYPT_CA_SERVER` or by `LETSENCRYPT_STAGING=1` indirection via the compose overlay)."
    - "The ACME resolver is INERT until a router opts in via `tls.certResolver: letsencrypt`. The existing `dynamic.yml` `tls.certificates:` block (lines 130-133) does NOT opt in — dev profile keeps using `/certs/local.crt` byte-for-byte unchanged."
    - "NEW `compose/traefik/dynamic.prod.yml` exists, mirroring `dynamic.dev.yml` overlay shape (SPDX hash-header line 1 + Phase 17 comment header + `http:` block). Per-host routers (api, web, app, grafana, mailpit production hostnames overridable via env or operator edit) each declare `tls.certResolver: letsencrypt` — ONE router per host, NO wildcard (D1 invariant)."
    - "NEW `compose/docker-compose.acme.yml` overlay exists at `compose/docker-compose.acme.yml`, mirroring `compose/docker-compose.ingress.yml` shape (no `version:`, no `name:`, services-only top-level + named volumes). Activates ACME via env `LETSENCRYPT_EMAIL` (required) + optional `LETSENCRYPT_STAGING=1` (sets `LETSENCRYPT_CA_SERVER` to staging URL). Adds named volume `letsencrypt:/letsencrypt` to the `traefik` service so ACME state persists. KEEPS the existing `compose/traefik/certs:/certs:ro` bind-mount so dev cert fallback works for routers that did not opt in. Header comment documents that operator MUST publish `:80` to host (HTTP-01 challenge requirement, per 17-PATTERNS risk callout line 402)."
    - "`compose/docker-compose.acme.yml config` (resolved via `docker compose -f docker-compose.yml -f compose/docker-compose.acme.yml config`) succeeds without errors — overlay is well-formed."
    - "`charts/openwhispr/Chart.yaml` has a NEW dependency entry appended to the existing `dependencies:` block (after the valkey + minio entries around lines 46-57): `- name: cert-manager`, `version: \"1.16.4\"`, `repository: \"https://charts.jetstack.io\"`, `condition: certManager.bundled`, `alias: certManager`. The `alias` deliberately uses camelCase `certManager` (NOT lowercase `certmanager`) so the existing `values.yaml` `certManager.*` namespace passes through to the sub-chart's value keys cleanly (per 17-PATTERNS analog §4 line 294)."
    - "`charts/openwhispr/values.yaml` `certManager:` block (currently 3 lines at 354-356) is extended with 6 NEW keys per CONTEXT Q2 lines 132-138: `bundled: false`, `issuerKind: ClusterIssuer`, `renderIssuer: false`, `acmeEmail: \"\"`, `acmeStaging: false`, `installCRDs: true`. Existing `enabled: true` + `clusterIssuer: letsencrypt-prod` keys remain UNCHANGED — brownfield-compatible defaults (no upgrade migration required for operators on the prior chart)."
    - "`charts/openwhispr/values.schema.json` is updated to declare the 6 new `certManager.*` properties (with correct JSON-schema types: `bundled` boolean, `issuerKind` string with enum `[\"ClusterIssuer\", \"Issuer\"]`, `renderIssuer` boolean, `acmeEmail` string, `acmeStaging` boolean, `installCRDs` boolean). Without this update, `helm install --strict` rejects the new keys (per 17-PATTERNS risk callout line 404 — pre-existing gate)."
    - "NEW `charts/openwhispr/templates/issuer.yaml` renders a `(Cluster)Issuer` body ONLY when `tls.enabled && certManager.enabled && certManager.renderIssuer` are all true. Gating expression mirrors `templates/certificate-api.yaml:14`. The `kind:` is templated from `.Values.certManager.issuerKind` (ClusterIssuer default; Issuer alternative adds `metadata.namespace: {{ .Release.Namespace }}`). The `spec.acme.email` uses `required` to fail-fast when `renderIssuer=true` but `acmeEmail` is empty. The `spec.acme.server` flips between staging and prod URL based on `.Values.certManager.acmeStaging`. The HTTP-01 solver `ingress.class` reads from `.Values.ingress.className`."
    - "`charts/openwhispr/templates/certificate-api.yaml:29` and `certificate-web.yaml:28` have `kind: ClusterIssuer` replaced with `kind: {{ .Values.certManager.issuerKind }}`. The default value `ClusterIssuer` preserves byte-for-byte backward compatibility with existing operator deployments (verified by extending the existing `tests/tls_test.yaml` matrix)."
    - "NEW `charts/openwhispr/tests/issuer_test.yaml` exists with the 5-row matrix from 17-PATTERNS analog §8 (lines 363-367): (1) renderIssuer=false default → zero docs rendered; (2) renderIssuer=true + ClusterIssuer → 1 ClusterIssuer doc; (3) issuerKind=Issuer → namespaced Issuer with `.metadata.namespace`; (4) renderIssuer=true + empty acmeEmail → render fails with required-message; (5) acmeStaging=true → server URL is `acme-staging-v02`."
    - "`charts/openwhispr/tests/tls_test.yaml` is extended with at least one assertion: `issuerKind=Issuer` → `certificate-api.yaml` `issuerRef.kind == Issuer` (proves the templated kind switch round-trips through both `issuer.yaml` and `certificate-*.yaml`)."
    - "`charts/openwhispr/tests/subcharts_test.yaml` is extended with `bundled=false` default → no cert-manager sub-chart resources rendered, and `bundled=true + installCRDs=true` → cert-manager CRDs render (validates the brownfield-safe opt-in)."
    - "Both `helm template charts/openwhispr` (default values) and `helm template charts/openwhispr --set certManager.bundled=true --set certManager.renderIssuer=true --set certManager.acmeEmail=ops@example.com` succeed without error. `pnpm helm:unittest` (or `helm unittest charts/openwhispr`) reports all tests GREEN."
    - "Two atomic commits land per CONTEXT Q4 lines 197-199: (a) compose-plane atomic — `compose/traefik/traefik.yml` + `compose/traefik/dynamic.prod.yml` + `compose/docker-compose.acme.yml`; (b) K8s-plane atomic — Chart.yaml + values.yaml + values.schema.json + issuer.yaml + certificate-{api,web}.yaml + 3 helm-unittest test files. ZERO `--no-verify` across both."
    - "`cert-manager` sub-chart version pinned at `1.16.4`; no bump to 1.17.x even if available (CONTEXT deferred #4)."
  artifacts:
    - path: "compose/traefik/traefik.yml"
      provides: "Single ACME HTTP-01 resolver `letsencrypt` (inert until a router opts in)."
      contains: "certificatesResolvers"
    - path: "compose/traefik/dynamic.prod.yml"
      provides: "NEW prod overlay with per-host routers + `certResolver: letsencrypt`."
    - path: "compose/docker-compose.acme.yml"
      provides: "NEW compose overlay; env-driven ACME activation + named-volume persistence."
    - path: "charts/openwhispr/Chart.yaml"
      provides: "Optional cert-manager 1.16.4 sub-chart dep (condition `certManager.bundled`)."
    - path: "charts/openwhispr/values.yaml"
      provides: "Extended `certManager` block with 6 new keys (`bundled`, `issuerKind`, `renderIssuer`, `acmeEmail`, `acmeStaging`, `installCRDs`)."
    - path: "charts/openwhispr/values.schema.json"
      provides: "JSON-schema declarations for the 6 new keys (prevents `helm install --strict` rejection)."
    - path: "charts/openwhispr/templates/issuer.yaml"
      provides: "Renders `(Cluster)Issuer` body gated by `renderIssuer`."
    - path: "charts/openwhispr/templates/certificate-api.yaml"
      provides: "Templated `issuerRef.kind` (was hardcoded `ClusterIssuer`)."
    - path: "charts/openwhispr/templates/certificate-web.yaml"
      provides: "Templated `issuerRef.kind` (was hardcoded `ClusterIssuer`)."
    - path: "charts/openwhispr/tests/issuer_test.yaml"
      provides: "5-row helm-unittest matrix covering renderIssuer × issuerKind × acmeEmail × acmeStaging."
    - path: "charts/openwhispr/tests/tls_test.yaml"
      provides: "Extended with `issuerKind=Issuer` certificate-api round-trip assertion."
    - path: "charts/openwhispr/tests/subcharts_test.yaml"
      provides: "Extended with `bundled=true/false` cert-manager render matrix."
  key_links:
    - from: "compose/traefik/traefik.yml certificatesResolvers.letsencrypt"
      to: "compose/traefik/dynamic.prod.yml routers"
      via: "Resolver INERT until a router opts in via `tls.certResolver: letsencrypt`; dev profile (`dynamic.yml`) does NOT opt in."
      pattern: "certResolver: letsencrypt"
    - from: "compose/docker-compose.acme.yml"
      to: "compose/traefik/traefik.yml"
      via: "Overlay supplies `LETSENCRYPT_EMAIL` + `LETSENCRYPT_CA_SERVER` env to the traefik service; named volume `letsencrypt:/letsencrypt` persists ACME state."
      pattern: "LETSENCRYPT_EMAIL|/letsencrypt"
    - from: "charts/openwhispr/Chart.yaml cert-manager dep"
      to: "charts/openwhispr/values.yaml certManager.bundled"
      via: "Helm `condition: certManager.bundled` controls sub-chart render."
      pattern: "condition: certManager.bundled"
    - from: "charts/openwhispr/templates/issuer.yaml"
      to: "charts/openwhispr/templates/certificate-{api,web}.yaml"
      via: "Issuer kind + name produced by issuer.yaml is consumed by Certificate `issuerRef` — both must use the SAME `.Values.certManager.issuerKind`."
      pattern: "issuerRef.kind|kind: .Values.certManager.issuerKind"
    - from: "charts/openwhispr/values.yaml"
      to: "charts/openwhispr/values.schema.json"
      via: "Schema must declare new keys or `helm install --strict` rejects them (pre-existing gate; PATTERNS critical-correction)."
      pattern: "certManager"
---

<objective>
Deliver production ACME wiring for the docker-compose profile and the
optional bundled cert-manager sub-chart for the Helm profile. Two atomic
commits:

1. **Compose-plane atomic** — extends `compose/traefik/traefik.yml` with a
   single `certificatesResolvers.letsencrypt` HTTP-01 resolver (env-driven
   email + caServer; INERT until a router opts in), authors NEW
   `compose/traefik/dynamic.prod.yml` with per-host routers (no wildcard,
   D1 invariant), and authors NEW `compose/docker-compose.acme.yml`
   overlay activating ACME via `LETSENCRYPT_EMAIL` + optional
   `LETSENCRYPT_STAGING=1`.

2. **K8s-plane atomic** — extends `Chart.yaml` with an optional cert-manager
   1.16.4 sub-chart (condition `certManager.bundled`, default off — brownfield
   safe), extends `values.yaml` with 6 new `certManager.*` keys, updates
   `values.schema.json` to declare those keys (PATTERNS critical-correction —
   else `helm install --strict` rejects them), authors NEW
   `templates/issuer.yaml` rendering a `(Cluster)Issuer` body when
   `renderIssuer=true`, templates `issuerRef.kind` in the existing two
   Certificate templates from hardcoded `ClusterIssuer` to
   `{{ .Values.certManager.issuerKind }}`, and authors / extends three
   helm-unittest files (`issuer_test.yaml` NEW, `tls_test.yaml` extend,
   `subcharts_test.yaml` extend).

Wave 1. Parallel-safe with 17-01 (disjoint file trees per 17-PATTERNS
cross-plan conflict matrix line 415-434). 17-02 sequences AFTER both Wave 1
plans close.

The plan deliberately does NOT touch `compose/traefik/dynamic.yml` or
`compose/traefik/dynamic.dev.yml` — dev profile stays on the mkcert cert
17-01 provisions; only the prod profile opts in to ACME.

Predicted ZERO `--no-verify` per CONTEXT line 203.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/17-trusted-local-tls-production-acme-v2/17-CONTEXT.md
@.planning/phases/17-trusted-local-tls-production-acme-v2/17-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@compose/traefik/traefik.yml
@compose/traefik/dynamic.yml
@compose/traefik/dynamic.dev.yml
@compose/docker-compose.ingress.yml
@compose/docker-compose.observability.yml
@charts/openwhispr/Chart.yaml
@charts/openwhispr/values.yaml
@charts/openwhispr/values.schema.json
@charts/openwhispr/templates/certificate-api.yaml
@charts/openwhispr/templates/certificate-web.yaml
@charts/openwhispr/templates/_helpers.tpl
@charts/openwhispr/tests/tls_test.yaml
@charts/openwhispr/tests/subcharts_test.yaml
@charts/openwhispr/examples/cert-manager-clusterissuer-letsencrypt.yaml
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 1: Extend `compose/traefik/traefik.yml` with `certificatesResolvers.letsencrypt`</name>
  <files>
    - compose/traefik/traefik.yml
  </files>
  <action>
    Per 17-PATTERNS analog §1 (lines 239-252) + CONTEXT Q2-A3:

    1. Locate the `providers:` block at `compose/traefik/traefik.yml:101-122`
       (or wherever it actually lives — read first).
    2. Locate the `log:` block at around line 124.
    3. Insert a NEW top-level `certificatesResolvers:` block BETWEEN
       `providers:` and `log:`:

       ```yaml
       certificatesResolvers:
         letsencrypt:
           acme:
             email: "${LETSENCRYPT_EMAIL}"
             storage: /letsencrypt/acme.json
             caServer: "${LETSENCRYPT_CA_SERVER:-https://acme-v02.api.letsencrypt.org/directory}"
             httpChallenge:
               entryPoint: web
       ```

    4. The resolver reuses the existing `:80 web` entrypoint (declared at
       `traefik.yml:28-39`) for the HTTP-01 challenge. The 308 redirect to
       `:443` does NOT block the challenge because Traefik 3 intercepts
       `/.well-known/acme-challenge/*` BEFORE the redirect middleware fires
       (verified upstream behaviour; 17-PATTERNS risk callout line 402).

    5. Do NOT modify any other block. The existing `# No separate ACME`
       comment (if present at line 17) was about shared-cert reuse and is
       superseded — UPDATE the comment to reflect the new state ("ACME
       resolver added; INERT until a router opts in via
       `tls.certResolver: letsencrypt` in `dynamic.prod.yml`").

    Bundle with the compose-plane atomic commit (Task 3).
  </action>
  <verify>
    <automated>grep -q 'certificatesResolvers:' compose/traefik/traefik.yml && grep -q 'letsencrypt:' compose/traefik/traefik.yml && grep -q 'httpChallenge:' compose/traefik/traefik.yml && grep -q 'entryPoint: web' compose/traefik/traefik.yml && grep -q 'LETSENCRYPT_EMAIL' compose/traefik/traefik.yml</automated>
  </verify>
  <done>traefik.yml has the `certificatesResolvers.letsencrypt` block; YAML parses (`yq . compose/traefik/traefik.yml` or `python -c 'import yaml; yaml.safe_load(open("compose/traefik/traefik.yml"))'` exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: Create `compose/traefik/dynamic.prod.yml` (per-host routers, no wildcard)</name>
  <files>
    - compose/traefik/dynamic.prod.yml (new)
  </files>
  <action>
    Per 17-PATTERNS analog §2 (lines 254-267) + CONTEXT Q2-A3 + D1 (never
    wildcard):

    Mirror `compose/traefik/dynamic.dev.yml` overlay shape:

    1. Line 1: `# SPDX-License-Identifier: FSL-1.1-ALv2`
    2. Lines 2-5: Phase 17 header comment block — explains this is the
       production profile that opts each router into ACME; operator overrides
       host names by editing this file or via env-substitution if the chosen
       compose runtime supports it.
    3. `http:` block with per-host routers (one router per host, NO
       `Host(\`*.example.com\`)` wildcard). Skeleton (operator-overridable
       hostnames):

       ```yaml
       http:
         routers:
           api-prod:
             rule: "Host(`api.${PUBLIC_DOMAIN}`)"
             service: api-svc
             entryPoints: [websecure]
             tls:
               certResolver: letsencrypt
           web-prod:
             rule: "Host(`${PUBLIC_DOMAIN}`)"
             service: web-svc
             entryPoints: [websecure]
             tls:
               certResolver: letsencrypt
           app-prod:
             rule: "Host(`app.${PUBLIC_DOMAIN}`)"
             service: app-svc
             entryPoints: [websecure]
             tls:
               certResolver: letsencrypt
           grafana-prod:
             rule: "Host(`grafana.${PUBLIC_DOMAIN}`)"
             service: grafana-svc
             entryPoints: [websecure]
             tls:
               certResolver: letsencrypt
       ```

    4. Verify the `service` names align with the Phase 14 ingress overlay
       (`compose/docker-compose.ingress.yml`) `services:` declarations.
       Read `docker-compose.ingress.yml` and confirm; if service names
       differ (e.g. `api`, `web`, `app`, `grafana` without `-svc` suffix),
       use the actual names — preserve operator-runnable invariant.
    5. Per CONTEXT D1: NO `Host(\`*.…\`)` rules. ONE router per host.
       Mailpit deliberately omitted from prod profile (it's a dev-only
       inbox; operators that need it in prod copy the api-prod block
       pattern themselves).
  </action>
  <verify>
    <automated>test -e compose/traefik/dynamic.prod.yml && grep -q 'certResolver: letsencrypt' compose/traefik/dynamic.prod.yml && ! grep -q 'Host(`\*\.' compose/traefik/dynamic.prod.yml && grep -c 'certResolver: letsencrypt' compose/traefik/dynamic.prod.yml | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>dynamic.prod.yml exists with ≥3 per-host routers each declaring `certResolver: letsencrypt`; no wildcard rules.</done>
</task>

<task type="auto">
  <name>Task 3: Create `compose/docker-compose.acme.yml` overlay + atomic commit A (compose-plane)</name>
  <files>
    - compose/docker-compose.acme.yml (new)
    - compose/traefik/traefik.yml (from Task 1)
    - compose/traefik/dynamic.prod.yml (from Task 2)
  </files>
  <action>
    Per CONTEXT Q5 (line 207) + 17-PATTERNS analog §3 (lines 271-280):

    1. Create `compose/docker-compose.acme.yml` mirroring
       `compose/docker-compose.ingress.yml` shape — no `version:`, no `name:`,
       services-only top-level + named-volumes block. Skeleton:

       ```yaml
       # SPDX-License-Identifier: FSL-1.1-ALv2
       # Phase 17 — Production ACME activation overlay.
       #
       # Compose with:
       #   docker compose -f docker-compose.yml \
       #     -f compose/docker-compose.ingress.yml \
       #     -f compose/docker-compose.acme.yml up -d --wait
       #
       # Requires:
       #   LETSENCRYPT_EMAIL=ops@example.com  (operator identity to ACME)
       #   PUBLIC_DOMAIN=example.com           (consumed by dynamic.prod.yml router rules)
       # Optional:
       #   LETSENCRYPT_STAGING=1               (flips caServer to LE staging)
       #
       # IMPORTANT: HTTP-01 challenge requires :80 reachable from the public
       # internet. The base compose file's `ports:` for the traefik service
       # MUST publish :80 to the host. Operators behind a NAT must port-forward.

       services:
         traefik:
           command:
             - --providers.file.directory=/etc/traefik/dynamic
             - --providers.file.watch=true
           environment:
             LETSENCRYPT_EMAIL: "${LETSENCRYPT_EMAIL:?LETSENCRYPT_EMAIL is required for ACME overlay}"
             LETSENCRYPT_CA_SERVER: "${LETSENCRYPT_CA_SERVER:-${LETSENCRYPT_STAGING:+https://acme-staging-v02.api.letsencrypt.org/directory}}"
           volumes:
             - letsencrypt:/letsencrypt
             - ./compose/traefik/dynamic.prod.yml:/etc/traefik/dynamic/dynamic.prod.yml:ro

       volumes:
         letsencrypt:
       ```

       Notes:
       - `LETSENCRYPT_EMAIL:?…` syntax fails-fast at `docker compose up`
         time if the operator forgot to set the env (mirrors Phase 14
         operator-friendly precedent).
       - `LETSENCRYPT_CA_SERVER:-${LETSENCRYPT_STAGING:+…staging…}` =
         "if LETSENCRYPT_CA_SERVER explicit, use that; else if
         LETSENCRYPT_STAGING set to anything truthy, use staging URL;
         else fall through to traefik.yml's `:-https://acme-v02…directory`
         default (production)."
       - KEEP the existing `compose/traefik/certs:/certs:ro` bind-mount from
         the ingress overlay — dev cert fallback is harmless when prod
         routers all opt into ACME.

    2. Smoke: `docker compose -f docker-compose.yml -f
       compose/docker-compose.ingress.yml -f compose/docker-compose.acme.yml
       config` (with `LETSENCRYPT_EMAIL=ops@example.com PUBLIC_DOMAIN=example.com`
       set) exits 0. NO actual ACME issuance attempted locally — staging URL
       call would require :80 on the public internet.

    3. **Atomic commit A (compose-plane)** — stage Tasks 1+2+3 files together:
       - `compose/traefik/traefik.yml`
       - `compose/traefik/dynamic.prod.yml`
       - `compose/docker-compose.acme.yml`

       Run `lefthook run pre-commit --files <staged>`. Expected ZERO
       `--no-verify`. Commit:

       ```
       feat(17-03): production ACME via Traefik resolver + docker-compose.acme.yml overlay

       Delivers compose-plane half of TLS-02-prod + TLS-03:

       - `compose/traefik/traefik.yml` gains a single ACME HTTP-01 resolver
         `letsencrypt` (env-driven email + caServer). INERT until a router
         opts in.
       - NEW `compose/traefik/dynamic.prod.yml` declares per-host routers
         (api, web, app, grafana) each with `tls.certResolver: letsencrypt`.
         NO wildcard (D1 invariant).
       - NEW `compose/docker-compose.acme.yml` overlay activates ACME via
         required `LETSENCRYPT_EMAIL` + optional `LETSENCRYPT_STAGING=1`;
         mounts named volume `letsencrypt:/letsencrypt` for ACME state
         persistence. Header documents `:80` reachability requirement.

       Dev profile (`dynamic.yml` + `dynamic.dev.yml`) intentionally
       untouched; mkcert cert from 17-01 remains in effect for `*.localhost`.

       Refs TLS-02, TLS-03.
       ```
  </action>
  <verify>
    <automated>test -e compose/docker-compose.acme.yml && LETSENCRYPT_EMAIL=ops@example.com PUBLIC_DOMAIN=example.com docker compose -f docker-compose.yml -f compose/docker-compose.ingress.yml -f compose/docker-compose.acme.yml config >/dev/null 2>&1; test $? -eq 0 && git log -1 --format='%s' | grep -q '^feat(17-03): production ACME'</automated>
  </verify>
  <done>Atomic commit A landed; `docker compose ... config` resolves the overlay; ZERO `--no-verify`.</done>
</task>

<task type="auto">
  <name>Task 4: Add cert-manager sub-chart dep to `charts/openwhispr/Chart.yaml`</name>
  <files>
    - charts/openwhispr/Chart.yaml
  </files>
  <action>
    Per 17-PATTERNS analog §4 (lines 282-294) + CONTEXT Q2-B3:

    1. Locate the existing `dependencies:` block at `Chart.yaml:46-57` (valkey
       + minio entries).
    2. Append a NEW THIRD entry:

       ```yaml
         - name: cert-manager
           version: "1.16.4"
           repository: "https://charts.jetstack.io"
           condition: certManager.bundled
           alias: certManager
       ```

       The `alias: certManager` camelCase IS deliberate (not a typo) — it
       matches the existing `certManager.*` namespace in `values.yaml` so
       sub-chart value passthrough is keyed consistently
       (e.g. `certManager.installCRDs` → cert-manager's `installCRDs` value).

    3. Run `helm dependency update charts/openwhispr` — fetches the
       cert-manager chart into `charts/openwhispr/charts/` (gitignored). Do
       NOT commit the fetched chart tarball.

    4. Verify `Chart.lock` (if tracked) is regenerated and committed
       alongside Chart.yaml.

    Bundle with the K8s-plane atomic commit (Task 8).
  </action>
  <verify>
    <automated>grep -q 'name: cert-manager' charts/openwhispr/Chart.yaml && grep -q 'version: "1.16.4"' charts/openwhispr/Chart.yaml && grep -q 'condition: certManager.bundled' charts/openwhispr/Chart.yaml && grep -q 'alias: certManager' charts/openwhispr/Chart.yaml</automated>
  </verify>
  <done>Chart.yaml has the cert-manager dep entry with the four expected fields; `helm dependency update` succeeds.</done>
</task>

<task type="auto">
  <name>Task 5: Extend `values.yaml` certManager block + update `values.schema.json`</name>
  <files>
    - charts/openwhispr/values.yaml
    - charts/openwhispr/values.schema.json
  </files>
  <action>
    Per 17-PATTERNS analog §5 (lines 296-313) + critical correction line 404:

    1. **`values.yaml`** — locate the `certManager:` block at lines 354-356:
       ```yaml
       certManager:
         enabled: true
         clusterIssuer: letsencrypt-prod
       ```
       Append 6 NEW keys (preserve the 2 existing keys):
       ```yaml
         bundled: false             # Phase 17 — opt-in to bundled jetstack/cert-manager sub-chart. Default off (brownfield safe).
         issuerKind: ClusterIssuer  # Phase 17 — ClusterIssuer (default; matches existing chart) or Issuer (namespaced).
         renderIssuer: false        # Phase 17 — render (Cluster)Issuer body from this chart. Default off; operator usually applies issuer externally.
         acmeEmail: ""              # Phase 17 — required when renderIssuer=true.
         acmeStaging: false         # Phase 17 — flip to LE staging endpoint when true.
         installCRDs: true          # Phase 17 — passthrough to bundled cert-manager sub-chart (only when bundled=true).
       ```

    2. **`values.schema.json`** — read the existing schema and locate the
       `certManager` object node (likely under `properties.certManager`).
       Add JSON-schema property declarations for the 6 new keys with correct
       types + descriptions:
       ```json
       "bundled":      { "type": "boolean", "default": false, "description": "Opt-in to bundled cert-manager sub-chart." },
       "issuerKind":   { "type": "string",  "enum": ["ClusterIssuer", "Issuer"], "default": "ClusterIssuer" },
       "renderIssuer": { "type": "boolean", "default": false },
       "acmeEmail":    { "type": "string",  "default": "" },
       "acmeStaging":  { "type": "boolean", "default": false },
       "installCRDs":  { "type": "boolean", "default": true }
       ```
       Preserve existing `enabled` + `clusterIssuer` declarations untouched.
       If the existing schema has `"additionalProperties": false` set on the
       `certManager` node, the missing declarations would cause `helm
       install --strict` to reject — that is exactly the gate this task
       closes (17-PATTERNS critical-correction).

    3. Run `helm template charts/openwhispr --strict` — MUST succeed.

    Bundle with K8s-plane atomic commit (Task 8).
  </action>
  <verify>
    <automated>grep -A20 '^certManager:' charts/openwhispr/values.yaml | grep -q 'bundled: false' && grep -A20 '^certManager:' charts/openwhispr/values.yaml | grep -q 'issuerKind: ClusterIssuer' && grep -A20 '^certManager:' charts/openwhispr/values.yaml | grep -q 'renderIssuer: false' && python3 -c "import json; s=json.load(open('charts/openwhispr/values.schema.json')); c=s.get('properties',{}).get('certManager',{}).get('properties',{}); assert 'bundled' in c and 'issuerKind' in c and 'renderIssuer' in c and 'acmeEmail' in c and 'acmeStaging' in c and 'installCRDs' in c"</automated>
  </verify>
  <done>values.yaml + values.schema.json both declare the 6 new keys; `helm template charts/openwhispr --strict` succeeds.</done>
</task>

<task type="auto">
  <name>Task 6: Author NEW `templates/issuer.yaml` + template `issuerRef.kind` in `certificate-{api,web}.yaml`</name>
  <files>
    - charts/openwhispr/templates/issuer.yaml (new)
    - charts/openwhispr/templates/certificate-api.yaml
    - charts/openwhispr/templates/certificate-web.yaml
  </files>
  <action>
    Per 17-PATTERNS analog §6+§7 (lines 315-355):

    1. **NEW `charts/openwhispr/templates/issuer.yaml`** — body per CONTEXT
       Q2-C3 + 17-PATTERNS lines 327-353. Verbatim:

       ```yaml
       {{- /* SPDX-License-Identifier: FSL-1.1-ALv2 */ -}}
       {{- if and .Values.tls.enabled .Values.certManager.enabled .Values.certManager.renderIssuer -}}
       {{- $fullname := include "openwhispr.fullname" . -}}
       apiVersion: cert-manager.io/v1
       kind: {{ .Values.certManager.issuerKind }}
       metadata:
         name: {{ .Values.certManager.clusterIssuer }}
         {{- if eq .Values.certManager.issuerKind "Issuer" }}
         namespace: {{ .Release.Namespace }}
         {{- end }}
         labels:
           {{- include "openwhispr.labels" . | nindent 4 }}
       spec:
         acme:
           email: {{ required "certManager.acmeEmail required when renderIssuer=true" .Values.certManager.acmeEmail }}
           server: {{ if .Values.certManager.acmeStaging }}https://acme-staging-v02.api.letsencrypt.org/directory{{ else }}https://acme-v02.api.letsencrypt.org/directory{{ end }}
           privateKeySecretRef:
             name: {{ $fullname }}-acme-key
           solvers:
             - http01:
                 ingress:
                   class: {{ .Values.ingress.className }}
       {{- end }}
       ```

       Verify that `_helpers.tpl` exposes `openwhispr.fullname` and
       `openwhispr.labels` (17-PATTERNS line 355 — confirmed by grep on
       `_helpers.tpl:35`).

    2. **`charts/openwhispr/templates/certificate-api.yaml:29`** — replace
       `kind: ClusterIssuer` with `kind: {{ .Values.certManager.issuerKind }}`.
       The default value `ClusterIssuer` preserves byte-for-byte backward
       compatibility for operators on the prior chart.

    3. **`charts/openwhispr/templates/certificate-web.yaml:28`** — same edit.

    4. Smoke: `helm template charts/openwhispr` (default values) → output
       contains zero docs from `issuer.yaml` (renderIssuer=false default).
       `helm template charts/openwhispr --set certManager.renderIssuer=true
       --set certManager.acmeEmail=ops@example.com` → output contains one
       ClusterIssuer doc with `apiVersion: cert-manager.io/v1`.

    Bundle with K8s-plane atomic commit (Task 8).
  </action>
  <verify>
    <automated>test -e charts/openwhispr/templates/issuer.yaml && grep -q 'kind: {{ .Values.certManager.issuerKind }}' charts/openwhispr/templates/issuer.yaml && grep -q 'kind: {{ .Values.certManager.issuerKind }}' charts/openwhispr/templates/certificate-api.yaml && grep -q 'kind: {{ .Values.certManager.issuerKind }}' charts/openwhispr/templates/certificate-web.yaml && ! grep -q '^    kind: ClusterIssuer$' charts/openwhispr/templates/certificate-api.yaml</automated>
  </verify>
  <done>issuer.yaml renders ClusterIssuer-by-default; both certificate-*.yaml templates use the templated kind; helm-template smoke under both default and `renderIssuer=true` paths succeeds.</done>
</task>

<task type="auto">
  <name>Task 7: Author + extend helm-unittest matrices (`issuer_test.yaml` NEW; `tls_test.yaml`, `subcharts_test.yaml` extend)</name>
  <files>
    - charts/openwhispr/tests/issuer_test.yaml (new)
    - charts/openwhispr/tests/tls_test.yaml
    - charts/openwhispr/tests/subcharts_test.yaml
  </files>
  <action>
    Per 17-PATTERNS analog §8 (lines 357-373):

    1. **NEW `charts/openwhispr/tests/issuer_test.yaml`** — 5 `it:` rows
       mirroring `tests/tls_test.yaml` shape (release block + tests block):

       - `it: certManager.renderIssuer=false (default) — issuer.yaml renders zero docs`
         set: `certManager.renderIssuer: false` (default), `tls.enabled: true`,
         `certManager.enabled: true`. Assert `templates: [templates/issuer.yaml]`
         renders `documentCount: 0` (or equivalent helm-unittest assertion).
       - `it: tls.enabled=true + certManager.enabled=true + renderIssuer=true — issuer.yaml renders 1 ClusterIssuer`
         set additionally `certManager.renderIssuer: true`,
         `certManager.acmeEmail: ops@example.com`. Assert documentCount 1,
         `kind: ClusterIssuer`, no `metadata.namespace`.
       - `it: certManager.issuerKind=Issuer — issuer.yaml renders namespaced Issuer`
         set `certManager.issuerKind: Issuer`. Assert `kind: Issuer`,
         `metadata.namespace` equals the release namespace.
       - `it: certManager.renderIssuer=true + acmeEmail empty — render fails with required error`
         set `certManager.renderIssuer: true`, leave `acmeEmail: ""`. Assert
         render fails with the `required` message substring `certManager.acmeEmail required`.
       - `it: certManager.acmeStaging=true — server URL is acme-staging-v02`
         set `acmeStaging: true`. Assert `spec.acme.server` contains
         `acme-staging-v02`.

    2. **EXTEND `charts/openwhispr/tests/tls_test.yaml`** — append ONE new
       `it:` row:
       - `it: certManager.issuerKind=Issuer — certificate-api.yaml issuerRef.kind = Issuer`
         set `certManager.issuerKind: Issuer`. Assert
         `templates: [templates/certificate-api.yaml]` produces doc with
         `spec.issuerRef.kind == Issuer`.

    3. **EXTEND `charts/openwhispr/tests/subcharts_test.yaml`** — append TWO
       new `it:` rows:
       - `it: certManager.bundled=false (default) — no cert-manager sub-chart resources`
         set `certManager.bundled: false`. Assert no docs from
         `charts/cert-manager/templates/**/*.yaml`.
       - `it: certManager.bundled=true + installCRDs=true — cert-manager CRDs render`
         set `certManager.bundled: true`, `certManager.installCRDs: true`.
         Assert at least one CRD doc renders. (Note: this requires `helm
         dependency update` to have fetched the sub-chart — Task 4
         precondition.)

    4. Smoke: `helm unittest charts/openwhispr` (or `pnpm helm:unittest` if
       wired in package.json) — all assertions GREEN. If `helm unittest`
       isn't available locally, defer to GHA CI; the helm-template smoke
       from Task 6 + the dry-render gate from Task 5 cover the static
       checks.

    Bundle with K8s-plane atomic commit (Task 8).
  </action>
  <verify>
    <automated>test -e charts/openwhispr/tests/issuer_test.yaml && grep -c '^- it:' charts/openwhispr/tests/issuer_test.yaml | awk '$1 >= 5 {exit 0} {exit 1}' && grep -q 'issuerKind: Issuer' charts/openwhispr/tests/tls_test.yaml && grep -q 'bundled: true' charts/openwhispr/tests/subcharts_test.yaml</automated>
  </verify>
  <done>5+ `it:` rows in issuer_test.yaml; tls_test.yaml + subcharts_test.yaml extended; `helm unittest charts/openwhispr` reports all GREEN (or static smoke gates pass when helm-unittest unavailable locally).</done>
</task>

<task type="auto">
  <name>Task 8: Atomic commit B — K8s-plane (Chart.yaml + values + issuer.yaml + certificates + helm-unittest)</name>
  <files>
    - charts/openwhispr/Chart.yaml
    - charts/openwhispr/values.yaml
    - charts/openwhispr/values.schema.json
    - charts/openwhispr/templates/issuer.yaml
    - charts/openwhispr/templates/certificate-api.yaml
    - charts/openwhispr/templates/certificate-web.yaml
    - charts/openwhispr/tests/issuer_test.yaml
    - charts/openwhispr/tests/tls_test.yaml
    - charts/openwhispr/tests/subcharts_test.yaml
  </files>
  <action>
    Per CONTEXT Q4 lines 197-199 — Tasks 4-7 land in ONE atomic commit
    ("K8s-plane atomic"):

    1. Stage all files from Tasks 4-7. Do NOT stage the fetched cert-manager
       chart tarball (gitignored under `charts/openwhispr/charts/`).
    2. Run `lefthook run pre-commit --files <staged>` — ZERO `--no-verify`
       expected. If lefthook fires, HALT per CONTEXT line 203.
    3. Run final pre-commit sweep:
       - `helm dependency update charts/openwhispr` (idempotent — confirms
         lock state)
       - `helm template charts/openwhispr` (default values)
       - `helm template charts/openwhispr --set certManager.bundled=true --set certManager.renderIssuer=true --set certManager.acmeEmail=ops@example.com`
       - `helm unittest charts/openwhispr` (if available)
       - `pnpm spdx:audit-hash` (verifies issuer.yaml + test yaml headers)
    4. Commit message (HEREDOC):

       ```
       feat(17-03): optional bundled cert-manager sub-chart + (Cluster)Issuer template

       Delivers K8s-plane half of TLS-02-prod + TLS-03:

       - Chart.yaml — NEW optional cert-manager 1.16.4 sub-chart dep
         (condition `certManager.bundled`, default OFF for brownfield
         safety; alias `certManager` aligns sub-chart values namespace
         with existing chart keys).
       - values.yaml — `certManager` block extended with 6 new keys
         (`bundled`, `issuerKind`, `renderIssuer`, `acmeEmail`,
         `acmeStaging`, `installCRDs`). Existing `enabled` + `clusterIssuer`
         unchanged — backward-compatible.
       - values.schema.json — declares the 6 new keys so `helm install
         --strict` doesn't reject (closes a pre-existing gate).
       - NEW templates/issuer.yaml — renders (Cluster)Issuer body gated by
         `tls.enabled && certManager.enabled && certManager.renderIssuer`.
         Issuer kind switchable via `certManager.issuerKind` (ClusterIssuer
         default; Issuer adds `metadata.namespace`). `required` fails
         render-time if `renderIssuer=true` with empty `acmeEmail`.
         `acmeStaging` flips server URL.
       - certificate-{api,web}.yaml — `issuerRef.kind` templated from
         `.Values.certManager.issuerKind` (was hardcoded ClusterIssuer);
         default preserves existing behaviour byte-for-byte.
       - helm-unittest — NEW `tests/issuer_test.yaml` (5 rows: default,
         renderIssuer toggle, issuerKind switch, acmeEmail required-fail,
         acmeStaging URL); EXTEND `tls_test.yaml` (issuerKind round-trip
         through certificate-api); EXTEND `subcharts_test.yaml`
         (bundled=false default / bundled=true CRDs).

       NEVER wildcard (D1). cert-manager pinned at 1.16.4; no 1.17.x bump
       (CONTEXT deferred #4).

       Refs TLS-02, TLS-03.
       ```
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^feat(17-03): optional bundled cert-manager' && helm template charts/openwhispr >/dev/null 2>&1 && helm template charts/openwhispr --set certManager.renderIssuer=true --set certManager.acmeEmail=ops@example.com >/dev/null 2>&1</automated>
  </verify>
  <done>Atomic commit B landed; helm-template smoke under both default and `renderIssuer=true` paths succeeds; ZERO `--no-verify`.</done>
</task>

</tasks>

<test_strategy>
- Unit: NONE — this plan is YAML / JSON-schema / helm-template only. No
  TypeScript code; CLAUDE.md ≥90/90/90/90 coverage rule does not apply.
- Integration: `helm template charts/openwhispr` (default values) AND
  `helm template charts/openwhispr --set certManager.renderIssuer=true
  --set certManager.acmeEmail=ops@example.com` both succeed at Task 6 +
  Task 8 verify steps.
- Helm-unittest: `tests/issuer_test.yaml` 5 rows + `tls_test.yaml` extension
  + `subcharts_test.yaml` extension. Live execution gated by
  `helm unittest charts/openwhispr` (or `pnpm helm:unittest` if wired).
  When unavailable locally, GHA CI runs the suite — static helm-template
  smoke covers the regression gate locally.
- E2E: 17-02 Gherkin scenario 3 (`@cjm-tls-acme-staging`) is the
  end-to-end gate; tagged `@after-docker-up @expected-red` per Phase 15-16
  precedent. Live execution deferred to GHA CI.
- Compose-resolve smoke: `docker compose -f docker-compose.yml -f
  compose/docker-compose.ingress.yml -f compose/docker-compose.acme.yml
  config` exits 0 at Task 3 verify step.
- Coverage waiver: explicit — YAML / JSON / Helm templates are outside the
  TS-only diff-coverage gate.
</test_strategy>

<deviation_handling>
- **`compose/traefik/traefik.yml` `providers:` block lives at different
  lines than 101-122 (refactor drift).** Re-grep `^providers:` then insert
  the `certificatesResolvers:` block immediately after the `providers:`
  block ends (next top-level key). Preserve YAML indentation conventions
  of the existing file.
- **`values.schema.json` does NOT have a `certManager` node at all (pre-Phase-17
  schema didn't cover it).** Add the `certManager` object node with `type:
  object`, `additionalProperties: false`, and the 8 properties (2 existing +
  6 new). Set `required: []` (nothing required at chart-render time;
  `acmeEmail` enforcement happens at helm-render time via `required`).
- **`values.schema.json` is missing entirely OR is gitignored.** If
  missing, the gate isn't pre-existing — note the omission in 17-SUMMARY
  and proceed without the schema edit. Set `helm install --strict`
  expectations accordingly.
- **`helm dependency update` fails (no internet, registry blocked).**
  Per CONTEXT deferred #4, the sub-chart is optional. Skip the fetch step;
  Chart.yaml dep entry is declarative only — `condition: certManager.bundled`
  defaults OFF so the chart still renders cleanly without the tarball.
  Document the deferred fetch in 17-SUMMARY.
- **`helm unittest` not installed.** Defer all helm-unittest execution to
  GHA CI (`pnpm helm:unittest` or `helm unittest` step). The static
  helm-template smokes at Task 6 + Task 8 still gate the regression
  surface locally.
- **`docker compose ... config` requires the base `docker-compose.yml` to
  declare a `traefik` service.** Read the existing
  `compose/docker-compose.ingress.yml` first; if the base file has no
  `traefik` service (it's defined only in the ingress overlay), the acme
  overlay must come AFTER the ingress overlay in the `-f` chain (encoded
  in the acme.yml header comment). Verify the smoke command order.
- **lefthook fires on commit A or B unexpectedly.** HALT, do NOT apply
  `--no-verify`. Document as ME-02 per CONTEXT line 203. The Phase 17
  invariant is ZERO `--no-verify`.
- **`helm template --strict` rejects with "additionalProperties" error on a
  key that 17-03 does NOT introduce.** That is pre-existing drift; flag in
  17-SUMMARY, fix in a follow-up plan or unrelated commit. Do NOT extend
  17-03 scope.
</deviation_handling>

<success_criteria>
1. `compose/traefik/traefik.yml` has a `certificatesResolvers.letsencrypt`
   block with HTTP-01 challenge on `web` entrypoint; resolver INERT
   (dev profile unchanged).
2. `compose/traefik/dynamic.prod.yml` exists with ≥3 per-host routers each
   declaring `certResolver: letsencrypt`; ZERO wildcard rules.
3. `compose/docker-compose.acme.yml` exists; `docker compose ... config`
   resolves the overlay with `LETSENCRYPT_EMAIL` + `PUBLIC_DOMAIN` set.
4. `charts/openwhispr/Chart.yaml` declares cert-manager 1.16.4 sub-chart
   dep with `condition: certManager.bundled` (default OFF) and `alias:
   certManager`.
5. `charts/openwhispr/values.yaml` `certManager:` block contains all 8
   keys (2 existing + 6 new).
6. `charts/openwhispr/values.schema.json` declares all 6 new keys with
   correct types + the `issuerKind` enum.
7. `charts/openwhispr/templates/issuer.yaml` exists; renders zero docs by
   default, one doc with `renderIssuer=true`, namespaced Issuer when
   `issuerKind=Issuer`, fails render-time when `acmeEmail` empty +
   `renderIssuer=true`, server URL flips on `acmeStaging`.
8. `certificate-{api,web}.yaml` use templated `issuerRef.kind`; default
   `ClusterIssuer` preserves prior behaviour.
9. `tests/issuer_test.yaml` (5 rows), extended `tls_test.yaml`, and
   extended `subcharts_test.yaml` cover the matrix.
10. `helm template charts/openwhispr` succeeds under default values AND
    under `--set certManager.renderIssuer=true --set
    certManager.acmeEmail=ops@example.com`.
11. Two atomic commits land — compose-plane commit A + K8s-plane commit B
    (either order, disjoint file trees per 17-PATTERNS cross-plan matrix).
12. ZERO `--no-verify` across both commits.
13. cert-manager version pinned exactly at `1.16.4` (no 1.17.x bump).
</success_criteria>

<output>
After completion, create `.planning/phases/17-trusted-local-tls-production-acme-v2/17-03-SUMMARY.md`
per get-shit-done summary template. Include:
- Commit SHAs for atomic commit A (compose-plane) and atomic commit B
  (K8s-plane).
- Output of `docker compose -f docker-compose.yml -f
  compose/docker-compose.ingress.yml -f compose/docker-compose.acme.yml
  config` (success / error excerpt only).
- Output of `helm template charts/openwhispr` (default) + `helm template
  charts/openwhispr --set certManager.renderIssuer=true --set
  certManager.acmeEmail=ops@example.com`.
- `helm unittest` result (or noted as deferred to GHA CI).
- Confirmation that the dev profile (`dynamic.yml` + `dynamic.dev.yml`)
  was NOT touched (git diff stats for both commits show zero entries for
  those paths).
- Forward-reference to 17-02 Gherkin scenario 3 (`@cjm-tls-acme-staging`)
  that exercises this plan's output end-to-end in GHA CI.
- Empirical lefthook result on both commits (expected: clean / ZERO
  `--no-verify`).
</output>
