---
phase: 02.7
plan: 02
type: tdd
wave: 2
depends_on: ["02.7-01"]
files_modified:
  - apps/api/src/lib/mint-bearer.ts
  - apps/api/src/lib/mint-bearer.test.ts
  - apps/api/src/__tests__/oauth-channel-scheme-mint-bearer.test.ts
autonomous: true
requirements: [TDD-01, TDD-01b, AUTH-A1, AUTH-02, CONTRACT-01]
must_haves:
  truths:
    - "apps/api/src/lib/mint-bearer.ts performs direct OIDC token exchange (NOT auth.handler('/api/auth/oauth2/callback/...') delegation — that path 400s on state_not_found per RESEARCH point 1)"
    - "mintBearer lowercases the IdP-returned email BEFORE calling internalAdapter.findUserByEmail or createOAuthUser (D-03 alignment — createOAuthUser does NOT auto-lowercase per RESEARCH §D-03 point 5)"
    - "mintBearer obtains internalAdapter via `await auth.$context` then `ctx.internalAdapter` (verified path)"
    - "mintBearer returns the raw 32-char session.token from internalAdapter.createSession() — bearer plugin self-signs on receive per RESEARCH §D-01 'Bearer plugin token format'"
    - "Existing auth-callback.ts route invokes mintBearer and passes the token into buildProtocolRedirect; final 302 emits `<scheme>://?bearer_token=<urlencoded>` per AUTH-02"
    - "Tests cover: token-exchange POST shape (form body grant_type/code/redirect_uri/code_verifier/client_id/client_secret), userinfo fetch with bearer header, lowercased-email upsert, returned bearer matches fake session.token"
  artifacts:
    - path: "apps/api/src/lib/mint-bearer.ts"
      provides: "Real OIDC mintBearer adapter (D-01); replaces broken auth.handler delegation"
      contains: "internalAdapter"
    - path: "apps/api/src/__tests__/oauth-channel-scheme-mint-bearer.test.ts"
      provides: "End-to-end mintBearer test with stubbed fetch + fake $context"
  key_links:
    - from: "apps/api/src/routes/auth-callback.ts"
      to: "apps/api/src/lib/mint-bearer.ts"
      via: "import { mintBearer }"
      pattern: "mintBearer"
    - from: "apps/api/src/lib/mint-bearer.ts"
      to: "auth.$context.internalAdapter"
      via: "await auth.$context"
      pattern: "auth.\\$context"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
GREEN AUTH-02: rewrite `apps/api/src/lib/mint-bearer.ts` to perform real OIDC token exchange + Better Auth internalAdapter user upsert + session creation, returning the raw session.token. Replaces the existing stub which delegates to `auth.handler('/api/auth/oauth2/callback/...')` and ALWAYS fails with `state_not_found` (Better Auth callback reads PKCE state from its own `verification` table, not our `oauth_state` — verified in RESEARCH point 1).

Purpose: Closes AUTH-A1 (deferred from Phase 02 Plan 05) and unblocks the OAuth contract tests (oauth-redirect.test.ts expected wire shape `<scheme>://?bearer_token=`).

Output: Rewritten mint-bearer.ts + rewritten mint-bearer.test.ts + new end-to-end test. Atomic commit `feat(02.7): D-01 — real mintBearer via internalAdapter + IdP token exchange (closes AUTH-A1)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-CONTEXT.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-RESEARCH.md
@apps/api/src/lib/mint-bearer.ts
@apps/api/src/lib/mint-bearer.test.ts
@apps/api/src/routes/auth-callback.ts
@apps/api/src/routes/desktop-signin.ts
@apps/api/src/lib/scheme-allowlist.ts
@packages/contract-tests/src/oauth-redirect.test.ts
@apps/api/src/__tests__/auth-trusted-origins.test.ts

<interfaces>
<!-- Better Auth internal API surface (verified against installed better-auth@1.6.9 — RESEARCH §D-01) -->

From better-auth dist/auth/base.mjs:33:
```ts
auth.$context: Promise<AuthContext>
```

From @better-auth/core/dist/types/context.d.mts:200 + better-auth dist/db/internal-adapter.mjs:
```ts
ctx.internalAdapter: {
  findUserByEmail(email: string, options?: unknown): Promise<{user: User, accounts: Account[]} | null>;  // line 444
  createOAuthUser(
    user: {email: string; name: string; emailVerified: boolean; image?: string | null},
    account: {providerId: string; accountId: string; accessToken?: string; idToken?: string | null; scope?: string}
  ): Promise<{user: User, account: Account}>;  // line 39 — NOTE: does NOT auto-lowercase email
  createUser(user: {email: string; ...}): Promise<User>;  // line 62 — DOES auto-lowercase email
  createSession(userId: string, dontRememberMe?: boolean, override?: unknown, overrideAll?: unknown): Promise<Session>;  // line 141 — Session.token is raw 32-char string
}
```

From better-auth dist/plugins/bearer/index.mjs:32-37:
```js
// before hook: raw 32-char tokens (no ".") are self-signed via serializeSignedCookie
// when options?.requireSignature is unset (we do not set it).
// → Returning session.token directly is safe; bearer plugin signs on receive.
```

From apps/api/src/lib/scheme-allowlist.ts (already shipped Plan 05):
```ts
export function buildProtocolRedirect(scheme: string, bearerToken: string): string;
// → returns "openwhispr://?bearer_token=<urlencoded>" matching contract regex
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify A2 — confirm internalAdapter shape before any code edit</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs (lines 39, 62, 141, 444 per RESEARCH)
  </read_first>
  <action>
    Run from repo root:
    ```
    node -e "import('./apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs').then(m => console.log('export keys:', Object.keys(m)))"
    grep -nE "(createOAuthUser|findUserByEmail|createSession|createUser)\\s*[:=]" apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs | head -20
    ```
    Confirm the four methods exist at the documented lines and signatures match the <interfaces> block. If signatures DIFFER (Assumption A2 fails), pause and revise the Task 2/3 plan; otherwise proceed unchanged.
  </action>
  <verify>
    <!-- Note: this verify confirms method-name PRESENCE only (A2 existence check). Parameter-shape / signature contract enforcement is delegated to Task 3 GREEN tests: any drift in createOAuthUser / findUserByEmail / createSession parameter arity or shape will break the vi.fn() mocks set up in Task 2 (mint-bearer.test.ts assertions on .toHaveBeenCalledWith(...)) and fail Task 3 GREEN. If signature drift is suspected here, run the optional probe: `node -e "import('./apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs').then(m => console.log(m))"` and inspect arity, then revise Task 2 mocks before proceeding. -->
    <automated>grep -q "createOAuthUser" apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs && grep -q "findUserByEmail" apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs && grep -q "createSession" apps/api/node_modules/better-auth/dist/db/internal-adapter.mjs</automated>
  </verify>
  <acceptance_criteria>All four method names present in installed source; A2 confirmed; or revisions documented in plan SUMMARY before proceeding.</acceptance_criteria>
  <done>A2 confirmed via grep; ready to proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — rewrite mint-bearer.test.ts + new oauth-channel-scheme-mint-bearer.test.ts</name>
  <files>apps/api/src/lib/mint-bearer.test.ts, apps/api/src/__tests__/oauth-channel-scheme-mint-bearer.test.ts</files>
  <read_first>
    - apps/api/src/lib/mint-bearer.test.ts (the four old assertions that delegate to auth.handler — these get DELETED and rewritten)
    - apps/api/src/__tests__/auth-trusted-origins.test.ts (closest existing pattern: vi.hoisted + vi.mock + vi.stubGlobal)
    - packages/contract-tests/src/oauth-redirect.test.ts line 82 (expected wire-shape regex `^openwhispr://\?bearer_token=`)
  </read_first>
  <behavior>
    apps/api/src/lib/mint-bearer.test.ts (REWRITE — drop old four assertions):
    - Test 1: token-exchange POST shape — vi.stubGlobal('fetch', spy); call mintBearer({provider:'oidc', code:'abc', codeVerifier:'verifier-xyz', scheme:'openwhispr'}); assert spy called with tokenEndpoint URL, method POST, content-type form-urlencoded, body containing grant_type=authorization_code, code=abc, code_verifier=verifier-xyz, redirect_uri ends with /api/auth/desktop-callback/oidc, client_id + client_secret from env
    - Test 2: userinfo fetch — assert spy called with userinfoEndpoint and Authorization: Bearer <access_token>
    - Test 3: lowercased-email upsert — IdP returns email "Mixed@Case.test"; assert internalAdapter.findUserByEmail called with "mixed@case.test" (lowercased) AND createOAuthUser called with email "mixed@case.test"
    - Test 4: existing-user path — findUserByEmail returns {user:{id:'u1'},accounts:[]}; createOAuthUser NOT called; createSession called with 'u1'; returned bearer === fake session.token
    - Test 5: new-user path — findUserByEmail returns null; createOAuthUser called with provider/accountId; createSession called with new user.id
    - Test 6: token-exchange failure — fetch returns 400; assert mintBearer throws Error matching /token exchange 400/

    apps/api/src/__tests__/oauth-channel-scheme-mint-bearer.test.ts (NEW):
    - End-to-end via app.inject(): build a Fastify instance with auth-callback route registered; mock auth.$context to return fake internalAdapter; stub fetch for IdP exchange; pre-seed a CAS-consumable oauth_state row with scheme='openwhispr-dev'; inject GET /api/auth/desktop-callback/oidc?code=abc&state=<state>; assert response.statusCode === 302 AND response.headers.location matches /^openwhispr-dev:\/\/\?bearer_token=/

    Each test docstring includes:
      Source-of-record commit: <filled at commit time>
      Reverts: restoring auth.handler delegation in mint-bearer.ts → state_not_found 400 → all six unit tests RED + e2e test RED.
  </behavior>
  <action>
    Use the vi.hoisted + vi.mock pattern from auth-trusted-origins.test.ts. Build the fake `auth` object literal-style:
    ```ts
    const fakeInternalAdapter = {
      findUserByEmail: vi.fn(),
      createOAuthUser: vi.fn(),
      createSession: vi.fn().mockResolvedValue({ token: 'a'.repeat(32), userId: 'u1' }),
    };
    const fakeAuth = { $context: Promise.resolve({ internalAdapter: fakeInternalAdapter }) };
    ```
    For env: vi.stubEnv('OIDC_CLIENT_ID','x'); vi.stubEnv('OIDC_CLIENT_SECRET','y'); vi.stubEnv('OIDC_TOKEN_URL','https://idp.test/token'); vi.stubEnv('OIDC_USERINFO_URL','https://idp.test/userinfo'); vi.stubEnv('AUTH_URL','https://api.localhost').

    Run `pnpm -F @openwhispr/api test mint-bearer oauth-channel-scheme-mint-bearer` and CONFIRM RED (current stub doesn't import internalAdapter / doesn't call fetch / doesn't lowercase). Do NOT modify mint-bearer.ts in this task.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts 2>&1 | grep -qE "FAIL" || (echo "must be RED" && exit 1)</automated>
  </verify>
  <acceptance_criteria>Both test files compile + run + multiple assertions FAIL referencing missing internalAdapter / fetch / lowercase logic. mint-bearer.ts UNCHANGED in this commit step.</acceptance_criteria>
  <done>RED witnesses captured; ready for GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — rewrite apps/api/src/lib/mint-bearer.ts to real OIDC + internalAdapter</name>
  <files>apps/api/src/lib/mint-bearer.ts</files>
  <read_first>
    - apps/api/src/lib/mint-bearer.ts entire file (the broken auth.handler delegation — to be deleted)
    - apps/api/src/routes/auth-callback.ts lines 213-231 (mintBearer call site — verify the function signature contract: what args are passed in, what return type is consumed)
    - apps/api/src/routes/desktop-signin.ts lines 128-148 (oauth_state row creation — confirm OIDC_AUTHORIZE_URL pattern; mint-bearer mirrors with OIDC_TOKEN_URL + OIDC_USERINFO_URL)
  </read_first>
  <action>
    Rewrite mint-bearer.ts using the EXACT template from RESEARCH §D-01 "Recommended (plain fetch)" code block. Key requirements:
    - Use plain `globalThis.fetch` (Node 24 native) — NO new deps
    - Read tokenEndpoint from `process.env.OIDC_TOKEN_URL` (operator override path; documented in desktop-signin.ts:148 already)
    - Read userinfoEndpoint from `process.env.OIDC_USERINFO_URL`
    - Read OIDC_CLIENT_ID + OIDC_CLIENT_SECRET from env (throw if missing — fail fast)
    - redirect_uri MUST be `${process.env.AUTH_URL}/api/auth/desktop-callback/${args.provider}` (matches desktop-signin's PKCE registration)
    - Lowercase email BEFORE calling findUserByEmail AND BEFORE createOAuthUser (per D-03 alignment + RESEARCH §D-03 point 5: createOAuthUser does NOT auto-lowercase)
    - Get internalAdapter via `const ctx = await auth.$context; const ia = ctx.internalAdapter;`
    - Existing user → reuse user.id; new user → createOAuthUser
    - createSession(userId, false) — return session.token raw (NOT signed; bearer plugin self-signs on receive)
    - Throw on token-exchange non-2xx with descriptive Error including status

    Function signature MUST match the existing call site in auth-callback.ts (do NOT change the consumer). Verify by re-reading auth-callback.ts lines 213-231 BEFORE the edit.

    After edit, run:
    1) `cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts` — assert ALL GREEN
    2) `cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts --coverage --coverage.include='src/lib/mint-bearer.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 --coverage.thresholds.statements=90 --coverage.thresholds.functions=90` — assert ≥90% per-file coverage on mint-bearer.ts
    3) `pnpm -F @openwhispr/api test` — assert no regression in other test files (auth-callback.test.ts in particular must stay green)

    NO `as never` casts; NO `// @ts-ignore`; NO `--legacy` flags. If TypeScript complains about `auth` typing on $context, write a proper type assertion using Better Auth's exported `Auth` type or define a narrow `AuthLike` interface in mint-bearer.ts itself.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts && pnpm vitest run src/lib/mint-bearer.test.ts --coverage --coverage.include='src/lib/mint-bearer.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 --coverage.thresholds.statements=90 --coverage.thresholds.functions=90 && cd .. && ! grep -nE "as never|@ts-ignore" apps/api/src/lib/mint-bearer.ts</automated>
  </verify>
  <acceptance_criteria>All tests green; ≥90% coverage on mint-bearer.ts; no `as never` / `@ts-ignore`; auth-callback.test.ts still green.</acceptance_criteria>
  <done>mintBearer is real; AUTH-02 wire shape achievable end-to-end (Plan 06 verifies via contract test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OIDC IdP → mintBearer | IdP responses (token + userinfo) are untrusted JSON |
| mintBearer → internalAdapter | Lowercased email is the canonical user identity |
| mintBearer return → auth-callback route | session.token must be opaque/random per AUTH-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.7-05 | Spoofing | IdP impersonation via SSRF on OIDC_TOKEN_URL | accept | Operator-controlled env; v1 single-tenant; SSRF defense is Phase 6 work |
| T-02.7-06 | Tampering | Open redirect via attacker-controlled scheme | mitigate | scheme is read from server-side oauth_state row (existing Plan 05); never from query — verified by re-using buildProtocolRedirect |
| T-02.7-07 | Information Disclosure | IdP error body leaked in thrown Error | mitigate | Error message includes only status code + provider name, NOT response body |
| T-02.7-08 | Elevation of Privilege | findUserByEmail collision via case-mixed email | mitigate | explicit .toLowerCase() before lookup AND before createOAuthUser; D-03 Layer B (Plan 05) adds DB-level functional unique index defense in depth |
</threat_model>

<verification>
- `pnpm -F @openwhispr/api test` exits 0 (all api tests including mint-bearer + e2e)
- ≥90% line/branch coverage on apps/api/src/lib/mint-bearer.ts
- `! grep -nE "auth.handler\\(['\"]\\/api\\/auth\\/oauth2" apps/api/src/lib/mint-bearer.ts` (broken delegation removed)
- `grep -q "internalAdapter" apps/api/src/lib/mint-bearer.ts` (real path present)
- `grep -q "toLowerCase" apps/api/src/lib/mint-bearer.ts` (D-03 alignment)
</verification>

<success_criteria>
- mintBearer rewrite complete; old delegation gone; tests green
- Coverage ≥90% on mint-bearer.ts
- Atomic commit: `feat(02.7): D-01 — real mintBearer via internalAdapter + IdP token exchange (closes AUTH-A1)`
</success_criteria>

<output>
After completion, create `.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-02-SUMMARY.md` per template.
</output>
