---
phase: 02.7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bootstrap.sh
  - Makefile
  - packages/contract-tests/src/env.ts
  - tests/unit/bootstrap-cert-gen.test.ts
  - tests/unit/probe-backend-redirect-error.test.ts
autonomous: true
requirements: [TDD-01, TDD-01b, CONTRACT-01, WIRE-20]
must_haves:
  truths:
    - "tools/bootstrap.sh generates compose/traefik/certs/{local.crt,local.key} idempotently with SANs covering *.localhost, localhost, *.example.test, api.example.test, auth.example.test, 127.0.0.1, ::1"
    - "Cert is regenerated only if missing OR expires within 30 days; openssl x509 -checkend $((86400*30)) gates regeneration"
    - "Makefile contract-test target runs against https://api.localhost with NODE_EXTRA_CA_CERTS=$(PWD)/compose/traefik/certs/local.crt — NO http:// and NO NODE_TLS_REJECT_UNAUTHORIZED=0"
    - "Makefile contract-test-deployed target refuses to run when NODE_TLS_REJECT_UNAUTHORIZED=0 (echoes refusal + exit 1)"
    - "packages/contract-tests/src/env.ts AUTH_URL default is BACKEND_URL (collapsed per D-04); env override is preserved"
    - "packages/contract-tests/src/env.ts probeBackend() passes redirect: 'error' to fetch — http→https 308 surfaces as caught TypeError → returns false (not silent skip-25)"
    - "All five new/modified test files pass: tests/unit/bootstrap-cert-gen.test.ts, tests/unit/probe-backend-redirect-error.test.ts (and existing tests stay green)"
  artifacts:
    - path: "tools/bootstrap.sh"
      provides: "openssl SAN cert-gen block (D-05) — closes Phase 02.6 NODE_TLS_REJECT_UNAUTHORIZED workaround"
      contains: "openssl req -x509 -nodes -days 3650"
    - path: "Makefile"
      provides: "https contract-test target + refuse-on-NODE_TLS guard"
      contains: "NODE_EXTRA_CA_CERTS"
    - path: "packages/contract-tests/src/env.ts"
      provides: "AUTH_URL default collapse + probeBackend redirect:'error'"
      contains: "redirect: \"error\""
    - path: "tests/unit/bootstrap-cert-gen.test.ts"
      provides: "RED-then-GREEN test for D-05 cert-gen idempotence + SAN coverage"
    - path: "tests/unit/probe-backend-redirect-error.test.ts"
      provides: "RED-then-GREEN test for D-05 probeBackend loud-fail on 308"
  key_links:
    - from: "Makefile contract-test"
      to: "compose/traefik/certs/local.crt"
      via: "NODE_EXTRA_CA_CERTS env var"
      pattern: "NODE_EXTRA_CA_CERTS=\\$\\(PWD\\)/compose/traefik/certs/local.crt"
    - from: "tools/bootstrap.sh"
      to: "compose/traefik/certs/{local.crt,local.key}"
      via: "openssl req -x509"
      pattern: "openssl req -x509"
---

<objective>
Wave 1 infra: land the openssl SAN cert generation in `tools/bootstrap.sh` (closes the missing contract documented at `compose/traefik/dynamic.yml:4` + `compose/traefik/certs/.gitkeep`), rewrite the Makefile contract-test target to use HTTPS + NODE_EXTRA_CA_CERTS (no NODE_TLS_REJECT_UNAUTHORIZED workaround), collapse AUTH_URL default to BACKEND_URL (D-04), and harden probeBackend with `redirect: 'error'` (D-05).

Purpose: Without this plan, all subsequent contract tests still fail at Traefik (TLS) or 404 (auth.localhost). Both env.ts touches (D-04 default + D-05 probe redirect) are folded here per RESEARCH "Critical: env.ts collision" note.

Output: 3 production file changes (bootstrap.sh, Makefile, env.ts) + 2 new test files. Atomic commit `feat(02.7): D-04+D-05 — bootstrap cert-gen + https contract-test + AUTH_URL collapse + probe loud-fail`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-CONTEXT.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-RESEARCH.md
@tools/bootstrap.sh
@Makefile
@packages/contract-tests/src/env.ts
@compose/traefik/dynamic.yml
@compose/traefik/traefik.yml
@tests/unit/bootstrap-interpolate.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for cert-gen + probeBackend</name>
  <files>tests/unit/bootstrap-cert-gen.test.ts, tests/unit/probe-backend-redirect-error.test.ts</files>
  <read_first>
    - tests/unit/bootstrap-interpolate.test.ts (Phase 02.4 G1 reference pattern: execFileSync('bash', [SCRIPT]) inside mkdtemp + BOOTSTRAP_REPO_ROOT)
    - tools/bootstrap.sh (verify it has ZERO openssl cert-gen logic right now — test must RED on this absence)
    - packages/contract-tests/src/env.ts (current probeBackend WITHOUT redirect:'error')
  </read_first>
  <behavior>
    bootstrap-cert-gen.test.ts:
    - Test 1: First run — execFileSync bash bootstrap.sh in mkdtemp; assert compose/traefik/certs/local.crt + local.key exist; assert openssl x509 -text shows DNS SANs (localhost, *.localhost, api.localhost, auth.localhost, grafana.localhost, minio-console.localhost, mailpit.localhost, api.example.test, auth.example.test, *.example.test) and IP SANs (127.0.0.1, ::1)
    - Test 2: Idempotence — record local.crt mtime; re-run bootstrap.sh; assert mtime unchanged (cert valid >30 days → skip regeneration)
    - Test 3: Expiry trigger — overwrite local.crt with a near-expiry cert (e.g., openssl -days 10) → re-run bootstrap.sh → assert new mtime AND new validity ≥365 days
    - Test 4: Key permissions — assert local.key is mode 600
    probe-backend-redirect-error.test.ts:
    - Test 1: vi.stubGlobal('fetch', vi.fn().mockResolvedValue(new Response(null, {status: 308, headers: {Location: 'https://api.localhost/api/health'}}))); assert probeBackend() returns false (NOT true, NOT throws)
    - Test 2: vi.stubGlobal('fetch', vi.fn().mockResolvedValue(new Response('ok', {status: 200}))); assert probeBackend() returns true (regression guard for happy path)
    Each test docstring includes:
      Source-of-record commit: <filled at commit time>
      Reverts: deleting/no-op-ing the openssl block in bootstrap.sh restores RED on test 1; removing redirect:'error' from probeBackend restores RED on probe test 1.
  </behavior>
  <action>
    Create both files. Use vitest 4 syntax matching tests/unit/bootstrap-interpolate.test.ts. For cert-gen use `execFileSync('bash', [path.join(repoRoot,'tools/bootstrap.sh')], {cwd: tmpdir, env: {...process.env, BOOTSTRAP_REPO_ROOT: tmpdir, ...}, stdio: 'pipe'})`. For SAN parsing call `execFileSync('openssl', ['x509','-in', certPath, '-noout','-text'])` and grep DNS:/IP: lines. Use vi.stubGlobal('fetch', ...) per the existing pattern in apps/api/src/__tests__/auth-trusted-origins.test.ts. Run `pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts tests/unit/probe-backend-redirect-error.test.ts` and CONFIRM RED before proceeding. Do NOT modify production code in this task.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts tests/unit/probe-backend-redirect-error.test.ts 2>&1 | grep -E "FAIL|RED" || (echo "tests must FAIL RED before GREEN task" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    Both test files compile + run + ALL assertions FAIL with messages matching the missing production logic (no openssl block, no redirect:'error'). vitest exits non-zero. No production file changes in this task.
  </acceptance_criteria>
  <done>Two RED test files committed-ready; vitest exits non-zero on the targeted files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — bootstrap.sh openssl cert-gen + Makefile https rewrite + env.ts AUTH_URL collapse + probeBackend redirect:'error'</name>
  <files>tools/bootstrap.sh, Makefile, packages/contract-tests/src/env.ts</files>
  <read_first>
    - tools/bootstrap.sh entire file (verify the BACKUP_AGE_IDENTITY block location; cert-gen appends AFTER it per RESEARCH "Add to tools/bootstrap.sh BEFORE the final exit (after the BACKUP_AGE_IDENTITY block, after line 262)")
    - Makefile lines 75-89 (current contract-test + contract-test-deployed targets)
    - packages/contract-tests/src/env.ts lines 12-15 (BACKEND_URL declaration + AUTH_URL declaration ordering) and lines 33-42 (probeBackend body)
    - compose/traefik/dynamic.yml (verify the SAN list covers every router host)
  </read_first>
  <action>
    1) tools/bootstrap.sh — append the openssl SAN cert-gen block AT END (after the BACKUP_AGE_IDENTITY block, before the final exit). Use the EXACT bash snippet from RESEARCH §D-05 "Exact openssl SAN-cert generation block" (the one starting `# Phase 02.7 / D-05 — Traefik self-signed cert ...`). Required SANs (DNS): localhost, *.localhost, api.localhost, auth.localhost, grafana.localhost, minio-console.localhost, mailpit.localhost, api.example.test, auth.example.test, *.example.test. IP SANs: 127.0.0.1, ::1. Validity: -days 3650. Idempotence: openssl x509 -checkend $((86400*30)). chmod 600 on key, 644 on cert. CERT_DIR uses ${REPO_ROOT}/compose/traefik/certs (REPO_ROOT already defined earlier in bootstrap.sh — verify variable name; if it's named differently use the existing convention).

    2) Makefile lines 75-82 — replace contract-test target body with the LITERAL block from RESEARCH §D-05 "Exact Makefile rewrite":
       ```
       contract-test:
       \tdocker compose --profile default --profile contract-test up -d --wait
       \t@docker compose --profile contract-test run --rm seed ; \\
       \trc=$$? ; \\
       \tif [ $$rc -ne 0 ]; then docker compose down -v ; exit $$rc ; fi ; \\
       \tBACKEND_URL=https://api.localhost \\
       \t  NODE_EXTRA_CA_CERTS=$(PWD)/compose/traefik/certs/local.crt \\
       \t  pnpm -F @openwhispr/contract-tests test --run ; \\
       \trc=$$? ; docker compose down -v ; exit $$rc
       ```
       (AUTH_URL omitted — default collapses to BACKEND_URL via env.ts.) Use real TAB indentation per Makefile rules.

    3) Makefile contract-test-deployed — prepend the LITERAL guard from RESEARCH §D-05 "contract-test-deployed refuse-on-NODE_TLS guard":
       `@test "$$NODE_TLS_REJECT_UNAUTHORIZED" != "0" || (echo "refusing to run with TLS verification disabled (NODE_TLS_REJECT_UNAUTHORIZED=0)" && exit 1)`
       as the FIRST line of the recipe.

    4) packages/contract-tests/src/env.ts — change the AUTH_URL default at lines 14-15 from `process.env.AUTH_URL ?? "http://auth.localhost"` to `process.env.AUTH_URL ?? BACKEND_URL` (env override preserved). Verify BACKEND_URL is declared BEFORE AUTH_URL.

    5) packages/contract-tests/src/env.ts probeBackend (lines 33-42) — add `redirect: "error"` to the fetch options. EXACT diff per RESEARCH §D-05 "probeBackend redirect:'error'":
       ```ts
       const res = await fetch(`${BACKEND_URL}/api/health`, {
         signal: AbortSignal.timeout(3000),
         redirect: "error",
       });
       ```

    6) Run `pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts tests/unit/probe-backend-redirect-error.test.ts` — assert ALL GREEN.
    7) Run per-file coverage: `pnpm vitest run tests/unit/probe-backend-redirect-error.test.ts --coverage --coverage.include='packages/contract-tests/src/env.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 --coverage.thresholds.statements=90 --coverage.thresholds.functions=90` — assert ≥90% on env.ts. (bootstrap.sh is bash, line-coverage measured by execution presence — note in plan SUMMARY.)
    8) Run `make contract-test --dry-run` to validate Makefile syntax; full `make contract-test` end-to-end belongs to Plan 06.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts tests/unit/probe-backend-redirect-error.test.ts && pnpm vitest run tests/unit/probe-backend-redirect-error.test.ts --coverage --coverage.include='packages/contract-tests/src/env.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 && grep -q "redirect: \"error\"" packages/contract-tests/src/env.ts && grep -q "NODE_EXTRA_CA_CERTS" Makefile && grep -q "openssl req -x509 -nodes -days 3650" tools/bootstrap.sh && ! grep -q "NODE_TLS_REJECT_UNAUTHORIZED=0" Makefile tools/bootstrap.sh packages/contract-tests/src/env.ts</automated>
  </verify>
  <acceptance_criteria>
    All four greps pass; both new test files exit 0; ≥90% line + branch coverage on env.ts; NODE_TLS_REJECT_UNAUTHORIZED=0 absent from production paths; Makefile syntax valid (`make -n contract-test` exits 0).
  </acceptance_criteria>
  <done>Both RED tests are GREEN; bootstrap.sh has the openssl block; Makefile runs https + CA cert; env.ts collapses default + redirect:'error'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → bootstrap.sh | openssl invocation runs with operator privileges |
| host CI → docker-compose stack | NODE_EXTRA_CA_CERTS controls TLS trust scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.7-01 | Information Disclosure | local.key on disk world-readable | mitigate | chmod 600 on local.key in cert-gen block |
| T-02.7-02 | Tampering | dev cert leaked into prod path | mitigate | refuse-on-NODE_TLS_REJECT_UNAUTHORIZED=0 guard in contract-test-deployed; documented operator must use Traefik ACME in prod |
| T-02.7-03 | Repudiation | bootstrap.sh runs without trace | accept | bootstrap.sh emits "bootstrap: generated …" / "skip regeneration" stdout lines for audit |
| T-02.7-04 | DoS | openssl CPU spike on every bootstrap | mitigate | idempotence check (openssl x509 -checkend 30d) — only regenerates when needed |
</threat_model>

<verification>
- `pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts tests/unit/probe-backend-redirect-error.test.ts` exits 0
- Per-file coverage ≥90% on packages/contract-tests/src/env.ts
- `make -n contract-test` parses without error
- `! grep -r 'NODE_TLS_REJECT_UNAUTHORIZED=0' Makefile tools/ packages/contract-tests/`
- `openssl x509 -in compose/traefik/certs/local.crt -noout -text | grep DNS:` shows all 10 expected DNS SANs after manual `bash tools/bootstrap.sh` run on a clean tree
</verification>

<success_criteria>
- Wave 1 infra ready: HTTPS contract-test path is functional end-to-end (Plan 06 will exercise it)
- env.ts AUTH_URL default no longer points at the non-existent http://auth.localhost
- probeBackend loudly surfaces redirect mismatches instead of silently skipping the suite
- Atomic commit: `feat(02.7): D-04+D-05 — bootstrap cert-gen + https contract-test + AUTH_URL collapse + probe loud-fail`
</success_criteria>

<output>
After completion, create `.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-01-SUMMARY.md` per template.
</output>
