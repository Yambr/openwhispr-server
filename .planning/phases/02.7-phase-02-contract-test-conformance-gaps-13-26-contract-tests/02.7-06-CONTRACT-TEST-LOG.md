# Phase 02.7 Plan 06 — Contract-Test Run Log

**Status:** RED — STOPPED at seed step. NEW defect surfaced (NOT one of the 13 the cascade was meant to close). Plan 06 acceptance gate FAILED. Phase 02.8 candidate.

**Timestamp (UTC):** 2026-05-09T20:59:25Z (start) → 2026-05-09T21:01:00Z (stop)
**Git SHA at run:** `8becce5d74026c04c344b440bd48e1a82c02efe5`
**Plan SHA chain:** 8f59b10 (01) → bd2a29b (02) → 0e22a0d (03) → 58aeba7 (04) → 35cf4b6 (05)

---

## Task 1 — Pre-flight clean stack rebuild — PASS

```
$ make clean-stack
... volumes removed (openwhispr_minio_data, openwhispr_postgres_data, openwhispr_valkey_data)

$ rm -f compose/traefik/certs/local.crt compose/traefik/certs/local.key
$ bash tools/bootstrap.sh
bootstrap: .env written (28 keys, 28 generated, 0 preserved)
bootstrap: generated /Users/nick/openwhispr-server/compose/traefik/certs/local.crt (10-year validity, SANs cover *.localhost + *.example.test)

$ openssl x509 -in compose/traefik/certs/local.crt -noout -text | grep -E 'DNS:|IP Address:'
DNS:localhost, DNS:*.localhost, DNS:api.localhost, DNS:auth.localhost, DNS:grafana.localhost, DNS:minio-console.localhost, DNS:mailpit.localhost, DNS:api.example.test, DNS:auth.example.test, DNS:*.example.test, IP Address:127.0.0.1, IP Address:0:0:0:0:0:0:0:1
```

**Plan automated verify:** `TASK1_VERIFY_PASS` — cert exists, valid >30 days, SAN list covers all required Traefik vhosts (`api.localhost`, `auth.example.test`, `*.localhost`, `127.0.0.1`).

**Build:** `docker compose build api migrate seed` → exit 0 (all images CACHED).

---

## Task 2 — `make contract-test` end-to-end — FAIL (NEW defect; STOP per plan)

### Compose-up snapshot (all services Healthy before seed)

```
NAME                          IMAGE                                          STATUS
openwhispr-api-1              openwhispr-api                                 Up 3 min (healthy)
openwhispr-fixture-idp-1      openwhispr-fixture-idp                         Up 3 min (healthy)
openwhispr-grafana-1          grafana/grafana:11.6.0                         Up 3 min (unhealthy — pre-existing)
openwhispr-loki-1             grafana/loki:3.5.0                             Up 3 min (healthy)
openwhispr-mimir-1            grafana/mimir:2.16.0                           Up 3 min
openwhispr-minio-1            minio/minio:RELEASE.2025-09-07T16-13-09Z       Up 3 min (healthy)
openwhispr-otel-collector-1   otel/opentelemetry-collector-contrib:0.151.0   Up 3 min
openwhispr-pgbouncer-1        edoburu/pgbouncer:v1.25.1-p0                   Up 3 min (healthy)
openwhispr-postgres-1         postgres:17.5-alpine                           Up 3 min (healthy)
openwhispr-tempo-1            grafana/tempo:2.8.0                            Up 3 min (healthy)
openwhispr-traefik-1          traefik:v3.6                                   Up 3 min (healthy)
openwhispr-valkey-1           valkey/valkey:8.1-alpine                       Up 3 min (healthy)
```

Migrate exited 0 (migrations 0000→0004 applied successfully — Plan 02.7-05's migration 0004 lands clean against fresh DB).

### Seed step (NEW defect)

```
container openwhispr-seed-1 exited (1)
make: *** [contract-test] Error 1
exit_code=2
```

#### Seed container stderr

```
Error: seed: signUp(fixture@conformance.test) failed: HTTP 422 body={"message":"Failed to create user","code":"FAILED_TO_CREATE_USER"}
    at signUp (/app/packages/data/dist/seed-conformance.cjs:67:9)
    at process.processTicksAndRejections (node:internal/process/task_queues:104:5)
    at async seedConformanceFixtures (/app/packages/data/dist/seed-conformance.cjs:93:27)
```

This is the **Plan 02.7-04 D-03A loud-fail working as designed** — it's catching a REAL upstream failure that the previous broad-swallow path would have silently masked, leaving the DB unseeded and the 13/26 contract failures as the downstream symptom. The fail-loud upgrade is doing its job.

#### API container error (root cause)

```
2026-05-09T21:00:27.031Z ERROR [Better Auth]: Failed to create user
_DrizzleQueryError: Failed query: insert into "users"
  ("id", "tenant_id", "email", "name", "email_verified", "email_verified_at",
   "image", "password_hash", "created_at", "updated_at")
  values ($1, default, $2, $3, $4, default, default, default, $5, $6) returning ...
params: 04xaRzi0ScgyXxWRtKwGG74OkqNZb0yO,fixture@conformance.test,Fixture User,false,...
  cause: error: invalid input syntax for type uuid: "04xaRzi0ScgyXxWRtKwGG74OkqNZb0yO"
      at NodePgPreparedQuery.queryWithCache (file:///app/dist/index.js:8014:19)
      at async withReturning (.../@better-auth/drizzle-adapter/dist/index.mjs:52:45)
      at async Object.create (.../@better-auth/drizzle-adapter/dist/index.mjs:260:12)
      at async createWithHooks (.../better-auth/dist/db/with-hooks.mjs:25:66)
      at async Object.createUser (.../better-auth/dist/db/internal-adapter.mjs:58:11)
      at async file:///app/node_modules/better-auth/dist/api/routes/sign-up.mjs:218:18
```

**Defect classification:** Schema/library impedance mismatch. `packages/data/src/schema/users.ts:17` declares `id: uuid("id").primaryKey().defaultRandom()` — Postgres `uuid` type — but Better Auth v1.6.9's default ID generator emits a 32-character base32-ish opaque string (`04xaRzi0ScgyXxWRtKwGG74OkqNZb0yO`), which `pg` rejects with SQLSTATE 22P02 (`invalid input syntax for type uuid`). Postgres' column `default` (a `gen_random_uuid()` call) is bypassed because Better Auth's adapter explicitly supplies `$1 = id` rather than letting Postgres default-generate.

**Why this only surfaces NOW:** Pre-Plan 02.7-04, `signUp()` swallowed all 4xx as "already exists" idempotent. The compose contract-test never seeded a fixture user because every signup silently failed with this exact 422; the missing fixture row was the proximate cause of the 13/26 failures (D-03 in CONTEXT). Plan 02.7-05 fixed the lookup case-sensitivity AND Plan 02.7-04 made signup loud — together they correctly bubble the underlying never-fixed Better Auth ID-generator/uuid-column mismatch up to the seed exit code.

This is **NOT one of the 13 conformance failures the cascade was meant to close** — it is the root cause UNDER them, masked for two phases by the silent-swallow. Per Plan 02.7-06 task instructions and CLAUDE.md scope-boundary rule:

> "If any new defect surfaces (NEW red test that's NOT one of the 13 the cascade was meant to close): STOP, surface as deviation, do NOT auto-fix."

**Action:** STOPPED. NOT auto-fixing. Surfaced for Phase 02.8 (or a hot-patch plan 02.7-08 — operator decision).

---

## Task 3 — Reverse-patch evidence — NOT EXECUTED

Reverse-patch experiments require the GREEN baseline (Task 2) as the "good state" to revert from. With Task 2 RED on a NEW defect, reverse-patch experiments would conflate two regressions and produce uninterpretable RED logs.

Recommendation: re-execute reverse-patch experiments AFTER the Phase 02.8 hot-fix (or 02.7-08) lands the user-id fix and Task 2 reaches GREEN.

---

## Task 4 — Human-verify checkpoint — REPLACED with HUMAN-DECISION checkpoint

The plan's intended checkpoint type was `human-verify` (visual confirmation of GREEN witness). Because Task 2 is RED on a NEW root-cause defect, the appropriate checkpoint type is now `human-action` / `human-decision`: the operator must choose remediation strategy. See `02.7-06-SUMMARY.md` for full options matrix.

---

## Self-Check

- ✅ Cert generated correctly (Task 1 verify PASS)
- ✅ All 12 docker-compose services reached Healthy
- ✅ Migrations 0000→0004 applied clean
- ❌ Seed step exited 1 — `FAILED_TO_CREATE_USER` (uuid vs Better Auth id-generator mismatch)
- ❌ Contract-test vitest never executed (gated by seed exit 0)
- ❌ 26/26 GREEN witness — NOT achieved
- ✅ Working tree CLEAN after run (no inadvertent edits; cert regen is `.gitignore`'d)
- ✅ Stack torn down with `docker compose down -v --remove-orphans`

---

*Phase: 02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests*
*Generated: 2026-05-10 by gsd-executor*
