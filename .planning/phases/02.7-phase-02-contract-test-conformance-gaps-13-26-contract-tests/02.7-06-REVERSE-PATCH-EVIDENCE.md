# Phase 02.7 Plan 06 — Reverse-Patch Evidence

**Status:** NOT EXECUTED — gated on Task 2 GREEN baseline.

## Why deferred

Reverse-patch experiments work by:
1. Confirming GREEN baseline (the production fix is in place; tests are GREEN)
2. Inverting the fix
3. Confirming the test goes RED with the predicted signature
4. Restoring; confirming GREEN restored

With Task 2 (Plan 06) RED on a NEW defect (`FAILED_TO_CREATE_USER` — Better Auth id-generator vs uuid column mismatch — see `02.7-06-CONTRACT-TEST-LOG.md`), the GREEN baseline does not exist, so reverse-patch RED logs would conflate two regressions and produce uninterpretable evidence (every test would be RED for two reasons; we couldn't tell whether the inverse-patch is what caught it).

## Per-defect status (when GREEN baseline is restored)

| # | Experiment | Defect ID | Target | Status |
|---|------------|-----------|--------|--------|
| 1 | Revert mintBearer rewrite | D-01 | `apps/api/src/lib/mint-bearer.ts` | DEFERRED |
| 2 | Revert dual-auth try/catch | D-02 | `apps/api/src/middleware/dual-auth.ts` | DEFERRED |
| 3 | Revert migration 0004 | D-03 Layer B | `packages/data/migrations/0004_email_lowercase_normalize.sql` + `meta/_journal.json` | DEFERRED |
| 4 | Skip cert-gen in bootstrap.sh | D-05 | `tools/bootstrap.sh` | DEFERRED |

## Partial unit-level evidence already in plan SUMMARYs

Even though the docker-compose end-to-end reverse-patches are deferred, **per-test reverse-patch claims are already documented in each predecessor SUMMARY's docstring**:

- **Plan 02.7-01:** "removing the openssl block from bootstrap.sh restores RED on all 4 cert-gen tests (verified during initial RED step). Removing `redirect: 'error'` from env.ts restores RED on the probe `passes redirect:'error' to fetch` assertion." (Plan 01 SUMMARY)
- **Plan 02.7-02:** "8 immediate failures across `mint-bearer.test.ts` and `oauth-channel-scheme-mint-bearer.test.ts`, all matching `TypeError: auth.handler is not a function`" — verified in Task 2 RED. (Plan 02 SUMMARY)
- **Plan 02.7-03:** Embedded in test docstrings: "removing the try/catch wrapper around `auth.api.getSession` … → tests 1-3 RED" / "removing the `else if (err instanceof APIError)` branch → all five APIError tests RED" — initial RED of 6/11 confirmed. (Plan 03 SUMMARY)
- **Plan 02.7-04:** "Initial RED run confirmed empirically: 5 of 13 tests failed with the predicted signatures (`expected throws to match /HTTP 422/`, `expected throws to match /preflight failed/`)." (Plan 04 SUMMARY)
- **Plan 02.7-05:** Per-test coverage table maps each migration step to its testcontainer assertion (Test 1 → backfill; Test 2 → DO $$ collision RAISE; Test 3 → unique violation; Test 4 → cross-tenant). (Plan 05 SUMMARY)

These per-plan unit-level reverse-patch witnesses exist in the test docstrings of the test files themselves, and were exercised in each plan's RED phase as part of the TDD discipline. They satisfy CONTEXT D-06 step 5 at the per-plan level.

What is **deferred** is the Plan 06 end-to-end docker-compose reverse-patch (which exercises the production paths under the live Traefik/Postgres/Better Auth stack). That deferral is conditional on Phase 02.8 hot-fix landing.

## Re-execution preconditions

Reverse-patch experiments will be re-attempted in **Plan 02.7-08** (or under Phase 02.8) AFTER:
- Better Auth user-id generator aligned with uuid column (likely options: (a) override Better Auth `advanced.generateId` to emit `crypto.randomUUID()`; (b) change `users.id` column to `text` per Better Auth's default expectation; (c) add a Better Auth user-create hook to rewrite the id field). Operator decision required.
- `make contract-test` exits 0 with 26/26 GREEN.

At that point, the four inverse-patch sequences from this plan can run cleanly against a stable GREEN baseline.

---

*Phase: 02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests*
*Generated: 2026-05-10 by gsd-executor*
