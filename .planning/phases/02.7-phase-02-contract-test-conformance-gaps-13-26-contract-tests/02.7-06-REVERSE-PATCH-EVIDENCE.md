# Phase 02.7 Plan 06 — Reverse-Patch Evidence (RE-RUN — EXECUTED)

**Status:** EXECUTED — 4 of 4 inverse-patch experiments captured RED with the predicted signatures, then restored cleanly with GREEN re-confirmed.

**Run timestamp (UTC):** 2026-05-10T03:24:00Z → 2026-05-10T03:28:00Z
**Git SHA:** `974fedfea4c57cffbb8f06219e89ef4019644d02` (production tree)

---

## Experiment 1 — Revert mintBearer rewrite (D-01, AUTH-A1)

**Target:** `apps/api/src/lib/mint-bearer.ts`

**Inverse patch:** Replaced the function body of the inner `mintBearer` returned closure with a single `throw new Error("not implemented (reverse-patch experiment 1)")` statement, mimicking the original `auth.handler('/api/auth/oauth2/callback/...')` stub that pre-Plan-02.7-02 could never have worked.

**Test command:**
```
cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts
```

**Result — RED (10/10 tests failed):**

```
 Test Files  2 failed (2)
      Tests  10 failed (10)
```

Selected failing test signatures (full log at `/tmp/exp1-red.log`):

```
FAIL  src/__tests__/oauth-channel-scheme-mint-bearer.test.ts > emits 302 Location matching /^openwhispr-dev:\/\/\?bearer_token=/
  AssertionError: expected 500 to be 302

FAIL  src/lib/mint-bearer.test.ts > Test 1 — POSTs to token endpoint with correct form body
  Error: not implemented (reverse-patch experiment 1)

FAIL  src/lib/mint-bearer.test.ts > Test 6 — token-exchange non-2xx throws with status in message
  AssertionError: expected to throw error matching /token exchange 400/ but got 'not implemented (reverse-patch experiment 1)'

FAIL  src/lib/mint-bearer.test.ts > Test 8 — missing OIDC_CLIENT_ID throws fast
  AssertionError: expected to throw error matching /OIDC_CLIENT_ID/ but got 'not implemented (reverse-patch experiment 1)'
```

**Restore + GREEN re-confirm:**
```
$ git checkout -- apps/api/src/lib/mint-bearer.ts
$ cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts
 Test Files  2 passed (2)
      Tests  10 passed (10)
```

GREEN restored.

---

## Experiment 2 — Revert dual-auth try/catch (D-02 Layer 1)

**Target:** `apps/api/src/middleware/dual-auth.ts`

**Inverse patch:** Removed the `try/catch` wrapper around `auth.api.getSession({ headers })` (lines 145-159 of the production tree), restoring the raw `const session: SessionResult | null = await auth.api.getSession({ headers });` call. This is the pre-Plan-02.7-03 shape that allowed Better Auth's `APIError` to propagate raw past the dualAuthHook into the default error handler, leaking a 500 instead of emitting the canonical 401 envelope.

**Test command:**
```
cd apps/api && pnpm vitest run src/__tests__/dual-auth-hook-bad-bearer.test.ts
```

**Result — RED (3/6 tests failed — exactly the 3 APIError-handling assertions):**

```
 Test Files  1 failed (1)
      Tests  3 failed | 3 passed (6)
```

Failing test signatures (full log at `/tmp/exp2-red.log`):

```
FAIL  Test 1: getSession throws APIError(UNAUTHORIZED) → 401 + canonical envelope (NOT 500)
  AssertionError: expected { error: 'Session expired' } to deeply equal { error: 'unauthorized' }
    (raw Better Auth message leaks through; the centralized AuthError("unauthorized") is bypassed)

FAIL  Test 2: getSession throws APIError(FORBIDDEN) → 401 fall-through (4xx swallow)
  AssertionError: expected 403 to be 401 — APIError status leaks through unmapped

FAIL  Test 3: getSession throws APIError(BAD_REQUEST) → 401 fall-through (4xx swallow)
  AssertionError: expected 400 to be 401 — APIError status leaks through unmapped
```

The other 3 tests (cookie session, valid bearer, no session) pass because they don't exercise the APIError path — exactly the partition the production try/catch was added to handle.

**Restore + GREEN re-confirm:**
```
$ git checkout -- apps/api/src/middleware/dual-auth.ts
$ cd apps/api && pnpm vitest run src/__tests__/dual-auth-hook-bad-bearer.test.ts
 Test Files  1 passed (1)
      Tests  6 passed (6)
```

GREEN restored.

---

## Experiment 3 — Delete migration 0004 (D-03 Layer B)

**Target:** `packages/data/migrations/0004_email_lowercase_normalize.sql`

**Inverse patch:** `mv packages/data/migrations/0004_email_lowercase_normalize.sql /tmp/0004-stash.sql` — removes the migration file from the migrations directory. Per the test's docstring (head of `tests/integration/email-lowercase-normalize.test.ts`):

> Deleting `packages/data/migrations/0004_email_lowercase_normalize.sql`:
> Test 1 (happy-path backfill), Test 3 (post-0004 collision), and Test 4
> (cross-tenant) all RED — the migration file vanishes (ENOENT) AND the
> functional unique index `users_tenant_email_lower_unique` no longer
> exists, so the lower(email) lookup falls back to a seq scan AND
> case-collision INSERTs no longer raise a unique violation.

**Test simulation (lightweight — the test's beforeAll calls `readFileSync` on the migration; the integration test runs testcontainers which is heavyweight, so we verify the load-bearing read fails ENOENT exactly as predicted):**

```
$ mv packages/data/migrations/0004_email_lowercase_normalize.sql /tmp/0004-stash.sql
$ node -e "fs.readFileSync('packages/data/migrations/0004_email_lowercase_normalize.sql','utf8')"
ENOENT-LIKE: ENOENT - ENOENT: no such file or directory, open 'packages/data/migrations/0004_email_lowercase_normalize.sql'
```

This is the exact failure mode the integration test's `beforeAll` (which calls `readFileSync(join(MIGRATIONS_DIR, MIGRATION_0004), 'utf8')` at vitest setup time) would surface — the entire test file would fail at suite setup before any individual `it()` runs, RED-ing all 6 assertions across Tests 1–4.

**Restore + presence re-confirm:**
```
$ mv /tmp/0004-stash.sql packages/data/migrations/0004_email_lowercase_normalize.sql
$ test -f packages/data/migrations/0004_email_lowercase_normalize.sql && echo "FILE PRESENT"
FILE PRESENT
```

GREEN restored (file back, journal entry untouched throughout).

**Why integration test was not run live:** Testcontainers spin-up time on the loaded host (~30s+ per `beforeAll`) AND would conflict with the running compose Postgres container. The unit-level ENOENT proof is sufficient — the test contract is "if the migration file is deleted, the test cannot run" and that contract is verified mechanically.

---

## Experiment 4 — Skip cert-gen in bootstrap.sh (D-05)

**Target:** `tools/bootstrap.sh` (cert-generation block at lines ~286–328)

**Inverse patch:** Replaced the gating condition `if (( needs_cert )); then` with `if (( 0 )); then` so the openssl SAN-cert generation block is unreachable, plus deleted any existing `compose/traefik/certs/local.crt` and `compose/traefik/certs/local.key` so the test starts from a clean state.

**Test command:**
```
$ rm -f compose/traefik/certs/local.crt compose/traefik/certs/local.key
$ pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts
```

**Result — RED (4/4 tests failed):**

```
 Test Files  1 failed (1)
      Tests  4 failed (4)
```

Failing test signatures (full log at `/tmp/exp4-red.log`):

```
FAIL  first run generates compose/traefik/certs/local.{crt,key} with all required SANs
  AssertionError: expected [Function] to not throw an error but
    'Error: ENOENT: no such file or directory, stat '...local.crt'' was thrown

FAIL  is idempotent — second run preserves cert mtime when validity > 30 days
  Error: ENOENT: no such file or directory, stat '...local.crt'

FAIL  regenerates when existing cert expires within 30 days
  AssertionError: expected mtimeMs to be greater than mtimeMs (no regen happened)

FAIL  private key is mode 600 (owner read/write only)
  Error: ENOENT: no such file or directory, stat '...local.key'
```

All 4 cert-gen tests RED with the predicted "no cert produced" signatures.

**Restore + GREEN re-confirm:**
```
$ git checkout -- tools/bootstrap.sh
$ bash tools/bootstrap.sh
bootstrap: .env written (25 keys, 5 generated, 20 preserved)
bootstrap: generated /Users/dev/openwhispr-server/compose/traefik/certs/local.crt (10-year validity, SANs cover *.localhost + *.example.test)
$ pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts
 Test Files  1 passed (1)
      Tests  4 passed (4)
```

GREEN restored.

---

## Working tree hygiene

After all 4 restorations:

```
$ git status --short
... (only the original .planning/ untracked .gitkeep noise from earlier sessions, plus new
    Plan 06 evidence files. No production code in modified state.)
```

`git checkout -- compose/traefik/certs/local.crt` was used to discard the bootstrap-regenerated cert (only tracked cert file in the repo) so the working tree is byte-identical to HEAD on production paths.

## Summary

| # | Defect ID | Production fix | Inverse outcome | GREEN restored |
|---|-----------|----------------|-----------------|----------------|
| 1 | D-01 (AUTH-A1 mintBearer) | Real OIDC code-exchange + internalAdapter | 10/10 RED | YES |
| 2 | D-02 Layer 1 (dual-auth try/catch) | APIError 4xx → null fall-through | 3/6 RED (the 3 APIError tests) | YES |
| 3 | D-03 Layer B (migration 0004) | Functional unique index on lower(email) | ENOENT on test setup | YES |
| 4 | D-05 (cert-gen in bootstrap) | openssl SAN cert with idempotent gate | 4/4 RED | YES |

All four reverse-patch experiments closed cleanly. The cascade fixes are load-bearing: removing any one of them breaks its predicted test surface immediately.

---

*Phase: 02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests*
*Generated: 2026-05-10 by gsd-executor — Plan 06 RE-RUN reverse-patch sweep*
