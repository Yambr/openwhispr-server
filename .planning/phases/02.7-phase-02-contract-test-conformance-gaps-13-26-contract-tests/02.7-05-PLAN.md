---
phase: 02.7
plan: 05
type: tdd
wave: 2
depends_on: ["02.7-01"]
files_modified:
  - packages/data/migrations/0004_email_lowercase_normalize.sql
  - packages/data/migrations/meta/_journal.json
  - packages/data/src/schema/users.ts
  - apps/api/src/routes/check-user.ts
  - tests/integration/email-lowercase-normalize.test.ts
autonomous: true
requirements: [TDD-01, TDD-01b, WIRE-01, DATA-02, CONTRACT-01]
must_haves:
  truths:
    - "packages/data/migrations/0004_email_lowercase_normalize.sql is a NEW atomic migration with 4 statements separated by --> statement-breakpoint: (1) DO $$ collision check raising EXCEPTION, (2) UPDATE users SET email=lower(email) WHERE email <> lower(email), (3) DROP INDEX IF EXISTS users_tenant_email_unique, (4) CREATE UNIQUE INDEX users_tenant_email_lower_unique ON users (tenant_id, lower(email))"
    - "Migration ordering is collision-check FIRST (refuses to auto-deduplicate), THEN backfill, THEN drop+recreate index"
    - "packages/data/migrations/meta/_journal.json contains a new entry for 0004 (drizzle-kit journal append pattern matching 0001/0003)"
    - "packages/data/src/schema/users.ts emailUnique index renamed to users_tenant_email_lower_unique (functional form per A3 OR plain rename + drift comment)"
    - "apps/api/src/routes/check-user.ts query uses sql`SELECT 1 FROM users WHERE lower(email) = lower(${body.email}) LIMIT 1`"
    - "tests/integration/email-lowercase-normalize.test.ts uses testcontainers postgres; applies migrations 0000→0004 fresh; asserts: pre-existing Mixed@Case rows lowercased, INSERT collision raises unique violation, lookup via lower() succeeds, collision-precondition migration RAISES EXCEPTION"
  artifacts:
    - path: "packages/data/migrations/0004_email_lowercase_normalize.sql"
      provides: "Functional unique index on lower(email) + idempotent backfill (D-03 Layer B)"
      contains: "lower(email)"
    - path: "packages/data/migrations/meta/_journal.json"
      provides: "drizzle-kit journal entry for 0004"
    - path: "packages/data/src/schema/users.ts"
      provides: "Schema rename to users_tenant_email_lower_unique"
    - path: "apps/api/src/routes/check-user.ts"
      provides: "lower(email) lookup using new functional index"
      contains: "lower(email)"
    - path: "tests/integration/email-lowercase-normalize.test.ts"
      provides: "Testcontainer test for migration + lookup + collision-fail behavior"
  key_links:
    - from: "apps/api/src/routes/check-user.ts"
      to: "packages/data/migrations/0004_email_lowercase_normalize.sql functional index"
      via: "lower(email) WHERE clause hits users_tenant_email_lower_unique"
      pattern: "lower\\(email\\)"
---

<objective>
GREEN D-03 Layer B: case-insensitive email uniqueness via new migration `0004_email_lowercase_normalize.sql`. Closes the WIRE-01 check-user gap when fixture emails enter the DB with mixed case (createOAuthUser does NOT auto-lowercase per RESEARCH §D-03 point 5; raw drizzle inserts don't either).

Migration ordering is critical: dupe-check FIRST (refuses to auto-deduplicate per CONTEXT "Specific Ideas" — data-loss event requires human decision), THEN backfill, THEN drop old index, THEN create new functional index. All four steps in the same migration transaction.

Output: 1 new migration + journal append + 3 production file edits + 1 new integration test. Atomic commit `fix(02.7): D-03B — migration 0004 functional unique on lower(email) + check-user lower() lookup`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-CONTEXT.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-RESEARCH.md
@packages/data/migrations/0001_better_auth.sql
@packages/data/migrations/0003_better_auth_tenant_defaults.sql
@packages/data/migrations/meta/_journal.json
@packages/data/src/schema/users.ts
@apps/api/src/routes/check-user.ts
@tests/integration/postgres-roles-idempotent.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify A3 — drizzle-orm 0.45 functional index DSL support</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - apps/api/node_modules/drizzle-orm/pg-core/index.d.ts (or equivalent)
    - packages/data/src/schema/users.ts (current uniqueIndex DSL usage)
  </read_first>
  <action>
    Run from repo root:
    ```
    grep -rE "uniqueIndex" apps/api/node_modules/drizzle-orm/pg-core/ 2>/dev/null | head -5
    grep -rE "uniqueIndex.*sql" packages/ apps/ 2>/dev/null | head -5
    ```
    Determine: does `uniqueIndex("name").on(t.tenantId, sql`lower(${t.email})`)` parse cleanly with drizzle-orm 0.45?

    Decision matrix:
    - If YES → write functional form in users.ts (Task 3)
    - If NO (TS error or drizzle-kit can't introspect) → plain rename to `users_tenant_email_lower_unique` keeping column-form `.on(t.tenantId, t.email)` and add a block comment documenting intentional functional drift

    Document the chosen path in your task output before proceeding.
  </action>
  <verify>
    <automated>grep -rq "uniqueIndex" apps/api/node_modules/drizzle-orm/pg-core/ 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>A3 path chosen + documented; Task 3 implementation strategy known.</acceptance_criteria>
  <done>Drift decision recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — write failing testcontainers integration test for migration + lookup</name>
  <files>tests/integration/email-lowercase-normalize.test.ts</files>
  <read_first>
    - tests/integration/postgres-roles-idempotent.test.ts (testcontainer pattern reference)
    - packages/data/migrations/0001_better_auth.sql (verify --> statement-breakpoint syntax)
    - packages/data/migrations/0003_better_auth_tenant_defaults.sql (DO $$ block + ALTER ROLE pattern)
  </read_first>
  <behavior>
    Test file uses testcontainers postgres (PostgreSqlContainer pattern from postgres-roles-idempotent.test.ts):

    - Test 1 (happy path migration): start fresh postgres → apply migrations 0000..0003 → INSERT 3 users with mixed-case emails ('Alice@Example.com', 'BOB@example.com', 'charlie@example.com') under default tenant (no case collisions) → apply migration 0004 → SELECT email FROM users → assert all three rows now have lowercased email AND SELECT 1 FROM users WHERE lower(email)='alice@example.com' returns a row
    - Test 2 (collision precondition): apply 0000..0003 → INSERT 'X@Y.com' AND 'x@y.com' (both same tenant) → apply migration 0004 → assert PostgreSQL error THROWN matching /refusing to auto-deduplicate/ AND no UPDATE/INDEX changes were applied (the migration runs in a transaction; partial application impossible)
    - Test 3 (post-0004 unique violation): apply 0000..0004 fresh → INSERT 'foo@bar.com' → INSERT 'FOO@bar.com' on same tenant → assert PostgreSQL unique_violation referencing users_tenant_email_lower_unique
    - Test 4 (cross-tenant allowed): apply 0000..0004 → create second tenant → INSERT 'foo@bar.com' tenant1 + 'foo@bar.com' tenant2 → both succeed (uniqueness scoped to (tenant_id, lower(email)))

    Each test docstring includes:
      Source-of-record commit: <filled at commit time>
      Reverts: deleting migration 0004 → tests 1, 3, 4 RED on missing index / case-mixed lookup; removing the DO $$ collision-check block → test 2 RED.
  </behavior>
  <action>
    Use the EXACT testcontainer pattern from tests/integration/postgres-roles-idempotent.test.ts. Apply migrations sequentially via `pg.Client` and `client.query(fs.readFileSync(migrationPath,'utf8'))`. For per-statement breakpoints, split on the literal `--> statement-breakpoint` separator (drizzle-kit convention).

    The test reads SQL files from disk (NOT through @openwhispr/data build output) so it does not need the package compiled. For RED state, the test will fail because:
    - migration 0004 file does not exist yet → Test 1 fails with ENOENT
    - even if file existed, the assertions hit missing index / missing collision check

    Run: `pnpm vitest run tests/integration/email-lowercase-normalize.test.ts` and CONFIRM RED.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/email-lowercase-normalize.test.ts 2>&1 | grep -qE "FAIL|ENOENT" || (echo "must be RED" && exit 1)</automated>
  </verify>
  <acceptance_criteria>Integration test compiles + runs against testcontainer; assertions FAIL because migration 0004 + check-user lower() lookup do not exist.</acceptance_criteria>
  <done>RED captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — write migration 0004 + journal append + schema rename + check-user lower() lookup</name>
  <files>packages/data/migrations/0004_email_lowercase_normalize.sql, packages/data/migrations/meta/_journal.json, packages/data/src/schema/users.ts, apps/api/src/routes/check-user.ts</files>
  <read_first>
    - packages/data/migrations/0001_better_auth.sql (verify --> statement-breakpoint syntax)
    - packages/data/migrations/0003_better_auth_tenant_defaults.sql (DO $$ block + journal entry style)
    - packages/data/migrations/meta/_journal.json (current entry list — append a new entry with idx incremented)
    - packages/data/src/schema/users.ts line 31 (current emailUnique definition)
    - apps/api/src/routes/check-user.ts line 58 (current SELECT)
  </read_first>
  <action>
    1) Create `packages/data/migrations/0004_email_lowercase_normalize.sql` with the LITERAL contents from RESEARCH §D-03 "Migration SQL (exact statement order)" — comments included for audit. Specifically the four-step block: (a) DO $$ collision check with RAISE EXCEPTION 'migration 0004: found % case-collision email dupes ...; refusing to auto-deduplicate ...', (b) UPDATE "users" SET "email" = lower("email") WHERE "email" <> lower("email"), (c) DROP INDEX IF EXISTS "users_tenant_email_unique", (d) CREATE UNIQUE INDEX "users_tenant_email_lower_unique" ON "users" (tenant_id, lower(email)). Each separated by --> statement-breakpoint.

    2) Append to `packages/data/migrations/meta/_journal.json` a new entry matching the format of the existing 0003 entry: increment idx, set tag to "0004_email_lowercase_normalize", preserve JSON formatting/indentation exactly.

    3) Update `packages/data/src/schema/users.ts` per Task 1's A3 decision:
       - If A3 supports functional DSL: change to `emailUnique: uniqueIndex("users_tenant_email_lower_unique").on(t.tenantId, sql`lower(${t.email})`)` and ensure `import { sql } from "drizzle-orm"` exists
       - If A3 does NOT support: rename only to `users_tenant_email_lower_unique` keeping column-form `.on(t.tenantId, t.email)` and prepend a block comment: `// Functional drift: actual index is ON (tenant_id, lower(email)). drizzle-orm 0.45 DSL does not express functional indexes natively; see migrations/0004_email_lowercase_normalize.sql.`

    4) Update `apps/api/src/routes/check-user.ts` line 58. Change:
       FROM: `sql`SELECT 1 FROM users WHERE email = ${body.email} LIMIT 1``
       TO:   `sql`SELECT 1 FROM users WHERE lower(email) = lower(${body.email}) LIMIT 1``
       (The new functional index makes this an index lookup, not a seq scan.)

    5) Run:
       - `pnpm vitest run tests/integration/email-lowercase-normalize.test.ts` → must be GREEN
       - `pnpm vitest run tests/integration/email-lowercase-normalize.test.ts --coverage --coverage.include='apps/api/src/routes/check-user.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 --coverage.thresholds.statements=90 --coverage.thresholds.functions=90` → ≥90% coverage on check-user.ts
       - `pnpm test` → no regression
       (SQL files are not subject to vitest line-coverage; functional coverage is via the integration test exercising both branches — collision-fail + happy-path. Document this in plan SUMMARY per Phase 02.4 cert-gen precedent.)

       NO `as never` / `@ts-ignore`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/email-lowercase-normalize.test.ts && pnpm vitest run tests/integration/email-lowercase-normalize.test.ts --coverage --coverage.include='apps/api/src/routes/check-user.ts' --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 && grep -q 'lower(email)' packages/data/migrations/0004_email_lowercase_normalize.sql && grep -q 'lower(email)' apps/api/src/routes/check-user.ts && grep -q 'users_tenant_email_lower_unique' packages/data/migrations/0004_email_lowercase_normalize.sql && grep -q 'users_tenant_email_lower_unique' packages/data/src/schema/users.ts && grep -q '0004_email_lowercase_normalize' packages/data/migrations/meta/_journal.json</automated>
  </verify>
  <acceptance_criteria>Integration test green; ≥90% coverage on check-user.ts; all four greps pass; full workspace test suite still green.</acceptance_criteria>
  <done>D-03 Layer B landed; case-insensitive email uniqueness enforced at DB + lookup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → users table | DDL + backfill UPDATE crosses RLS-bypass owner boundary |
| /api/check-user → users table | unauthenticated route (rate-limited per Phase 02 Plan 04); read-only SELECT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.7-14 | Tampering | Auto-deduplication of collision rows = silent data loss | mitigate | DO $$ block RAISES EXCEPTION; refuses to auto-dedupe; operator must resolve manually |
| T-02.7-15 | Information Disclosure | check-user lower() lookup confirms email existence (enumeration) | accept | Existing T-02-03-03 disposition (Phase 02); rate-limit (10/min/IP) mitigates abuse; behavior unchanged from pre-D-03 |
| T-02.7-16 | DoS | sequential scan on lower(email) before index | mitigate | functional unique index makes the WHERE clause an index lookup (B-tree on lower(email)) |
| T-02.7-17 | Repudiation | backfill UPDATE has no audit trail | accept | seed-time / migration-time only; not user-initiated; covered by deployment migration logs |
</threat_model>

<verification>
- `pnpm vitest run tests/integration/email-lowercase-normalize.test.ts` exits 0
- ≥90% coverage on apps/api/src/routes/check-user.ts
- All four greps in `<verify>` pass
- `pnpm test` exits 0 (no regression in other tests)
- Migration journal index increments correctly (no duplicate idx)
</verification>

<success_criteria>
- Atomic commit: `fix(02.7): D-03B — migration 0004 functional unique on lower(email) + check-user lower() lookup`
- WIRE-01 check-user gap closed (case-insensitive lookup hits index)
</success_criteria>

<output>
After completion, create `.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-05-SUMMARY.md` per template.
</output>
