---
phase: 02.7
plan: 06
type: execute
wave: 3
depends_on: ["02.7-01", "02.7-02", "02.7-03", "02.7-04", "02.7-05"]
files_modified: []
autonomous: false
requirements: [TDD-01, CONTRACT-01]
must_haves:
  truths:
    - "make clean-stack && bash tools/bootstrap.sh && docker compose build && docker compose --profile default --profile contract-test up -d --wait succeeds without errors"
    - "make contract-test runs end-to-end against https://api.localhost with the bootstrap-generated cert and exits 0 with all 26/26 contract tests GREEN"
    - "Full contract-test stdout/stderr captured to .planning/phases/02.7-.../02.7-06-CONTRACT-TEST-LOG.md"
    - "NO production code changes in this plan — pure verification"
    - "Reverse-patch evidence: 4 inverse-patch experiments executed and recorded (revert mintBearer rewrite → OAuth tests RED; revert dual-auth try/catch → bearer-invalid test 500 not 401; revert migration 0004 → integration test RED on case-mixed lookup; skip cert-gen in bootstrap.sh → cert test RED)"
  artifacts:
    - path: ".planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-CONTRACT-TEST-LOG.md"
      provides: "Captured make contract-test stdout + 26/26 GREEN witness"
    - path: ".planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-REVERSE-PATCH-EVIDENCE.md"
      provides: "Per-defect inverse-patch RED witnesses"
  key_links: []
---

<objective>
Wave 3 verification: prove the entire Phase 02.7 closure works end-to-end against the live docker-compose stack. NO production code changes — pure black-box conformance run.

Captures the 26/26 GREEN witness AND the four reverse-patch experiments per CONTEXT D-06 step 5.

Output: 2 evidence markdown files. Atomic commit `docs(02.7): contract-test 26/26 GREEN witness + reverse-patch evidence`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-CONTEXT.md
@.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-RESEARCH.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight clean stack rebuild</name>
  <files>(no files modified)</files>
  <read_first>
    - Makefile (locate clean-stack target from Phase 02.6; if absent, use `docker compose down -v && docker volume prune -f --filter label=com.docker.compose.project=$(basename $PWD)`)
  </read_first>
  <action>
    From repo root, in order:
    1) `make clean-stack` (or fallback `docker compose down -v` if target missing)
    2) `rm -f compose/traefik/certs/local.crt compose/traefik/certs/local.key` (force fresh cert generation)
    3) `bash tools/bootstrap.sh` — verify stdout includes "bootstrap: generated …local.crt" line (not "skip regeneration")
    4) `openssl x509 -in compose/traefik/certs/local.crt -noout -text | grep -E 'DNS:|IP:'` — capture SAN list to confirm Plan 01 cert covers all expected hosts
    5) `docker compose build api migrate seed` — assert exit 0
  </action>
  <verify>
    <automated>test -f compose/traefik/certs/local.crt && test -f compose/traefik/certs/local.key && openssl x509 -in compose/traefik/certs/local.crt -noout -checkend $((86400*30))</automated>
  </verify>
  <acceptance_criteria>Cert exists, valid >30 days, SAN list covers all Traefik vhosts; api/migrate/seed images build clean.</acceptance_criteria>
  <done>Clean stack ready for contract-test run.</done>
</task>

<task type="auto">
  <name>Task 2: Run make contract-test end-to-end and capture log</name>
  <files>.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-CONTRACT-TEST-LOG.md</files>
  <read_first>(none — black-box run)</read_first>
  <action>
    1) Run from repo root with full output capture:
       ```
       make contract-test 2>&1 | tee /tmp/02.7-contract-test.log
       echo "exit_code=$?" >> /tmp/02.7-contract-test.log
       ```
    2) Create `.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-CONTRACT-TEST-LOG.md` containing:
       - Header section: timestamp, git SHA (`git rev-parse HEAD`), docker compose ps snapshot
       - Full stdout/stderr from /tmp/02.7-contract-test.log
       - Footer: vitest summary line (e.g., "Test Files  X passed (X) | Tests  26 passed (26)") highlighted
    3) The expected outcome is 26/26 GREEN. If ANY test is RED, stop the plan and document the failing test name + assertion in the SUMMARY; the orchestrator will route to gap-closure rather than continue to Plan 07.
    4) Out-of-scope failures (litellm/realtime contract tests if present in suite) are NOT acceptable — Phase 02 contract scope is auth lifecycle + WIRE-01..04 + WIRE-17..20 + AUTH-01..07. If unrelated tests fail, document but escalate to user — do NOT silently mark as "out of scope" unless CONTEXT.md explicitly lists them in D-07.
  </action>
  <verify>
    <automated>grep -qE "Tests +26 passed" /tmp/02.7-contract-test.log && grep -qE "exit_code=0" /tmp/02.7-contract-test.log</automated>
  </verify>
  <acceptance_criteria>26/26 contract tests pass; exit code 0; full log preserved as evidence file.</acceptance_criteria>
  <done>GREEN witness captured.</done>
</task>

<task type="auto">
  <name>Task 3: Reverse-patch evidence — four inverse experiments</name>
  <files>.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-REVERSE-PATCH-EVIDENCE.md</files>
  <read_first>(none — uses git stash workflow)</read_first>
  <action>
    For EACH of the four experiments below, follow the same flow:
    1) `git stash push -- <target file(s)>` (preserve good state)
    2) Apply the inverse patch (manual edit per the experiment description)
    3) Run the targeted test command
    4) Capture the FAIL output (status code + first 30 lines of vitest output)
    5) `git checkout -- <target file(s)>` to restore good state
    6) Re-run the targeted test to confirm GREEN restored

    Experiments:

    **Experiment 1 — Revert mintBearer rewrite (D-01)**
    - Target: `apps/api/src/lib/mint-bearer.ts`
    - Inverse: replace function body with `return await auth.handler('/api/auth/oauth2/callback/oidc?code=&state=')` style stub (or simply `throw new Error('not implemented')`)
    - Test command: `cd apps/api && pnpm vitest run src/lib/mint-bearer.test.ts src/__tests__/oauth-channel-scheme-mint-bearer.test.ts`
    - Expected: FAIL on internalAdapter / fetch assertions

    **Experiment 2 — Revert dual-auth try/catch (D-02)**
    - Target: `apps/api/src/middleware/dual-auth.ts`
    - Inverse: remove the try/catch block; restore raw `const session = await auth.api.getSession({ headers });`
    - Test command: `cd apps/api && pnpm vitest run src/__tests__/dual-auth-hook-bad-bearer.test.ts`
    - Expected: FAIL — APIError propagates raw, no AuthError thrown

    **Experiment 3 — Revert migration 0004 (D-03 Layer B)**
    - Target: `packages/data/migrations/0004_email_lowercase_normalize.sql` + `packages/data/migrations/meta/_journal.json` (revert journal entry too)
    - Inverse: `git rm packages/data/migrations/0004_email_lowercase_normalize.sql && git checkout HEAD~1 -- packages/data/migrations/meta/_journal.json`
    - Test command: `pnpm vitest run tests/integration/email-lowercase-normalize.test.ts`
    - Expected: FAIL — ENOENT or migration step missing

    **Experiment 4 — Skip cert-gen in bootstrap.sh (D-05)**
    - Target: `tools/bootstrap.sh`
    - Inverse: add `exit 0` at the top of the openssl cert-gen block (skips it); also delete compose/traefik/certs/local.crt
    - Test command: `pnpm vitest run tests/unit/bootstrap-cert-gen.test.ts`
    - Expected: FAIL — cert file not generated; SAN check fails

    Create `.planning/phases/02.7-.../02.7-06-REVERSE-PATCH-EVIDENCE.md` documenting each experiment with:
    - Experiment name + defect ID
    - Inverse patch description (3-5 lines)
    - Test command run
    - First 30 lines of FAIL output (proves test caught the regression)
    - Confirmation that GREEN restored after `git checkout`

    NEVER commit the inverse patches. After all four experiments, run `git status` and verify the working tree is clean (only the new evidence MD file is untracked).
  </action>
  <verify>
    <automated>test -f .planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-REVERSE-PATCH-EVIDENCE.md && git diff --quiet apps/ packages/ tools/ Makefile && grep -cE "Experiment [1-4]" .planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-REVERSE-PATCH-EVIDENCE.md | grep -q "^4$"</automated>
  </verify>
  <acceptance_criteria>Evidence file documents all 4 experiments with FAIL outputs; working tree clean (no inverse patches committed); GREEN restored.</acceptance_criteria>
  <done>Reverse-patch discipline upheld per Phase 02.4 precedent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>26/26 contract test GREEN witness + 4 reverse-patch experiments (Plans 01-05 verified end-to-end against the live docker-compose stack)</what-built>
  <how-to-verify>
    1) Open `.planning/phases/02.7-.../02.7-06-CONTRACT-TEST-LOG.md` — confirm "Tests 26 passed (26)" line is present and exit_code=0
    2) Open `.planning/phases/02.7-.../02.7-06-REVERSE-PATCH-EVIDENCE.md` — confirm all 4 experiments show a FAIL output and a "GREEN restored" note
    3) Run `git status` — confirm working tree is clean (only the two evidence MD files untracked)
    4) (Optional sanity) Re-run `make contract-test` — should still pass 26/26
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 07 (SUMMARY + UAT flip), or describe any anomalies (e.g., out-of-scope test failures) for revision.</resume-signal>
</task>

</tasks>

<verification>
- `make contract-test` exits 0 with 26/26 GREEN
- All four reverse-patch experiments captured FAIL → GREEN-restored cycles
- Working tree clean after evidence collection
</verification>

<success_criteria>
- Atomic commit: `docs(02.7): contract-test 26/26 GREEN witness + reverse-patch evidence`
- Both evidence files in `.planning/phases/02.7-.../`
- Phase ready for SUMMARY + UAT flip (Plan 07)
</success_criteria>

<output>
After completion, create `.planning/phases/02.7-phase-02-contract-test-conformance-gaps-13-26-contract-tests/02.7-06-SUMMARY.md` per template.
</output>
