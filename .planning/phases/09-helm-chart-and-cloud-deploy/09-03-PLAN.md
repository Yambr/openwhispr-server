---
phase: 09
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/lint-compose-chart-parity.ts
  - tools/lint-compose-chart-parity.test.ts
  - tools/compose-chart-parity.allowlist.json
  - .github/workflows/helm-lint.yml
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "Tool enumerates services from docker-compose.yml + load-test variants; cross-references against rendered chart manifests; reports services missing from chart that are not in the allowlist"
    - "Exits 0 against current compose + (Wave-0 partial) chart with all current production services on the allowlist (placeholder until later waves add the real Deployments)"
    - "Exits nonzero when a fixture compose adds a service not present in chart and not allowlisted"
    - "Wired into helm-lint.yml as a step that runs after `helm template`"
    - "≥ 90/90/90/90 coverage on the TS driver"
  artifacts:
    - path: "tools/lint-compose-chart-parity.ts"
      provides: "Parser: yaml-parse docker-compose.yml + helm template render, diff service-set vs Deployment/StatefulSet/Job names, report"
    - path: "tools/compose-chart-parity.allowlist.json"
      provides: "Explicit allowlist of compose services NOT in chart (test-only: fixture-idp, seed, contract-test-runner, mailpit-dev; mapped-elsewhere: traefik→cluster-prereq, loki/tempo/mimir/grafana→cluster-prereq)"
  key_links:
    - from: "tools/lint-compose-chart-parity.ts"
      to: "docker-compose.yml"
      via: "yaml.parse(readFileSync('docker-compose.yml'))"
      pattern: "docker-compose.yml"
    - from: "tools/lint-compose-chart-parity.ts"
      to: "charts/openwhispr/"
      via: "child_process: helm template charts/openwhispr -f tests/values-ci.yaml"
      pattern: "helm template"
---

# Plan 09-03: Compose ↔ Chart Parity Lint

<objective>
Add `tools/lint-compose-chart-parity.ts` that fails CI when a new compose service appears without a corresponding chart resource (or explicit allowlist entry). This guards against drift as later phases extend compose without updating the chart.

Output: ts driver + allowlist json + integration into helm-lint.yml + ≥90% coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-helm-chart-and-cloud-deploy/09-RESEARCH.md
@docker-compose.yml
@CLAUDE.md

<interfaces>
Allowlist seed entries (research §"Service Inventory Mapping"):
  test-only:
    - fixture-idp
    - seed
    - contract-test-runner
    - mailpit (gated by dev.enabled)
  cluster-prereq:
    - traefik
    - cert-manager
    - loki
    - tempo
    - mimir
    - grafana
    - prometheus
  bundled-ai-conditional:
    - speaches (chart renders only when bundledAi.enabled=true)

Chart Deployment/StatefulSet names follow {fullname}-<service> via _helpers.tpl.

CLI shape:
  tsx tools/lint-compose-chart-parity.ts [--compose <file>] [--chart-render-cmd '<cmd>'] [--allowlist <json>]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Driver + allowlist + tests with ≥90% coverage</name>
  <files>tools/lint-compose-chart-parity.ts, tools/lint-compose-chart-parity.test.ts, tools/compose-chart-parity.allowlist.json</files>
  <behavior>
    - Vitest tests against synthetic compose + chart fixtures
    - Test: chart with all compose services (minus allowlist) renders → exit 0
    - Test: fixture compose adds new service `quibblr` not on allowlist → exit nonzero with `Missing chart resource: quibblr`
    - Test: allowlist entry for `traefik` → no diagnostic emitted
    - Test: bundledAi-conditional services emit diagnostic only when not gated correctly
    - Coverage ≥ 90/90/90/90
  </behavior>
  <action>Parse docker-compose.yml + docker-compose.load-test.yml + docker-compose.load-test.realistic.yml with `yaml` package; collect unique service names. Shell out to `helm template charts/openwhispr -f .github/ci/values-ci.yaml --set bundledAi.enabled=true` to render with all conditional branches, parse with `yaml.parseAllDocuments`, extract `kind: Deployment|StatefulSet|Job|DaemonSet` names (stripped of fullname prefix via heuristic or values lookup). Compute set diff. Report each missing service. Allowlist categories: `test-only`, `cluster-prereq`, `bundled-ai-conditional`. Exit nonzero on any unhandled missing service. Use real yaml parse — no internal mocks (per CLAUDE.md). Mock only `child_process.execFileSync` (boundary).</action>
  <verify>
    <automated>pnpm exec vitest run tools/lint-compose-chart-parity.test.ts --coverage --coverage.lines=90 --coverage.branches=90 --coverage.functions=90 --coverage.statements=90</automated>
  </verify>
  <done>Driver behaves correctly across positive + negative + allowlist cases; coverage gate passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire into helm-lint.yml</name>
  <files>.github/workflows/helm-lint.yml</files>
  <behavior>
    - helm-lint.yml has a new step `Compose-chart parity` running after `helm template`
    - Step uses pre-rendered chart manifests cached as workflow artifact (or re-renders inline)
    - Step fails workflow on nonzero exit
    - actionlint clean
  </behavior>
  <action>Append job step after `helm template` step. Pass `--allowlist tools/compose-chart-parity.allowlist.json` explicitly. Run `pnpm exec tsx tools/lint-compose-chart-parity.ts`. Note: at Wave 0 the chart does NOT yet have api/web/worker/litellm etc. Deployments — the allowlist must temporarily include them and Plan 09-06/09-07 remove their allowlist entries as those plans land. Document the temporary allowlist entries with a comment `// REMOVE WHEN 09-06 LANDS` so they don't become permanent.</action>
  <verify>
    <automated>actionlint .github/workflows/helm-lint.yml</automated>
  </verify>
  <done>Workflow wired; temporary allowlist entries flagged for removal in later plans.</done>
</task>

</tasks>

<verification>
- Vitest ≥ 90/90/90/90
- actionlint clean
- Allowlist has comments marking entries that later plans must remove
</verification>

<success_criteria>
- Compose-parity lint active in CI
- Allowlist documents every compose service NOT in chart and why
</success_criteria>

<output>
After completion, create `.planning/phases/09-helm-chart-and-cloud-deploy/09-03-SUMMARY.md`.
</output>
