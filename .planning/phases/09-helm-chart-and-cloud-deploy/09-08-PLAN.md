---
phase: 09
plan: 08
type: execute
wave: 2
depends_on:
  - "05"
files_modified:
  - charts/openwhispr/templates/migrate-job.yaml
  - charts/openwhispr/templates/migrate-serviceaccount.yaml
  - charts/openwhispr/values.yaml
  - charts/openwhispr/values.schema.json
  - charts/openwhispr/tests/migrate_test.yaml
  - tools/compose-chart-parity.allowlist.json
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04]
must_haves:
  truths:
    - "Migrate Job has annotations `helm.sh/hook: pre-install,pre-upgrade`, `helm.sh/hook-weight: \"-5\"`, `helm.sh/hook-delete-policy: before-hook-creation`"
    - "Job has `backoffLimit: 0` (one-shot)"
    - "Job initContainer `wait-for-postgres` runs `pg_isready -h {fullname}-pg-rw -p 5432 -U openwhispr_owner` in a loop with 60s timeout (pitfall #15 mitigation)"
    - "Job container image is `openwhispr/migrate:{appVersion}` (pinned, never `latest` — T-09-07 mitigation)"
    - "Job command is `node /app/tools/migrate.js` with DATABASE_URL pointing at the CNPG -rw service (NOT the pooler — migrations bypass transaction pooling)"
    - "Job name includes `{{ .Release.Revision }}` for upgrade visibility (each helm upgrade creates a new Job)"
    - "Snapshot test asserts all 4 hook annotations + the pg_isready initContainer + the migrate.js command"
    - "Compose-parity allowlist entry for `migrate` removed"
  artifacts:
    - path: "charts/openwhispr/templates/migrate-job.yaml"
      provides: "pre-install/pre-upgrade Helm hook Job with pg_isready initContainer + pinned image + migrate.js command"
    - path: "charts/openwhispr/templates/migrate-serviceaccount.yaml"
      provides: "Dedicated ServiceAccount + minimal RBAC for the migrate Job (no cluster-scoped perms needed; namespace-scoped read-only to Secret)"
  key_links:
    - from: "charts/openwhispr/templates/migrate-job.yaml"
      to: "charts/openwhispr/templates/postgres-cluster.yaml"
      via: "initContainer pg_isready against {fullname}-pg-rw:5432"
      pattern: "pg_isready.*-pg-rw"
    - from: "charts/openwhispr/templates/migrate-job.yaml"
      to: "drizzle/<timestamp>.sql"
      via: "command node /app/tools/migrate.js runs Drizzle migrations"
      pattern: "tools/migrate.js"
---

# Plan 09-08: Migrate Job (pre-install/pre-upgrade Helm Hook)

<objective>
Template the migrate Job with proper Helm hook annotations + the pg_isready initContainer that closes pitfall #15 (helm `--wait` doesn't wait on CRD-backed resources like CNPG Cluster). Hard-pin the image tag to `Chart.AppVersion` (T-09-07 mitigation).

Output: migrate Job template + ServiceAccount + snapshot tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-helm-chart-and-cloud-deploy/09-RESEARCH.md
@docker-compose.yml
@CLAUDE.md

<interfaces>
Helm hook annotations (per research §"Migration Job"):
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation

Image: {fullname}/migrate (built by release.yml; pre-existing in compose).
Command: `node /app/tools/migrate.js` — invokes drizzle-kit migrate.

initContainer:
  image: postgres:17-alpine
  command: ["sh", "-c", "until pg_isready -h $(POSTGRES_HOST) -p 5432 -U $(POSTGRES_USER) -d openwhispr; do sleep 2; done"]
  env: POSTGRES_HOST={fullname}-pg-rw, POSTGRES_USER=openwhispr_owner
  Timeout: 60s via initContainer-level activeDeadlineSeconds? No — initContainers don't honor activeDeadlineSeconds at container level; use Job-level activeDeadlineSeconds: 300 (5 min — long enough for slow CNPG bootstrap).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migrate-job.yaml + ServiceAccount + snapshot tests + rolling-deploy safety</name>
  <files>charts/openwhispr/templates/migrate-job.yaml, charts/openwhispr/templates/migrate-serviceaccount.yaml, charts/openwhispr/values.yaml, charts/openwhispr/values.schema.json, charts/openwhispr/tests/migrate_test.yaml, tools/compose-chart-parity.allowlist.json</files>
  <behavior>
    - RED: migrate_test.yaml asserts annotation `helm.sh/hook: pre-install,pre-upgrade` exists (fails first since file doesn't exist)
    - GREEN: full Job renders with the 4 hook annotations, backoffLimit 0, activeDeadlineSeconds 300, initContainer pg_isready loop, container image `{{ .Values.image.repository }}/migrate:{{ .Values.image.migrate.tag | default .Chart.AppVersion }}`, command `["node", "/app/tools/migrate.js"]`, DATABASE_URL pointing at -pg-rw (NOT pooler — migrations need direct connection per Drizzle/PgBouncer transaction-mode incompatibility)
    - Snapshot test asserts DATABASE_URL does NOT contain `-pooler` (transaction pooling breaks Drizzle's DDL)
    - Snapshot test asserts image tag is NOT `latest` and NOT empty string (T-09-07)
    - ServiceAccount renders with minimal RBAC (no special perms; just exists for audit/identity)
    - Rolling-deploy safety doc: README section on "Helm chart migrations follow expand → switch → contract across release boundaries; squawk gate enforces per-PR; multi-release expand/contract dance is documented in docs/operations.md (Phase 10)"
  </behavior>
  <action>Implement per interface. Add Job activeDeadlineSeconds 300 so a hung migration fails the helm install/upgrade within 5 minutes (vs hanging forever). Mount the secret as envFrom for DATABASE_URL credentials. Per pitfall #9 (Drizzle migrations pass squawk individually but break rolling deploy), add a snapshot test that scans the rendered template and warns (does not block at render time) if any `DROP COLUMN` or `DROP TABLE` is committed — this is a chart-side complement to the squawk PR-gate. Remove `migrate` from compose-chart-parity allowlist (now mapped).</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f 'tests/migrate_test.yaml' && helm template charts/openwhispr -f .github/ci/values-ci.yaml 2>/dev/null | grep -A20 'name:.*migrate' | grep -c 'helm.sh/hook: pre-install,pre-upgrade'</automated>
  </verify>
  <done>Migrate Job renders with all 4 hook annotations + pg_isready initContainer + pinned image + direct -rw DATABASE_URL.</done>
</task>

</tasks>

<verification>
- All 4 hook annotations present
- pg_isready initContainer present (closes pitfall #15)
- Image tag pinned (T-09-07)
- DATABASE_URL bypasses pooler
</verification>

<success_criteria>
- DEPLOY-03 migrate-job portion done
- DEPLOY-04 pre-deploy Job pattern in place
- Pitfall #15 (helm --wait + CRDs) structurally mitigated
</success_criteria>

<output>
After completion, create `.planning/phases/09-helm-chart-and-cloud-deploy/09-08-SUMMARY.md`.
</output>
