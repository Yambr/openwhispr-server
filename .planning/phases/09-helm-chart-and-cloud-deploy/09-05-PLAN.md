---
phase: 09
plan: 05
type: execute
wave: 1
depends_on:
  - "04"
files_modified:
  - charts/openwhispr/templates/pooler.yaml
  - charts/openwhispr/templates/pooler-userlist-secret.yaml
  - charts/openwhispr/Chart.yaml
  - charts/openwhispr/Chart.lock
  - charts/openwhispr/values.yaml
  - charts/openwhispr/values.schema.json
  - charts/openwhispr/tests/pooler_test.yaml
  - charts/openwhispr/tests/subcharts_test.yaml
  - charts/openwhispr/charts/.gitignore
  - docs/licensing-bitnami-2024.md
autonomous: true
requirements: [DEPLOY-02]
must_haves:
  truths:
    - "CNPG `Pooler` CR renders pointing at `{fullname}-pg` Cluster with `pgbouncer` type and transaction mode (per A6)"
    - "Pooler exposes connection string via `{fullname}-pg-pooler` Service consumable as DATABASE_URL"
    - "Chart.yaml includes Bitnami Valkey + MinIO as dependencies pinned to specific versions"
    - "values.valkey passthrough disables HA in OSS quickstart (architecture=standalone) and enables it (architecture=replication) in cloud-ha overlay"
    - "values.minio passthrough configures single-node single-disk in OSS overlay; cloud-ha overlay sets mode=distributed with 4 nodes"
    - "Bitnami license note documented in docs/licensing-bitnami-2024.md including the 2024 license change and the corporate-override path to Option B (operators)"
    - "Snapshot tests assert: pooler renders pgbouncer config with pool_mode=transaction and default_pool_size=100; sub-charts only render when their values trees are present"
  artifacts:
    - path: "charts/openwhispr/templates/pooler.yaml"
      provides: "CNPG Pooler CR: type=pgbouncer, instances=2, pgbouncer.poolMode=transaction, parameters max_client_conn / default_pool_size, monitoring.enablePodMonitor=true"
    - path: "charts/openwhispr/Chart.yaml"
      provides: "Updated dependencies block: valkey (Bitnami) + minio (Bitnami), pinned versions, condition gates on values.valkey.enabled / values.minio.enabled"
    - path: "docs/licensing-bitnami-2024.md"
      provides: "Documentation of Bitnami license change + verification that current Helm chart licensing remains Apache 2.0 + corporate override path to Option B (operators)"
  key_links:
    - from: "charts/openwhispr/templates/pooler.yaml"
      to: "charts/openwhispr/templates/postgres-cluster.yaml"
      via: "spec.cluster.name references {fullname}-pg"
      pattern: "cluster:.*name:.*-pg"
    - from: "charts/openwhispr/Chart.yaml"
      to: "https://charts.bitnami.com/bitnami"
      via: "dependencies.[].repository"
      pattern: "charts.bitnami.com"
---

# Plan 09-05: CNPG Pooler (A6) + Bitnami Valkey + MinIO Sub-Charts (A5)

<objective>
Replace the compose 4-instance PgBouncer pattern with the CNPG `Pooler` CRD (per A6 lock — less code, native HA, automatic SCRAM credential rotation). Vendor Bitnami Valkey + MinIO sub-charts via Chart.yaml dependencies (per A5 lock). Document the Bitnami 2024 license change so corporate operators have a clear override path.

Pitfall #10 (CNPG Pooler doesn't expose the Phase 01.2 userlist.txt SCRAM-regen pattern) is mitigated by accepting CNPG's native credential management — the Phase 08.1 admin password fix becomes built-in.

Output: Pooler template + Chart.yaml dependencies + values trees + sub-chart snapshot tests + license note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-helm-chart-and-cloud-deploy/09-RESEARCH.md
@.planning/phases/09-helm-chart-and-cloud-deploy/09-DISCUSS-BLOCKERS.md
@.planning/phases/09-04-SUMMARY.md
@docker-compose.yml
@CLAUDE.md

<interfaces>
CNPG Pooler API (postgresql.cnpg.io/v1, kind: Pooler):
  spec.cluster.name: {fullname}-pg
  spec.instances: 2 (HA)
  spec.type: pgbouncer
  spec.pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "100"
      reserve_pool_size: "20"
    paused: false
  spec.monitoring.enablePodMonitor: true

Bitnami Valkey chart: oci://registry-1.docker.io/bitnamicharts/valkey, pinned 2.x (verify latest).
Bitnami MinIO chart: oci://registry-1.docker.io/bitnamicharts/minio, pinned 14.x.

Connection string consumers: api / worker plans (09-06, 09-08) will use:
  DATABASE_URL=postgres://openwhispr_owner@{fullname}-pg-pooler-rw:5432/openwhispr
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pooler CR template + snapshot tests</name>
  <files>charts/openwhispr/templates/pooler.yaml, charts/openwhispr/templates/pooler-userlist-secret.yaml, charts/openwhispr/values.yaml, charts/openwhispr/values.schema.json, charts/openwhispr/tests/pooler_test.yaml</files>
  <behavior>
    - RED: pooler_test.yaml asserts Pooler.spec.cluster.name == {fullname}-pg
    - GREEN: Rendered Pooler has type=pgbouncer, pgbouncer.poolMode=transaction, default_pool_size=100, instances=2
    - With values.pooler.enabled=false, the template emits nothing (compose-parity allowlist not needed)
    - monitoring.enablePodMonitor=true so PodMonitor is picked up by Wave-3 OTel plan (09-10) ServiceMonitor sweep
    - pooler.parameters values overridable per cloud-ha overlay (max_client_conn 2000)
  </behavior>
  <action>Implement the Pooler CR per interface. Add values.pooler tree (enabled, instances, poolMode, max_client_conn, default_pool_size, reserve_pool_size). values.schema.json validates poolMode enum [session, transaction, statement] and integer bounds. Pitfall #10 carry-over: do NOT replicate the Phase 01.2 userlist.txt initContainer pattern (Pooler handles credentials natively). Remove the temporary allowlist entry `pgbouncer` from `tools/compose-chart-parity.allowlist.json` (since Pooler now maps to the compose `pgbouncer` service — the parity check needs to know this mapping; either remove allowlist entry OR add a service-name-alias map to lint-compose-chart-parity.ts; prefer the alias map for clarity).</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f 'tests/pooler_test.yaml'</automated>
  </verify>
  <done>Pooler renders; allowlist alias map updated; snapshot tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bitnami Valkey + MinIO sub-chart dependencies + license note</name>
  <files>charts/openwhispr/Chart.yaml, charts/openwhispr/Chart.lock, charts/openwhispr/values.yaml, charts/openwhispr/charts/.gitignore, charts/openwhispr/tests/subcharts_test.yaml, docs/licensing-bitnami-2024.md</files>
  <behavior>
    - `helm dependency update charts/openwhispr` resolves both deps; Chart.lock pinned
    - charts/openwhispr/charts/ gitignored (vendored on dep-update, not committed)
    - subcharts_test.yaml asserts Valkey StatefulSet rendered with single replica in OSS quickstart; rendered with replication in cloud-ha overlay
    - subcharts_test.yaml asserts MinIO StatefulSet rendered with mode=standalone in OSS, distributed (4 nodes) in cloud-ha
    - docs/licensing-bitnami-2024.md documents: (a) the 2024 Bitnami license change (Bitnami Secure premium tier) (b) confirms the Helm charts themselves remain Apache 2.0 (c) corporate operators wanting Option B (operators) override path: documented examples/cloud-redis-operator-values.yaml + examples/cloud-minio-operator-values.yaml deferral note
    - English-only per CLAUDE.md constitutional rule
  </behavior>
  <action>Pin Valkey to a verified version (run `helm search repo bitnami/valkey --versions | head` mentally; pin to latest minor available at plan time, e.g. 2.x). Pin MinIO to 14.x. Add `condition: valkey.enabled` and `condition: minio.enabled` on each dependency. values.yaml adds the valkey and minio trees passing through Bitnami chart values (architecture, replica.replicaCount, mode, statefulset.replicaCount, persistence.size, auth.* — auth from the existing Secret). docs/licensing-bitnami-2024.md is a short factual note; cite Bitnami's 2024 changelog. The Option B operator override path is a deferred enhancement; do not implement the operator paths here.</action>
  <verify>
    <automated>helm dependency update charts/openwhispr && helm unittest charts/openwhispr -f 'tests/subcharts_test.yaml' && wc -l docs/licensing-bitnami-2024.md | awk '{ exit ($1 &gt; 10 ? 0 : 1) }'</automated>
  </verify>
  <done>Deps resolve; sub-chart tests green; license note covers the 2024 change.</done>
</task>

</tasks>

<verification>
- helm dependency update clean
- Pooler renders correctly
- Sub-chart snapshot tests green
- License note exists and addresses the 2024 change
</verification>

<success_criteria>
- A5 (Bitnami) and A6 (Pooler) decisions implemented
- Compose-parity allowlist updated with `pgbouncer→pooler` alias
- Bitnami license clarity ships
</success_criteria>

<output>
After completion, create `.planning/phases/09-helm-chart-and-cloud-deploy/09-05-SUMMARY.md`.
</output>
