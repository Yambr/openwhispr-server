---
phase: 07.1-web-app-implementation
plan: 12
type: execute
wave: 2
depends_on: [05, 06]
files_modified:
  - apps/web/src/app/(admin)/admin/observability/page.tsx
  - apps/web/src/app/(admin)/admin/config/page.tsx
  - apps/web/src/components/screens/admin/ObservabilityClient.tsx
  - apps/web/src/components/screens/admin/ConfigClient.tsx
  - apps/web/src/components/screens/admin/__tests__/ObservabilityClient.test.tsx
  - apps/web/src/components/screens/admin/__tests__/ConfigClient.test.tsx
  - apps/web/tests/e2e/a2-observability.spec.ts
  - apps/web/tests/e2e/a3-config.spec.ts
autonomous: true
requirements: [WEB-IMPL-02, WEB-IMPL-04]
must_haves:
  truths:
    - "A2 /admin/observability renders 6 dashboard cards + Quick-links from NEXT_PUBLIC_* env (zero API calls on this server)"
    - "A2 missing NEXT_PUBLIC_GRAFANA_BASE_URL renders error Alert with operator instructions"
    - "A3 /admin/config fetches GET /api/stt-config + GET /api/note-recording-config in parallel"
    - "A3 contains NO env-block (D-API4 — removed in v1)"
    - "All four states per screen (loading/empty/error/success) + axe clean"
    - "Admin pages do NOT perform application-layer role check (D-ADMIN-1)"
  artifacts:
    - { path: "apps/web/src/app/(admin)/admin/observability/page.tsx", provides: "A2 RSC entry" }
    - { path: "apps/web/src/app/(admin)/admin/config/page.tsx", provides: "A3 RSC entry" }
  key_links:
    - { from: "A2 ObservabilityClient", to: "NEXT_PUBLIC_GRAFANA_BASE_URL", via: "process.env at build time", pattern: "NEXT_PUBLIC_GRAFANA_BASE_URL" }
    - { from: "A3 ConfigClient", to: "GET /api/stt-config + /api/note-recording-config", via: "two useQuery in parallel", pattern: "sttConfig|noteRecordingConfig" }
---

<role>
Vertical slice for the admin surface — A2 Observability hub (pure static cards, zero API calls) and A3 Config view (two parallel GETs). Runs in parallel with Wave 2 Plan 07 because admin auth is Traefik-gated, NOT session-gated (D-ADMIN-1).

D-API4 — A3 has NO "Effective env" block.
D-ADMIN-1 — NO application-layer role check; the `(admin)/layout.tsx` from Plan 06 already lacks a session gate.
</role>

<context>
@.planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md (A2, A3)
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
</context>

<files_to_read>
- UI-SPEC-admin.md A2 + A3 sections (States, Copy keys, Wireframe, primitives, journey)
- 07-CONTEXT.md D-API4 (no env-block in A3), D-ADMIN-1 (Traefik gate, no app role check)
- apps/api/src/routes/stt-config.ts + note-recording-config.ts (verify response shape)
- Plan 03 .env.example for NEXT_PUBLIC_GRAFANA_BASE_URL etc.
</files_to_read>

<files_to_modify>
See frontmatter. Disjoint from Plan 07 — Plan 12 owns `(admin)/*` exclusively.
</files_to_modify>

<tests>
RED commit `test(07.1-12): add failing tests for A2 + A3 + state matrix`

1. vitest:
   - ObservabilityClient (Client because reads `process.env.NEXT_PUBLIC_*` which Next inlines at build into client bundles):
     - With NEXT_PUBLIC_GRAFANA_BASE_URL set → 6 dashboard cards (api-latency, worker-queue, postgres, litellm, security, system) each as anchor with `href` composed from grafana base + dashboard slug; all anchors have `target="_blank" rel="noopener noreferrer"`
     - Without GRAFANA env → renders Alert with `admin.observability.error-env-missing.{title,body}.label`
     - Quick-links: Loki/Mimir/Tempo/Alertmanager rows; if TEMPO/MIMIR/LOKI env unset, link defaults to Grafana root
   - ConfigClient — two `useQuery` (sttConfig, noteRecordingConfig); loading→two Skeleton Tables; success→two Tables with rows from response; error→Alert with Retry that refetches both queries; partial-success degrades gracefully (one Card with data, one with Alert); NO env-block
2. Playwright a2/a3 specs — 4 states + axe:
   - A2 loading: N/A per UI-SPEC. Test that page renders without async wait
   - A2 empty: N/A
   - A2 error: unset NEXT_PUBLIC_GRAFANA_BASE_URL in env for this test run (set to empty string via Playwright `route.continue` is irrelevant since env is build-time — instead, use a separate test build OR test the error state via component vitest only; in Playwright, assert that with env set, the error Alert is absent)
   - A2 success: assert 6 dashboard cards + 4 quick-links present; click one → opens new tab (verify `target="_blank"` attribute)
   - A2 axe clean
   - A3 loading: loadingFor /api/stt-config AND loadingFor /api/note-recording-config
   - A3 empty: N/A (config always returns defaults)
   - A3 error: errorFor /api/stt-config 500 → Alert + Retry
   - A3 success: seeded defaults from compose env → both Tables populated
   - A3 axe clean
   - For both screens: assert Playwright spec runs with basic-auth (Playwright `httpCredentials` config in spec OR setExtraHTTPHeaders Authorization Basic header derived from ADMIN_BASIC_AUTH_USERS test value `admin:testpw123`)
3. RED, commit.

GREEN commit `feat(07.1-12): A2 observability hub + A3 config view`

4. Write `(admin)/admin/observability/page.tsx` — RSC that just renders `<ObservabilityClient />`. No prefetch (static cards).
5. Write `ObservabilityClient.tsx` (`'use client'`):
   - Reads `process.env.NEXT_PUBLIC_GRAFANA_BASE_URL` / TEMPO / MIMIR / LOKI directly
   - If GRAFANA empty → renders Alert per UI-SPEC error state
   - Else: header "Observability" + Open-Grafana button → grafana root; Card grid (lg:grid-cols-2) of 6 dashboard cards each pointing at a Grafana dashboard slug (use canonical Phase 6 Plan 11 dashboard ids per UI-SPEC; the slug is part of the URL path appended to `grafanaBaseUrl`); Quick-links Card with 4 rows
   - All external links: `target="_blank" rel="noopener noreferrer"`
6. Write `(admin)/admin/config/page.tsx` — RSC with `makeServerQueryClient()` + two `prefetchQuery` (sttConfig + noteRecordingConfig); wrap in HydrationBoundary; render `<ConfigClient />`.
7. Write `ConfigClient.tsx`:
   - Two `useQuery` (parallel); both render `Skeleton` Table on isPending
   - Success: two `Card` blocks side-by-side (lg) or stacked (mobile); each contains a `Table` with rows from UI-SPEC A3 Data table (default model, default language, providers, max duration, sample rate, allowed formats, diarization Badge)
   - Refresh button → invalidate both query keys
   - Docs link → `docs/litellm-target-spec.md` in new tab
   - NO env-block (D-API4) — do not add even as a placeholder
8. Run tests — green. Coverage ≥ 90/90/90/90.
9. Commit.

Constraints:
- A2 zero API calls on THIS server — only external links to operator's Grafana.
- A3 only `GET /api/stt-config` + `GET /api/note-recording-config` (D-S1).
- D-API4: NO env-block.
- D-ADMIN-1: NO application-layer role check. Test that visiting `/admin/*` works for any user the basic-auth gate admits.
- All `NEXT_PUBLIC_*` env reads happen at build time — document this in the commit body so operators know to rebuild on env change.
</tests>

<commit_message>
test(07.1-12): add failing tests for A2 observability + A3 config + state matrix
feat(07.1-12): A2 observability hub + A3 config view — admin slice complete

- A2 /admin/observability: 6 dashboard cards + Quick-links from NEXT_PUBLIC_*
  env (zero API calls on this server); error Alert when GRAFANA env unset
- A3 /admin/config: two parallel queries (GET /api/stt-config + GET
  /api/note-recording-config); two Tables in Cards; Refresh invalidates both
- D-API4: NO env-block in A3 (removed in v1)
- D-ADMIN-1: NO application-layer role check; Traefik basic-auth is the gate
- All external links target="_blank" rel="noopener noreferrer"
- 4 UI states × 2 screens + axe WCAG 2.2 AA per screen
- Playwright specs use httpCredentials for basic-auth
- D-S1: zero new API endpoints
- Coverage ≥ 90/90/90/90

Refs: WEB-IMPL-02, WEB-IMPL-04, D-API4, D-ADMIN-1, D-S1
</commit_message>
