---
phase: 07.1-web-app-implementation
plan: 10
type: execute
wave: 3
depends_on: [07]
files_modified:
  - apps/web/src/app/(auth)/app/notes/page.tsx
  - apps/web/src/app/(auth)/app/notes/[id]/page.tsx
  - apps/web/src/app/(auth)/app/notes/search/page.tsx
  - apps/web/src/components/screens/notes/NotesListClient.tsx
  - apps/web/src/components/screens/notes/FoldersSidebar.tsx
  - apps/web/src/components/screens/notes/NoteDetailClient.tsx
  - apps/web/src/components/screens/notes/NotesSearchClient.tsx
  - apps/web/src/components/screens/notes/__tests__/*.test.tsx
  - apps/web/tests/e2e/u8-notes-list.spec.ts
  - apps/web/tests/e2e/u9-note-detail.spec.ts
  - apps/web/tests/e2e/u10-notes-search.spec.ts
autonomous: true
requirements: [WEB-IMPL-02, WEB-IMPL-04]
must_haves:
  truths:
    - "U8 /app/notes renders folder sidebar (read-only per D-UX5) + notes table"
    - "U8 folder click filters notes by folder_id in query key"
    - "U9 /app/notes/[id] renders note with tabs (Content / Transcript / Enhanced if present) + metadata sidebar"
    - "U10 /app/notes/search renders search input → POST /api/notes/search with debounce 300ms"
    - "Folders are read-only (no create/rename/delete UI per D-UX5)"
    - "4 UI states per screen + axe clean"
  artifacts:
    - { path: "apps/web/src/app/(auth)/app/notes/page.tsx", provides: "U8 RSC entry" }
    - { path: "apps/web/src/app/(auth)/app/notes/[id]/page.tsx", provides: "U9 RSC entry" }
    - { path: "apps/web/src/app/(auth)/app/notes/search/page.tsx", provides: "U10 RSC entry" }
  key_links:
    - { from: "NotesListClient", to: "GET /api/notes/list + GET /api/folders/list", via: "two parallel useQuery", pattern: "folders/list" }
    - { from: "NotesSearchClient", to: "POST /api/notes/search", via: "useMutation OR useQuery with debounced enabled", pattern: "notes/search" }
---

<role>
Vertical slice for notes (U8 list with folder sidebar, U9 detail, U10 search). D-UX5: folders are READ-ONLY in web (desktop owns writes); no create/rename/delete UI.
</role>

<context>
@.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
</context>

<files_to_read>
- UI-SPEC-end-user.md U8 + U9 + U10 sections
- 07-CONTEXT.md D-UX5 (folders read-only)
- apps/api/src/routes/notes/{list,search,delete}.ts + folders/list.ts (verify shapes)
- packages/wire-schemas/ NoteInput, CloudNote, SearchResult
</files_to_read>

<files_to_modify>
See frontmatter. Owns its own folder.
</files_to_modify>

<tests>
RED commit `test(07.1-10): add failing tests for U8 + U9 + U10 + state matrix`

1. vitest:
   - FoldersSidebar — lists folders from useQuery(queryKeys.folders()); shows readonly-body copy (`end-user.notes-list.folders.readonly-body.text`); selecting a folder updates URL search param `?folder=`
   - NotesListClient — Table columns: Title, Folder, Words, Created; Load-more; Delete row mutation
   - NoteDetailClient — Tabs (Content / Transcript / Enhanced — render only tabs whose content key is non-empty); metadata Card with Created, Folder, Audio duration, Participants (if note_type === 'meeting'), Note type
   - NotesSearchClient — debounced input (300ms), POST /api/notes/search with `{ q, limit: 20 }`; renders SearchResult rows with score badge; empty-type copy when q is empty; empty-none copy when q non-empty but zero results
2. Playwright u8/u9/u10 specs covering 4 states × 3 screens + axe. Seed via seedNotes/seedFolders fixtures.
3. RED, commit.

GREEN commit `feat(07.1-10): U8 notes list + folders sidebar + U9 detail + U10 search`

4. Implement files per RESEARCH patterns. Notes/search uses POST per Appendix D D-API endpoint table. Folder sidebar uses shadcn Sheet on mobile.
5. Tests green, coverage ≥ 90/90/90/90.
6. Commit.

Constraints:
- D-UX5: zero folder mutation UI — only render the list.
- D-S1: only GET /api/notes/list, POST /api/notes/search, DELETE /api/notes/delete, GET /api/folders/list.
- Search debounce via `useDeferredValue` or simple setTimeout-in-useEffect (300ms).
- Score badge formatted to 2 decimals.
</tests>

<commit_message>
test(07.1-10): add failing tests for U8/U9/U10 notes slice + state matrix
feat(07.1-10): U8 notes list + folders sidebar + U9 detail + U10 search

- U8 /app/notes: FoldersSidebar (read-only per D-UX5) + Notes Table; folder
  filter via ?folder= query param; Delete row → DELETE /api/notes/delete
- U9 /app/notes/[id]: Tabs Content/Transcript/Enhanced (render only non-empty)
  + metadata Card; Copy/Export JSON/Export MD/Delete actions
- U10 /app/notes/search: debounced (300ms) search → POST /api/notes/search
  with score-sorted results; empty/error/loading states per UI-SPEC
- 4 UI states × 3 screens + axe WCAG 2.2 AA per screen
- D-S1: zero new API endpoints; D-UX5: folders strictly read-only
- Coverage ≥ 90/90/90/90

Refs: WEB-IMPL-02, WEB-IMPL-04, D-UX5, D-S1
</commit_message>
