---
phase: 07.1-web-app-implementation
type: index
total_plans: 14
waves: 5
requirements: [WEB-IMPL-01, WEB-IMPL-02, WEB-IMPL-03, WEB-IMPL-04]
generated: 2026-05-12
---

# Phase 07.1 — Web App Implementation — Plan Index

Vertical-slice TDD implementation of `apps/web/` (Next.js 15 + React 19 + Tailwind 4 + shadcn/ui v2) delivering every UI-SPEC screen (A2, A3, U1–U13) at the exact route paths the spec names, with Playwright e2e (15 screens × 4 UI states + axe-core WCAG 2.2 AA, ~75 tests) running against the real docker-compose stack.

## Wave Structure

| Wave | Plans | Parallel? | Depends on | Theme |
|------|-------|-----------|------------|-------|
| 0 | 01, 02, 03, 04 | sequential | — | Infrastructure: scaffold, shadcn, compose+traefik, vitest+playwright |
| 1 | 05, 06 | sequential | Wave 0 | Foundations: Better Auth client+server+middleware, providers (Query/i18n/RHF) |
| 2 | 07 (gating) | — | 05, 06 | Auth slice (U1, U2, U3) — gates all authenticated slices |
| 2 | 12 | parallel with 07 | 05, 06 | Admin slice (A2, A3) — admin is Traefik-gated, not session-gated |
| 3 | 08, 09, 10, 11 | parallel | 07 | Account+Usage (U4,U5), Transcriptions (U6,U7), Notes (U8,U9,U10), Conversations (U11,U12,U13) |
| 4 | 13 | — | 07–12 | Integration: cross-screen smoke, size-limit, perf, GHA workflow, lefthook |
| 5 | 14 | — | 13 | Finalize: full sweep, SUMMARY.md, STATE.md + ROADMAP.md update |

## File-Ownership Matrix (Wave 2 + Wave 3 parallel slices)

| Plan | Owns (writes) |
|------|---------------|
| 07 Auth | `apps/web/src/app/(public)/sign-in/`, `(public)/sign-up/`, `(public)/verify-email/`, `apps/web/src/components/screens/auth/`, `apps/web/tests/e2e/u1-sign-in.spec.ts`, `u2-sign-up.spec.ts`, `u3-verify-email.spec.ts`, `apps/web/src/lib/schemas/auth.ts` |
| 08 Account+Usage | `apps/web/src/app/(auth)/app/page.tsx`, `apps/web/src/app/(auth)/app/account/`, `apps/web/src/components/screens/usage/`, `apps/web/src/components/screens/account/`, `apps/web/tests/e2e/u4-usage.spec.ts`, `u5-account.spec.ts` |
| 09 Transcriptions | `apps/web/src/app/(auth)/app/transcriptions/`, `apps/web/src/components/screens/transcriptions/`, `apps/web/tests/e2e/u6-trx-list.spec.ts`, `u7-trx-detail.spec.ts` |
| 10 Notes | `apps/web/src/app/(auth)/app/notes/`, `apps/web/src/components/screens/notes/`, `apps/web/tests/e2e/u8-notes-list.spec.ts`, `u9-note-detail.spec.ts`, `u10-notes-search.spec.ts` |
| 11 Conversations | `apps/web/src/app/(auth)/app/conversations/`, `apps/web/src/components/screens/conversations/`, `apps/web/tests/e2e/u11-conv-list.spec.ts`, `u12-conv-detail.spec.ts`, `u13-conv-search.spec.ts` |
| 12 Admin | `apps/web/src/app/(admin)/`, `apps/web/src/components/screens/admin/`, `apps/web/tests/e2e/a2-observability.spec.ts`, `a3-config.spec.ts` |

Disjoint by construction — each slice owns its own folder under `apps/web/src/app/`, its own components subfolder, and its own e2e spec files. The only shared mutable surface is `apps/web/src/locales/en/{admin,end-user,common}.json`; plans append disjoint key namespaces (Plan 07 → `end-user.signin.*`, `end-user.signup.*`, `end-user.verify.*`; Plan 08 → `end-user.usage.*`, `end-user.account.*`; etc.). Locale-file ordering merge conflicts are avoided by appending — never reordering.

## Goal-Backward Verification Checklist

Each item below maps to a plan task. The Wave 5 verifier asserts every line passes.

1. `apps/web/` directory exists; `pnpm --filter @openwhispr/web typecheck` exits 0 → Plan 01 task 1
2. `apps/web/next.config.ts` sets `output: 'standalone'` + `outputFileTracingRoot` + security headers (CSP/HSTS/X-Frame-Options DENY/Referrer-Policy/Permissions-Policy) → Plan 01 task 2 (WEB-IMPL-03 acceptance)
3. Tailwind 4 `@theme` block in `globals.css` transcribes Appendix A tokens verbatim → Plan 01 task 3
4. `components.json` exists; full shadcn primitive inventory (Appendix E, 16 primitives) installed under `apps/web/src/components/ui/` → Plan 02 task 1
5. `docker-compose.yml` has `web` service with traefik labels; Traefik routes `/` → web, `/api/*` → api → Plan 03 task 1 (WEB-IMPL-03 acceptance)
6. `/admin/*` returns 401 without basic-auth header; returns 200 with valid `ADMIN_BASIC_AUTH_USERS` credential (Plan 03 e2e smoke) → Plan 03 task 2 (D-ADMIN-1)
7. `playwright.config.ts` + `vitest.config.ts` + fixtures (`auth.ts`, `seed.ts`, `states.ts`) exist; `pnpm --filter @openwhispr/web test --coverage` runs and `90/90/90/90` thresholds defined → Plan 04
8. `better-auth/react` client exports `useSession`, `signIn.email`, `signIn.social`, `signUp.email`, `signOut`, `verifyEmail`, `revokeSession`, `revokeOtherSessions`, `deleteAccount` (smoke step verifies 1.6.9 surface per RESEARCH OQ4 / A2) → Plan 05 task 1
9. `(auth)/layout.tsx` calls `auth.api.getSession({ headers })`; redirects to `/sign-in` on null → Plan 05 task 2
10. `middleware.ts` uses `getSessionCookie` (Edge-safe existence check) for `/app/:path*`, not `/admin/*` → Plan 05 task 3 (D-ADMIN-1)
11. TanStack Query provider + query-keys factory + react-hook-form/zod resolver + i18next provider (App-Router native, NOT next-i18next) wired in root layout → Plan 06
12. U1 sign-in renders all four UI states (loading/empty/error/success) + axe clean; clicking submit with valid credentials lands on `/app` (real backend) → Plan 07 (D-TEST-1, D-TEST-3, D-UX2: forgot-password disabled)
13. U2 sign-up + U3 verify-email cover same state matrix + axe → Plan 07
14. U4 Usage = KPI cards only (D-STACK-6 — no Recharts; D-API6 — no activity feed; data from `GET /api/usage`) → Plan 08
15. U5 Account renders profile + sessions table + Delete-account dialog; uses Better Auth `list-sessions` / `revoke-session` / `revoke-other-sessions` / `delete-account` paths → Plan 08
16. U6 Transcriptions list + U7 detail (flat paragraphs, NO timecodes per D-API1) using `GET /api/transcriptions/list` + `DELETE /api/transcriptions/delete` → Plan 09
17. U8 Notes list with folder sidebar (read-only per D-UX5) + U9 detail + U10 search; uses `GET /api/notes/list`, `POST /api/notes/search`, `GET /api/folders/list`, `DELETE /api/notes/delete` → Plan 10
18. U11 Conversations list + U12 detail with messages keyset pagination + U13 search; uses `GET /api/conversations/list`, `GET /api/conversations/messages`, `POST /api/conversations/search`, `DELETE /api/conversations/delete` → Plan 11
19. A2 Observability hub static cards from `NEXT_PUBLIC_GRAFANA_BASE_URL` (+ optional Tempo/Mimir/Loki); zero API calls on this server → Plan 12
20. A3 Config view fetches `GET /api/stt-config` + `GET /api/note-recording-config` in parallel; no env-block (D-API4) → Plan 12
21. Bundle size-limit ≤200KB gz per route across the 10 declared routes → Plan 13 (D-PERF-1, WEB-IMPL-01 acceptance)
22. `.github/workflows/web.yml` runs typecheck → vitest+coverage → build → size-limit → playwright against real docker compose → Plan 13 (D-CI-1, WEB-IMPL-04 acceptance)
23. `lefthook.yml` extended with `web` group (pre-commit typecheck + biome on staged `apps/web/**`; pre-push vitest) → Plan 13 (D-CI-2)
24. Cross-screen smoke: sign-in → notes list → note detail → delete → sign-out flow passes end-to-end against real api+postgres+valkey+better-auth → Plan 13 (CLAUDE.md e2e mandate)
25. Vitest coverage report shows ≥90/90/90/90 on diff for the phase touchset → Plan 14 (CLAUDE.md TDD-02 + WEB-IMPL-04 acceptance)
26. 15 screens × 4 states + axe-core scan ≈75 Playwright tests green (D-TEST-1) → Plan 14
27. No emojis in source; English-only artifacts; no `next-i18next`; no ESLint (Biome only); no Recharts; no localStorage tokens; no app-level admin role check → Plan 14 audit (D-STACK-6, D-STACK-7, D-SEC-2, D-ADMIN-1, CLAUDE.md)
28. `STATE.md` + `ROADMAP.md` updated; `07.1-SUMMARY.md` written with checklist results → Plan 14

## Requirement Coverage

| Req | Plans |
|-----|-------|
| WEB-IMPL-01 (apps/web scaffold + bundle gate) | 01, 02, 04, 13 |
| WEB-IMPL-02 (every UI-SPEC screen at spec routes) | 07, 08, 09, 10, 11, 12 |
| WEB-IMPL-03 (same-origin Traefik deploy + admin gate + security headers) | 01, 03 |
| WEB-IMPL-04 (Playwright e2e + axe + coverage) | 04, 07–12, 13, 14 |

## TDD Discipline

Every plan in waves 1–3 follows the constitutional cycle:
- **RED commit** `test(07.1-NN): add failing test for <slice>` — tests committed first; CI shows them failing
- **GREEN commit** `feat(07.1-NN): implement <slice>` — production code lands together with test fixes, suite goes green
- **REFACTOR commit** (optional) `refactor(07.1-NN): clean up <slice>` — no behavior change, tests still green

Wave 0 plans land scaffolding via `chore(07.1-NN): ...`; their tests come from Plan 04 fixtures.

## Locked Decisions Honored

D-DEPLOY-1, D-DEPLOY-2 → Plan 01, 03
D-ADMIN-1, D-ADMIN-2 → Plan 03, 12
D-TEST-1, D-TEST-2, D-TEST-3, D-TEST-4 → Plan 04, every wave-2/3 plan, Plan 14
D-STACK-1..8 → Plan 01 (Next 15/React 19/TS strict/Tailwind 4), 02 (shadcn v2), 04 (vitest), 05 (better-auth/react 1.6.9), 06 (TanStack Q5 + RHF7 + zod 4 + i18next + date-fns), all wave-3 (no Recharts)
D-PERF-1, D-PERF-2 → Plan 13
D-SEC-1, D-SEC-2 → Plan 01, 05
D-STRUCT-1, D-STRUCT-2 → Plan 01
D-CI-1, D-CI-2 → Plan 13
