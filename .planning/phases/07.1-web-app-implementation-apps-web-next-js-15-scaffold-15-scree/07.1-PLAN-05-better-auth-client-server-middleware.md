---
phase: 07.1-web-app-implementation
plan: 05
type: execute
wave: 1
depends_on: [01, 02, 04]
files_modified:
  - apps/web/src/lib/auth-client.ts
  - apps/web/src/lib/auth-server.ts
  - apps/web/src/lib/http.ts
  - apps/web/src/middleware.ts
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/lib/__tests__/auth-client.test.ts
  - apps/web/src/lib/__tests__/auth-server.test.ts
  - apps/web/src/lib/__tests__/http.test.ts
  - apps/web/package.json
  - packages/auth/src/index.ts (verify/extend exports)
autonomous: true
requirements: [WEB-IMPL-02]
must_haves:
  truths:
    - "createAuthClient() exposes signIn.email, signIn.social, signUp.email, signOut, useSession, verifyEmail, revokeSession, revokeOtherSessions, deleteAccount, listSessions (smoke verifies 1.6.9 surface)"
    - "auth-server.getServerSession() returns Better Auth session shape using auth.api.getSession({ headers: await headers() })"
    - "middleware.ts uses getSessionCookie() and matcher /app/:path* only (NOT /admin/*)"
    - "(auth)/layout.tsx redirects to /sign-in when getServerSession() returns null"
    - "http.fetcher forwards cookies in RSC context via headers() (Pitfall 2 mitigation)"
  artifacts:
    - { path: "apps/web/src/lib/auth-client.ts", provides: "createAuthClient + exported hooks/methods" }
    - { path: "apps/web/src/lib/auth-server.ts", provides: "getServerSession() RSC helper" }
    - { path: "apps/web/src/middleware.ts", provides: "Edge-safe cookie existence gate" }
    - { path: "apps/web/src/app/(auth)/layout.tsx", provides: "real session validation + redirect" }
  key_links:
    - { from: "(auth)/layout.tsx", to: "apps/api Better Auth session store", via: "auth.api.getSession({ headers })", pattern: "auth\\.api\\.getSession" }
    - { from: "middleware.ts", to: "better-auth/cookies", via: "getSessionCookie(req)", pattern: "getSessionCookie" }
---

<role>
You wire Better Auth into apps/web on three surfaces: the React client (Client Components), the RSC server helper (RSC + Route Handlers), and the Edge middleware (cookie existence check only). You also write the (auth)/layout.tsx that gates every authenticated screen.

Strict TDD: every file under apps/web/src/lib/ ships with vitest unit tests in the same atomic commit. Coverage on these files MUST be ≥90/90/90/90.
</role>

<context>
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
@apps/api/src/auth.ts
@packages/auth/src/index.ts
</context>

<files_to_read>
- .planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md § Pattern 2, § Pattern 3, § Pattern 4, § Pitfall 2, § Pitfall 3, § Pitfall 8, § Assumptions A1 + A2 + OQ1
- .planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md (D-STACK-5 — better-auth 1.6.9; D-SEC-2; D-ADMIN-1 — no /admin matcher)
- apps/api/src/auth.ts (Better Auth instance factory — confirm session config, no cookie cache enabled)
- packages/auth/src/index.ts (existing exports — confirm whether `auth` instance is re-exported; if not, refactor minimally per RESEARCH OQ1)
- apps/api/package.json (confirm better-auth 1.6.9)
</files_to_read>

<files_to_modify>
- apps/web/package.json — add deps `better-auth@1.6.9` (pin exactly; verify against apps/api/package.json)
- packages/auth/src/index.ts — IF the existing `auth` instance is not re-exported, add `export { auth } from './server';` (and move the buildAuth call into `packages/auth/src/server.ts` if it currently lives in `apps/api/src/auth.ts`). RESEARCH OQ1: prefer minimal refactor — if `apps/api/src/auth.ts` exports `auth`, change `apps/api/src/index.ts` to import from `packages/auth` instead, and move auth.ts into packages/auth/src/server.ts. Coordinate this carefully to keep apps/api green.
- apps/web/src/lib/auth-client.ts — `'use client'` + `createAuthClient` + named exports (`useSession`, `signIn`, `signUp`, `signOut`)
- apps/web/src/lib/auth-server.ts — `getServerSession()` returns `auth.api.getSession({ headers: await headers() })` typed result
- apps/web/src/lib/http.ts — `serverFetch(path: string, init?: RequestInit)` for RSC; forwards cookie header (Pitfall 2); `clientFetch(path: string, init?: RequestInit)` for Client (relative path, browser carries cookies same-origin)
- apps/web/src/middleware.ts — verbatim from RESEARCH § Pattern 4
- apps/web/src/app/(auth)/layout.tsx — verbatim from RESEARCH § Pattern 2
- apps/web/src/lib/__tests__/auth-client.test.ts — vitest unit test
- apps/web/src/lib/__tests__/auth-server.test.ts — vitest unit test
- apps/web/src/lib/__tests__/http.test.ts — vitest unit test
</files_to_modify>

<task>
RED-then-GREEN cycle in TWO atomic commits.

**RED commit** `test(07.1-05): add failing tests for auth-client, auth-server, http, middleware`

1. Write `apps/web/src/lib/__tests__/auth-client.test.ts`:
   - Asserts default-export `authClient` has function-typed properties: `signIn.email`, `signIn.social`, `signUp.email`, `signOut`, `useSession`, `verifyEmail`, `revokeSession`, `revokeOtherSessions`, `deleteAccount`, `listSessions` (the surface RESEARCH A2 marks as "needs smoke verification against 1.6.9")
   - Asserts re-exports `useSession`, `signIn`, `signUp`, `signOut` are functions
2. Write `apps/web/src/lib/__tests__/auth-server.test.ts`:
   - Mock `next/headers` to return a Headers instance with a `cookie` value
   - Mock the imported `auth` module's `api.getSession` to resolve `{ session: { id: 's1' }, user: { id: 'u1', email: 'x@y' } }` for valid cookies and `null` for missing cookies
   - Assert `getServerSession()` resolves to the expected object when cookies present
   - Assert `getServerSession()` resolves to `null` when no session
3. Write `apps/web/src/lib/__tests__/http.test.ts`:
   - Mock `next/headers` and global `fetch`
   - Assert `serverFetch('/api/usage')` calls `fetch(INTERNAL_API_URL + '/api/usage', { headers: { cookie: '<from headers()>' }, ...})` per Pitfall 2
   - Assert `serverFetch` throws (or returns an error envelope per WIRE-17) on non-2xx
   - Assert `clientFetch('/api/usage')` calls `fetch('/api/usage', { credentials: 'include', ... })` and works against relative paths only (no INTERNAL_API_URL in client)
4. Write a Playwright spec `apps/web/tests/e2e/05-auth-middleware.spec.ts` (delete after GREEN if it bloats; OK to keep):
   - Visiting `/app` while signed-out redirects to `/sign-in`
   - Visiting `/admin/observability` while signed-out (but with basic auth) does NOT redirect (admin gating is Traefik-only)
   - Visiting `/sign-in` while signed-out renders the route (no redirect loop)
5. Add `apps/web/src/lib/auth-client.ts` stub that `throw new Error('not implemented')` — drives the test to red
6. Run `pnpm --filter @openwhispr/web test src/lib/__tests__` — MUST fail
7. Run `pnpm --filter @openwhispr/web exec playwright test 05-auth-middleware` — MUST fail (likely with 404/build errors)
8. Commit RED with the message above.

**GREEN commit** `feat(07.1-05): Better Auth client + server + middleware + (auth) layout`

9. Add `better-auth@1.6.9` to `apps/web/package.json`. `pnpm install`.
10. Verify/refactor `packages/auth` exports per RESEARCH OQ1 — minimal change preferred. Run `pnpm --filter @openwhispr/api typecheck && pnpm --filter @openwhispr/api test` to confirm no regression.
11. **Smoke step (RESEARCH OQ4 / A2)** — before writing auth-client.ts, run:
    ```bash
    cd apps/web
    node --input-type=module -e "
      import { createAuthClient } from 'better-auth/react';
      const c = createAuthClient({ baseURL: 'https://api.localhost' });
      const expected = ['signIn','signUp','signOut','useSession','verifyEmail','revokeSession','revokeOtherSessions','deleteAccount','listSessions'];
      for (const k of expected) console.log(k, typeof c[k]);
    "
    ```
    Confirm every expected key is `function` or `object` (signIn/signUp are objects exposing `.email`/`.social`). If any is `undefined`, surface it in the commit body and update the test to skip the missing-named method (rare in 1.6.x; A2 is flagged as "needs smoke" precisely for this reason).
12. Write `apps/web/src/lib/auth-client.ts` verbatim from RESEARCH § Pattern 3 plus the additional named exports needed by U5 (`listSessions`, `revokeSession`, `revokeOtherSessions`, `deleteAccount`). No `baseURL` — same-origin (D-DEPLOY-1).
13. Write `apps/web/src/lib/auth-server.ts` verbatim from RESEARCH § Pattern 2. Import `auth` from `@openwhispr/auth/server` (the package re-export).
14. Write `apps/web/src/lib/http.ts`:
    - `INTERNAL_API_URL` env (default `http://api:3000` in production; `https://api.localhost` in tests — read from `process.env.INTERNAL_API_URL`)
    - `serverFetch(path, init?)`: `const cookieHeader = (await headers()).get('cookie') ?? ''; return fetch(\`${INTERNAL_API_URL}${path}\`, { ...init, headers: { ...init?.headers, cookie: cookieHeader } });` — throws a typed `ApiError` on non-2xx with the error envelope per WIRE-17
    - `clientFetch(path, init?)`: `return fetch(path, { ...init, credentials: 'include' });` — throws `ApiError` on non-2xx
    - Export `ApiError` class with `status`, `body`, `message` fields
15. Write `apps/web/src/middleware.ts` verbatim from RESEARCH § Pattern 4. Matcher MUST be `['/app/:path*']` — explicitly NOT `/admin/*` (D-ADMIN-1).
16. Write `apps/web/src/app/(auth)/layout.tsx`:
    ```tsx
    import { redirect } from 'next/navigation';
    import { getServerSession } from '@/lib/auth-server';
    export default async function AuthLayout({ children }: { children: React.ReactNode }) {
      const session = await getServerSession();
      if (!session) redirect('/sign-in');
      return <>{children}</>;
    }
    ```
    The `<AppShell>` sidebar/nav is left for Plan 06 to wrap children once providers exist; for now the layout is the bare auth gate.
17. Run `pnpm --filter @openwhispr/web typecheck` — MUST exit 0.
18. Run `pnpm --filter @openwhispr/web test src/lib/__tests__ --coverage` — MUST exit 0, coverage on src/lib/{auth-client,auth-server,http}.ts ≥ 90/90/90/90.
19. Run `pnpm --filter @openwhispr/web exec playwright test 05-auth-middleware` — MUST exit 0.
20. Commit GREEN.

Constraints:
- Pin better-auth at 1.6.9 EXACTLY (matches apps/api per D-STACK-5; CONTEXT corrected the prompt's 1.6.10).
- Middleware MUST stay Edge-safe: no DB imports, no `better-auth/server` imports — only `better-auth/cookies` (Pitfall 3).
- NO cookie cache enabled in apps/api auth.ts (Pitfall 2 / better-auth#7008). Confirm by reading apps/api/src/auth.ts.
- NO localStorage anywhere (D-SEC-2).
- NO `/admin/*` in middleware matcher (D-ADMIN-1).
</task>

<tests>
RED: unit tests for auth-client/auth-server/http and Playwright spec 05-auth-middleware fail before code is written.
GREEN: all four green, coverage ≥90/90/90/90 on the three lib files, packages/auth refactor (if needed) leaves apps/api green.

Manual smoke step at step 11 verifies RESEARCH Assumption A2 (better-auth/react 1.6.9 surface). Paste its output into the GREEN commit body.
</tests>

<commit_message>
test(07.1-05): add failing tests for auth-client, auth-server, http, middleware
feat(07.1-05): wire better-auth/react 1.6.9 + RSC server + Edge middleware + (auth) layout

- packages/auth: re-export Better Auth `auth` instance from /server for cross-app import
- apps/web/src/lib/auth-client.ts: createAuthClient (same-origin) exposing
  signIn.email/social, signUp.email, signOut, useSession, verifyEmail,
  listSessions, revokeSession, revokeOtherSessions, deleteAccount
- apps/web/src/lib/auth-server.ts: getServerSession() via auth.api.getSession({ headers })
- apps/web/src/lib/http.ts: serverFetch forwards cookie header (Pitfall 2),
  clientFetch uses credentials:'include', ApiError envelope per WIRE-17
- apps/web/src/middleware.ts: getSessionCookie() existence check, matcher
  /app/:path* only (D-ADMIN-1 — no /admin matcher)
- apps/web/src/app/(auth)/layout.tsx: real validation, redirect → /sign-in on null
- 1.6.9 surface smoke verified (RESEARCH A2)
- Coverage ≥ 90/90/90/90 on src/lib/{auth-client,auth-server,http}.ts

Refs: WEB-IMPL-02, D-STACK-5, D-SEC-2, D-ADMIN-1, D-DEPLOY-1
</commit_message>
