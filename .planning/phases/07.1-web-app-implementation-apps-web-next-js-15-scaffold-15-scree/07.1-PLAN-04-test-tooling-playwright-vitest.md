---
phase: 07.1-web-app-implementation
plan: 04
type: execute
wave: 0
depends_on: [01, 03]
files_modified:
  - apps/web/vitest.config.ts
  - apps/web/vitest.setup.ts
  - apps/web/playwright.config.ts
  - apps/web/tests/e2e/fixtures/auth.ts
  - apps/web/tests/e2e/fixtures/seed.ts
  - apps/web/tests/e2e/fixtures/states.ts
  - apps/web/tests/e2e/00-infra.spec.ts
  - apps/web/package.json
autonomous: true
requirements: [WEB-IMPL-04]
must_haves:
  truths:
    - "pnpm --filter @openwhispr/web test runs vitest with happy-dom environment"
    - "Coverage thresholds set to 90/90/90/90 (lines/branches/functions/statements)"
    - "Playwright config webServer=docker compose --wait, baseURL=https://api.localhost, ignoreHTTPSErrors=true, chromium-only"
    - "00-infra.spec.ts asserts /api/health 200, / 200, /admin/* 401 unauth + 200 with basic-auth"
    - "fixtures/states.ts exposes loadingFor + errorFor route() helpers honoring D-TEST-3 boundary"
    - "fixtures/auth.ts signs up a deterministic test user via Better Auth and persists storageState"
    - "fixtures/seed.ts seeds + clears notes/transcriptions/conversations via apps/api endpoints"
  artifacts:
    - { path: "apps/web/playwright.config.ts", provides: "Playwright config per RESEARCH § Code Examples" }
    - { path: "apps/web/vitest.config.ts", provides: "vitest + happy-dom + v8 coverage 90/90/90/90" }
    - { path: "apps/web/tests/e2e/fixtures/states.ts", provides: "loadingFor/errorFor state injection" }
    - { path: "apps/web/tests/e2e/00-infra.spec.ts", provides: "smoke covering WEB-IMPL-03" }
  key_links:
    - { from: "fixtures/auth.ts", to: "apps/api Better Auth /api/auth/sign-up/email", via: "fetch + storageState", pattern: "sign-up/email" }
    - { from: "vitest coverage thresholds", to: "CLAUDE.md TDD-02 90/90/90/90", via: "vitest.config.ts test.coverage.thresholds", pattern: "thresholds" }
---

<role>
You set up the testing substrate — vitest + happy-dom for unit/component, Playwright 1.59.1 + @axe-core/playwright for e2e against the real docker-compose stack. Plans 07–12 author per-screen specs against your fixtures; you author the infra smoke (00-infra.spec.ts) yourself.

Strict TDD: 00-infra.spec.ts is written RED-first BEFORE you touch Plan 03 artifacts (but Plan 03 already exists when you start — so RED here means write the spec, run it, watch it fail until your fixtures are in place, then make it green by adding fixtures). Commit cycle: `test(07.1-04): add failing infra smoke spec` (RED) → `chore(07.1-04): add vitest+playwright fixtures` (GREEN).
</role>

<context>
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
</context>

<files_to_read>
- .planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md sections: § Playwright Setup, § Vitest + RTL, § Validation Architecture, § Code Examples (playwright.config.ts, vitest.config.ts, fixtures/states.ts, per-screen e2e template)
- .planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md (D-TEST-1, D-TEST-2, D-TEST-3, D-TEST-4)
- apps/api/src/routes/better-auth-handler.ts (verify sign-up endpoint shape for fixtures)
- apps/api/src/routes/notes/create.ts, transcriptions/batch-create.ts, conversations/create.ts (verify seed payloads for fixtures)
- compose/traefik/certs/ contents (root-ca.crt for NODE_EXTRA_CA_CERTS in Playwright)
- package.json (root) — confirm @playwright/test 1.59.1 already devDep
</files_to_read>

<files_to_modify>
- apps/web/package.json — add devDeps: vitest, @vitejs/plugin-react, @testing-library/react, @testing-library/user-event, happy-dom, @axe-core/playwright, @vitest/coverage-v8. Scripts: `test: "vitest run"`, `test:watch: "vitest"`, `test:coverage: "vitest run --coverage"`, `e2e: "playwright test"`, `e2e:ui: "playwright test --ui"`
- apps/web/vitest.config.ts (verbatim from RESEARCH § Code Examples → vitest.config.ts)
- apps/web/vitest.setup.ts (@testing-library/react auto-cleanup, fetch polyfill if missing, React 19 act() global)
- apps/web/playwright.config.ts (verbatim from RESEARCH § Code Examples → playwright.config.ts)
- apps/web/tests/e2e/fixtures/states.ts (verbatim from RESEARCH § Code Examples → fixtures/states.ts)
- apps/web/tests/e2e/fixtures/auth.ts — workerScoped sign-in fixture; uses fetch against `https://api.localhost/api/auth/sign-up/email` to create a deterministic `alice+${workerIndex}@test.local` user with password `Pwa9!#testStrong`, then `signIn.email` to acquire cookies, persists `storageState` to `tests/e2e/.auth/alice-${workerIndex}.json`. Exposes `signInAs(page, email)` that loads the stored state into the page context.
- apps/web/tests/e2e/fixtures/seed.ts — exposes `seedUsage`, `seedTranscriptions`, `seedNotes`, `seedFolders`, `seedConversations`, `clearAllData`. Each does a fetch against the corresponding apps/api endpoint using the test user's cookies (acquired from auth.ts storageState). Seeding for `empty` state = clearAllData; for `success` = seed at least 1 row of representative data.
- apps/web/tests/e2e/00-infra.spec.ts — the four assertions in `must_haves.truths` plus a fifth: `Strict-Transport-Security` and `X-Frame-Options: DENY` present in `/` response headers.
</files_to_modify>

<task>
RED-then-GREEN cycle:

**RED commit** `test(07.1-04): add failing infra smoke spec + vitest threshold sentinel`
1. Write `apps/web/tests/e2e/00-infra.spec.ts` (no fixtures yet). Tests:
   - `GET https://api.localhost/api/health` → 200, body 'OK'
   - `GET https://api.localhost/` → 200, HTML response
   - `GET https://api.localhost/admin/observability` (no auth) → 401
   - `GET https://api.localhost/admin/observability` (basic auth = `admin:testpw123`) → either 200 or 404 (gate passed; route may not exist until Plan 12 — both prove the basic-auth middleware fired)
   - Response headers on `/` include `strict-transport-security`, `x-frame-options: deny`, `referrer-policy`, `permissions-policy`, `content-security-policy`
   - Response headers on `/sign-in` include `content-security-policy` whose value matches the STRICT_AUTH_CSP (no third-party origins)
2. Write a vitest sentinel: `apps/web/src/lib/__tests__/coverage-sentinel.test.ts` that simply imports nothing and asserts `expect(true).toBe(true)` — this gives vitest a file to run so the threshold gate fires once real code lands. Suppress this in the v8 coverage `exclude` list.
3. Run `pnpm --filter @openwhispr/web exec playwright test 00-infra` — MUST fail because fixtures and configs are missing. Commit the RED.

**GREEN commit** `chore(07.1-04): wire vitest + playwright + fixtures (infra smoke green)`
4. Add devDeps to `apps/web/package.json` per the list above. `pnpm install`.
5. Write `apps/web/vitest.config.ts` verbatim from RESEARCH; thresholds 90/90/90/90; exclude pages/layouts/ui primitives (rationale in RESEARCH § vitest.config.ts).
6. Write `apps/web/vitest.setup.ts`:
   ```ts
   import { afterEach } from 'vitest';
   import { cleanup } from '@testing-library/react';
   afterEach(() => cleanup());
   ```
7. Write `apps/web/playwright.config.ts` verbatim from RESEARCH § Code Examples. Confirm webServer.command uses `docker compose --profile default up -d --wait` and `reuseExistingServer: true`; CI branch sets webServer to undefined.
8. Write `apps/web/tests/e2e/fixtures/states.ts` verbatim from RESEARCH § Code Examples → fixtures/states.ts. `loadingFor` uses `setTimeout(30_000)` (longer than test timeout); `errorFor` uses `route.fulfill({ status, contentType: 'application/json', body: JSON.stringify({ error: 'fixture' }) })`.
9. Write `apps/web/tests/e2e/fixtures/auth.ts`:
   - `globalSetup` style not needed; use Playwright `test.extend` with workerScoped fixture
   - Function `provisionTestUser(workerIndex: number)` signs up `alice+${workerIndex}@test.local` if not already present (idempotent via Better Auth: on duplicate, just sign-in)
   - For email-verification gating: apps/api Better Auth dev config does not require email confirmation (verify this assumption against `apps/api/src/auth.ts`; if it does, hit `apps/api`'s test-only `/api/_test/verify-email?email=` route IF it exists OR set the Better Auth flag in compose `.env` to `EMAIL_VERIFICATION=optional` for the test profile). If no test bypass exists in apps/api, raise the issue in the commit body and ship the fixture against `signUp.email` + immediate `signIn.email` (current apps/api default is verification not enforced — verified in CONTEXT § Integration Points "Better Auth session cookies are SameSite=Lax HttpOnly")
   - Export `signInAs(page, email)` that uses `page.context().storageState` from the persisted file
10. Write `apps/web/tests/e2e/fixtures/seed.ts`:
    - For each resource type, call apps/api endpoint with cookie auth (cookies pre-loaded by `signInAs`)
    - `seedNotes({ count: 1 })` → POST `/api/notes/create` with minimal valid payload (per packages/wire-schemas NoteInput)
    - `seedTranscriptions({ count: 1, withText: '...' })` → POST `/api/transcriptions/create`
    - `seedFolders({ count: 1 })` → POST `/api/folders/create`
    - `seedConversations({ count: 1, withMessages: 2 })` → POST `/api/conversations/create` then `/api/conversations/messages`
    - `seedUsage(payload)` → POST `/api/streaming-usage` to populate the ledger
    - `clearAllData()` → DELETE `/api/notes/delete-all` + iterate `/api/conversations/list` deleting each + iterate `/api/transcriptions/list` calling `/api/transcriptions/batch-delete`
11. Run `pnpm --filter @openwhispr/web exec playwright test 00-infra` — MUST exit 0 (docker compose stack from Plan 03 must be up). Commit GREEN.

Constraints:
- D-TEST-3: `route()` interception ONLY for `loading` (use `loadingFor` helper) and `error` (use `errorFor` helper) UI states. `empty` uses `clearAllData()` then assert; `success` uses `seed*` then assert.
- Coverage thresholds NON-NEGOTIABLE at 90/90/90/90.
- Tests are deterministic — fixed test user emails per workerIndex; no random data.
- No internal-logic mocks: only `route()` against network boundaries.
- Self-signed cert: `ignoreHTTPSErrors: true` in playwright.config.ts; CI sets `NODE_EXTRA_CA_CERTS=compose/traefik/certs/root-ca.crt` env (Plan 13 wires this in web.yml).
</task>

<tests>
This plan IS the test infrastructure plan. Its own validation is:
- RED: `pnpm --filter @openwhispr/web exec playwright test 00-infra` exits non-zero before fixtures are written
- GREEN: same command exits 0 after fixtures + configs are written
- vitest sanity: `pnpm --filter @openwhispr/web test` runs and reports 1 passing (the sentinel) with coverage thresholds defined but not failing because no covered files yet
</tests>

<commit_message>
test(07.1-04): add failing infra smoke spec + vitest coverage sentinel
chore(07.1-04): wire vitest happy-dom + playwright chromium + fixtures

- vitest.config.ts: happy-dom env, v8 coverage 90/90/90/90 thresholds
- vitest.setup.ts: @testing-library/react auto-cleanup
- playwright.config.ts: baseURL https://api.localhost, ignoreHTTPSErrors,
  webServer=docker compose --wait, chromium-only (D-TEST-1)
- fixtures/states.ts: loadingFor + errorFor (D-TEST-3 route() ONLY for
  loading/error; empty + success use real seeded data)
- fixtures/auth.ts: workerScoped Better Auth sign-up + storageState
- fixtures/seed.ts: notes/folders/transcriptions/conversations/usage
  seed + clearAllData against real apps/api endpoints
- 00-infra.spec.ts: smoke for /api/health, /, /admin gate, security headers
- @axe-core/playwright + happy-dom + @vitest/coverage-v8 installed

Refs: WEB-IMPL-04, D-TEST-1, D-TEST-2, D-TEST-3, D-TEST-4
</commit_message>
