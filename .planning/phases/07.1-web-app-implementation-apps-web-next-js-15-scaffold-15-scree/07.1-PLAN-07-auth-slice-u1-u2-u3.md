---
phase: 07.1-web-app-implementation
plan: 07
type: execute
wave: 2
depends_on: [05, 06]
files_modified:
  - apps/web/src/app/(public)/layout.tsx
  - apps/web/src/app/(public)/sign-in/page.tsx
  - apps/web/src/app/(public)/sign-up/page.tsx
  - apps/web/src/app/(public)/verify-email/page.tsx
  - apps/web/src/components/screens/auth/SignInForm.tsx
  - apps/web/src/components/screens/auth/SignUpForm.tsx
  - apps/web/src/components/screens/auth/VerifyEmailClient.tsx
  - apps/web/src/components/screens/auth/OidcButtons.tsx
  - apps/web/src/lib/schemas/auth.ts
  - apps/web/src/components/screens/auth/__tests__/SignInForm.test.tsx
  - apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx
  - apps/web/src/components/screens/auth/__tests__/VerifyEmailClient.test.tsx
  - apps/web/src/lib/schemas/__tests__/auth.test.ts
  - apps/web/tests/e2e/u1-sign-in.spec.ts
  - apps/web/tests/e2e/u2-sign-up.spec.ts
  - apps/web/tests/e2e/u3-verify-email.spec.ts
autonomous: true
requirements: [WEB-IMPL-02, WEB-IMPL-04]
must_haves:
  truths:
    - "U1 /sign-in renders email+password form (RHF + zod) + OIDC buttons (Google, GitHub, SSO) controlled by NEXT_PUBLIC_OIDC_PROVIDERS"
    - "U1 forgot-password link is rendered as disabled static text per D-UX2 (Phase 7.x backlog)"
    - "U1 valid sign-in lands on /app; invalid shows end-user.signin.error.* alert"
    - "U2 /sign-up renders name+email+password form + same OIDC buttons; success shows verification email message"
    - "U3 /verify-email reads ?token=, posts to authClient.verifyEmail, shows loading→success/error states"
    - "Every UI state (loading/empty/error/success) covered per screen; axe-core WCAG 2.2 AA clean on each"
    - "Submit handlers call authClient.signIn.email / signUp.email / verifyEmail directly (no custom fetch)"
  artifacts:
    - { path: "apps/web/src/app/(public)/sign-in/page.tsx", provides: "U1 RSC entry" }
    - { path: "apps/web/src/components/screens/auth/SignInForm.tsx", provides: "U1 Client form" }
    - { path: "apps/web/src/app/(public)/sign-up/page.tsx", provides: "U2 RSC entry" }
    - { path: "apps/web/src/app/(public)/verify-email/page.tsx", provides: "U3 RSC entry reading searchParams.token" }
    - { path: "apps/web/src/lib/schemas/auth.ts", provides: "signInSchema, signUpSchema zod" }
    - { path: "apps/web/tests/e2e/u1-sign-in.spec.ts", provides: "Playwright state matrix + axe" }
  key_links:
    - { from: "SignInForm onSubmit", to: "authClient.signIn.email", via: "@hookform/resolvers/zod + authClient", pattern: "authClient\\.signIn\\.email" }
    - { from: "VerifyEmailClient", to: "authClient.verifyEmail", via: "useEffect on mount with searchParams.token", pattern: "authClient\\.verifyEmail" }
---

<role>
You implement the auth slice — U1 sign-in, U2 sign-up, U3 verify-email — end-to-end (RSC route + Client form + zod schema + RHF + state matrix Playwright + axe). This slice gates every authenticated slice (08–11) so it ships first in Wave 2.

Strict TDD: vitest component tests + Playwright specs are written RED before implementation. RED commit precedes GREEN commit visibly in git log per CLAUDE.md.

D-UX2 — Forgot-password link is DISABLED in v1. Render as static muted text or a Link to `/forgot-password` that shows "Password reset is coming soon". Do NOT enable the flow.

D-UX4 — OIDC button labels: `Continue with Google`, `Continue with GitHub`, `Continue with SSO` (generic label so corp ops can rebrand via i18n override). Visibility controlled by parsing `process.env.NEXT_PUBLIC_OIDC_PROVIDERS` (default `'google,github,oidc'`).

Per Appendix D, U1 fires `POST /api/auth/sign-in/email`; U2 fires `POST /api/auth/sign-up/email`; U3 fires `POST /api/auth/verify-email`. All via Better Auth catch-all. NO new API endpoints (D-S1).
</role>

<context>
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
@.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md
@.planning/phases/07-frontend-ui-spec/07-CONTEXT.md
</context>

<files_to_read>
- UI-SPEC-end-user.md sections for U1, U2, U3 (Purpose, Route, Data, Actions, States, Copy keys, Wireframe, shadcn primitives)
- 07.1-RESEARCH.md § Pattern 1 (Server/Client boundary for public form vs one-shot mutation), § Pattern 3 (Better Auth React client), § Security Domain (open-redirect mitigation, reflected-XSS on searchParams.token)
- 07-CONTEXT.md D-UX1, D-UX2, D-UX4 (auth UX decisions)
- apps/web/src/lib/auth-client.ts (Plan 05 — confirm exported methods)
- apps/web/src/locales/en/end-user.json (Plan 06 — confirm signin/signup/verify keys present)
</files_to_read>

<files_to_modify>
See frontmatter.

Disjoint with parallel Plan 12 (Admin) — Plan 12 owns `(admin)/*`; Plan 07 owns `(public)/*`. No shared writes.
</files_to_modify>

<tests>
RED commit `test(07.1-07): add failing tests for U1/U2/U3 forms, schemas, e2e state matrix`

1. Write `apps/web/src/lib/schemas/__tests__/auth.test.ts`:
   - `signInSchema.parse({email:'a@b.com',password:'Pwa9!testStrong'})` succeeds; rejects invalid email; rejects empty password
   - `signUpSchema.parse({name:'A',email:'a@b.com',password:'Pwa9!testStrong'})` succeeds; rejects short name (<1); rejects weak password (min 8 chars enforced per Better Auth default)
2. Write `apps/web/src/components/screens/auth/__tests__/SignInForm.test.tsx`:
   - Renders email, password inputs and Sign in button
   - Renders Google + GitHub + SSO buttons when NEXT_PUBLIC_OIDC_PROVIDERS unset/default
   - Hides specific OIDC button when env excludes it (mock process.env at test time)
   - Renders disabled "Forgot password?" text (D-UX2)
   - Invalid email shows inline RHF validation error
   - Submit calls mocked authClient.signIn.email with the form values; on `{ error }` returned, renders Alert with `end-user.signin.error.title.text`
3. Write similar tests for SignUpForm.test.tsx and VerifyEmailClient.test.tsx:
   - SignUpForm: name/email/password, OIDC buttons, success state renders `end-user.signup.success.title.text`, duplicate-email error renders `end-user.signup.error.duplicate.text`
   - VerifyEmailClient: useEffect fires authClient.verifyEmail with token from prop; loading state shows `end-user.verify.loading.body.text`; success renders Sign-in CTA; error renders generic copy with Back-to-signup CTA
4. Write Playwright specs (per-screen template from RESEARCH § Code Examples; one file per screen):
   - `u1-sign-in.spec.ts` — 5 tests: loading state via `loadingFor('**/api/auth/sign-in/email')`, empty state N/A → assert form fields are pristine (no autofill); error state via wrong password against REAL Better Auth (no route() — D-TEST-3); success via correct credentials lands on `/app`; axe-core scan with `withTags(['wcag2a','wcag2aa','wcag22aa'])` → `expect(results.violations).toEqual([])`
   - `u2-sign-up.spec.ts` — same five states. Success: create new email `bob+${Date.now()}@test.local`, expect `end-user.signup.success.*` rendered. Error: existing email → duplicate copy. Empty N/A. Loading via `loadingFor('**/api/auth/sign-up/email')`. axe pass.
   - `u3-verify-email.spec.ts` — five states. Success: stub a valid token by signing up a user then extracting verification link via the apps/api mailpit dev SMTP capture (use existing test fixture from Phase 2 if present; otherwise hit apps/api directly via Better Auth's `/api/auth/get-verification-link?email=` only if such a test endpoint exists — RESEARCH OQ verify; if not, skip success with a TODO comment and rely on the unit test for verifyEmail call coverage). Error: visit `/verify-email?token=invalid` → renders error state. Loading: `loadingFor('**/api/auth/verify-email')`. Empty: visit without `?token=` → renders error variant per UI-SPEC. axe pass.
5. Run `pnpm --filter @openwhispr/web test src/lib/schemas src/components/screens/auth` + `pnpm --filter @openwhispr/web exec playwright test u1 u2 u3` — MUST fail.
6. Commit RED.

GREEN commit `feat(07.1-07): U1 sign-in + U2 sign-up + U3 verify-email`

7. Write `apps/web/src/lib/schemas/auth.ts`:
   ```ts
   import { z } from 'zod';
   export const signInSchema = z.object({ email: z.string().email(), password: z.string().min(8) });
   export type SignInInput = z.infer<typeof signInSchema>;
   export const signUpSchema = z.object({ name: z.string().min(1).max(100), email: z.string().email(), password: z.string().min(8).max(200) });
   export type SignUpInput = z.infer<typeof signUpSchema>;
   ```
8. Write `apps/web/src/app/(public)/layout.tsx` — centered card shell, no sidebar:
   ```tsx
   export default function PublicLayout({ children }: { children: React.ReactNode }) {
     return <main className="min-h-svh flex items-center justify-center p-6 bg-background">{children}</main>;
   }
   ```
9. Write `apps/web/src/components/screens/auth/OidcButtons.tsx` (`'use client'`):
   - Parses `process.env.NEXT_PUBLIC_OIDC_PROVIDERS` (default `'google,github,oidc'`) → array
   - Renders one Button per provider, labeled by copy key (`end-user.signin.oidc.{google|github|sso}.label` / `end-user.signup.oidc.*`)
   - onClick → `authClient.signIn.social({ provider })`
10. Write `apps/web/src/components/screens/auth/SignInForm.tsx` (`'use client'`):
    - useForm with zodResolver(signInSchema)
    - Card with title (copy key), email + password inputs, "Forgot password?" disabled muted text, Sign-in button, OIDC buttons below separator, link to sign-up at bottom
    - onSubmit calls authClient.signIn.email; on success router.push('/app'); on error setAlert from copy keys
    - shadcn primitives: Card, Form, Input, Label, Button, Alert, Separator
11. Write `apps/web/src/app/(public)/sign-in/page.tsx`:
    ```tsx
    import { SignInForm } from '@/components/screens/auth/SignInForm';
    export default function SignInPage() { return <SignInForm />; }
    ```
12. Write `apps/web/src/components/screens/auth/SignUpForm.tsx` — mirror SignInForm structure plus name field; success state replaces the form with `end-user.signup.success.{title,body}.text`; duplicate-email error via Better Auth error code.
13. Write `apps/web/src/app/(public)/sign-up/page.tsx` similar to U1.
14. Write `apps/web/src/components/screens/auth/VerifyEmailClient.tsx` (`'use client'`):
    - Props: `{ token: string | undefined }`
    - useState: status = `'loading'|'success'|'error'`
    - useEffect on mount: if token, call `authClient.verifyEmail({ query: { token } })`; set status accordingly. If no token, set status='error'.
    - Renders Card with copy keys per state
15. Write `apps/web/src/app/(public)/verify-email/page.tsx` (RSC):
    ```tsx
    import { VerifyEmailClient } from '@/components/screens/auth/VerifyEmailClient';
    import { z } from 'zod';
    const TokenSchema = z.string().min(1).max(512).regex(/^[A-Za-z0-9._-]+$/);
    export default async function VerifyEmailPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) {
      const sp = await searchParams;
      const parsed = TokenSchema.safeParse(sp.token);
      return <VerifyEmailClient token={parsed.success ? parsed.data : undefined} />;
    }
    ```
    Token shape validation defends against reflected-XSS (RESEARCH § Security Domain). NEVER render the raw token to DOM.
16. Run vitest + playwright — green. Verify axe scan returns zero violations on each page (semantic landmarks, Form labels, alt text, focus-visible rings from shadcn).
17. Run `pnpm --filter @openwhispr/web test --coverage --filter '**/auth/**' '**/schemas/auth**'` — coverage ≥ 90/90/90/90.
18. Commit GREEN.

Constraints:
- NO custom fetch — every auth call goes through `authClient.*` (D-STACK-5, RESEARCH "Don't hand-roll auth UI").
- D-UX2: Forgot-password link is DISABLED (rendered as muted static text or non-functional anchor).
- D-UX4: SSO button label literally "Continue with SSO" — generic; corp ops rebrand via i18n override.
- Post-signin redirect is HARDCODED to `/app` — do NOT accept `?next=` (RESEARCH § Security Domain open-redirect mitigation).
- Token searchParam in U3 validated against a tight regex before any use; never rendered.
- 4 UI states per screen × 3 screens × ~5 test cases = ~15 e2e tests + 1 axe per screen = 18 tests total for this slice. D-TEST-3: route() only for loading/error.
- English-only copy from locale bundles; no inline strings.
</tests>

<commit_message>
test(07.1-07): add failing tests for U1/U2/U3 forms, schemas, state matrix + axe
feat(07.1-07): U1 sign-in + U2 sign-up + U3 verify-email — auth slice complete

- /sign-in (U1): RHF + zod + Better Auth signIn.email + OIDC (Google/GitHub/SSO)
  - Forgot-password link disabled per D-UX2 (Phase 7.x backlog)
  - SSO label generic per D-UX4 (corp i18n override)
  - OIDC button visibility from NEXT_PUBLIC_OIDC_PROVIDERS
  - Post-signin hardcoded /app redirect (no ?next= per Security Domain)
- /sign-up (U2): RHF + zod + Better Auth signUp.email + OIDC + success state
- /verify-email (U3): RSC parses + validates ?token= against tight regex (XSS
  defense), Client useEffect fires authClient.verifyEmail with loading/success/error
- 4 UI states × 3 screens via Playwright + axe-core WCAG 2.2 AA per screen
- D-TEST-3: route() only for loading + error; success + empty hit real Better Auth
- Coverage ≥ 90/90/90/90 on slice

Refs: WEB-IMPL-02, WEB-IMPL-04, D-UX1, D-UX2, D-UX4, D-S1, D-STACK-5
</commit_message>
