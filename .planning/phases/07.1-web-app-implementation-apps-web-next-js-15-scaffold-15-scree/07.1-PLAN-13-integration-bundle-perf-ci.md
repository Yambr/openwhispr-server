---
phase: 07.1-web-app-implementation
plan: 13
type: execute
wave: 4
depends_on: [07, 08, 09, 10, 11, 12]
files_modified:
  - apps/web/size-limit.config.js
  - apps/web/perf-budgets.md
  - apps/web/tests/e2e/99-cross-screen-smoke.spec.ts
  - apps/web/tests/perf/measure.mjs
  - .github/workflows/web.yml
  - lefthook.yml
  - apps/web/package.json
autonomous: true
requirements: [WEB-IMPL-01, WEB-IMPL-03, WEB-IMPL-04]
must_haves:
  truths:
    - "size-limit gate fails build when any route exceeds 200KB gzipped"
    - "Cross-screen smoke e2e: sign-in → notes list → note detail → delete → sign-out lands on /sign-in"
    - "Perf measurement script captures LCP and INP per route to perf-budgets.md (informational floor — not a hard gate)"
    - ".github/workflows/web.yml runs typecheck → vitest+coverage → build → size-limit → playwright (4 jobs)"
    - "Playwright job in CI brings up the real docker compose stack + trusts self-signed cert via NODE_EXTRA_CA_CERTS"
    - "lefthook web hook: pre-commit typecheck + biome on staged apps/web/**; pre-push pnpm --filter @openwhispr/web test"
    - "web.yml YAML is syntactically valid (verified locally via python/node yaml parser)"
    - "Every constituent CI command (typecheck, vitest, build, size-limit, playwright) has been verified locally before the commit"
  artifacts:
    - { path: ".github/workflows/web.yml", provides: "CI gate per D-CI-1" }
    - { path: "apps/web/size-limit.config.js", provides: "10-route bundle budget (D-PERF-1)" }
    - { path: "apps/web/perf-budgets.md", provides: "informational LCP/INP floor (D-PERF-2)" }
    - { path: "lefthook.yml", provides: "web hook entry (D-CI-2)" }
    - { path: "apps/web/tests/e2e/99-cross-screen-smoke.spec.ts", provides: "happy-path through 5+ screens against real stack" }
  key_links:
    - { from: "GHA workflow", to: "docker compose --wait", via: "BASE_URL=https://api.localhost + NODE_EXTRA_CA_CERTS", pattern: "docker compose .* --wait" }
    - { from: "size-limit.config.js", to: ".next/static/chunks/app/**", via: "glob paths per route", pattern: "size-limit" }
---

<role>
Wave 4 integration plan. You add the bundle-budget gate, the cross-screen smoke spec, the perf measurement script, the GitHub Actions workflow, and the lefthook web hook. This plan closes WEB-IMPL-01 (bundle gate), WEB-IMPL-03 (CI verifies same-origin + admin gate end-to-end), and WEB-IMPL-04 (CI runs the full e2e + axe matrix).

Strict TDD on the smoke spec; the rest is infra glue.

This plan is **fully autonomous**. The GHA workflow's first remote run cannot happen before the commit lands (push triggers it). The executor verifies the workflow YAML is syntactically valid AND that every constituent command has been run locally; the first PR/push after this commit confirms the remote run. The remote-green confirmation is recorded as a follow-up by Plan 14 — it is NOT a blocking gate on this plan.
</role>

<context>
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
@.github/workflows/ci.yml
@lefthook.yml
</context>

<files_to_read>
- 07.1-RESEARCH.md § Code Examples (size-limit.config.js, .github/workflows/web.yml verbatim block)
- 07.1-CONTEXT.md D-PERF-1, D-PERF-2, D-CI-1, D-CI-2
- .github/workflows/ci.yml (canonical workflow style — step-security/harden-runner SHA + pnpm/action-setup@v4 + actions/setup-node@v5 + Node 24 + pnpm 11.0.8)
- lefthook.yml (existing pre-commit groups — append web group without disturbing biome/english/tenant-context/ui-spec entries)
- compose/traefik/certs/ (self-signed root CA path for NODE_EXTRA_CA_CERTS)
</files_to_read>

<files_to_modify>
See frontmatter.
</files_to_modify>

<tests>
RED commit `test(07.1-13): add failing cross-screen smoke + size-limit sentinel`

1. Write `apps/web/tests/e2e/99-cross-screen-smoke.spec.ts`:
   - Sign in as the fixture user
   - Navigate /app → assert Words used KPI present
   - Navigate /app/notes → click Search → type query → assert results
   - Navigate /app/notes/[seeded-id] → assert content
   - Delete the note → assert redirected to list and note gone
   - Sign out → assert landed on /sign-in
   - axe scan each navigated page (one consolidated assertion per page)
2. Add `apps/web/size-limit.config.js` (verbatim from RESEARCH § Code Examples — 10 routes × 200KB) but set the limit on `/sign-in` to a deliberately-failing `1 KB` value first, run `pnpm --filter @openwhispr/web exec size-limit` → MUST fail → revert to 200 KB before commit (RED demonstration in commit body — paste the failing output, then the passing output).
3. RED commit.

GREEN commit `feat(07.1-13): bundle gate + cross-screen smoke + perf measure + web.yml + lefthook`

4. Finalize `apps/web/size-limit.config.js` with 200 KB per route across all 10 declared paths. Add `size-limit` and `@size-limit/preset-app` to devDependencies (Plan 04 might already have these; re-verify). Run `pnpm --filter @openwhispr/web exec size-limit` locally — MUST exit 0. Record output snippet in commit body under `## Local CI verification → size-limit`.
5. Write `apps/web/perf-budgets.md`:
   - Table of routes with target LCP <2.5s, INP <200ms (D-PERF-2)
   - Note: informational floor only; not a CI gate
   - Instructions for running `node apps/web/tests/perf/measure.mjs` locally against the docker-compose stack
6. Write `apps/web/tests/perf/measure.mjs`:
   - Uses Playwright to navigate each route, gathers Performance API entries (`getEntriesByType('navigation')` + `PerformanceObserver` for LCP/INP via web-vitals npm package or `chrome-launcher` lighthouse — pick whichever has smaller cost; prefer simple PerformanceObserver)
   - Writes a markdown table append to `perf-budgets.md` under a `## Last measurement` heading
   - Exits 0 regardless (not a hard gate)
7. Write `.github/workflows/web.yml` verbatim from RESEARCH § Code Examples → web.yml block. Critical fields:
   - `paths: ['apps/web/**', 'packages/wire-schemas/**', 'apps/api/src/routes/**', '.github/workflows/web.yml']`
   - `concurrency: { group: ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true }`
   - `permissions: { contents: read }`
   - Jobs: typecheck → vitest → build-and-size → playwright (dependent chain)
   - Playwright job: `cp .env.example .env`, generate ADMIN_BASIC_AUTH_USERS htpasswd hash inline, run `./compose/traefik/bootstrap-tls.sh`, `docker compose --profile default up -d --wait`, run playwright with `BASE_URL=https://api.localhost` + `NODE_EXTRA_CA_CERTS=${{ github.workspace }}/compose/traefik/certs/root-ca.crt`, upload playwright-report on failure, `docker compose --profile default down -v` in if:always() teardown
   - step-security/harden-runner SHA pin from existing ci.yml
   - pnpm/action-setup@v4 version: 11.0.8
   - actions/setup-node@v5 node-version: '24', cache: 'pnpm'
8. Extend `lefthook.yml`:
   ```yaml
   pre-commit:
     commands:
       web-typecheck:
         glob: "apps/web/**/*.{ts,tsx}"
         run: pnpm --filter @openwhispr/web typecheck
       web-biome:
         glob: "apps/web/**/*.{ts,tsx,json}"
         run: pnpm exec biome check --write {staged_files}
         stage_fixed: true
   pre-push:
     commands:
       web-test:
         glob: "apps/web/**"
         run: pnpm --filter @openwhispr/web test
   ```
   Append to existing groups — do not replace.

9. **Validate web.yml syntax + verify every constituent CI command locally (autonomous, replaces the prior human-action step).**

   a. **YAML syntax validation** — run one of:
   ```bash
   python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/web.yml"))' && echo "yaml=OK"
   # OR (if python yaml not available)
   node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/web.yml','utf8')); console.log('yaml=OK')"
   ```
   MUST print `yaml=OK`. If the parser raises an exception, fix the YAML before continuing.

   b. **Local execution of every constituent CI command** — run each, capture exit code:
   ```bash
   pnpm --filter @openwhispr/web typecheck                            # job: typecheck
   pnpm --filter @openwhispr/web test --coverage                      # job: vitest
   pnpm --filter @openwhispr/web build                                # job: build-and-size step 1
   pnpm --filter @openwhispr/web exec size-limit                      # job: build-and-size step 2
   pnpm --filter @openwhispr/web exec playwright test                 # job: playwright (against the docker-compose stack already up from Plan 03)
   ```
   Each MUST exit 0. Record the exit codes in the commit body under `## Local CI verification` (one line per command).

   c. **Document the remote-run expectation** in the commit body:
   ```
   ## Local CI verification
   yaml=OK
   typecheck=0
   vitest=0
   build=0
   size-limit=0
   playwright=0

   ## GHA web.yml remote-run note
   GHA workflow syntax valid. All CI commands verified locally.
   First push will confirm remote run; Plan 14 SUMMARY records the remote-green
   confirmation as a follow-up (Known follow-ups section).
   ```

10. Commit GREEN.

Constraints:
- Size-limit threshold 200 KB gz per route (D-PERF-1) — NON-NEGOTIABLE; lower if any route trends below 180 KB after measurement.
- LCP/INP measurement is informational only (D-PERF-2); not a hard CI gate.
- lefthook must NOT slow pre-commit > 10s on a typical change; typecheck-only is fine, full vitest belongs in pre-push.
- web.yml `paths` filter MUST include `apps/api/src/routes/**` so route changes that break e2e are caught.
- Self-signed cert must be trusted via NODE_EXTRA_CA_CERTS in CI; locally Playwright config sets `ignoreHTTPSErrors: true`.
- This plan is **autonomous** — no human verification of remote GHA run. The first remote run is structurally non-pre-pushable; Plan 14 records the post-push confirmation as a follow-up, not a gate.
</tests>

<commit_message>
test(07.1-13): add failing cross-screen smoke + bundle-budget sentinel
feat(07.1-13): bundle gate + perf measure + GHA web.yml + lefthook web hook

- size-limit.config.js: 200KB gz per route across 10 routes (D-PERF-1)
- perf-budgets.md + tests/perf/measure.mjs: LCP<2.5s/INP<200ms floor
  (informational only per D-PERF-2)
- 99-cross-screen-smoke.spec.ts: sign-in → notes list → search → detail →
  delete → sign-out against real docker compose stack (WEB-IMPL-03 closure)
- .github/workflows/web.yml: typecheck → vitest+coverage → build → size-limit
  → playwright (chained); playwright job brings up docker compose, trusts
  self-signed cert via NODE_EXTRA_CA_CERTS, uploads playwright-report on fail
- lefthook.yml: pre-commit web-typecheck + web-biome on staged apps/web/**;
  pre-push web-test (D-CI-2)
- GHA paths include apps/api/src/routes/** so route changes catch e2e
- Hardened runner SHA pin mirrors ci.yml style

## Local CI verification
yaml=OK
typecheck=0
vitest=0
build=0
size-limit=0
playwright=0

## GHA web.yml remote-run note
GHA workflow syntax valid. All CI commands verified locally. First push will
confirm remote run; Plan 14 SUMMARY records remote-green as a follow-up.

Refs: WEB-IMPL-01, WEB-IMPL-03, WEB-IMPL-04, D-PERF-1, D-PERF-2, D-CI-1, D-CI-2
</commit_message>
