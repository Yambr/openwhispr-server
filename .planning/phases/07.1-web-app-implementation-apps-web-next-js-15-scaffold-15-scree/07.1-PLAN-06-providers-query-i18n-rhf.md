---
phase: 07.1-web-app-implementation
plan: 06
type: execute
wave: 1
depends_on: [01, 02, 05]
files_modified:
  - apps/web/src/lib/query-client.ts
  - apps/web/src/lib/query-client-server.ts
  - apps/web/src/lib/query-keys.ts
  - apps/web/src/lib/i18n.ts
  - apps/web/src/lib/i18n-client.tsx
  - apps/web/src/lib/theme-provider.tsx
  - apps/web/src/lib/error-boundary.tsx
  - apps/web/src/locales/en/admin.json
  - apps/web/src/locales/en/end-user.json
  - apps/web/src/locales/en/common.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/components/screens/AppShell.tsx
  - apps/web/src/components/screens/AdminShell.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/lib/__tests__/query-keys.test.ts
  - apps/web/src/lib/__tests__/i18n.test.ts
  - apps/web/src/lib/__tests__/error-boundary.test.tsx
  - apps/web/package.json
autonomous: true
requirements: [WEB-IMPL-01, WEB-IMPL-02]
must_haves:
  truths:
    - "Root layout wraps children in QueryProvider, I18nProvider, ThemeProvider, ErrorBoundary, Toaster"
    - "queryKeys factory exports stable keys for usage, session, sessions, sttConfig, noteRecordingConfig, transcriptions, notes, folders, conversations"
    - "i18n loads from src/locales/en/{admin,end-user,common}.json without next-i18next"
    - "All UI-SPEC copy keys from Appendix C exist in the locale bundles"
    - "AppShell renders sidebar with end-user nav (Dashboard, Transcriptions, Notes, Conversations, Account) + sign-out button"
    - "AdminShell renders sidebar with admin nav (Observability, Configuration)"
  artifacts:
    - { path: "apps/web/src/lib/query-keys.ts", provides: "queryKeys factory per UI-SPEC Conventions" }
    - { path: "apps/web/src/lib/i18n.ts", provides: "App-Router native i18next createInstance per request" }
    - { path: "apps/web/src/locales/en/end-user.json", provides: "Appendix C copy keys for end-user" }
    - { path: "apps/web/src/locales/en/admin.json", provides: "Appendix C copy keys for admin" }
    - { path: "apps/web/src/components/screens/AppShell.tsx", provides: "end-user layout shell" }
    - { path: "apps/web/src/components/screens/AdminShell.tsx", provides: "admin layout shell" }
  key_links:
    - { from: "root layout", to: "providers", via: "<ThemeProvider><I18nProvider><QueryProvider><ErrorBoundary>{children}</ErrorBoundary></QueryProvider></I18nProvider></ThemeProvider>", pattern: "QueryProvider" }
    - { from: "i18n.ts", to: "locales JSON", via: "resourcesToBackend dynamic import", pattern: "resourcesToBackend" }
---

<role>
You install the cross-cutting providers — TanStack Query 5, i18next (App-Router native — NOT next-i18next), react-hook-form + zod resolver, theme provider, error boundary, the query-keys factory mirroring UI-SPEC Conventions, the AppShell + AdminShell sidebars, and the full Appendix C copy-key inventory in English.

Strict TDD: every file under apps/web/src/lib/ ships with vitest tests in the same commit. Coverage ≥90/90/90/90.
</role>

<context>
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
@.planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md
@.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md
</context>

<files_to_read>
- .planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md § Pattern 5, § Pattern 6, § Pitfall 1, § Pitfall 4
- .planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md Appendix C (i18n key index — alphabetized, EVERY key the entire UI-SPEC suite uses)
- .planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md (sidebar nav copy keys for U4/U6/U8/U11/U5 nav.sidebar.label entries)
</files_to_read>

<files_to_modify>
See frontmatter `files_modified`. Notable:
- `apps/web/src/locales/en/end-user.json` — populate with every `end-user.*` key from Appendix C (≈ 130 keys)
- `apps/web/src/locales/en/admin.json` — populate with every `admin.*` key from Appendix C (≈ 30 keys)
- `apps/web/src/locales/en/common.json` — `common.action.cancel.label`, `common.action.confirm.label`, `common.action.copy.label`, `common.theme.toggle.label`, `common.signout.label`, `common.error.network.title.text`, `common.error.network.body.text`, `common.loading.label` (these don't appear in Appendix C but are reusable; add as needed)
</files_to_modify>

<task>
RED commit `test(07.1-06): add failing tests for query-keys, i18n loader, error boundary, locale-key coverage`

1. Write `apps/web/src/lib/__tests__/query-keys.test.ts`:
   - For each of the 14 query-key factories listed in RESEARCH § Pattern 5 query-keys block, assert the key shape exactly (e.g., `queryKeys.usage()` deep-equals `['usage']`; `queryKeys.notes.list({limit:20})` deep-equals `['notes','list',{limit:20}]`)
2. Write `apps/web/src/lib/__tests__/i18n.test.ts`:
   - `getServerI18n('en', ['end-user','common']).then(i => i.t('end-user.signin.title.heading.text'))` returns `'Sign in to OpenWhispr'` (from Appendix C)
   - Missing key returns the key itself (i18next default fallback behavior)
3. Write `apps/web/src/lib/__tests__/error-boundary.test.tsx`:
   - Throwing child renders fallback with `common.error.network.title.text`
   - Reset button rehydrates children
4. Write `apps/web/src/locales/__tests__/coverage.test.ts`:
   - Parse Appendix C from `.planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md` at test time (fs.readFileSync + regex extract keys from the "i18n key index" table)
   - Assert EVERY extracted key has a non-empty string value in the matching `apps/web/src/locales/en/{admin,end-user,common}.json` bundle
5. Run `pnpm --filter @openwhispr/web test src/lib/__tests__ src/locales/__tests__` — MUST fail.
6. Commit RED.

GREEN commit `feat(07.1-06): install TanStack Query 5 + i18next + RHF + theme + locale bundles + shells`

7. Add deps to `apps/web/package.json`:
   - `@tanstack/react-query@5`, `@tanstack/react-table@8`, `i18next`, `react-i18next`, `i18next-resources-to-backend`, `react-hook-form@7`, `@hookform/resolvers@5`, `zod@4`, `date-fns`, `lucide-react`
   - devDeps: none new (already in Plan 04)
   `pnpm install`.
8. Write `apps/web/src/lib/query-client.ts` (`'use client'` `QueryProvider` from RESEARCH § Pattern 5) and `apps/web/src/lib/query-client-server.ts` (`makeServerQueryClient()` factory creating a fresh QueryClient per RSC render with `staleTime: 60_000`).
9. Write `apps/web/src/lib/query-keys.ts` exactly per RESEARCH § Pattern 5 block (factories for usage, session, sessions, sttConfig, noteRecordingConfig, transcriptions.{list,detail}, notes.{list,detail,search}, folders, conversations.{list,messages,search}).
10. Write `apps/web/src/lib/i18n.ts` (server `getServerI18n` per RESEARCH § Pattern 6 — uses `i18next-resources-to-backend` with dynamic import of `@/locales/${language}/${namespace}.json`).
11. Write `apps/web/src/lib/i18n-client.tsx` (`'use client'` `I18nProvider` per RESEARCH § Pattern 6 — receives `lng` + `resources` as serialized props from RSC parent).
12. Write `apps/web/src/lib/theme-provider.tsx` (`'use client'` minimal theme provider — reads `localStorage.getItem('theme')` ONLY for the theme toggle preference, which is NOT a token per D-SEC-2; sets `data-theme="dark"` on `<html>`).
13. Write `apps/web/src/lib/error-boundary.tsx` (`'use client'` class component using React 19 — catches render errors, renders `<Alert variant="destructive">` with copy keys, exposes Reset action).
14. Write `apps/web/src/locales/en/end-user.json` populating every `end-user.*` key from Appendix C with the English values listed in that index. Do the same for `admin.json` and `common.json`. **Use the Appendix C table as the single source of truth — do not invent strings.**
15. Write `apps/web/src/components/screens/AppShell.tsx`:
    - Sidebar with nav rows: Dashboard (`end-user.usage.nav.sidebar.label` → `/app`), Transcriptions (`end-user.trx-list.nav.sidebar.label` → `/app/transcriptions`), Notes (`end-user.notes-list.nav.sidebar.label` → `/app/notes`), Conversations (`end-user.conv-list.nav.sidebar.label` → `/app/conversations`), Account (`end-user.account.nav.sidebar.label` → `/app/account`)
    - Header with sign-out button (`common.signout.label`) calling `authClient.signOut()` then `router.push('/sign-in')` and theme toggle
    - Renders `{children}` in main area
    - Mobile: sidebar collapses to `<Sheet>` per UI-SPEC Appendix B breakpoint matrix
16. Write `apps/web/src/components/screens/AdminShell.tsx`:
    - Sidebar with: Observability (`admin.observability.nav.sidebar.label` → `/admin/observability`), Configuration (`admin.config.nav.sidebar.label` → `/admin/config`)
    - Renders `{children}`
    - No sign-out (operator basic-auth gating is browser-level)
17. Update `apps/web/src/app/layout.tsx`:
    ```tsx
    import { ThemeProvider } from '@/lib/theme-provider';
    import { I18nProvider } from '@/lib/i18n-client';
    import { QueryProvider } from '@/lib/query-client';
    import { ErrorBoundary } from '@/lib/error-boundary';
    import { Toaster } from '@/components/ui/sonner';
    import { getServerI18n } from '@/lib/i18n';
    import './globals.css';

    export default async function RootLayout({ children }: { children: React.ReactNode }) {
      const i18n = await getServerI18n('en', ['admin','end-user','common']);
      const resources = i18n.services.resourceStore.data['en'];
      return (
        <html lang="en">
          <body>
            <ThemeProvider>
              <I18nProvider lng="en" resources={resources}>
                <QueryProvider>
                  <ErrorBoundary>{children}</ErrorBoundary>
                </QueryProvider>
              </I18nProvider>
            </ThemeProvider>
            <Toaster />
          </body>
        </html>
      );
    }
    ```
18. Update `apps/web/src/app/(auth)/layout.tsx` to wrap children in `<AppShell>` AFTER the session gate (`<AppShell user={session.user}>{children}</AppShell>`).
19. Write `apps/web/src/app/(admin)/layout.tsx` wrapping children in `<AdminShell>` — no session gate (Traefik handles auth).
20. Run `pnpm --filter @openwhispr/web typecheck` + `pnpm --filter @openwhispr/web test --coverage` — both green; coverage on touched lib files ≥ 90/90/90/90; locale coverage test passes (every Appendix C key resolves).
21. Commit GREEN.

Constraints:
- NO `next-i18next` (Pages Router only). Use `i18next` + `react-i18next` + `i18next-resources-to-backend` (D-STACK-7).
- RSC→Client prop boundary must be serializable (Pitfall 1): pass `resources` as a plain object snapshot, NOT the i18next instance.
- Theme preference in localStorage is allowed (it's a UI preference, not a token; D-SEC-2 forbids TOKENS in localStorage).
- AppShell uses `<Sheet>` for mobile sidebar collapse per UI-SPEC Appendix B; respect breakpoint matrix (mobile 0, tablet 640, desktop 1024).
- Locale bundles MUST be the Appendix C source of truth — no invented copy.
</task>

<tests>
- src/lib/__tests__/query-keys.test.ts (RED → GREEN)
- src/lib/__tests__/i18n.test.ts (RED → GREEN)
- src/lib/__tests__/error-boundary.test.tsx (RED → GREEN)
- src/locales/__tests__/coverage.test.ts (RED → GREEN — parses Appendix C, validates all keys present)
- src/components/screens/__tests__/AppShell.test.tsx (component render, all 5 nav rows present, sign-out button calls authClient.signOut mock)
- src/components/screens/__tests__/AdminShell.test.tsx (2 nav rows present, no sign-out button)

Coverage gate: ≥90/90/90/90 on every src/lib file and src/components/screens/AppShell + AdminShell touched here.
</tests>

<commit_message>
test(07.1-06): add failing tests for providers, query-keys, i18n, locale coverage
feat(07.1-06): TanStack Query 5 + i18next App-Router + RHF/zod + theme + Appendix C locales + shells

- TanStack Query 5: QueryProvider (client) + makeServerQueryClient (RSC)
- query-keys factory: usage, session, sessions, sttConfig, noteRecordingConfig,
  transcriptions.{list,detail}, notes.{list,detail,search}, folders,
  conversations.{list,messages,search} (mirrors UI-SPEC Conventions)
- i18next App-Router native (NOT next-i18next per D-STACK-7):
  getServerI18n() per-RSC + I18nProvider (Client) with serialized resources
- locales/en/{admin,end-user,common}.json: Appendix C verbatim — ~160 keys
- error-boundary: class component renders Alert with i18n on render failure
- theme-provider: data-theme dark toggle (localStorage UI preference only;
  no tokens — D-SEC-2)
- AppShell: 5-row end-user sidebar + sign-out + theme toggle, mobile Sheet
- AdminShell: 2-row admin sidebar, no sign-out (Traefik gate)
- Root layout wraps Theme → I18n → Query → ErrorBoundary + Toaster
- (auth)/layout.tsx wraps in AppShell after session gate; (admin)/layout.tsx
  wraps in AdminShell with no session gate (D-ADMIN-1)
- Coverage ≥ 90/90/90/90; locale-key coverage test parses Appendix C

Refs: WEB-IMPL-01, WEB-IMPL-02, D-STACK-3, D-STACK-4, D-STACK-7, D-STACK-8, D-SEC-2
</commit_message>
