---
phase: 07.1-web-app-implementation
plan: 08
type: execute
wave: 3
depends_on: [07]
files_modified:
  - apps/web/src/app/(auth)/app/page.tsx
  - apps/web/src/app/(auth)/app/account/page.tsx
  - apps/web/src/components/screens/usage/UsageDashboardClient.tsx
  - apps/web/src/components/screens/usage/KpiCard.tsx
  - apps/web/src/components/screens/account/AccountClient.tsx
  - apps/web/src/components/screens/account/SessionsTable.tsx
  - apps/web/src/components/screens/account/DeleteAccountDialog.tsx
  - apps/web/src/components/screens/usage/__tests__/*.test.tsx
  - apps/web/src/components/screens/account/__tests__/*.test.tsx
  - apps/web/tests/e2e/u4-usage.spec.ts
  - apps/web/tests/e2e/u5-account.spec.ts
autonomous: true
requirements: [WEB-IMPL-02, WEB-IMPL-04]
must_haves:
  truths:
    - "U4 /app renders four KPI cards from GET /api/usage: wordsUsed, wordsRemaining, plan, limitReached"
    - "U4 contains NO chart library (Recharts deferred per D-STACK-6); NO activity feed (D-API6)"
    - "U5 /app/account renders profile (name/email/verified/created), sessions table, danger-zone delete dialog"
    - "U5 sessions table uses Better Auth list-sessions/revoke-session/revoke-other-sessions"
    - "U5 delete-account requires typing user's email to confirm; calls authClient.deleteAccount then router.push('/sign-in')"
    - "All 4 UI states + axe WCAG 2.2 AA clean on both screens"
  artifacts:
    - { path: "apps/web/src/app/(auth)/app/page.tsx", provides: "U4 RSC entry with prefetchQuery(usage)" }
    - { path: "apps/web/src/components/screens/usage/UsageDashboardClient.tsx", provides: "U4 useQuery + KPI render" }
    - { path: "apps/web/src/app/(auth)/app/account/page.tsx", provides: "U5 RSC entry with prefetchQuery(session+sessions)" }
    - { path: "apps/web/src/components/screens/account/AccountClient.tsx", provides: "U5 client composite" }
  key_links:
    - { from: "UsageDashboardClient", to: "GET /api/usage", via: "useQuery({ queryKey: queryKeys.usage() })", pattern: "queryKeys\\.usage" }
    - { from: "SessionsTable", to: "Better Auth list-sessions", via: "authClient.listSessions()", pattern: "listSessions" }
    - { from: "DeleteAccountDialog", to: "Better Auth delete-account", via: "authClient.deleteAccount() then router.push('/sign-in')", pattern: "deleteAccount" }
---

<role>
You implement the account + usage slice: U4 Usage (KPI-only — D-STACK-6, D-API6) + U5 Account (profile + sessions + delete). End-to-end vertical slice with strict TDD.
</role>

<context>
@.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md (U4, U5)
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-CONTEXT.md
@.planning/phases/07.1-web-app-implementation-apps-web-next-js-15-scaffold-15-scree/07.1-RESEARCH.md
</context>

<files_to_read>
- UI-SPEC-end-user.md sections U4 + U5 (states, copy keys, wireframe, primitives, journey)
- 07-CONTEXT.md D-API2 (sessions via Better Auth catch-all), D-API6 (no activity feed), D-STACK-6 (Recharts deferred)
- 07.1-RESEARCH.md § Pattern 1, § Pattern 5 (RSC prefetch + HydrationBoundary), § Per-screen e2e template
- apps/api/src/routes/usage.ts (verify response shape: { wordsUsed, wordsRemaining, plan, limitReached })
</files_to_read>

<files_to_modify>
See frontmatter. Owns: `apps/web/src/app/(auth)/app/page.tsx` (U4 root), `app/account/page.tsx` (U5), `components/screens/{usage,account}/*`, e2e files u4 + u5.
</files_to_modify>

<tests>
RED commit `test(07.1-08): add failing tests for U4 KPI, U5 sessions + delete dialog`

1. vitest component tests:
   - KpiCard renders title + value + caption from copy keys
   - UsageDashboardClient: shows 4 Skeletons on isPending; shows 4 KpiCards on success with formatted numbers; shows Alert on error; "empty state" N/A — assert that wordsUsed=0 still renders KPIs with "0" value (UI-SPEC says empty is N/A)
   - SessionsTable: lists rows from listSessions(); current session row shows badge `(this device)` derived from session.id match; Revoke button calls authClient.revokeSession({ token }); Revoke-other-sessions button calls authClient.revokeOtherSessions()
   - DeleteAccountDialog: input must equal user's email; Confirm disabled until match; calls authClient.deleteAccount() on confirm; on success router.push('/sign-in')
2. Playwright `u4-usage.spec.ts` — verbatim from RESEARCH § Per-screen e2e template plus axe scan
3. Playwright `u5-account.spec.ts` — 4 states + axe:
   - loading: `loadingFor('**/api/auth/list-sessions')` and `loadingFor('**/api/auth/get-session')`
   - empty: list-sessions returns single (current) session only — assert "no other sessions" copy
   - error: `errorFor('**/api/auth/list-sessions', 500)` shows Alert + Retry
   - success: seed 2 sessions (sign in from two contexts), assert 2 rows, revoke flow
   - axe clean
4. Run tests — RED. Commit.

GREEN commit `feat(07.1-08): U4 usage KPI dashboard + U5 account + sessions + delete`

5. Write U4 RSC page (`(auth)/app/page.tsx`):
   - Use `makeServerQueryClient()` + `prefetchQuery({ queryKey: queryKeys.usage(), queryFn: () => serverFetch('/api/usage').then(r => r.json()) })`
   - Wrap `<UsageDashboardClient />` in `<HydrationBoundary state={dehydrate(qc)}>`
6. Write `UsageDashboardClient.tsx`:
   - `useQuery({ queryKey: queryKeys.usage(), queryFn: () => clientFetch('/api/usage').then(r => r.json()) })`
   - Loading: render 4 Skeletons with `data-testid="usage-skeleton"`
   - Error: render Alert (destructive) + Retry button calling `query.refetch()`
   - Success: 4 KpiCards: Words used, Words remaining, Plan, Limit reached
   - Number formatting via `Intl.NumberFormat('en')`; Plan badge color = green if not limit reached, amber if limit reached
   - Each KpiCard has `data-testid="kpi-<slug>"`
7. Write U5 RSC page + `AccountClient.tsx`:
   - Card 1: Profile — name, email + verified badge, member-since (date-fns format)
   - Card 2: SessionsTable — rows: Device (User-Agent parse), IP, Started, Expires, Revoke action; header action Revoke-other-sessions; current session badge
   - Card 3 (danger zone): Delete-account dialog (AlertDialog) with email-confirm input
8. Implement actions:
   - revokeSession → mutation invalidates `queryKeys.sessions()`
   - revokeOtherSessions → mutation invalidates `queryKeys.sessions()`
   - deleteAccount → on success: `await authClient.signOut().catch(() => {})` (defensive) then `router.push('/sign-in')`
9. Run vitest + playwright — green. Coverage ≥ 90/90/90/90.
10. Commit GREEN.

Constraints:
- NO Recharts (D-STACK-6); KPI cards only.
- NO activity feed (D-API6).
- D-S1: only `GET /api/usage` + Better Auth catch-all paths — no new endpoints.
- All copy from `end-user.usage.*` + `end-user.account.*` keys.
- date-fns for "member since" formatting.
</tests>

<commit_message>
test(07.1-08): add failing tests for U4 KPI + U5 account/sessions/delete
feat(07.1-08): U4 usage dashboard (KPI-only) + U5 account + sessions + delete

- U4 /app: 4 KPI cards (wordsUsed, wordsRemaining, plan, limitReached) from
  GET /api/usage; NO Recharts (D-STACK-6); NO activity feed (D-API6)
- RSC prefetchQuery + HydrationBoundary; useQuery on client
- U5 /app/account: Profile card, SessionsTable, DeleteAccountDialog
- SessionsTable: Better Auth listSessions/revokeSession/revokeOtherSessions
- DeleteAccountDialog: type-email-to-confirm; authClient.deleteAccount → /sign-in
- 4 UI states × 2 screens + axe WCAG 2.2 AA per screen
- D-S1: zero new API endpoints
- Coverage ≥ 90/90/90/90

Refs: WEB-IMPL-02, WEB-IMPL-04, D-API2, D-API6, D-STACK-6, D-S1
</commit_message>
