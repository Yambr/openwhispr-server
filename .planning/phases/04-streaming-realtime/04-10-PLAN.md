---
phase: 04
plan: 10
type: execute
wave: 5
depends_on: [04-08, 04-09]
files_modified:
  - .github/workflows/nightly-realtime-soak.yml
  - tests/e2e/realtime-soak-live.test.ts
  - compose/live-soak/docker-compose.live.yml
  - docs/operations.md
  - docs/self-hosting.md
autonomous: true
requirements: [SCALE-05]
must_haves:
  truths:
    - ".github/workflows/nightly-realtime-soak.yml exists with schedule cron + push tag trigger"
    - "Workflow has if-guard restricting live execution to schedule + tag events (not PR)"
    - "Workflow consumes OPENAI_API_KEY from GitHub secrets"
    - "tests/e2e/realtime-soak-live.test.ts runs 65-min soak against real OpenAI Realtime"
    - "Workflow uploads close-frame log artifact on success or failure (always() condition)"
    - "compose/live-soak/docker-compose.live.yml overlay points the realtime upstream at wss://api.openai.com/v1/realtime (real OpenAI), NOT mock-realtime; nightly workflow uses this overlay (NOT compose/e2e/docker-compose.e2e.yml)"
    - "docs/operations.md (or docs/self-hosting.md) documents the :8443 entrypoint, ACME implications (cert-reuse for production), and the soak schedule"
    - "docs/self-hosting.md notes the new env vars: ASSEMBLYAI_API_KEY, DEEPGRAM_API_KEY, optional ASSEMBLYAI_TOKEN_TTL, DEEPGRAM_TOKEN_TTL, DEFAULT_AGENT_MODEL"
  artifacts:
    - path: ".github/workflows/nightly-realtime-soak.yml"
      provides: "Scheduled 06:00 UTC + tag-triggered live 65-min realtime soak"
      contains: "cron"
    - path: "tests/e2e/realtime-soak-live.test.ts"
      provides: "65-min soak against real OpenAI Realtime with close-code attribution"
    - path: "docs/operations.md"
      provides: ":8443 operator guidance (cert-reuse, sizing, soak schedule, env vars)"
      contains: "8443"
  key_links:
    - from: ".github/workflows/nightly-realtime-soak.yml"
      to: "tests/e2e/realtime-soak-live.test.ts"
      via: "pnpm test command in workflow steps"
      pattern: "realtime-soak-live"
---

<objective>
Final wave: live-soak GHA workflow + operator-facing docs. The 65-min OpenAI Realtime soak runs nightly + on release tags, gated to scheduled/tag events only (cost ~$15-25 per run, never on PR). Docs updates cover the :8443 entrypoint, ACME cert-reuse strategy, and the new env vars.

Purpose: Closes SCALE-05 SC#2 live-soak validation. Closes operator-facing documentation gap for the new ingress entrypoint and three new env-keyed token-mint endpoints.
Output: 1 GHA workflow + 1 live-soak test + 2 docs files updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@tests/e2e/realtime-soak-hermetic.test.ts
@.github/workflows
@docs
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tests/e2e/realtime-soak-live.test.ts (65-min real OpenAI soak)</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.10 lines 779-855
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-23 D-24 D-25
    - tests/e2e/realtime-soak-hermetic.test.ts (Plan 09 Task 3 — adapt for live)
  </read_first>
  <files>tests/e2e/realtime-soak-live.test.ts</files>
  <acceptance_criteria>
    - Test runs for 3900 seconds (65 minutes + 5-min margin per RESEARCH.md §2.10).
    - Connects to wss://api.localhost:8443/v1/realtime (NOT mock-realtime — real OpenAI via LiteLLM passthrough).
    - Drives ping every 20s; sends response.create occasionally; records every close frame and ping RTT.
    - Pass: zero close frames with code 1001 or 1011 before T+3600s. Codes 1006 are recorded but do NOT fail the test (community-documented OpenAI 1006 flakes).
    - Asserts p95 ping RTT < 1000ms across the 65 minutes.
    - Writes a close-frame log to tests/e2e/realtime-soak.log for artifact upload.
    - Test file has a top-level skip-when-no-key guard: 'describe.skipIf(!process.env.OPENAI_API_KEY)' so local dev runs (without keys) skip rather than fail.
    - testTimeout for this test: 4200000 (70 min ceiling).
    - compose/live-soak/docker-compose.live.yml created; overrides realtime upstream to point at real OpenAI (wss://api.openai.com/v1/realtime via LiteLLM gpt-realtime model entry consuming OPENAI_API_KEY); does NOT reference mock-realtime service.
    - Atomic commit.
  </acceptance_criteria>
  <action>
Adapt tests/e2e/realtime-soak-hermetic.test.ts to:
1. Change duration from 305s to 3905s.
2. Change the upstream from mock-realtime to real OpenAI Realtime — accomplished via a NEW compose overlay `compose/live-soak/docker-compose.live.yml` (created by this task) that overrides the LiteLLM realtime model entry to point at `wss://api.openai.com/v1/realtime` (or LiteLLM with the gpt-realtime model + real OPENAI_API_KEY). This overlay MUST NOT reference the mock-realtime service. The nightly workflow (Task 2) uses this overlay INSTEAD of compose/e2e/docker-compose.e2e.yml — under no circumstance does the live job run against the hermetic mock.
3. Change close-code attribution threshold from 300 to 3600 (T+3600s).
4. Write all close events + ping RTTs to tests/e2e/realtime-soak.log as JSONL (one event per line) so the GHA artifact upload captures them.
5. Add describe.skipIf(!OPENAI_API_KEY) at the top.
6. Distinguish 1006 from 1001/1011 in the attribution table (1006 is upstream, ours are 1001/1011).
  </action>
  <verify>
    <automated>test -f tests/e2e/realtime-soak-live.test.ts &amp;&amp; grep -qE 'OPENAI_API_KEY|skipIf' tests/e2e/realtime-soak-live.test.ts &amp;&amp; grep -qE '3900|3905' tests/e2e/realtime-soak-live.test.ts &amp;&amp; test -f compose/live-soak/docker-compose.live.yml &amp;&amp; grep -qE 'wss://api.openai.com|api.openai.com/v1/realtime|openai/gpt-realtime' compose/live-soak/docker-compose.live.yml &amp;&amp; ! grep -q 'mock-realtime' compose/live-soak/docker-compose.live.yml</automated>
  </verify>
  <done>Live-soak test exists with 65-min duration + skip-when-no-key guard.</done>
</task>

<task type="auto">
  <name>Task 2: Implement .github/workflows/nightly-realtime-soak.yml</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.10 lines 783-810
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-23
    - .github/workflows (existing workflow conventions, especially nightly.yml if it exists)
    - tests/e2e/realtime-soak-live.test.ts (Task 1)
  </read_first>
  <files>.github/workflows/nightly-realtime-soak.yml</files>
  <acceptance_criteria>
    - Workflow file exists at the literal path .github/workflows/nightly-realtime-soak.yml.
    - 'on:' section has 'schedule:' with 'cron: 0 6 * * *' AND 'push:' with 'tags: [v*]' AND 'workflow_dispatch:' (manual trigger for ad-hoc runs).
    - Job has 'if:' guard: 'github.event_name == \'schedule\' || startsWith(github.ref, \'refs/tags/\') || github.event_name == \'workflow_dispatch\''.
    - Job timeout-minutes >= 90.
    - Steps include: actions/checkout (SHA-pinned per project conventions; use the same version as ci.yml), Docker compose up using the LIVE overlay `compose/live-soak/docker-compose.live.yml` (NOT compose/e2e/docker-compose.e2e.yml), pnpm test invoking 'tests/e2e/realtime-soak-live.test.ts' with E2E=1 + OPENAI_API_KEY from secrets, archive close-frame log via actions/upload-artifact (also SHA-pinned) with 'if: always()' so failures upload the log too.
    - Workflow file MUST NOT reference 'mock-realtime' anywhere (verified by grep -v): the live job runs against real OpenAI exclusively.
    - Workflow does NOT trigger on pull_request (PR cost prevention).
    - Atomic commit.
  </acceptance_criteria>
  <action>
Write the workflow per RESEARCH.md §2.10 lines 783-808 with these specific corrections:
- Use SHA-pinned 3rd-party actions per project security policy (Trivy 2026-03-19 incident response). Fetch the current SHA for actions/checkout@v4 and actions/upload-artifact@v4 from existing project workflows (e.g., .github/workflows/ci.yml) and reuse the exact pinned SHA + version-tag comment.
- Workflow name: 'nightly-realtime-soak'.
- Schedule: '0 6 * * *' (06:00 UTC daily).
- Tag trigger: matches 'v*'.
- workflow_dispatch added to allow operator-initiated ad-hoc runs (per CONTEXT.md deferred ideas: 'workflow_dispatch UI for ad-hoc live-soak runs — nice-to-have CI ergonomics, planner may include').
- Steps:
  1. Checkout (SHA-pinned).
  2. Setup pnpm + Node 24 (mirror existing ci.yml setup).
  3. docker compose -f docker-compose.yml -f compose/live-soak/docker-compose.live.yml up -d --wait (full stack — LIVE overlay, NOT the hermetic e2e overlay; live overlay points realtime upstream at real OpenAI).
  4. pnpm install --frozen-lockfile.
  5. Run live soak: env OPENAI_API_KEY=secrets.OPENAI_API_KEY E2E=1 pnpm vitest run --config tests/e2e/vitest.e2e.config.ts tests/e2e/realtime-soak-live.test.ts.
  6. actions/upload-artifact with name 'realtime-soak-log' and path 'tests/e2e/realtime-soak.log' AND if: always().
  7. docker compose down (cleanup).
- Branch protection: this workflow is NOT a required check on main (it's gated to scheduled events; PRs do not trigger it).
  </action>
  <verify>
    <automated>test -f .github/workflows/nightly-realtime-soak.yml &amp;&amp; grep -qE 'cron.*0 6 \* \* \*' .github/workflows/nightly-realtime-soak.yml &amp;&amp; grep -qE 'workflow_dispatch' .github/workflows/nightly-realtime-soak.yml &amp;&amp; grep -qE 'OPENAI_API_KEY' .github/workflows/nightly-realtime-soak.yml &amp;&amp; grep -qE 'if: always' .github/workflows/nightly-realtime-soak.yml &amp;&amp; grep -qE 'compose/live-soak/docker-compose.live.yml' .github/workflows/nightly-realtime-soak.yml &amp;&amp; ! grep -q 'mock-realtime' .github/workflows/nightly-realtime-soak.yml</automated>
  </verify>
  <done>Nightly + tag + manual-dispatch live-soak workflow committed.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/operations.md and docs/self-hosting.md for :8443 entrypoint + new env vars</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.3 lines 369-377 (ACME paths)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-21 D-23
    - .planning/phases/04-streaming-realtime/04-VALIDATION.md Manual-Only Verifications
    - docs/operations.md (existing — Phase 1)
    - .env.example (current state after Plan 03)
  </read_first>
  <files>docs/operations.md, docs/self-hosting.md</files>
  <acceptance_criteria>
    - docs/operations.md has a new section titled 'Realtime ingress (:8443)' covering: dedicated entrypoint rationale (T-04-02 isolation), the 3600s idleTimeout ceiling, cert-reuse strategy for production (cert-manager mounts the same secret to both :443 and :8443; no separate ACME challenge required since HTTP-01 cannot validate on :8443 anyway), DNS-01 alternative path noted as a TODO, and a brief note that the nightly-realtime-soak workflow validates the topology.
    - docs/self-hosting.md (or a new section in docs/operations.md if self-hosting.md does not exist) documents the new env vars: ASSEMBLYAI_API_KEY (required for /api/streaming-token, missing key returns 503), DEEPGRAM_API_KEY (same for /api/deepgram-streaming-token), DEFAULT_AGENT_MODEL (optional override of the qwen/qwen3.6-plus default for /api/agent/stream), ASSEMBLYAI_TOKEN_TTL (optional, default 60), DEEPGRAM_TOKEN_TTL (optional, default 30). OPENAI_API_KEY is referenced (already documented in Phase 3 docs).
    - docs/operations.md includes a troubleshooting note (per BACKEND_SPEC contract on /api/agent/stream): a 200 response with a terminal NDJSON line `{type:"finish", finishReason:"upstream_error", ...}` indicates LLM upstream unavailability — headers were already flushed before the upstream call (BACKEND_SPEC requirement to surface NDJSON content-type within the first-line latency budget). Operators correlating these failures should grep api logs for the matching `x-litellm-call-id` (LiteLLM emits this on every upstream attempt) to identify the failing provider/model.
    - Both docs cross-reference the corresponding BACKEND_SPEC sections.
    - English-only per DOCS-09.
    - Atomic commit.
  </acceptance_criteria>
  <action>
Append to docs/operations.md a new section. If the file does not yet exist (Phase 1 deferred), create it with a stub including only the realtime ingress section.

If docs/self-hosting.md exists, append the env-var section there. If not, add the env-var section to docs/operations.md instead under a 'Phase 4 — Streaming + Realtime env vars' heading.

Cross-reference BACKEND_SPEC sections by relative path 'docs/wire-contract.md' (Phase 10 will create that index doc) — for now, reference the upstream spec path /Users/nick/openwhispr/docs/BACKEND_SPEC.md as the source of truth.
  </action>
  <verify>
    <automated>grep -q '8443' docs/operations.md &amp;&amp; (grep -q 'ASSEMBLYAI_API_KEY' docs/operations.md || grep -q 'ASSEMBLYAI_API_KEY' docs/self-hosting.md)</automated>
  </verify>
  <done>Operator-facing docs updated for the :8443 entrypoint and the new env vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA workflow secrets to live OpenAI | OPENAI_API_KEY only available in scheduled/tag/dispatch contexts (gated by if-guard); PRs from forks cannot read secrets |
| Operator-facing docs | Source-of-truth disclosures of env vars + ingress topology |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | DoS | live realtime ingress at :8443 | mitigate | 65-min nightly soak (Task 1+2) empirically validates the timeout config against real OpenAI behavior; close-frame artifact uploaded on failure for post-mortem |
| T-04-COST | DoS (financial) | live OpenAI API usage in CI | mitigate | if-guard restricts the live job to scheduled/tag/dispatch events; PRs from forks cannot trigger; ~$15-25 per execution capped by 65-min wall-clock |
| T-04-DOC-DRIFT | Information Disclosure (operator confusion) | env-var configuration | mitigate | Task 3 documents the exact env-var names and missing-key 503 behavior so operators do not misdiagnose 503s |
</threat_model>

<verification>
- .github/workflows/nightly-realtime-soak.yml exists with cron + tag + dispatch triggers and if-guard.
- tests/e2e/realtime-soak-live.test.ts has 65-min duration and skipIf(!OPENAI_API_KEY).
- docs/operations.md (or self-hosting.md) covers :8443 + ACME cert-reuse + new env vars.
- gh workflow list shows the new workflow registered.
</verification>

<success_criteria>
- SCALE-05 SC#2 live-soak validation gate in place (nightly schedule).
- Operator-facing docs updated.
- Phase 4 closure unblocked: gsd-verifier can confirm hermetic 5-min soak passes (plan 09) and the live-soak gate is registered (this plan).
</success_criteria>

<output>
After completion, create .planning/phases/04-streaming-realtime/04-10-SUMMARY.md with: workflow registration confirmation (gh workflow list), live-soak skip-when-no-key behavior, docs additions inventory, Phase 4 closure readiness.
</output>
