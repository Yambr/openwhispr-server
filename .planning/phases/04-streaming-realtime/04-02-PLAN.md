---
phase: 04
plan: 02
type: tdd
wave: 1
depends_on: [04-01]
files_modified:
  - apps/api/src/lib/sse-parser.ts
  - apps/api/src/lib/sse-parser.test.ts
  - apps/api/src/lib/tool-call-accumulator.ts
  - apps/api/src/lib/tool-call-accumulator.test.ts
autonomous: true
requirements: [WIRE-07]
must_haves:
  truths:
    - "Pure async generator sseToNdjson handles all 7 fixtures without throwing"
    - "Tool-call accumulator emits ONE consolidated chunk per tool_calls index on finish_reason=tool_calls"
    - "Tool-call accumulator drops state silently when finish_reason=stop with partial args (LiteLLM#17246 safety)"
    - "Both modules at ≥90/90/90/90 coverage"
  artifacts:
    - path: "apps/api/src/lib/sse-parser.ts"
      provides: "Pure async generator transforming OpenAI SSE → BACKEND_SPEC NDJSON chunks"
      exports: ["sseToNdjson", "type StreamChunk", "type SseToNdjsonInput"]
    - path: "apps/api/src/lib/tool-call-accumulator.ts"
      provides: "Pure state machine accumulating delta.tool_calls[].function.arguments by index"
      exports: ["createToolCallAccumulator", "type ToolCallAccumulator"]
  key_links:
    - from: "apps/api/src/lib/sse-parser.ts"
      to: "apps/api/src/lib/tool-call-accumulator.ts"
      via: "Injected ToolCallAccumulator dependency in SseToNdjsonInput.acc"
      pattern: "input.acc.absorb|input.acc.flush"
---

<objective>
TDD plan: turn the Wave 0 RED stubs GREEN by implementing the pure SSE→NDJSON parser and tool-call accumulator. Both are framework-free pure functions with ≥90/90/90/90 coverage achievable from the fixture corpus.

Purpose: Provides the building blocks Wave 2 plan 05 (`/api/agent/stream` route) will compose. Mitigates T-04-03 (NDJSON stream-injection) by validating each `data:` frame is `JSON.parse`-able before forwarding.
Output: 2 production modules + 2 fully-passing test suites at ≥90/90/90/90.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@apps/api/src/routes/agent/__fixtures__/text-only.sse
@apps/api/src/routes/agent/__fixtures__/single-tool-call.sse
@apps/api/src/lib/sse-parser.test.ts
@apps/api/src/lib/tool-call-accumulator.test.ts

<interfaces>
<!-- Output contracts that Wave 2 plan 05 will consume -->

```typescript
// sse-parser.ts
export type StreamChunk =
  | { type: "text-delta"; text: string }
  | { type: "tool-call"; toolCallId: string; toolName: string; args: unknown }
  | { type: "tool-result"; toolCallId: string; result: unknown }
  | { type: "finish"; finishReason: string; usage: { promptTokens: number; completionTokens: number } };

export interface SseToNdjsonInput {
  body: ReadableStream<Uint8Array>;
  acc: ToolCallAccumulator;
}

export async function* sseToNdjson(input: SseToNdjsonInput): AsyncGenerator<StreamChunk>;

// tool-call-accumulator.ts
export interface ToolCallAccumulator {
  absorb(delta: { tool_calls?: Array<{ index: number; id?: string; function?: { name?: string; arguments?: string } }> }): void;
  flush(): Array<{ type: "tool-call"; toolCallId: string; toolName: string; args: unknown }>;
  hasPending(): boolean;
}
export function createToolCallAccumulator(): ToolCallAccumulator;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tool-call accumulator (state machine first — sse-parser depends on it)</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §2.4 (lines 382-440)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-09 (delta accumulation by index)
    - apps/api/src/lib/tool-call-accumulator.test.ts (Wave 0 RED stubs)
  </read_first>
  <files>
    apps/api/src/lib/tool-call-accumulator.ts,
    apps/api/src/lib/tool-call-accumulator.test.ts
  </files>
  <behavior>
    - Test 1: Single tool call — absorb 5 deltas with `{index:0, function:{arguments:"frag"}}` parts; flush returns one chunk with merged args parsed as JSON object.
    - Test 2: Multi tool call — absorb interleaved deltas with `index:0` and `index:1`; flush returns 2 chunks in index order (0 first).
    - Test 3: Malformed args JSON — absorb a delta whose accumulated arguments string is invalid JSON; flush returns chunk with `args: { __unparsed: "<raw>" }` (graceful fallback, not a throw).
    - Test 4: Missing function.name — absorb a delta with id but no name; flush silently skips that index (returns chunk only for indexes with name).
    - Test 5: hasPending() returns true when state non-empty, false after flush.
    - Test 6: hasPending() with finish_reason=stop safety — caller receives hasPending=true but does NOT call flush (LiteLLM#17246 safety, T-04-03 mitigation). State is cleared by clear() or by next absorb call from a new stream.
    - Test 7: id fallback — when function.id is absent, emitted chunk uses `tc_<index>` as toolCallId.
  </behavior>
  <action>
RED first (Wave 0 stubs already failing). Replace test file with the 7 behavior tests above. Each test imports `createToolCallAccumulator` from `./tool-call-accumulator.js` (relative .js for ESM/NodeNext).

Then write `tool-call-accumulator.ts` per RESEARCH.md §2.4 (lines 386-432 — copy verbatim with these exact corrections):
- Use `Map<number, PartialToolCall>` keyed by `tc.index`.
- `absorb()` mutates state, returns void.
- `flush()` sorts indexes ascending, skips entries without `name`, parses `args` with JSON.parse fallback to `{__unparsed: rawString}`, sets `toolCallId: p.id ?? "tc_" + k`, clears state, returns array.
- `hasPending(): boolean` returns `state.size > 0`.
- Add NO public clear() method — flush() is the only state-clearing path.

GREEN: all 7 tests pass.
REFACTOR: extract `parseArgsOrFallback(raw: string): unknown` if branches feel duplicated; keep module ≤ 60 LoC.

Coverage gate: `pnpm --filter @openwhispr/api test src/lib/tool-call-accumulator.test.ts --run --coverage` reports ≥90/90/90/90 on tool-call-accumulator.ts.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test src/lib/tool-call-accumulator.test.ts --run --coverage 2>&1 | tee /tmp/04-02-t1.log && grep -E 'tool-call-accumulator\.ts.*\| *9[0-9]\.|tool-call-accumulator\.ts.*\| *100' /tmp/04-02-t1.log | wc -l | grep -qE '^\s*[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @openwhispr/api test src/lib/tool-call-accumulator.test.ts --run` exits 0 with all 7 tests passing.
    - Coverage report shows tool-call-accumulator.ts at ≥90% on lines, branches, functions, statements (4 axes).
    - File contains `export function createToolCallAccumulator()` and the returned object literal has methods `absorb`, `flush`, `hasPending`.
    - `git log -1 --name-only` shows BOTH `tool-call-accumulator.ts` AND `tool-call-accumulator.test.ts` in the SAME commit (TDD constitutional rule — fix + test land together).
  </acceptance_criteria>
  <done>Accumulator GREEN at ≥90/90/90/90; Wave 2 plan 05 can compose it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SSE→NDJSON pure async generator</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §2.1 (lines 110-200)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-01, D-02, D-06 (hand-rolled producer, flush semantics, inline async generator)
    - apps/api/src/lib/sse-parser.test.ts (Wave 0 RED stubs)
    - apps/api/src/lib/tool-call-accumulator.ts (Task 1 just landed — accumulator interface)
    - All 7 fixtures in apps/api/src/routes/agent/__fixtures__/
  </read_first>
  <files>
    apps/api/src/lib/sse-parser.ts,
    apps/api/src/lib/sse-parser.test.ts
  </files>
  <behavior>
    - Test text-only.sse: yields N `text-delta` chunks (one per content delta) + 1 `finish` chunk with `finishReason:"stop"` and `usage.promptTokens`/`completionTokens` populated. Does NOT yield anything after `[DONE]` sentinel.
    - Test single-tool-call.sse: yields 0 `text-delta`, 1 `tool-call` chunk (consolidated from accumulator on `finish_reason:"tool_calls"`), 1 `finish` chunk with `finishReason:"tool_calls"`. The `tool-call.args` is a parsed JSON object.
    - Test multi-tool-call.sse: yields 2 `tool-call` chunks in index order (0 then 1).
    - Test text-then-tool.sse: yields text-delta chunks BEFORE the tool-call chunk. Order preserved.
    - Test premature-close.sse: yields whatever was emitted before close, then 1 `finish` chunk with `finishReason:"incomplete"` and zero usage tokens. Does NOT throw.
    - Test malformed-payload.sse: skips the malformed frame (logs would happen at caller; parser tolerates), continues with subsequent valid frames.
    - Test utf8-split.sse: yields a `text-delta` chunk whose text contains the full "🎉" emoji intact (not corrupted by the read-boundary split). Use `Readable.from([buf.slice(0, splitByte), buf.slice(splitByte)])` to force the split.
  </behavior>
  <action>
RED first. Replace Wave 0 stub assertions with concrete expectations per behavior list above. Each fixture test reads its file with `readFileSync(...)`, splits the buffer at a known offset for utf8-split (use the `# split-at-byte-N` header line from Wave 0 to determine N), constructs a `ReadableStream` via `Readable.toWeb(Readable.from(chunks))`, and asserts the emitted chunk array.

Then write `sse-parser.ts` per RESEARCH.md §2.1 lines 122-177. Verbatim copy with these exact corrections:
- File path: `apps/api/src/lib/sse-parser.ts` (NOT `routes/agent/sse-to-ndjson.ts` — flatter structure per Phase 2 convention).
- Export `sseToNdjson` as named export.
- Use `TextDecoder("utf-8")` with `decoder.decode(value, {stream: true})` for incremental decoding.
- Partial-frame buffer is a single `let buf = ""`.
- SSE frame separator is `\n\n`. Within a frame, the line starting with `data: ` carries the JSON payload. `data: [DONE]` is the end-of-stream sentinel — `return` immediately.
- Helper `function translateChunk(json: any, acc: ToolCallAccumulator): StreamChunk[]`:
  - If `json.choices[0].delta.content` is a non-empty string → emit `{type:"text-delta", text: <content>}`.
  - If `json.choices[0].delta.tool_calls` exists → call `acc.absorb(json.choices[0].delta)`; emit nothing.
  - If `json.choices[0].finish_reason === "tool_calls"` → emit `acc.flush()` results, then emit `{type:"finish", finishReason:"tool_calls", usage: {promptTokens: json.usage?.prompt_tokens ?? 0, completionTokens: json.usage?.completion_tokens ?? 0}}`.
  - If `json.choices[0].finish_reason === "stop"` → emit only the `finish` chunk; if `acc.hasPending()` is true, log no-op (parser is pure; caller logs); discard accumulator state by NOT calling flush.
  - On malformed JSON in `data:` line (caught by `JSON.parse` throw): silently skip that frame (the parser is tolerant; T-04-03 mitigation: malformed upstream cannot poison downstream).

GREEN: all 7 fixture tests pass.
REFACTOR: extract `translateChunk` to a sibling un-exported function if it crosses 30 LoC; keep parser ≤ 120 LoC total.

Coverage: ≥90/90/90/90 on sse-parser.ts via fixture corpus.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test src/lib/sse-parser.test.ts --run --coverage 2>&1 | tee /tmp/04-02-t2.log && grep -E 'sse-parser\.ts.*\| *9[0-9]\.|sse-parser\.ts.*\| *100' /tmp/04-02-t2.log | wc -l | grep -qE '^\s*[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @openwhispr/api test src/lib/sse-parser.test.ts --run` exits 0 with 7 fixture tests passing.
    - Coverage on `sse-parser.ts` ≥90/90/90/90.
    - The utf8-split test asserts the emitted text contains exactly one `🎉` (verifiable by `expect(text.includes("🎉")).toBe(true)`).
    - The premature-close test asserts the LAST emitted chunk is `{type:"finish", finishReason:"incomplete", ...}`.
    - The malformed-payload test asserts the parser does NOT throw and emits at least one valid chunk from surrounding frames.
    - `git log -1 --name-only` shows sse-parser.ts AND sse-parser.test.ts in the SAME commit.
  </acceptance_criteria>
  <done>Parser GREEN against all 7 fixtures at ≥90/90/90/90 coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiteLLM upstream → sseToNdjson | Untrusted SSE frames cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | sse-parser.ts | mitigate | Parser validates each `data:` frame is JSON.parse-able before forwarding; malformed frames are silently dropped (verified by malformed-payload.sse fixture test). Tool-call accumulator drops accumulator state on finish_reason=stop with partial args (verified by Task 1 Test 6) — prevents partial-args injection (LiteLLM#17246 shape). |
</threat_model>

<verification>
- `pnpm --filter @openwhispr/api test src/lib/ --run --coverage` shows both files at ≥90/90/90/90 on all axes.
- `git log -1 --stat` for each task commit shows production + test file in same commit.
</verification>

<success_criteria>
- 2 modules + 2 test suites GREEN at ≥90/90/90/90.
- All 7 SSE fixtures and 7 accumulator behavior tests pass.
- Wave 2 plan 05 can import `sseToNdjson` and `createToolCallAccumulator` directly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-streaming-realtime/04-02-SUMMARY.md` with: per-file coverage axes, fixture-test outcomes, and a one-line atomic-commit-per-task confirmation.
</output>
