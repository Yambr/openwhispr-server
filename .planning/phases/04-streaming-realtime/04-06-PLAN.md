---
phase: 04
plan: 06
type: tdd
wave: 3
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - apps/api/src/routes/agent/stream.ts
  - apps/api/src/routes/agent/stream.test.ts
  - apps/api/src/routes/agent/translate-tools.ts
  - apps/api/src/routes/agent/translate-tools.test.ts
  - apps/api/src/routes/index.ts
  - apps/api/src/index.ts
autonomous: true
requirements: [WIRE-07, WIRE-13, WIRE-14, WIRE-15]
must_haves:
  truths:
    - "POST /api/agent/stream returns 200 with Content-Type application/x-ndjson"
    - "Handler uses reply.hijack(), reply.raw.flushHeaders(), request.raw.socket.setNoDelay(true) sequence per RESEARCH §2.2"
    - "Each emitted chunk goes through reply.raw.write(JSON.stringify(chunk) + newline) — one chunk per upstream delta"
    - "Legacy tools array translated to OpenAI tools shape via translateLegacyTools (D-07)"
    - "systemPrompt prepended additively at messages[0] (D-11)"
    - "model defaults to req.body.model ?? env.DEFAULT_AGENT_MODEL ?? qwen/qwen3.6-plus (D-10)"
    - "stream_options.include_usage:true forwarded; x-litellm-call-id captured in server log only (D-12)"
    - "Client disconnect aborts upstream (request.raw.on close fires AbortController)"
    - "All 4 new routes registered in routes/index.ts; index.ts buildApp wires the 3 token routes unconditionally and agent/stream conditionally on litellm presence"
    - "Coverage >=90/90/90/90 on stream.ts and translate-tools.ts"
  artifacts:
    - path: "apps/api/src/routes/agent/stream.ts"
      provides: "POST /api/agent/stream NDJSON streaming chat handler with hand-rolled SSE→NDJSON producer"
      exports: ["buildAgentStreamRoutes", "type AgentStreamDeps"]
    - path: "apps/api/src/routes/agent/translate-tools.ts"
      provides: "Pure helpers: translateLegacyTools, prependSystemPrompt"
      exports: ["translateLegacyTools", "prependSystemPrompt"]
    - path: "apps/api/src/routes/index.ts"
      provides: "Updated buildAllRoutes registering 4 new Phase 4 routes"
  key_links:
    - from: "apps/api/src/routes/agent/stream.ts"
      to: "apps/api/src/lib/sse-parser.ts"
      via: "sseToNdjson async generator over upstream.body"
      pattern: "sseToNdjson"
    - from: "apps/api/src/routes/agent/stream.ts"
      to: "apps/api/src/lib/tool-call-accumulator.ts"
      via: "createToolCallAccumulator injected into sseToNdjson"
      pattern: "createToolCallAccumulator"
    - from: "apps/api/src/routes/index.ts"
      to: "apps/api/src/routes/agent/stream.ts and tokens/{assemblyai,deepgram,openai-realtime}.ts"
      via: "buildAllRoutes plugins array"
      pattern: "buildAgentStreamRoutes|buildAssemblyAITokenRoutes|buildDeepgramTokenRoutes|buildOpenAIRealtimeTokenRoutes"
---

<objective>
TDD plan: implement /api/agent/stream route handler composing the Wave 1 utilities, plus tool-translation and system-prompt helpers. Wire all four new Phase 4 routes (agent/stream + 3 token mints) into the buildAllRoutes registrar.

Purpose: Closes WIRE-07 NDJSON line-flush contract. Delivers the load-bearing route for Phase 4 SC#1 (first-line < 500ms through full chain — verified empirically in Wave 3 plan 09).
Output: 2 production modules + 2 test suites at >=90/90/90/90, plus updates to routes/index.ts + apps/api/src/index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@apps/api/src/lib/sse-parser.ts
@apps/api/src/lib/tool-call-accumulator.ts
@apps/api/src/routes/realtime.ts
@apps/api/src/routes/transcribe.ts
@apps/api/src/routes/index.ts
@apps/api/src/index.ts
@apps/api/src/routes/tokens/assemblyai.ts
@apps/api/src/routes/tokens/deepgram.ts
@apps/api/src/routes/tokens/openai-realtime.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement translate-tools.ts pure helpers (translateLegacyTools, prependSystemPrompt)</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.2 lines 240-258 (where these helpers are called)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-07 D-11
    - /Users/nick/openwhispr/docs/BACKEND_SPEC.md §/api/agent/stream tools array shape
  </read_first>
  <files>apps/api/src/routes/agent/translate-tools.ts, apps/api/src/routes/agent/translate-tools.test.ts</files>
  <behavior>
    translateLegacyTools tests:
    - Test 1: undefined input returns undefined.
    - Test 2: empty array returns empty array.
    - Test 3: single legacy tool {name, description, parameters} translates to {type:'function', function:{name, description, parameters}}.
    - Test 4: multiple tools preserve order.
    - Test 5: missing description tolerated (translated as undefined description).
    prependSystemPrompt tests:
    - Test 6: undefined systemPrompt returns messages array unchanged.
    - Test 7: empty string systemPrompt returns unchanged.
    - Test 8: with systemPrompt 'foo' and existing messages [{role:user, content:hi}], returns [{role:system, content:foo}, {role:user, content:hi}].
    - Test 9: with systemPrompt 'foo' AND existing leading system message, prepends ADDITIVELY (does NOT replace) — D-11 'Never replace an existing system message'. Result: [{role:system, content:foo}, {role:system, content:original}, {role:user, content:hi}].
  </behavior>
  <action>
RED first. GREEN: implement translate-tools.ts as a pure module:

translateLegacyTools(tools): if undefined returns undefined; otherwise tools.map(t => ({type: 'function', function: {name: t.name, description: t.description, parameters: t.parameters}})).

prependSystemPrompt(messages, systemPrompt): if !systemPrompt returns messages; otherwise returns [{role: 'system', content: systemPrompt}, ...messages] (always-prepend per D-11).

Coverage >=90/90/90/90 trivially achievable.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/agent/translate-tools.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 9 tests pass.
    - Coverage on translate-tools.ts >=90/90/90/90.
    - prependSystemPrompt does NOT replace an existing system message (Test 9 explicitly verifies).
    - Atomic commit.
  </acceptance_criteria>
  <done>Pure helpers GREEN; stream.ts can compose them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /api/agent/stream route handler with hand-rolled NDJSON producer</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.1 (parser usage) + section 2.2 (Fastify lifecycle full handler lines 207-296)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-01 D-02 D-03 D-04 D-06 D-08 D-10 D-11 D-12
    - apps/api/src/lib/sse-parser.ts (Wave 1 plan 02 - must exist)
    - apps/api/src/lib/tool-call-accumulator.ts (Wave 1 plan 02 - must exist)
    - apps/api/src/routes/agent/translate-tools.ts (this plan task 1)
    - apps/api/src/routes/transcribe.ts (route factory pattern + LiteLLM client deps reference)
    - packages/litellm-client (Phase 3 client interface)
  </read_first>
  <files>apps/api/src/routes/agent/stream.ts, apps/api/src/routes/agent/stream.test.ts</files>
  <behavior>
    Test 1 (200 + content-type + first-line speed): with a fixture LiteLLM mock returning the text-only.sse content with a 100ms cadence between SSE chunks, the route returns 200 with Content-Type 'application/x-ndjson' AND the first NDJSON line is read from the raw socket within 200ms of the response headers (unit-level latency budget; e2e budget < 500ms is in plan 09).
    Test 2 (chunk vocabulary byte-for-byte): for the multi-tool-call.sse fixture, the emitted NDJSON lines parse to chunks matching BACKEND_SPEC: text-delta has key 'text', tool-call has keys 'toolCallId', 'toolName', 'args', finish has keys 'finishReason' and 'usage' with promptTokens and completionTokens.
    Test 3 (legacy tools translation): request body has tools array with legacy shape; the upstream POST to LiteLLM is intercepted via undici MockAgent, body parsed as JSON, asserted to contain tools array with OpenAI shape (type:'function', function.name, function.parameters).
    Test 4 (system prompt additive): request body has systemPrompt 'foo' AND messages with a leading system message; upstream body has messages[0]={role:system,content:foo} and messages[1]={role:system,content:<original>} — additive prepend verified.
    Test 5 (model default chain): request body has no model AND env DEFAULT_AGENT_MODEL='custom-model'; upstream body has model='custom-model'. With env unset, model='qwen/qwen3.6-plus'. With body.model='explicit', body.model wins.
    Test 6 (stream_options forwarded): upstream body always has stream:true AND stream_options:{include_usage:true} AND user equal to req.user.id.
    Test 7 (x-litellm-call-id captured server-side only): MockAgent sets header x-litellm-call-id 'call_xyz' on the upstream response; server log line includes 'call_xyz'; the wire response does NOT contain 'call_xyz' anywhere (verified by reading the full response body and asserting absent — T-04-LEAK).
    Test 8 (client disconnect aborts upstream): test client closes its socket mid-stream; the AbortController signal fires on upstream fetch (verified by MockAgent intercept's signal.aborted being true after the client disconnect).
    Test 9 (upstream non-2xx): MockAgent returns 503 from LiteLLM; route emits ONE finish chunk with finishReason 'upstream_error' and ends the response. Status code on the response is 200 (already sent before upstream call) but the finish chunk signals the error to the client.
    Test 10 (mid-stream error): MockAgent stream throws after 3 chunks; route emits a finish chunk with finishReason 'stream_error' if the socket is still writable, then ends.
    Test 11 (auth gate): unauthenticated request returns 401 BEFORE the handler hijacks the reply (Phase 2 D-04 dual-auth hook).
    Test 12 (X-Accel-Buffering header): response headers include X-Accel-Buffering equal to 'no' (forward-compat for nginx-fronting operators per D-04).
  </behavior>
  <action>
RED first. Tests use:
- Fastify + dual-auth hook + global error handler + LiteLLM client stub deps.
- undici MockAgent intercepting LITELLM_BASE_URL/v1/chat/completions; mockAgent emits SSE chunks at controlled cadence using setTimeout-driven pushes.
- Inject req.user.id via a test-only onRequest hook (Phase 2 pattern).

GREEN: write apps/api/src/routes/agent/stream.ts exporting buildAgentStreamRoutes(deps: AgentStreamDeps). Deps include: db (TransactionalDb — for future ledger; currently unused but reserved), litellm (the existing LitellmClient from packages/litellm-client — for baseUrl + masterKey access matching Phase 3 D-03).

Handler implementation per RESEARCH.md §2.2 lines 207-285 verbatim with these specific corrections and additions:

1. zod schema for request body: messages (required, array of {role, content}), optional model (string), optional systemPrompt (string), optional tools (array of {name, description?, parameters}). Validate via Fastify's schema option OR an inline parse with a 400 fallback.

2. Auth gate: throw AuthError('unauthorized') if req.user?.id is absent (the global dual-auth hook should already have populated it; this is defense-in-depth).

3. Set headers via reply.header before reply.hijack: 'Content-Type'='application/x-ndjson', 'X-Accel-Buffering'='no', 'Cache-Control'='no-cache, no-transform'.

4. reply.hijack() then reply.raw.flushHeaders() then req.raw.socket.setNoDelay(true).

5. const abort = new AbortController; req.raw.once('close', () => abort.abort()).

6. Build upstream POST: url = deps.litellm.baseUrl + '/v1/chat/completions'; headers = content-type application/json AND authorization 'Bearer ' + deps.litellm.masterKey AND x-litellm-spend-logs-metadata = JSON.stringify with keys openwhispr_request_id (req.id) and openwhispr_user_id (req.user.id). Body uses translateLegacyTools(req.body.tools), prependSystemPrompt(req.body.messages, req.body.systemPrompt), model resolved via the chain D-10, plus stream:true, stream_options:{include_usage:true}, user:req.user.id. Pass abort.signal as signal.

7. If !upstream.ok or !upstream.body: write one finish chunk with finishReason 'upstream_error' and zero usage, then reply.raw.end(). Return.

8. Log: req.log.info with key litellmCallId from upstream.headers.get('x-litellm-call-id'). Ensure the log line is structured (pino) and the value is NEVER written to the wire response.

9. Drain via for-await over sseToNdjson({body: upstream.body, acc: createToolCallAccumulator()}). Per chunk: reply.raw.write(JSON.stringify(chunk) + '\n').

10. try/catch: on mid-stream error, if !reply.raw.writableEnded, write a finish chunk with finishReason 'stream_error'. finally: if !reply.raw.writableEnded, reply.raw.end().

REFACTOR: keep handler under 130 LoC; coverage >=90/90/90/90 (every branch from upstream-ok / upstream-non-ok / upstream-no-body / mid-stream error / client disconnect / 401-gate is exercised).

CRITICAL: After reply.hijack() Fastify's global error handler does NOT fire. Handler errors land in the try/catch — verified by Test 9 + Test 10 emitting synthetic finish chunks rather than the canonical error envelope.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/agent/stream.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 12 tests pass; coverage on stream.ts >=90/90/90/90.
    - The handler invokes reply.hijack() exactly once (verified by spy in Test 1).
    - The wire response NEVER contains the x-litellm-call-id value (Test 7 explicit assertion).
    - Test 8 explicitly verifies the upstream fetch's signal becomes aborted within 100ms of client disconnect.
    - Test 12 verifies X-Accel-Buffering=no is set.
    - The atomic commit contains stream.ts + stream.test.ts together.
  </acceptance_criteria>
  <done>/api/agent/stream route GREEN; ready for routes/index.ts wiring in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire 4 new routes into buildAllRoutes + buildApp; add integration test for full registration</name>
  <read_first>
    - apps/api/src/routes/index.ts (current registrar)
    - apps/api/src/index.ts (buildApp owner)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-13 D-19
    - apps/api/src/routes/agent/stream.ts (Task 2)
    - apps/api/src/routes/tokens/{assemblyai,deepgram,openai-realtime}.ts (plans 03/04)
  </read_first>
  <files>apps/api/src/routes/index.ts, apps/api/src/index.ts, apps/api/src/routes/__tests__/registration.test.ts</files>
  <behavior>
    Test 1: after buildApp with full deps including litellm + litellmMasterKey, the Fastify instance has these route declarations: POST /api/streaming-token, POST /api/deepgram-streaming-token, POST /api/openai-realtime-token, POST /api/agent/stream.
    Test 2: the 3 token routes are registered EVEN WHEN litellm is undefined (they don't depend on LiteLLM — they call provider APIs directly per D-13).
    Test 3: /api/agent/stream is registered ONLY when litellm dep is present (it forwards to LiteLLM via deps.litellm.baseUrl).
    Test 4: the existing realtime route at /v1/realtime is still registered (regression — Phase 3 wsUpstream not broken).
  </behavior>
  <action>
RED first: write integration test asserting the 4 routes are present after buildAllRoutes is invoked with appropriate deps.

GREEN: edit apps/api/src/routes/index.ts:

1. Import the 4 new factories: buildAgentStreamRoutes, buildAssemblyAITokenRoutes, buildDeepgramTokenRoutes, buildOpenAIRealtimeTokenRoutes.

2. Extend AllRoutesDeps interface: add agentStream type definition implicitly via existing litellm dep (no new field — agent/stream uses deps.litellm). Add no new fields for token routes (they read process.env directly per D-13/D-18).

3. In the plugins array assembly:
   - ALWAYS push the 3 token routes (independent of litellm presence — they call providers directly).
   - INSIDE the `if (deps.litellm)` block, push buildAgentStreamRoutes({db: deps.db, litellm: deps.litellm}) alongside the existing transcribe/reason/realtime registrations.

4. Re-export the 4 new factories from the index module.

Edit apps/api/src/index.ts buildApp: no changes needed if buildAllRoutes already receives the litellm dep — the 3 token routes don't need new wiring; the agent/stream route uses the same litellm client as transcribe/reason.

REFACTOR: ensure the order in plugins array is: existing routes, then token routes (3), then agent/stream (when litellm present), then existing transcribe/reason/realtime (preserve Phase 3 ordering).
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/__tests__/registration.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 4 registration tests pass.
    - grep -E 'buildAgentStreamRoutes|buildAssemblyAITokenRoutes|buildDeepgramTokenRoutes|buildOpenAIRealtimeTokenRoutes' apps/api/src/routes/index.ts returns 4+ matches each.
    - Test 4 confirms /v1/realtime is still registered (no Phase 3 regression).
    - Coverage on routes/index.ts changes >=90/90/90/90 (entire file gate).
    - Atomic commit.
  </acceptance_criteria>
  <done>All 4 Phase 4 routes registered and integration-test-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop to /api/agent/stream | Authenticated; req.user populated by dual-auth |
| Fastify to LiteLLM upstream | Master key replaces desktop bearer; spend-log metadata header injected |
| upstream SSE frames to NDJSON producer | Untrusted; parser validates JSON.parse-ability per Wave 1 plan 02 mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | sse-parser composition in stream.ts | mitigate | Parser drops malformed frames; tool-call accumulator drops state on finish_reason=stop with partial args (Wave 1 plan 02 mitigation carries forward, exercised here via Test 2 multi-tool-call fixture) |
| T-04-LEAK | Information Disclosure | x-litellm-call-id capture | mitigate | Captured into req.log.info ONLY; never written to reply.raw. Test 7 explicitly verifies the call-id string does not appear anywhere in the wire response body |
| T-04-AUTH | Spoofing | reply.hijack() bypassing global error handler | accept (mitigated by design) | dual-auth hook runs BEFORE the route handler; handler's first action is to defensively re-check req.user.id and throw AuthError if missing (which DOES fire the global error handler since it throws before hijack). Verified by Test 11 |
| T-04-DISCONNECT | DoS | orphaned upstream calls on client disconnect | mitigate | AbortController wired via request.raw.on('close') handler; upstream fetch receives signal. Verified by Test 8 |
</threat_model>

<verification>
- pnpm --filter @openwhispr/api test src/routes/agent/ src/routes/__tests__/registration.test.ts --run --coverage shows GREEN at >=90/90/90/90.
- All atomic commits land production + test together.
- /v1/realtime regression test passes (Phase 3 not broken).
</verification>

<success_criteria>
- WIRE-07 NDJSON line-flush handler GREEN.
- All 4 Phase 4 routes registered.
- T-04-03, T-04-LEAK, T-04-DISCONNECT mitigations test-verified.
</success_criteria>

<output>
After completion, create .planning/phases/04-streaming-realtime/04-06-SUMMARY.md with: per-file coverage axes, the 12 stream.ts test outcomes, registration confirmation grep output.
</output>
