---
phase: 04
plan: 07
type: tdd
wave: 2
depends_on: [04-05]
files_modified:
  - tests/e2e/mock-realtime/server.ts
  - tests/e2e/mock-realtime/server.test.ts
  - tests/e2e/mock-realtime/Dockerfile
  - compose/e2e/docker-compose.e2e.yml
  - apps/api/src/routes/realtime.ts
  - apps/api/src/routes/realtime.test.ts
autonomous: true
requirements: [SCALE-05]
must_haves:
  truths:
    - "tests/e2e/mock-realtime/server.ts implements minimum OpenAI Realtime protocol: emits session.created on connect, response.done on response.create, transparent ping/pong"
    - "Mock-realtime server runs in compose/e2e/docker-compose.e2e.yml under the e2e profile, networked on openwhispr_internal"
    - "apps/api/src/routes/realtime.ts wsClientOptions tightened: handshakeTimeout 10000, wsReconnect false (D-27)"
    - "Phase 3 /v1/realtime auth-bypass + master-key-injection guarantees preserved (regression tests still pass)"
    - "Mock-realtime test suite at >=90/90/90/90"
  artifacts:
    - path: "tests/e2e/mock-realtime/server.ts"
      provides: "Hermetic OpenAI Realtime protocol mock for e2e soak"
      exports: ["startMockRealtimeServer", "type StopHandle"]
    - path: "tests/e2e/mock-realtime/Dockerfile"
      provides: "Container image build for compose e2e profile"
    - path: "compose/e2e/docker-compose.e2e.yml"
      provides: "e2e profile compose overlay declaring mock-realtime service"
    - path: "apps/api/src/routes/realtime.ts"
      provides: "Tightened wsUpstream config with handshakeTimeout + wsReconnect false"
  key_links:
    - from: "compose/e2e/docker-compose.e2e.yml mock-realtime service"
      to: "tests/e2e/mock-realtime/Dockerfile"
      via: "build context"
      pattern: "build:"
    - from: "apps/api/src/routes/realtime.ts wsClientOptions"
      to: "@fastify/http-proxy v11"
      via: "handshakeTimeout + wsReconnect config keys"
      pattern: "handshakeTimeout|wsReconnect"
---

<objective>
TDD plan: implement the hermetic mock-realtime WS echo server in the package skeleton from plan 01. Wire it into a new compose e2e profile overlay. Tighten the existing /v1/realtime wsUpstream config per D-27 (handshakeTimeout 10000, wsReconnect false).

Purpose: Closes SCALE-05 hermetic e2e prerequisite. Wave 3 plan 09 hermetic 5-min soak depends on this. Plan 05 dedicated :8443 entrypoint is required (depends_on: 04-05).
Output: Mock server implementation + Dockerfile + compose overlay + tightened realtime.ts + tests at >=90/90/90/90.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@apps/api/src/routes/realtime.ts
@tests/e2e/mock-realtime/package.json
@tests/e2e/mock-realtime/server.ts
@compose/traefik/dynamic.yml
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tests/e2e/mock-realtime/server.ts (real implementation, replacing plan-01 stub)</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.9 lines 732-775
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-22
    - tests/e2e/mock-realtime/package.json (plan-01)
  </read_first>
  <files>tests/e2e/mock-realtime/server.ts, tests/e2e/mock-realtime/server.test.ts</files>
  <behavior>
    Test 1 (session.created on connect): client connects via ws library; first message received is JSON parse-able with type 'session.created' and session.id starting with 'sess_'.
    Test 2 (response.done on response.create): client sends JSON with type 'response.create'; receives JSON with type 'response.done' and response.id starting with 'resp_'.
    Test 3 (ping/pong forwarding): client sends WebSocket ping frame (raw, not JSON); server's ws library handles pong at protocol layer (verified by ws library auto-pong behavior); RTT measurable.
    Test 4 (multiple sequential request-response cycles): 10 response.create messages each receive a response.done.
    Test 5 (graceful shutdown): startMockRealtimeServer returns a StopHandle with a stop() method; calling stop() closes all open connections cleanly with code 1000.
    Test 6 (port binding): server listens on a configurable port (default 8765); test uses port 0 for ephemeral binding.
  </behavior>
  <action>
RED first.

GREEN: replace tests/e2e/mock-realtime/server.ts (currently a stub from plan-01) with the full implementation per RESEARCH.md §2.9 lines 736-760.

Implementation:
- Use Fastify 5 + @fastify/websocket plugin (already in package.json from plan-01).
- Export startMockRealtimeServer({port}: {port: number}) returning {url, stop}. The url is 'ws://host:port/v1/realtime'; stop closes the Fastify instance.
- Register route GET /v1/realtime with websocket: true. Handler:
  1. On connection (conn callback fires): conn.socket.send(JSON.stringify({type: 'session.created', session: {id: 'sess_' + Date.now(), object: 'realtime.session'}})).
  2. conn.socket.on('message', raw => parse JSON; if msg.type === 'response.create' then send response.done with response.id 'resp_' + Date.now()).
  3. ws library handles ping/pong frames automatically per RFC 6455 — no explicit handler needed.

Default export: a CLI entry point `if (import.meta.url === \`file://\${process.argv[1]}\`)` block calling startMockRealtimeServer({port: Number(process.env.PORT ?? 8765)}).

Tests use the `ws` library (not @fastify/websocket — that's server-side) as the test client.

Coverage >=90/90/90/90.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/mock-realtime test --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; coverage >=90/90/90/90.
    - server.ts implements the OpenAI Realtime minimum protocol (session.created, response.done).
    - Tests run host-side without Docker (ephemeral port binding).
    - Atomic commit.
  </acceptance_criteria>
  <done>Mock-realtime server real implementation GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Dockerfile + compose e2e overlay; integration test boots the profile</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.9 lines 763-775
    - tests/e2e/mock-realtime/server.ts (Task 1)
    - docker-compose.yml (existing services)
    - compose/traefik/dynamic.yml
  </read_first>
  <files>tests/e2e/mock-realtime/Dockerfile, compose/e2e/docker-compose.e2e.yml</files>
  <action>
Dockerfile (multi-stage minimal):
```
FROM node:24-alpine AS build
WORKDIR /app
COPY package.json tsconfig.json ./
RUN npm install --no-audit --no-fund
COPY server.ts ./
RUN npx tsc -p tsconfig.json

FROM node:24-alpine AS runtime
WORKDIR /app
COPY --from=build /app/node_modules ./node_modules
COPY --from=build /app/dist ./dist
ENV PORT=8765
EXPOSE 8765
CMD ["node", "dist/server.js"]
```

compose/e2e/docker-compose.e2e.yml — overlay declaring the mock-realtime service AND any e2e-only LiteLLM config that points at mock-realtime:

```
services:
  mock-realtime:
    profiles: [e2e]
    build:
      context: ../../tests/e2e/mock-realtime
      dockerfile: Dockerfile
    networks: [openwhispr_internal]
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8765',()=>process.exit(0)).on('error',()=>process.exit(1))"]
      interval: 5s
      retries: 12
```

NOTE: this plan does NOT yet wire LiteLLM realtime upstream to mock-realtime — that wiring is a Wave 3 plan 09 concern when the hermetic soak test fixture configures the upstream URL via env override.

Acceptance check:
- docker compose -f docker-compose.yml -f compose/e2e/docker-compose.e2e.yml --profile e2e config returns valid YAML (no errors).
- docker compose --profile e2e up -d mock-realtime starts the container; healthcheck reaches healthy state.

This task does NOT add a TDD test cycle (Dockerfile + YAML are config, not logic). The acceptance is a smoke check.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; docker compose -f docker-compose.yml -f compose/e2e/docker-compose.e2e.yml --profile e2e config &gt;/dev/null 2&gt;&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - docker compose config exits 0.
    - test -f tests/e2e/mock-realtime/Dockerfile.
    - test -f compose/e2e/docker-compose.e2e.yml.
    - The compose overlay defines a 'mock-realtime' service with profile 'e2e' and a healthcheck.
    - docker compose --profile e2e up -d mock-realtime; docker inspect (with --format) shows healthcheck status reaches 'healthy' within 60s.
  </acceptance_criteria>
  <done>Hermetic mock-realtime container builds and runs healthy under the e2e profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten apps/api/src/routes/realtime.ts wsClientOptions per D-27</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-27
    - apps/api/src/routes/realtime.ts (existing implementation)
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.9 paragraph on @fastify/http-proxy v11
  </read_first>
  <files>apps/api/src/routes/realtime.ts, apps/api/src/routes/realtime.test.ts</files>
  <behavior>
    Test 1 (handshakeTimeout 10000): the @fastify/http-proxy register call passes wsClientOptions with handshakeTimeout equal to 10000.
    Test 2 (wsReconnect false): the register call passes wsReconnect equal to false (or omits it explicitly relying on default=false; verified by reading the call args).
    Test 3 (regression: rewriteRequestHeaders preserved): existing buildRewriteRequestHeaders behavior from Phase 3 still works (master-key swap, spend-logs metadata).
    Test 4 (regression: ?user query param injection preserved): pre-handler still sets req.raw.url to include ?user=req.user.id.
    Test 5 (regression: AuthError on missing user): pre-handler still throws AuthError when req.user is absent.
  </behavior>
  <action>
RED first if Phase 3 tests don't already cover the new wsClientOptions keys: extend realtime.test.ts with Tests 1 and 2.

GREEN: edit apps/api/src/routes/realtime.ts buildRealtimeRoutes function. In the app.register(fastifyHttpProxy, {...}) call:
- ADD wsClientOptions key with handshakeTimeout: 10000 (KEEP existing rewriteRequestHeaders inside the same wsClientOptions object).
- ADD wsReconnect: false at the top level of the register options (sibling of wsClientOptions, NOT inside it — per @fastify/http-proxy v11 API).

Phase 3 regression tests must continue passing — DO NOT modify buildRewriteRequestHeaders or the preHandler logic.

Coverage on realtime.ts must remain at the Phase 3 level (>=90/90/90/90).
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/realtime.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass (new + existing Phase 3 regression tests).
    - grep -E 'handshakeTimeout.*10000' apps/api/src/routes/realtime.ts returns at least 1 match.
    - grep -E 'wsReconnect.*false' apps/api/src/routes/realtime.ts returns at least 1 match.
    - Coverage on realtime.ts >=90/90/90/90.
    - Atomic commit.
  </acceptance_criteria>
  <done>realtime.ts wsClientOptions tightened per D-27; Phase 3 contract preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermetic e2e mock-realtime container | Internal docker-bridge only; never exposed externally. Same trust posture as the LiteLLM service container |
| Traefik :8443 to Fastify wsUpstream to mock-realtime | ws:// internal hop (D-26) — externally reachable port (Traefik :8443) is wss:// (TLS-terminated by Traefik) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | DoS | wsClientOptions.handshakeTimeout | mitigate | 10s handshake ceiling prevents stuck-connecting clients from holding ingress slots indefinitely (Task 3 Test 1) |
| T-04-RECONNECT-LOOP | DoS | wsReconnect false | mitigate | Auto-reconnect in proxy hides ingress timeout bugs and creates retry storms; explicitly disabled (Task 3 Test 2) — let the client handle reconnect |
| T-04-MOCK-EXPOSE | Information Disclosure | mock-realtime container | accept | Container runs only under `e2e` profile; never started in production compose-up; docker network is internal-only. No production attack surface |
</threat_model>

<verification>
- pnpm --filter @openwhispr/mock-realtime test --run shows server.ts at >=90/90/90/90.
- pnpm --filter @openwhispr/api test src/routes/realtime.test.ts --run shows wsClientOptions assertions GREEN + Phase 3 regressions GREEN.
- docker compose --profile e2e up -d mock-realtime; healthy within 60s.
</verification>

<success_criteria>
- Mock-realtime hermetic server live and healthy under e2e profile.
- realtime.ts D-27 tightening landed without Phase 3 regression.
- Wave 3 plan 09 can target the mock-realtime container for the hermetic 5-min soak.
</success_criteria>

<output>
After completion, create .planning/phases/04-streaming-realtime/04-07-SUMMARY.md with: server.ts coverage axes, healthcheck timing, realtime.ts before/after wsClientOptions diff.
</output>
