---
phase: 04
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/routes/agent/__fixtures__/text-only.sse
  - apps/api/src/routes/agent/__fixtures__/single-tool-call.sse
  - apps/api/src/routes/agent/__fixtures__/multi-tool-call.sse
  - apps/api/src/routes/agent/__fixtures__/text-then-tool.sse
  - apps/api/src/routes/agent/__fixtures__/premature-close.sse
  - apps/api/src/routes/agent/__fixtures__/malformed-payload.sse
  - apps/api/src/routes/agent/__fixtures__/utf8-split.sse
  - apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json
  - apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json
  - apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json
  - tests/spikes/04-provider-shapes.md
  - apps/api/src/lib/sse-parser.test.ts
  - apps/api/src/lib/tool-call-accumulator.test.ts
  - tests/e2e/mock-realtime/package.json
  - tests/e2e/mock-realtime/tsconfig.json
  - tests/e2e/mock-realtime/server.ts
  - compose/traefik/dynamic.yml
autonomous: true
requirements: [WIRE-07, WIRE-13, WIRE-14, WIRE-15, SCALE-05]
must_haves:
  truths:
    - "≥7 SSE fixtures exist under apps/api/src/routes/agent/__fixtures__/"
    - "3 provider response fixtures exist under apps/api/src/routes/tokens/__fixtures__/"
    - "tests/spikes/04-provider-shapes.md documents AssemblyAI v3 / Deepgram Grant / OpenAI client_secrets shapes from real curl"
    - "Failing test stubs exist for sse-parser and tool-call-accumulator (RED state)"
    - "tests/e2e/mock-realtime/ package skeleton exists with package.json + tsconfig + server.ts stub"
  artifacts:
    - path: "apps/api/src/routes/agent/__fixtures__/text-only.sse"
      provides: "Recorded LiteLLM SSE for text-only stream with [DONE] sentinel"
    - path: "apps/api/src/routes/agent/__fixtures__/single-tool-call.sse"
      provides: "One tool_calls delta with arguments split across ≥3 deltas + finish_reason=tool_calls"
    - path: "apps/api/src/routes/agent/__fixtures__/multi-tool-call.sse"
      provides: "Two tool_calls (index:0 and index:1) interleaved deltas"
    - path: "apps/api/src/routes/agent/__fixtures__/text-then-tool.sse"
      provides: "Text deltas followed by a tool_call delta (LiteLLM#17246 shape)"
    - path: "apps/api/src/routes/agent/__fixtures__/premature-close.sse"
      provides: "Stream ends mid-frame, no [DONE]"
    - path: "apps/api/src/routes/agent/__fixtures__/malformed-payload.sse"
      provides: "One frame contains invalid JSON in data: payload"
    - path: "apps/api/src/routes/agent/__fixtures__/utf8-split.sse"
      provides: "Multi-byte UTF-8 character split across read boundary"
    - path: "tests/spikes/04-provider-shapes.md"
      provides: "Curl-verified AssemblyAI v3 + Deepgram Grant + OpenAI client_secrets request/response examples"
    - path: "tests/e2e/mock-realtime/server.ts"
      provides: "Skeleton stub for hermetic mock-realtime WS echo server (full impl in Wave 2)"
  key_links:
    - from: "apps/api/src/lib/sse-parser.test.ts"
      to: "apps/api/src/routes/agent/__fixtures__/*.sse"
      via: "readFileSync inside it() blocks"
      pattern: "__fixtures__"
    - from: "compose/traefik/dynamic.yml"
      to: "compose/traefik/traefik.yml"
      via: "Traefik file provider watch"
      pattern: "providers.file"
---

<objective>
Capture wave-0 fixtures and test scaffolding so all later waves can do TDD against real recorded provider shapes (no assumed shapes, no mocks of internal logic). Lands the seven LiteLLM SSE fixtures, three provider response fixtures, the mock-realtime package boundary, and the failing test stubs that turn GREEN in Wave 1.

Purpose: Eliminate "I don't know what the upstream actually emits" risk before TDD red→green starts (RESEARCH.md §3 W0).
Output: 7 .sse files, 3 .json provider fixtures, 1 spikes markdown, 2 RED test files, mock-realtime package skeleton, dynamic.yml placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@.planning/phases/04-streaming-realtime/04-VALIDATION.md
@compose/litellm/litellm_config.contract.yaml
@compose/traefik/traefik.yml
@compose/traefik/dynamic.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture LiteLLM SSE fixture corpus (7 files)</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §2.1 (fixture corpus list lines 187-196)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-01, D-09 (SSE→NDJSON producer, tool-call accumulator)
    - compose/litellm/litellm_config.contract.yaml (existing mock-LiteLLM contract profile)
    - /Users/dev/openwhispr/docs/BACKEND_SPEC.md §/api/agent/stream (chunk vocabulary)
  </read_first>
  <files>
    apps/api/src/routes/agent/__fixtures__/text-only.sse,
    apps/api/src/routes/agent/__fixtures__/single-tool-call.sse,
    apps/api/src/routes/agent/__fixtures__/multi-tool-call.sse,
    apps/api/src/routes/agent/__fixtures__/text-then-tool.sse,
    apps/api/src/routes/agent/__fixtures__/premature-close.sse,
    apps/api/src/routes/agent/__fixtures__/malformed-payload.sse,
    apps/api/src/routes/agent/__fixtures__/utf8-split.sse,
    tools/spike/capture-sse-fixtures.sh
  </files>
  <action>
Create capture script `tools/spike/capture-sse-fixtures.sh` that:
1. Boots `compose/litellm/litellm_config.contract.yaml` profile via `docker compose -f docker-compose.yml -f compose/litellm/contract-overlay.yml --profile contract up -d litellm` (or equivalent profile name from Phase 3 D-05).
2. For each fixture name, sends a `POST http://localhost:4000/v1/chat/completions` curl with appropriate `mock_response` selector and `stream: true`, redirecting raw stdout into `apps/api/src/routes/agent/__fixtures__/<name>.sse`.

Fixture content requirements (per RESEARCH.md §2.1, lines 187-196):
- `text-only.sse`: LiteLLM `mock_response: "Hello world this is a test response"` with `stream:true`. MUST contain ≥3 `data: {"choices":[{"delta":{"content":"..."}}]}` frames + final `data: {"choices":[{"delta":{},"finish_reason":"stop"}],"usage":{"prompt_tokens":N,"completion_tokens":N}}` + `data: [DONE]\n\n` sentinel.
- `single-tool-call.sse`: hand-authored if mock_response cannot emit tool_calls — write OpenAI streaming format with one `tool_calls[0].function.arguments` string fragment split across 5 deltas, terminating with `finish_reason: "tool_calls"`.
- `multi-tool-call.sse`: two tool_calls keyed by `index:0` and `index:1` with interleaved arguments deltas.
- `text-then-tool.sse`: text content deltas followed by a tool_calls delta (LiteLLM#17246 shape).
- `premature-close.sse`: ends mid-frame after one `data: {"choices":[{"delta":{"content":"partial`  (no closing brace, no \n\n, no [DONE]).
- `malformed-payload.sse`: one frame `data: {invalid json here}\n\n` between two valid frames.
- `utf8-split.sse`: contains "🎉" (4-byte emoji) where the byte boundary falls inside the codepoint — produced by writing the file with the emoji split across what will be two `read()` calls in tests (mark a known byte offset as the split point in a comment header line `: split-at-byte-N`).

If LiteLLM mock_response cannot produce all shapes, hand-author the SSE files following the OpenAI streaming spec verbatim — the goal is realistic recorded shapes, not necessarily live captures. Document the source of each fixture (live capture vs hand-authored) in a header comment line `# source: live|hand-authored — captured 2026-05-XX`.
  </action>
  <verify>
    <automated>ls apps/api/src/routes/agent/__fixtures__/*.sse | wc -l | grep -q '^\s*7$' && grep -l 'data:' apps/api/src/routes/agent/__fixtures__/*.sse | wc -l | grep -q '^\s*7$'</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/api/src/routes/agent/__fixtures__/*.sse` returns exactly 7 files (text-only, single-tool-call, multi-tool-call, text-then-tool, premature-close, malformed-payload, utf8-split).
    - Every fixture contains at least one `data: ` line.
    - `text-only.sse` contains `data: [DONE]` and a `usage` field with numeric `prompt_tokens` and `completion_tokens`.
    - `single-tool-call.sse` contains `"finish_reason":"tool_calls"` and at least 3 separate `data: ` frames carrying `tool_calls[0].function.arguments`.
    - `multi-tool-call.sse` contains both `"index":0` and `"index":1` in separate `data: ` frames.
    - `premature-close.sse` does NOT contain `[DONE]` and does NOT end with `\n\n`.
    - `malformed-payload.sse` contains a `data: ` line whose payload fails `JSON.parse` (verifiable via `node -e "console.log(JSON.parse('...'))"` returning non-zero exit).
    - `utf8-split.sse` contains the bytes `0xF0 0x9F 0x8E 0x89` (verifiable via `xxd apps/api/src/routes/agent/__fixtures__/utf8-split.sse | grep 'f09f 8e89'`).
    - Each file has a leading `# source: ...` header comment line.
  </acceptance_criteria>
  <done>All 7 SSE fixtures committed under apps/api/src/routes/agent/__fixtures__/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify provider response shapes via curl + record fixtures + spike doc</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §2.5 (provider call patterns lines 441-575)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-14, D-15, D-16 (AssemblyAI v3 / Deepgram / OpenAI Realtime exact shapes)
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §5 W0-S2/W0-S3 (open implementation questions)
    - /Users/dev/openwhispr/docs/BACKEND_SPEC.md §/api/streaming-token, §/api/deepgram-streaming-token, §/api/openai-realtime-token
  </read_first>
  <files>
    apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json,
    apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json,
    apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json,
    tests/spikes/04-provider-shapes.md
  </files>
  <action>
Run three real curl calls (operator must export `ASSEMBLYAI_API_KEY`, `DEEPGRAM_API_KEY`, `OPENAI_API_KEY` from .env or shell — these are personal keys; if any is unavailable the spike falls back to the documented shape with a `# unverified` marker in the spike doc):

1. AssemblyAI v3:
   ```
   curl -s "https://streaming.assemblyai.com/v3/token?expires_in_seconds=60" \
     -H "Authorization: ${ASSEMBLYAI_API_KEY}" \
     > apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json
   ```
   Expected shape per D-14: `{"token":"<base64-ish>"}`. Confirm the field is exactly `token` (not `access_token`).

2. Deepgram Grant Token:
   ```
   curl -s -X POST "https://api.deepgram.com/v1/auth/grant" \
     -H "Authorization: Token ${DEEPGRAM_API_KEY}" \
     -H "Content-Type: application/json" \
     -d '{"ttl_seconds":30}' \
     > apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json
   ```
   Expected per D-15: `{"access_token":"...","expires_in":30}`. Confirm field name is `access_token`.

3. OpenAI Realtime client_secrets:
   ```
   curl -s -X POST "https://api.openai.com/v1/realtime/client_secrets" \
     -H "Authorization: Bearer ${OPENAI_API_KEY}" \
     -H "Content-Type: application/json" \
     -d '{"session":{"type":"realtime","model":"gpt-realtime"}}' \
     > apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json
   ```
   Expected per D-16: `{"value":"ek_...","session":{...}}`. Confirm field name is `value` (NOT `client_secret`).

Then write `tests/spikes/04-provider-shapes.md` with three sections (one per provider). Each section MUST include:
- Request method + URL + headers + body
- Response JSON (sanitized — token values replaced with `<REDACTED>` strings of identical length)
- Confirmed field name for the token (token / access_token / value)
- Observed TTL in seconds
- Date verified
- A `Verified: yes|no (fallback to documented shape)` line

If any provider key is unavailable, write the documented-shape JSON manually into the fixture file and mark `Verified: no` in the spike doc. Future contributors must re-run the spike when keys become available; CI does not gate on this fixture.
  </action>
  <verify>
    <automated>test -f apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json && test -f apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json && test -f apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json && test -f tests/spikes/04-provider-shapes.md && grep -q 'AssemblyAI' tests/spikes/04-provider-shapes.md && grep -q 'Deepgram' tests/spikes/04-provider-shapes.md && grep -q 'OpenAI' tests/spikes/04-provider-shapes.md && jq -e '.token' apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json && jq -e '.access_token' apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json && jq -e '.value' apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixture JSON files exist and are valid JSON (parseable by `jq`).
    - AssemblyAI fixture has top-level `token` key (string).
    - Deepgram fixture has top-level `access_token` key (string).
    - OpenAI fixture has top-level `value` key (string starting with `ek_` or `<REDACTED-ek_...>` pattern).
    - `tests/spikes/04-provider-shapes.md` mentions all three providers and contains explicit `Verified:` lines.
    - No real API key string appears in any committed file (grep `-rE 'sk-[A-Za-z0-9]{20,}'` returns nothing in fixture files).
  </acceptance_criteria>
  <done>3 provider fixtures + spike doc committed; field names confirmed (or fallback marked).</done>
</task>

<task type="auto">
  <name>Task 3: Land mock-realtime package skeleton + RED test stubs + dynamic.yml placeholder</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md §2.9 (mock-realtime server lines 732-775)
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-22 (hermetic mock-realtime)
    - .planning/phases/04-streaming-realtime/04-VALIDATION.md Wave 0 Requirements (lines 65-73)
    - apps/api/src/routes/realtime.ts (existing wsUpstream — read for context)
    - compose/traefik/dynamic.yml (existing dynamic config)
  </read_first>
  <files>
    tests/e2e/mock-realtime/package.json,
    tests/e2e/mock-realtime/tsconfig.json,
    tests/e2e/mock-realtime/server.ts,
    apps/api/src/lib/sse-parser.test.ts,
    apps/api/src/lib/tool-call-accumulator.test.ts,
    compose/traefik/dynamic.yml
  </files>
  <action>
**Sub-task 3a — mock-realtime package skeleton:**
Create `tests/e2e/mock-realtime/package.json` with:
```json
{
  "name": "@openwhispr/mock-realtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/websocket": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
```

`tests/e2e/mock-realtime/tsconfig.json` — extend the workspace base tsconfig with `outDir: dist`, `rootDir: .`, `module: NodeNext`, `target: ES2022`.

`tests/e2e/mock-realtime/server.ts` — STUB only (real impl is Wave 2 plan 06). Throw `Error("not implemented — Wave 2")` from the default export. The skeleton's purpose is to make the package importable so Wave 1 / Wave 3 tests can reference it without resolution errors.

Run `pnpm install` at workspace root after writing the package so pnpm-workspace.yaml picks it up (verify `tests/e2e/*` glob is in pnpm-workspace.yaml; if not, add `tests/e2e/mock-realtime` explicitly).

**Sub-task 3b — RED test stubs (TDD red state):**
`apps/api/src/lib/sse-parser.test.ts` — references all 7 fixtures. Each `it()` MUST exist and MUST currently fail because `sse-parser.ts` does not exist. Use:
```ts
import { describe, it, expect } from "vitest";
import { sseToNdjson } from "./sse-parser.js"; // Wave 1 will create this — currently fails import
import { readFileSync } from "node:fs";
import { Readable } from "node:stream";

const FIXTURES = ["text-only", "single-tool-call", "multi-tool-call", "text-then-tool", "premature-close", "malformed-payload", "utf8-split"] as const;

describe("sseToNdjson", () => {
  for (const name of FIXTURES) {
    it(`handles ${name} fixture`, async () => {
      const raw = readFileSync(`apps/api/src/routes/agent/__fixtures__/${name}.sse`);
      const stream = Readable.toWeb(Readable.from([raw]));
      const acc = { absorb: () => {}, flush: () => [], hasPending: () => false };
      const out: unknown[] = [];
      for await (const chunk of sseToNdjson({ body: stream as ReadableStream<Uint8Array>, acc })) out.push(chunk);
      // Wave 1 fills assertion. Initial RED: just expect length >= 0 to make it compile/red on import-fail.
      expect(out.length).toBeGreaterThanOrEqual(0);
    });
  }
});
```

`apps/api/src/lib/tool-call-accumulator.test.ts` — RED stubs for: single tool, multi tool, malformed JSON args, missing-name silent skip, finish_reason==="stop" with partial accumulator (hasPending=true, flush returns []).

Both files compile to RED because the production modules don't exist yet — the import fails, vitest reports red. Acceptance: `pnpm --filter @openwhispr/api test src/lib/sse-parser.test.ts --run` exits NON-zero.

**Sub-task 3c — dynamic.yml placeholder:**
If `compose/traefik/dynamic.yml` does NOT contain a `# Phase 04 placeholder` marker comment, append a one-line marker comment at end of file:
```
# Phase 04 placeholder — Wave 1 plan 04 will add `websecure-realtime` entrypoint binding for /v1/realtime router.
```
This task does NOT yet move the api-realtime router; that's Wave 1.
  </action>
  <verify>
    <automated>test -f tests/e2e/mock-realtime/package.json && test -f tests/e2e/mock-realtime/server.ts && test -f apps/api/src/lib/sse-parser.test.ts && test -f apps/api/src/lib/tool-call-accumulator.test.ts && grep -q 'Phase 04 placeholder' compose/traefik/dynamic.yml && (cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test src/lib/sse-parser.test.ts --run 2>&1 | grep -qE '(FAIL|failed|Cannot find module)')</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/mock-realtime/package.json` parses as valid JSON and has `name: "@openwhispr/mock-realtime"`.
    - `tests/e2e/mock-realtime/server.ts` exists and throws `not implemented` when invoked.
    - `apps/api/src/lib/sse-parser.test.ts` references all 7 fixture names.
    - `apps/api/src/lib/tool-call-accumulator.test.ts` exists with at least 5 `it()` blocks (single, multi, malformed, missing-name, finish-stop-with-pending).
    - Both test files FAIL when run (RED state) because `./sse-parser.js` and `./tool-call-accumulator.js` do not yet exist.
    - `compose/traefik/dynamic.yml` contains the string `Phase 04 placeholder`.
  </acceptance_criteria>
  <done>Package skeleton, RED test stubs, and dynamic.yml marker committed. Wave 1 can now turn the tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → committed fixture files | Operator-supplied API keys could leak into fixture files via curl output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Information Disclosure | Provider response fixtures | mitigate | Task 2 sanitizes captured token values to `<REDACTED>` strings before commit; acceptance criterion greps for `sk-[A-Za-z0-9]{20,}` returning nothing |
| T-04-01 (deferred) | Information Disclosure | Token-mint endpoints | mitigate (Wave 1+) | This wave only captures shapes; Wave 1 plans 02/03/05 mitigate the runtime threat (rate limit + missing-key 503) |
</threat_model>

<verification>
- `ls apps/api/src/routes/agent/__fixtures__/*.sse | wc -l` returns 7
- `ls apps/api/src/routes/tokens/__fixtures__/*.json | wc -l` returns 3
- `test -f tests/spikes/04-provider-shapes.md`
- `pnpm --filter @openwhispr/api test src/lib/sse-parser.test.ts --run` exits non-zero (RED state expected)
- `grep -rE 'sk-[A-Za-z0-9]{20,}|asm_[A-Za-z0-9]{20,}' apps/api/src/routes/tokens/__fixtures__/` returns no matches
</verification>

<success_criteria>
- 7 SSE fixtures + 3 provider JSON fixtures + 1 spike doc + 1 mock-realtime package skeleton + 2 RED test files all committed
- No real API keys in any fixture
- RED test state confirmed (Wave 1 will turn GREEN)
- pnpm-workspace.yaml resolves the new package
</success_criteria>

<output>
After completion, create `.planning/phases/04-streaming-realtime/04-01-SUMMARY.md` with: fixture inventory, source per fixture (live vs hand-authored), provider verification status (yes/no per provider), RED-test confirmation command output.
</output>
