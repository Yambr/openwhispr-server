---
phase: 04
plan: 03
type: tdd
wave: 1
depends_on: [04-01]
files_modified:
  - apps/api/src/routes/tokens/_call-provider.ts
  - apps/api/src/routes/tokens/_call-provider.test.ts
  - apps/api/src/routes/tokens/assemblyai.ts
  - apps/api/src/routes/tokens/assemblyai.test.ts
  - apps/api/src/routes/tokens/deepgram.ts
  - apps/api/src/routes/tokens/deepgram.test.ts
  - .env.example
autonomous: true
requirements: [WIRE-13, WIRE-14]
must_haves:
  truths:
    - "POST /api/streaming-token returns 200 with body shape token-string when ASSEMBLYAI_API_KEY set"
    - "POST /api/streaming-token returns 503 with exact error string AssemblyAI not configured (set ASSEMBLYAI_API_KEY in .env) when env unset"
    - "POST /api/deepgram-streaming-token returns 200 token-string mapped from upstream access_token field"
    - "POST /api/deepgram-streaming-token returns 503 envelope with DEEPGRAM_API_KEY in message when env unset"
    - "Per-user 30/min rate limit applied via @fastify/rate-limit keyGenerator on req.user.id"
    - "Provider call timeouts: 3s connect, 5s total — timeout maps to 503"
    - "All 3 modules at >=90/90/90/90 coverage; only undici MockAgent used (process boundary)"
  artifacts:
    - path: "apps/api/src/routes/tokens/_call-provider.ts"
      provides: "Shared undici fetch helper with timeout + status mapping"
      exports: ["callProvider"]
    - path: "apps/api/src/routes/tokens/assemblyai.ts"
      provides: "POST /api/streaming-token route plugin (factory pattern)"
      exports: ["buildAssemblyAITokenRoutes"]
    - path: "apps/api/src/routes/tokens/deepgram.ts"
      provides: "POST /api/deepgram-streaming-token route plugin"
      exports: ["buildDeepgramTokenRoutes"]
    - path: ".env.example"
      provides: "ASSEMBLYAI_API_KEY and DEEPGRAM_API_KEY documented"
      contains: "ASSEMBLYAI_API_KEY="
  key_links:
    - from: "apps/api/src/routes/tokens/assemblyai.ts"
      to: "apps/api/src/routes/tokens/_call-provider.ts"
      via: "callProvider helper"
      pattern: "callProvider"
    - from: "apps/api/src/routes/tokens/assemblyai.ts"
      to: "apps/api/src/plugins/rate-limit.ts"
      via: "config.rateLimit.keyGenerator override on req.user.id"
      pattern: "keyGenerator"
---

<objective>
TDD plan: implement AssemblyAI v3 and Deepgram Grant-Token mint routes with the shared undici helper. Both routes inherit Phase 2 dual-auth + global error envelope. Per-user 30/min rate limit (D-19) applied via @fastify/rate-limit per-route override on req.user.id.

Purpose: Closes WIRE-13 + WIRE-14. Mitigates T-04-01 (token-mint key leakage) via missing-key 503 + per-user rate limit + provider 5s timeout. Mitigates T-04-04 (cross-user rate-limit bypass) by binding keyGenerator to req.user.id with the dual-auth hook running first.
Output: 3 production modules + 3 test suites at >=90/90/90/90, .env.example updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-CONTEXT.md
@apps/api/src/plugins/rate-limit.ts
@apps/api/src/routes/index.ts
@apps/api/src/routes/transcribe.ts
@apps/api/src/error-handler.ts
@apps/api/src/middleware/dual-auth.ts
@apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json
@apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json
@.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _call-provider.ts shared helper with full error mapping</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.5 lines 441-484
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-18 D-20
  </read_first>
  <files>apps/api/src/routes/tokens/_call-provider.ts, apps/api/src/routes/tokens/_call-provider.test.ts</files>
  <behavior>
    Test 1 (200 path): undici MockAgent intercepts a 200 response with valid JSON; helper returns ok-true with parsed json.
    Test 2 (401 to 503-not-configured): provider returns 401; helper returns ok-false with status 503 and message containing the env var name.
    Test 3 (403 to 503-not-configured): same envelope as 401.
    Test 4 (429 to 503-upstream-error): provider returns 429; message contains 'token mint upstream error'.
    Test 5 (500 to 503-upstream-error): same as 429.
    Test 6 (timeout to 503-timed-out): MockAgent delays response > 5000ms; helper aborts, message contains 'token mint timed out'.
    Test 7 (malformed JSON body to 503-malformed): provider returns 200 with body 'not json'; helper returns 503 with message containing 'malformed response'.
    Test 8 (connect timeout 3s): MockAgent simulates connect failure within 3s; helper returns timed-out 503.
  </behavior>
  <action>
RED first: write 8 tests in _call-provider.test.ts using undici MockAgent + setGlobalDispatcher. Use vi.useFakeTimers() + vi.advanceTimersByTime(5001) for timeout tests.

GREEN: write _call-provider.ts per RESEARCH.md section 2.5 lines 446-483. Adjustments:
- Use 'import { Agent, fetch } from undici' (Node 24 ships undici as global; explicit import lets tests inject MockAgent).
- Singleton 'providerAgent' with connect.timeout 3000.
- Total timeout: AbortController + setTimeout(5000) with clearTimeout in finally.
- Status mapping order: 401/403 to not-configured, 429 or status>=500 to upstream-error, JSON parse failure to malformed, AbortError to timed-out.
- All 503 messages formatted as providerLabel + ' ' + suffix where suffix is one of: 'not configured (set ENV in .env)', 'token mint upstream error', 'token mint timed out', 'token mint malformed response'.

REFACTOR: extract status-code-to-message switch into a sibling private function if branches feel duplicated. Coverage target >=90/90/90/90.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/tokens/_call-provider.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass.
    - Coverage report shows _call-provider.ts at >=90% lines, branches, functions, statements.
    - Helper returns the literal substrings 'not configured (set', 'token mint upstream error', 'token mint timed out', 'token mint malformed response' verifiable via grep on test file.
    - No internal logic mocked; only undici MockAgent (process boundary, allowed by CLAUDE.md).
    - git log -1 --name-only shows production + test file in the same commit (TDD constitutional).
  </acceptance_criteria>
  <done>callProvider helper landed at >=90/90/90/90; AssemblyAI and Deepgram routes can compose it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /api/streaming-token (AssemblyAI v3) route + .env.example</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.5 AssemblyAI block lines 488-503
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-14 D-18 D-19 D-20
    - apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json
    - apps/api/src/routes/transcribe.ts (route factory pattern reference)
  </read_first>
  <files>apps/api/src/routes/tokens/assemblyai.ts, apps/api/src/routes/tokens/assemblyai.test.ts, .env.example</files>
  <behavior>
    Test 1 (success): with ASSEMBLYAI_API_KEY=test, MockAgent serves the fixture JSON from streaming.assemblyai.com/v3/token; route returns 200 with body containing the token string.
    Test 2 (missing-key 503): with ASSEMBLYAI_API_KEY unset, route returns 503 with body exactly equal to the JSON object having key 'error' and value 'AssemblyAI not configured (set ASSEMBLYAI_API_KEY in .env)'.
    Test 3 (unauthenticated 401): no bearer/cookie, dual-auth hook returns 401 BEFORE handler fires; rate-limit bucket NOT consumed (T-04-04 mitigation verified by checking bucket count via the in-memory rate-limit store).
    Test 4 (provider 401 to 503): MockAgent returns 401 from upstream; route returns 503 with not-configured envelope.
    Test 5 (TTL env override): ASSEMBLYAI_TOKEN_TTL=120 makes the URL query string contain 'expires_in_seconds=120' (verified via MockAgent intercept inspector).
    Test 6 (per-user rate-limit): same authenticated user issues 31 requests within a minute; the 31st returns 429 with global error envelope. Two different userIds at 30 each both succeed (key-isolation proves T-04-04 mitigation).
    Test 7 (malformed provider response to 503): MockAgent returns 200 with body containing key 'not_token'; route returns 503 with message containing 'malformed response'.
  </behavior>
  <action>
RED first. Tests use Fastify + fastify-plugin to bootstrap a minimal app with the dual-auth hook (Phase 2 D-04), the rate-limit plugin (Phase 2 D-28), and the AssemblyAI route registered. Use undici MockAgent for provider calls. Inject 'request.user = id u1' via a test-only onRequest hook simulating dual-auth populated session. For rate-limit test use the existing in-memory mode of @fastify/rate-limit (plugins/rate-limit.ts already supports it).

GREEN: write apps/api/src/routes/tokens/assemblyai.ts exporting buildAssemblyAITokenRoutes (no-arg factory matching Phase 2 pattern). The factory returns an async Fastify plugin that:

1. Defines preHandler 'requireAssemblyAIConfigured': if process.env.ASSEMBLYAI_API_KEY is unset, reply.code(503).send with body having key 'error' and value 'AssemblyAI not configured (set ASSEMBLYAI_API_KEY in .env)' and DO NOT call done.
2. Registers app.post on path '/api/streaming-token' with config.rateLimit having max 30, timeWindow '1 minute', keyGenerator returning req.user?.id ?? req.ip; preHandler array contains requireAssemblyAIConfigured.
3. Handler reads ttl = Number(process.env.ASSEMBLYAI_TOKEN_TTL ?? 60) and calls callProvider with url 'https://streaming.assemblyai.com/v3/token?expires_in_seconds=' + ttl, method GET, headers having key 'authorization' value process.env.ASSEMBLYAI_API_KEY (NO 'Bearer' prefix per D-14), envVarName 'ASSEMBLYAI_API_KEY', providerLabel 'AssemblyAI'.
4. On ok-false: reply.code(r.status).send having key 'error' value r.message.
5. On ok-true: extract 'token' field; if not a string then reply.code(503).send error 'AssemblyAI token mint malformed response'; else reply.send body having key 'token' value the string.

NOTE: Add code comment explaining the no-Bearer-prefix is intentional per AssemblyAI v3 API spec.

Update .env.example by appending three new lines (preserve existing content):
- A comment line: '# Phase 4 - Streaming + Realtime (WIRE-13)'
- A line: 'ASSEMBLYAI_API_KEY='
- A line: 'ASSEMBLYAI_TOKEN_TTL=60'

REFACTOR: keep route module under 60 LoC; coverage target >=90/90/90/90.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/tokens/assemblyai.test.ts --run --coverage &amp;&amp; grep -q '^ASSEMBLYAI_API_KEY=' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests pass; coverage on assemblyai.ts >=90/90/90/90.
    - The file contains the literal Authorization-header line WITHOUT 'Bearer' prefix (verifiable via grep -n 'authorization' on the file showing only process.env reference, no 'Bearer ').
    - .env.example contains a line starting with 'ASSEMBLYAI_API_KEY=' and a line starting with 'ASSEMBLYAI_TOKEN_TTL='.
    - Test 6 explicitly creates two userIds and verifies isolation.
    - Test 3 verifies that an unauthenticated request returns 401 AND does NOT consume the rate-limit bucket (assertion on bucket count).
    - git log -1 --name-only shows assemblyai.ts + assemblyai.test.ts + .env.example in the same commit.
  </acceptance_criteria>
  <done>AssemblyAI streaming-token route GREEN at >=90/90/90/90, .env.example updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement /api/deepgram-streaming-token (Deepgram Grant Token) route</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.5 Deepgram block lines 505-524
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-15 D-18 D-19 D-20
    - apps/api/src/routes/tokens/__fixtures__/deepgram-grant-token-response.json
    - apps/api/src/routes/tokens/assemblyai.ts (Task 2 just landed; mirror the pattern)
  </read_first>
  <files>apps/api/src/routes/tokens/deepgram.ts, apps/api/src/routes/tokens/deepgram.test.ts, .env.example</files>
  <behavior>
    Test 1 (success): with DEEPGRAM_API_KEY=test, MockAgent serves fixture JSON from api.deepgram.com/v1/auth/grant; route returns 200 with body having key 'token' value mapped from upstream's 'access_token' field.
    Test 2 (missing-key 503): DEEPGRAM_API_KEY unset, route returns 503 with body error 'Deepgram not configured (set DEEPGRAM_API_KEY in .env)'.
    Test 3 (TTL env override): DEEPGRAM_TOKEN_TTL=60 sends body JSON with key ttl_seconds value 60.
    Test 4 (per-user rate-limit 30/min): same isolation properties as AssemblyAI test 6.
    Test 5 (malformed response): upstream returns 200 with body having key 'not_access_token'; route returns 503 'Deepgram token mint malformed response'.
    Test 6 (unauthenticated 401): no auth, dual-auth returns 401 before handler.
  </behavior>
  <action>
RED first. Test setup mirrors Task 2 with MockAgent intercepting api.deepgram.com.

GREEN: write deepgram.ts factory. Differences from Task 2:
- Path: '/api/deepgram-streaming-token'.
- Method: POST (not GET).
- URL: 'https://api.deepgram.com/v1/auth/grant'.
- Headers: 'authorization' value 'Token ' + process.env.DEEPGRAM_API_KEY (Token prefix per D-15, NOT Bearer).
- Headers also include 'content-type' value 'application/json'.
- Body: JSON.stringify having key ttl_seconds value Number(process.env.DEEPGRAM_TOKEN_TTL ?? 30).
- On ok-true: extract upstream access_token; if not string then 503 malformed; else reply.send body with key 'token' value access_token. NOTE the field-rename from upstream access_token to wire token per BACKEND_SPEC.
- envVarName 'DEEPGRAM_API_KEY', providerLabel 'Deepgram'.

Update .env.example appending:
- Comment '# Phase 4 - Deepgram (WIRE-14)'
- Line 'DEEPGRAM_API_KEY='
- Line 'DEEPGRAM_TOKEN_TTL=30'

REFACTOR: keep under 60 LoC; coverage >=90/90/90/90.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/tokens/deepgram.test.ts --run --coverage &amp;&amp; grep -q '^DEEPGRAM_API_KEY=' .env.example</automated>
  </verify>
  <acceptance_criteria>
    - All 6 tests pass; coverage on deepgram.ts >=90/90/90/90.
    - File uses 'Token ' prefix in Authorization header (verifiable via grep showing the literal 'Token ').
    - Wire response has key 'token' (NOT 'access_token') — verified by Test 1 assertion.
    - .env.example contains DEEPGRAM_API_KEY and DEEPGRAM_TOKEN_TTL lines.
    - git log -1 --name-only shows deepgram.ts + deepgram.test.ts + .env.example in same commit.
  </acceptance_criteria>
  <done>Deepgram streaming-token route GREEN; field-rename from access_token to token verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop client to /api/streaming-token | Authenticated bearer required; req.user.id populated by dual-auth |
| Fastify to AssemblyAI/Deepgram | Server-held API key never leaves the server; only ephemeral mints (60s/30s TTL) cross to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tokens/assemblyai.ts, tokens/deepgram.ts | mitigate | Per-user 30/min rate limit (Task 2 Test 6, Task 3 Test 4); provider 5s timeout in _call-provider; missing-key 503 in preHandler. Master keys never appear in any response body |
| T-04-04 | Spoofing | rate-limit keyGenerator | mitigate | dual-auth hook runs BEFORE rate-limit (Phase 2 D-04); unauthenticated returns 401 BEFORE bucket consumption (Task 2 Test 3, Task 3 Test 6). keyGenerator reads req.user.id (set by dual-auth); falls back to req.ip only as defense-in-depth |
| T-04-01-MALFORMED | Tampering | _call-provider.ts | mitigate | Helper validates upstream response is valid JSON with the expected token field; malformed responses surface as 503 not 200-with-empty-token (Task 2 Test 7, Task 3 Test 5) |
</threat_model>

<verification>
- pnpm --filter @openwhispr/api test src/routes/tokens/ --run --coverage shows all 3 files at >=90/90/90/90.
- All tests use undici MockAgent only (process boundary); no internal logic mocked.
- .env.example has both ASSEMBLYAI_API_KEY and DEEPGRAM_API_KEY lines.
- Each task lands as its own atomic commit with both production + test files.
</verification>

<success_criteria>
- WIRE-13 (AssemblyAI mint with gating) GREEN.
- WIRE-14 (Deepgram mint with gating + field rename) GREEN.
- T-04-01 + T-04-04 mitigations test-verified.
</success_criteria>

<output>
After completion, create .planning/phases/04-streaming-realtime/04-03-SUMMARY.md with: per-file coverage axes, missing-key 503 message exact strings, and atomic-commit-per-task confirmation.
</output>
