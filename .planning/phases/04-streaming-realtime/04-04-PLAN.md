---
phase: 04
plan: 04
type: tdd
wave: 2
depends_on: [04-01, 04-03]
files_modified:
  - apps/api/src/routes/tokens/openai-realtime.ts
  - apps/api/src/routes/tokens/openai-realtime.test.ts
autonomous: true
requirements: [WIRE-15]
must_haves:
  truths:
    - "POST /api/openai-realtime-token with body streams=2 returns 200 with clientSecrets array of length >=2"
    - "POST /api/openai-realtime-token with body streams=1 returns 200 with clientSecret string AND clientSecrets array of length 1 (shape consistency)"
    - "Promise.all fail-fast: if one of two parallel mints fails, the whole request returns 503 (no partial success leakage)"
    - "Missing OPENAI_API_KEY returns 503 with exact error 'OpenAI Realtime not configured (set OPENAI_API_KEY in .env)'"
    - "streams value not in {1,2} returns 400 with global error envelope"
    - "Per-user 30/min rate limit applied"
    - "Coverage >=90/90/90/90 on openai-realtime.ts"
  artifacts:
    - path: "apps/api/src/routes/tokens/openai-realtime.ts"
      provides: "POST /api/openai-realtime-token route plugin with parallel-mint logic"
      exports: ["buildOpenAIRealtimeTokenRoutes"]
  key_links:
    - from: "apps/api/src/routes/tokens/openai-realtime.ts"
      to: "apps/api/src/routes/tokens/_call-provider.ts"
      via: "callProvider helper invoked twice in Promise.all when streams=2"
      pattern: "Promise.all"
---

<objective>
TDD plan: implement /api/openai-realtime-token with parallel-mint semantics for streams=2 (D-17). Two parallel POST calls to OpenAI client_secrets via Promise.all (fail-fast). The wire response shape mirrors BACKEND_SPEC byte-for-byte: top-level clientSecret (singular, the first secret) AND clientSecrets array (always populated, length 1 or 2).

Purpose: Closes WIRE-15. Mitigates T-04-01 (key leakage) via missing-key 503 + per-user rate limit. Reuses _call-provider helper from plan 03 (depends_on: [04-01, 04-03] — Wave 2; 04-03 lands first in Wave 1 with the shared helper).
Output: 1 production module + 1 test suite at >=90/90/90/90.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-streaming-realtime/04-CONTEXT.md
@.planning/phases/04-streaming-realtime/04-RESEARCH.md
@apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json
@apps/api/src/plugins/rate-limit.ts
@apps/api/src/routes/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wait for _call-provider.ts (plan 03 task 1) then implement openai-realtime route</name>
  <read_first>
    - .planning/phases/04-streaming-realtime/04-RESEARCH.md section 2.5 OpenAI Realtime block lines 526-554
    - .planning/phases/04-streaming-realtime/04-CONTEXT.md D-16 D-17 D-18 D-19 D-20
    - apps/api/src/routes/tokens/_call-provider.ts (must exist; if missing, fail loud)
    - apps/api/src/routes/tokens/__fixtures__/openai-client-secret-response.json
    - /Users/dev/openwhispr/docs/BACKEND_SPEC.md section /api/openai-realtime-token lines 534-572
  </read_first>
  <files>apps/api/src/routes/tokens/openai-realtime.ts, apps/api/src/routes/tokens/openai-realtime.test.ts</files>
  <behavior>
    Test 1 (streams=1 success): with OPENAI_API_KEY=test, MockAgent serves one fixture response; route returns 200 with body having key clientSecret value 'ek_xxx' AND key clientSecrets array of length 1 with same value.
    Test 2 (streams=2 success): MockAgent serves TWO different responses (ek_aaa and ek_bbb) on two intercepted POST calls; route returns 200 with clientSecret='ek_aaa' (first) AND clientSecrets=['ek_aaa','ek_bbb']. Both calls happen in parallel (verified by intercept timing being within 50ms of each other).
    Test 3 (streams not 1 or 2 to 400): body streams=3 returns 400 with global error envelope having key 'error' value containing 'streams must be 1 or 2'.
    Test 4 (streams=2 fail-fast): MockAgent serves 200 on first intercept and 500 on second; route returns 503 with message containing 'token mint upstream error'. The first secret is NOT leaked to the wire response.
    Test 5 (missing OPENAI_API_KEY to 503): preHandler returns 503 with body error 'OpenAI Realtime not configured (set OPENAI_API_KEY in .env)'.
    Test 6 (per-user rate limit 30/min): two userIds isolated; 31st request from same user returns 429.
    Test 7 (model env override): body 'model'='gpt-realtime-2025' is forwarded into the upstream POST body session.model field (verified via MockAgent intercept inspector); when omitted, defaults to 'gpt-realtime'.
    Test 8 (malformed upstream): MockAgent returns 200 with body NOT having 'value' field; route returns 503 with message containing 'malformed response'.
    Test 9 (unauthenticated 401): no auth, dual-auth returns 401 before handler; rate-limit not consumed.
  </behavior>
  <action>
RED first. Test setup mirrors plan 03 (Fastify + dual-auth + rate-limit + MockAgent) but registers the OpenAI route only.

GREEN: write apps/api/src/routes/tokens/openai-realtime.ts exporting buildOpenAIRealtimeTokenRoutes. The factory:

1. Defines preHandler 'requireOpenAIConfigured': if process.env.OPENAI_API_KEY unset, reply.code(503).send body error 'OpenAI Realtime not configured (set OPENAI_API_KEY in .env)' and DO NOT call done. Note: OPENAI_API_KEY is shared with Phase 3 D-12 (already in .env.example); this plan does NOT modify .env.example.

2. Defines zod schema for request body: object with optional 'streams' (number, default 1, allowed values 1 or 2), optional 'model' (string). Use Fastify's schema validation OR an inline check at handler top.

3. Registers app.post on '/api/openai-realtime-token' with config.rateLimit max 30 timeWindow '1 minute' keyGenerator returning req.user?.id ?? req.ip; preHandler array contains requireOpenAIConfigured.

4. Handler logic:
   a. Read 'streams' from req.body, default 1. If not 1 and not 2 then reply.code(400).send body error 'streams must be 1 or 2'.
   b. Read 'model' from req.body, default 'gpt-realtime'.
   c. Build mintOne lambda calling callProvider with: url 'https://api.openai.com/v1/realtime/client_secrets', method POST, headers having authorization 'Bearer ' + process.env.OPENAI_API_KEY and content-type 'application/json', body JSON.stringify with key 'session' value object having type 'realtime' and model the resolved model string, envVarName 'OPENAI_API_KEY', providerLabel 'OpenAI Realtime'.
   d. Use Promise.all on an array of mintOne() invocations of length equal to streams. NOTE: Promise.all is fail-fast; do NOT use Promise.allSettled (per RESEARCH.md lines 545-547 — partial success leakage is worse than fail-fast).
   e. After Promise.all resolves, scan results array; if any entry has ok===false, reply.code(failed.status).send body error failed.message and return. Also stop here BEFORE constructing any partial response (T-04-01 mitigation: do not leak first secret on partial failure).
   f. Map results to secrets array via results.map(r => r.json.value as string); if any secret is empty string then reply.code(503).send body error 'OpenAI Realtime token mint malformed response'.
   g. reply.send body having key clientSecret value secrets[0] AND key clientSecrets value the secrets array.

REFACTOR: keep route under 80 LoC. Coverage target >=90/90/90/90.

CRITICAL: the wire response has BOTH clientSecret (singular) and clientSecrets (array) — desktop asserts both presents; do NOT collapse into a single field even when streams=1.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server &amp;&amp; pnpm --filter @openwhispr/api test src/routes/tokens/openai-realtime.test.ts --run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - All 9 tests pass; coverage on openai-realtime.ts >=90/90/90/90.
    - Test 2 explicitly verifies the two MockAgent intercepts fire within 50ms of each other (parallel, not sequential).
    - Test 4 explicitly verifies the response body does NOT contain the first secret value when the second mint fails.
    - The wire response when streams=1 contains BOTH clientSecret (string) AND clientSecrets (array of length 1) — verified by Test 1 assertions.
    - The wire response when streams=2 contains clientSecrets of length exactly 2 — verified by Test 2.
    - File uses 'Bearer ' prefix in Authorization header (D-16) — verifiable via grep showing 'Bearer '.
    - git log -1 --name-only shows openai-realtime.ts + openai-realtime.test.ts in the same commit.
  </acceptance_criteria>
  <done>OpenAI Realtime token mint route GREEN at >=90/90/90/90 with parallel-mint fail-fast verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop to /api/openai-realtime-token | Authenticated; req.user.id populated by dual-auth |
| Fastify to OpenAI client_secrets | Server-held OPENAI_API_KEY; only ephemeral ek_ secrets (~60s TTL) cross to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tokens/openai-realtime.ts | mitigate | Per-user 30/min rate limit (Test 6); 5s provider timeout via _call-provider; missing-key 503 in preHandler (Test 5); on Promise.all partial failure, the first successful secret is NEVER written to the wire response (Test 4 explicitly verifies) |
| T-04-04 | Spoofing | rate-limit keyGenerator | mitigate | dual-auth runs before rate-limit; unauthenticated returns 401 without consuming bucket (Test 9) |
| T-04-INPUT | Tampering | streams parameter | mitigate | Explicit allowlist {1,2}; values outside the set return 400 (Test 3). Prevents integer overflow / negative / array attacks |
</threat_model>

<verification>
- pnpm --filter @openwhispr/api test src/routes/tokens/openai-realtime.test.ts --run --coverage shows >=90/90/90/90.
- Test 4 (fail-fast secret-leakage prevention) is the load-bearing assertion for T-04-01 partial-success leakage.
- Atomic commit with both production + test files.
</verification>

<success_criteria>
- WIRE-15 GREEN.
- Promise.all parallelism + fail-fast semantics test-verified.
- streams=1 wire shape includes both singular clientSecret and clientSecrets array.
</success_criteria>

<output>
After completion, create .planning/phases/04-streaming-realtime/04-04-SUMMARY.md with: coverage axes, parallel-mint timing assertion, fail-fast secret-leakage test outcome.
</output>
