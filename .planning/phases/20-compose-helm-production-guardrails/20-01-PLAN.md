---
phase: 20-compose-helm-production-guardrails
plan: 01
type: execute
wave: A
depends_on: []
files_modified:
  - tools/lint-compose-resources.ts
  - tools/lint-compose-resources.test.ts
  - tools/__tests__/fixtures/compose-resources/bad/docker-compose.yml
  - tools/__tests__/fixtures/compose-resources/good/docker-compose.yml
  - docker-compose.yml
  - compose/docker-compose.ingress.yml
  - compose/docker-compose.pgbouncer.yml
  - compose/docker-compose.storage.yml
  - compose/docker-compose.observability.yml
  - compose/docker-compose.embedded-litellm.yml
  - compose/docker-compose.load-test.yml
  - compose/docker-compose.load-test.realistic.yml
  - compose/e2e/docker-compose.e2e.yml
  - compose/live-soak/docker-compose.live.yml
  - Makefile
  - package.json
autonomous: false  # Task 4 is a human-verify checkpoint for ROADMAP SC7 (docker kill auto-restart smoke)
requirements: [SR-20.1, SR-20.2, SR-20.7]
must_haves:
  truths:
    - "tools/lint-compose-resources.ts exists, exports auditComposeResources(repoRoot)"
    - "Running auditComposeResources against the live repo tree returns [] (zero violations)"
    - "Vitest coverage on tools/lint-compose-resources.ts >= 90/90/90/90 (lines/branches/functions/statements)"
    - "Every long-running service in docker-compose.yml + compose/*.yml + compose/e2e/*.yml + compose/live-soak/*.yml declares deploy.resources.limits.memory at-or-above the ROADMAP floor"
    - "Traefik, PgBouncer, MinIO, and all 5 LGTM services declare `restart: unless-stopped`"
    - "make lint-compose-resources exits 0 against the live tree and exits 1 against the BAD fixture"
    - "Git log shows test commits preceding production-YAML commits (TDD RED then GREEN)"
  artifacts:
    - path: tools/lint-compose-resources.ts
      provides: "auditComposeResources(repoRoot): Violation[] — R1 missing memory limit, R2 missing restart, R3 below floor"
      contains: "export function auditComposeResources"
    - path: tools/lint-compose-resources.test.ts
      provides: "vitest harness: live-tree zero-violation regression sentinel + BAD fixture flags all codes + GOOD fixture clean"
      contains: "describe(\"lint-compose-resources"
    - path: tools/__tests__/fixtures/compose-resources/bad/docker-compose.yml
      provides: "Synthetic BAD compose fixture exercising R1 + R2 + R3"
      contains: "services:"
    - path: tools/__tests__/fixtures/compose-resources/good/docker-compose.yml
      provides: "Synthetic GOOD compose fixture — all services compliant"
      contains: "deploy:"
    - path: docker-compose.yml
      provides: "Base compose with deploy.resources.limits on postgres/valkey/litellm/api/worker/web"
      contains: "deploy:"
    - path: Makefile
      provides: "lint-compose-resources target"
      contains: "lint-compose-resources:"
    - path: package.json
      provides: "test:lint-compose-resources script with coverage thresholds"
      contains: "test:lint-compose-resources"
  key_links:
    - from: tools/lint-compose-resources.ts
      to: "COMPOSE_FILES, MEMORY_FLOORS_BYTES, SHORT_LIVED_ALLOWLIST"
      via: "exported constants consumed by tests and future CI workflow"
      pattern: "export const (COMPOSE_FILES|MEMORY_FLOORS_BYTES|SHORT_LIVED_ALLOWLIST)"
    - from: tools/lint-compose-resources.test.ts
      to: tools/lint-compose-resources.ts
      via: "import { auditComposeResources } from \"./lint-compose-resources.js\""
      pattern: "from \"./lint-compose-resources"
    - from: Makefile
      to: tools/lint-compose-resources.ts
      via: "pnpm exec tsx tools/lint-compose-resources.ts"
      pattern: "pnpm exec tsx tools/lint-compose-resources.ts"
---

<objective>
Land SR-20.1 (compose `deploy.resources.limits.memory`), SR-20.2 (`restart: unless-stopped` on Traefik/PgBouncer/MinIO/LGTM), and SR-20.7 (TDD discipline: RED before GREEN) via a new `tools/lint-compose-resources.ts` codifying the rule and a single mechanical YAML pass across all 10 compose files.

Purpose: Close audit findings A1 (BLOCKER: no `deploy.resources.limits` anywhere), A2 (Traefik missing restart), A5/A6/A7 (PgBouncer/MinIO/LGTM missing restart). Without resource caps, postgres/litellm/api can OOM-kill the host; without restart policies, a Traefik crash = full ingress outage.

Output: A vitest-tested lint script (≥ 90/90/90/90 coverage) + production YAML changes across 10 compose files + Makefile target + package.json script. CI wiring lands in plan 20-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-compose-helm-production-guardrails/20-CONTEXT.md
@.planning/phases/20-compose-helm-production-guardrails/20-RESEARCH.md
@.planning/phases/20-compose-helm-production-guardrails/20-PATTERNS.md
@.planning/phases/20-compose-helm-production-guardrails/20-AUDIT-SOURCE.md

# Pattern source files (copy structure verbatim — do not reinvent)
@tools/lint-traefik-routes.ts
@tools/lint-traefik-routes.test.ts
@tools/lint-compose-chart-parity.ts

# Compose files being modified
@docker-compose.yml
@compose/docker-compose.ingress.yml
@compose/docker-compose.pgbouncer.yml
@compose/docker-compose.storage.yml
@compose/docker-compose.observability.yml
@compose/docker-compose.embedded-litellm.yml
@compose/docker-compose.load-test.yml

# Glue
@Makefile
@package.json

<interfaces>
<!-- Constants the lint script MUST export so plan 20-03's CI workflow can re-use them.
     Source: 20-RESEARCH.md §2 recommended shape. -->

```ts
// tools/lint-compose-resources.ts
export interface Violation {
  readonly code: "R1-MISSING-MEMORY-LIMIT" | "R2-MISSING-RESTART" | "R3-MEMORY-BELOW-FLOOR";
  readonly file: string;
  readonly service: string;
  readonly message: string;
}

export const COMPOSE_FILES: readonly string[];                  // 10 entries — see action below
export const SHORT_LIVED_ALLOWLIST: ReadonlySet<string>;        // migrate, seed, contract-test-runner, fixture-idp, k6, mailpit, speaches
export const MEMORY_FLOORS_BYTES: Readonly<Record<string, number>>;

export function auditComposeResources(repoRoot: string): Violation[];
```

ROADMAP-locked memory floors (from 20-CONTEXT.md SR-20.1 + 20-RESEARCH.md §3):
- postgres: 2G, litellm: 1G, api: 1G, worker: 512M, web: 512M (chart floor 384M; recommend 512M)
- otel-collector: 256M, loki/tempo/mimir: 512M, grafana: 256M
- valkey: 256M, traefik: 256M, pgbouncer: 128M, minio: 512M

Recommended write values per service (lint enforces >= floor; assign exact value below):
- postgres → memory: 2G, cpus: "2.0"
- valkey → memory: 256M
- litellm → memory: 1G, cpus: "2.0"  (parity with chart limits.memory: 1Gi)
- api → memory: 1G, cpus: "2.0"
- worker → memory: 512M, cpus: "1.0"
- web → memory: 512M, cpus: "1.0"
- traefik → memory: 256M
- pgbouncer → memory: 128M
- minio → memory: 512M
- otel-collector → memory: 256M
- loki/tempo/mimir → memory: 512M
- grafana → memory: 256M
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write lint script signature, vitest harness, and synthetic fixtures (live-tree assertion FAILS)</name>
  <files>tools/lint-compose-resources.ts, tools/lint-compose-resources.test.ts, tools/__tests__/fixtures/compose-resources/bad/docker-compose.yml, tools/__tests__/fixtures/compose-resources/good/docker-compose.yml, package.json</files>
  <behavior>
    - vitest: `auditComposeResources(REPO_ROOT)` returns `[]` against live repo — MUST FAIL at this commit (production YAML still bare).
    - vitest: against BAD fixture, returned violations include all three codes R1-MISSING-MEMORY-LIMIT, R2-MISSING-RESTART, R3-MEMORY-BELOW-FLOOR.
    - vitest: against GOOD fixture, returns `[]`.
    - Coverage: targets file `tools/lint-compose-resources.ts` only; thresholds 90/90/90/90 in package.json script.
  </behavior>
  <action>
Implement per 20-RESEARCH.md §2 + 20-PATTERNS.md §2.1–§2.3. Copy header, imports, `readYamlSafe`, `findRepoRoot`, CLI entry guard verbatim from `tools/lint-traefik-routes.ts` (lines 1-2, 28-31, 53-59, 304-323). Replace logic with `auditComposeResources` per 20-RESEARCH.md §12 code example. Export `COMPOSE_FILES` (10 entries — see Interfaces block), `SHORT_LIVED_ALLOWLIST`, `MEMORY_FLOORS_BYTES`. Implement `parseMemoryString` per 20-RESEARCH.md §2 regex.

Vitest harness copies `tools/lint-traefik-routes.test.ts` 3-test shape verbatim (live-tree zero / BAD trips all codes / GOOD clean). Fixture paths: `tools/__tests__/fixtures/compose-resources/{bad,good}/docker-compose.yml`.

Synthetic BAD fixture (~15 lines): 3 services — `postgres` (deploy.resources.limits.memory: 64M → R3 below 2G floor), `traefik` (no deploy block → R1, no restart → R2), `api` (restart: unless-stopped + memory: 1G → clean).
Synthetic GOOD fixture (~15 lines): 2 services — `postgres` (memory: 2G, restart: unless-stopped) + `valkey` (memory: 256M, restart: unless-stopped).

package.json: add `test:lint-compose-resources` script copying the shape of existing `test:lint-compose-chart-parity` at package.json line ~33 — swap filename, set coverage.include to `tools/lint-compose-resources.ts`, thresholds 90/90/90/90 on lines/branches/functions/statements per CLAUDE.md gate.

Commit RED with message: `test(20-01-01): red — lint-compose-resources fails on services missing limits + restart`.
This commit's vitest run MUST show test 1 (live-tree regression sentinel) FAILING. The BAD/GOOD synthetic tests pass at this commit (they validate lint logic, which is now correct).
  </action>
  <verify>
    <automated>pnpm test:lint-compose-resources 2>&1 | tee /tmp/red.log; grep -q "returns zero violations against the live repo tree" /tmp/red.log &amp;&amp; grep -E "(FAIL|✗|✘)" /tmp/red.log</automated>
  </verify>
  <done>RED commit on HEAD; vitest output confirms the live-tree sentinel test FAILS (production YAML still bare); BAD and GOOD synthetic tests PASS (lint logic is correct); coverage on `tools/lint-compose-resources.ts` ≥ 90/90/90/90 measured against the two synthetic fixture runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — apply deploy.resources.limits + restart to base docker-compose.yml + 9 overlay files (lint flips to clean)</name>
  <files>docker-compose.yml, compose/docker-compose.ingress.yml, compose/docker-compose.pgbouncer.yml, compose/docker-compose.storage.yml, compose/docker-compose.observability.yml, compose/docker-compose.embedded-litellm.yml, compose/docker-compose.load-test.yml, compose/docker-compose.load-test.realistic.yml, compose/e2e/docker-compose.e2e.yml, compose/live-soak/docker-compose.live.yml</files>
  <behavior>
    - vitest live-tree sentinel test flips from RED to GREEN: `auditComposeResources(REPO_ROOT)` returns `[]`.
    - All 6 base services (postgres, valkey, litellm, api, worker, web) declare `deploy.resources.limits.memory` + `restart: unless-stopped` (where applicable; migrate keeps `restart: "no"`).
    - Traefik (compose/docker-compose.ingress.yml), PgBouncer (compose/docker-compose.pgbouncer.yml), MinIO (compose/docker-compose.storage.yml), and all 5 LGTM services (compose/docker-compose.observability.yml) declare `restart: unless-stopped`.
    - All overlay services declare `deploy.resources.limits.memory` at-or-above ROADMAP floor.
  </behavior>
  <action>
For each compose file, for each non-short-lived service, add a `deploy:` block under the service per the shape in 20-RESEARCH.md §1 + §12 code example. Memory values per the "Recommended write values" table in this plan's `<interfaces>` block. CPU limits optional but apply on postgres/litellm/api per 20-RESEARCH.md §3 recommendation.

For services on the SR-20.2 restart-policy list (Traefik in `compose/docker-compose.ingress.yml`, PgBouncer in `compose/docker-compose.pgbouncer.yml`, MinIO in `compose/docker-compose.storage.yml`, otel-collector/loki/tempo/mimir/grafana in `compose/docker-compose.observability.yml`), add `restart: unless-stopped` at the top-level service key (NOT under `deploy.restart_policy` — see 20-RESEARCH.md §10 P2). For all other long-running services in any file, also add `restart: unless-stopped` per the lint's R2 rule.

`migrate` service in `docker-compose.yml` is short-lived (already has `restart: "no"`) — DO NOT add deploy block (allowlist exempt; per 20-RESEARCH.md §2 SHORT_LIVED_ALLOWLIST).

For `compose/docker-compose.load-test.yml` / `.realistic.yml` / `compose/e2e/docker-compose.e2e.yml` / `compose/live-soak/docker-compose.live.yml`: only long-running services (e.g. mock-litellm, speaches when present) get limits + restart; k6/seed/contract-test-runner/fixture-idp/cjm-runner short-lived bots stay allowlisted.

Constitutional rule (CLAUDE.md Hard Rule #1): these production-YAML edits exist to satisfy a NEW guardrail (the lint), not to silence a fail in pre-existing production code. The lint asserts the rule the audit (A1/A2/A5/A6/A7) requires. Per 20-RESEARCH.md §"Project Constraints" point 3, RED→GREEN on NEW guardrails IS the expected workflow.

Commit GREEN with message: `feat(20-01-02): green — deploy.resources.limits + restart unless-stopped across 10 compose files (closes A1/A2/A5/A6/A7)`.
  </action>
  <verify>
    <automated>pnpm test:lint-compose-resources &amp;&amp; pnpm exec tsx tools/lint-compose-resources.ts</automated>
  </verify>
  <done>vitest live-tree sentinel test now PASSES (live tree returns 0 violations); CLI invocation prints `lint-compose-resources: clean` and exits 0; `git diff --stat HEAD~1 HEAD` shows the 10 compose files modified with deploy + restart additions; no production logic (route handlers, plugin source, migrations) touched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Makefile target + verify CLI integration (housekeeping)</name>
  <files>Makefile</files>
  <action>
Add `lint-compose-resources` target near Makefile line 26 (next to the `lint-rls` target — see 20-PATTERNS.md §2.10). Body:
```
lint-compose-resources:
	pnpm exec tsx tools/lint-compose-resources.ts
```
Also add `lint-compose-resources` to the `.PHONY:` list at Makefile lines 5-10.

Commit message: `chore(20-01-03): wire make lint-compose-resources target`.
  </action>
  <verify>
    <automated>make lint-compose-resources &amp;&amp; grep -q "^lint-compose-resources:" Makefile &amp;&amp; grep -q "lint-compose-resources" <(awk '/^\.PHONY:/{p=1}p' Makefile | head -20)</automated>
  </verify>
  <done>`make lint-compose-resources` exits 0 against the live tree; Makefile shows target + .PHONY entry.</done>
</task>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 4: HUMAN-VERIFY — docker kill auto-restart smoke (ROADMAP SC7)</name>
  <files>(none — runtime observation only; no commit)</files>
  <behavior>
    - Operator boots the default stack: `docker compose --profile default up -d --wait`.
    - For each restart-policy-target service (pgbouncer, traefik, minio, loki, grafana — sample of 5 covering the SR-20.2 surface), operator runs `docker kill openwhispr-&lt;service&gt;-1` then waits ≤ 5 s and confirms `docker ps --filter name=openwhispr-&lt;service&gt; --format '{{.Status}}'` shows `Up ...` (not `Exited`).
    - SR-20.2 YAML declaration alone is insufficient — this task observes the runtime behavior the audit (A2/A5/A6/A7) demanded close.
  </behavior>
  <action>
Operator runs the following verification recipe locally (NOT in CI — Docker engine semantics are host-specific, and CI ubuntu-latest already validates `docker compose config` parses; runtime restart-loop behavior is what we're observing here):

```bash
# Boot default stack with full overlay set (ingress + observability + storage + pgbouncer)
docker compose \
  -f docker-compose.yml \
  -f compose/docker-compose.ingress.yml \
  -f compose/docker-compose.pgbouncer.yml \
  -f compose/docker-compose.storage.yml \
  -f compose/docker-compose.observability.yml \
  --profile default up -d --wait

# Kill and observe (each line must show "Up" within 5 s)
for svc in pgbouncer traefik minio loki grafana; do
  echo "=== Killing $svc ==="
  docker kill "$(docker ps --filter name=$svc --format '{{.Names}}' | head -1)"
  sleep 5
  docker ps --filter name=$svc --format '{{.Names}}\t{{.Status}}'
done

# Cleanup
docker compose -f docker-compose.yml \
  -f compose/docker-compose.ingress.yml \
  -f compose/docker-compose.pgbouncer.yml \
  -f compose/docker-compose.storage.yml \
  -f compose/docker-compose.observability.yml \
  down
```

Operator records the output in `.planning/phases/20-compose-helm-production-guardrails/20-01-SUMMARY.md` under a `## SC7 Verification` section with the 5 `Up …` lines pasted as evidence, then signs off (`[x]` checkbox) to unblock Wave C (20-03).

If any container shows `Exited` after the 5 s wait, this is a defect — escalate to the orchestrator and do NOT proceed to Wave C. Investigate whether the compose file's `restart:` key was actually picked up (most common cause: nested under `deploy:` block on Docker Compose v2 — see 20-RESEARCH.md §10 P2 — `restart:` must be top-level, NOT `deploy.restart_policy`).
  </action>
  <verify>
    <human>All 5 containers show `Up …` within 5 s of `docker kill`; evidence pasted in 20-01-SUMMARY.md under `## SC7 Verification`; operator signs off with `[x]` checkbox.</human>
  </verify>
  <done>SC7 evidence captured in 20-01-SUMMARY.md; operator checkbox signed; Wave C (20-03) unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compose-author → container runtime | Resource limits enforced by cgroup v2 prevent OOM-DoS on shared host |
| operator → container runtime | Restart policy bounds recovery time after crashed long-running service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | DoS | postgres / litellm / api containers without memory cap | mitigate | `deploy.resources.limits.memory: 2G/1G/1G` per 20-RESEARCH.md §3; lint script R1 + R3 enforces presence + floor |
| T-20-02 | DoS | Traefik / PgBouncer / MinIO / LGTM crashing with no auto-restart | mitigate | `restart: unless-stopped` per SR-20.2; lint script R2 enforces presence |
| T-20-03 | Tampering | compose-author silently regresses limits via merge override | mitigate | Lint runs on EVERY compose file path (not just base); R3 catches under-floor merges; CI gate lands in plan 20-03 |
| T-20-04 | Spoofing | malicious YAML evading parser via unicode tricks | accept | yaml package is workspace-pinned; no parser-version drift; lint failure on unparseable file already in CLI exit-code contract (20-PATTERNS.md §3.5 exit 2) |
</threat_model>

<verification>
Manual phase-level checks (orchestrator runs after plan completes per CLAUDE.md Hard Rule #3):

1. `git log --oneline -5` — verify 3 commits: `test(20-01-01): red`, `feat(20-01-02): green`, `chore(20-01-03)`
2. `pnpm test:lint-compose-resources` — coverage ≥ 90/90/90/90 reported on `tools/lint-compose-resources.ts`
3. `pnpm exec tsx tools/lint-compose-resources.ts` — prints `lint-compose-resources: clean`, exits 0
4. `make lint-compose-resources` — exits 0
5. `git diff HEAD~2 HEAD~1 -- docker-compose.yml | head -40` — confirms deploy + restart blocks added
6. `docker compose config > /dev/null` — confirms base file still valid (syntactic smoke; full matrix lands in plan 20-03)
7. `git status --short` — working tree clean
</verification>

<success_criteria>
- SR-20.1 satisfied: every long-running compose service declares `deploy.resources.limits.memory` ≥ floor
- SR-20.2 satisfied: Traefik / PgBouncer / MinIO / 5×LGTM declare `restart: unless-stopped`
- SR-20.7 satisfied: git log shows RED (test) commit preceding GREEN (production-YAML) commit
- Lint script vitest coverage ≥ 90/90/90/90
- 0 violations against live tree
- Coverage Success Criterion 1 (lint script + ≥90% + non-zero on broken fixture) PARTIAL (CI wiring lands in 20-03)
- Coverage Success Criterion 2 (live tree → 0 violations) MET
</success_criteria>

<output>
Create `.planning/phases/20-compose-helm-production-guardrails/20-01-SUMMARY.md` with: commit SHAs, vitest coverage numbers, lint output, and any deferred items observed during work.
</output>
