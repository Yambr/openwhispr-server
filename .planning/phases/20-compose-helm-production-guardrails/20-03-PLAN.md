---
phase: 20-compose-helm-production-guardrails
plan: 03
type: execute
wave: C
depends_on: [20-01, 20-02b]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [SR-20.6, SR-20.7]
must_haves:
  truths:
    - "New job named `compose-lint` exists in .github/workflows/ci.yml"
    - "Job matrix has 8 profile cells: default, contract-test, observability, pgbouncer, storage, load-test-mock, load-test-realistic, e2e"
    - "Each cell runs `docker compose -f docker-compose.yml [-f compose/...overlay.yml ...] --profile <p> config > /dev/null` and exits non-zero on YAML/interpolation failure"
    - "Job invokes `make lint-compose-resources` as a final step (Phase 20-01 lint script)"
    - "Job runs in parallel to helm-lint; both gate merge to main"
    - "Job wall-time <60 s on ubuntu-latest"
    - "Divergence-by-design (Phase 20-PLAN-CHECK loop-1 warning 7 acknowledged): RED→GREEN signal is CI-URL based, not git-commit based; documented in commit body + 20-03-SUMMARY"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "compose-lint job with 8-profile matrix"
      contains: "compose-lint:"
---

<objective>
Land SR-20.6 (CI compose-lint job). Single atomic commit. Pure CI YAML — no git-commit RED→GREEN pair possible; the SR-20.7 invariant is preserved via CI run URLs captured in 20-03-SUMMARY.md (a deliberate-break PR removing one `deploy.resources.limits.memory` turns the job red; restoring flips it green).

This plan depends on Phase 20-01 (`tools/lint-compose-resources.ts` + `make lint-compose-resources` target — already landed in commits `120fda1` + `a5614e2` + `b9e5210`) AND Phase 20-02b's SC6 sign-off (don't merge CI gate until securityContext changes verified on live kind).
</objective>

<divergence_by_design>
Per Phase 20-PLAN-CHECK loop-1 warning 7 (acknowledged in loop 2):

The constitutional TDD shape (RED commit asserts failing state, GREEN commit makes it pass) cannot be expressed as a git-commit pair for a single CI YAML file. The RED→GREEN signal is captured as CI run URLs in 20-03-SUMMARY.md:
- RED-evidence: open a test branch removing one `deploy.resources.limits.memory` line from an overlay → push → verify new compose-lint job turns red
- GREEN-evidence: restore the line → job goes green

SR-20.7's underlying invariant (evidence of failing-then-passing state precedes merge) is preserved.
</divergence_by_design>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add compose-lint job to ci.yml with 8-profile matrix</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - New top-level job `compose-lint` appears in ci.yml
    - Job matrix: 8 profile cells with explicit `-f docker-compose.yml [-f compose/docker-compose.<overlay>.yml]+` and `--profile <p>` per cell
    - Each cell shells: `docker compose <flags> config > /dev/null`
    - Final step: `make lint-compose-resources`
    - Job uses ubuntu-latest, sets up Node 24 + pnpm (for tsx), installs deps, then runs the matrix step
    - Runtime budget: <60 s total wall time (matrix cells run in parallel)
  </behavior>
  <action>
Append to `.github/workflows/ci.yml` following the helm-lint job's structural shape (existing precedent in `.github/workflows/helm-lint.yml` if separated, or in ci.yml itself).

Profile matrix:
| Cell | `-f` chain | `--profile` |
|---|---|---|
| default | `docker-compose.yml` | default |
| contract-test | `docker-compose.yml -f compose/docker-compose.contract-test.yml` | default,contract-test |
| observability | `docker-compose.yml -f compose/docker-compose.observability.yml` | default |
| pgbouncer | `docker-compose.yml -f compose/docker-compose.pgbouncer.yml` | default |
| storage | `docker-compose.yml -f compose/docker-compose.storage.yml` | default |
| load-test-mock | `docker-compose.yml -f compose/docker-compose.load-test.yml` | load-test-mock |
| load-test-realistic | `docker-compose.yml -f compose/docker-compose.load-test.yml -f compose/docker-compose.load-test.realistic.yml` | load-test-realistic |
| e2e | `docker-compose.yml -f compose/e2e/docker-compose.e2e.yml` | default,e2e |

Step shape:
```yaml
- name: Validate compose config
  run: docker compose ${{ matrix.compose_args }} --profile ${{ matrix.profile }} config > /dev/null
```

Concurrency safety: this job does NOT pull images (`config` validates merge + interpolation only), so it's pure YAML linting. Time budget ~5 s per cell × 8 cells in parallel = total ~10 s + Node setup overhead.

Commit: `ci(20-03-01): compose-lint job — 8-profile matrix gate on PRs (closes audit C1)`

Commit body MUST document the divergence-by-design (SR-20.7 narrow exception for single-file CI YAML — see 20-03-PLAN.md `<divergence_by_design>` block).
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml 2>&amp;1 | head -5 || true; echo "---"; grep -A 2 "compose-lint:" .github/workflows/ci.yml | head -10</automated>
    <human>RED-evidence URL recorded in 20-03-SUMMARY.md after the PR opens (deliberate break → red CI). GREEN-evidence URL recorded after restoring break (green CI).</human>
  </verify>
  <done>1 commit; ci.yml contains compose-lint job; actionlint clean; 20-03-SUMMARY.md will record CI run URLs after PR opens</done>
</task>

</tasks>
