---
phase: 20-compose-helm-production-guardrails
plan: 03
type: execute
wave: C
depends_on: [01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [SR-20.6, SR-20.7]
must_haves:
  truths:
    - ".github/workflows/ci.yml declares a new `compose-lint` job"
    - "compose-lint job runs an 8-cell matrix over { default, contract-test, observability, pgbouncer, storage, load-test-mock, load-test-realistic, e2e } compose-profile combinations"
    - "Each matrix cell runs `docker compose -f docker-compose.yml -f compose/docker-compose.<overlay>.yml config > /dev/null` and exits 0"
    - "compose-lint job invokes `pnpm exec tsx tools/lint-compose-resources.ts` (the artifact landed by plan 20-01) and runs `pnpm test:lint-compose-resources` for the coverage gate"
    - "Job uses step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 (SHA-pin per ci.yml convention)"
    - "Job runs on PRs touching `docker-compose*.yml`, `compose/**/*.yml`, or `tools/lint-compose-resources.ts`"
    - "Total job wall-time stays under 60 s on ubuntu-latest (research §8: matrix cells ~3-5s each, run in parallel)"
    - "A test PR that deliberately deletes a `deploy.resources.limits.memory` from one overlay turns the job red"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "compose-lint job — 8-cell config matrix + vitest coverage gate + CLI lint invocation"
      contains: "compose-lint:"
  key_links:
    - from: .github/workflows/ci.yml
      to: tools/lint-compose-resources.ts
      via: "pnpm exec tsx tools/lint-compose-resources.ts step"
      pattern: "pnpm exec tsx tools/lint-compose-resources.ts"
    - from: .github/workflows/ci.yml
      to: package.json
      via: "pnpm test:lint-compose-resources script (coverage gate)"
      pattern: "test:lint-compose-resources"
    - from: .github/workflows/ci.yml
      to: docker-compose.yml
      via: "matrix step `docker compose -f docker-compose.yml -f compose/docker-compose.<overlay>.yml config`"
      pattern: "docker compose -f docker-compose.yml"
---

<objective>
Land SR-20.6 (CI `compose-lint` job in `.github/workflows/ci.yml`) and SR-20.7 (TDD discipline: RED commit precedes GREEN). Gate every PR touching `docker-compose*.yml` / `compose/**/*.yml` / `tools/lint-compose-resources.ts` on a fast 8-cell matrix.

Purpose: Close audit finding C1 (no `docker compose config` gate in CI — compose-syntax/interpolation bugs reach e2e). Without this gate, plan 20-01's lint script + production YAML edits can silently drift on future PRs.

Output: One new job in `.github/workflows/ci.yml`. Pure CI YAML. Depends on plan 20-01 because the job invokes `tools/lint-compose-resources.ts` which doesn't exist until 20-01 lands.

This plan is the simplest in the phase but is gated behind 20-01 — if 20-01 has not merged, this job would no-op or fail to find the script.

**Divergence-by-design (acknowledged in 20-PLAN-CHECK.md warning 7):** Because this plan modifies exactly one file (`.github/workflows/ci.yml`) — a CI configuration artifact, not application code — the constitutional TDD shape (RED commit asserts the failing state, GREEN commit makes it pass) cannot be expressed as a git-commit pair. Instead the RED→GREEN signal is captured as **CI run URLs** in 20-03-SUMMARY.md: a deliberate-break PR (remove a `deploy.resources.limits.memory` line from one overlay) turns the new `compose-lint` job red, restoring it flips it green. This is an explicit, narrow exception to the SR-20.7 git-commit RED→GREEN pattern, justified by the single-file scope and the unavailability of a meaningful "RED then GREEN" git-commit shape for pure CI YAML. SR-20.7's underlying invariant — *evidence of the failing-then-passing state precedes the merge* — is preserved via the CI URL pair.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-compose-helm-production-guardrails/20-CONTEXT.md
@.planning/phases/20-compose-helm-production-guardrails/20-RESEARCH.md
@.planning/phases/20-compose-helm-production-guardrails/20-PATTERNS.md

# Pattern source files
@.github/workflows/ci.yml
@.github/workflows/helm-lint.yml
@docker-compose.yml
@compose/docker-compose.observability.yml
@compose/docker-compose.pgbouncer.yml
@compose/docker-compose.storage.yml
@compose/docker-compose.ingress.yml
@compose/docker-compose.load-test.yml
@compose/docker-compose.load-test.realistic.yml
@compose/docker-compose.contract-test.yml
@compose/e2e/

<interfaces>
<!-- Job structure (per 20-PATTERNS.md §2.9 + 20-RESEARCH.md §8) -->

Job name: `compose-lint`
runs-on: ubuntu-latest (Docker Compose v2 plugin preinstalled per actions/runner-images Ubuntu24 readme — verified in 20-RESEARCH.md §8)

Steps (in order):
1. step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 (SHA-pin from ci.yml:18, 164, 224 — DO NOT bump)
2. actions/checkout@v5 with fetch-depth: 0
3. pnpm/action-setup@v4 with version: 11.0.8
4. actions/setup-node@v5 with node-version: '24', cache: 'pnpm'
5. pnpm install --frozen-lockfile
6. (vitest coverage gate) pnpm test:lint-compose-resources
7. (CLI lint) pnpm exec tsx tools/lint-compose-resources.ts
8. (matrix) docker compose config — 8 profile combinations, each cell a separate matrix entry

Path-trigger (per ci.yml convention):
on:
  pull_request:
    paths:
      - "docker-compose.yml"
      - "compose/**/*.yml"
      - "tools/lint-compose-resources.ts"
      - "tools/lint-compose-resources.test.ts"
      - ".github/workflows/ci.yml"

Matrix cell mapping (per 20-RESEARCH.md §8 + 20-CONTEXT.md SR-20.6):

| profile name        | -f chain                                                                                                |
|---------------------|---------------------------------------------------------------------------------------------------------|
| default             | -f docker-compose.yml                                                                                   |
| contract-test       | -f docker-compose.yml -f compose/docker-compose.contract-test.yml                                       |
| observability       | -f docker-compose.yml -f compose/docker-compose.observability.yml                                       |
| pgbouncer           | -f docker-compose.yml -f compose/docker-compose.pgbouncer.yml                                           |
| storage             | -f docker-compose.yml -f compose/docker-compose.storage.yml                                             |
| load-test-mock      | -f docker-compose.yml -f compose/docker-compose.load-test.yml                                           |
| load-test-realistic | -f docker-compose.yml -f compose/docker-compose.load-test.realistic.yml                                 |
| e2e                 | -f docker-compose.yml -f compose/e2e/docker-compose.e2e.yml  (CONFIRM exact filename via `ls compose/e2e/` during plan execution per 20-RESEARCH.md §"Open Questions" #1) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN — append compose-lint job to .github/workflows/ci.yml (single atomic RED then GREEN flip)</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    - The new `compose-lint` job appears under `jobs:` in ci.yml.
    - 8 matrix cells, fail-fast: false, each running `docker compose <chain> config > /dev/null`.
    - Vitest coverage gate runs `pnpm test:lint-compose-resources` (must pass ≥ 90/90/90/90 from plan 20-01).
    - CLI step runs `pnpm exec tsx tools/lint-compose-resources.ts` and expects exit 0.
    - Path-trigger filter restricts the job to compose-related PRs.
  </behavior>
  <action>
This is a CI-only change. TDD here is unusual — the "RED" demonstration is: open a test branch that deliberately removes one `deploy.resources.limits.memory` from an overlay → push → verify the new `compose-lint` job turns red on the test PR. RED-evidence is captured in the SUMMARY.md as a CI run URL, not a code commit.

**Atomic implementation steps (single commit, but with clear RED→GREEN narrative):**

1. **First**: Before pushing, on a scratch branch, hand-remove a `deploy.resources.limits.memory` from `compose/docker-compose.observability.yml:loki`. Push to a draft PR. Confirm the new compose-lint job is **red** (lint script exits 1 with R1-MISSING-MEMORY-LIMIT). Capture the failing-run URL.
2. **Then**: Restore the removed line on the same branch. Push again. Confirm compose-lint flips **green**.
3. **Commit message** for the production change: `ci(20-03-01): add compose-lint job — 8-cell docker compose config matrix + vitest coverage gate (closes C1 / SR-20.6)`.
4. Record both run URLs (red and green) in `20-03-SUMMARY.md`.

Implementation — append the new `compose-lint:` job under the existing `jobs:` map in `.github/workflows/ci.yml`. Place it after `lint-english` (around line 29-41 — see 20-PATTERNS.md §2.9) so it runs in parallel with the existing lint family.

Job spec (per 20-PATTERNS.md §2.9 + 20-RESEARCH.md §8):

```yaml
  compose-lint:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        profile:
          - { name: default,              files: "-f docker-compose.yml" }
          - { name: contract-test,        files: "-f docker-compose.yml -f compose/docker-compose.contract-test.yml" }
          - { name: observability,        files: "-f docker-compose.yml -f compose/docker-compose.observability.yml" }
          - { name: pgbouncer,            files: "-f docker-compose.yml -f compose/docker-compose.pgbouncer.yml" }
          - { name: storage,              files: "-f docker-compose.yml -f compose/docker-compose.storage.yml" }
          - { name: load-test-mock,       files: "-f docker-compose.yml -f compose/docker-compose.load-test.yml" }
          - { name: load-test-realistic,  files: "-f docker-compose.yml -f compose/docker-compose.load-test.realistic.yml" }
          - { name: e2e,                  files: "-f docker-compose.yml -f compose/e2e/docker-compose.e2e.yml" }
    steps:
      - uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450  # v2.19.1
        with: { egress-policy: audit }
      - uses: actions/checkout@v5
        with: { fetch-depth: 0 }
      - uses: pnpm/action-setup@v4
        with: { version: 11.0.8 }
      - uses: actions/setup-node@v5
        with: { node-version: '24', cache: 'pnpm' }
      - run: pnpm install --frozen-lockfile
      - name: vitest coverage gate — lint-compose-resources
        run: pnpm test:lint-compose-resources
      - name: CLI lint — lint-compose-resources
        run: pnpm exec tsx tools/lint-compose-resources.ts
      - name: docker compose config (preinstalled on ubuntu-latest)
        run: docker compose ${{ matrix.profile.files }} config > /dev/null
```

Add path-trigger to the existing `on:` block of `ci.yml` if the workflow uses path filters (or — if `ci.yml` runs on all PRs — DO NOT narrow the trigger; only widen the existing path list to include compose-related globs so the job runs when appropriate). Inspect the current `on:` block before editing and pick the option matching the existing convention.

**Verify the `e2e` cell's exact filename** during implementation (per 20-RESEARCH.md §"Open Questions" #1): `ls compose/e2e/` — current `files_modified` in this plan and 20-01 references `compose/e2e/docker-compose.e2e.yml`. If the actual filename differs (e.g. `compose/e2e/docker-compose.cjm.yml` per 20-RESEARCH.md §8), update the matrix cell accordingly and note in the SUMMARY.

Coverage Success Criterion 5 from ROADMAP requires: "deliberately breaking one overlay YAML (test in PR) turns the job red." Step 1 above produces the evidence run URL for the SUMMARY.
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml | yq '.jobs."compose-lint".strategy.matrix.profile | length' | grep -q '^8$' &amp;&amp; grep -q "pnpm exec tsx tools/lint-compose-resources.ts" .github/workflows/ci.yml &amp;&amp; grep -q "harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450" .github/workflows/ci.yml &amp;&amp; (command -v actionlint &gt;/dev/null &amp;&amp; actionlint .github/workflows/ci.yml || echo "actionlint not installed locally; CI will run it")</automated>
  </verify>
  <done>1 commit on HEAD (`ci(20-03-01)`); `compose-lint` job present in ci.yml with 8-cell matrix + 4-step body (harden-runner / checkout / pnpm / node setup / install / vitest / CLI lint / docker compose config); actionlint accepts the file (or CI's actionlint job will); SUMMARY.md records a RED-evidence run URL (deleted limit → job red) and a GREEN-evidence run URL (restored limit → job green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> main branch | compose-lint job blocks merge when compose YAML regresses on SR-20.1/SR-20.2 |
| github-actions runner -> external network | egress-policy: audit (via harden-runner) — observed outbound only, no block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering | malicious PR drops `deploy.resources.limits` to evade postgres OOM cap | mitigate | compose-lint job (this plan) — R1 + R3 enforced on PR merge |
| T-20-03-02 | Tampering | malicious PR replaces step-security/harden-runner with unpinned tag | accept | SHA-pin convention enforced repo-wide (ci.yml:18/164/224); branch protection rules outside this plan's scope |
| T-20-03-03 | DoS | flaky `docker compose config` step slows CI | accept | Each matrix cell is parse-only (no image pull); 20-RESEARCH.md §8 measured ~3-5 s per cell |
| T-20-03-04 | Supply chain | actions/checkout / setup-node tags can drift | accept | Repo convention already uses major-version tags; SHA-pinning of all third-party actions is a future hardening phase |
</threat_model>

<verification>
1. `git log --oneline -1` — 1 commit: `ci(20-03-01)`.
2. `yq '.jobs."compose-lint".strategy.matrix.profile | length' .github/workflows/ci.yml` — returns 8.
3. `actionlint .github/workflows/ci.yml` — exits 0 (or `gh pr checks` confirms actionlint job green on the PR).
4. `gh pr checks <PR>` (after pushing) shows `compose-lint (default)` ... `compose-lint (e2e)` × 8 matrix entries — all green on a clean main.
5. RED-evidence URL recorded in SUMMARY.md: a deliberate `deploy.resources.limits.memory` removal turned the job red.
6. GREEN-evidence URL recorded in SUMMARY.md: restoring the line turned it green again.
7. `git status --short` — working tree clean.
</verification>

<success_criteria>
- SR-20.6 satisfied: compose-lint job present with all 8 profile combinations + vitest coverage gate + CLI lint invocation
- SR-20.7 satisfied: RED-evidence + GREEN-evidence run URLs captured (CI-level RED/GREEN since the change is a single CI file)
- Coverage Success Criterion 1 (lint script in CI on every PR touching compose) MET
- Coverage Success Criterion 5 (deliberately breaking overlay turns job red) MET via the test PR captured during execution
- Job wall-time < 60 s per 20-RESEARCH.md §8 measurement
</success_criteria>

<output>
Create `.planning/phases/20-compose-helm-production-guardrails/20-03-SUMMARY.md` with: commit SHA, GitHub Actions run URLs (RED + GREEN), job wall-time observed, confirmation of all 8 matrix cells green on main HEAD, and any e2e overlay filename adjustment made during execution per 20-RESEARCH.md §"Open Questions" #1.
</output>
