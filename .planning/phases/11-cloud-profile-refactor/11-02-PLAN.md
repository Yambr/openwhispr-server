---
phase: 11
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - charts/openwhispr/examples/values-external-litellm.yaml
  - charts/openwhispr/examples/values-corporate-litellm.yaml
  - charts/openwhispr/templates/api-deployment.yaml
  - charts/openwhispr/templates/worker-deployment.yaml
  - charts/openwhispr/templates/externalsecret.yaml
  - charts/openwhispr/tests/litellm_test.yaml
  - charts/openwhispr/tests/api_test.yaml
  - charts/openwhispr/tests/corporate_litellm_test.yaml
  - charts/openwhispr/tests/helm_test_hook_test.yaml
  - docker-compose.external-litellm.yml
  - .env.external.example
  - examples/README.md
  - docs/self-hosting.md
  - docs/litellm-target-spec.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DOCS-04]
tags: [helm, chart, external-litellm, corporate, variant-b, eso, negative-render]

must_haves:
  truths:
    - "helm template with values-external-litellm.yaml produces ZERO Deployment/openwhispr-litellm documents"
    - "helm template with values-external-litellm.yaml produces ZERO Service/openwhispr-litellm documents"
    - "api Deployment env LITELLM_BASE_URL == .Values.litellm.externalBaseUrl when litellm.embedded=false"
    - "worker Deployment env LITELLM_BASE_URL == .Values.litellm.externalBaseUrl when litellm.embedded=false"
    - "ExternalSecret data block does NOT include HF_TOKEN ref when bundledAi.enabled=false (corporate path)"
    - "helm test (templates/tests/first-launch-slo.yaml) does NOT require PYANNOTE_API_KEY — verified by render-time assertion that test pod env list does not include it"
    - "docker-compose.external-litellm.yml brings up api+web+worker+infra WITHOUT an embedded litellm service; api env contains LITELLM_BASE_URL pointing at corporate-supplied URL"
    - "values-corporate-litellm.yaml retained as deprecated alias of values-external-litellm.yaml (header comment) for backward-compat"
  artifacts:
    - path: "charts/openwhispr/examples/values-external-litellm.yaml"
      provides: "Variant B canonical overlay"
    - path: "docker-compose.external-litellm.yml"
      provides: "Variant B compose entrypoint"
    - path: ".env.external.example"
      provides: "Variant B env scaffold with LITELLM_BASE_URL + LITELLM_VIRTUAL_KEY notes"
    - path: "charts/openwhispr/tests/corporate_litellm_test.yaml"
      provides: "Negative-render + LITELLM_BASE_URL wiring assertions"
    - path: "examples/README.md"
      contains: "Variant B — Quick start"
  key_links:
    - from: "charts/openwhispr/templates/litellm-deployment.yaml"
      to: ".Values.litellm.embedded"
      via: "outer guard short-circuits to zero docs when false (existing, verify untouched)"
      pattern: "if \\.Values\\.litellm\\.embedded"
    - from: "charts/openwhispr/templates/api-deployment.yaml"
      to: ".Values.litellm.externalBaseUrl"
      via: "env LITELLM_BASE_URL value when litellm.embedded=false"
      pattern: "LITELLM_BASE_URL.*externalBaseUrl"
    - from: "charts/openwhispr/templates/externalsecret.yaml"
      to: ".Values.bundledAi.enabled"
      via: "(established in 11-01) HF_TOKEN data block conditional"
      pattern: "if \\.Values\\.bundledAi\\.enabled"
---

# Plan 11-02: Variant B — external LiteLLM overlay hardening + negative-render coverage

<objective>
Promote the existing skeleton `values-corporate-litellm.yaml` to canonical `values-external-litellm.yaml`, add a complete operator bundle (compose + env scaffold + README block + docs cross-link), and lock test coverage with `hasDocuments: count: 0` negative-render assertions and LITELLM_BASE_URL wiring assertions that did not exist before. Verify Open Question #1 (helm-test diarization independence).

Purpose: Satisfies DEPLOY-01/02 corporate path; DOCS-04 (litellm-target-spec.md cross-link); resolves Pitfall #5 (missing negative-render coverage) and Open Question #1.

Output: 3 new operator-runnable artifacts (compose, values, env), 2 modified Deployment templates verified for env wiring, 4 new/extended unittest fixtures including the negative-render lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cloud-profile-refactor/11-RESEARCH.md
@.planning/phases/11-cloud-profile-refactor/11-PLAN.md
@.planning/phases/11-cloud-profile-refactor/11-01-PLAN.md
@CLAUDE.md
@charts/openwhispr/values.yaml
@charts/openwhispr/templates/api-deployment.yaml
@charts/openwhispr/templates/worker-deployment.yaml
@charts/openwhispr/templates/litellm-deployment.yaml
@charts/openwhispr/templates/externalsecret.yaml
@charts/openwhispr/examples/values-corporate-litellm.yaml
@charts/openwhispr/tests/litellm_test.yaml
@charts/openwhispr/tests/api_test.yaml
@charts/openwhispr/tests/helm_test_hook_test.yaml
@docker-compose.yml
@compose/litellm/litellm_config.yaml
@docs/litellm-target-spec.md

<interfaces>
Variant B chart contract:
- `litellm.embedded: false` → ZERO `Deployment/openwhispr-litellm` AND ZERO `Service/openwhispr-litellm` docs rendered
- `litellm.externalBaseUrl: "https://litellm.internal.example.com"` → api + worker Deployment env LITELLM_BASE_URL == this value
- `litellm.virtualKey: "sk-corp-..."` → api + worker Deployment env LITELLM_MASTER_KEY (or LITELLM_VIRTUAL_KEY, per existing convention — verify against current template) sourced from `.Values.litellm.virtualKey` OR existing Secret key
- `bundledAi.enabled: false` (Variant B default) → ESO data block does NOT include HF_TOKEN ref
- `secrets.pyannoteApiKey: ""` (optional) → helm-test probe pod env does NOT mark it required

Compose service set for `docker-compose.external-litellm.yml`:
- api, web, worker, migrate, postgres, pgbouncer, valkey, minio, traefik, otel-collector, grafana, loki, tempo, mimir
- NOT included: litellm service (NO embedded LiteLLM), speaches
- api/worker env carries `LITELLM_BASE_URL` from env-file
- A health-check note: the compose stack alone cannot prove LITELLM_BASE_URL reachability since corporate URL is operator-supplied; docs explain that.

.env.external.example required keys (delta from Variant A):
- DROP: nothing from Variant A's 12 keys (LITELLM_MASTER_KEY semantics shift — see litellm-target-spec.md for naming; verify exactly during implementation)
- ADD: `LITELLM_BASE_URL=https://litellm.example.com` (corporate)
- ADD: `LITELLM_VIRTUAL_KEY=sk-virtualkey-from-corp-litellm-admin`
- NOTE: OPENROUTER_API_KEY, OPENAI_API_KEY become "optional — only if your corporate LiteLLM does not have these model aliases configured"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — corporate_litellm_test.yaml + extended litellm_test.yaml/api_test.yaml for negative-render and env wiring</name>
  <files>charts/openwhispr/tests/corporate_litellm_test.yaml, charts/openwhispr/tests/litellm_test.yaml, charts/openwhispr/tests/api_test.yaml, charts/openwhispr/tests/helm_test_hook_test.yaml</files>
  <behavior>
    - corporate_litellm_test.yaml NEW: cases against `values-external-litellm.yaml` (file added in Task 2 — RED at this point because file does not exist OR temp-stub the file to skeleton):
      - `hasDocuments: count: 0` for `Deployment/openwhispr-litellm`
      - `hasDocuments: count: 0` for `Service/openwhispr-litellm`
      - api Deployment env LITELLM_BASE_URL value matches `.Values.litellm.externalBaseUrl`
      - worker Deployment env LITELLM_BASE_URL value matches `.Values.litellm.externalBaseUrl`
      - ExternalSecret rendered with `secretsMode=external`: data list does NOT contain `HF_TOKEN`
    - litellm_test.yaml EXTEND: existing cases preserved; add case "litellm.embedded=false short-circuits Deployment" — `set: {litellm: {embedded: false}}` + `hasDocuments: count: 0`.
    - api_test.yaml EXTEND: case "api LITELLM_BASE_URL respects externalBaseUrl" — `set: {litellm: {embedded: false, externalBaseUrl: "https://test-litellm.internal"}}` + assert env entry.
    - helm_test_hook_test.yaml EXTEND: assert helm-test pod env does NOT mark PYANNOTE_API_KEY as required (Open Question #1).
    - All NEW assertions must FAIL: corporate values file is still skeleton-only without explicit `litellm.embedded: false` and `externalBaseUrl` properly set; env wiring may already work but assertion file is new.
  </behavior>
  <action>Author the new corporate_litellm_test.yaml + extend 3 existing test files. Do NOT modify any chart template or example values file yet.</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f "tests/corporate_litellm_test.yaml" 2>&1 | grep -qE "FAIL|fail"</automated>
  </verify>
  <done>All RED assertions visible in helm-unittest output. Atomic commit: `test(11-02): add red negative-render + env-wiring assertions for external-litellm variant`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — values-external-litellm.yaml canonical overlay + values-corporate-litellm.yaml deprecation alias</name>
  <files>charts/openwhispr/examples/values-external-litellm.yaml, charts/openwhispr/examples/values-corporate-litellm.yaml</files>
  <behavior>
    - `values-external-litellm.yaml` NEW: canonical Variant B overlay. Sets `litellm.embedded: false`, `litellm.externalBaseUrl: "https://litellm.example.com"` (placeholder), `bundledAi.enabled: false`, `secrets.hfToken: ""`. Comments document each value with operator-actionable provenance. Includes a comment block explaining model-alias expectations (corporate LiteLLM must expose `whisper-large-v3`, `gpt-realtime`, `qwen3.6-plus` etc. per `compose/litellm/litellm_config.yaml` mapping — link to `docs/litellm-target-spec.md`).
    - `values-corporate-litellm.yaml` MODIFIED: top header comment marks deprecated alias; body content equals values-external-litellm.yaml (for backward-compat with any operator using the older name).
  </behavior>
  <action>Author the new canonical file; rewrite corporate file body to match (preserve filename for back-compat).</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f "tests/corporate_litellm_test.yaml" && helm lint charts/openwhispr --values charts/openwhispr/examples/values-external-litellm.yaml</automated>
  </verify>
  <done>corporate_litellm_test.yaml GREEN. Atomic commit: `feat(11-02): add canonical values-external-litellm.yaml + deprecate corporate alias`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Verify api/worker Deployment LITELLM_BASE_URL wiring + (if missing) hard-wire externalBaseUrl env</name>
  <files>charts/openwhispr/templates/api-deployment.yaml, charts/openwhispr/templates/worker-deployment.yaml</files>
  <behavior>
    - Confirm via render-diff that current templates already wire `LITELLM_BASE_URL` from `.Values.litellm.externalBaseUrl` when `litellm.embedded=false`. If yes, this task is verification-only and the assertion in Task 1 will go green without code changes.
    - If NOT wired (verify via `helm template ... | grep LITELLM_BASE_URL`), edit the api and worker Deployment env blocks to add a conditional: `{{- if not .Values.litellm.embedded }} - name: LITELLM_BASE_URL value: {{ .Values.litellm.externalBaseUrl | quote }} {{- end }}`. Preserve the existing embedded-mode env wiring (which routes to in-cluster service).
    - Run `helm template` snapshot diff to confirm only the conditional block changed.
  </behavior>
  <action>Read the current api-deployment.yaml + worker-deployment.yaml. If wiring exists, document in commit message "verification-only — env already wired correctly". If not, implement the conditional. Either path produces a GREEN unittest.</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f "tests/api_test.yaml" -f "tests/corporate_litellm_test.yaml"</automated>
  </verify>
  <done>api + worker env LITELLM_BASE_URL assertions GREEN against values-external-litellm.yaml. Atomic commit: `feat(11-02): wire api/worker litellm_base_url to externalbaseurl when embedded=false` OR `test(11-02): lock existing api/worker litellm_base_url wiring with regression test`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — Variant B operator bundle (compose + env scaffold + README block + docs)</name>
  <files>docker-compose.external-litellm.yml, .env.external.example, examples/README.md, docs/self-hosting.md, docs/litellm-target-spec.md</files>
  <behavior>
    - `docker-compose.external-litellm.yml` NEW: derived from `docker-compose.embedded-litellm.yml` (Plan 11-01) minus the litellm service. api/worker env carries `LITELLM_BASE_URL=${LITELLM_BASE_URL}` from .env. Optional `LITELLM_VIRTUAL_KEY` env support if corporate LiteLLM requires it.
    - `.env.external.example` NEW: 12 Variant-A keys plus `LITELLM_BASE_URL=https://litellm.example.com` and `LITELLM_VIRTUAL_KEY=` (commented-out optional). Comments explain: "Point at your existing corporate LiteLLM Proxy. Model aliases required: see docs/litellm-target-spec.md." OPENROUTER_API_KEY and OPENAI_API_KEY commented "optional unless your corporate LiteLLM lacks these aliases".
    - `examples/README.md` EXTEND: add "Quick start — Variant B (external corporate LiteLLM)" section. Compose path: `cp .env.external.example .env && docker compose -f docker-compose.external-litellm.yml up --wait`. Chart path: `helm install openwhispr ./charts/openwhispr -f ./charts/openwhispr/examples/values-external-litellm.yaml`. Banner: "Variant B assumes you have an existing internal LiteLLM Proxy. See docs/litellm-target-spec.md for the required model-alias contract."
    - `docs/self-hosting.md` EXTEND: add "Variant B — External corporate LiteLLM" section.
    - `docs/litellm-target-spec.md` EXTEND: add a "Variant B operator quick-reference" subsection cross-linking values-external-litellm.yaml + docker-compose.external-litellm.yml as the canonical operator entry points; reaffirm model-alias contract.
  </behavior>
  <action>Author the 2 new operator files; modify 3 existing docs. Verify compose config validates.</action>
  <verify>
    <automated>LITELLM_BASE_URL=https://example.com docker compose -f docker-compose.external-litellm.yml config -q && ! grep -E "^\s*litellm:\s*$" docker-compose.external-litellm.yml</automated>
  </verify>
  <done>Variant B bundle exists; compose validates; no embedded litellm service present. Atomic commit: `feat(11-02): ship variant b external-litellm operator bundle (compose + env + readme + docs)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: GREEN — Resolve Open Question #1: helm-test does not depend on PYANNOTE_API_KEY</name>
  <files>charts/openwhispr/templates/tests/first-launch-slo.yaml, charts/openwhispr/tests/helm_test_hook_test.yaml</files>
  <behavior>
    - Read `templates/tests/first-launch-slo.yaml` (the helm test probe). Verify that the test pod's curl-loop does NOT hit `/v1/audio/diarization`. If it does, gate that step on `.Values.secrets.pyannoteApiKey | ne ""`.
    - helm_test_hook_test.yaml assertion: helm-test pod manifest does NOT include PYANNOTE_API_KEY in its env list when `secrets.pyannoteApiKey=""`. If diarization-step probe is conditional on pyannoteApiKey, also assert step absent in default Variant A/B mode.
    - Document the resolution in `.planning/phases/11-cloud-profile-refactor/11-02-SUMMARY.md` under "Open Questions Resolved".
  </behavior>
  <action>Inspect, gate if necessary, assert in test. If file content already satisfies (test does not touch diarization), commit is "verification-only".</action>
  <verify>
    <automated>helm unittest charts/openwhispr -f "tests/helm_test_hook_test.yaml"</automated>
  </verify>
  <done>helm-test independence from PYANNOTE_API_KEY verified or established. Atomic commit: `test(11-02): assert helm-test probe does not require pyannote_api_key (open question 1)`.</done>
</task>

</tasks>

<verification>
- All 5 task verifications green
- `helm unittest charts/openwhispr` ≥ 120 (post-11-01 baseline) + new Variant B cases
- `pnpm -r test` no regression
- `docker compose -f docker-compose.external-litellm.yml config -q` green
- `helm lint charts/openwhispr --values charts/openwhispr/examples/values-external-litellm.yaml` green
- helm template against values-external-litellm.yaml renders ZERO openwhispr-litellm Deployment/Service docs
</verification>

<success_criteria>
- Variant B bundle exists as 3 new operator-runnable files
- Negative-render lock prevents regression of `litellm.embedded=false` short-circuit
- LITELLM_BASE_URL wiring verified or established
- ESO HF_TOKEN absence confirmed under Variant B (corporate ESO mode)
- Open Question #1 resolved
- No regression on chart or app tests
</success_criteria>

<output>
Create `.planning/phases/11-cloud-profile-refactor/11-02-SUMMARY.md` summarizing the Variant B bundle, negative-render assertion catalog, Open Question #1 resolution, and any deferred items.
</output>
