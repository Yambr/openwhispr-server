---
phase: 08.2-agent-stream-undici-dispatcher-fix
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/litellm-client/src/index.ts
  - packages/litellm-client/src/index.test.ts
autonomous: true
requirements: [SCALE-02, TEST-LOAD-01]
must_haves:
  truths:
    - "LitellmClient.chatCompletionsStream(req) exists and returns Promise<Dispatcher.ResponseData> with body as a Node Readable."
    - "The streaming method sets bodyTimeout: 0 on the undici.request call."
    - "The streaming method forwards req.signal (AbortSignal) to undici.request when provided."
    - "The streaming method serialises body with stream: true and stream_options.include_usage: true (preserving caller's extras.stream_options merge)."
    - "Non-2xx upstream responses throw LitellmUpstreamError carrying statusCode + bodyText, mirroring chatCompletions behaviour."
    - "The streaming method emits the SAME authHeaders + spend-logs metadata shape as chatCompletions (canonical {openwhispr_request_id})."
    - "The streaming method does NOT accept or pass a per-call dispatcher option — it relies on the process-wide SSRF dispatcher via setGlobalDispatcher (T-08.2-01 mitigation)."
    - "Coverage on the new method ≥ 90/90/90/90."
  artifacts:
    - path: "packages/litellm-client/src/index.ts"
      provides: "chatCompletionsStream method on the LitellmClient interface + implementation."
      contains: "chatCompletionsStream"
    - path: "packages/litellm-client/src/index.test.ts"
      provides: "≥ 5 RED→GREEN unit tests covering chatCompletionsStream contract."
      contains: "chatCompletionsStream"
  key_links:
    - from: "packages/litellm-client/src/index.ts chatCompletionsStream"
      to: "undici.request via doRequest"
      via: "doRequest(`${config.baseUrl}/v1/chat/completions`, { method:'POST', headers, body, bodyTimeout: 0, signal })"
      pattern: "bodyTimeout:\\s*0"
---

# Plan 08.2-01: Add chatCompletionsStream to @openwhispr/litellm-client

<objective>
Extend the shared LiteLLM client with a streaming chat-completions method that returns the raw `Dispatcher.ResponseData` so callers (specifically `apps/api/src/routes/agent/stream.ts`) can consume the upstream Node `Readable` body for SSE → NDJSON streaming. The method MUST inherit the process-wide SSRF dispatcher (no per-call dispatcher), forward `AbortSignal`, set `bodyTimeout: 0`, and surface non-2xx responses via the existing `LitellmUpstreamError`.

Purpose: Eliminate the `undici.fetch` failure class in agent-stream by consolidating onto the same `undici.request` code path that transcribe + reason already prove safe under load.

Output: One new method on `LitellmClient` + tests; no consumer code yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-agent-stream-undici-dispatcher-fix/08.2-RESEARCH.md
@.planning/phases/08.2-agent-stream-undici-dispatcher-fix/08.2-PLAN.md
@packages/litellm-client/src/index.ts
@packages/litellm-client/src/index.test.ts
@packages/litellm-client/src/errors.ts

<interfaces>
From packages/litellm-client/src/index.ts (existing — already in scope, do NOT redefine):

```typescript
export interface ChatCompletionRequest {
  model?: string;
  messages: Array<{ role: string; content: string }>;
  userId: string;
  requestId: string;
  extras?: Record<string, unknown>;
}

export interface LitellmClient {
  chatCompletions(req: ChatCompletionRequest): Promise<Dispatcher.ResponseData>;
  audioTranscriptions(args: AudioTranscriptionRequest): Promise<Dispatcher.ResponseData>;
  passthrough(path: string, args: PassthroughRequest): Promise<Dispatcher.ResponseData>;
  readonly baseUrl: string;
}
```

New surface to add (research §"Reference pattern (proposed chatCompletionsStream)"):

```typescript
export interface ChatCompletionsStreamRequest extends ChatCompletionRequest {
  signal?: AbortSignal;
  /** Optional override; defaults to 0 (no body timeout — long-lived SSE). */
  bodyTimeout?: number;
}

// On LitellmClient:
chatCompletionsStream(req: ChatCompletionsStreamRequest): Promise<Dispatcher.ResponseData>;
```

`LitellmUpstreamError(statusCode: number, bodyText: string)` already exists in `errors.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing unit tests for chatCompletionsStream</name>
  <files>packages/litellm-client/src/index.test.ts</files>
  <behavior>
    Add a new `describe("chatCompletionsStream", ...)` block in `packages/litellm-client/src/index.test.ts`. Use the existing MockAgent + setGlobalDispatcher fixture pattern already established in this file. ALL of the following tests MUST initially RED (method does not yet exist):

    - Test A "forwards stream:true and stream_options.include_usage:true in the request body": MockAgent intercept asserts the POST body to `/v1/chat/completions` is JSON with `stream: true` and `stream_options.include_usage: true`. Caller may pass `extras.stream_options.X` — assert it merges (X preserved alongside `include_usage: true`).
    - Test B "applies canonical authHeaders + spend-logs metadata": intercept asserts headers contain `authorization: Bearer <masterKey>`, `x-litellm-end-user-id: <userId>`, and `x-litellm-spend-logs-metadata` whose JSON parses to `{ openwhispr_request_id: <requestId> }` exactly (no extra keys per T-08.2-05 canonical decision).
    - Test C "sets bodyTimeout: 0": stub `opts.request` (the injected `doRequest`) with a vitest fn; assert the second arg passed to it contains `bodyTimeout: 0`.
    - Test D "forwards signal to undici.request when provided": stub `opts.request`; pass `signal: ac.signal`; assert the call options contain that exact signal reference. Then call `ac.abort()` and assert the AbortSignal seen by `doRequest` is the same instance (already-aborted check is enough — we don't need a live socket).
    - Test E "non-2xx upstream throws LitellmUpstreamError with statusCode + bodyText": MockAgent replies 502 with body `"upstream timed out"`. Assert `await client.chatCompletionsStream({...})` rejects with `LitellmUpstreamError` whose `status === 502` and message/body contains `"upstream timed out"`.
    - Test F "2xx upstream returns Dispatcher.ResponseData with Node Readable body (NOT consumed)": MockAgent replies 200 with an SSE-shaped body fixture (`data: {"id":"chat-1"...}\n\ndata: [DONE]\n\n`). Assert returned `res.statusCode === 200`, `res.body` is a Node Readable, and the method did NOT call `.text()` / `.json()` / pre-consume the body (use a flag attached to a custom passthrough stream).
    - Test G "does NOT accept or pass a per-call dispatcher option (T-08.2-01 mitigation)": stub `opts.request`; assert the second arg passed to `doRequest` has NO `dispatcher` key (`Object.hasOwn(opts, 'dispatcher') === false`).

    All seven tests RED on first run because `chatCompletionsStream` is not declared on the `LitellmClient` interface and not implemented.
  </behavior>
  <action>Append the new `describe("chatCompletionsStream", ...)` block to `packages/litellm-client/src/index.test.ts` with the seven tests above. Reuse the existing MockAgent helpers / `buildLitellmClient` factory in the file. Do NOT touch `index.ts` in this task. Run `pnpm --filter @openwhispr/litellm-client test --run` and confirm all seven tests fail with a method-not-defined / type error. Commit as a SINGLE atomic commit together with Task 2 (per CLAUDE.md TDD-01b: test commit accompanies fix commit in the same atomic commit).</action>
  <verify>
    <automated>pnpm --filter @openwhispr/litellm-client test --run -- chatCompletionsStream</automated>
  </verify>
  <done>Seven new tests exist in `packages/litellm-client/src/index.test.ts` and all fail with "chatCompletionsStream is not a function" or the TypeScript equivalent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement chatCompletionsStream</name>
  <files>packages/litellm-client/src/index.ts</files>
  <behavior>
    Implement `chatCompletionsStream` on `LitellmClient` exactly as described in research §"Reference pattern (proposed chatCompletionsStream)" so all seven RED tests from Task 1 turn GREEN. Specifically:

    - Add `ChatCompletionsStreamRequest` interface extending `ChatCompletionRequest` with optional `signal?: AbortSignal` and optional `bodyTimeout?: number`.
    - Add `chatCompletionsStream(req: ChatCompletionsStreamRequest): Promise<Dispatcher.ResponseData>` to the `LitellmClient` interface.
    - Implementation: resolve model via `req.model ?? config.defaultChatModel`; call `checkProviderKey(model)`; build body merging `req.extras` (with `stream_options` deep-merged so caller's `include_usage`-companion keys are preserved alongside the forced `include_usage: true`) + `model` + `messages` + `user: req.userId` + `stream: true` + `stream_options: { include_usage: true, ...(req.extras?.stream_options ?? {}) }`. Call `doRequest(url, { method: 'POST', headers: { ...authHeaders(req.userId, req.requestId), 'content-type': 'application/json' }, body, bodyTimeout: req.bodyTimeout ?? 0, ...(req.signal ? { signal: req.signal } : {}) })`. Inline non-2xx → `LitellmUpstreamError` mapping (do NOT call `ensureOk` because that returns the response unchanged on 2xx but consumes body on 4xx via `.text()` — same behaviour is needed here for the error path; safest path is to inline the same check so we control the 2xx path NEVER touching the body). Return the raw `Dispatcher.ResponseData` on 2xx.
    - Export `ChatCompletionsStreamRequest` from the package barrel (end of file, where existing types are re-exported).
  </behavior>
  <action>Edit `packages/litellm-client/src/index.ts`: (1) extend the `LitellmClient` interface with the new method signature; (2) add `ChatCompletionsStreamRequest` interface; (3) implement `chatCompletionsStream` inside the returned object literal in `buildLitellmClient`; (4) re-export `ChatCompletionsStreamRequest` from the barrel. Do NOT pass a `dispatcher:` option to `doRequest` under any circumstance (T-08.2-01). Do NOT touch any other method. Run `pnpm --filter @openwhispr/litellm-client test --run` and confirm all seven new tests now GREEN. Then run `pnpm --filter @openwhispr/litellm-client test --run -- --coverage` and confirm coverage on the new method ≥ 90/90/90/90. Single atomic commit subject: `feat(08.2-01): add chatCompletionsStream to @openwhispr/litellm-client`.</action>
  <verify>
    <automated>pnpm --filter @openwhispr/litellm-client test --run && pnpm --filter @openwhispr/litellm-client test --run -- --coverage 2>&1 | grep -E "All files.*\| *([0-9]{2,3}\.[0-9]+|100) *\|" | awk '{print}' && grep -c "chatCompletionsStream" packages/litellm-client/src/index.ts</automated>
  </verify>
  <done>All seven tests GREEN; coverage on `packages/litellm-client/src/index.ts` ≥ 90% on lines/branches/functions/statements; `chatCompletionsStream` exported on the interface and present in the barrel; ZERO `dispatcher:` occurrences in `packages/litellm-client/src/index.ts` (grep -c "dispatcher:" returns 0).</done>
</task>

</tasks>

<verification>
- `pnpm --filter @openwhispr/litellm-client test --run` — all tests green, including the seven new ones.
- `pnpm --filter @openwhispr/litellm-client test --run -- --coverage` — coverage on `index.ts` ≥ 90/90/90/90.
- Static check: `grep -n "dispatcher:" packages/litellm-client/src/index.ts | grep -v "^#"` returns nothing (T-08.2-01 regression guard).
- Tests A–G all assert canonical behaviour; no test stubs `undici.request` at a level that bypasses the MockAgent dispatch contract.
</verification>

<success_criteria>
- `chatCompletionsStream` method exists on `LitellmClient`, returns `Promise<Dispatcher.ResponseData>`, body is a Node `Readable` left unconsumed on 2xx.
- `bodyTimeout: 0` and `signal` are forwarded to `undici.request` per Task 2 contract.
- Non-2xx upstreams throw `LitellmUpstreamError` carrying `statusCode + bodyText`.
- No per-call dispatcher option is introduced (SSRF gate preserved).
- Coverage on diff ≥ 90/90/90/90.
- All commits land as a single atomic commit pair (test + impl) per CLAUDE.md TDD-01b.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-agent-stream-undici-dispatcher-fix/08.2-01-SUMMARY.md` recording: file diff size, test count, coverage numbers, and any surprises encountered (expected: none — this is a pure additive method that mirrors `chatCompletions`).
</output>
