# Plan 32-04 — E2E + docs + ROADMAP/REQUIREMENTS prose fix + SUMMARY/COVERAGE

**Status:** ready
**Depends on:** 32-01, 32-02, 32-03
**Atomic commits:** 1 — docs+E2E batched per CONTEXT instruction.

## Goal

Land: (a) E2E test booting real compose stack and asserting `42501` on un-wrapped INSERT; (b) `docs/security.md` "RLS posture" section; (c) ROADMAP + REQUIREMENTS prose fix from "11 tables × 88 cases" → "16 tables × 128 cases"; (d) `32-SUMMARY.md` + `32-COVERAGE.md` artifacts; (e) `32-DEFERRED.md` placeholder for any apps/api test failures discovered.

## E2E shape — `tests/e2e/rls-fail-closed.spec.ts`

Per DISCIPLINE Rule 3 + LOCKER constraints, do NOT add a synthetic test-only route to production. Approach:

1. Boot compose stack with `make e2e-up` or equivalent (mirror lockers.test.ts pattern; or skip compose-up if a direct PG container suffices — given the question is purely RLS-at-the-database, a direct testcontainer instance is acceptable as long as it applies all migrations).
2. Connect to PG as `openwhispr_app` (no withTenant, no GUC).
3. Attempt `INSERT INTO notes (tenant_id, owner_user_id, type, title) VALUES (gen_random_uuid(), gen_random_uuid(), 'note', 'leak')`.
4. Assert error code `42501`.
5. Verify `SELECT * FROM notes` (still no GUC) returns 0 rows.

This is real-stack proof: real PG, real role, real RLS. The 128-case unit suite owns the per-cell coverage; this E2E owns the integration assertion.

## docs/security.md additions

Append section "## RLS posture (Phase 32)":
- Before Phase 32: role-default GUC bound `app.tenant_id` to `00000000-...` for every connection. Any query outside `withTenant()` silently bound to default tenant. **Fail-open.**
- After Phase 32: role default RESET, column DEFAULTs DROPPED on Better Auth tables, RLS bodies require non-null GUC. Any query outside `withTenant()`:
  - SELECT → 0 rows
  - INSERT → PG 42501 raised
  - UPDATE/DELETE → 0 rows affected
- Migration: `0018_rls_fail_closed.sql`.
- Threat model: TM-1 (default-tenant leakage) → closed.

## ROADMAP + REQUIREMENTS prose fix

`.planning/ROADMAP.md` line 81: replace "Property test (88 = 11 tenant-scoped tables × 4 ops × 2 ctx)" → "Property test (128 = 16 tenant-scoped tables × 4 ops × 2 ctx)". Same correction in `.planning/REQUIREMENTS.md` CRIT-FIX-01 row.

Also flip Phase 32 from `[ ]` to `[x]` in ROADMAP. Add closure prose: `**Phase 32: RLS fail-closed (CR-7 closure)** — CLOSED YYYY-MM-DD. Migration 0018... <evidence summary>`.

## 32-DEFERRED.md

Empty placeholder pre-populated. Will be filled if any `apps/api/**/*.test.ts` fails because routes legitimately read empty result sets under fail-closed. Per CLAUDE.md Hard Rule 1 — NEVER edit production route code in this phase; log to deferred for Phase 41.

## 32-SUMMARY.md + 32-COVERAGE.md

Standard artifacts. SUMMARY enumerates all atomic commits with SHAs; COVERAGE captures per-file before/after on the four axes.

## Commit

`feat(32-04): E2E rls-fail-closed.spec.ts + docs/security.md RLS posture + ROADMAP 16x128 fix + Phase 32 closure artifacts`
