---
phase: 19.1-reset-mail-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/auth.ts
  - apps/api/tests/unit/__tests__/auth-send-reset-password.test.ts
  - apps/api/tests/unit/__tests__/auth-locale-and-enqueue.test.ts
  - tests/e2e-cjm/features/password-reset.feature
autonomous: true
requirements:
  - SR-19.1.1
  - SR-19.1.4 (narrow happy-path bar — mailpit-arrival assertion only; Plan 02 extends to full round-trip)
user_setup: []

must_haves:
  truths:
    - "Better Auth `sendResetPassword` hook is registered under `emailAndPassword` and routes through `opts.enqueueEmail` with `template_id: \"password_reset\"` when DI is wired."
    - "When `opts.enqueueEmail` is absent, the hook falls back to inline `email.send` (backward-compat with non-API call sites)."
    - "Locale resolution mirrors `sendVerificationEmail`: `user.locale === \"ru\" ? \"ru\" : \"en\"`."
    - "tenant_id resolution mirrors `sendVerificationEmail`: `user.tenantId ?? \"00000000-0000-0000-0000-000000000000\"`."
    - "Variables payload contains exactly: `{ name, reset_url, url, expires_minutes: 60 }` (D-02 + D-03 defensive alias)."
    - "en+ru template-placeholder parity: the set of `{token}` placeholders in `apps/worker/src/i18n/locales/en/email/password_reset/{subject,body}.txt` is identical to the ru counterpart (D-07 verification gate; ROADMAP SC #2)."
    - "`@cjm-3.1` scenario no longer carries `@expected-red @after-phase-19.1` and passes end-to-end against the compose stack: signup → request reset → mailpit returns a `password_reset` email within 30 s."
    - "Zero `--no-verify` across all commits in this plan (D-08)."
  artifacts:
    - path: apps/api/src/auth.ts
      provides: "sendResetPassword hook registered under emailAndPassword block"
      contains: "sendResetPassword"
    - path: apps/api/tests/unit/__tests__/auth-send-reset-password.test.ts
      provides: "RED-then-GREEN unit coverage for hook routing (enqueueEmail path + inline fallback path + variables contract + locale + tenant_id)"
      min_lines: 150
    - path: tests/e2e-cjm/features/password-reset.feature
      provides: "@cjm-3.1 scenario without @expected-red @after-phase-19.1 tags"
      contains: "@cjm-3.1"
  key_links:
    - from: apps/api/src/auth.ts
      to: "opts.enqueueEmail"
      via: "sendResetPassword closure"
      pattern: "template_id:\\s*\"password_reset\""
    - from: apps/api/src/auth.ts
      to: "apps/worker/src/i18n/locales/{en,ru}/email/password_reset/*"
      via: "template_id string match in worker email-delivery job"
      pattern: "password_reset"
    - from: tests/e2e-cjm/features/password-reset.feature
      to: "@cjm-3.1 scenario"
      via: "tag flip — remove @expected-red @after-phase-19.1"
      pattern: "@cjm-3\\.1\\s*$"
---

<objective>
Wire Better Auth's `sendResetPassword` lifecycle hook (BA 1.6.9 source-of-truth: `password.mjs:42` reads `ctx.context.options.emailAndPassword?.sendResetPassword`) so that `/api/auth/forget-password` enqueues a `password_reset` email through the existing worker BullMQ pipeline, and flip the `@cjm-3.1` scenario from `@expected-red @after-phase-19.1` to GREEN against the live compose stack.

Purpose: Close the narrow happy-path bar for ROADMAP SR-19.1.4 — a password-reset email actually arrives in mailpit after a `forget-password` request. This is the first of two plans in Phase 19.1; Plan 02 extends the scenario to the full reset round-trip (URL parse → POST `/reset-password` → sign-in).

Output: One new unit-test file (`auth-send-reset-password.test.ts`), one edit to `apps/api/src/auth.ts` adding the hook, one feature-tag flip, and an optional REFACTOR commit if the hook diff exposes natural extract-helper opportunities shared with `sendVerificationEmail`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-reset-mail-wiring/19.1-CONTEXT.md
@.planning/phases/19.1-reset-mail-wiring/19.1-DISCUSSION-LOG.md
@CLAUDE.md
@apps/api/src/auth.ts
@apps/api/tests/unit/__tests__/auth-send-verification-email.test.ts
@apps/api/tests/unit/__tests__/auth-locale-and-enqueue.test.ts
@apps/worker/src/i18n/template-renderer.ts
@apps/worker/src/i18n/locales/en/email/password_reset/subject.txt
@apps/worker/src/i18n/locales/en/email/password_reset/body.txt
@apps/worker/src/i18n/locales/ru/email/password_reset/subject.txt
@apps/worker/src/i18n/locales/ru/email/password_reset/body.txt
@tests/e2e-cjm/features/password-reset.feature
@tests/e2e-cjm/steps/password-reset.steps.ts

<interfaces>
<!-- BA 1.6.9 vendored source-of-truth pin (D-06). -->
<!-- File: node_modules/.pnpm/better-auth@1.6.9_*/node_modules/better-auth/dist/api/routes/password.mjs -->
<!-- Lines 42-73: hook gate + signature -->

The BA reset-password route invokes the hook with:
  sendResetPassword: async (
    { user, url, token }: { user: { id, email, name?, locale?, tenantId? }, url: string, token: string },
    request?: Request
  ) => Promise<void>

Existing prior-art pattern at `apps/api/src/auth.ts:318-351` (sendVerificationEmail closure):
  - reads `opts.enqueueEmail` from BuildAuthOptions
  - if present: `await opts.enqueueEmail({ tenant_id, to, template_id, locale, variables, request_id })`
  - else: `await email.send({ to, subject, text, html })` inline fallback
  - locale = user.locale === "ru" ? "ru" : "en"
  - tenant_id = user.tenantId ?? "00000000-0000-0000-0000-000000000000"

Existing template files (verified in tree per CONTEXT.md):
  apps/worker/src/i18n/locales/en/email/password_reset/{subject.txt,body.txt,body.html}
  apps/worker/src/i18n/locales/ru/email/password_reset/{subject.txt,body.txt,body.html}

KNOWN_TEMPLATE_IDS at `apps/worker/src/i18n/template-renderer.ts:42-43` already includes `password_reset`.

Canonical cjm harness invocation (verified from repo Makefile, NOT the empty `tests/e2e-cjm/package.json`):
  `E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"`
The Makefile target boots a hermetic compose project `-p e2e-cjm`, waits for readiness, then runs `pnpm exec playwright test --grep "$SCENARIO" --config tests/e2e-cjm/playwright.config.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — unit test for sendResetPassword hook routing</name>
  <files>apps/api/tests/unit/__tests__/auth-send-reset-password.test.ts</files>
  <behavior>
    Mirror `auth-locale-and-enqueue.test.ts` mocking strategy (process-boundary mocks for `better-auth`, `better-auth/adapters/drizzle`, `@openwhispr/email`). Drive `buildAuth({ db, email, enqueueEmail })` and assert against `auth.options.emailAndPassword.sendResetPassword`.

    Test cases (per D-01, D-02, D-03):
    - Test 1: hook is registered under `emailAndPassword.sendResetPassword` (NOT under top-level or `emailVerification`). Use `auth.options.emailAndPassword.sendResetPassword` typeof === "function".
    - Test 2: when `enqueueEmail` is provided and user has `locale: "en"`, hook calls `enqueueEmail` exactly once with `{ tenant_id: "00000000-0000-0000-0000-000000000000" (when user.tenantId omitted), to: user.email, template_id: "password_reset", locale: "en", variables: { name: user.name ?? user.email, reset_url: url, url, expires_minutes: 60 }, request_id: <any non-empty string> }`.
    - Test 3: when `user.locale === "ru"`, locale field === "ru".
    - Test 4: when `user.locale` is undefined or unrecognized, locale defaults to "en".
    - Test 5: when `user.tenantId` is provided, it is forwarded as `tenant_id`.
    - Test 6: when `user.name` is undefined, `variables.name` falls back to `user.email`.
    - Test 7: `variables.reset_url === url` AND `variables.url === url` (defensive alias per D-03).
    - Test 8: `variables.expires_minutes === 60` (literal, per D-02; BA default `resetPasswordTokenExpiresIn = 3600s`).
    - Test 9: when `enqueueEmail` is omitted, hook calls `email.send` with `{ to: user.email, subject: <non-empty string>, text: <contains url>, html: <contains url> }`. Do NOT assert exact subject copy — that lives in the worker template.
    - Test 10: when `enqueueEmail` throws, the error propagates (BA logs + sets reset state to fail; matches Pitfall #4 of sendVerificationEmail prior-art).

    The test file MUST be RED until Task 2 lands the hook. Confirm RED by running `pnpm --filter @openwhispr/api test auth-send-reset-password` — expect failure ("sendResetPassword is not a function" or equivalent).
  </behavior>
  <action>Create the new test file. Mirror imports, mock surface, and `AuthOptionsRuntime` interface from `auth-locale-and-enqueue.test.ts:16-90`. Extend `AuthOptionsRuntime` with `emailAndPassword?: { sendResetPassword?: (args: { user: { id?: string; email: string; name?: string; locale?: string; tenantId?: string }; url: string; token: string }, request?: unknown) => Promise<void> }`. Each `it()` block constructs a fresh `enqueueCalls` array and an `EmailService` stub with a `sent` capture array, then invokes the hook closure directly via `auth.options.emailAndPassword.sendResetPassword({ user, url, token }, undefined)`. Use `expect.objectContaining` for the enqueue payload assertion to permit a forward-compatible `request_id` shape. Commit as a single RED commit.</action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test auth-send-reset-password 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, contains the 10 cases above, file length ≥ 150 lines (per frontmatter `min_lines`), and `pnpm --filter @openwhispr/api test auth-send-reset-password` exits non-zero with failures pointing at the missing hook (NOT at a syntax error). Commit message: `test(19.1-01): add RED unit coverage for sendResetPassword hook routing`. ZERO `--no-verify`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement sendResetPassword hook in auth.ts + verify en+ru template parity</name>
  <files>apps/api/src/auth.ts</files>
  <behavior>
    After this task, every assertion from Task 1 must pass. The hook closure lives INSIDE the `emailAndPassword` block (per D-01, BA 1.6.9 `password.mjs:42`), not under `emailVerification` and not at the top level. Additionally, en+ru template-placeholder parity is verified before declaring the task done (D-07 / ROADMAP SC #2).
  </behavior>
  <action>
    Edit `apps/api/src/auth.ts`. Inside the existing `emailAndPassword: { enabled: true, requireEmailVerification: ... }` block (currently ending at line ~300), add a new property `sendResetPassword` mirroring the prior-art shape of `emailVerification.sendVerificationEmail` at lines 318-351.

    Implementation shape (per D-01, D-02, D-03; do NOT inline code in this action — the executor mirrors the verification-hook prior-art line-by-line):
      - Signature: `async ({ user, url, token }: { user: { id?: string; email: string; name?: string; locale?: string; tenantId?: string }; url: string; token: string }, _request?: unknown) => Promise<void>`.
      - If `opts.enqueueEmail` is truthy: resolve `locale` (`user.locale === "ru" ? "ru" : "en"`), resolve `tenant_id` (`user.tenantId ?? "00000000-0000-0000-0000-000000000000"`), call `await opts.enqueueEmail({ tenant_id, to: user.email, template_id: "password_reset", locale, variables: { name: user.name ?? user.email, reset_url: url, url, expires_minutes: 60 }, request_id: crypto.randomUUID() })` and `return`.
      - Else: `await email.send({ to: user.email, subject: "Reset your OpenWhispr password", text: \`Click to reset: ${url}\`, html: \`<p>Click to reset: <a href="${url}">${url}</a></p>\` })`.

    Add a Phase 19.1 / Plan 01 comment block above the hook citing BA 1.6.9 `password.mjs:42` (the canonical key gate) and D-01..D-03 for traceability. Keep the comment style consistent with the existing Phase 13 / Plan 01 comment block at lines 301-317.

    Run `pnpm --filter @openwhispr/api test auth-send-reset-password` — MUST exit 0. Run `pnpm --filter @openwhispr/api typecheck` — MUST exit 0. Run `pnpm --filter @openwhispr/api test` (full unit suite) — MUST exit 0 with no new failures vs Phase 19 baseline.

    THEN verify en+ru template-placeholder parity (D-07 / ROADMAP SC #2 gate). Run both:
      diff <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/en/email/password_reset/body.txt | sort -u) \
           <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/ru/email/password_reset/body.txt | sort -u)
      diff <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/en/email/password_reset/subject.txt | sort -u) \
           <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/ru/email/password_reset/subject.txt | sort -u)
    Both diffs MUST be empty (zero output, exit 0). If non-empty: HALT. Surface as a scope-expansion gap in `.planning/deferred-items.md` per CLAUDE.md Hard Rule #1 — do NOT invent or rewrite template content under test-fix pressure. A future targeted i18n-parity phase will close it.

    Commit message: `feat(19.1-01): wire sendResetPassword hook via opts.enqueueEmail`.

    Constraint: Do NOT touch `auth-locale-and-enqueue.test.ts` unless typecheck fails on a shared interface — in that case, extend the runtime interface declaration, never narrow it. Do NOT touch `auth-send-verification-email.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test auth-send-reset-password 2>&1 | tail -10 && pnpm --filter @openwhispr/api typecheck 2>&1 | tail -5 && pnpm --filter @openwhispr/api test 2>&1 | tail -10 && diff <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/en/email/password_reset/body.txt | sort -u) <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/ru/email/password_reset/body.txt | sort -u) && diff <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/en/email/password_reset/subject.txt | sort -u) <(grep -oE "\{[a-z_]+\}" apps/worker/src/i18n/locales/ru/email/password_reset/subject.txt | sort -u)</automated>
  </verify>
  <done>All 10 Task-1 assertions GREEN. Full `@openwhispr/api` unit suite GREEN. `pnpm typecheck` exit 0. en+ru placeholder-parity diffs both empty (subject.txt + body.txt). Diff scope = single edit inside `emailAndPassword` block of `apps/api/src/auth.ts` (one new property + leading comment block, ~30-45 lines). Coverage on the new closure ≥ 90/90/90/90 — verify via `pnpm --filter @openwhispr/api coverage` if available, else rely on the 10 cases above which exercise both enqueue and inline-fallback branches plus locale + tenant_id + name + expires + alias permutations. ZERO `--no-verify`. If parity diff fails → HALT, do not edit templates.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — flip @cjm-3.1 from @expected-red and verify against compose stack</name>
  <files>tests/e2e-cjm/features/password-reset.feature</files>
  <action>
    Edit `tests/e2e-cjm/features/password-reset.feature:6`. Remove the `@expected-red @after-phase-19.1` tags from the `@cjm-3.1` scenario line, leaving exactly `  @cjm-3.1` (preserve the two-space indent so cucumber tag scoping is unchanged). Do NOT modify the `@cjm-3.2` scenario or any step bindings.

    Then run the cjm e2e suite against the compose stack using the canonical Makefile target (verified from repo `Makefile`; `tests/e2e-cjm/package.json` is intentionally script-less — the harness is driven by `make` + a hermetic `-p e2e-cjm` compose project):

      E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"

    This boots the hermetic compose project (api + worker + mailpit + valkey + postgres + pgbouncer), waits for readiness via `tests/e2e-cjm/support/wait-for-readiness.ts`, then runs `pnpm exec playwright test --grep "@cjm-3.1" --config tests/e2e-cjm/playwright.config.ts`. Verify the scenario passes within the existing 30-second mailpit poll window.

    If the scenario fails: STOP. Do NOT modify production code to "make it pass" (CLAUDE.md Hard Rule #1). Diagnose: re-read `apps/worker/src/jobs/email-delivery.ts` to confirm the template-id dispatch path; re-read `apps/worker/src/i18n/template-renderer.ts` to confirm `password_reset` lookup; check mailpit logs via `docker compose -p e2e-cjm logs mailpit`; check api+worker logs. If the failure points at a missing piece NOT covered by D-02..D-07 (e.g., template file missing in image, worker not consuming the queue), HALT and surface the gap in a comment on the PLAN — do not silently patch.

    Commit message: `test(19.1-01): flip @cjm-3.1 to GREEN — password-reset round-trip via mailpit`.

    Constraint: ZERO `--no-verify`. Sequential — no parallel subagents on this tree. English-only source.
  </action>
  <verify>
    <automated>grep -E "^\s+@cjm-3\.1\s*$" tests/e2e-cjm/features/password-reset.feature && ! grep -E "@expected-red.*@cjm-3\.1|@cjm-3\.1.*@expected-red|@after-phase-19\.1" tests/e2e-cjm/features/password-reset.feature</automated>
  </verify>
  <done>The `@cjm-3.1` line in `tests/e2e-cjm/features/password-reset.feature` reads exactly `  @cjm-3.1` with no `@expected-red` or `@after-phase-19.1` neighbours. The scenario passes end-to-end against the hermetic `-p e2e-cjm` compose project — mailpit receives a `password_reset` email for the freshly signed-up user within 30 s. ZERO `--no-verify`. Note: the Then-step in this plan still asserts only mailpit arrival; Plan 02 extends Then-steps to parse the URL + POST `/reset-password` + sign-in.</done>
</task>

<task type="auto">
  <name>Task 4 (OPTIONAL): REFACTOR — extract shared locale + tenant_id resolution helper</name>
  <files>apps/api/src/auth.ts</files>
  <action>
    OPTIONAL — execute ONLY if Task 2 produced visible duplication between `sendResetPassword` and `sendVerificationEmail` (both now resolve `locale` and `tenant_id` identically). If duplication is < 4 lines, skip this task entirely.

    If executing: extract a private module-scope helper `resolveEnqueueIdentity(user: { locale?: string; tenantId?: string }): { locale: "en" | "ru"; tenant_id: string }` near the top of `apps/api/src/auth.ts` (after the `fallbackLog` export, before `buildAuth`). Update BOTH `sendVerificationEmail` and `sendResetPassword` closures to call it. Re-run full unit suite + typecheck.

    Constraint: Refactor MUST be behaviour-preserving. ALL existing tests (verification + reset + locale-and-enqueue) MUST stay GREEN with no source edits. If ANY test goes red, revert this task — the refactor failed, not the production code.

    Commit message: `refactor(19.1-01): extract resolveEnqueueIdentity helper shared by verification + reset hooks`.
  </action>
  <verify>
    <automated>pnpm --filter @openwhispr/api test 2>&1 | tail -10 && pnpm --filter @openwhispr/api typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Either (a) skipped because duplication was trivial, OR (b) helper extracted, all unit tests + typecheck GREEN, ZERO `--no-verify`. If revert was needed, commit must NOT exist (clean working tree).</done>
</task>

</tasks>

<verification>
- `apps/api/src/auth.ts` contains a `sendResetPassword` property INSIDE the `emailAndPassword` object literal (not under `emailVerification`, not at top level). `grep -nE "sendResetPassword" apps/api/src/auth.ts` returns at least one hit on the property name plus a comment block referencing D-01 / BA 1.6.9 `password.mjs`.
- `apps/api/tests/unit/__tests__/auth-send-reset-password.test.ts` exists, is ≥ 150 lines, and the full `@openwhispr/api` unit suite is GREEN with no new RED tests.
- `pnpm --filter @openwhispr/api typecheck` exit 0.
- `tests/e2e-cjm/features/password-reset.feature` line 6 reads `  @cjm-3.1` with NO `@expected-red` and NO `@after-phase-19.1` tags adjacent.
- `@cjm-3.1` scenario passes end-to-end against the hermetic `-p e2e-cjm` compose project via `E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"`.
- en+ru placeholder-parity diffs (subject.txt + body.txt) both empty (ROADMAP SC #2 gate verified, NOT assumed).
- Every commit in this plan was created WITHOUT `--no-verify` (D-08).
- Per-task coverage ≥ 90/90/90/90 floor not regressed on `apps/api/src/auth.ts` diff.
</verification>

<success_criteria>
1. RED commit exists for the unit test (single commit; pre-existing-RED handling per D-39 does NOT apply because this test is new).
2. GREEN commit lands the hook in `apps/api/src/auth.ts` AND verifies en+ru placeholder parity before commit (HALT path if parity fails).
3. GREEN commit flips `@cjm-3.1` and the scenario passes against the live hermetic compose stack via the canonical `make e2e-cjm` target.
4. Optional REFACTOR commit either does not exist (skipped) or lands cleanly with all tests still GREEN.
5. Total commits: 3 (mandatory RED+GREEN+TAG-FLIP) or 4 (with optional refactor).
6. ZERO `--no-verify` across every commit.
7. Sequential execute: NO parallel subagents on the shared working tree.
8. `pnpm --filter @openwhispr/api test` + `typecheck` GREEN at end of plan.
9. Plan 02 unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-reset-mail-wiring/19.1-01-SUMMARY.md` covering: commits landed (SHAs + messages), files touched, observable truths verified against the live codebase (one bullet per `must_haves.truths` entry with the verification command + observed output — including the en+ru parity diff commands and their empty stdout), any deviations from CONTEXT.md and why, and any deferred items surfaced (especially any parity-gap HALT).
</output>
</output>
