# Phase 19.1 — Discussion Log

**Date:** 2026-05-15
**Mode:** compact discuss (Better Auth source-of-truth verification + Phase 13 prior-art parity; no advisor spawned — questions answered directly from vendored BA 1.6.9 source)
**User pacing:** "по плану" (continue per plan); explicit Phase 19.1 selection from Phase 19 closure.

## Trigger

Phase 19 closure declared v2.1 milestone CLOSED-WITH-PARTIAL-DEBT. ROADMAP carved Phase 19.1 (commit `4b40a7a`) as the first of 4 downstream-code phases addressing repointed `@cjm` `@expected-red` tags. User: "по плану" → start 19.1 discuss-phase.

## Scope reality check (decisive — preceded gray-area selection)

Codebase scout revealed ROADMAP SR-19.1.2 + SR-19.1.3 are STALE:

- `apps/worker/src/i18n/locales/{en,ru}/email/password_reset/*` template files **already exist** in tree (Phase 13).
- `apps/worker/src/i18n/template-renderer.ts:42-43` already lists `password_reset` in `KNOWN_TEMPLATE_IDS`.
- Worker BullMQ dispatches by `template_id` agnostically — no per-template branching needed.

The phase reduces to: **add `sendResetPassword` hook to auth.ts + flip `@cjm-3.1` from `@expected-red` to GREEN + (optionally) extend the scenario to full round-trip**.

## Gray areas presented + answered (all 4 selected by user)

1. **Hook location (emailAndPassword vs top-level)** — Answered from BA 1.6.9 source: `password.mjs:42` confirms `ctx.context.options.emailAndPassword?.sendResetPassword`. Unlike `sendVerificationEmail`, reset BELONGS under `emailAndPassword`. Locked as D-01.
2. **Template variable contract** — Existing template uses `{name} + {reset_url} + {expires_minutes}`. BA default `resetPasswordTokenExpiresIn = 3600s` (password.mjs:64). Accept default, send `expires_minutes: 60` literal. Locked as D-02.
3. **Feature scope: mailpit-only vs full round-trip** — Two-plan split: 19.1-01 closes mailpit-arrival GREEN; 19.1-02 extends to full reset round-trip per SR-19.1.5. Locked as D-04.
4. **Variables alias** — Mirror Phase 13 defensive pattern: send both `reset_url` AND `url`. Locked as D-03.

## Decisions captured (D-01..D-10)

All in CONTEXT.md. Key locks:

- 2 plans sequential `19.1-01 → 19.1-02`
- ~6-8 atomic commits
- Hook under `emailAndPassword` (BA 1.6.9 verified)
- Template variables: name, reset_url, url, expires_minutes
- BA default 1h expiry (no operator knob)
- TDD D-39 doctrine carries (downstream-code phase, not production-fix; Hard Rule #1 IN FORCE)
- ZERO `--no-verify`
- en+ru parity already satisfied (no template work)

## Deferred ideas

- SR-19.1b per-file search_path test-isolation — v3 test-infra hardening
- Custom `resetPasswordTokenExpiresIn` operator knob — not requested
- React-email overhaul — out of scope
- Multi-device session reset invalidation — D-LOAD-EV territory

## Claude's discretion items

- Compact discuss (no advisor spawned) — appropriate because all 4 gray areas resolved from BA vendored source + Phase 13 prior-art evidence.
- ~1.5-2h total Phase 19.1 runtime estimate.
- Phase 19.1 close advances 19.2/19.3/19.4 readiness (independent phases, no cross-dependencies).
