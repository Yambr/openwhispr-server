---
phase: 19.1-reset-mail-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 19.1-01
files_modified:
  - tests/e2e-cjm/features/password-reset.feature
  - tests/e2e-cjm/steps/password-reset.steps.ts
autonomous: true
requirements:
  - SR-19.1.5
  - SR-19.1.4 (round-trip extension — Plan 01 closed the mailpit-arrival bar; this plan closes the full sign-in-with-new-password bar)
user_setup: []

must_haves:
  truths:
    - "The `@cjm-3.1` scenario asserts the FULL reset round-trip: signup → request reset → mailpit arrival → parse reset URL from email body → POST `/api/auth/reset-password` with `{ token, newPassword }` → sign-in with the new password returns 200."
    - "A reset-URL parser helper extracts the `token` query parameter from the mailpit message body (text or html) using a regex compatible with the worker template's rendered `reset_url`."
    - "The new step-bindings call Better Auth's canonical reset endpoint `/api/auth/reset-password` (NOT `/api/auth/forget-password`)."
    - "The new sign-in step uses `/api/auth/sign-in/email` and asserts HTTP 200 + a session cookie / token in the response, mirroring the harness's existing sign-in pattern from `@cjm-3.2` prior-art."
    - "Old password is rejected after reset (negative assertion) — defends against a no-op reset where the password row is silently unchanged. The original password is re-derived deterministically via `freshTenant(tenantId).password` (verified deterministic: the helper at `tests/e2e-cjm/support/fixtures.ts:44-53` returns the hardcoded literal `\"Cjm2Pass!23\"` for ANY tenantId)."
    - "Zero `--no-verify` across all commits in this plan (D-08)."
  artifacts:
    - path: tests/e2e-cjm/features/password-reset.feature
      provides: "@cjm-3.1 scenario with extended Then-steps covering parse + POST reset + sign-in + old-password-rejected"
      contains: "@cjm-3.1"
    - path: tests/e2e-cjm/steps/password-reset.steps.ts
      provides: "New Then-step bindings: URL parse, reset POST, sign-in, old-password-rejected"
      contains: "/api/auth/reset-password"
  key_links:
    - from: tests/e2e-cjm/steps/password-reset.steps.ts
      to: "/api/auth/reset-password"
      via: "undici postJsonRaw call inside new Then step"
      pattern: "/api/auth/reset-password"
    - from: tests/e2e-cjm/steps/password-reset.steps.ts
      to: "/api/auth/sign-in/email"
      via: "undici postJsonRaw call inside new sign-in Then step"
      pattern: "/api/auth/sign-in/email"
    - from: tests/e2e-cjm/steps/password-reset.steps.ts
      to: "mailpit message body"
      via: "GET /api/v1/message/{ID} (or equivalent mailpit message-body endpoint)"
      pattern: "/api/v1/message/"
---

<objective>
Extend the `@cjm-3.1` cucumber scenario beyond the Plan-01 mailpit-arrival assertion to cover the FULL password-reset round-trip required by ROADMAP SR-19.1.5: parse the reset URL from the delivered email, exchange the token at `/api/auth/reset-password`, sign in with the new password, and verify the old password no longer works.

Purpose: SR-19.1.4 + SR-19.1.5 close the "user actually regained account access" outcome — without the round-trip extension, Plan 01's GREEN proves only that an email was enqueued, not that it leads to a usable reset. This plan adds the missing behavioural coverage WITHOUT touching any production code in `apps/api` or `apps/worker`.

Output: Extended scenario in `tests/e2e-cjm/features/password-reset.feature` (additional `Then` lines under `@cjm-3.1`) + new step bindings in `tests/e2e-cjm/steps/password-reset.steps.ts` (URL parser, reset POST, sign-in, old-password rejected).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-reset-mail-wiring/19.1-CONTEXT.md
@.planning/phases/19.1-reset-mail-wiring/19.1-01-PLAN.md
@.planning/phases/19.1-reset-mail-wiring/19.1-01-SUMMARY.md
@CLAUDE.md
@tests/e2e-cjm/features/password-reset.feature
@tests/e2e-cjm/steps/password-reset.steps.ts
@tests/e2e-cjm/support/fixtures.ts
@apps/worker/src/i18n/locales/en/email/password_reset/body.txt
@apps/worker/src/i18n/locales/en/email/password_reset/body.html
@apps/worker/src/i18n/locales/ru/email/password_reset/body.txt

<interfaces>
<!-- Existing step-binding patterns to mirror -->

`tests/e2e-cjm/steps/password-reset.steps.ts` already provides:
  - `ScenarioState` interface with `email`, `password`, `resetCursor`, `lastResetStatus`, `lastResetBody`
  - `stateFor(tenantId)` factory
  - `localhostDispatcher(url)` undici Agent factory
  - `postJsonRaw` from `../support/fixtures`
  - `freshTenant` from `../support/fixtures`
  - `Given` / `When` / `Then` / `expect` from `../support/world`

`freshTenant(tenantId)` determinism — VERIFIED at `tests/e2e-cjm/support/fixtures.ts:44-53`:
  When `tenantId` is provided (not undefined), the helper returns:
    { tenantId, email: `e2e+${slug}@local.test`, password: "Cjm2Pass!23", displayName: `CJM ${slug}` }
  where `slug = tenantId.slice(0, 8)`. The `password` field is a HARDCODED LITERAL ("Cjm2Pass!23") — identical for ANY tenantId. Therefore `freshTenant(tenantId).password` is fully deterministic in this plan's usage and is safe to re-derive in the "old-password-rejected" step WITHOUT capturing an `originalPassword` in `ScenarioState`. (If a future change makes the password tenant-derived or random, this plan's assumption breaks and the step must switch to capturing it during the Given binding.)

The existing `Then("a password-reset email arrives in mailpit within {int} seconds", ...)` step at lines 72-108 already polls mailpit for the message. The new steps in this plan need to retain the message ID (extend the loop to capture `found.ID`) so a follow-up GET against mailpit's message-detail endpoint can fetch the body to parse the reset URL.

Mailpit message-detail endpoint shape (verify in compose: `GET ${mailpitApiUrl}/api/v1/message/{ID}` returns `{ Text: string, HTML: string, ... }`).

Worker template `apps/worker/src/i18n/locales/en/email/password_reset/body.txt` interpolates `{reset_url}` somewhere in the body — the parser regex MUST tolerate the rendered URL containing query-string `?token=...` or hash-fragment forms, depending on what Better Auth's `forget-password` redirectTo yields. Inspect the actual rendered email body during execution (mailpit GUI or curl) before fixing the regex.

Better Auth reset endpoint contract (verified in 19.1-01):
  POST /api/auth/reset-password
  body: { token: string, newPassword: string }
  success: 200 (empty or JSON object)
  failure: 4xx with `{ code, message }` or `{ error: { code, message } }` envelope (see existing @cjm-3.2 Then assertion at lines 126-144)

Better Auth sign-in endpoint contract (mirror harness usage elsewhere):
  POST /api/auth/sign-in/email
  body: { email: string, password: string }
  success: 200 with session token in body and/or set-cookie header
  failure: 401

Canonical cjm harness invocation (verified from repo Makefile, NOT the empty `tests/e2e-cjm/package.json`):
  `E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"`
The Makefile boots a hermetic `-p e2e-cjm` compose project and runs `pnpm exec playwright test --grep "$SCENARIO" --config tests/e2e-cjm/playwright.config.ts`. Omit `SCENARIO=...` to run the full suite (with the default `--grep-invert "@expected-red"`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend @cjm-3.1 feature with round-trip Then-steps</name>
  <files>tests/e2e-cjm/features/password-reset.feature</files>
  <behavior>
    After this task, `@cjm-3.1` will reference 4 new step phrases that have NO corresponding bindings yet. Running the scenario MUST fail with "Undefined" steps (cucumber convention) — this is the RED state.
  </behavior>
  <action>
    Edit `tests/e2e-cjm/features/password-reset.feature`. Under the existing `@cjm-3.1` scenario, append these new `Then` / `And` steps AFTER the existing `Then a password-reset email arrives in mailpit within 30 seconds` line (preserve the existing line — the new bindings extend, not replace):

      And the reset URL is extracted from the password-reset email
      And the password is reset via /api/auth/reset-password with the new password "BrandNewPass!23"
      And the user can sign in with the new password "BrandNewPass!23"
      And the user cannot sign in with the original password

    Use exactly these phrasings — cucumber matches by string. The phrases are deliberately verbose to avoid collision with `@cjm-3.2` `When a password reset is attempted with token {string}`.

    Do NOT touch `@cjm-3.2` or any other scenario. Do NOT add step bindings yet — that is Task 2.

    Run the cjm harness against the hermetic compose project with the `@cjm-3.1` tag filter:
      E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"
    Expect cucumber to report 4 undefined steps. Capture the cucumber snippet output (which prints suggested step skeletons) — useful for cross-checking the Task-2 bindings.

    Commit message: `test(19.1-02): extend @cjm-3.1 with round-trip Then-steps (RED — undefined bindings)`.
  </action>
  <verify>
    <automated>grep -cE "is extracted from the password-reset email|/api/auth/reset-password with the new password|can sign in with the new password|cannot sign in with the original password" tests/e2e-cjm/features/password-reset.feature</automated>
  </verify>
  <done>The `@cjm-3.1` scenario contains all 4 new step lines; the grep above returns 4. The scenario fails when run via the cjm harness because the 4 new steps are undefined. ZERO `--no-verify`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the 4 new step bindings + reset-URL parser helper</name>
  <files>tests/e2e-cjm/steps/password-reset.steps.ts</files>
  <behavior>
    After this task, all 4 new step phrases resolve to bindings and `@cjm-3.1` passes end-to-end against the compose stack: signup → forget-password → mailpit arrival → URL parse → /reset-password POST → sign-in with new password (200) → sign-in with old password (401).
  </behavior>
  <action>
    Edit `tests/e2e-cjm/steps/password-reset.steps.ts`. Extensions:

    1. Extend `ScenarioState` with optional fields: `resetMessageId?: string`, `resetUrl?: string`, `resetToken?: string`, `lastResetPostStatus?: number`, `lastSignInStatus?: number`. Keep existing fields untouched.

    2. Modify the existing `Then("a password-reset email arrives in mailpit within {int} seconds", ...)` step at lines 72-108 to capture `s.resetMessageId = found.ID` when a match is found (single-line addition before `return`). Behaviour preserved: timeout + cutoff logic unchanged.

    3. Add new step `Then("the reset URL is extracted from the password-reset email", async ({ mailpitApiUrl, tenantId }) => { ... })`. Implementation:
       - Reuse `localhostDispatcher` for the mailpit fetch.
       - GET `${mailpitApiUrl}/api/v1/message/${s.resetMessageId}` — fail clearly if `s.resetMessageId` is undefined ("ordering bug: parser ran before mailpit arrival step").
       - Parse the JSON body. Concatenate `Text` and `HTML` fields (whichever exist) into a single search corpus.
       - Run a regex matching the reset URL. Use a permissive shape: `/https?:\/\/[^\s"'<>]+(?:reset-password|reset)[^\s"'<>]*\?token=([A-Za-z0-9_\-\.]+)/i`. The first capture group is the token.
       - If no match: throw a descriptive error including a truncated body excerpt (first 500 chars) — this surface is critical for debugging template drift.
       - Assign `s.resetUrl = match[0]` and `s.resetToken = match[1]`. Assert both are non-empty strings.

    4. Add new step `Then("the password is reset via /api/auth/reset-password with the new password {string}", async ({ apiBaseURL, tenantId }, newPassword: string) => { ... })`. Implementation:
       - Read `s.resetToken` — fail clearly if undefined.
       - POST `${apiBaseURL}/api/auth/reset-password` with body `{ token: s.resetToken, newPassword }` via `postJsonRaw`.
       - Capture `s.lastResetPostStatus = res.status`. Assert `status === 200`. If non-200, include the response body in the error.
       - Update `s.password = newPassword` so the next sign-in step uses the new password.

    5. Add new step `Then("the user can sign in with the new password {string}", async ({ apiBaseURL, tenantId }, newPassword: string) => { ... })`. Implementation:
       - POST `${apiBaseURL}/api/auth/sign-in/email` with body `{ email: s.email, password: newPassword }` via `postJsonRaw`.
       - Capture status. Assert `status === 200`. If non-200, include response body in error.

    6. Add new step `Then("the user cannot sign in with the original password", async ({ apiBaseURL, tenantId }) => { ... })`. Implementation:
       - The original password is the fixed literal `"Cjm2Pass!23"` returned by `freshTenant(tenantId).password` for ALL tenantIds (deterministic — see `<interfaces>` block above; verified at `tests/e2e-cjm/support/fixtures.ts:44-53`). Re-derive directly: `const originalPassword = freshTenant(tenantId).password;`. Do NOT introduce an `originalPassword` field in `ScenarioState` — the determinism guarantee removes the need.
       - POST `${apiBaseURL}/api/auth/sign-in/email` with body `{ email: s.email, password: originalPassword }` via `postJsonRaw`.
       - Capture status. Assert `status >= 400 && status < 500`. Do NOT assert exact body — Better Auth's invalid-credentials shape is a separate contract (already covered by other tests).

    7. ASCII-only source per CLAUDE.md English-only rule. No non-ASCII glyphs in regexes or string literals.

    8. After implementation: run the cjm harness against the hermetic compose project:
         E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"
       MUST pass end-to-end within the harness's existing timeout budget.

    If the scenario fails: STOP. Per CLAUDE.md Hard Rule #1, do NOT modify production code in `apps/api` or `apps/worker` to make this scenario pass. Diagnose. Common gotchas: (a) regex too strict for actual rendered URL — relax the alternation; (b) Better Auth's reset-password endpoint returns 200 but a non-JSON body — accept by reading `.text()` not `.json()`; (c) sign-in returns 200 with empty body — that's fine, the harness asserts status only. (NOTE: the prior "freshTenant determinism" gotcha has been resolved up-front — see step 6 — and is no longer a runtime branch.)

    Commit message: `feat(19.1-02): bind round-trip Then-steps for @cjm-3.1 — parse + reset + sign-in + old-rejected`.
  </action>
  <verify>
    <automated>grep -cE "the reset URL is extracted|the password is reset via /api/auth/reset-password|the user can sign in with the new password|the user cannot sign in with the original password" tests/e2e-cjm/steps/password-reset.steps.ts</automated>
  </verify>
  <done>All 4 new step bindings exist in `password-reset.steps.ts` (grep returns 4). `@cjm-3.1` passes end-to-end via `E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"`. `pnpm typecheck` exit 0. ZERO `--no-verify`. Per-step diff scope: ~80-120 lines added to `password-reset.steps.ts` (4 new step bindings + 1 helper-style regex constant + ScenarioState field additions + 1-line addition to existing mailpit step). Coverage waiver: cucumber step bindings in `tests/e2e-cjm/steps/` are exempt from the 90/90/90/90 floor because the harness's vitest coverage scope does not include cjm step files (verified in Phase 13 SUMMARY). The end-to-end GREEN run IS the coverage proof for these bindings.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — full cjm e2e green run + ROADMAP/STATE close-out</name>
  <files>tests/e2e-cjm/features/password-reset.feature</files>
  <action>
    Final verification sweep:

    1. Run the FULL cjm e2e suite (no `SCENARIO=` filter — the Makefile default applies `--grep-invert "@expected-red"`):
         E2E_CJM=1 make e2e-cjm
       MUST be GREEN with no new failures vs Phase 19 baseline. If any unrelated scenario fails, surface as a deferred-items entry — do NOT fix here.

    2. Run `pnpm --filter @openwhispr/api test` + `pnpm --filter @openwhispr/api typecheck` + `pnpm --filter @openwhispr/worker test`. All exit 0.

    3. Verify no production source files were modified across Plans 01+02 outside the expected set:
       - `git diff --name-only main...HEAD` should show ONLY: `apps/api/src/auth.ts`, `apps/api/tests/unit/__tests__/auth-send-reset-password.test.ts`, `tests/e2e-cjm/features/password-reset.feature`, `tests/e2e-cjm/steps/password-reset.steps.ts`, plus the planning artefacts in `.planning/phases/19.1-reset-mail-wiring/`.
       - If anything else appears, HALT and surface to the orchestrator — Hard Rule #1 may have been violated upstream.

    4. Update `docs/customer-journeys.md` `@cjm-3.1` row: remove the `@expected-red` annotation (per ROADMAP SR-19.1.4 success-criterion 4). Single-line edit.

    5. (Orchestrator owns ROADMAP / STATE updates — this plan does NOT touch them.)

    Commit message: `docs(19.1-02): mark @cjm-3.1 as GREEN in customer-journeys.md`.

    Constraint: ZERO `--no-verify`. Sequential execute. English-only source.
  </action>
  <verify>
    <automated>grep -E "@cjm-3\.1" docs/customer-journeys.md | grep -v "@expected-red" | head -3</automated>
  </verify>
  <done>Full cjm suite GREEN via `E2E_CJM=1 make e2e-cjm`. All workspace test + typecheck exit 0. `docs/customer-journeys.md` `@cjm-3.1` row no longer carries `@expected-red`. Working tree clean. ZERO `--no-verify`.</done>
</task>

<task type="auto">
  <name>Task 4 (OPTIONAL): REFACTOR — extract mailpit message-detail fetch helper</name>
  <files>tests/e2e-cjm/steps/password-reset.steps.ts</files>
  <action>
    OPTIONAL — execute ONLY if Task 2's URL-parser step introduced visible duplication with the existing `mailpitApiUrl` GET pattern at lines 80-81 (`messages?query=...`). If duplication is < 8 lines OR only one mailpit detail-fetch call site exists, skip this task.

    If executing: extract a helper `async function fetchMailpitMessage(mailpitApiUrl: string, messageId: string): Promise<{ Text?: string; HTML?: string }>` at the top of the file. Call from the new parse step. Re-run `E2E_CJM=1 make e2e-cjm SCENARIO="@cjm-3.1"`. MUST stay GREEN.

    Constraint: Behaviour-preserving. Any RED → revert.

    Commit message: `refactor(19.1-02): extract fetchMailpitMessage helper`.
  </action>
  <verify>
    <automated>grep -cE "fetchMailpitMessage" tests/e2e-cjm/steps/password-reset.steps.ts</automated>
  </verify>
  <done>Either (a) skipped because single call site / trivial duplication, OR (b) helper extracted, `@cjm-3.1` still GREEN, ZERO `--no-verify`.</done>
</task>

</tasks>

<verification>
- `tests/e2e-cjm/features/password-reset.feature` contains the 4 new `And/Then` lines under `@cjm-3.1` (verified by grep).
- `tests/e2e-cjm/steps/password-reset.steps.ts` defines the 4 new step bindings AND extends the mailpit-arrival step to capture `resetMessageId`.
- `@cjm-3.1` passes end-to-end against the live hermetic compose stack covering: signup → forget-password → mailpit arrival → URL parse → /reset-password POST 200 → sign-in 200 with new password → sign-in 4xx with old password.
- `docs/customer-journeys.md` `@cjm-3.1` row carries no `@expected-red` annotation.
- No production source files in `apps/api/src/**` or `apps/worker/src/**` were modified in this plan (Plan 02 is test-only — Hard Rule #1 hygiene check).
- `pnpm typecheck` + `pnpm test` (per-package) exit 0.
- Every commit in this plan was created WITHOUT `--no-verify` (D-08).
- Coverage waiver for cjm step bindings: cucumber step bindings in `tests/e2e-cjm/steps/` are exempt from the 90/90/90/90 floor because the harness's vitest coverage scope does not include cjm step files (verified in Phase 13 SUMMARY). The end-to-end GREEN run IS the coverage proof for these bindings.
</verification>

<success_criteria>
1. RED commit lands the 4 new feature lines without bindings (RED via "undefined step" cucumber state).
2. GREEN commit lands the 4 new step bindings + parser helper; `@cjm-3.1` passes end-to-end.
3. Docs commit removes `@expected-red` from `docs/customer-journeys.md` for `@cjm-3.1`.
4. Optional REFACTOR commit either does not exist (skipped) or lands cleanly.
5. Total commits: 3 (mandatory RED+GREEN+DOCS) or 4 (with optional refactor).
6. ZERO `--no-verify` across every commit.
7. Sequential execute: NO parallel subagents on the shared working tree.
8. Plan 02 produces NO modifications under `apps/api/src/**` or `apps/worker/src/**` (Hard Rule #1 — test-only plan).
9. Phase 19.1 is ready for orchestrator close-out: STATE → CLOSED, ROADMAP progress table updated, 19.1 row marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-reset-mail-wiring/19.1-02-SUMMARY.md` covering: commits landed (SHAs + messages), files touched, observable truths verified against the live codebase (one bullet per `must_haves.truths` entry with the verification command + observed output), the full `@cjm-3.1` round-trip flow as executed end-to-end (sample logs / mailpit IDs welcome), any deviations from CONTEXT.md and why, any deferred items surfaced. Note Phase 19.1 close-out readiness for the orchestrator.
</output>
</output>
