---
phase: 13-e2e-cjm-harness-v2-ships-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/email/package.json
  - packages/email/tsconfig.json
  - packages/email/vitest.config.ts
  - packages/email/README.md
  - packages/email/src/index.ts
  - packages/email/src/EmailSender.ts
  - packages/email/src/EmailSender.test.ts
  - apps/api/src/email.ts
  - apps/api/src/routes/health.ts
  - apps/api/src/routes/health.test.ts
  - apps/api/vitest.setup.ts
  - apps/api/vitest.config.ts
  - apps/worker/src/index.ts
  - apps/worker/package.json
  - apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx
  - apps/web/src/components/screens/notes/__tests__/NoteDetailClient.test.tsx
  - apps/web/src/components/screens/notes/__tests__/NotesListClient.test.tsx
  - packages/contract-tests/schemas/health.ts
  - tools/lint-weak-assertions.ts
  - tools/lint-weak-assertions.test.ts
  - tools/global-vitest-teardown.ts
  - tools/__tests__/global-vitest-teardown.test.ts
  - tools/__tests__/readiness-probe.test.ts
  - tests/e2e-cjm/playwright.config.ts
  - tests/e2e-cjm/bddgen.config.ts
  - tests/e2e-cjm/support/compose-harness.ts
  - tests/e2e-cjm/support/mailpit-helper.ts
  - tests/e2e-cjm/support/wait-for-readiness.ts
  - tests/e2e-cjm/support/world.ts
  - tests/e2e-cjm/steps/auth.steps.ts
  - tests/e2e-cjm/features/signup-verify.feature
  - vitest.config.ts
  - Makefile
  - .github/workflows/e2e-cjm.yml
autonomous: true
requirements:
  - E2E-01
  - E2E-03
  - E2E-04
  - E2E-09
  - E2E-10
  - E2E-11
  - E2E-12
tags: [e2e, cucumber, playwright-bdd, testcontainers, smtp, nodemailer]

must_haves:
  truths:
    - "A developer can run `E2E_CJM=1 make e2e-cjm` on a clean clone and both `@cjm-1.1` (signup happy) and `@cjm-1.2` (already-registered negative twin) exit 0"
    - "The single atomic commit landing the harness ALSO removes the `noopSender` from apps/worker/src/index.ts and wires `createEmailSender` from @openwhispr/email"
    - "Running `pnpm vitest run` no longer leaks testcontainers — `docker ps --filter label=org.testcontainers=true` shows zero containers after the run"
    - "Running `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0 (all 7 weak-assertion sites rewritten)"
    - "`GET /api/health` returns `{ status: 'ok', migrations_completed: true }` after migrate completes"
    - "GHA job `e2e-cjm` boots its own compose stack (independent of `e2e-hermetic`) and prunes testcontainers in `always()`"
    - "In production (`NODE_ENV=production`) with `SMTP_HOST` unset, `createEmailSender` throws at module init (no silent dev-fallback)"
  artifacts:
    - path: "packages/email/src/EmailSender.ts"
      provides: "createEmailSender factory exporting EmailSender + types"
      exports: ["createEmailSender", "EmailSender", "SendArgs", "SendResult", "Logger"]
    - path: "packages/email/src/EmailSender.test.ts"
      provides: "vitest unit tests for EmailSender incl. prod loud-fail branch"
      min_lines: 80
    - path: "tools/lint-weak-assertions.ts"
      provides: "tsx-script linter matching tools/lint-english.ts exit-code contract"
      contains: "WEAK_ASSERTION"
    - path: "tools/global-vitest-teardown.ts"
      provides: "vitest globalTeardown default export + installSignalHook()"
      exports: ["default", "installSignalHook"]
    - path: "tests/e2e-cjm/support/compose-harness.ts"
      provides: "bootStack(), tearStack(), waitForReadiness()"
      contains: "--profile"
    - path: "tests/e2e-cjm/features/signup-verify.feature"
      provides: "@cjm-1.1 and @cjm-1.2 reference Gherkin scenarios"
      contains: "@cjm-1.1"
    - path: ".github/workflows/e2e-cjm.yml"
      provides: "GHA job that runs make e2e-cjm with E2E_CJM=1 and prunes in always()"
      contains: "label=org.testcontainers=true"
    - path: "Makefile"
      provides: "e2e-cjm target with E2E_CJM=1 gate and --grep-invert @expected-red"
      contains: "e2e-cjm"
  key_links:
    - from: "apps/worker/src/index.ts"
      to: "@openwhispr/email"
      via: "import { createEmailSender } at top + sender: realSender at emailWorker construction"
      pattern: "from \"@openwhispr/email\""
    - from: "tests/e2e-cjm/support/compose-harness.ts"
      to: "docker compose --profile default up -d"
      via: "spawn child process"
      pattern: "--profile.*default"
    - from: "apps/api/src/routes/health.ts"
      to: "_meta.__drizzle_migrations"
      via: "checkMigrationsCompleted() SQL probe"
      pattern: "migrations_completed"
    - from: "vitest.config.ts (root)"
      to: "tools/global-vitest-teardown.ts"
      via: "test.globalTeardown entry"
      pattern: "global-vitest-teardown"
    - from: ".github/workflows/e2e-cjm.yml"
      to: "docker container prune"
      via: "always() step"
      pattern: "if: always\\(\\)"
---

<objective>
Ship the Cucumber + playwright-bdd e2e harness at `tests/e2e-cjm/`, atomically replace the worker `noopSender` with a real nodemailer-backed `EmailSender` in new `packages/email/`, close the testcontainers leak via `tools/global-vitest-teardown.ts`, ship the `tools/lint-weak-assertions.ts` tsx-script linter + sweep ALL 7 sites, harden `/api/health` with `migrations_completed`, and prove the harness end-to-end with `@cjm-1.1` (signup happy) + `@cjm-1.2` (already-registered negative twin) GREEN against the real docker-compose stack.

Purpose: Every subsequent v2 phase (12, 14, 15, 16, 17) writes RED Gherkin tests against this harness. Happy-path-only tests that shipped bugs green in v1 (TD-13.a/d) become structurally impossible — every happy path requires a negative-twin sibling enforced by `tools/lint-cjm-doc.ts` (authored in 13-02).

Output: New `packages/email/` shared package, new `tests/e2e-cjm/` harness, new `tools/lint-weak-assertions.ts` + `tools/global-vitest-teardown.ts`, real `createEmailSender` wired into apps/worker, hardened health probe, GHA `e2e-cjm` job + Makefile target, 2 reference Gherkin scenarios GREEN.

Atomic-commit constraint (D-04, NON-NEGOTIABLE): Wave 0 and Wave 1 are WORKING-TREE checkpoints, NOT commit boundaries. There is exactly ONE git commit for plan 13-01, produced by Task 13-01-08 after `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.1` AND `SCENARIO=@cjm-1.2` both report GREEN. Wave 0 tasks stage RED tests in the working tree; Wave 1 tasks turn them GREEN in the same uncommitted working tree; Wave 2 ties off the integration delta and produces the single atomic commit covering harness scaffold + worker `noopSender` removal + `packages/email/` + `tools/lint-weak-assertions.ts` + weak-assertion sweep + readiness probe + reference scenarios @cjm-1.1 + @cjm-1.2 GREEN. Do NOT `git commit` anywhere prior to Task 13-01-08.

<tdd_protocol>
**TDD protocol (constitutional, per CLAUDE.md "Each fix lands with its tests in the SAME atomic commit"):** Author each task's tests RED in the working tree first, observe them fail, then author the corresponding production code to turn them GREEN. Wave boundaries are working-tree checkpoints, NOT commit boundaries. Plan 13-01 produces exactly ONE commit, landed at Task 13-01-08 after `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.1` AND `SCENARIO=@cjm-1.2` both report GREEN. Do not `git commit` anywhere prior. This guarantees tests and their fixes land in the same atomic commit per D-04 and the CLAUDE.md constitutional rule.
</tdd_protocol>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-CONTEXT.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-RESEARCH.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-VALIDATION.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor will consume/modify directly. No codebase scavenger hunt required. -->

From `apps/api/src/email.ts` (lines 27–105 — verbatim extract per D-06):
```typescript
export interface SendArgs { to: string; subject: string; text: string; html?: string; }
export interface SendResult { delivered: boolean; reason?: string; }
export interface EmailService { send(args: SendArgs): Promise<SendResult>; }
// makeEmailService(log: FastifyBaseLogger): EmailService
```

From `apps/worker/src/jobs/email-delivery.ts:47` (existing — keep local re-export):
```typescript
export type EmailSender = { send(args: SendArgs): Promise<SendResult>; }
```

From `apps/worker/src/index.ts:43,66-72,124-134` (current state to be replaced):
```typescript
import { ... type EmailSender } from "./jobs/email-delivery.js";
const noopSender: EmailSender = { async send() { return { delivered: true, reason: "no-op-sender" }; } };
// ...
const emailWorker = new Worker(
  QUEUE_NAMES.emailDelivery,
  buildEmailDeliveryHandler({ pool: appOwnerPool, sender: noopSender, renderer: templateRenderer }),
  { connection },
);
```

From `apps/api/src/routes/health.ts:15-22` (current — to be hardened):
```typescript
handler: async () => ({ status: "ok" as const })
```

From `packages/contract-tests/schemas/health.ts` (current HealthResponse — must add `migrations_completed: z.boolean()`).

From `tools/lint-english.ts:1-157` (analog for lint-weak-assertions.ts — shebang `#!/usr/bin/env -S pnpm exec tsx`, exit codes 0/1/2, stderr offender format `file:line:col preview`).

From `tools/global-vitest-teardown.ts` shape (NEW — vitest 4 globalTeardown signature):
```typescript
export default async function globalTeardown(): Promise<void>
export function installSignalHook(): void  // idempotent, registers SIGINT/SIGTERM once
```

Locked dep versions (REQUIREMENTS.md E2E-01 + user decision 1 — HOLD, do NOT lift):
- `@cucumber/cucumber@12.8.2`
- `playwright-bdd@8.4.2`
- `@playwright/test@1.60.0` (minor bump from 1.59.1)
- `@axe-core/playwright@^4.x`
- `nodemailer` — reuse existing root version

SMTP env contract (user decision 7 — `SMTP_PASSWORD` keeps the existing name, overrides CONTEXT D-07's `SMTP_PASS`):
- `SMTP_HOST` (required in production — loud-fail if unset)
- `SMTP_PORT` (default 587)
- `SMTP_USER`
- `SMTP_PASSWORD` (NOT `SMTP_PASS` — keep existing wired name)
- `SMTP_FROM`
- `SMTP_SECURE` (boolean override; default `port === 465`)
- `SMTP_REJECT_UNAUTHORIZED` (boolean; default true)

Loud-fail gate (D-07): `if (process.env.NODE_ENV === "production" && !process.env.SMTP_HOST) throw new Error("SMTP_HOST is required in production (event:email.smtp_required_in_production)")`. Branch placed at the dev-fallback site in `createEmailSender`.

Logger abstraction (PATTERNS.md): introduce structural `Logger` interface `{ info, warn, error: (obj, msg?) => void }` so both Fastify's `FastifyBaseLogger` and pino's `Logger` satisfy structurally — no adapters at call sites.

7 weak-assertion sites (user decision 3 — sweep ALL):
- `apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx` lines 147, 165, 186 (3 sites — rewrite to `findByText` + `toBeInTheDocument` or `toHaveLength(1)`)
- `apps/web/src/components/screens/notes/__tests__/NoteDetailClient.test.tsx` lines 360, 370 (2 sites — `toHaveLength(N)` per actual count)
- `apps/web/src/components/screens/notes/__tests__/NotesListClient.test.tsx` lines 127, 166, 276, 295 (4 sites — note: line 166 uses `.toBeGreaterThanOrEqual(2)` which is ALSO banned; rewrite to `toHaveLength(2)`)
- Total = 9 occurrences across 7 sites (SignUpForm:3, NoteDetail:2, NotesList:4). Sweep until `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0.
</interfaces>
</context>

<tasks>

<!-- =================== WAVE 0 — RED TESTS =================== -->

<task type="auto" tdd="true">
  <name>Task 13-01-01: Add deps + scaffold packages/email/ + tests/e2e-cjm/ skeleton + bddgen.config.ts (RED: bddgen --dry-run compiles signup-verify.feature shell)</name>
  <files>package.json, pnpm-workspace.yaml, packages/email/package.json, packages/email/tsconfig.json, packages/email/vitest.config.ts, packages/email/src/index.ts, tests/e2e-cjm/playwright.config.ts, tests/e2e-cjm/bddgen.config.ts, tests/e2e-cjm/features/signup-verify.feature, tests/e2e-cjm/support/world.ts</files>
  <read_first>
    - packages/litellm-client/package.json (analog for packages/email/package.json shape per PATTERNS.md)
    - packages/litellm-client/tsconfig.json (analog)
    - packages/litellm-client/vitest.config.ts (analog — 90/90/90/90 thresholds, include src/**/*.ts)
    - pnpm-workspace.yaml (verify packages/* glob already includes new package)
    - package.json (verify @playwright/test 1.59.1 currently pinned — bump to 1.60.0; verify no @cucumber/cucumber or playwright-bdd entries yet)
    - playwright-bdd 8.4.2 README §"bddgen --dry-run" — confirm whether `--dry-run` resolves `importTestFrom`. If unconfirmed at executor time, the 3-line `support/world.ts` placeholder created in this task ensures bddgen never fails on a missing import target.
  </read_first>
  <behavior>
    - Test 1: `pnpm install --frozen-lockfile` succeeds with new deps present in root package.json devDependencies: `@cucumber/cucumber@12.8.2`, `playwright-bdd@8.4.2`, `@playwright/test@1.60.0`, `@axe-core/playwright@^4`.
    - Test 2: `pnpm exec bddgen --dry-run --config tests/e2e-cjm/bddgen.config.ts` exits 0 and prints a generated path for `tests/e2e-cjm/features/signup-verify.feature` (file may contain only a stub Feature header + a single placeholder Scenario tagged `@cjm-1.1`).
    - Test 3: `packages/email/package.json` declares `"name": "@openwhispr/email"`, `"private": true`, `"type": "module"`, `"main": "./src/index.ts"`, `"exports": { ".": "./src/index.ts" }`, dependency on `nodemailer` (version pinned to the existing root-installed version), devDependency on `vitest` matching root.
  </behavior>
  <action>
    Add to root `package.json` devDependencies: `@cucumber/cucumber@12.8.2`, `playwright-bdd@8.4.2`, `@axe-core/playwright@^4.10.2`. Bump `@playwright/test` from 1.59.1 to 1.60.0. Run `pnpm install` to refresh lockfile (committed).
    Create `packages/email/` as a workspace package mirroring `packages/litellm-client/`'s file shape: `package.json` (name `@openwhispr/email`, private, ESM, main+exports point at `./src/index.ts`, deps: `nodemailer`, devDeps include `@types/nodemailer`, `vitest`, `typescript`), `tsconfig.json` (extends `../../tsconfig.base.json`, includes `src/**/*.ts`), `vitest.config.ts` (90/90/90/90 thresholds; include `src/**/*.ts`).
    Create `packages/email/src/index.ts` as a barrel re-exporting from `./EmailSender.js` (placeholder — implementation lands in Task 13-01-04).
    Create `tests/e2e-cjm/playwright.config.ts` with `retries: 0` (D-12 — retry-on-flake BANNED), `use: { baseURL: "https://app.localhost", ignoreHTTPSErrors: true }`, `testDir: "./.bdd-gen"`, `workers: 1`, projects: [{ name: "chromium", use: devices["Desktop Chrome"] }]. Import devices from `@playwright/test`.
    Create `tests/e2e-cjm/bddgen.config.ts` with `paths: ["tests/e2e-cjm/features/**/*.feature"]`, `outputDir: "tests/e2e-cjm/.bdd-gen"`, `importTestFrom: "tests/e2e-cjm/support/world.ts"` (world.ts shell created in this task — see below). Set `verbose: true`.
    Create a 3-line `tests/e2e-cjm/support/world.ts` placeholder shell that re-exports `test` and `expect` from `@playwright/test` and calls `createBdd(test)` from `playwright-bdd` (or, if playwright-bdd 8.4.2 does not require the file to exist for `--dry-run`, leave as a no-op re-export). This guarantees `bddgen --dry-run` succeeds regardless of whether playwright-bdd resolves `importTestFrom` at dry-run time. Final world.ts shape lands in Task 13-01-07.
    Create `tests/e2e-cjm/features/signup-verify.feature` with a Feature header and a single placeholder Scenario tagged `@cjm-1.1` containing a `Given/When/Then` triple. NO Cucumber `retry:` config anywhere (D-12).
    Do NOT add any package.json scripts that reference watch mode.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile &amp;&amp; pnpm exec bddgen --dry-run --config tests/e2e-cjm/bddgen.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install --frozen-lockfile` exits 0.
    - `grep -E "\"@cucumber/cucumber\": \"12\\.8\\.2\"" package.json` returns 1 match.
    - `grep -E "\"playwright-bdd\": \"8\\.4\\.2\"" package.json` returns 1 match.
    - `grep -E "\"@playwright/test\": \"1\\.60\\.0\"" package.json` returns 1 match.
    - `pnpm exec bddgen --dry-run --config tests/e2e-cjm/bddgen.config.ts` exits 0 and prints at least one generated spec path containing `signup-verify`.
    - `cat packages/email/package.json | grep -E "\"name\": \"@openwhispr/email\""` returns 1 match.
    - `cat tests/e2e-cjm/playwright.config.ts | grep -v '^//' | grep -c "retries: 0"` is ≥ 1.
  </acceptance_criteria>
  <done>New deps pinned at locked versions; packages/email/ skeleton scaffolded; bddgen compiles the placeholder feature stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-02: tools/global-vitest-teardown.ts + apps/api/vitest.setup.ts SIGINT/SIGTERM hook (RED: vitest integration test asserts orphan container cleanup)</name>
  <files>tools/global-vitest-teardown.ts, tools/__tests__/global-vitest-teardown.test.ts, apps/api/vitest.setup.ts, apps/api/vitest.config.ts, vitest.config.ts</files>
  <read_first>
    - tests/e2e/helpers/phase6-compose.ts (testcontainers cleanup posture — lines 53–54, 216–218, 275)
    - apps/api/vitest.config.ts (verify it currently has NO setupFiles entry — PATTERNS.md discrepancy #1)
    - tools/lint-english.test.ts (analog for execFileSync test pattern)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-RESEARCH.md §"testcontainers leak fix" (lines 459–525 — vitest 4 globalTeardown shape)
    - vitest.config.ts at repo root (verify whether present; create if absent — vitest workspace mode)
  </read_first>
  <behavior>
    - Test 1: `globalTeardown` default export is an async function; invoking it spawns `docker container prune -f --filter label=org.testcontainers=true` via `execFileSync` and swallows ENOENT (no `docker` binary in CI sandbox).
    - Test 2: `installSignalHook()` is idempotent — calling twice registers exactly one SIGINT handler and one SIGTERM handler (assert via `process.listenerCount("SIGINT")` delta).
    - Test 3: SIGINT handler exits with code 130; SIGTERM handler exits with code 143 (assert via spawn child + signal injection).
  </behavior>
  <action>
    Create `tools/global-vitest-teardown.ts` exporting `default async function globalTeardown()` that runs `execFileSync("docker", ["container", "prune", "-f", "--filter", "label=org.testcontainers=true"], { stdio: "inherit" })` inside a `try { } catch { }` (swallow only if `err.code === "ENOENT"` or process exit code != 0; never throw — vitest globalTeardown must not abort the test report). Also export `installSignalHook(): void` that registers SIGINT/SIGTERM handlers exactly once (use module-scoped `installed = false` guard). Handlers call the same prune command, then `process.exit(sig === "SIGINT" ? 130 : 143)`.
    Create `tools/__tests__/global-vitest-teardown.test.ts` using vitest. Mock `node:child_process` `execFileSync`. Assert: (a) default export invokes execFileSync with exact argv `["container", "prune", "-f", "--filter", "label=org.testcontainers=true"]` and binary `docker`; (b) catches and swallows execFileSync throws; (c) `installSignalHook()` called twice yields `process.listenerCount("SIGINT") === 1` net new listener; (d) handler invocation calls `process.exit` with 130/143 (mock `process.exit`).
    Create `apps/api/vitest.setup.ts` that imports `installSignalHook` from `../../tools/global-vitest-teardown.ts` and calls it at module top.
    Modify `apps/api/vitest.config.ts` to add `test.setupFiles: ["./vitest.setup.ts"]`.
    Create or modify root `vitest.config.ts` to set `test.globalTeardown: ["./tools/global-vitest-teardown.ts"]`. If root vitest.config.ts does not exist, create one declaring a vitest projects workspace covering apps/api, apps/worker, apps/web, packages/email, packages/litellm-client, packages/observability, packages/data, packages/contract-tests, tools — match whatever shape the repo currently uses for cross-package vitest runs (Read vitest.config.* at repo root first).
  </action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/global-vitest-teardown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tools/__tests__/global-vitest-teardown.test.ts` exits 0 with ≥ 4 tests passing.
    - `grep -v '^//' apps/api/vitest.config.ts | grep -c "setupFiles"` ≥ 1.
    - `grep -v '^//' apps/api/vitest.setup.ts | grep -c "installSignalHook"` ≥ 1.
    - `grep -v '^//' vitest.config.ts | grep -c "global-vitest-teardown"` ≥ 1.
    - `grep -E "process\\.exit\\(.*130" tools/global-vitest-teardown.ts` returns ≥ 1 match.
  </acceptance_criteria>
  <done>vitest globalTeardown wired at repo root; apps/api uses setupFiles to install signal hooks; integration tests cover idempotency + exit-code contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-03: tools/lint-weak-assertions.ts (tsx-script linter + --self-test) (RED: 3 vitest tests assert it flags the family)</name>
  <files>tools/lint-weak-assertions.ts, tools/lint-weak-assertions.test.ts</files>
  <read_first>
    - tools/lint-english.ts (lines 1–157 — full skeleton; shebang, exit codes, stderr `file:line:col preview` format)
    - tools/lint-english.test.ts (lines 1–60 — execFileSync test pattern, tmpdir fixture)
    - tools/lint-rls.ts (line 1 shebang + exit-code precedent)
    - apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx lines 145–190 (real-world offender shape)
    - apps/web/src/components/screens/notes/__tests__/NotesListClient.test.tsx lines 120–300 (offender shape for `.toBeGreaterThanOrEqual(2)` family)
  </read_first>
  <behavior>
    - Test 1: Running the tool against a clean fixture tree exits 0 with empty stderr.
    - Test 2: Running against a tmpdir containing `getAllByText(/foo/).length.toBeGreaterThan(0)` exits 1 and prints `<file>:<line>:<col>` to stderr.
    - Test 3: Running against `queryAllByRole("button").length.toBeGreaterThanOrEqual(2)` exits 1 (family pattern — `.toBeGreaterThan(n)` AND `.toBeGreaterThanOrEqual(n)` both flagged for any `getAllBy*` / `queryAllBy*` / `findAllBy*` matcher).
    - Test 4: `pnpm tsx tools/lint-weak-assertions.ts --self-test` exits 0 (self-test verifies the regex against an inlined positive AND negative fixture string).
    - Test 5: Internal error (e.g., unreadable directory passed as argv[2]) exits 2.
  </behavior>
  <action>
    Create `tools/lint-weak-assertions.ts` with shebang `#!/usr/bin/env -S pnpm exec tsx`, SPDX-License-Identifier header (Apache-2.0), structured per `tools/lint-english.ts`: glob `**/*.test.ts` + `**/*.test.tsx` under argv[2] (default cwd), IGNORE `node_modules/dist/coverage/.git/.bdd-gen/`. Use regex covering the whole family: `/\.(getAllBy|queryAllBy|findAllBy)\w*\([^)]*\)\.length\.toBeGreaterThan(OrEqual)?\(\s*\d+\s*\)/`. For every match: push offender `{ file, line, col, preview }`. Stderr format: `Weak-assertion violation: N occurrence(s). Use findByText + toBeInTheDocument or toHaveLength(N) instead.` then one offender per line `file:line:col preview`. Exit codes: 0/1/2 per `tools/lint-english.ts` contract.
    Add `--self-test` argv handling: when present, evaluate the regex against an inlined positive fixture (`screen.getAllByText(/x/).length.toBeGreaterThan(0)`) AND a negative fixture (`expect(await screen.findByText(/x/)).toBeInTheDocument()`); exit 0 if positive matches AND negative does not; exit 1 otherwise.
    Create `tools/lint-weak-assertions.test.ts` using `execFileSync("pnpm", ["exec", "tsx", SCRIPT, fixtureDir])` pattern from `tools/lint-english.test.ts`. Build fixture trees via `mkdtempSync` + `writeFileSync`. Cover all 5 test cases above. Use `vitest` `describe`/`it`/`expect`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/lint-weak-assertions.test.ts &amp;&amp; pnpm tsx tools/lint-weak-assertions.ts --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tools/lint-weak-assertions.test.ts` exits 0 with ≥ 5 tests passing.
    - `pnpm tsx tools/lint-weak-assertions.ts --self-test` exits 0.
    - `head -2 tools/lint-weak-assertions.ts | grep -c "pnpm exec tsx"` = 1.
    - `grep -v '^//' tools/lint-weak-assertions.ts | grep -c "toBeGreaterThan"` ≥ 1.
    - Running `pnpm tsx tools/lint-weak-assertions.ts /tmp/empty-fixture` (where /tmp/empty-fixture is an empty dir) exits 0.
  </acceptance_criteria>
  <done>Linter script + 5 unit tests committed; `--self-test` mode validates regex semantics in CI without needing a fixture tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-04: packages/email/ — extract EmailSender + prod loud-fail + Logger abstraction (RED: vitest unit tests fail on existing apps/api/src/email.ts)</name>
  <files>packages/email/src/EmailSender.ts, packages/email/src/EmailSender.test.ts, packages/email/src/index.ts, packages/email/README.md</files>
  <read_first>
    - apps/api/src/email.ts (lines 1–105 — verbatim extract target)
    - apps/api/src/email.test.ts (lines 1–80+ — copy verbatim, adjust import path; add new loud-fail test)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md §"EmailSender.ts" and §"EmailSender.test.ts" (Logger interface, env-var contract, loud-fail branch)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-RESEARCH.md §"packages/email/ shape" + §"SMTP env contract"
  </read_first>
  <behavior>
    - Test 1: `createEmailSender({ log, env })` with `SMTP_HOST` set returns a sender that calls `nodemailer.createTransport({ host, port, secure, auth })` with auth derived from `SMTP_USER` + `SMTP_PASSWORD`.
    - Test 2: Calling `sender.send({ to, subject, text })` invokes `transporter.sendMail({ from, to, subject, text, html })`; returns `{ delivered: true }` on success; logs `event: "email.sent"` at info level.
    - Test 3: `transporter.sendMail` throwing causes `sender.send` to re-throw (Pitfall 4: NEVER swallow — Better Auth must see rejection). Log emits `event: "email.failed"` at error level.
    - Test 4: `NODE_ENV === "production"` + `SMTP_HOST` unset → `createEmailSender(...)` throws `Error` matching `/email\.smtp_required_in_production/`.
    - Test 5: `NODE_ENV !== "production"` + `SMTP_HOST` unset → returns a dev-fallback sender that resolves `{ delivered: true, reason: "smtp-not-configured" }` and logs `event: "email.smtp_not_configured"` at warn level.
    - Test 6: `SMTP_SECURE === "true"` overrides port-derived default (assert `createTransport` called with `secure: true` even when `port !== 465`).
    - Test 7: `SMTP_REJECT_UNAUTHORIZED === "false"` propagates to `createTransport` `tls: { rejectUnauthorized: false }`.
    - Test 8: Logger interface accepts both Fastify-style `log.info({ obj }, msg)` and pino-style — assert call signature compatibility via TypeScript compile + a runtime adapter test using a plain object `{ info: vi.fn(), warn: vi.fn(), error: vi.fn() }`.
  </behavior>
  <action>
    Create `packages/email/src/EmailSender.ts`. Define structural `export interface Logger { info(obj: Record<string, unknown>, msg?: string): void; warn(obj, msg?): void; error(obj, msg?): void; }`. Define `export interface SendArgs`, `export interface SendResult`, `export interface EmailSender { send(args: SendArgs): Promise<SendResult>; }`. Define `export function createEmailSender(opts: { log: Logger; env: NodeJS.ProcessEnv }): EmailSender`.
    Inside `createEmailSender`: read `host = env.SMTP_HOST`. If `!host`: `if (env.NODE_ENV === "production") throw new Error("SMTP_HOST is required in production (event:email.smtp_required_in_production)");` else emit `log.warn({ event: "email.smtp_not_configured" }, "SMTP not configured; emails will not be delivered")` and return the dev-fallback sender.
    Otherwise read `port = Number(env.SMTP_PORT ?? "587")`, `user = env.SMTP_USER`, `password = env.SMTP_PASSWORD` (NOT SMTP_PASS — user decision 7), `from = env.SMTP_FROM ?? "no-reply@openwhispr.local"`. Compute `secure = env.SMTP_SECURE !== undefined ? env.SMTP_SECURE === "true" : port === 465`. Compute `rejectUnauthorized = env.SMTP_REJECT_UNAUTHORIZED !== "false"`. Build `auth = user && password ? { user, pass: password } : undefined`. Build `tls = rejectUnauthorized ? undefined : { rejectUnauthorized: false }`. Call `nodemailer.createTransport({ host, port, secure, auth, ...(tls ? { tls } : {}) })`.
    Return `EmailSender` whose `send` invokes `transporter.sendMail({ from, to, subject, text, html })` inside try/catch; success logs `event: "email.sent"` info + returns `{ delivered: true }`; failure logs `event: "email.failed"` error + re-throws.
    Create `packages/email/src/index.ts` re-exporting all public types + `createEmailSender`.
    Create `packages/email/src/EmailSender.test.ts` mirroring `apps/api/src/email.test.ts` pattern (mock nodemailer via `vi.mock("nodemailer", ...)`, env cleanup in beforeEach/afterEach). Add Test 4 (prod loud-fail) and Tests 6/7 (SMTP_SECURE + SMTP_REJECT_UNAUTHORIZED env overrides) and Test 8 (plain-object Logger).
    Create `packages/email/README.md` documenting the env-var contract. Include explicit note: `SMTP_PASSWORD` (NOT `SMTP_PASS`) — keeps the existing wired contract; user decision 7 overrides CONTEXT D-07's draft `SMTP_PASS`. Document `NODE_ENV=production` loud-fail. ENGLISH ONLY per CLAUDE.md.
  </action>
  <verify>
    <automated>pnpm vitest run packages/email</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/email` exits 0 with ≥ 8 tests passing.
    - Coverage report shows ≥ 90/90/90/90 on `packages/email/src/EmailSender.ts`.
    - `grep -E "SMTP_PASSWORD" packages/email/src/EmailSender.ts` returns ≥ 1 match.
    - `grep -E "SMTP_PASS\b" packages/email/src/EmailSender.ts` returns 0 matches (the bare `SMTP_PASS` env name is NOT used).
    - `grep -E "email\\.smtp_required_in_production" packages/email/src/EmailSender.ts` returns ≥ 1 match.
    - `grep -v '^//' packages/email/README.md | grep -c "SMTP_PASSWORD"` ≥ 1.
    - `grep -v '^//' packages/email/src/EmailSender.ts | grep -c "FastifyBaseLogger"` = 0 (Logger is structural; no Fastify coupling).
  </acceptance_criteria>
  <done>packages/email is a self-contained, Fastify-decoupled shared package with prod loud-fail and ≥90/90/90/90 coverage.</done>
</task>

<!-- =================== WAVE 1 — GREEN IMPLEMENTATION =================== -->

<task type="auto" tdd="true">
  <name>Task 13-01-05: Harden /api/health with migrations_completed + update contract schema (RED: vitest test asserts new field)</name>
  <files>apps/api/src/routes/health.ts, apps/api/src/routes/health.test.ts, packages/contract-tests/schemas/health.ts</files>
  <read_first>
    - apps/api/src/routes/health.ts (lines 1–40 — current handler returns `{ status: "ok" }`)
    - packages/contract-tests/schemas/health.ts (verify shape; locate HealthResponse zod schema)
    - tools/lint-rls.ts lines 154–157 (pattern: `new Client({ connectionString }).connect()`)
    - apps/api/src/db.ts or wherever the existing pool is exported (reuse pool, not one-shot client)
    - apps/api/src/migrate.ts or `_meta.__drizzle_migrations` table layout (referenced from STATE.md decisions)
  </read_first>
  <behavior>
    - Test 1: `GET /api/health` returns `{ status: "ok", migrations_completed: true }` when `_meta.__drizzle_migrations` has ≥ 1 row.
    - Test 2: `GET /api/health` returns `{ status: "ok", migrations_completed: false }` when the table is empty or unreachable.
    - Test 3: `packages/contract-tests/schemas/health.ts` HealthResponse zod schema includes `migrations_completed: z.boolean()`.
    - Test 4: Contract conformance fixture asserting `HealthResponse.parse(...)` succeeds with the new field present.
  </behavior>
  <action>
    Modify `apps/api/src/routes/health.ts` handler to invoke a new internal helper `checkMigrationsCompleted(pool): Promise<boolean>` that runs `SELECT COUNT(*) > 0 AS done FROM _meta.__drizzle_migrations` against the existing api pool (reuse pool — NOT a one-shot client). On any error, return `false` (do not throw — the handler must still return 200 for liveness). Return `{ status: "ok", migrations_completed }`.
    Modify `packages/contract-tests/schemas/health.ts` zod schema to add `migrations_completed: z.boolean()`. Keep schema open or closed per existing convention (Read first to decide).
    Modify or create `apps/api/src/routes/health.test.ts` with Tests 1+2 using Fastify `inject` + a mocked pool. Add Test 3+4 in `packages/contract-tests` test suite if conformance fixture exists.
    Search all contract conformance fixtures asserting the HealthResponse shape and update them: `grep -rln "HealthResponse\\|/api/health" packages/contract-tests/` then update assertions in-place.
  </action>
  <verify>
    <automated>pnpm vitest run apps/api/src/routes/health.test.ts packages/contract-tests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/api/src/routes/health.test.ts` exits 0 with ≥ 2 tests.
    - `pnpm vitest run packages/contract-tests` exits 0 (no regressions).
    - `grep -v '^//' apps/api/src/routes/health.ts | grep -c "migrations_completed"` ≥ 1.
    - `grep -v '^//' packages/contract-tests/schemas/health.ts | grep -c "migrations_completed"` ≥ 1.
    - Coverage ≥ 90/90/90/90 on `apps/api/src/routes/health.ts` diff.
  </acceptance_criteria>
  <done>Readiness probe field added; contract schema updated atomically; conformance suite still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-06: Weak-assertion sweep — rewrite all 7 sites (9 occurrences) across SignUpForm + NoteDetailClient + NotesListClient tests</name>
  <files>apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx, apps/web/src/components/screens/notes/__tests__/NoteDetailClient.test.tsx, apps/web/src/components/screens/notes/__tests__/NotesListClient.test.tsx</files>
  <read_first>
    - apps/web/src/components/screens/auth/__tests__/SignUpForm.test.tsx lines 130–195 (3 sites: 147, 165, 186)
    - apps/web/src/components/screens/notes/__tests__/NoteDetailClient.test.tsx lines 350–380 (2 sites: 360, 370)
    - apps/web/src/components/screens/notes/__tests__/NotesListClient.test.tsx lines 120–305 (4 sites: 127, 166, 276, 295 — note line 166 uses `toBeGreaterThanOrEqual(2)`)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md §"MODIFY apps/web/.../SignUpForm.test.tsx" (replacement patterns)
  </read_first>
  <behavior>
    - Test 1: Existing assertions remain semantically correct after rewrite — the test files still validate the same component behavior. Run the apps/web vitest suite and assert all rewritten tests pass.
    - Test 2: `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0.
  </behavior>
  <action>
    For each of the 7 sites, replace `.getAllBy<Matcher>(...).length.toBeGreaterThan(0)` (and `.toBeGreaterThanOrEqual(N)` family) with one of:
    - `expect(await screen.findByText(/.../)).toBeInTheDocument()` — when the assertion is "exactly one element exists eventually" (auto-waits). Use for all 3 SignUpForm sites where the assertion is duplicate-banner regression detection (UICONF-06).
    - `expect(screen.getAllByText(/.../)).toHaveLength(N)` — when multiple matches are semantically expected. Use for NotesListClient line 166 where N=2 ("Work" labels in fixture data) and NoteDetailClient lines 360/370 where N matches the actual em-dash placeholder count (Read fixture to confirm N per site).
    Sweep until `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0. If the linter flags a site that wasn't in the enumerated 7, fix it too (sweep is exhaustive).
    Do NOT modify production code (only test files).
  </action>
  <verify>
    <automated>pnpm vitest run apps/web &amp;&amp; pnpm tsx tools/lint-weak-assertions.ts apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run apps/web` exits 0 with no regression vs current baseline (510 unit + 85 e2e per STATE.md).
    - `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0 with empty stderr.
    - `grep -rE "\\.(getAllBy|queryAllBy|findAllBy)\\w+\\([^)]*\\)\\.length\\.toBeGreaterThan(OrEqual)?\\(" apps/web/src/components/screens/` returns 0 matches.
    - `git diff --stat apps/web/src/components/screens/` shows ≥ 9 line changes across exactly 3 files.
  </acceptance_criteria>
  <done>All 7 sites swept; lint script exits clean against apps/web; no production-code changes (test-only diff).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-01-07: compose-harness + mailpit-helper + wait-for-readiness + world.ts + auth.steps.ts (RED: readiness-probe.test.ts asserts probe contract)</name>
  <files>tests/e2e-cjm/support/compose-harness.ts, tests/e2e-cjm/support/mailpit-helper.ts, tests/e2e-cjm/support/wait-for-readiness.ts, tests/e2e-cjm/support/world.ts, tests/e2e-cjm/steps/auth.steps.ts, tools/__tests__/readiness-probe.test.ts</files>
  <read_first>
    - tests/e2e/compose-helper.ts (lines 1–100 — `BACKEND_URL`, `HERMETIC_ENV`, `run()`, `compose()`, `bringStackUp()` patterns to wrap)
    - tests/e2e/helpers/phase6-compose.ts (compose project name reuse, --no-deps, withProjectName pattern)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md §"tests/e2e-cjm/support/compose-harness.ts" + §"readiness-probe contract"
    - playwright-bdd 8.4.2 docs (importTestFrom signature for world.ts)
  </read_first>
  <behavior>
    - Test 1 (readiness-probe.test.ts): `waitForReadiness()` polls Postgres `SELECT 1`, GET `/api/health` (asserts `migrations_completed === true`), GET `http://localhost:8025/api/v1/messages` (mailpit), GET `https://app.localhost/` — uses exponential backoff with a HARD CAP of 90s. Throws on timeout. NO retry loop in suite (D-12 — flake is a bug).
    - Test 2: `bootStack()` invokes `docker compose --profile default up -d` (TD-14.f workaround per user decision 5).
    - Test 3: `mailpit-helper.ts` exports `waitForEmail(toAddress, subjectRegex, timeoutMs)` that polls `GET http://localhost:8025/api/v1/messages?query=to:<addr>` with exponential backoff capped at the passed timeout (default 30s); throws on timeout; returns the parsed message envelope on success.
    - Test 4: `mailpit-helper.ts` exports `extractVerificationLink(messageId): Promise<string>` that GETs `/api/v1/message/<id>` and regex-extracts a `https://app.localhost/verify-email?token=...` URL from the HTML body.
  </behavior>
  <action>
    Create `tests/e2e-cjm/support/compose-harness.ts`. Export constants `BASE_URL = "https://app.localhost"`, `API_URL = "https://api.localhost"`, `MAILPIT_API = "http://localhost:8025/api/v1"`. Export `async function bootStack(): Promise<void>` that spawns `docker compose --profile default up -d` (PATTERNS.md §"compose-harness.ts" — explicit profile to defeat TD-14.f). On non-zero exit, throw `Error("compose up failed: <code>")`. After successful `up`, call `waitForReadiness()` (imported from `./wait-for-readiness.ts`).
    Export `async function tearStack(): Promise<void>` running `docker compose down -v --remove-orphans` followed by `docker container prune -f --filter label=org.testcontainers=true`.
    Create `tests/e2e-cjm/support/wait-for-readiness.ts` exporting `async function waitForReadiness(opts?: { timeoutMs?: number }): Promise<void>`. Probe sequence:
      1. Postgres: `new Client({ connectionString: env.DATABASE_URL_OWNER ?? env.DATABASE_URL }).connect()` → `SELECT 1` → end.
      2. Fastify: `fetch("https://api.localhost/api/health", { ... ignoreHTTPSErrors via NODE_TLS_REJECT_UNAUTHORIZED=0 }).then(r => r.json())` → assert `status === "ok" && migrations_completed === true`.
      3. Mailpit: `fetch("http://localhost:8025/api/v1/messages")` → 200.
      4. Web: `fetch("https://app.localhost/")` → 200.
    Exponential backoff: initial 250ms, factor 2, cap 8s per probe attempt, total budget 90s default. Throw `Error("waitForReadiness timed out after <ms>: <last-failed-probe>")` on timeout.
    Create `tests/e2e-cjm/support/mailpit-helper.ts` with `waitForEmail` and `extractVerificationLink` per behavior contract. Use the same backoff helper from `wait-for-readiness.ts` (factor it into a private `backoffPoll` util or inline if simpler). Default `waitForEmail` timeout = 30s.
    Replace the 3-line placeholder `tests/e2e-cjm/support/world.ts` (from Task 13-01-01) with the final playwright-bdd 8.4.2 `test` extension exporting a `Fixtures` object containing `bootStack`-aware setup + per-scenario tenant isolation (each scenario creates a unique tenant via `apps/api` admin endpoint OR via direct seed — choose the existing path used by tests/e2e). Match `importTestFrom` shape required by bddgen.config.ts.
    Create `tests/e2e-cjm/steps/auth.steps.ts` with the 4–6 step definitions needed by the two reference scenarios (`@cjm-1.1` signup-happy: "Given I am on the sign-up page" / "When I fill the form with <email>, <password>, <displayName>" / "And I submit" / "Then a verification email arrives at <email>" / "And clicking the verification link marks me as verified". `@cjm-1.2`: "Given an account already exists with <email>" / "When I attempt to sign up with <email> again" / "Then the form shows the already-registered banner exactly once"). Use `@playwright/test` from playwright-bdd's `createBdd()` export.
    Create `tools/__tests__/readiness-probe.test.ts` covering Test 1 with a stubbed compose stack (`vi.mock("node-fetch")` or `undici` MockAgent + a fake Postgres Client). Assert backoff respects timeout; assert all 4 probes are invoked in order; assert it throws when `/api/health` returns `migrations_completed: false`.
  </action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/readiness-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tools/__tests__/readiness-probe.test.ts` exits 0 with ≥ 4 tests.
    - `grep -v '^//' tests/e2e-cjm/support/compose-harness.ts | grep -c -- "--profile.*default"` ≥ 1.
    - `grep -v '^//' tests/e2e-cjm/support/wait-for-readiness.ts | grep -c "migrations_completed"` ≥ 1.
    - `grep -v '^//' tests/e2e-cjm/support/mailpit-helper.ts | grep -c "extractVerificationLink"` = 1.
    - `grep -rE "retry: [^0]|retries: [^0]" tests/e2e-cjm/` returns 0 matches (D-12).
  </acceptance_criteria>
  <done>Compose harness boots default profile (TD-14.f-safe), readiness probes enforce migrations_completed, mailpit helper round-trips verification link, step file ready for reference scenarios.</done>
</task>

<!-- =================== WAVE 2 — ATOMIC INTEGRATION COMMIT (D-04) =================== -->

<task type="auto" tdd="true">
  <name>Task 13-01-08: ATOMIC COMMIT (D-04) — wire createEmailSender into apps/worker + delete/shim apps/api/src/email.ts + author @cjm-1.1 + @cjm-1.2 scenarios + Makefile e2e-cjm + GHA e2e-cjm.yml — VERIFY GREEN end-to-end</name>
  <files>apps/worker/src/index.ts, apps/worker/package.json, apps/api/src/email.ts, tests/e2e-cjm/features/signup-verify.feature, Makefile, .github/workflows/e2e-cjm.yml</files>
  <read_first>
    - apps/worker/src/index.ts lines 1–140 (replace lines 66–72 noopSender decl + line 130 sender wiring)
    - apps/worker/package.json (add `"@openwhispr/email": "workspace:*"` dependency)
    - apps/api/src/email.ts (decide: delete OR re-export shim based on importer count via `grep -rln "from.*api/src/email\\|from.*\"../email\"" apps/api/src/`)
    - Makefile lines 1–10 (.PHONY list), 249–253 (`e2e-hermetic` analog), 279–298 (`e2e-test-phase6` for E2E=1 gate)
    - .github/workflows/ci.yml lines 385–432 (`e2e-hermetic` job — exact analog per user decision 5; independent compose boot)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md §"e2e-cjm.yml" + §"Makefile target e2e-cjm"
  </read_first>
  <behavior>
    - Test 1: `apps/worker/src/index.ts` no longer contains `noopSender` declaration; imports `createEmailSender` from `@openwhispr/email`; constructs `realSender` at module scope; wires `sender: realSender` at emailWorker construction.
    - Test 2: `apps/api/src/email.ts` is either deleted (if ≤5 importers, all updated to `@openwhispr/email`) OR is a 6-line re-export shim from `@openwhispr/email`.
    - Test 3: `tests/e2e-cjm/features/signup-verify.feature` contains `@cjm-1.1` Scenario "Signup happy path — verification email round-trip" AND `@cjm-1.2` Scenario "Sign-up rejected when email is already registered" (negative twin).
    - Test 4: `make e2e-cjm` (with E2E_CJM=1) on a clean clone boots the stack, runs bddgen, executes playwright with `--grep-invert "@expected-red"`, and exits 0 with both `@cjm-1.1` and `@cjm-1.2` GREEN.
    - Test 5: `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.1` exits 0; `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.2` exits 0.
    - Test 6: `.github/workflows/e2e-cjm.yml` boots its OWN compose stack (does NOT reuse `e2e-hermetic`), pins all third-party actions by SHA, includes an `always()` step running `docker container prune --filter label=org.testcontainers=true`.
    - Test 7: `make e2e-cjm` without `E2E_CJM=1` exits non-zero with the refusal message.
    - **ATOMIC-COMMIT INVARIANT (D-04):** `git log -1 --stat` for the integration commit shows all of: `apps/worker/src/index.ts`, `packages/email/` files, `tools/lint-weak-assertions.ts`, the 3 sweep test files, `apps/api/src/routes/health.ts`, `tests/e2e-cjm/features/signup-verify.feature` with both scenarios, `Makefile`, `.github/workflows/e2e-cjm.yml` in a single commit.
  </behavior>
  <action>
    Stage every file produced or modified by Tasks 13-01-01 through 13-01-07, plus the new file edits in this task, into a single commit. Do not `git commit` anywhere prior to this task. All prior task outputs MUST be present in the working tree but uncommitted.

    Modify `apps/worker/src/index.ts`: remove lines 66–72 (`noopSender` const declaration). Add `import { createEmailSender } from "@openwhispr/email";` at the top with other package imports. Before `main()`, add `const realSender = createEmailSender({ log, env: process.env });` (will loud-fail at module init in production if SMTP_HOST unset — D-07). Replace line 130 `sender: noopSender` with `sender: realSender`.
    Modify `apps/worker/package.json` dependencies to add `"@openwhispr/email": "workspace:*"`.
    Delete `apps/api/src/email.ts` IF importer count ≤ 5 (run `grep -rln "from.*\\./email\\b\\|from.*api/src/email" apps/api/src/` first). Update every importer to `import { ... } from "@openwhispr/email"`. Map `makeEmailService` → `createEmailSender`, `EmailService` type → `EmailSender` type. IF importer count > 5, leave `apps/api/src/email.ts` as a 6-line re-export shim: `export { createEmailSender as makeEmailService } from "@openwhispr/email"; export type { SendArgs, SendResult, EmailSender as EmailService, Logger } from "@openwhispr/email";`.
    Author `tests/e2e-cjm/features/signup-verify.feature` REPLACING the Task 13-01-01 placeholder content. Write:
      - Feature header: `Feature: Signup with email verification` + 1-line description.
      - `@cjm-1.1` Scenario "Signup happy path — verification email round-trip": Given the user is on the sign-up page; When they submit valid credentials (email = unique-per-scenario, password ≥ 8 chars, displayName); Then a verification email arrives at their address within 30s (via mailpit-helper.waitForEmail); And clicking the verification link transitions the account to verified (assert via `GET /api/auth/get-session` or equivalent live API).
      - `@cjm-1.2` Scenario "Sign-up rejected when email is already registered" (negative twin): Given an account already exists with email <X>; When the user submits the sign-up form with the same email; Then the form shows the already-registered banner exactly once (assert `await screen.findByText` count via Playwright `locator.count() === 1`) — explicitly tests UICONF-06 duplicate-banner regression.
    Add `tests/e2e-cjm/steps/auth.steps.ts` step bodies covering every Given/When/Then in those scenarios. Use `@playwright/test`'s `expect` and the `createBdd()`-returned `Given/When/Then` from playwright-bdd. Each scenario MUST provision its own unique tenant (per D-13) — use the same provisioning helper as worker-scoped Playwright fixtures from Phase 07.1 (read tests/e2e/ for the existing pattern; if absent, generate `tenant-<uuid>` per scenario via direct API call).
    Add `Makefile` target `e2e-cjm` per PATTERNS.md §"Makefile target":
      - Refuses to run unless `E2E_CJM=1`.
      - Refuses to run if `.env` is missing.
      - Runs `docker compose --profile default up -d`.
      - Runs `pnpm exec tsx tests/e2e-cjm/support/wait-for-readiness.ts` (CLI entry that imports + invokes `waitForReadiness()`).
      - Runs `pnpm exec bddgen --config tests/e2e-cjm/bddgen.config.ts`.
      - Runs `pnpm exec playwright test --config tests/e2e-cjm/playwright.config.ts --grep-invert "@expected-red" $${SCENARIO:+--grep "$${SCENARIO}"}` with `NODE_TLS_REJECT_UNAUTHORIZED=0` set.
      - Captures the exit code, runs teardown (`docker compose down -v --remove-orphans` + `docker container prune -f --filter label=org.testcontainers=true`), exits with the captured code.
      - Append `e2e-cjm` to the existing `.PHONY` list (Makefile line 5).
    Create `.github/workflows/e2e-cjm.yml` per PATTERNS.md §"e2e-cjm.yml". Mirror `ci.yml` job `e2e-hermetic` (lines 385–432) EXACTLY for SHA-pinned actions, `harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450`, `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd`, `pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1`, `actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444`, `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` — with version-tag comments. Use `runs-on: ubuntu-24.04`, `timeout-minutes: 25`, `needs: [lint, typecheck, test]`, `env: { E2E_CJM: "1" }`. Steps: harden → checkout fetch-depth 0 → setup-pnpm v11.0.8 → setup-node 24 cache pnpm → `pnpm install --frozen-lockfile` → `pnpm exec playwright install --with-deps chromium` → bootstrap fixture .env via `tools/bootstrap.sh --ci || true` → add `/etc/hosts` entries `127.0.0.1 app.localhost api.localhost auth.localhost mailpit.localhost` → `docker compose build api migrate worker` → `make e2e-cjm` → on `failure()` capture compose logs + upload-artifact → on `always()` `docker container prune -f --filter "label=org.testcontainers=true" || true` → on `always()` `docker compose down -v --remove-orphans || true`.
    Run the full proof: `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.1` AND `E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.2` — assert exit 0 for both. ONLY THEN stage every Wave 0 / Wave 1 / Wave 2 file with `git add -A` and produce the single atomic commit. Commit message subject: `feat(13-01): ship e2e-cjm harness + replace worker noopSender with real EmailSender + close testcontainers leak + lint weak assertions`.
  </action>
  <verify>
    <automated>E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.1 &amp;&amp; E2E_CJM=1 make e2e-cjm SCENARIO=@cjm-1.2 &amp;&amp; grep -v '^//' apps/worker/src/index.ts | grep -c "noopSender" | xargs -I{} test {} -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' apps/worker/src/index.ts | grep -c "noopSender"` = 0.
    - `grep -v '^//' apps/worker/src/index.ts | grep -c "createEmailSender"` ≥ 1.
    - `grep -c "@openwhispr/email" apps/worker/package.json` ≥ 1.
    - `E2E_CJM=1 make e2e-cjm` exits 0 and reports both `@cjm-1.1` and `@cjm-1.2` GREEN in the playwright summary.
    - `make e2e-cjm` (without `E2E_CJM=1`) exits non-zero with refusal text matching `/E2E_CJM=1 required/`.
    - `grep -v '^#' .github/workflows/e2e-cjm.yml | grep -c "label=org.testcontainers=true"` ≥ 1.
    - `grep -v '^#' .github/workflows/e2e-cjm.yml | grep -c "if: always"` ≥ 1.
    - `grep -c "e2e-cjm" Makefile` ≥ 2 (target + .PHONY entry).
    - `git log -1 --name-only` for the final atomic commit lists ALL of: `apps/worker/src/index.ts`, `packages/email/src/EmailSender.ts`, `tools/lint-weak-assertions.ts`, the 3 swept test files under `apps/web/src/components/screens/`, `apps/api/src/routes/health.ts`, `tests/e2e-cjm/features/signup-verify.feature`, `Makefile`, `.github/workflows/e2e-cjm.yml`.
    - `git log --oneline` between the phase-13 branch base and HEAD shows EXACTLY ONE commit produced by plan 13-01 (i.e. no intermediate Wave 0 or Wave 1 commits exist).
    - Phase verifier coverage report shows ≥ 90/90/90/90 on `packages/email/src/**` and `tools/lint-weak-assertions.ts` and `tools/global-vitest-teardown.ts`.
  </acceptance_criteria>
  <done>Atomic D-04 commit lands; harness is GREEN end-to-end with both reference scenarios; worker no longer silently drops emails; lint rule active; testcontainers leak closed in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nodemailer SMTP transport → external SMTP server | Untrusted credentials in env vars (SMTP_USER, SMTP_PASSWORD); TLS strictness gated by SMTP_REJECT_UNAUTHORIZED |
| Cucumber Playwright browser → Fastify api over https://api.localhost | Trusts self-signed dev TLS; production behavior NOT exercised by harness — that's Phase 17 |
| Mailpit HTTP API at localhost:8025 → test harness | Test-only fixture; never exposed outside compose network |
| docker compose stack → host filesystem | Volume mounts include `~/.docker/config.json` indirectly (testcontainers ryuk) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | packages/email/EmailSender.ts | mitigate | Log `event: "email.sent"` carries `messageId` only; subject and body never logged. Existing pattern verbatim from apps/api/src/email.ts:121 |
| T-13-02 | Denial of service | apps/worker createEmailSender at module init | mitigate | Loud-fail at boot when `NODE_ENV === "production" && !SMTP_HOST` (D-07) — fails fast rather than silently dropping verification emails (the TD-mailpit / TD-13.c brownfield bug) |
| T-13-03 | Tampering | tools/global-vitest-teardown.ts | accept | `docker container prune --filter label=org.testcontainers=true` is scoped by label; cannot prune unlabeled containers. No mitigation needed. |
| T-13-04 | Repudiation | tests/e2e-cjm uses real auth API | accept | Per-scenario tenant isolation (D-13) + unique email per scenario → no shared state to repudiate |
| T-13-05 | Spoofing | mailpit-helper.ts polls mailpit by `to:<addr>` | mitigate | Each scenario generates a unique `tenant-<uuid>+<scenario-id>@e2e.local` email; mailpit query is exact-match on `to:` field; collision probability negligible |
| T-13-06 | Elevation of privilege | apps/worker realSender now uses real SMTP_USER/PASSWORD in CI | mitigate | CI env uses mailpit credentials only (no real SMTP); production-only loud-fail prevents accidental dev creds reaching prod |
| T-13-07 | Information disclosure | apps/api/src/routes/health.ts `migrations_completed` field | mitigate | Field is a boolean — zero PII. Endpoint is unauthenticated by design (liveness probe for orchestrators) and already exposes `status: "ok"`. New field reveals only schema readiness state. |
</threat_model>

<verification>
- All 8 tasks GREEN.
- `make e2e-cjm` exits 0 on a clean `git clone`.
- `pnpm vitest run packages/email tools` exits 0 with ≥ 90/90/90/90 coverage.
- `pnpm tsx tools/lint-weak-assertions.ts apps/web` exits 0.
- `git log -1 --name-only` for the atomic commit covers the D-04 file set.
- `git log --oneline` confirms exactly ONE commit produced by plan 13-01 (no intermediate Wave 0 or Wave 1 commits).
- GHA `e2e-cjm` workflow validates locally via `act` (best-effort) and remotely on first PR push.
- No regression in existing `make e2e-hermetic` or `make test`.
</verification>

<success_criteria>
- ROADMAP success criterion #1 (partial — happy + negative twin GREEN for reference scenarios; full 8-feature coverage lands in 13-02).
- ROADMAP success criterion #3 (atomic harness + worker noopSender→nodemailer commit) — FULLY MET.
- ROADMAP success criterion #4 (testcontainers leak closed + ESLint-equivalent weak-assertion rule + sweep) — FULLY MET via tsx-script linter (user decision 2) + 7-site sweep (user decision 3).
- ROADMAP success criterion #5 (readiness probes gate scenarios, per-scenario tenant isolation, retry-on-flake BANNED) — FULLY MET.
- Coverage gate: ≥ 90/90/90/90 on `packages/email/src/**` + `tools/lint-weak-assertions.ts` + `tools/global-vitest-teardown.ts`.
- ROADMAP success criterion #2 (`docs/customer-journeys.md` enumerates ~20 journeys; 8 feature files) — DELIVERED IN 13-02.
</success_criteria>

<output>
After completion, create `.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-01-SUMMARY.md` documenting:
- The atomic D-04 commit SHA and the file inventory it touched.
- Confirmation that `git log --oneline` shows exactly ONE commit produced by plan 13-01.
- The 7 weak-assertion sites swept (file, line, before/after pattern).
- The `apps/api/src/email.ts` decision (deleted vs shim, and the importer count).
- Coverage figures per package.
- Both reference scenarios' wall-clock runtime under `make e2e-cjm`.
- Any deviations from this plan (e.g., importer count > 5 → shim retained).
</output>
