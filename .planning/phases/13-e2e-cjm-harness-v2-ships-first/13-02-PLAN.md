---
phase: 13-e2e-cjm-harness-v2-ships-first
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - docs/customer-journeys.md
  - tools/lint-cjm-doc.ts
  - tools/lint-cjm-doc.test.ts
  - tests/e2e-cjm/features/signin.feature
  - tests/e2e-cjm/features/password-reset.feature
  - tests/e2e-cjm/features/transcribe.feature
  - tests/e2e-cjm/features/admin-onboarding.feature
  - tests/e2e-cjm/features/locale-switch.feature
  - tests/e2e-cjm/features/oidc-providers.feature
  - tests/e2e-cjm/features/error-paths.feature
  - tests/e2e-cjm/fixtures/silent.wav
  - tests/e2e-cjm/steps/signin.steps.ts
  - tests/e2e-cjm/steps/password-reset.steps.ts
  - tests/e2e-cjm/steps/transcribe.steps.ts
  - tests/e2e-cjm/steps/admin.steps.ts
  - tests/e2e-cjm/steps/locale.steps.ts
  - tests/e2e-cjm/steps/oidc.steps.ts
  - tests/e2e-cjm/steps/error-paths.steps.ts
  - tests/e2e-cjm/support/fixtures.ts
  - Makefile
  - .github/workflows/e2e-cjm.yml
autonomous: true
requirements:
  - E2E-02
  - E2E-05
  - E2E-06
  - E2E-07
  - E2E-08
tags: [e2e, cucumber, gherkin, cjm, documentation, customer-journeys]

must_haves:
  truths:
    - "docs/customer-journeys.md enumerates ~20 named user journeys with `@cjm-N.M` Gherkin anchors"
    - "Every happy-path journey in docs/customer-journeys.md has at least one negative-twin journey in the same section"
    - "Every `.feature` file ships at least one happy path AND one negative twin scenario (no happy-path-only features)"
    - "tools/lint-cjm-doc.ts exits non-zero if any `.feature` Scenario tag lacks a matching docs/customer-journeys.md §N.M anchor"
    - "Downstream-phase scenarios for Phase 5/6/7 land tagged `@expected-red @after-phase-N` and are filtered by `--grep-invert \"@expected-red\"` in CI"
    - "Running `make e2e-cjm` GREEN-includes 13-01's @cjm-1.1 + @cjm-1.2 AND every new in-phase scenario for Phases 13"
  artifacts:
    - path: "docs/customer-journeys.md"
      provides: "Canonical CJM document — ~20 journeys, happy + negative-twin pairs, @cjm-N.M anchors"
      min_lines: 200
      contains: "@cjm-1.1"
    - path: "tools/lint-cjm-doc.ts"
      provides: "Two-mode linter: (a) validate CJM doc happy/negative pairing; (b) validate .feature ↔ CJM cross-reference"
      contains: "--features"
    - path: "tests/e2e-cjm/features/signin.feature"
      provides: "@cjm-2.* signin scenarios incl. 403 unverified + resend CTA (E2E-05)"
      contains: "@cjm-2."
    - path: "tests/e2e-cjm/features/transcribe.feature"
      provides: "@cjm-4.* transcribe round-trip (E2E-06)"
      contains: "@cjm-4."
    - path: "tests/e2e-cjm/features/admin-onboarding.feature"
      provides: "@cjm-5.* admin landing + @expected-red @after-phase-12 wizard scenarios (E2E-07)"
      contains: "@expected-red"
    - path: "tests/e2e-cjm/features/locale-switch.feature"
      provides: "@cjm-6.* locale switch incl. @expected-red @after-phase-15 (E2E-08)"
      contains: "@expected-red"
  key_links:
    - from: "tools/lint-cjm-doc.ts"
      to: "docs/customer-journeys.md"
      via: "markdown parse — every @cjm-N.M anchor → require sibling negative twin"
      pattern: "@cjm-\\\\d+\\\\.\\\\d+"
    - from: "tools/lint-cjm-doc.ts --features"
      to: "tests/e2e-cjm/features/**/*.feature"
      via: "scenario-tag → doc-anchor cross-check"
      pattern: "--features"
    - from: ".github/workflows/e2e-cjm.yml"
      to: "tools/lint-cjm-doc.ts"
      via: "lint step before make e2e-cjm"
      pattern: "lint-cjm-doc"
    - from: "tests/e2e-cjm/features/admin-onboarding.feature"
      to: "@expected-red @after-phase-12"
      via: "tag annotation"
      pattern: "@expected-red"
---

<objective>
Author the canonical `docs/customer-journeys.md` (CJM doc) BEFORE any new `.feature` file lands (D-10 hard rule), ship the remaining 7 Gherkin feature files covering ~18 additional scenarios (~20 total when combined with 13-01's `signup-verify.feature`), and embed downstream-phase RED scenarios as `@expected-red @after-phase-N` tagged Scenarios filtered out of the default `--grep-invert "@expected-red"` CI run.

Purpose: Satisfy ROADMAP success criterion #2 (~20 journeys, every happy path has at least one negative twin). Unblock Phases 12, 14, 15, 16, 17 to write their RED tests by removing `@expected-red` tags as each phase ships. Encode the negative-twin invariant as machine-checkable via `tools/lint-cjm-doc.ts`.

Output: New `docs/customer-journeys.md` (~250 lines), new `tools/lint-cjm-doc.ts` two-mode linter + tests, 7 new `.feature` files + step files, CI wiring that runs the CJM-doc lint before bddgen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-CONTEXT.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-RESEARCH.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-PATTERNS.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-VALIDATION.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Inputs from 13-01 (already landed by the time 13-02 runs). -->

From 13-01 (must exist before 13-02 starts):
- `packages/email/` shared package
- `tests/e2e-cjm/{playwright.config.ts, bddgen.config.ts, support/compose-harness.ts, support/mailpit-helper.ts, support/wait-for-readiness.ts, support/world.ts, steps/auth.steps.ts}`
- `tests/e2e-cjm/features/signup-verify.feature` with `@cjm-1.1` + `@cjm-1.2`
- `Makefile` target `e2e-cjm` with `--grep-invert "@expected-red"` already wired
- `.github/workflows/e2e-cjm.yml`
- `tools/lint-weak-assertions.ts` (sibling utility — share pattern for `lint-cjm-doc.ts`)

CJM journey roster (D-08, locked):
- 1. signup-verify.feature — `@cjm-1.*` (HAPPY `@cjm-1.1` + 4 negative twins: `@cjm-1.2` already-registered, `@cjm-1.3` password<8 per-field error, `@cjm-1.4` locale-scoped error copy, `@cjm-1.5` social-button gating when zero providers) — ALREADY ships in 13-01 for 1.1 + 1.2; 13-02 adds 1.3, 1.4, 1.5
- 2. signin.feature — `@cjm-2.*` (HAPPY `@cjm-2.1` + negative twin `@cjm-2.2` 403 unverified path w/ resend CTA, E2E-05)
- 3. password-reset.feature — `@cjm-3.*` (HAPPY `@cjm-3.1` + negative twin `@cjm-3.2` invalid-token)
- 4. transcribe.feature — `@cjm-4.*` (HAPPY `@cjm-4.1` multipart → response shape match, E2E-06; + negative twin `@cjm-4.2` malformed audio → typed-error envelope)
- 5. admin-onboarding.feature — `@cjm-5.*` (`@cjm-5.1` admin landing returns real page, E2E-07; `@cjm-5.2` basicauth break-glass; downstream `@cjm-5.3 @expected-red @after-phase-12` wizard happy path)
- 6. locale-switch.feature — `@cjm-6.*` (`@cjm-6.1` locale-switch en↔ru sets cookie, E2E-08; downstream `@cjm-6.2 @expected-red @after-phase-15` /api/locale routing through api.localhost host split)
- 7. oidc-providers.feature — `@cjm-7.*` (`@cjm-7.1` zero providers → zero buttons; downstream `@cjm-7.2 @expected-red @after-phase-12` one configured provider → exactly one button rendered)
- 8. error-paths.feature — `@cjm-8.*` (`@cjm-8.1` 4xx error renders typed envelope `{ error: { code, message } }`; `@cjm-8.2` 5xx renders generic friendly screen, NOT raw stack)

Atomic tagging contract (user decision 4):
- Downstream-phase scenarios use BOTH tags: `@expected-red` (filter from CI by default) AND `@after-phase-N` (signal which phase removes the `@expected-red` tag).
- CI Makefile (13-01) already runs `--grep-invert "@expected-red"`.
- When Phase 12 ships, that phase removes the `@expected-red` tag from `@cjm-5.3` and `@cjm-7.2` (leaving `@after-phase-12` as a comment-marker only).

Lint contract for tools/lint-cjm-doc.ts (D-10 enforcement):
- Mode 1 (default, no flag): Read `docs/customer-journeys.md`. Extract every `@cjm-N.M` anchor. Group by N (feature ordinal). For each N: assert at least 2 anchors AND at least one with "negative" / "error" / "fails" / "rejected" / "twin" keyword in its journey title (the negative-twin invariant). Exit 1 if any happy-only group exists.
- Mode 2 (`--features` flag): Glob `tests/e2e-cjm/features/**/*.feature`. For each `Scenario` line preceded by an `@cjm-N.M` tag, assert a matching `§N.M` heading or `<a name>` anchor exists in `docs/customer-journeys.md`. Exit 1 on any orphan tag.
- Mode 3 (`--features --check-expected-red`): Validate that every `@expected-red` tag is paired with an `@after-phase-N` tag.
- Exit codes 0/1/2 per `tools/lint-english.ts` contract.

Shared step infrastructure (NEW — `tests/e2e-cjm/support/fixtures.ts`):
- Re-export `createBdd()` from `playwright-bdd` once; every `*.steps.ts` imports `Given, When, Then` from there.
- Export `freshTenant()` helper (per-scenario unique tenant per D-13).
- Export `signedInAs(email, password)` helper (programmatic sign-in via API to skip the form when not under test).
</interfaces>
</context>

<tasks>

<!-- =================== WAVE 0 — CJM DOC + LINTER (RED) =================== -->

<task type="auto" tdd="true">
  <name>Task 13-02-01: docs/customer-journeys.md authored first (D-10 hard rule) — ~20 journeys with @cjm-N.M anchors + happy/negative pairing</name>
  <files>docs/customer-journeys.md</files>
  <read_first>
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-CONTEXT.md §"CJM journey roster" (D-08, D-09, D-10)
    - .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-RESEARCH.md §"CJM doc structure"
    - docs/wire-contracts-phase-3.md (role-match analog for heading-driven structure per PATTERNS.md)
    - tests/e2e-cjm/features/signup-verify.feature (13-01 output — anchors `@cjm-1.1` + `@cjm-1.2` already exist)
  </read_first>
  <behavior>
    - The file enumerates 8 top-level sections (1 per feature in the locked roster).
    - Each section has at least 2 `@cjm-N.M` anchors: at least one HAPPY entry and at least one NEGATIVE TWIN entry.
    - Every error branch the backend emits on a given journey AND every silent-failure mode (worker noop, no email, no observability) appears under its journey section.
    - Downstream-phase scenarios are documented under their journey section with an `(after-phase-N — currently @expected-red)` marker.
    - File contains no emojis (CLAUDE.md hard rule).
    - File is ENGLISH ONLY.
  </behavior>
  <action>
    Write `docs/customer-journeys.md` with this structure:
    - H1: `Customer Journeys`. Intro paragraph: "Each journey enumerates a real user path through OpenWhispr. Every happy path has at least one negative twin in the same section. Anchors `@cjm-N.M` correspond 1:1 to `Scenario` tags in `tests/e2e-cjm/features/`. Downstream phases remove their `@expected-red` tag once shipped."
    - H2 `## 1. Signup with email verification` — anchors:
      - `### @cjm-1.1 Signup happy path` (user opens /sign-up, fills email+password+displayName, submits, receives verification email, clicks link, account flips to verified)
      - `### @cjm-1.2 Already-registered email dedup (negative twin)` (existing account → form shows already-registered banner exactly once)
      - `### @cjm-1.3 Password shorter than 8 chars (negative twin)` (per-field Zod error rendered next to the password input)
      - `### @cjm-1.4 Locale-scoped error copy (negative twin)` (Accept-Language=ru → error rendered in Russian; closes UICONF-03)
      - `### @cjm-1.5 Zero providers → zero social buttons (negative twin)` (capabilities API returns empty providers → no OidcButtons rendered)
    - H2 `## 2. Sign in` — anchors:
      - `### @cjm-2.1 Sign-in happy path`
      - `### @cjm-2.2 403 unverified — resend CTA visible (negative twin)` — closes E2E-05 / TD-13.c
    - H2 `## 3. Password reset` — anchors:
      - `### @cjm-3.1 Password-reset happy path`
      - `### @cjm-3.2 Invalid-token error (negative twin)`
    - H2 `## 4. Transcribe round-trip` — anchors:
      - `### @cjm-4.1 Multipart audio → response shape match` (E2E-06)
      - `### @cjm-4.2 Malformed audio → typed-error envelope (negative twin)`
    - H2 `## 5. Admin onboarding` — anchors:
      - `### @cjm-5.1 /admin reaches a real page` (E2E-07; closes TD-12.a)
      - `### @cjm-5.2 Basicauth break-glass (negative-context twin)`
      - `### @cjm-5.3 Wizard happy path (after-phase-12 — currently @expected-red)` — first-run wizard flips setup_state to completed
    - H2 `## 6. Locale switch` — anchors:
      - `### @cjm-6.1 en↔ru cookie set` (E2E-08)
      - `### @cjm-6.2 /api/locale routing via api.localhost host split (after-phase-15 — currently @expected-red)`
    - H2 `## 7. OIDC providers` — anchors:
      - `### @cjm-7.1 Zero providers configured → zero buttons (negative twin)`
      - `### @cjm-7.2 One provider configured → exactly one button (after-phase-12 — currently @expected-red)`
    - H2 `## 8. Error paths` — anchors:
      - `### @cjm-8.1 4xx renders typed envelope { error: { code, message } }`
      - `### @cjm-8.2 5xx renders friendly screen — NEVER raw stack (negative twin)`
    Each anchor section MUST contain: a 1–3 line user-story description; "Backend error branches:" sub-list enumerating every non-2xx the journey can emit; "Silent-failure modes:" sub-list (e.g., for §1.1: "worker noopSender silently drops verification email (closed by Phase 13-01)").
    NO emojis. ENGLISH only.
  </action>
  <verify>
    <automated>test -f docs/customer-journeys.md &amp;&amp; grep -E "^### @cjm-[0-9]+\\.[0-9]+" docs/customer-journeys.md | wc -l | xargs -I{} test {} -ge 18</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^### @cjm-[0-9]+\\.[0-9]+" docs/customer-journeys.md | wc -l` ≥ 18 (≥ 18 anchors; target is ~20 including downstream).
    - `grep -cE "^## [0-9]+\\." docs/customer-journeys.md` = 8 (exactly 8 H2 sections matching the locked roster).
    - For each of N ∈ {1..8}: `grep -E "^### @cjm-${N}\\." docs/customer-journeys.md | wc -l` ≥ 2 (every section has ≥ 2 anchors).
    - `grep -E "negative|twin|error|fails|rejected" docs/customer-journeys.md | wc -l` ≥ 8 (every section signals at least one negative-twin entry).
    - `grep -cE "after-phase-[0-9]+" docs/customer-journeys.md` ≥ 3 (downstream `@expected-red` markers present for phases 12 + 15).
    - `pnpm tsx tools/lint-english.ts docs/customer-journeys.md` exits 0 (English-only).
    - `grep -P "[\\x{1F300}-\\x{1FAFF}\\x{2600}-\\x{27BF}]" docs/customer-journeys.md` returns 0 matches (no emojis).
  </acceptance_criteria>
  <done>Canonical CJM doc landed BEFORE any new feature file — D-10 hard rule satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13-02-02: tools/lint-cjm-doc.ts two-mode linter + 6 vitest tests (RED first against the just-authored CJM doc — must PASS once linter implements rules)</name>
  <files>tools/lint-cjm-doc.ts, tools/lint-cjm-doc.test.ts</files>
  <read_first>
    - tools/lint-weak-assertions.ts (sibling — copy shebang, exit-code contract, file-scan skeleton)
    - tools/lint-docs-headings.ts (analog per PATTERNS.md for markdown parsing)
    - tools/lint-english.test.ts (execFileSync test pattern via tmpdir fixture)
    - docs/customer-journeys.md (just-authored — Mode 1 input)
    - tests/e2e-cjm/features/signup-verify.feature (13-01 output — Mode 2 cross-check input)
  </read_first>
  <behavior>
    - Test 1 (Mode 1, default): Running against the just-authored `docs/customer-journeys.md` exits 0 (all 8 sections have happy + negative twin).
    - Test 2 (Mode 1, regression): Running against a fixture CJM doc with a section containing ONLY a happy entry exits 1 with stderr listing the offending section.
    - Test 3 (Mode 2, `--features`): Running with `tests/e2e-cjm/features/` containing a `@cjm-99.1` Scenario whose anchor is absent from the doc exits 1.
    - Test 4 (Mode 2 happy): Running with all 13-01 features against the just-authored doc exits 0.
    - Test 5 (Mode 3, `--check-expected-red`): Running against a fixture .feature with `@expected-red` but no `@after-phase-N` exits 1.
    - Test 6: Internal error (CJM doc not found) exits 2.
  </behavior>
  <action>
    Create `tools/lint-cjm-doc.ts` with shebang `#!/usr/bin/env -S pnpm exec tsx`, SPDX-License-Identifier header. Argv parser: positional CJM doc path (default `docs/customer-journeys.md`), flag `--features <glob>` (default `tests/e2e-cjm/features/**/*.feature`), flag `--check-expected-red`.
    Mode 1 (always runs): parse CJM doc. Extract anchors via regex `/^### @cjm-(\d+)\.(\d+)/gm`. For each captured `(N, M)`: also capture the rest of the heading line. Group by N. For each N: assert `count ≥ 2` AND assert at least one heading contains a negative-twin keyword (regex `/negative|twin|error|fails|rejected|invalid|malformed/i`). Push offenders for each violation. Exit 1 if offenders non-empty.
    Mode 2 (`--features` present): glob feature files. For each file, extract every `@cjm-N.M` tag (regex `/@cjm-(\d+)\.(\d+)/g`). For each tag: assert a matching `### @cjm-N.M` heading exists in the CJM doc. Push offenders for each orphan tag.
    Mode 3 (`--check-expected-red` present, requires `--features`): for each feature, find every Scenario block tagged `@expected-red`; assert it is also tagged `@after-phase-<number>` (regex `/@after-phase-\d+/`). Push offenders for each unpaired `@expected-red`.
    Stderr format: `CJM lint violation: N offender(s).` then one line per offender `file:line:col  message`. Exit-code contract: 0/1/2.
    Create `tools/lint-cjm-doc.test.ts` covering all 6 behaviors using `execFileSync` + tmpdir fixtures pattern.
  </action>
  <verify>
    <automated>pnpm vitest run tools/lint-cjm-doc.test.ts &amp;&amp; pnpm tsx tools/lint-cjm-doc.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tools/lint-cjm-doc.test.ts` exits 0 with ≥ 6 tests passing.
    - `pnpm tsx tools/lint-cjm-doc.ts` exits 0 against the in-repo CJM doc.
    - `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features` exits 0 against 13-01's `signup-verify.feature`.
    - `head -2 tools/lint-cjm-doc.ts | grep -c "pnpm exec tsx"` = 1.
    - Coverage ≥ 90/90/90/90 on `tools/lint-cjm-doc.ts`.
  </acceptance_criteria>
  <done>Two-mode linter committed with self-tests; CI gate can now enforce the D-10 "CJM doc before .feature" invariant + the @expected-red ↔ @after-phase-N pairing rule.</done>
</task>

<!-- =================== WAVE 1 — 7 NEW FEATURE FILES + STEP DEFS =================== -->

<task type="auto" tdd="true">
  <name>Task 13-02-03: Author 7 feature files + step defs covering @cjm-2.* .. @cjm-8.* + missing 1.3/1.4/1.5 — happy + negative twins + @expected-red downstream scenarios</name>
  <files>tests/e2e-cjm/features/signin.feature, tests/e2e-cjm/features/password-reset.feature, tests/e2e-cjm/features/transcribe.feature, tests/e2e-cjm/features/admin-onboarding.feature, tests/e2e-cjm/features/locale-switch.feature, tests/e2e-cjm/features/oidc-providers.feature, tests/e2e-cjm/features/error-paths.feature, tests/e2e-cjm/features/signup-verify.feature, tests/e2e-cjm/fixtures/silent.wav, tests/e2e-cjm/steps/signin.steps.ts, tests/e2e-cjm/steps/password-reset.steps.ts, tests/e2e-cjm/steps/transcribe.steps.ts, tests/e2e-cjm/steps/admin.steps.ts, tests/e2e-cjm/steps/locale.steps.ts, tests/e2e-cjm/steps/oidc.steps.ts, tests/e2e-cjm/steps/error-paths.steps.ts, tests/e2e-cjm/support/fixtures.ts</files>
  <read_first>
    - tests/e2e-cjm/features/signup-verify.feature (13-01 output — Gherkin style baseline)
    - tests/e2e-cjm/steps/auth.steps.ts (13-01 output — step-definition baseline)
    - tests/e2e-cjm/support/world.ts (13-01 output — world fixture shape)
    - docs/customer-journeys.md (13-02-01 output — every @cjm-N.M anchor must map to a Scenario tag)
    - apps/api/src/routes/transcribe.ts (round-trip contract for @cjm-4.1)
    - apps/api/src/routes/auth/* (verify endpoints for signin/password-reset)
    - apps/web/src/app/(public)/sign-in/page.tsx (UI route for @cjm-2.*)
    - apps/web/src/app/(admin)/admin/page.tsx (verify @cjm-5.1 — should currently 404 if TD-12.a not yet closed by Phase 12; if so, @cjm-5.1 ships RED until Phase 12 — author it as @expected-red @after-phase-12 INSTEAD)
  </read_first>
  <behavior>
    - Each new feature file contains at least 2 Scenarios (1 happy + 1 negative twin).
    - Downstream-phase scenarios MUST carry both `@expected-red` AND `@after-phase-<N>` tags.
    - Running `make e2e-cjm` (which already passes `--grep-invert "@expected-red"` per 13-01) GREEN for every in-phase scenario; @expected-red scenarios are invisible to the default run.
    - Per-scenario tenant isolation enforced (D-13) via `freshTenant()` helper from `support/fixtures.ts`.
    - `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features` exits 0 (every Scenario tag has a matching doc anchor).
    - `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` exits 0 (every `@expected-red` is paired with `@after-phase-N`).
  </behavior>
  <action>
    Create `tests/e2e-cjm/support/fixtures.ts` exporting:
      - `createBdd(test)` re-export from `playwright-bdd` (one canonical call site so step files share the same test fixture).
      - `freshTenant(): Promise<{ tenantId, email, password, displayName }>` — generates a uuid-suffixed email `e2e+<uuid>@local.test`, provisions via the existing api seed/admin path (Read tests/e2e/ helpers first to reuse, do NOT invent).
      - `signedInAs(page, email, password): Promise<void>` — programmatic API sign-in via `POST /api/auth/sign-in/email` then cookies attached to the Playwright context, skipping the form when the form is NOT under test.

    Extend `tests/e2e-cjm/features/signup-verify.feature` (already exists from 13-01) by appending the 3 remaining `@cjm-1.*` Scenarios:
      - `@cjm-1.3 Password shorter than 8 chars`: submits password length 6 → asserts per-field Zod error message rendered next to the password input via Playwright `locator('[data-testid="password-error"]')` (or whatever testid SignUpForm uses — Read first).
      - `@cjm-1.4 Locale-scoped error copy`: sets `Accept-Language: ru` cookie/header → submits invalid form → asserts error copy matches Russian translation (Read `apps/web/locales/ru/*.json` for expected string).
      - `@cjm-1.5 Zero providers → zero social buttons`: against the stack which currently has zero OIDC providers configured → asserts `OidcButtons` renders zero buttons (count == 0). Closes UICONF-02 + E2E-03's social-button-gating twin.

    Create `tests/e2e-cjm/features/signin.feature` with:
      - `@cjm-2.1 Sign-in happy path`: verified user signs in → redirected to `/notes` (or current authed landing per `apps/web` routing).
      - `@cjm-2.2 Sign-in rejected for unverified email — resend CTA shown`: unverified user signs in → 403 path → asserts resend-verification CTA button is visible (closes TD-13.c, E2E-05).

    Create `tests/e2e-cjm/features/password-reset.feature` with:
      - `@cjm-3.1 Password-reset happy path`: requests reset → mailpit-helper.waitForEmail → clicks link → sets new password → can sign in.
      - `@cjm-3.2 Invalid-token error`: GET `/reset-password?token=garbage` → asserts invalid-token banner.

    Create `tests/e2e-cjm/features/transcribe.feature` (E2E-06) with:
      - `@cjm-4.1 Multipart audio → response shape match`: signed-in user POSTs a small WAV fixture to `/api/transcribe` → asserts response JSON matches `BACKEND_SPEC.md` transcribe response shape (delegate the byte-for-byte assertion to packages/contract-tests/schemas — the feature only asserts 200 + presence of `text` key). Use a real audio fixture at `tests/e2e-cjm/fixtures/silent.wav` (8KB silent PCM) — generate it inline via `ffmpeg` if absent and commit.
      - `@cjm-4.2 Malformed audio → typed-error envelope`: POST non-audio bytes → assert 415 or 422 with `{ error: { code, message } }`.

    Create `tests/e2e-cjm/features/admin-onboarding.feature` (E2E-07) with:
      - `@cjm-5.1 @expected-red @after-phase-12 /admin reaches a real page`: GET `/admin` → asserts 200 + page heading "Admin" (TAGGED EXPECTED-RED because TD-12.a is NOT yet closed; Phase 12 removes the tag).
      - `@cjm-5.2 Basicauth break-glass`: GET `/admin` with HTTP basic-auth credentials configured in env → asserts 200 path.
      - `@cjm-5.3 @expected-red @after-phase-12 Wizard happy path`: GET `/setup` (route ships in Phase 12) → completes single-page wizard → asserts setup_state flips to 'completed' + user is logged in as admin.

    Create `tests/e2e-cjm/features/locale-switch.feature` (E2E-08) with:
      - `@cjm-6.1 en↔ru cookie set`: clicks locale toggle → asserts `Set-Cookie: openwhispr-locale=ru` in response → reload renders Russian copy on landing.
      - `@cjm-6.2 @expected-red @after-phase-15 /api/locale routing via api.localhost host split`: GET `https://api.localhost/api/locale` (NOT `https://app.localhost/api/locale`) → asserts 200 (Phase 15 closes TD-15.g host-split routing).

    Create `tests/e2e-cjm/features/oidc-providers.feature` with:
      - `@cjm-7.1 Zero providers configured → zero buttons`: stack has zero OIDC providers → `/sign-in` renders zero OidcButtons (count == 0).
      - `@cjm-7.2 @expected-red @after-phase-12 One provider configured → exactly one button`: env configures one OIDC provider via `OIDC_PROVIDERS_JSON` (Phase 12 contract) → `/sign-in` renders exactly one button (count == 1).

    Create `tests/e2e-cjm/features/error-paths.feature` with:
      - `@cjm-8.1 4xx renders typed envelope { error: { code, message } }`: hit a known-4xx endpoint (e.g., `GET /api/transcribe` with no body) → assert response body matches `{ error: { code: string, message: string } }`.
      - `@cjm-8.2 5xx renders friendly screen, NEVER raw stack`: induce a 5xx via a route that 500s when given a known-bad param (read apps/api routes for one), or use a fault-injection env-switch if available → assert HTML response body does NOT contain `at Object.<anonymous>` or `node_modules/` substrings (no stack trace leakage).

    Generate and commit `tests/e2e-cjm/fixtures/silent.wav` (8KB silent PCM WAV at 16kHz mono) via local `ffmpeg -f lavfi -i anullsrc=r=16000:cl=mono -t 0.25 -acodec pcm_s16le tests/e2e-cjm/fixtures/silent.wav`. Stage as a binary fixture in the same commit as the transcribe feature file.

    For each new feature, write the matching `*.steps.ts` file that imports `Given/When/Then` from `tests/e2e-cjm/support/fixtures.ts` and implements every Given/When/Then phrase. Each scenario calls `freshTenant()` at the Given step to provision an isolated tenant.

    NO emojis. ENGLISH only. NO `retry:` or `retries:` overrides anywhere (D-12).
  </action>
  <verify>
    <automated>pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red &amp;&amp; E2E_CJM=1 make e2e-cjm</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features` exits 0.
    - `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` exits 0.
    - `grep -rE "@cjm-[0-9]+\\.[0-9]+" tests/e2e-cjm/features/ | wc -l` ≥ 18 (matches CJM doc anchor count).
    - `grep -rE "@expected-red" tests/e2e-cjm/features/ | wc -l` ≥ 3 (downstream Phase 12 + 15 scenarios tagged).
    - `grep -rE "@after-phase-[0-9]+" tests/e2e-cjm/features/ | wc -l` ≥ 3 (every @expected-red is paired).
    - `test -f tests/e2e-cjm/fixtures/silent.wav` and the file is committed (binary fixture present in the working tree and tracked by git).
    - `E2E_CJM=1 make e2e-cjm` exits 0 — all in-phase scenarios GREEN; @expected-red scenarios filtered by `--grep-invert`.
    - `grep -rE "retry: [^0]|retries: [^0]" tests/e2e-cjm/` returns 0 matches.
    - `grep -P "[\\x{1F300}-\\x{1FAFF}\\x{2600}-\\x{27BF}]" tests/e2e-cjm/features/ tests/e2e-cjm/steps/` returns 0 matches.
  </acceptance_criteria>
  <done>7 new feature files + step defs + extended signup-verify shipped; ~20 anchors live; @expected-red downstream scenarios tagged; full suite GREEN for in-phase scenarios.</done>
</task>

<!-- =================== WAVE 2 — CI WIRING + VERIFY =================== -->

<task type="auto" tdd="true">
  <name>Task 13-02-04: Wire tools/lint-cjm-doc.ts into Makefile + GHA e2e-cjm job — gate authored before bddgen runs</name>
  <files>Makefile, .github/workflows/e2e-cjm.yml</files>
  <read_first>
    - Makefile (existing `e2e-cjm` target from 13-01 Task 13-01-08)
    - .github/workflows/e2e-cjm.yml (existing job from 13-01)
    - tools/lint-cjm-doc.ts (just-shipped 13-02-02)
  </read_first>
  <behavior>
    - The `e2e-cjm` Makefile target runs `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` BEFORE `bddgen`. If it exits non-zero, the target exits without booting compose or running bddgen.
    - The GHA `e2e-cjm.yml` job has a step `Lint CJM doc and feature cross-refs` running the same command, placed AFTER `pnpm install` and BEFORE `playwright install`.
    - Failing the lint step short-circuits the workflow before docker compose costs are incurred.
  </behavior>
  <action>
    Append to the `e2e-cjm` Makefile target a step that runs `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` immediately after the existing E2E_CJM=1 gate and `.env` existence check, and BEFORE the `docker compose ... up` invocation. The line must be `@`-prefixed (silent shell echo) and exit-on-failure (Makefile default).
    Append to `.github/workflows/e2e-cjm.yml` a new step titled `Lint customer-journeys.md and feature cross-refs` that runs `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red`. Position the step AFTER the existing `pnpm install --frozen-lockfile` step and BEFORE the `Install Playwright Chromium` step (or BEFORE `Bootstrap fixture .env`, whichever runs first based on the 13-01 ordering). No other workflow changes — preserve all SHA-pins, timeouts, always() prune and teardown steps from 13-01.
  </action>
  <verify>
    <automated>grep -c "lint-cjm-doc" Makefile &amp;&amp; grep -c "lint-cjm-doc" .github/workflows/e2e-cjm.yml &amp;&amp; E2E_CJM=1 make e2e-cjm</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lint-cjm-doc" Makefile` ≥ 1.
    - `grep -c "lint-cjm-doc" .github/workflows/e2e-cjm.yml` ≥ 1.
    - In the Makefile, `lint-cjm-doc` line appears BEFORE the `docker compose ... up` line (verify via `awk` line-number comparison: `awk '/lint-cjm-doc/{a=NR} /docker compose.*up/{b=NR} END{exit !(a<b)}' Makefile`).
    - `E2E_CJM=1 make e2e-cjm` still exits 0 (no regression from 13-01's atomic commit).
    - Introducing a deliberate CJM-doc regression (manually break a section's negative-twin pairing temporarily) causes `make e2e-cjm` to exit non-zero BEFORE `docker compose up` runs (verify via dry-run on a scratch branch — DO NOT commit the regression).
  </acceptance_criteria>
  <done>CJM lint gates the e2e-cjm pipeline locally and in CI; D-10 invariant is now machine-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/customer-journeys.md → developer reading the file | Doc-only; no executable surface |
| tools/lint-cjm-doc.ts → CI runner | Runs against repo files only; no network |
| tests/e2e-cjm/features/transcribe.feature → /api/transcribe | Authenticated request; tenant-isolated per scenario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Information disclosure | error-paths.feature @cjm-8.2 | mitigate | The scenario itself ASSERTS that 5xx responses never leak stack traces — this threat is the test's reason for existing |
| T-13-02-02 | Tampering | @expected-red ↔ @after-phase-N pairing | mitigate | `tools/lint-cjm-doc.ts --check-expected-red` blocks PRs that introduce `@expected-red` without phase-marker — prevents "expected-red forever" rot |
| T-13-02-03 | Repudiation | docs/customer-journeys.md drift vs feature files | mitigate | Mode-2 lint (`--features`) enforces 1:1 anchor↔tag correspondence on every PR; CJM doc is the canonical source of truth |
| T-13-02-04 | Denial of service | freshTenant() per scenario could exhaust tenant pool | accept | tenant rows are uuid-keyed; per-scenario isolation is the explicit design (D-13). Cleanup happens via global-vitest-teardown + compose `down -v` on every CI run. |
</threat_model>

<verification>
- All 4 tasks GREEN.
- `pnpm tsx tools/lint-cjm-doc.ts` exits 0 against the in-repo CJM doc.
- `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` exits 0.
- `E2E_CJM=1 make e2e-cjm` exits 0 with all in-phase scenarios GREEN; @expected-red scenarios are filtered out (invisible to the runner — NOT skipped).
- Coverage ≥ 90/90/90/90 on `tools/lint-cjm-doc.ts`.
- `docs/customer-journeys.md` enumerates ≥ 18 anchors across exactly 8 sections; every section has at least one negative-twin entry.
</verification>

<success_criteria>
- ROADMAP success criterion #1 (full suite GREEN — happy + negative twin for every in-phase journey) — FULLY MET.
- ROADMAP success criterion #2 (~20 journeys in CJM doc; every happy path has a negative twin; doc enforced before features) — FULLY MET; D-10 machine-enforced.
- E2E-02, E2E-05, E2E-06, E2E-07, E2E-08 — covered by their respective feature files.
- Downstream Phases 12, 15 now have RED scenarios waiting to be flipped GREEN; tag-based filtering proves a flake-free path.
</success_criteria>

<output>
After completion, create `.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-02-SUMMARY.md` documenting:
- Final anchor count in `docs/customer-journeys.md` (target ~20).
- Per-feature scenario count (happy + negative-twin breakdown).
- Wall-clock runtime of `make e2e-cjm` after 13-02 lands (compare to 13-01 baseline).
- List of `@expected-red @after-phase-N` scenarios queued for downstream phases (Phase 12 → 2 scenarios; Phase 15 → 1 scenario; etc).
- Coverage figure for `tools/lint-cjm-doc.ts`.
- Any deviations (e.g., a Scenario re-tagged @expected-red because the underlying API endpoint turned out to not exist yet — surface as a follow-up phase concern).
</output>
