---
phase: 02.16
status: locked
date: 2026-05-10
mode: diagnose-then-fix
---

# Phase 02.16 — CONTEXT

## Goal

Close cascade tail Group H: api callback completion 500 on 3 oauth-redirect tests. Fully exercised end-to-end after Phase 02.15 (Group G transport closed). Server-side defect; needs diagnosis FIRST then targeted fix.

## Investigation plan (diagnose mode)

1. Reproduce: run `make contract-test`, capture api container logs at LOG_LEVEL=debug; identify the 500 stack trace
2. Classify root cause:
   - **(a)** Better Auth genericOAuth token-exchange fails against fixture-idp /token shape (id_token / access_token format)
   - **(b)** mintBearer (Phase 02.7-02) errors during internalAdapter.createOAuthUser/createSession
   - **(c)** auth-callback.ts oauth_state CAS-consume logic errors (Plan 05 holdover)
   - **(d)** Other (TBD by stack trace)

## Locked decisions

### D-01 — Diagnose-first, then narrow fix

Per "no workarounds" rule: do NOT add try/catch swallowing. Read the stack, identify the actual broken function, fix the actual defect.

### D-02 — TDD per fix shape

Once root cause known, the test pattern depends on classification:
- (a) → unit test for genericOAuth token-exchange via mock fixture-idp response
- (b) → unit test for mintBearer (Phase 02.7-02 has scaffolding to extend)
- (c) → unit test for auth-callback CAS consume
- (d) → TBD

Reverse-patch evidence per Phase 02.4 D-02 pattern.

### D-03 — Acceptance gate

1. New test RED before fix, GREEN after
2. Re-run E2E: 3 Group H tests advance past 500 (either green or surface NEW class)
3. Single atomic commit
4. Reverse-patch evidence

### D-04 — Out of scope

- Fixture-idp protocol enhancement beyond what BA strictly requires (defer)
- Multi-IdP genericOAuth array (already supported)
- mycorp-whispr scheme env wiring (Group E variant — Phase 02.17 candidate)
