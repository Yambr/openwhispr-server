---
phase: 02.21
status: locked
date: 2026-05-10
mode: diagnose-then-fix
---

# Phase 02.21 — CONTEXT

## Goal

Close last 2-3 cascade tail Group C residuals (final phase before Phase 02.7 closure):
- conventions 404 envelope returns 401 (envelope-shape mismatch on missing route?)
- delete-account cookie cascade gets 200 instead of 401 (cookie persistence bug?)
- token-rotation suite beforeAll sign-in 403 (test isolation? rate-limit residual?)

After this phase: contract-test reaches 26/26 GREEN, Phase 02.7 plans 06+07 finally close, 02-HUMAN-UAT.md Item 1 unconditionally ✅.

## Investigation plan (diagnose-first)

These are 3 separate failures likely with 3 separate root causes. Each needs:
1. Reproduce in isolation (run JUST that test file)
2. Capture api logs at LOG_LEVEL=debug
3. Identify the root cause
4. Apply targeted fix per CLAUDE.md rules (no workarounds)
5. Reverse-patch evidence per fix

## Locked decisions

### D-01 — Diagnose each independently before fixing

DO NOT bundle. Each gets its own analysis + fix commit. Bundling 3 diagnoses risks landing 3 fragile fixes.

### D-02 — Acceptance gate per residual

For each residual:
- Reproduce → diagnose → write failing unit test (if applicable, may be E2E only)
- Apply targeted fix
- Reverse-patch evidence

### D-03 — TDD per fix shape

Where unit test makes sense (e.g., conventions envelope shape — clearly testable in isolation), write it. Where E2E is the only signal (suite-isolation), document via the contract test as the RED→GREEN witness.

### D-04 — Out of scope

- Refactor to fix all 3 with a single architectural change (resist temptation)
- Suite-isolation framework changes (vitest config) — defer if a residual needs it; it's likely a per-test fix
- Cleanup pre-existing TS errors (logged in deferred-items.md across phases) — separate concern

## Files

To-be-determined post-diagnosis. Likely:
- conventions: apps/api error envelope on 404 routes
- delete-account: route handler cookie clearing on 401
- token-rotation: test setup OR rate-limit/state hygiene

## Acceptance gate

1. Each fix has reverse-patch evidence
2. E2E reaches 26/26 GREEN (or documents acceptable out-of-scope failures with strong rationale — e.g., "this 1 test is testing Phase 04 LiteLLM behavior wired in 02 only as a stub")
3. Each fix in its own atomic commit (3 commits total expected)
4. After this phase: re-run Phase 02.7-06 + 02.7-07 closure plans cleanly
