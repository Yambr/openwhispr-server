---
phase: 06-observability-ops-hardening-workers
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/otel-bootstrap.ts
  - apps/api/src/index.ts
  - apps/api/src/plugins/request-log.ts
  - apps/api/package.json
  - compose/grafana/provisioning/datasources/loki.yaml
  - tests/e2e/horizontal-scale.test.ts
  - tests/e2e/ssrf-block.test.ts
  - tests/e2e/audit-log-write.test.ts
  - tests/e2e/reconciliation-drift.test.ts
  - tests/e2e/log-scrub-sentinel.test.ts
  - tests/e2e/probes-dependency.test.ts
  - tests/e2e/rate-limit-layered.test.ts
  - tests/e2e/otel-trace-propagation.test.ts
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
threat_model_refs: [T-bearer-leak]
must_haves:
  truths:
    - "apps/api/src/otel-bootstrap.ts loads BEFORE pino import (apps/api/src/index.ts imports otel-bootstrap as the FIRST statement)"
    - "@opentelemetry/sdk-node + @opentelemetry/auto-instrumentations-node@0.75.0 + @opentelemetry/instrumentation-pino@0.63.0 wired"
    - "Instrumentations active: fastify, undici, pg, ioredis, bullmq + manual Better Auth spans"
    - "Instrumentations DISABLED: @opentelemetry/instrumentation-fs, @opentelemetry/instrumentation-dns (per D-T1)"
    - "@opentelemetry/instrumentation-pino injects trace_id/span_id/trace_flags into every pino log record emitted from inside an active span"
    - "pino redact paths configured: req.headers.authorization, req.headers.cookie, *.token, *.secret, *.password, *.apiKey, res.headers[set-auth-token], URL query code=/state= (D-T4)"
    - "Censor token: '[REDACTED]'"
    - "NO /metrics Prom-pull endpoint registered on the API (D-T6 single metrics path)"
    - "Grafana Loki datasource has derivedFields config linking trace_id JSON regex → Tempo datasource"
    - "8 e2e RED stub files exist under tests/e2e/ (completes the 31-stub Wave 0 inventory)"
  artifacts:
    - path: "apps/api/src/otel-bootstrap.ts"
      provides: "NodeSDK init with PinoInstrumentation + getNodeAutoInstrumentations (skip fs/dns)"
    - path: "compose/grafana/provisioning/datasources/loki.yaml"
      provides: "Loki ↔ Tempo derivedFields trace correlation"
  key_links:
    - from: "apps/api/src/index.ts line 1"
      to: "apps/api/src/otel-bootstrap.ts"
      via: "import side-effect BEFORE any other import"
      pattern: "^import.*otel-bootstrap"
    - from: "pino log line"
      to: "Tempo span"
      via: "Grafana Loki derivedFields regex `\"trace_id\":\"([a-f0-9]+)\"`"
      pattern: "trace_id.*tempo"
---

<objective>
Wave 0 telemetry bootstrap + final 8 e2e RED stubs.

Purpose: OTel SDK must initialize BEFORE pino is imported anywhere so PinoInstrumentation can patch pino at require-time and auto-inject trace_id into every log line (D-T3). This is the foundation Plans 10 (log scrub) and 11 (dashboards) depend on. Also materializes the e2e stubs to complete the Wave 0 31-file RED floor.
Output: otel-bootstrap module + pino redact config + Loki datasource derived-fields + 8 e2e stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md
@.planning/phases/06-observability-ops-hardening-workers/06-RESEARCH.md §3
@.planning/phases/06.1-add-tempo-mimir-minimal-filesystem-backed-configs-both-crash/06.1-CONTEXT.md
@CLAUDE.md
@apps/api/src/index.ts
@apps/api/src/plugins/request-log.ts
@apps/api/package.json
@compose/otel-collector/config.yaml
@apps/api/src/otel-bootstrap.test.ts
@apps/api/src/plugins/request-log.test.ts
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| pino log line → stdout → OTel Collector → Loki | secrets must be scrubbed AT SOURCE |
| Loki query result → Grafana Explore → Tempo trace link | trace_id is correlation key |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Info-disclosure | Bearer/cookie leak via pino logs | mitigate | pino `redact` config at source (D-T4); Plan 10 adds sentinel-token sweep test. NOT Collector-side scrubbing (raw secret would briefly exist in stdout). |
| T-06-07 | Tampering | OTel SDK boots AFTER pino import → trace_id missing from logs | mitigate | otel-bootstrap.ts imported as FIRST statement in apps/api/src/index.ts (load-order test verifies) |
| T-06-08 | Repudiation | trace_id present in log but Loki ↔ Tempo link broken | mitigate | Plan 11 e2e test in tests/e2e/otel-trace-propagation.test.ts asserts Grafana derivedFields resolves |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OTel SDK bootstrap + pino redact config + flip 2 Wave 0 stubs</name>
  <files>
    apps/api/src/otel-bootstrap.ts,
    apps/api/src/index.ts,
    apps/api/src/plugins/request-log.ts,
    apps/api/package.json,
    apps/api/src/otel-bootstrap.test.ts,
    apps/api/src/plugins/request-log.test.ts,
    compose/grafana/provisioning/datasources/loki.yaml
  </files>
  <read_first>
    .planning/phases/06-observability-ops-hardening-workers/06-RESEARCH.md §3 (verified versions + load order pattern),
    .planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md (D-T1 instrumentation scope, D-T3 pino correlation, D-T4 redact paths verbatim, D-T6 no /metrics, D-T7 English-only),
    apps/api/src/index.ts (current entry — note where pino is first imported),
    apps/api/src/plugins/request-log.ts (existing pino plugin),
    compose/otel-collector/config.yaml (06.1 D-04 otlphttp/loki pipeline)
  </read_first>
  <behavior>
    - otel-bootstrap.ts: instantiates `NodeSDK` with `getNodeAutoInstrumentations` + `PinoInstrumentation`; disables fs+dns instrumentations; configures logKeys to `trace_id`, `span_id`, `trace_flags`; calls `sdk.start()` synchronously at module top-level (no default export, side-effect-only)
    - apps/api/src/index.ts: VERY FIRST line `import "./otel-bootstrap.js";` (before any other import including pino, fastify)
    - request-log.ts: pino instance configured with `redact: { paths: [...], censor: '[REDACTED]' }` containing ALL D-T4 paths verbatim
    - NO `/metrics` route registered anywhere (verified by grepping for `'/metrics'` in route registration code → must return zero hits beyond /api/health alias)
    - Grafana loki.yaml derivedFields entry: name=TraceID, matcherRegex='"trace_id":"([a-f0-9]+)"', url='${__value.raw}', datasourceUid=tempo
    - otel-bootstrap.test.ts flipped GREEN: assert PinoInstrumentation in SDK instrumentations list; assert fs+dns disabled; assert load order via Node module resolution order test
    - request-log.test.ts flipped GREEN: log a payload containing `{token: 'sentinel-XYZ', headers: {authorization: 'Bearer abc'}}`; capture stdout; assert `sentinel-XYZ` absent; assert `[REDACTED]` present; assert log keys are English-only (no Cyrillic) via regex `/^[\x00-\x7F]+$/`
  </behavior>
  <action>
    Add npm deps to `apps/api/package.json`:
    ```
    "@opentelemetry/sdk-node": "^0.55.0",
    "@opentelemetry/auto-instrumentations-node": "^0.75.0",
    "@opentelemetry/instrumentation-pino": "^0.63.0",
    "@opentelemetry/api": "^1.9.0"
    ```

    Create `apps/api/src/otel-bootstrap.ts` per RESEARCH.md §3 lines 244-268 verbatim. Critical: this file has NO exports — it's a side-effect module that calls `sdk.start()` at top level.

    Update `apps/api/src/index.ts` so the FIRST line is `import "./otel-bootstrap.js";` (note the `.js` extension for ESM resolution). All other imports follow.

    Update `apps/api/src/plugins/request-log.ts` — add pino `redact` config object:
    ```ts
    const redactPaths = [
      'req.headers.authorization',
      'req.headers.cookie',
      'req.headers["set-cookie"]',
      'res.headers["set-auth-token"]',
      'res.headers["set-cookie"]',
      '*.token',
      '*.secret',
      '*.password',
      '*.apiKey',
      '*.api_key',
      '*.virtualKey',
      '*.client_secret',
      '*.access_token',
      '*.refresh_token',
      'req.body.password',
      'req.body.token',
      'req.query.code',
      'req.query.state',
    ];
    const log = pino({ redact: { paths: redactPaths, censor: '[REDACTED]' } });
    ```

    Create `compose/grafana/provisioning/datasources/loki.yaml`:
    ```yaml
    apiVersion: 1
    datasources:
      - name: Loki
        type: loki
        access: proxy
        url: http://loki:3100
        jsonData:
          derivedFields:
            - name: TraceID
              matcherRegex: '"trace_id":"([a-f0-9]+)"'
              url: '${__value.raw}'
              datasourceUid: tempo
    ```

    Flip the two RED stubs to GREEN: implement assertions per the behavior list. For load-order, use `vi.mock` to spy on pino import resolution timing OR introspect `process.moduleLoadList` after a fresh require of `apps/api/src/index.ts`.

    Verify NO `/metrics` route exists: `grep -r "'/metrics'" apps/api/src/routes apps/api/src/index.ts` MUST return zero hits.
  </action>
  <verify>
    <automated>pnpm -F @openwhispr/api test --run apps/api/src/otel-bootstrap.test.ts apps/api/src/plugins/request-log.test.ts &amp;&amp; ! grep -r "'/metrics'" apps/api/src/routes apps/api/src/index.ts 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/otel-bootstrap.ts` exists with `new NodeSDK(...)` + `sdk.start()`
    - `head -1 apps/api/src/index.ts` is `import "./otel-bootstrap.js";` (or equivalent FIRST-position import)
    - `grep -c "PinoInstrumentation" apps/api/src/otel-bootstrap.ts` ≥ 1
    - `grep -c "instrumentation-fs.*enabled: false" apps/api/src/otel-bootstrap.ts` ≥ 1
    - `grep -c "instrumentation-dns.*enabled: false" apps/api/src/otel-bootstrap.ts` ≥ 1
    - `grep -c "req.headers.authorization" apps/api/src/plugins/request-log.ts` ≥ 1
    - `grep -c "'\\[REDACTED\\]'" apps/api/src/plugins/request-log.ts` ≥ 1
    - `grep -rc "'/metrics'" apps/api/src/routes apps/api/src/index.ts 2>/dev/null | grep -v ':0$' | wc -l` returns 0
    - `compose/grafana/provisioning/datasources/loki.yaml` contains `derivedFields:` and `datasourceUid: tempo`
    - `pnpm -F @openwhispr/api test --run apps/api/src/otel-bootstrap.test.ts` passes
    - `pnpm -F @openwhispr/api test --run apps/api/src/plugins/request-log.test.ts` passes (sentinel scrubbed)
  </acceptance_criteria>
  <done>
    OTel SDK + pino redact + Loki↔Tempo correlation foundation in place. Wave 1+ handlers automatically get trace_id in logs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize 8 tests/e2e/ RED stubs (Wave 0 final)</name>
  <files>
    tests/e2e/horizontal-scale.test.ts,
    tests/e2e/ssrf-block.test.ts,
    tests/e2e/audit-log-write.test.ts,
    tests/e2e/reconciliation-drift.test.ts,
    tests/e2e/log-scrub-sentinel.test.ts,
    tests/e2e/probes-dependency.test.ts,
    tests/e2e/rate-limit-layered.test.ts,
    tests/e2e/otel-trace-propagation.test.ts
  </files>
  <read_first>
    .planning/phases/06-observability-ops-hardening-workers/06-VALIDATION.md (8 e2e stub list lines 87-94),
    .planning/phases/06-observability-ops-hardening-workers/06-RESEARCH.md §7 (testcontainers DockerComposeEnvironment.withScale pattern),
    .planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md (D-P3 horizontal-scale flow, D-S5 SSRF violation contract, D-R2 reconciliation gauge metrics)
  </read_first>
  <behavior>
    - horizontal-scale.test.ts: gated on `E2E=1`; spawns DockerComposeEnvironment with `.withScale('api', 2)`; signs in via Traefik; hits a session-protected endpoint 20x via Traefik round-robin; asserts ≥1 hit each replica via `x-served-by` header; all 20 return 200 with same session.id
    - ssrf-block.test.ts: gated on `E2E=1`; outbound request to 169.254.169.254 (or testcontainer nginx serving 302 redirect there) → asserts 502 + `{error: "Upstream blocked by SSRF policy", request_id}` + audit_log row with `action='security.ssrf_blocked'`
    - audit-log-write.test.ts: gated on `E2E=1`; performs auth.signin via real Better Auth; asserts audit_log row exists with `action='auth.signin'`, `payload.request_id`, `payload.ip`, `payload.user_agent` (truncated 512), `payload.method='password'`
    - reconciliation-drift.test.ts: gated on `E2E=1`; seeds mismatched usage_ledger vs LiteLLM_SpendLogs (drift > 0.5% rows OR > $0.01); triggers reconciliation-daily-check job; asserts Mimir gauge `litellm_reconciliation_drift_pct{tenant_id}` emitted with non-zero value; asserts reconciliation-discrepancy child job enqueued; asserts backfill runs ingest-litellm-spend idempotently and closes the drift
    - log-scrub-sentinel.test.ts: gated on `E2E=1`; POST to a route with `Authorization: Bearer SENTINEL-TOKEN-XYZ-${Date.now()}`; captures docker container logs of api service for 10s; asserts the sentinel string is NOT present anywhere in captured stdout
    - probes-dependency.test.ts: gated on `E2E=1`; pause postgres container via `docker pause`; assert `/livez` STILL returns 200; assert `/readyz` returns 503 within 6s (5s cache + 1s); resume postgres; assert `/readyz` returns 200 within 6s
    - rate-limit-layered.test.ts: gated on `E2E=1`; one user exceeds `/api/transcribe` 20/min/user → 429 with `X-RateLimit-Remaining: 0`; many sessions from same IP exceed 60/min/IP → 429 (IP-tier triggered); polling carve-out `/api/auth/verification-status` 30/min/(IP,email) still works
    - otel-trace-propagation.test.ts: gated on `E2E=1`; perform a request that emits a pino log; query Tempo for the span with that trace_id; assert span exists with `service.name='openwhispr-api'`; assert Loki query for the same trace_id returns the log line
  </behavior>
  <action>
    Create 8 vitest test files under `tests/e2e/`. Each follows this template:

    ```ts
    // Phase 6 Wave 0 RED stub — TDD-01b. Implementation in Plan {NN}.
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { DockerComposeEnvironment, Wait, type StartedDockerComposeEnvironment } from 'testcontainers';

    describe.skipIf(process.env.E2E !== '1')('Phase 6 e2e: {behavior}', () => {
      let env: StartedDockerComposeEnvironment;
      beforeAll(async () => {
        // TODO Plan {NN}: boot compose stack with appropriate profile + scale
        throw new Error('not yet implemented — Plan {NN} implements this e2e');
      }, 180_000);
      afterAll(async () => env?.down({ removeVolumes: true }));

      it('{specific behavior assertion from D-* anchor}', async () => {
        throw new Error('not yet implemented');
      });
    });
    ```

    Map each stub to its Plan number (per the planner_structure_guidance skeleton in the orchestrator context):
    - horizontal-scale → Plan 04 (probes) + Plan 12 (verification)
    - ssrf-block → Plan 06 + Plan 12
    - audit-log-write → Plan 05 + Plan 12
    - reconciliation-drift → Plan 08 + Plan 12
    - log-scrub-sentinel → Plan 10 + Plan 12
    - probes-dependency → Plan 04 + Plan 12
    - rate-limit-layered → Plan 09 + Plan 12
    - otel-trace-propagation → Plan 11 + Plan 12

    Each stub MUST include at least 3 `it(...)` blocks covering the behavior bullets above; all throw `not yet implemented`. Use `describe.skipIf(process.env.E2E !== '1')` so unit-CI doesn't fail on these.

    Reference the D-* anchor in each test name: `it('returns 502 with security.ssrf_blocked audit row per D-S5', ...)`.
  </action>
  <verify>
    <automated>test -f tests/e2e/horizontal-scale.test.ts &amp;&amp; test -f tests/e2e/ssrf-block.test.ts &amp;&amp; test -f tests/e2e/audit-log-write.test.ts &amp;&amp; test -f tests/e2e/reconciliation-drift.test.ts &amp;&amp; test -f tests/e2e/log-scrub-sentinel.test.ts &amp;&amp; test -f tests/e2e/probes-dependency.test.ts &amp;&amp; test -f tests/e2e/rate-limit-layered.test.ts &amp;&amp; test -f tests/e2e/otel-trace-propagation.test.ts &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 8 e2e stub files exist under tests/e2e/
    - Each contains `describe.skipIf(process.env.E2E !== '1')`
    - Each contains at least 3 `it(...)` blocks
    - Each references at least one D-* anchor in test names (grep for `D-S5|D-P3|D-A6|D-R2|D-T4|D-RL2` across the 8 files returns ≥ 8 hits)
    - `grep -l '169.254.169.254' tests/e2e/ssrf-block.test.ts` matches
    - `grep -l 'withScale' tests/e2e/horizontal-scale.test.ts` matches
    - `pnpm tsc --noEmit` exits 0
    - Without `E2E=1` set, vitest skips all 8 files (`pnpm vitest run tests/e2e` exits 0 with "skipped" status)
  </acceptance_criteria>
  <done>
    8 e2e RED stubs complete the 31-file Wave 0 floor. Plan 12 verification can now flip these to GREEN.
  </done>
</task>

</tasks>

<verification>
- OTel SDK bootstraps before pino across the api package
- pino redact paths cover every D-T4 entry
- NO /metrics endpoint exists on the API
- 31 Wave 0 stubs all materialized (14 from Plan 01 Task 1 + 9 from Plan 01 Task 2 + 8 from this plan Task 2)
- `pnpm tsc --noEmit` exits 0
</verification>

<success_criteria>
Telemetry + log-scrubbing foundation in place. All e2e RED stubs ready for Plan 12 to flip GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-ops-hardening-workers/06-03-SUMMARY.md`.
</output>
