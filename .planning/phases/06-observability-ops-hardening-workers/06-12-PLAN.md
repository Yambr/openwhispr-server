---
phase: 06-observability-ops-hardening-workers
plan: 12
type: execute
wave: 3
depends_on: [04, 05, 06, 07, 08, 09, 10, 11]
files_modified:
  - tests/e2e/horizontal-scale.test.ts
  - tests/e2e/ssrf-block.test.ts
  - tests/e2e/audit-log-write.test.ts
  - tests/e2e/reconciliation-drift.test.ts
  - tests/e2e/log-scrub-sentinel.test.ts
  - tests/e2e/probes-dependency.test.ts
  - tests/e2e/rate-limit-layered.test.ts
  - tests/e2e/otel-trace-propagation.test.ts
  - Makefile
  - .github/workflows/ci.yml
  - .github/workflows/nightly.yml
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04, OBS-05, DATA-04, SCALE-01, SCALE-03, SCALE-04]
threat_model_refs: [T-bearer-leak, T-cross-tenant-job, T-ssrf, T-rate-limit-bypass, T-audit-loss, T-readiness-cascade, T-rls-bypass-via-partition]
must_haves:
  truths:
    - "All 8 Wave 0 e2e RED stubs flipped to GREEN — boot real compose stack via testcontainers DockerComposeEnvironment"
    - "horizontal-scale.test.ts: scale api=2; 20 round-trip hits; ≥1 per replica via x-served-by; all 200; same session.id (SCALE-01)"
    - "ssrf-block.test.ts: outbound to 169.254.169.254 → 502 + audit_log row action='security.ssrf_blocked'"
    - "audit-log-write.test.ts: auth.signin emits canonical row with all D-A7 required keys + partition routing correct"
    - "reconciliation-drift.test.ts: seed mismatched ledger vs LiteLLM_SpendLogs; reconciliation-daily-check runs; Mimir gauges emitted; reconciliation-discrepancy enqueued; backfill closes drift"
    - "log-scrub-sentinel.test.ts: POST with `Authorization: Bearer SENTINEL-...`; capture docker container logs (api + worker); assert SENTINEL ABSENT"
    - "probes-dependency.test.ts: docker pause postgres → /livez stays 200; /readyz returns 503 within 6s; resume → 200 within 6s"
    - "rate-limit-layered.test.ts: exceed /api/transcribe 20/min/user → 429; many sessions same IP exceed 60/min → 429; verification-status carve-out preserved"
    - "otel-trace-propagation.test.ts: outbound request emits pino log → Tempo span exists for trace_id → Loki query for same trace_id returns the log line"
    - "make e2e-test green against real compose stack"
    - "Coverage ≥90/90/90/90 on EVERY new/modified file across Plans 01-11 (final phase gate)"
    - "Nightly GHA workflow extended to include the new e2e tests"
  artifacts:
    - path: "Makefile"
      provides: "make e2e-test-phase6 target invoking the 8 e2e tests"
    - path: ".github/workflows/nightly.yml"
      provides: "Nightly run of Phase 6 e2e suite"
  key_links:
    - from: "every Phase 6 must_have truth"
      to: "an e2e assertion in Wave 3"
      via: "Plan 12 verification gate"
      pattern: ".*\\.test\\.ts"
---

<objective>
Wave 3 verification gate: flip all 8 e2e stubs from RED to GREEN by implementing them against the real docker-compose stack. Audit coverage axes (lines/branches/functions/statements ≥90 on every new/modified Phase 6 file). Wire nightly CI.

Purpose: Constitutional CLAUDE.md gate — phase passes only when (a) every must_have observable truth is verified against live codebase, (b) coverage ≥ 90/90/90/90 on the diff, (c) e2e suite green via `make e2e-test`.
Output: 8 GREEN e2e tests + Makefile target + nightly workflow + coverage report demonstrating ≥90/90/90/90 across the phase diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md
@.planning/phases/06-observability-ops-hardening-workers/06-RESEARCH.md
@.planning/phases/06-observability-ops-hardening-workers/06-VALIDATION.md
@CLAUDE.md
@tests/e2e/horizontal-scale.test.ts
@tests/e2e/ssrf-block.test.ts
@tests/e2e/audit-log-write.test.ts
@tests/e2e/reconciliation-drift.test.ts
@tests/e2e/log-scrub-sentinel.test.ts
@tests/e2e/probes-dependency.test.ts
@tests/e2e/rate-limit-layered.test.ts
@tests/e2e/otel-trace-propagation.test.ts
@Makefile
@.github/workflows/ci.yml
@.github/workflows/nightly.yml
</context>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| e2e test → real compose stack | testcontainers spawn full LGTM + api + worker + postgres + valkey + litellm-mock |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| ALL T-* from prior plans | n/a | covered by their respective e2e | mitigate | This plan's 8 e2e tests prove each T-* mitigation actually fires against real services |
| T-06-21 | Tampering | E2E flake masks regression | mitigate | Retry once with 30s extra timeout per RESEARCH.md Risk table; fail loud after retry |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement 4 e2e tests — horizontal-scale + probes-dependency + ssrf-block + audit-log-write</name>
  <files>
    tests/e2e/horizontal-scale.test.ts,
    tests/e2e/probes-dependency.test.ts,
    tests/e2e/ssrf-block.test.ts,
    tests/e2e/audit-log-write.test.ts,
    Makefile
  </files>
  <read_first>
    .planning/phases/06-observability-ops-hardening-workers/06-RESEARCH.md §7 (testcontainers DockerComposeEnvironment.withScale pattern verbatim),
    .planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md (D-P3 scale test flow, D-S5 SSRF violation contract, D-A6 audit actions, D-A7 required payload keys),
    apps/api/src/plugins/served-by.ts (Plan 04 — x-served-by source),
    apps/api/src/lib/ssrf-dispatcher.ts (Plan 06 — 502 + audit emission),
    apps/api/src/lib/audit.ts (Plan 05 — recordAudit shape)
  </read_first>
  <behavior>
    - horizontal-scale.test.ts (SCALE-01, D-P3):
      1. `new DockerComposeEnvironment('.', 'docker-compose.yml').withProfile('default').withScale('api', 2).withWaitStrategy('api', Wait.forHttp('/livez', 3000)).up()`
      2. Sign in via Traefik → capture bearer cookie
      3. Hit GET /api/me (or /api/usage) 20 times via Traefik base URL
      4. Collect `x-served-by` header from each response
      5. Assert: 20 responses with status 200; ≥1 hit each replica's hostname (round-robin proves distribution); all responses carry same session.id (verify via /api/me payload)
    - probes-dependency.test.ts (OBS-05, T-readiness-cascade):
      1. Boot compose default profile
      2. Assert GET /livez returns 200, /readyz returns 200, /startupz returns 200
      3. `docker pause postgres` (via testcontainers container handle)
      4. Within 6 seconds: GET /livez STILL 200 (no dep checks); GET /readyz returns 503 (dep failure)
      5. `docker unpause postgres`
      6. Within 8 seconds: /readyz returns 200 (cache expires + dep recovers)
    - ssrf-block.test.ts (SCALE-04, T-ssrf):
      1. Boot compose with `OUTBOUND_ALLOWED_HOSTS=evil.test,litellm,...` and `OUTBOUND_ALLOW_LOOPBACK=0`
      2. Spawn a tiny testcontainer nginx that 302-redirects from /redir to `http://169.254.169.254/latest/meta-data/`
      3. Trigger an API endpoint that performs an outbound fetch — for the test, use a debug-only POST that takes a URL (gated by NODE_ENV=test only) OR exercise via Better Auth OIDC misconfig
      4. Assert response is 502 with body `{error: 'Upstream blocked by SSRF policy', request_id: '...'}`
      5. Query audit_log (via owner pool): assert row exists with `action='security.ssrf_blocked'` AND `payload.target_url_host` is `169.254.169.254` (or the redirect host) AND `payload.rule` is `link_local_v4`
    - audit-log-write.test.ts (DATA-04):
      1. Boot compose default
      2. Sign in via Better Auth (POST /api/auth/sign-in/email) with real fixture creds
      3. Wait 1s for the tx
      4. SELECT row from audit_log via owner pool WHERE action='auth.signin' AND tenant_id=fixture.tenant_id ORDER BY created_at DESC LIMIT 1
      5. Assert: row exists; payload.request_id is a valid UUID; payload.ip is an IPv4/IPv6 OR null (if AUDIT_REDACT_IP=true); payload.user_agent ≤512 chars; payload.method='password'
      6. Verify partition routing: row's `tableoid::regclass` resolves to `audit_log_YYYY_MM` matching `to_char(now(), 'YYYY_MM')`
    - Add Makefile target `e2e-test-phase6`:
      ```
      e2e-test-phase6: ## Phase 6 e2e suite
      \tE2E=1 pnpm vitest run tests/e2e/horizontal-scale.test.ts tests/e2e/probes-dependency.test.ts tests/e2e/ssrf-block.test.ts tests/e2e/audit-log-write.test.ts
      ```
  </behavior>
  <action>
    Use the existing Phase 3 e2e testcontainer compose pattern (refer to Phase 3 Plan 09 + Phase 4 Plan 09 which already drive `docker compose up`). Each test file boots its own DockerComposeEnvironment in beforeAll (180s timeout per RESEARCH.md §7).

    For horizontal-scale: `withScale('api', 2)` is the critical API. Verify Traefik picks up both replicas — if file-provider mode pins a single backend, may need to add a second `servers:` entry OR switch the test profile to docker-provider mode (`compose/traefik/static.yml` adjustment).

    For ssrf-block: the test needs to provoke an SSRF target. Two options — both acceptable:
    - (a) Add a NODE_ENV=test-only debug route `POST /__test/fetch?url=...` that performs an outbound fetch (gated by env). Plan 06 acknowledged this would be useful — confirm if it exists; if not, add it here.
    - (b) Misconfigure OIDC issuer URL to point at `http://169.254.169.254/.well-known/...` and trigger a sign-in attempt — the Better Auth genericOAuth dispatch will go through the SSRF gate.
    Pick (a) for simplicity + isolation; gate with `if (process.env.NODE_ENV !== 'test') return reply.code(404).send();`

    For audit-log-write: use the existing Phase 2 fixture user pattern (signInFixture helper). The fixture seed already has a valid user; this test does NOT need to create one.

    Add `make e2e-test-phase6` target. Update `make e2e-test` to call `e2e-test-phase6` as a sub-target so the global e2e gate exercises Phase 6.

    Each test MUST tear down the compose env in afterAll (`removeVolumes: true`).

    Implement retry-once-on-flake per RESEARCH.md Risk table — wrap the assertion in a small retry helper that gives 30s extra timeout on first failure.
  </action>
  <verify>
    <automated>E2E=1 pnpm vitest run tests/e2e/horizontal-scale.test.ts tests/e2e/probes-dependency.test.ts tests/e2e/ssrf-block.test.ts tests/e2e/audit-log-write.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 e2e test files no longer contain `throw new Error('not yet implemented')`
    - Each test uses `DockerComposeEnvironment` from testcontainers
    - `grep -l 'withScale.*api.*2' tests/e2e/horizontal-scale.test.ts` matches
    - `grep -l 'x-served-by' tests/e2e/horizontal-scale.test.ts` matches
    - `grep -l 'docker.*pause\\|pause.*postgres' tests/e2e/probes-dependency.test.ts` matches
    - `grep -l '169.254.169.254' tests/e2e/ssrf-block.test.ts` matches
    - `grep -l 'security.ssrf_blocked' tests/e2e/ssrf-block.test.ts` matches
    - `grep -l 'auth.signin' tests/e2e/audit-log-write.test.ts` matches
    - `Makefile` contains `e2e-test-phase6:` target
    - `E2E=1 make e2e-test-phase6` exits 0 on the developer machine OR in CI runner (allow up to 10 min total)
  </acceptance_criteria>
  <done>
    4 of 8 e2e tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 4 e2e tests — reconciliation-drift + log-scrub-sentinel + rate-limit-layered + otel-trace-propagation + coverage gate + nightly CI wiring</name>
  <files>
    tests/e2e/reconciliation-drift.test.ts,
    tests/e2e/log-scrub-sentinel.test.ts,
    tests/e2e/rate-limit-layered.test.ts,
    tests/e2e/otel-trace-propagation.test.ts,
    .github/workflows/ci.yml,
    .github/workflows/nightly.yml
  </files>
  <read_first>
    .planning/phases/06-observability-ops-hardening-workers/06-CONTEXT.md (D-R2 drift gauges, D-T4 redact paths, D-RL2 rpm matrix, D-T3 trace correlation),
    apps/worker/src/jobs/reconciliation-daily-check.ts (Plan 08),
    tests/integration/log-scrub-sentinel.test.ts (Plan 10 integration version this e2e mirrors at compose level),
    .github/workflows/nightly.yml (existing nightly pattern from Phase 3 Plan 10)
  </read_first>
  <behavior>
    - reconciliation-drift.test.ts (OBS-04):
      1. Boot compose with mock-litellm profile (hermetic, Phase 3)
      2. Seed usage_ledger with N rows for tenant-A; seed mock LiteLLM_SpendLogs with N+10 rows for same tenant (induces drift > 0.5%)
      3. Manually enqueue reconciliation-daily-check job via worker queue
      4. Wait for job completion (poll BullMQ job status)
      5. Query Mimir HTTP API: `GET /prometheus/api/v1/query?query=litellm_reconciliation_drift_pct{tenant_id="..."}` → assert value > 0.5
      6. Assert reconciliation-discrepancy job enqueued (BullMQ queue introspection)
      7. Wait for discrepancy backfill to run; assert usage_ledger now has the missing N+10 rows (idempotent insert)
      8. Re-run reconciliation-daily-check; assert drift gauge now 0
    - log-scrub-sentinel.test.ts (OBS-03):
      1. Boot compose default + worker
      2. Sign in to capture a real session
      3. Send POST with `Authorization: Bearer SENTINEL-AUTH-${Date.now()}` (deliberately invalid; intent is to provoke log emission of the request)
      4. Wait 2s
      5. `await apiContainer.logs({ since: testStart })` → string
      6. Assert SENTINEL-AUTH-* substring ABSENT in stdout
      7. Repeat for worker container with a job carrying `{virtual_key: 'SENTINEL-VK-${...}'}` (will throw at Zod parse since virtual_key is forbidden in audit payloads — sentinel still must not leak in error log)
    - rate-limit-layered.test.ts (SCALE-04):
      1. Boot compose default
      2. Sign in user-A from IP-1
      3. Send 21 POST requests to /api/transcribe from user-A from IP-1 within 60s
      4. Assert: requests 1-20 return 200/201 (or 503 missing-key); request 21 returns 429 with body `{error: 'Too many requests'}` AND headers `X-RateLimit-Limit: 20`, `X-RateLimit-Remaining: 0`, `Retry-After: ...`
      5. Sign in 30 fresh users from same IP-2; each posts to /api/reason once; after the 60th request the IP-tier counter trips (60/min/IP for reason); request 61 returns 429
      6. Verification-status carve-out: poll /api/auth/verification-status?email=fixture@example.com 30 times within 60s; all 200 (or 4xx not-found, but NOT 429); 31st returns 429
      7. Assert audit_log row written with action='security.rate_limit_exceeded'
    - otel-trace-propagation.test.ts (OBS-01):
      1. Boot compose default + LGTM stack (tempo + loki + mimir)
      2. Send a request that emits a span (any authenticated route)
      3. Capture trace_id from the response — either set `traceresponse` header (W3C) OR extract from pino log captured via api container stdout
      4. Wait 5s for Tempo ingestion
      5. Query Tempo: `GET /api/traces/{trace_id}` → assert ≥1 span exists with `service.name='openwhispr-api'`
      6. Query Loki: `GET /loki/api/v1/query?query={service="api"} |~ "trace_id"="{trace_id}"` → assert ≥1 log line returned (proves Loki ↔ Tempo correlation works end-to-end via the derivedFields config)
    - Update `.github/workflows/nightly.yml`: add a new job `e2e-phase6` that runs `E2E=1 make e2e-test-phase6` on `ubuntu-latest` with the docker compose stack
    - Update `.github/workflows/ci.yml`: add an `e2e-phase6-quick` job that runs only the fastest 3 of these 8 tests (probes-dependency + audit-log-write + rate-limit-layered) on every PR; the slower 5 (involving LGTM stack ingest) run nightly
  </behavior>
  <action>
    Implement the 4 remaining e2e tests using the same DockerComposeEnvironment pattern. Particular care:
    - reconciliation-drift: enqueueing a BullMQ job from outside the worker container — use the Valkey connection to call `queue.add` directly OR use a debug API endpoint `POST /__test/trigger-job` (gated NODE_ENV=test)
    - log-scrub-sentinel: `apiContainer = environment.getContainer('api')`; then `await apiContainer.logs({ tail: 'all' })`
    - rate-limit-layered: rapid fire — use `Promise.all([...Array(21)].map(() => fetch(...)))`. Capture all 21 responses, sort by header X-RateLimit-Remaining.
    - otel-trace-propagation: 5s wait is a known delay; if Tempo ingestion is slower, fall back to polling Tempo for up to 30s.

    Update `.github/workflows/nightly.yml` adding the new job (mirror Phase 3 Plan 10 nightly-realtime-soak structure).

    Update `.github/workflows/ci.yml` for the 3-fast-test PR gate.

    After all 8 tests GREEN: run the FINAL coverage audit:
    ```
    pnpm -r test --coverage --run
    ```
    Then compute the per-file coverage diff: every file under `files_modified` in any Phase 6 plan MUST report ≥90/90/90/90. Use `tools/coverage-diff.cjs` (if exists from Phase 0/2) or manually inspect the V8 coverage JSON output.

    Document any file falling below threshold in `.planning/phases/06-observability-ops-hardening-workers/06-12-COVERAGE.md` with an action plan. The phase CANNOT close until every file ≥90/90/90/90.
  </action>
  <verify>
    <automated>E2E=1 pnpm vitest run tests/e2e/reconciliation-drift.test.ts tests/e2e/log-scrub-sentinel.test.ts tests/e2e/rate-limit-layered.test.ts tests/e2e/otel-trace-propagation.test.ts &amp;&amp; pnpm -r test --coverage --run</automated>
  </verify>
  <acceptance_criteria>
    - 4 e2e test files flipped to GREEN
    - `grep -l 'litellm_reconciliation_drift_pct' tests/e2e/reconciliation-drift.test.ts` matches
    - `grep -l 'SENTINEL-' tests/e2e/log-scrub-sentinel.test.ts` matches
    - `grep -l 'X-RateLimit-Remaining' tests/e2e/rate-limit-layered.test.ts` matches
    - `grep -l 'tempo\\|loki' tests/e2e/otel-trace-propagation.test.ts` matches
    - `.github/workflows/nightly.yml` contains `e2e-phase6` job
    - `.github/workflows/ci.yml` contains `e2e-phase6-quick` job
    - `E2E=1 make e2e-test-phase6` exits 0 with all 8 tests green
    - `pnpm -r test --coverage --run` reports ≥90/90/90/90 on EVERY file listed in any Phase 6 plan's files_modified
    - If any file is below threshold, .planning/phases/06-observability-ops-hardening-workers/06-12-COVERAGE.md documents the gap + closure plan (and the phase does NOT close until closed)
  </acceptance_criteria>
  <done>
    Phase 6 e2e suite all GREEN; coverage ≥90/90/90/90 on every modified file; CI extended; ready for `/gsd-verify-work` invocation.
  </done>
</task>

</tasks>

<verification>
- All 8 e2e tests GREEN under `E2E=1 make e2e-test-phase6`
- Full coverage report ≥90/90/90/90 on every Phase 6 file
- Nightly + PR CI workflows extended
- `make e2e-test` (global gate) exits 0
- `pnpm lint:tenant-context` exits 0
- `pnpm lint:rls` exits 0 (audit_log partition not false-positive)
</verification>

<success_criteria>
Phase 6 constitutional gate passes:
1. OTel auto-instrumentation covers Fastify + undici + pg + ioredis with correlation IDs propagated to LiteLLM upstream calls
2. JSON logs flow to Loki via OTel Collector; sentinel-token sweep confirms scrubbing; English-only keys
3. audit_log records 18-action canonical events; LiteLLM spend reconciled with discrepancy alerts
4. Liveness/readiness/startup probes wired; horizontal scale verified via `--scale api=2`
5. BullMQ workers run with tenant-context middleware; CI introspection gate live (3 layers)
6. Anti-abuse rate limiting (per-user + per-IP via Valkey token-bucket) live; verification-status polling carve-out preserved; SSRF defense gates every outbound HTTP
7. Tests written first (TDD); coverage ≥90/90/90/90; CI green
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-ops-hardening-workers/06-12-SUMMARY.md` documenting final test counts, coverage report, any deviations from the locked decisions, and the final gsd-verifier readiness checklist.
</output>
