---
phase: 19-server-error-closure
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - packages/data/migrations/0000_initial.sql
  - packages/data/migrations/0014_audit_log_partition.sql
  - packages/data/migrations/0014_audit_log_partition.down.sql
  - packages/data/migrations/meta/_journal.json
  - apps/api/tests/support/shared-pg.ts
  - apps/api/tests/unit/routes/__tests__/usage.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts
  - .planning/phases/08-client-server-audit/SERVER-ERRORS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SR-19.1
must_haves:
  truths:
    - "D-01 + D-02 (SR-19.1 strategy): Option (i) strip `\"public\".` prefixes from FK refs. Real scope = 11 grep hits across 3 migration files. Option (ii) env knob explicitly REJECTED — Deferred to v3."
    - "D-02 sites (FK refs to strip):\n  - `packages/data/migrations/0000_initial.sql:67,71,75,79,83,87` (6 FK refs to `\"public\".\"tenants\"` / `\"public\".\"users\"`)\n  - `packages/data/migrations/0014_audit_log_partition.sql:77` (1 FK ref `\"public\".\"tenants\"`)\n  - `packages/data/migrations/0014_audit_log_partition.down.sql:27,46` (2 hits — verify FK vs metadata by Read)"
    - "D-04 EXEMPT (LEAVE AS IS): `0014_audit_log_partition.sql:100` `p_parent_table => 'public.audit_log'` and `:115` `WHERE parent_table = 'public.audit_log'` — these are pg_partman METADATA literals (NOT FK refs). Touching them would break partman config. CONTEXT D-02 conflates these into the count; D-04 carves them out. The plan touches ONLY line 77 in `0014_audit_log_partition.sql`."
    - "D-05 EXEMPT: `0000_initial.sql:148` `GRANT USAGE ON SCHEMA public` stays — `_app` role already has it; test schemas inherit via search_path strategy."
    - "D-06 (journal re-hash): After SQL edits, run `pnpm --filter @openwhispr/data drizzle-kit generate` to recompute `packages/data/migrations/meta/_journal.json` content hashes. Production DBs apply by hash+idx via `_meta.__drizzle_migrations` and SKIP already-applied migrations — informational only on prod."
    - "D-20 ATOMIC same-commit revert: Phase 18.1.2-03 test-infra workaround (`apps/api/tests/support/shared-pg.ts` shared-`public`+TRUNCATE strategy) flips BACK to per-file `search_path` schemas in the SAME commit as migration SQL edits. If `search_path` strategy doesn't unblock W-2 in practice, revert whole commit cleanly per D-20 — NO 2-phase dance."
    - "D-25 (ledger close): `.planning/phases/08-client-server-audit/SERVER-ERRORS.md` Entries 1-5 transition `Owner: unassigned` → `Owner: Phase 19 (commit <SHA>)` with linked atomic commits. Add `## Status: CLOSED 2026-05-15` block per entry."
    - "D-26 (milestone close): `.planning/ROADMAP.md` Phase 19 row marked Complete; v2.1 milestone label transitions from `CLOSED-WITH-PRE-EXISTING-DEBT` → `CLOSED 2026-05-15`. `.planning/STATE.md` milestone status `closed-with-followup` → `closed`."
    - "D-22 TDD doctrine: migration SQL edits + journal regen + test-infra revert all single-GREEN single-commit (pre-existing constraint surface; no NEW exported API). Suite assertion is the existing test-infra suite passing after the W-2 revert. Plan 19-03 = ~3 commits: (a) migration SQL edits + journal regen + W-2 revert ATOMIC, (b) SERVER-ERRORS ledger close, (c) ROADMAP/STATE close."
    - "D-23 coverage 90/90/90/90 — migration SQL files are not coverage-instrumented (DDL not test code). Phase-aggregate `pnpm test` + `pnpm typecheck` exit 0 per D-24 is the bar."
    - "D-21 ZERO `--no-verify`. ≤ 7 commits per D-18 (3 planned, well under)."
  artifacts:
    - path: "packages/data/migrations/0000_initial.sql"
      provides: "FK refs unqualified (no `\"public\".` prefix)."
      contains: "REFERENCES \"tenants\""
    - path: "packages/data/migrations/0014_audit_log_partition.sql"
      provides: "Audit-log FK unqualified at L77; partman metadata literals at L100/L115 PRESERVED."
      contains: "REFERENCES \"tenants\""
    - path: "packages/data/migrations/meta/_journal.json"
      provides: "Re-stamped content hashes after SQL edits."
      contains: ""
    - path: "apps/api/tests/support/shared-pg.ts"
      provides: "Per-file `search_path` schema isolation strategy (Phase 18.1.2-03 Option A reverted)."
      contains: "search_path"
    - path: ".planning/phases/08-client-server-audit/SERVER-ERRORS.md"
      provides: "Entries 1-5 marked CLOSED with commit-linked owners."
      contains: "Status: CLOSED 2026-05-15"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 19 row Complete; v2.1 milestone CLOSED 2026-05-15."
      contains: "v2.1 milestone (CLOSED 2026-05-15)"
    - path: ".planning/STATE.md"
      provides: "Milestone status `closed`."
      contains: "closed"
  key_links:
    - from: "packages/data/migrations/*.sql (unqualified FK refs)"
      to: "Drizzle migrate() runtime resolution via search_path"
      via: "Postgres resolves unqualified table names against current_schemas(true); test isolation can prepend a per-file schema"
      pattern: "search_path"
    - from: "apps/api/tests/support/shared-pg.ts (per-file search_path)"
      to: "Drizzle _meta.__drizzle_migrations (per-schema instance)"
      via: "Each test file gets its own schema; migrations apply fresh per schema; no TRUNCATE needed"
      pattern: "CREATE SCHEMA"
    - from: ".planning/phases/08-client-server-audit/SERVER-ERRORS.md"
      to: "atomic commits from 19-01 + 19-02 + 19-03"
      via: "Owner: Phase 19 (commit <SHA>) — D-25 ledger close protocol"
      pattern: "Owner: Phase 19"
---

<objective>
Close SR-19.1 (migration schema-aware refactor) + Phase 18.1.2-03 W-2 test-infra revert (D-20 atomic) + SERVER-ERRORS Entries 1-5 ledger close (D-25) + v2.1 milestone CLOSED declaration (D-26). Sequential LAST in 3-plan graph (D-17 + D-19 rationale: drizzle bookkeeping ledger + cascade to test-infra retesting; banks CI-green progress from Plans 01 + 02 as safety net).

**~3 commits** (D-18 ≤ 7 budget):
1. `refactor(19-03-01): green — strip "public." prefixes from migration FKs + regen journal + revert 18.1.2-03 W-2 to search_path (SR-19.1, D-01..D-06, D-20)` (D-22 single GREEN; D-20 atomic same-commit)
2. `docs(19-03-02): close SERVER-ERRORS Entries 1-5 with Phase 19 commit-linked owners (D-25)`
3. `docs(19-03-03): close Phase 19 + v2.1 milestone CLOSED in ROADMAP/STATE (D-26)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/19-server-error-closure/19-CONTEXT.md
@.planning/phases/19-server-error-closure/19-DISCUSSION-LOG.md
@.planning/phases/08-client-server-audit/SERVER-ERRORS.md
@.planning/phases/19-server-error-closure/19-01-SUMMARY.md
@.planning/phases/19-server-error-closure/19-02-SUMMARY.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-PLAN.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-SUMMARY.md
@CLAUDE.md
@packages/data/migrations/0000_initial.sql
@packages/data/migrations/0014_audit_log_partition.sql
@packages/data/migrations/0014_audit_log_partition.down.sql
@packages/data/migrations/meta/_journal.json
@packages/data/drizzle.config.ts
@apps/api/tests/support/shared-pg.ts
@.planning/ROADMAP.md
@.planning/STATE.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 19-03-01: GREEN — strip `public.` prefixes + regen journal + revert W-2 to search_path (atomic)</name>
  <files>packages/data/migrations/0000_initial.sql, packages/data/migrations/0014_audit_log_partition.sql, packages/data/migrations/0014_audit_log_partition.down.sql, packages/data/migrations/meta/_journal.json, apps/api/tests/support/shared-pg.ts, apps/api/tests/unit/routes/__tests__/usage.integration.test.ts, apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts</files>
  <action>
Per D-01 + D-02 + D-03 + D-04 + D-05 + D-06 + D-20 + D-22 single GREEN + D-39.

**Step A — capture current test-infra state (W-2 baseline):**
1. Read `apps/api/tests/support/shared-pg.ts` to confirm current shape is Phase 18.1.2-03 Option A (shared `public` schema + TRUNCATE + bootstrapSharedRoles + provisionPgPartman). Document current shape verbatim in commit body.
2. Capture baseline: `pnpm --filter @openwhispr/api test routes 2>&1 | tee /tmp/19-03-01-pre.log`. Expect GREEN under current W-2 shape (Phase 18.1.2-03 closure).

**Step B — strip `"public".` prefixes from FK refs (D-01..D-05):**
3. Edit `packages/data/migrations/0000_initial.sql:67,71,75,79,83,87`:
   - At each line, change `REFERENCES "public"."tenants"` → `REFERENCES "tenants"` and `REFERENCES "public"."users"` → `REFERENCES "users"`.
   - Use Edit tool with surrounding context for each replacement (lines are FK constraints in ALTER TABLE ... ADD CONSTRAINT blocks).
   - DO NOT touch `0000:148` `GRANT USAGE ON SCHEMA public` (D-05 exempt).
   - DO NOT touch RLS policies / `current_setting('app.tenant_id', ...)` blocks (unqualified already).
4. Edit `packages/data/migrations/0014_audit_log_partition.sql:77`:
   - Change `REFERENCES "public"."tenants"` → `REFERENCES "tenants"`.
   - **CRITICAL D-04 EXEMPT:** DO NOT touch line 100 (`p_parent_table => 'public.audit_log'`) or line 115 (`WHERE parent_table = 'public.audit_log'`) — pg_partman metadata literals. Verify by Read of L100 and L115 to confirm they are `'public.audit_log'` STRING LITERALS (single-quoted, not double-quoted FK refs). CONTEXT D-02 listed 3 hits for 0014 but D-04 carves out 2 of them.
5. Edit `packages/data/migrations/0014_audit_log_partition.down.sql:27,46`:
   - Read both lines first. If they are FK refs (`REFERENCES "public".`), strip the prefix. If they are pg_partman metadata (`'public.audit_log'` string literal in undo_partition / DROP statements), LEAVE INTACT per D-04 pattern.
   - Document the L27 + L46 classifications (FK vs metadata) in commit body.

**Step C — Drizzle schema convention sanity check (D-03):**
6. Run: `grep -rn "pgSchema" packages/data/src/schema/ 2>&1 | head -5`. Expect zero hits (D-03 says all `pgTable(...)` are unwrapped). If any `pgSchema('public')` wrappers exist (contradicts D-03), HALT per D-26 3-branch user choice and document the contradiction.

**Step D — regenerate journal hashes (D-06):**
7. Run: `pnpm --filter @openwhispr/data drizzle-kit generate 2>&1 | tee /tmp/19-03-01-generate.log`. Drizzle 0.31.x auto-stamps `meta/_journal.json` content hashes from SQL file content. If drizzle-kit refuses because schema source files haven't changed (only SQL edited), use `drizzle-kit generate --custom` shape OR manually recompute hash entries — read drizzle-kit docs path: `node_modules/drizzle-kit/README.md` or `package.json` `drizzle-kit` scripts.
8. Verify `packages/data/migrations/meta/_journal.json` hash entries updated for 0000 + 0014. Diff via `git diff packages/data/migrations/meta/_journal.json`.

**Step E — HALT recipe (D-06 production-DB conflict guard):**
9. **Critical risk:** if the existing production DB applied 0000+0014 under the OLD hashes, `migrate()` may see hash mismatch on next boot. CONTEXT discretion HALT recipe (D-26 escalation): if local `docker compose up` reveals a migration replay conflict (`hash mismatch on idx N`), HALT and surface 3-branch user choice:
   - (a) regenerate from clean state (drops existing prod data — only acceptable on dev)
   - (b) manual hash patch (write a one-shot SQL that updates `_meta.__drizzle_migrations.hash` for the two affected idx values)
   - (c) defer SR-19.1 to v3 (revert this commit and document)
10. Test the boot: `docker compose -f docker-compose.dev.yml up -d postgres 2>&1 | tail -10 && pnpm --filter @openwhispr/api test entrypoint-db-shape 2>&1 | tail -10`. If GREEN, proceed. If RED with hash mismatch, HALT.

**Step F — revert Phase 18.1.2-03 W-2 to per-file `search_path` schemas (D-20 ATOMIC):**
11. Read `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-PLAN.md` + 18.1.2-03-SUMMARY.md to recover the PRE-W-2 shape (per-file `search_path` schemas). Search prior git history if needed: `git log --all --oneline --follow apps/api/tests/support/shared-pg.ts | head -10`, then `git show <pre-W-2-SHA>:apps/api/tests/support/shared-pg.ts > /tmp/shared-pg-pre-W2.ts` for reference.
12. Rewrite `apps/api/tests/support/shared-pg.ts`:
    - Per-file schema: `CREATE SCHEMA test_<unique_id>` in beforeAll; `SET search_path = test_<unique_id>, public` in connection setup.
    - Run drizzle `migrate()` against the per-file schema. With FK refs now unqualified (Step B), tables resolve into the per-file schema. RLS + pgcrypto / pgpartman extensions stay in `public` and are visible via search_path.
    - Drop schema in afterAll: `DROP SCHEMA test_<unique_id> CASCADE`.
    - Preserve `provisionPgPartman()` helper (Entry 3 image dependency stands).
    - Remove the TRUNCATE-in-beforeEach pattern (no longer needed; per-file schema is naturally isolated).
13. Edit `apps/api/tests/unit/routes/__tests__/usage.integration.test.ts` + `streaming-usage.integration.test.ts` + the 15 cluster-#2 files listed in CONTEXT canonical_refs:
    - REMOVE TRUNCATE-in-beforeEach if present.
    - Otherwise rely on `shared-pg.ts` for per-file isolation (no per-test logic needed).
    - This is a mass revert; use `grep -rln "TRUNCATE" apps/api/tests/unit/routes/__tests__/ apps/api/tests/unit/routes/{conversations,notes,folders,transcriptions,v1}/__tests__/ 2>&1` to enumerate touched files.

**Step G — verification + atomic commit:**
14. Run: `pnpm --filter @openwhispr/api test routes 2>&1 | tee /tmp/19-03-01-post.log`. MUST exit 0. Compare to baseline (`/tmp/19-03-01-pre.log`): GREEN→GREEN delta (no new RED).
15. Phase-aggregate (D-24): `pnpm test 2>&1 | tail -20 && pnpm typecheck 2>&1 | tail -10`. BOTH MUST exit 0.
16. **D-20 atomic-revert escape:** if Step G acceptance fails (W-2 revert breaks test isolation despite the SR-19.1 fix), `git checkout -- .` the whole commit cleanly and HALT per D-20 + D-26 — revert AS IF the SR-19.1 attempt never happened, document the failure in a NEW commit `docs(19-03-01): halt — W-2 revert blocked by <reason>; SR-19.1 deferred`. Do NOT push through a half-state.
17. Single atomic commit (D-20 mandatory same-commit): `refactor(19-03-01): green — strip "public." prefixes from migration FKs + regen journal + revert 18.1.2-03 W-2 to search_path (SR-19.1, D-01..D-06, D-20)`. Body MUST cite: D-01..D-06, D-20, SERVER-ERRORS Entry 1, Phase 18.1.2-03 baseline, verbatim grep -c "public\\." packages/data/migrations/*.sql delta (before/after), journal hash diff summary, post-test green proof.

WHY (D-01): Option (i) selected by advisor (lowest invasion; codebase already philosophically schema-agnostic per D-03). WHY (D-04): pg_partman metadata literals are NOT FKs; touching them breaks partman config. WHY (D-06): journal re-hash is informational on existing prod DBs (already-applied migrations skipped by `_meta.__drizzle_migrations`). WHY (D-20): atomic-revert pattern is the disciplined response to "what if test-infra revert breaks test isolation again?" — either both edits land together OR both revert together; no partial state.
  </action>
  <verify><automated>grep -c "\"public\"\\.\"\\(tenants\\|users\\)\"" packages/data/migrations/0000_initial.sql packages/data/migrations/0014_audit_log_partition.sql packages/data/migrations/0014_audit_log_partition.down.sql && pnpm --filter @openwhispr/api test routes 2>&1 | grep -E "(✓|PASS|passed|Test Files)" | head -5 && pnpm typecheck 2>&1 | tail -5</automated></verify>
  <done>1 commit; 11 `"public".` prefixes stripped (or fewer if 0014.down.sql lines were metadata); journal hashes regenerated; shared-pg.ts back on per-file `search_path` schemas; routes test suite GREEN; aggregate `pnpm test` + `pnpm typecheck` exit 0 per D-24; lefthook clean.</done>
</task>

<task type="auto">
  <name>Task 19-03-02: docs — close SERVER-ERRORS Entries 1-5 with Phase 19 commit-linked owners</name>
  <files>.planning/phases/08-client-server-audit/SERVER-ERRORS.md</files>
  <action>
Per D-25 + SR-19.1..SR-19.5 closure.

1. Read `.planning/phases/08-client-server-axit/SERVER-ERRORS.md` Entries 1-5 to confirm current `Owner: unassigned` status.
2. For each Entry, append a `## Status: CLOSED 2026-05-15` block BEFORE the entry's trailing `---` separator. Include:
   - Owner: Phase 19 (commit `<SHA>`)
   - Closing plan: `19-NN-PLAN.md`
   - Linked atomic commit SHA (from prior plans' SUMMARY files)
   - One-sentence closure note
3. Cross-reference (use `git log --oneline -n 20`):
   - Entry 1 (SR-19.1) → 19-03-01 commit SHA
   - Entry 2 (SR-19.2) → 19-01-02 commit SHA
   - Entry 3 (SR-19.5) → 19-01-04 commit SHA
   - Entry 4 (SR-19.3) → 19-02-02 commit SHA
   - Entry 5 (SR-19.4) → 19-01-03 commit SHA
4. Format example (Entry 1):
   ```markdown
   ## Status: CLOSED 2026-05-15
   - Owner: Phase 19 (commit `<sha-7>`)
   - Closing plan: 19-03-PLAN.md
   - Closure note: Stripped `"public".` prefixes from FK refs in 0000_initial.sql + 0014_audit_log_partition.sql + 0014_audit_log_partition.down.sql per advisor Option (i); journal hashes regenerated via drizzle-kit; Phase 18.1.2-03 W-2 test-infra workaround reverted to per-file `search_path` schemas in the same atomic commit (D-20).
   ```
5. Single atomic commit (D-22 docs-only): `docs(19-03-02): close SERVER-ERRORS Entries 1-5 with Phase 19 commit-linked owners (D-25)`. Body MUST cite: D-25, the 5 commit SHAs, SERVER-ERRORS schema-version note (append-protocol preserved).

WHY (D-25): ledger close protocol — production-fix phase MUST link its commits back to the entries it closes. Otherwise the ledger drifts and future phases can't tell which entries are still open.
  </action>
  <verify><automated>grep -c "Status: CLOSED 2026-05-15" .planning/phases/08-client-server-audit/SERVER-ERRORS.md</automated></verify>
  <done>1 commit; SERVER-ERRORS.md Entries 1-5 each have CLOSED block with commit SHA; lefthook clean.</done>
</task>

<task type="auto">
  <name>Task 19-03-03: docs — close Phase 19 + v2.1 milestone CLOSED in ROADMAP/STATE</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Per D-26 + D-24 (aggregate green precondition).

1. PRECONDITION CHECK (D-24): `pnpm test 2>&1 | tail -10 && pnpm typecheck 2>&1 | tail -5`. BOTH MUST exit 0. If either fails, HALT — D-26 milestone-CLOSED transition is gated on D-24 satisfaction. Do NOT commit ROADMAP/STATE changes if aggregate is RED.
2. Edit `.planning/ROADMAP.md`:
   - Find Phase 19 row in Progress Table (around line ~942 — verify by `grep -n "19. Server-error closure" .planning/ROADMAP.md`).
   - If row missing: append `| 19. Server-error closure | 3/3 | Complete | 2026-05-15 |`.
   - Find v2.1 milestone label (around line 930: `**— v2.1 milestone (CLOSED 2026-05-15) —**`). Per CONTEXT D-26, the milestone is ALREADY labeled CLOSED 2026-05-15 (Phase 18.1.2 set this). Verify the suffix: if it reads `CLOSED-WITH-PRE-EXISTING-DEBT 2026-05-15`, strip the `-WITH-PRE-EXISTING-DEBT` suffix to leave clean `CLOSED 2026-05-15`. If already clean (no suffix), no edit needed beyond Phase 19 row — document state in commit body.
3. Update Phase 19 entry detail block (around line 896-912) — add `**Completed**: 2026-05-15` line and mark all Plans `[x]` checked:
   ```
   **Plans**:
   - [x] 19-01-PLAN.md — SR-19.2 + SR-19.4 + SR-19.5 (Fastify types + otel export + ops docs)
   - [x] 19-02-PLAN.md — SR-19.3 (BYOK guard refactor + 18.1.2-04-01 revert)
   - [x] 19-03-PLAN.md — SR-19.1 + 18.1.2-03 W-2 revert + ledger/milestone close
   ```
4. Edit `.planning/STATE.md`:
   - `grep -n "milestone\|v2\\.1" .planning/STATE.md | head -10` to locate milestone status field.
   - Transition `closed-with-followup` → `closed` (or whatever the current key/value is — verify by Read). Update `last_updated` to `2026-05-15`.
   - If a `current_phase` key references Phase 19, update to a next-phase placeholder OR clear.
5. Single atomic commit (D-22 docs-only): `docs(19-03-03): close Phase 19 + v2.1 milestone CLOSED in ROADMAP/STATE (D-26)`. Body MUST cite: D-26, D-24 aggregate green precondition (paste the tail of `pnpm test` + `pnpm typecheck` proving exit 0), Plan SHA chain (19-01 + 19-02 + 19-03 commits referenced).

WHY (D-26): v2.1 milestone declaration is the final close of Phase 18.1.2 → Phase 19 chain. Aggregate green (D-24) is the precondition; without it, the milestone stays `-WITH-PRE-EXISTING-DEBT`. WHY (gating): committing milestone close before aggregate green is a workflow lie; user-memory `feedback_no_workarounds_enterprise.md` mandates honesty in milestone declarations.
  </action>
  <verify><automated>grep -c "19. Server-error closure" .planning/ROADMAP.md && pnpm test 2>&1 | tail -5 && pnpm typecheck 2>&1 | tail -3</automated></verify>
  <done>1 commit; Phase 19 row marked Complete in ROADMAP; v2.1 milestone label clean (no `-WITH-PRE-EXISTING-DEBT` suffix); STATE.md milestone status `closed`; aggregate `pnpm test` + `pnpm typecheck` exit 0 verified in commit body; lefthook clean.</done>
</task>

</tasks>

<verification>
After 3 commits (Plan 03 only; cumulative phase total ~8-10):

```bash
cd /Users/dev/openwhispr-server
# SR-19.1 closure proof
grep -rc "\"public\"\\." packages/data/migrations/*.sql packages/data/migrations/*.down.sql | grep -v ':0$' | head -5
# D-20 atomic revert proof
grep -c "search_path" apps/api/tests/support/shared-pg.ts
# D-25 ledger close proof
grep -c "Status: CLOSED 2026-05-15" .planning/phases/08-client-server-audit/SERVER-ERRORS.md   # 5
# D-26 milestone close proof
grep -E "Phase 19|v2\\.1 milestone" .planning/ROADMAP.md | head -3
# D-24 aggregate gate
pnpm test 2>&1 | tail -10
pnpm typecheck 2>&1 | tail -5
git log --oneline -n 10 | grep -cE "\\(19-(01|02|03)-0[1-9]\\)"   # 8-10
```
</verification>

<success_criteria>
1. 3 atomic commits (SR-19.1 + W-2 revert atomic; SERVER-ERRORS close; ROADMAP/STATE close).
2. 11 (or fewer if 0014.down lines were metadata) `"public".` prefixes stripped from FK refs.
3. pg_partman metadata literals at 0014:100 + 0014:115 PRESERVED (D-04).
4. `meta/_journal.json` hashes regenerated via drizzle-kit.
5. `apps/api/tests/support/shared-pg.ts` reverted to per-file `search_path` schemas; TRUNCATE-in-beforeEach removed.
6. Routes test suite GREEN; aggregate `pnpm test` + `pnpm typecheck` exit 0 (D-24).
7. SERVER-ERRORS.md Entries 1-5 each have `Status: CLOSED 2026-05-15` block with commit SHA (D-25).
8. ROADMAP Phase 19 row Complete; v2.1 milestone label clean `CLOSED 2026-05-15` (D-26).
9. STATE.md milestone status `closed`.
10. ZERO `--no-verify` (D-21); lefthook clean.
11. ≤ 7 commits per plan (D-18; 3 used).
</success_criteria>

<rollback>
- **HALT recipe (D-06 + D-26 3-branch):** If `docker compose up` reveals hash mismatch on existing prod DB after journal regen, HALT and surface user choice:
  - (a) Regenerate from clean state (acceptable on dev; drops data)
  - (b) Manual hash patch via one-shot SQL updating `_meta.__drizzle_migrations`
  - (c) Defer SR-19.1 to v3 (revert 19-03-01 entirely; document in SERVER-ERRORS Entry 1 as "deferred to v3")
- **HALT recipe (D-20 atomic-revert):** If post-W-2-revert routes suite is RED, `git checkout -- .` the whole 19-03-01 commit cleanly. Document the failure mode (which test, which assertion) and surface 3-branch user choice: (a) re-try with hybrid (some files per-schema, some shared-public — explicit list), (b) revert SR-19.1 entirely and re-document Entry 1 as v3-deferred, (c) escalate to advisor.
- If pg_partman extension creation in per-file schema fails (extensions are typically `public`-only), the `search_path` approach should still work because partman lives in `public` and is reachable via search_path. If not, document as a NEW SERVER-ERRORS entry (Entry 6) and HALT.
- If 0014_audit_log_partition.down.sql lines 27/46 turn out to be metadata literals (not FK refs), the strip count is 9 not 11. Document the discovered classification in commit body; this is NOT a HALT, it's a CONTEXT D-02 count correction.
- If D-24 aggregate is RED at Task 19-03-03 precondition check, do NOT commit milestone close. Loop back to whichever test is RED (likely something cascading from 19-03-01 W-2 revert); fix in a 19-03-04 commit, then re-attempt 19-03-03.
</rollback>

<output>
Create `.planning/phases/19-server-error-closure/19-03-SUMMARY.md` (≤ 80 LOC) with:
- 3 commit SHAs
- SR-19.1 closure proof (grep delta on `"public".` prefixes)
- D-20 atomic revert proof (shared-pg.ts diff summary)
- D-06 journal regen proof (hash diff in _journal.json)
- D-04 exempt confirmation (0014:100 + 0014:115 unchanged)
- D-25 ledger close proof (5 CLOSED blocks added)
- D-26 milestone close proof (ROADMAP + STATE deltas)
- D-24 aggregate gate proof (pnpm test + typecheck exit 0 tails)
- Lefthook status (clean)
- Phase 19 PHASE-SUMMARY.md trigger (separate top-level summary by orchestrator)
</output>
