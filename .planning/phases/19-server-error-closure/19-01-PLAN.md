---
phase: 19-server-error-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/types/fastify.d.ts
  - apps/api/src/otel-bootstrap.ts
  - apps/api/tests/unit/otel-bootstrap.test.ts
  - docs/operations.md
autonomous: true
requirements:
  - SR-19.2
  - SR-19.4
  - SR-19.5
must_haves:
  truths:
    - "D-07 (SR-19.2): NEW file `apps/api/src/types/fastify.d.ts` exists with `declare module 'fastify' { interface FastifyRequest { user?: ...; tenant?: ...; } }`. Closes Phase 14-04 typecheck-deferral root cause. `pnpm --filter @openwhispr/api typecheck` exit 0 on routes that previously red-flagged `req.user` / `req.tenant`."
    - "D-13 (SR-19.4): `apps/api/src/otel-bootstrap.ts:144` adds `export` keyword: `export const onSignal = ...`. Production `process.once('SIGTERM'/'SIGINT', onSignal)` at L147-148 preserved INTACT (side-effect carries)."
    - "D-14 (SR-19.4): `apps/api/tests/unit/otel-bootstrap.test.ts:124-131` reverted from Phase 18.1.2-04-03 `process.exit` spy workaround → direct `mod.onSignal()` invocation + spy `shutdownSdk` to assert behavior. Restores REAL behavior assertion (not exit-code observation)."
    - "D-15 (SR-19.5): `docs/operations.md` Phase 18.1.2-05-05 'Local development test prerequisites' section APPENDED with explicit pg_partman image recipe — `docker pull openwhispr/postgres:17.5-pgpartman` (or `make build-pg-partman` if target exists). References SERVER-ERRORS Entry 3."
    - "D-22 TDD doctrine: SR-19.2 NEW code = 2-commit RED+GREEN pair. SR-19.4 = single GREEN (pre-existing RED test transitions). SR-19.5 = docs-only single commit (no TDD)."
    - "Approximately 4 commits: SR-19.2 RED (failing typecheck snippet test), SR-19.2 GREEN (.d.ts + revert), SR-19.4 single GREEN, SR-19.5 docs."
    - "Coverage 90/90/90/90 on changed prod files (otel-bootstrap.ts) per D-23. `fastify.d.ts` is types-only (no runtime coverage; D-23 carve-out)."
    - "D-21 ZERO `--no-verify`. ≤ 7 commits per D-18 (well under)."
  artifacts:
    - path: "apps/api/src/types/fastify.d.ts"
      provides: "Ambient module augmentation for Fastify request decorators (user, tenant)."
      contains: "declare module 'fastify'"
    - path: "apps/api/src/otel-bootstrap.ts"
      provides: "Named export onSignal."
      contains: "export const onSignal"
    - path: "apps/api/tests/unit/otel-bootstrap.test.ts"
      provides: "Direct mod.onSignal() invocation (Phase 18.1.2-04-03 workaround reverted)."
      contains: "mod.onSignal"
    - path: "docs/operations.md"
      provides: "pg_partman prerequisite recipe under Local development test prerequisites."
      contains: "openwhispr/postgres:17.5-pgpartman"
  key_links:
    - from: "apps/api/src/types/fastify.d.ts"
      to: "apps/api/src/plugins/auth.ts + apps/api/src/plugins/tenant-context.ts"
      via: "FastifyRequest interface extension references decorator-shape types"
      pattern: "interface FastifyRequest"
    - from: "apps/api/tests/unit/otel-bootstrap.test.ts"
      to: "apps/api/src/otel-bootstrap.ts (named exports)"
      via: "vi.spyOn(mod, 'shutdownSdk') + direct mod.onSignal() (NOT process.emit)"
      pattern: "mod.onSignal"
---

<objective>
Close SR-19.2 (Fastify types) + SR-19.4 (otel export) + SR-19.5 (ops docs). Low-risk localized fixes; ZERO cascading test-infra impact. Sequential FIRST in 3-plan graph (D-17) to bank CI-green before BYOK refactor (Plan 02) and migration edits (Plan 03).

**~4 commits** (D-18 ≤ 7 budget):
1. `test(19-01-01): red — FastifyRequest user/tenant typecheck contract` (D-22 NEW; D-39 RED leg)
2. `feat(19-01-02): green — add apps/api/src/types/fastify.d.ts module augmentation (SR-19.2, D-07)` (D-22 NEW; D-39 GREEN leg)
3. `fix(19-01-03): green — export onSignal + revert Phase 18.1.2-04-03 test workaround (SR-19.4, D-13, D-14)` (D-22 single GREEN; pre-existing test transitions from spy-swallow → real behavior assertion)
4. `docs(19-01-04): append pg_partman prerequisite to docs/operations.md (SR-19.5, D-15)` (no TDD; docs-only)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/19-server-error-closure/19-CONTEXT.md
@.planning/phases/19-server-error-closure/19-DISCUSSION-LOG.md
@.planning/phases/08-client-server-audit/SERVER-ERRORS.md
@CLAUDE.md
@apps/api/src/plugins/auth.ts
@apps/api/src/plugins/tenant-context.ts
@apps/api/src/otel-bootstrap.ts
@apps/api/tests/unit/otel-bootstrap.test.ts
@docs/operations.md
@.planning/deferred-items.md
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-01-01: RED — FastifyRequest user/tenant typecheck contract</name>
  <files>apps/api/tests/unit/__tests__/fastify-request-types.test-d.ts</files>
  <behavior>
- Test 1 (typecheck-only): `expectTypeOf<FastifyRequest['user']>().toMatchTypeOf<{ id: string } | undefined>()` — the contract D-07 declares.
- Test 2: `expectTypeOf<FastifyRequest['tenant']>().toMatchTypeOf<{ id: string } | undefined>()`.
- Test 3 (runtime negative-control sanity): import succeeds; `req.user` access compiles after .d.ts exists.
  </behavior>
  <action>
Per D-07 + D-22 NEW (RED leg) + D-39.

1. Create `apps/api/tests/unit/__tests__/fastify-request-types.test-d.ts` (vitest `.test-d.ts` typecheck-test convention; verify by grepping for any `.test-d.ts` in the repo first: `grep -rln "test-d.ts" apps/ packages/ | head -5`). If none, use `expectTypeOf` from `vitest` in a normal `.test.ts` with `// @ts-expect-error` annotations to express RED.
2. Import `FastifyRequest` from `'fastify'`. Reference the decorator-shape types from `apps/api/src/plugins/auth.ts` (read first to capture the exact `user` shape — typically `{ id: string; tenantId: string; ... }`) and `apps/api/src/plugins/tenant-context.ts` (tenant decorator shape).
3. Write the 3 assertions above. The .d.ts does NOT exist yet — `FastifyRequest['user']` resolves to `never`/missing → typecheck RED.
4. Run `pnpm --filter @openwhispr/api typecheck 2>&1 | tee /tmp/19-01-01-red.log`. MUST emit a TS error citing `Property 'user' does not exist on type 'FastifyRequest'` or equivalent. Capture verbatim.
5. Commit: `test(19-01-01): red — FastifyRequest user/tenant typecheck contract`. Body MUST include verbatim RED snippet + cite D-07 + cite SERVER-ERRORS Entry 2.

WHY (D-07): pre-existing typecheck RED across many files for the same reason; CONTEXT D-08 confirms "Phase 14-04 typecheck-deferral root cause". A dedicated typecheck-test pins the contract before the .d.ts lands. WHY (D-22): NEW code surface → 2-commit pair, RED first.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api typecheck 2>&1 | grep -E "(error TS|Property 'user'|Property 'tenant')" | head -3</automated></verify>
  <done>1 commit; failing typecheck-test present; verbatim RED in commit body; lefthook clean (lefthook does not block on typecheck per existing config — verify by reading `.lefthook.yml`/`lefthook.yml` and document in body if relevant).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-01-02: GREEN — apps/api/src/types/fastify.d.ts module augmentation</name>
  <files>apps/api/src/types/fastify.d.ts, apps/api/tsconfig.json, .planning/deferred-items.md</files>
  <action>
Per D-07 + D-08 + D-22 NEW (GREEN leg) + D-39.

1. Read `apps/api/src/plugins/auth.ts` to capture exact `req.user` type shape (look for `fastify.decorateRequest('user', ...)` or auth plugin's type augmentation pattern). Read `apps/api/src/plugins/tenant-context.ts` for `tenant` decorator shape.
2. Create `apps/api/src/types/fastify.d.ts` (NEW file, ~30 LOC per D-07):
   - Import or inline reference the auth User type + tenant Tenant type.
   - Emit: `declare module 'fastify' { interface FastifyRequest { user?: <AuthUserType>; tenant?: <TenantType>; } }`.
   - `user` + `tenant` are OPTIONAL (`?`) — they are runtime-set by per-route decorator flow; unauthenticated routes have them undefined. This matches actual runtime behavior.
3. Verify `apps/api/tsconfig.json` `include` covers `src/**/*.d.ts` (typical default). If not, add `"src/**/*.d.ts"` to `include` array (read the file first; do not blindly edit).
4. Re-run: `pnpm --filter @openwhispr/api typecheck 2>&1 | tee /tmp/19-01-02-green.log`. MUST exit 0 on the `fastify-request-types.test-d.ts` assertions. The phase-wide aggregate may still have unrelated TS errors — capture the delta (Phase 14-04 root-cause closure) but do NOT add unrelated typecheck fixes to this commit.
5. Update `.planning/deferred-items.md §14-04` (locate via `grep -n "14-04" .planning/deferred-items.md`): transition entry status to `CLOSED — Phase 19-01-02 (commit <SHA-pending>)`. Reference SR-19.2.
6. Single atomic commit (D-22 GREEN leg pairs with 19-01-01 RED): `feat(19-01-02): green — add apps/api/src/types/fastify.d.ts module augmentation (SR-19.2, D-07)`. Body MUST cite: D-07, D-08, SERVER-ERRORS Entry 2, verbatim RED→GREEN delta on typecheck-test, Phase 14-04 deferred-items §14-04 transition.

WHY (D-07): SERVER-ERRORS Entry 2 — runtime decorator state is invisible to TypeScript without `declare module 'fastify'` augmentation. Zero runtime impact (types-only). WHY (D-08): closes Phase 14-04 root cause. WHY (D-23 carve-out): `.d.ts` is types-only → no Vitest runtime coverage applies; D-23 90/90/90/90 floor exempts .d.ts files.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api typecheck 2>&1 | grep -cE "Property 'user' does not exist|Property 'tenant' does not exist"</automated></verify>
  <done>1 commit; .d.ts file exists with `declare module 'fastify'`; typecheck RED→GREEN on the test-d file; deferred-items §14-04 marked CLOSED; lefthook clean.</done>
</task>

<task type="auto">
  <name>Task 19-01-03: GREEN — export onSignal + revert Phase 18.1.2-04-03 test workaround</name>
  <files>apps/api/src/otel-bootstrap.ts, apps/api/tests/unit/otel-bootstrap.test.ts</files>
  <action>
Per D-13 + D-14 + D-22 (pre-existing RED transitions → single GREEN; production fix supersedes Phase 18.1.2-04-03 test-side workaround) + D-39.

1. Capture current test state: `pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | tee /tmp/19-01-03-pre.log`. Per Phase 18.1.2-04-04 SUMMARY, this is currently GREEN under the spy-swallow workaround. The acceptance is that the REAL behavior assertion (direct `mod.onSignal()` + spy `shutdownSdk`) becomes the test shape.
2. Edit `apps/api/src/otel-bootstrap.ts:144`:
   - Verify current state via Read: should still say `export const onSignal` from Phase 18.1.2-04-04 (which also added export per D-09). If `export` is ALREADY present (the 18.1.2-04-04 commit landed it), this task becomes a TEST-ONLY revert. Document the discovered state in commit body and acceptance proof.
   - If NOT exported: change `const onSignal = (): void => { void shutdownSdk(); };` → `export const onSignal = (): void => { void shutdownSdk(); };`.
   - Leave `process.once("SIGTERM", onSignal)` + `process.once("SIGINT", onSignal)` INTACT.
3. Edit `apps/api/tests/unit/otel-bootstrap.test.ts:124-131`:
   - REMOVE the Phase 18.1.2-04-03 `process.exit` spy + `process.emit("SIGTERM")` workaround (assertion that exit code === 143).
   - REPLACE with direct invocation pattern:
     ```ts
     const mod = await import("../../src/otel-bootstrap");
     const spy = vi.spyOn(mod, "shutdownSdk").mockResolvedValue(undefined);
     expect(() => mod.onSignal()).not.toThrow();
     expect(spy).toHaveBeenCalled();
     ```
   - Rename surrounding `it(...)` to `it("onSignal invokes shutdownSdk", ...)`.
4. Re-run: `pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | tee /tmp/19-01-03-green.log`. MUST exit 0; the spy assertion MUST be observed (not just non-throw).
5. Coverage gate (D-23): `pnpm --filter @openwhispr/api test otel-bootstrap --coverage 2>&1 | grep -A 3 "otel-bootstrap"` — expect ≥ 90/90/90/90. If `process.once` registrations at L147-148 are no longer hit and drop coverage below 90, add `/* c8 ignore next 2 */` (or whatever the workspace's istanbul ignore convention is — verify by grep). Document any pragma in commit body.
6. Single atomic commit (D-22 single GREEN; library export + test revert atomic per D-13+D-14): `fix(19-01-03): green — export onSignal + revert Phase 18.1.2-04-03 test workaround (SR-19.4, D-13, D-14)`. Body MUST cite: D-13, D-14, SERVER-ERRORS Entry 5, Phase 18.1.2-04-03/04-04 transition, verbatim pre/post test snippet, coverage report.

WHY (D-13+D-14): SERVER-ERRORS Entry 5 — Phase 18.1.2-04-04 already added `export` and replaced the test. If 18.1.2-04 didn't fully revert the 18.1.2-04-03 spy-swallow shape (it likely did per the SUMMARY — verify by Read), then 19-01-03 is a no-op confirming closure. Otherwise this commit completes the revert. WHY (D-23): coverage pragma is acceptable when production side-effect (process.once) cannot be unit-tested without re-emitting signals (which causes the original Entry 5 issue).
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | grep -E "(✓|PASS|passed)" | head -3 && grep -c "export const onSignal" apps/api/src/otel-bootstrap.ts && grep -c "mod.onSignal" apps/api/tests/unit/otel-bootstrap.test.ts</automated></verify>
  <done>1 commit; otel-bootstrap test GREEN with REAL behavior assertion (shutdownSdk spy observed); export present; Phase 18.1.2-04-03 spy-swallow workaround removed; coverage ≥ 90/90/90/90 (with pragma if needed); lefthook clean.</done>
</task>

<task type="auto">
  <name>Task 19-01-04: docs — append pg_partman prerequisite to docs/operations.md</name>
  <files>docs/operations.md</files>
  <action>
Per D-15 + SR-19.5 (no TDD; docs-only).

1. Read `docs/operations.md` and locate the "Local development test prerequisites" section (created in Phase 18.1.2-05-05 per CONTEXT canonical_refs). `grep -n "Local development test prerequisites" docs/operations.md`.
2. Append a subsection (do NOT rewrite the section):
   ```markdown
   ### pg_partman extension (required for migration 0014)

   Migration `packages/data/migrations/0014_audit_log_partition.sql` requires
   the `pg_partman` extension. The standard `postgres:17-alpine` image does
   not ship it; use the project's custom image:

   ```bash
   docker pull openwhispr/postgres:17.5-pgpartman
   # OR build locally:
   make build-pg-partman   # if Makefile target exists; otherwise see infra/postgres/
   ```

   The integration test infrastructure (`apps/api/tests/support/shared-pg.ts`)
   already targets this image. See SERVER-ERRORS Entry 3 for the surfacing
   context.
   ```
3. Verify Makefile target existence: `grep -n "build-pg-partman" Makefile 2>&1 || echo "no Makefile target — phrasing already conditional"`. If absent, the snippet's conditional phrasing ("if Makefile target exists") covers it; if present, optionally tighten the wording.
4. Single atomic commit (D-22 docs-only carve-out): `docs(19-01-04): append pg_partman prerequisite to docs/operations.md (SR-19.5, D-15)`. Body MUST cite: D-15, SERVER-ERRORS Entry 3, Phase 18.1.2-05-05 section base.

WHY (D-15): SERVER-ERRORS Entry 3 closure — production migration is canonical (no code fix needed); operator/contributor onboarding needs the image recipe. WHY (no TDD): docs change, no executable assertion possible.
  </action>
  <verify><automated>grep -c "openwhispr/postgres:17.5-pgpartman" docs/operations.md</automated></verify>
  <done>1 commit; docs/operations.md contains pg_partman subsection under "Local development test prerequisites"; image tag string present; lefthook clean.</done>
</task>

</tasks>

<verification>
After 4 commits:

```bash
cd /Users/nick/openwhispr-server
pnpm --filter @openwhispr/api typecheck            # D-07 root-cause closure
pnpm --filter @openwhispr/api test otel-bootstrap  # D-13 + D-14 revert
grep -c "openwhispr/postgres:17.5-pgpartman" docs/operations.md  # D-15
git log --oneline -n 4 | grep -cE "(test|feat|fix|docs)\(19-01-0[1-4]\)"   # 4
```
</verification>

<success_criteria>
1. ~4 atomic commits (RED + GREEN .d.ts pair; single GREEN otel; docs).
2. `apps/api/src/types/fastify.d.ts` exists with `declare module 'fastify'`; typecheck-test GREEN.
3. `apps/api/src/otel-bootstrap.ts:144` exports `onSignal`; test uses direct `mod.onSignal()` + `shutdownSdk` spy.
4. `docs/operations.md` contains pg_partman prerequisite subsection.
5. `.planning/deferred-items.md §14-04` marked CLOSED.
6. Coverage ≥ 90/90/90/90 on `otel-bootstrap.ts` per D-23 (`.d.ts` exempt).
7. ZERO `--no-verify` (D-21); lefthook clean.
8. ≤ 7 commits per plan (D-18; 4 used).
</success_criteria>

<rollback>
- `apps/api/tsconfig.json` already excludes `.d.ts` somehow → adjust `include` array; do not move the .d.ts file out of `src/types/`.
- otel-bootstrap test still RED after revert (spy on dynamic import surface fails) → use `vi.mock("../../src/otel-bootstrap", async (orig) => ({ ...await orig(), shutdownSdk: vi.fn() }))` shape; document fallback in commit body.
- pg_partman section name in docs/operations.md drifted from "Local development test prerequisites" → grep for the actual heading and append under that; do NOT create duplicate sections.
- HALT recipe: if `pnpm typecheck` reveals 20+ NEW unrelated TS errors after .d.ts lands (e.g., overload conflicts), HALT and propose 3-branch user choice (a/revert and use `req as any`, b/narrow types, c/defer SR-19.2 to v3). Per D-26 escalation discipline.
</rollback>

<output>
Create `.planning/phases/19-server-error-closure/19-01-SUMMARY.md` (≤ 80 LOC per CONTEXT discretion) with:
- 4 commit SHAs
- D-07 typecheck root-cause closure proof (Phase 14-04 §14-04 transitioned)
- D-13/D-14 revert proof (test now uses real `mod.onSignal()`)
- D-15 docs append proof (pg_partman image tag present)
- Coverage report on `otel-bootstrap.ts`
- Lefthook status (clean)
- SERVER-ERRORS Entries 2, 3, 5 owner transitions queued for Plan 03 ledger update (D-25)
</output>
