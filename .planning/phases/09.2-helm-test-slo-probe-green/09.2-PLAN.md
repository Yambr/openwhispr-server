---
phase: 09.2
title: Phase 09.2 — helm test SLO probe GREEN
status: in_progress
created: 2026-05-13
parent_phase: 09.1
scope: subordinate
requirements_traced: [DEPLOY-05]
ai_dimension: none
---

# Phase 09.2 — helm test SLO probe GREEN

## Why this phase exists

Phase 09.1 closed with `helm install` successful (11 pods 1/1 Running in 59s) but `helm test` partial: SLO probe reaches the api Service in <40ms then Better Auth signup returns 403. 09.1 logged this as **F35** and deferred it to 09.2 because the root cause is application-side integration (probe ↔ Better Auth CSRF/origin policy), not chart wiring.

09.2 is the minimum work to flip `helm test` GREEN so DEPLOY-05 success criterion fully passes.

## Goal

> `helm test openwhispr -n ow --logs --timeout 5m` returns exit 0 with the probe Pod logging `{"ok":true,"elapsedMs":<5min,"step":"ok"}`.

## Scope (locked)

**In:**
- F35 root-cause investigation + fix.
- Probe + chart changes if needed.
- helm-unittest assertions for any chart change.
- helm install + helm test on kind cluster end-to-end.

**Out (recorded for later phases):**
- Advisor Option C — SET LOCAL app.tenant_id per-tx refactor.
- Reviewer H1/H3 — tautological assertion + per-revision Job churn cleanup.
- F21 follow-up — Traefik admin :9000 + preflight back on.
- F32 follow-up — worker HTTP probe hook.

## Plan list (estimated)

| # | Title | TDD axis |
| -- | --- | --- |
| 09.2-1 | Root-cause F35 (probe vs api Better Auth CSRF/origin/IP) | curl from in-cluster Pod against api Service |
| 09.2-2 | Apply fix (probe.ts Origin header + chart AUTH_TRUSTED_ORIGINS_EXTRA env) | vitest on probe + helm-unittest |
| 09.2-3 | Rebuild test-probe image + kind-load + `helm test` GREEN | live cluster |
| 09.2-4 | Close-out — SUMMARY + ROADMAP + STATE + commit | docs |

## Success criteria

1. `helm test openwhispr -n ow --logs` exits 0.
2. probe Pod logs `{"ok":true,"step":"ok","elapsedMs":<300000}`.
3. helm-unittest still 109/109 (or +N from new RED assertions).
4. No regression in 09.1 success criteria.

## Acceptance gate

(1) `helm test` GREEN. (2) RED→GREEN test pair for any chart/code change. (3) 09.2-SUMMARY.md committed. (4) ROADMAP slot for 09.2 flipped to [x].
