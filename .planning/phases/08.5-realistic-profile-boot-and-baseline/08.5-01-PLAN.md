---
phase: 08.5-realistic-profile-boot-and-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.load-test.yml
  - docker-compose.load-test.realistic.yml
  - compose/litellm/litellm_config.realistic.yaml
  - compose/litellm/__tests__/litellm_config.realistic.test.ts
  - tools/load-test/scripts/pre-warm-speaches.sh
  - tools/load-test/scripts/pre-warm-speaches.test.sh
  - tools/load-test/scripts/verify-compose.test.sh
  - .env.example
autonomous: true
requirements: [SCALE-02]
tags: [compose, litellm, speaches, env, prewarm, realistic]

must_haves:
  truths:
    - "docker compose --profile load-test-realistic config shows litellm.image == ghcr.io/berriai/litellm:main-v1.83.14-stable (NOT openwhispr-mock-litellm:dev)."
    - "The rendered realistic config mounts compose/litellm/litellm_config.realistic.yaml into the litellm container at /app/config.yaml read-only."
    - "Speaches preloads Systran/faster-whisper-large-v3 and pyannote/speaker-diarization-community-1 via PRELOAD_MODELS at boot."
    - "Speaches HF cache persists at /home/ubuntu/.cache/huggingface across `down + up`."
    - "pre-warm-speaches.sh --strict exits non-zero when a preloaded model dir is absent or the warm transcribe call fails."
    - ".env.example documents HF_TOKEN as required-for-realistic with a one-line operator note."
  artifacts:
    - path: "docker-compose.load-test.realistic.yml"
      provides: "Third-overlay file that un-mocks litellm under the realistic profile; re-states image, build:null, command, volumes, environment, depends_on, healthcheck."
    - path: "compose/litellm/litellm_config.realistic.yaml"
      provides: "model_list with whisper-large-v3 → openai/Systran/faster-whisper-large-v3 @ http://speaches:8000/v1; OpenRouter + OpenAI realtime aliases unchanged from bundled config."
    - path: "tools/load-test/scripts/pre-warm-speaches.sh"
      provides: "Extended with --strict flag; default-on when invoked under the realistic profile."
    - path: ".env.example"
      provides: "HF_TOKEN row + annotations on GROQ_API_KEY/OPENROUTER_API_KEY/OPENAI_API_KEY for realistic usage."
  key_links:
    - from: "docker-compose.load-test.realistic.yml"
      to: "compose/litellm/litellm_config.realistic.yaml"
      via: "volume mount :ro"
      pattern: "litellm_config.realistic.yaml.*:ro"
    - from: "docker-compose.load-test.yml (speaches service)"
      to: "PRELOAD_MODELS env var"
      via: "environment block"
      pattern: "PRELOAD_MODELS:.*faster-whisper-large-v3.*pyannote.*community-1"
    - from: "speaches volume mount"
      to: "/home/ubuntu/.cache/huggingface"
      via: "named volume speaches_models"
      pattern: "speaches_models:/home/ubuntu/\\.cache/huggingface"
---

<objective>
Land every compose-time + LiteLLM-config-time fix needed for the `load-test-realistic` profile to boot the REAL stack (real LiteLLM + Speaches with preloaded Whisper-large-v3 + pyannote) instead of the mock. This is the foundation Wave 2 (baseline.ts + run.sh) consumes.

Purpose: G1 + G2 + G3 + G6 + G7 from RESEARCH.md are all configuration-layer fixes that can land atomically before any TS / shell harness change. Decoupling them keeps the diff reviewable.
Output: 3 new files (overlay, litellm config, 1 vitest), 3 modified files (existing compose, pre-warm, env.example), 2 modified test files. Five atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-PLAN.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-RESEARCH.md

# Upstream canonical references — read for shape
@speaches-audio.md
@docs/litellm-target-spec.md

# Files to modify or pattern-match against
@docker-compose.load-test.yml
@compose/litellm/litellm_config.yaml
@compose/litellm/litellm_config.contract.yaml
@tools/load-test/scripts/pre-warm-speaches.sh
@tools/load-test/scripts/verify-compose.test.sh
@.env.example

<interfaces>
<!-- Key contracts extracted from the codebase for this plan. -->

From compose/litellm/litellm_config.yaml (template shape — realistic mirrors this but swaps whisper-large-v3):
  - model_list[] entries: { model_name, litellm_params: { model, api_base?, api_key, mode? } }
  - general_settings: { master_key, database_url, store_model_in_db }
  - litellm_settings: { drop_params, set_verbose }

From docker-compose.load-test.yml lines 321–344 (current mock override of services.litellm):
  - profiles: [default, load-test-mock, load-test-realistic]   # MUST narrow to [default, load-test-mock]
  - image: openwhispr-mock-litellm:dev
  - build.context: ./compose/mock-litellm
  - command: []
  - volumes: []
  - environment: { LITELLM_MASTER_KEY:"", LITELLM_DATABASE_URL:"", DATABASE_URL:"", OPENROUTER_API_KEY:"", GROQ_API_KEY:"", OPENAI_API_KEY:"" }
  - healthcheck.test: wget /health/liveliness on :4000

From docker-compose.yml (base) services.litellm:
  - image: ghcr.io/berriai/litellm:main-v1.83.14-stable
  - command: ["--config", "/app/config.yaml", ...]   # the realistic overlay must re-state this verbatim
  - volumes: [./compose/litellm/litellm_config.yaml:/app/config.yaml:ro]
  - environment: { LITELLM_MASTER_KEY, LITELLM_DATABASE_URL, OPENROUTER_API_KEY, GROQ_API_KEY, OPENAI_API_KEY }
  - healthcheck.test: wget /health/liveliness on :4000

From docker-compose.load-test.yml lines 360–377 (current speaches service — to be patched):
  - profiles: [load-test-realistic]
  - environment: { WHISPER_MODEL, WHISPER__MODEL, HF_TOKEN, ENABLE_UI } ← WHISPER_* are wrong (no-op); replace with PRELOAD_MODELS
  - volumes: [speaches_models:/root/.cache/huggingface]            ← wrong path (latest-cpu runs as ubuntu)
  - healthcheck.start_period: 180s                                  ← bump to 600s for first-boot 3 GB download
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds + extend verify-compose.test.sh — RED for the realistic overlay</name>
  <files>tools/load-test/scripts/verify-compose.test.sh, tools/load-test/scripts/pre-warm-speaches.test.sh, compose/litellm/__tests__/litellm_config.realistic.test.ts</files>
  <behavior>
    - verify-compose.test.sh new case: when `docker compose -f docker-compose.yml -f docker-compose.load-test.yml -f docker-compose.load-test.realistic.yml --profile load-test-realistic config` is run, the rendered output contains `image: ghcr.io/berriai/litellm:main-v1.83.14-stable` AND `litellm_config.realistic.yaml`. The current state (no third overlay) MUST fail this case → RED proven.
    - verify-compose.test.sh new case: rendered output for `services.speaches.environment` contains `PRELOAD_MODELS:` with both `Systran/faster-whisper-large-v3` and `pyannote/speaker-diarization-community-1` substrings. Current state has `WHISPER_MODEL` → RED.
    - verify-compose.test.sh new case: rendered `services.speaches.volumes` resolves the mount target to `/home/ubuntu/.cache/huggingface` (NOT `/root/.cache/huggingface`). RED.
    - pre-warm-speaches.test.sh new case: invoking `pre-warm-speaches.sh --strict` against a stub `docker compose ps -q speaches` that returns empty → exits non-zero with a clear message. Current script exits 0 with a warning → RED.
    - pre-warm-speaches.test.sh new case: invoking `pre-warm-speaches.sh --strict` against a stub that simulates the warm transcribe call returning non-zero → exits non-zero. Current script exits 0 → RED.
    - litellm_config.realistic.test.ts (vitest): loads the YAML via js-yaml, asserts `model_list` contains an entry with `model_name === "whisper-large-v3"` whose `litellm_params.api_base === "http://speaches:8000/v1"` and `litellm_params.model === "openai/Systran/faster-whisper-large-v3"`. The file does not exist yet → RED.
    - litellm_config.realistic.test.ts: asserts `gpt-realtime`, `gpt-realtime-mini`, `gpt-4o-realtime-preview`, `qwen3.6-plus`, `gemini-3-flash`, `gpt-4o-mini` aliases all present (parity with bundled config minus the swapped whisper entry).
    - litellm_config.realistic.test.ts: asserts `general_settings.master_key === "os.environ/LITELLM_MASTER_KEY"` (env-driven, no plaintext in YAML).
  </behavior>
  <action>
    Add the new test cases to the existing shell test files (append, do not replace). Create the new vitest file under `compose/litellm/__tests__/` (mirror the directory pattern used by other compose-level tests if any; otherwise create the dir). Use `js-yaml` from the workspace's existing devDependencies (verify via `grep js-yaml package.json` first; if absent, fall back to a regex-based YAML load against the literal file lines — RESEARCH §13 G2 spec is small enough to assert via grep). Commit the failing tests with subject `test(08.5-01): add realistic overlay + speaches env + prewarm strict RED`.
  </action>
  <verify>
    <automated>cd tools/load-test/scripts && sh verify-compose.test.sh; sh pre-warm-speaches.test.sh; cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/load-test test -- --run compose/litellm/__tests__/litellm_config.realistic.test.ts 2>&1 | grep -E "FAIL|RED" | head -5</automated>
  </verify>
  <done>All five new test cases fail with clear diagnostic messages; commit subject `test(08.5-01): …RED` lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN G1 — third compose overlay un-mocks litellm under realistic; G3+G6 fix Speaches env + cache path</name>
  <files>docker-compose.load-test.realistic.yml, docker-compose.load-test.yml</files>
  <behavior>
    - GREEN for the three verify-compose.test.sh cases added in Task 1.
    - The new `docker-compose.load-test.realistic.yml` defines `services.litellm` re-stating EVERY field the mock overlay touches (per RESEARCH §P10): `image: ghcr.io/berriai/litellm:main-v1.83.14-stable`, `build: null` (or omit build key — verify which form compose merge respects; if `build: null` is rejected, use `!reset` tag or omit), `command` re-stated verbatim from the base service, `volumes` includes `./compose/litellm/litellm_config.realistic.yaml:/app/config.yaml:ro`, `environment` restored to its base-service shape, `depends_on` includes postgres + migrate, `healthcheck` re-stated, `profiles: [load-test-realistic]`.
    - Existing `docker-compose.load-test.yml` narrows the mock-litellm override's `profiles` from `[default, load-test-mock, load-test-realistic]` to `[default, load-test-mock]` so the third file's realistic-only override wins cleanly with no key collision.
    - Existing `docker-compose.load-test.yml` `services.speaches`: replace `WHISPER_MODEL` + `WHISPER__MODEL` with `PRELOAD_MODELS: "Systran/faster-whisper-large-v3,pyannote/speaker-diarization-community-1"`. Bump `healthcheck.start_period: 180s` → `600s` (first-boot 3 GB download buffer per RESEARCH §P8). Change volume mount from `speaches_models:/root/.cache/huggingface` to `speaches_models:/home/ubuntu/.cache/huggingface` (RESEARCH §P2 / A5).
  </behavior>
  <action>
    Create `docker-compose.load-test.realistic.yml` re-stating every overridden litellm field per the merge-precedence concern in RESEARCH §P10. Use compose-2.x merge semantics: each top-level key on a service replaces (image/build/command), merges per-key (environment/depends_on), or is overlay-additive (profiles concat — that is why we narrow the mock override's profiles list in the SAME commit). The mount path must be `:ro` since LiteLLM only reads the config at boot. Atomic commit subject `feat(08.5-01): add realistic compose overlay; fix speaches preload + cache path` per D-01-style Conventional Commits. After the commit, re-run Task 1's verify tests — they must transition RED→GREEN. Use `docker compose -f ... -f ... -f ... --profile load-test-realistic config` to render and grep, NOT `docker compose up` (no live boot in this task).
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && sh tools/load-test/scripts/verify-compose.test.sh 2>&1 | grep -E "PASS|GREEN|^ok" | head -10</automated>
  </verify>
  <done>All three new verify-compose.test.sh cases PASS. `docker compose ... --profile load-test-realistic config | grep -c "openwhispr-mock-litellm"` returns 0. `docker compose ... config | grep -c "PRELOAD_MODELS"` ≥ 1. Atomic commit lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN G2 — litellm_config.realistic.yaml routes whisper-large-v3 to Speaches</name>
  <files>compose/litellm/litellm_config.realistic.yaml</files>
  <behavior>
    - GREEN for compose/litellm/__tests__/litellm_config.realistic.test.ts added in Task 1.
    - The YAML mirrors `litellm_config.yaml` (currently 78 lines) verbatim EXCEPT the `whisper-large-v3` entry: swap `model: whisper-large-v3` + `api_base: https://api.groq.com/openai/v1` + `api_key: os.environ/GROQ_API_KEY` for `model: openai/Systran/faster-whisper-large-v3` + `api_base: http://speaches:8000/v1` + `api_key: "speaches-dummy"`.
    - All OpenRouter aliases (`qwen3.6-plus`, `gemini-3-flash`, `gpt-4o-mini`) and OpenAI realtime aliases (`gpt-realtime`, `gpt-realtime-mini`, `gpt-4o-realtime-preview`) carried over unchanged (matching the canonical wiring from speaches-audio.md per RESEARCH §2: only the audio plane swaps).
    - Header comment block names the file as the realistic-profile alternate and references RESEARCH §G2 + speaches-audio.md as provenance.
    - Per RESEARCH §G13 / A2: agent-stream uses `model: "openrouter/anthropic/claude-haiku-4.5"` which LiteLLM v1.83+ auto-resolves via the `openrouter/*` wildcard configured in OpenRouter routing — NO explicit alias added (will be empirically confirmed in Wave 3 smoke; if it fails, that becomes a 08.5-04 gap-closure plan, not part of this task).
  </behavior>
  <action>
    Create `compose/litellm/litellm_config.realistic.yaml`. Source = `litellm_config.yaml` with the one swap above. Add a `general_settings.pass_through_endpoints` block for `/v1/audio/diarization → http://speaches:8000/v1/audio/diarization` per speaches-audio.md Section 2 — even though Mac smoke skips diarization (RESEARCH §G7 / §3 recommendation), having the pass-through in the config is correct topology and free of cost (no warm hit unless k6 invokes it). Commit subject `feat(08.5-01): add litellm_config.realistic.yaml — whisper→speaches`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/load-test test -- --run compose/litellm/__tests__/litellm_config.realistic.test.ts</automated>
  </verify>
  <done>Vitest file passes. `grep -c "http://speaches:8000/v1" compose/litellm/litellm_config.realistic.yaml` ≥ 1. `grep -c "groq" compose/litellm/litellm_config.realistic.yaml` returns 0 (no stale Groq routing leaked).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN G7 + G5 — pre-warm strict mode + .env.example HF_TOKEN row</name>
  <files>tools/load-test/scripts/pre-warm-speaches.sh, .env.example</files>
  <behavior>
    - GREEN for the two pre-warm-speaches.test.sh cases added in Task 1.
    - `pre-warm-speaches.sh` accepts `--strict` flag (also auto-detected when env `LOADTEST_PROFILE=realistic` is set, so run.sh can opt in without explicit flag).
    - In strict mode: container-not-running → exit 1 (was: exit 0 with warning). Warm transcribe failure → exit 1 (was: exit 0). Additionally checks `docker compose exec -T speaches sh -c 'ls /home/ubuntu/.cache/huggingface/hub/' | grep -c models--Systran--faster-whisper-large-v3` returns ≥ 1 AND grep for `models--pyannote--speaker-diarization-community-1` returns ≥ 1 (RESEARCH §Pre-warm strategy recommendation).
    - Default (no flag) behavior unchanged — existing Phase 08 callers see no semantic shift.
    - `.env.example`: add `HF_TOKEN=` row above the existing provider keys block with a one-line comment noting it is required only for `load-test-realistic` (pyannote weight download). Annotate the existing `GROQ_API_KEY` line that under realistic GROQ is unused (Speaches replaces it). Annotate `OPENROUTER_API_KEY` and `OPENAI_API_KEY` lines as required-for-realistic.
  </behavior>
  <action>
    Extend pre-warm-speaches.sh with a strict-mode branch. Keep the tolerant default for back-compat. Add the model-dir-presence check guarded by `if [ "$STRICT" = "1" ]`. Update .env.example with the new HF_TOKEN row + annotation comments (find the existing GROQ_API_KEY line, prepend the new HF_TOKEN block, append `# Required for: load-test-realistic (pyannote gated weights download)`). Atomic commit subject `feat(08.5-01): add pre-warm --strict mode + .env.example HF_TOKEN`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && sh tools/load-test/scripts/pre-warm-speaches.test.sh 2>&1 | tail -10; grep -c "HF_TOKEN" .env.example</automated>
  </verify>
  <done>Both new pre-warm-speaches.test.sh cases PASS. `.env.example` contains `HF_TOKEN=` row. `grep -c "load-test-realistic" .env.example` ≥ 2 (HF_TOKEN row + provider-key annotations).</done>
</task>

</tasks>

<verification>
- `docker compose -f docker-compose.yml -f docker-compose.load-test.yml -f docker-compose.load-test.realistic.yml --profile load-test-realistic config` renders without error.
- The rendered config's `services.litellm.image` is the real LiteLLM image, NOT the mock.
- The rendered config's `services.speaches.environment.PRELOAD_MODELS` contains both expected model IDs.
- The rendered config's `services.speaches.volumes` mounts to `/home/ubuntu/.cache/huggingface`.
- The new vitest file under `compose/litellm/__tests__/` passes.
- `pnpm --filter @openwhispr/load-test test` is green.
- All shell tests under `tools/load-test/scripts/*.test.sh` are green.
- No file in `tools/load-test/src/` is modified by this plan (that's Wave 2's surface).
- No live `docker compose up` is required for this plan — Wave 3 owns the live boot.
</verification>

<success_criteria>
1. `docker compose --profile load-test-realistic config | grep image: | grep -c berriai/litellm` ≥ 1 (real LiteLLM image present in render).
2. `docker compose --profile load-test-realistic config | grep -c openwhispr-mock-litellm` returns 0 (mock not selected under realistic).
3. `compose/litellm/litellm_config.realistic.yaml` exists with `api_base: http://speaches:8000/v1` for `whisper-large-v3`.
4. `pre-warm-speaches.sh --strict` fails fast on a missing container; the smoke gate semantics are preserved.
5. `.env.example` documents HF_TOKEN + annotates provider keys for realistic usage.
6. Five atomic commits land (one per task + one combining the two Task 4 files OR split into two; planner's call to keep commits focused).
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-01-SUMMARY.md` documenting: files created/modified, the merge-precedence approach taken for the third overlay (verbatim re-state vs. profile-narrowing), and any deviation from RESEARCH §3 deltas with rationale.
</output>
