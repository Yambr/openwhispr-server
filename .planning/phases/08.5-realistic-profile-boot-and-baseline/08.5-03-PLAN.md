---
phase: 08.5-realistic-profile-boot-and-baseline
plan: 03
type: execute
wave: 3
depends_on: ["08.5-01", "08.5-02"]
files_modified:
  - .planning/phases/08.5-realistic-profile-boot-and-baseline/runs/
  - .planning/phases/08.5-realistic-profile-boot-and-baseline/runs/RUN-LOG.md
  - docs/operations.md
autonomous: false
requirements: [SCALE-02, TEST-LOAD-01]
tags: [live-run, baseline, docs, operator-handoff]

must_haves:
  truths:
    - "make load-test PROFILE=realistic survives end-to-end on the Mac: docker compose --wait succeeds, pre-warm strict mode passes (both models resident), k6-smoke gate passes at 5 VU × 60s with ZERO TypeError and ZERO ws-zero-frames."
    - "tools/load-test/scripts/baseline.sh realistic produces a coherent summary JSON under runs/<stamp>-realistic-mac.json with non-zero p95 numbers for all four flows (transcribe, reason, agent-stream, realtime-ws)."
    - "runs/RUN-LOG.md captures: hostware (Mac model, CPU, RAM), Docker Desktop memory budget, model preload wall-clock, smoke gate result, baseline p95 per endpoint, baseline iter/error counts."
    - "docs/operations.md gains a new section distinguishing Mac-bound metrics (transcribe — CPU-bound, expect 8–20 s p95 on Mac, < 1 s on H100) from architecture-bound metrics (reason / agent-stream / realtime-ws — network-bound, similar on both)."
    - "docs/operations.md has explicit OPERATOR_RERUN_ON_GPU markers per metric block."
    - "All existing tests still pass (no test file was modified other than the additive Wave 1+2 extensions)."
  artifacts:
    - path: ".planning/phases/08.5-realistic-profile-boot-and-baseline/runs/<stamp>-realistic-mac.json"
      provides: "Raw k6 JSON output from the 12-min baseline run."
    - path: ".planning/phases/08.5-realistic-profile-boot-and-baseline/runs/<stamp>-realistic-mac-summary.json"
      provides: "k6 summary-export — per-endpoint p50/p95/p99/max + error rates."
    - path: ".planning/phases/08.5-realistic-profile-boot-and-baseline/runs/RUN-LOG.md"
      provides: "Human-readable run summary + Mac context + OPERATOR_RERUN_ON_GPU pointers."
    - path: "docs/operations.md"
      provides: "Extended operator-facing guidance distinguishing Mac vs architecture-bound metrics with explicit OPERATOR_RERUN_ON_GPU markers."
  key_links:
    - from: ".planning/phases/08.5-…/runs/RUN-LOG.md"
      to: "docs/operations.md (OPERATOR_RERUN_ON_GPU section)"
      via: "explicit cross-reference"
      pattern: "OPERATOR_RERUN_ON_GPU"
---

<objective>
Execute the live realistic-stack bring-up on the Mac, run the smoke gate + 12-min baseline, commit the artifacts, and write the operator-handoff documentation that maps Mac numbers to expected H100 numbers so the operator can run the EXACT same suite by swapping `.env` keys + `BASELINE_VUS`.

Purpose: ROADMAP success criteria 1, 3, 4, 6. The wiring is the deliverable; this plan turns it into a thing an operator can re-run. The plan is NON-AUTONOMOUS because the live run requires human verification (visual inspection of the boot, smoke gate, and baseline summary, and any debugging if the first-pass live boot surfaces an unknown unknown — e.g., G13 agent-stream wildcard resolution being wrong, or HF cache hot-path tripping on Apple Silicon, or LiteLLM v1.83.14 multipart pass-through having a Mac-arm64-specific quirk).
Output: One live boot, three artifacts (runs/*.json + RUN-LOG.md), one extended docs file. One or two atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-PLAN.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-01-PLAN.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-02-PLAN.md
@.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-RESEARCH.md

# Operator-facing doc to extend
@docs/operations.md

# Reference for run-log shape — Phase 08 already established the pattern
@.planning/phases/08-load-test-tuning-slo-publication/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live bring-up + smoke gate verification (autonomous portion of the run)</name>
  <files>(no files written by this task — captures observations)</files>
  <action>
    Preconditions: 08.5-01 + 08.5-02 merged. .env populated by operator with HF_TOKEN, OPENROUTER_API_KEY, OPENAI_API_KEY. Docker Desktop memory ≥ 16 GB (per RESEARCH §P4).

    Sequence:
    1. `make load-test PROFILE=realistic` — invokes run.sh realistic. Watch for: image pulls succeed (LiteLLM v1.83.14, Speaches latest-cpu), `docker compose up --wait` completes within ~10 min (first boot — 3 GB HF model download dominates), all containers report healthy.
    2. If `up --wait` times out due to Speaches start_period (600s ceiling per 08.5-01 Task 2): `docker compose logs speaches | tail -50` to confirm download progress; re-run `make load-test PROFILE=realistic` (cached weights will boot fast the second time).
    3. Pre-warm (strict): verify it asserts both model dirs present under `/home/ubuntu/.cache/huggingface/hub/` and the warm transcribe call returns < 15 s.
    4. Smoke gate (5 VU × 60 s, per 08.5-02 Task 4): MUST exit 0. If FAIL_REASON surfaces (TypeError, ws-zero-frames, threshold breach):
       - TypeError in script → 08.5-04 gap-closure plan (this plan blocks).
       - ws-zero-frames → check OPENAI_API_KEY validity, check Traefik 3600s idleConnTimeout (Phase 04 Plan 05 — should already be wired).
       - agent-stream 400/500 → RESEARCH A2 falsified: openrouter/* wildcard NOT auto-resolving. Fix: add explicit `openrouter/anthropic/claude-haiku-4.5` alias to litellm_config.realistic.yaml in a 08.5-04 patch plan, OR fall the k6 flow back to `gpt-4o-mini` (which has an explicit alias). DO NOT SILENTLY DEFER — surface to operator.
    5. Baseline gate: run.sh continues into k6 run dist/main.js (1000 VU × 30 min). For this plan we WANT to abort run.sh BEFORE the main.js run starts — the main.js plateau is mock-profile-shaped and will saturate on Mac. So: at this stage run only ran the smoke gate. Now invoke `tools/load-test/scripts/baseline.sh realistic` separately to run the 100 VU × 12 min baseline scenario.

    Action variant: instead of `make load-test PROFILE=realistic` (which proceeds to 1000-VU main.js post-smoke), introduce a `MAKE_BASELINE_ONLY=1` env knob that run.sh reads → exit 0 after smoke. Cleaner. But this is a small change — fold into 08.5-02 Task 4 if desired, OR document in this task that the operator runs:
       ```
       SMOKE_SKIP=0 OPENWHISPR_LOADTEST_KEEP_STACK=1 tools/load-test/scripts/run.sh realistic   # smoke only (Ctrl-C after gate passes)
       tools/load-test/scripts/baseline.sh realistic                                            # baseline on the still-running stack
       ```
       This keeps run.sh untouched and reuses the OPENWHISPR_LOADTEST_KEEP_STACK escape hatch (already in 08-Plan-06 surface).

    Recommend: extend baseline.sh in 08.5-02 Task 3 to skip the up/down lifecycle when OPENWHISPR_LOADTEST_REUSE_STACK=1 is set — operator boots once via run.sh, smoke gate runs, then baseline.sh re-uses the running stack. Cleanest operator flow. (Decision for the executor at 08.5-02 plan time.)
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && docker compose -f docker-compose.yml -f docker-compose.load-test.yml -f docker-compose.load-test.realistic.yml --profile load-test-realistic ps --format json | python3 -c "import json,sys; svc=[json.loads(l) for l in sys.stdin if l.strip()]; healthy=[s for s in svc if s.get('Health','')=='healthy' or s.get('State','')=='running']; print(f'{len(healthy)}/{len(svc)} healthy/running'); sys.exit(0 if len(healthy)==len(svc) and len(svc)>0 else 1)"</automated>
  </verify>
  <done>All realistic-profile containers healthy; smoke gate exits 0; baseline.sh runs to completion writing the two output files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the live run</name>
  <what-built>
    - The realistic stack booted on the Mac.
    - Speaches preloaded Whisper-large-v3 + pyannote-community-1 (verified by pre-warm strict mode).
    - k6-smoke at 5 VU × 60 s passed with ZERO TypeError, ZERO ws-zero-frames.
    - baseline.sh ran 100 VU × 12 min and wrote `.planning/phases/08.5-…/runs/<stamp>-realistic-mac.json` + `<stamp>-realistic-mac-summary.json`.
  </what-built>
  <how-to-verify>
    1. Inspect the summary JSON: `jq '.metrics["http_req_duration{endpoint:transcribe}"].values["p(95)"]' .planning/phases/08.5-…/runs/<stamp>-realistic-mac-summary.json` — expect a number in the 8000–25000 ms range on Apple Silicon CPU (advisory; if ≥ 60000 the Speaches queue saturated — note in RUN-LOG.md but do NOT block).
    2. Same query for `reason`, `agent-stream` (use `agent_stream_ttfb` and `agent_stream_total` custom metric names), `realtime_ws_roundtrip_ms{endpoint:realtime-ws}`. All four must be > 0 (non-zero p95 = the flow actually ran).
    3. Inspect `http_req_failed.value` — should be < 0.20 on the summary (advisory ceiling). Spikes are tolerable as long as the failure mode is documented in RUN-LOG.md.
    4. Confirm `ws_msgs_sent` and `ws_msgs_received` are both > 0 (Phase 08.4 regression class — the smoke gate already enforces this, but eyeball the baseline summary too).
    5. Smoke log (`runs/<stamp>-smoke.log` from k6-smoke.sh) shows `k6-smoke: PASS`.

    If any of (1)–(5) shows a hard failure (zero p95, ws-zero-frames, smoke FAIL), STOP and open a 08.5-04 gap-closure plan with the observed symptom. DO NOT proceed to docs.
  </how-to-verify>
  <resume-signal>Type "verified" with a one-line summary of p95 numbers, OR describe the failure mode + expected next plan (08.5-04 gap closure).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write runs/RUN-LOG.md + extend docs/operations.md with OPERATOR_RERUN_ON_GPU markers</name>
  <files>.planning/phases/08.5-realistic-profile-boot-and-baseline/runs/RUN-LOG.md, docs/operations.md</files>
  <action>
    Create `runs/RUN-LOG.md` with this structure (operator-facing):

      ```
      # Phase 08.5 — Mac realistic baseline run log

      ## Run <stamp>

      **Hostware:** Mac model + CPU + RAM (operator fills in or executor inspects via `system_profiler SPHardwareDataType`).
      **Docker Desktop memory budget:** N GB.
      **Wall-clock:** boot + first-time HF download = T1; subsequent boots = T2.
      **Smoke gate:** PASS at 5 VU × 60s.
      **Baseline shape:** 100 VU × (5m + 5m + 2m).

      ### p95 per endpoint (Mac M-class CPU)

      | Endpoint | p95 (ms) | Mac-bound? | OPERATOR_RERUN_ON_GPU |
      |---|---|---|---|
      | transcribe (Whisper-large-v3 via Speaches CPU) | <fill> | YES — CPU-bound | Expect < 1000 ms on H100; re-measure |
      | reason (OpenRouter qwen3.6-plus) | <fill> | NO — network-bound | Expect same range on H100 |
      | agent-stream TTFB (OpenRouter claude-haiku-4.5) | <fill> | NO — network-bound | Expect same range on H100 |
      | agent-stream total | <fill> | NO — network-bound | Expect same range on H100 |
      | realtime-ws first-frame (OpenAI Realtime) | <fill> | NO — network-bound | Expect same range on H100 |

      ### Failure modes observed
      <list any 5xx spikes, ws disconnects, etc. — or "none">

      ### Operator H100 re-run recipe
      1. Set `.env` provider keys (HF_TOKEN, OPENROUTER_API_KEY, OPENAI_API_KEY).
      2. Set `BASELINE_VUS=1000 BASELINE_DURATION_SUSTAIN=20m` in the operator's run-env.
      3. `make load-test PROFILE=realistic` for boot + smoke.
      4. `tools/load-test/scripts/baseline.sh realistic` for the H100 plateau.
      5. Commit the new `runs/<stamp>-realistic-h100.json` + `-h100-summary.json` + a new RUN-LOG.md entry under this directory.
      ```

    Then extend `docs/operations.md` with a new section "## Load Test — Realistic Profile (Mac vs H100)" containing:
    - The same Mac-bound vs architecture-bound metric table (without specific p95 numbers — those live in RUN-LOG.md).
    - Each metric block has an explicit `<!-- OPERATOR_RERUN_ON_GPU -->` HTML comment marker so a future tool can grep for places that need re-measurement.
    - A one-paragraph note: "The Mac baseline validates the audio plane wiring (LiteLLM → Speaches → Whisper-large-v3) plus the LLM/realtime network-bound flows. Mac transcribe p95 is dominated by CTranslate2 CPU inference (~0.5–1.0× realtime per RESEARCH §Mac feasibility); H100 p95 will be < 1 s. The other three flows are network-bound to public providers and should be similar on both platforms."
    - A pointer to the canonical operator H100 re-run recipe in RUN-LOG.md.

    Atomic commit subject: `docs(08.5-03): mac realistic baseline run log + operations.md gpu rerun markers`.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && test -f .planning/phases/08.5-realistic-profile-boot-and-baseline/runs/RUN-LOG.md && grep -c "OPERATOR_RERUN_ON_GPU" docs/operations.md | grep -v '^#' | awk '$1>=4 {ok=1} END{exit !ok}'</automated>
  </verify>
  <done>RUN-LOG.md exists and has the four p95 numbers filled. docs/operations.md has ≥ 4 OPERATOR_RERUN_ON_GPU markers (one per metric block). Commit lands.</done>
</task>

</tasks>

<verification>
- All Phase 08.5 ROADMAP success criteria are observable:
  1. `make load-test PROFILE=realistic` boots realistic — verified Task 1.
  2. Smoke at 5 VU × 60s with ZERO errors — verified Task 1.
  3. Baseline summary JSON committed — verified Task 2.
  4. RUN-LOG.md + OPERATOR_RERUN_ON_GPU markers — verified Task 3.
  5. Existing tests preserved — verified by running `pnpm --filter @openwhispr/load-test test` post-Wave-2 and post-Wave-3 (no test file modified by this plan).
  6. Mac vs architecture-bound metric distinction documented — verified Task 3.
- No source-artifact decision from RESEARCH or ROADMAP is silently omitted.
</verification>

<success_criteria>
1. Live boot of the realistic stack completes on the Mac with all containers healthy.
2. Smoke gate at 5 VU × 60s passes with ZERO TypeError / ZERO ws-zero-frames.
3. 100 VU × 12 min baseline produces `<stamp>-realistic-mac.json` + summary; all four endpoint p95 numbers are non-zero.
4. RUN-LOG.md captures the Mac p95 numbers + Mac context + operator H100 re-run recipe.
5. `docs/operations.md` has ≥ 4 `OPERATOR_RERUN_ON_GPU` markers (one per Mac-bound or architecture-bound metric block).
6. No test file is regressed — `pnpm --filter @openwhispr/load-test test` is green.
7. The phase exits with the artifacts committed; an operator can re-run the EXACT same suite on H100 by changing only `.env` and `BASELINE_VUS` env.
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-realistic-profile-boot-and-baseline/08.5-03-SUMMARY.md` documenting: the wall-clock boot time (first-boot vs cached), any unknown unknowns surfaced during live boot (RESEARCH A2/A3 confirmations or falsifications), the four p95 numbers, and the operator H100 re-run handoff status.
</output>
