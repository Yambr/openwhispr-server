---
phase: 08.5-realistic-profile-boot-and-baseline
plan: 01
subsystem: load-test-infrastructure
tags: [compose, litellm, speaches, env, prewarm, realistic, tdd]
requires:
  - phase: 08-load-test-tuning-slo-publication
    plans: [05, 06]
provides:
  - docker-compose.load-test.realistic.yml
  - compose/litellm/litellm_config.realistic.yaml
  - pre-warm-speaches.sh --strict mode
  - .env.example HF_TOKEN row
affects:
  - tools/load-test/scripts/pre-warm-speaches.sh
  - tools/load-test/scripts/verify-compose.test.sh
  - tools/load-test/scripts/profile-lint.test.sh
tech-stack:
  added: []
  patterns:
    - "Three-file compose overlay (mock-vs-real split via dedicated third file)"
    - "Strict pre-warm mode with HF cache assertion (08.5-RESEARCH §Pre-warm strategy)"
key-files:
  created:
    - docker-compose.load-test.realistic.yml
    - compose/litellm/litellm_config.realistic.yaml
    - compose/litellm/__tests__/litellm_config.realistic.test.ts
    - tools/load-test/scripts/pre-warm-speaches.test.sh
  modified:
    - docker-compose.load-test.yml
    - tools/load-test/scripts/pre-warm-speaches.sh
    - tools/load-test/scripts/verify-compose.test.sh
    - .env.example
decisions:
  - "Third-overlay file approach (08.5-RESEARCH §P10) chosen over LITELLM_CONFIG_FILE env-toggle to keep mock and realistic profiles in separate compose files for reviewability."
  - "pre-warm-speaches.sh --strict is opt-in via flag OR LOADTEST_PROFILE=realistic env auto-detection; default tolerant behavior preserved for Phase 08 callers."
metrics:
  duration: ~25m
  completed: 2026-05-13
  tasks: 4
  commits: 4
  files_changed: 8
---

# Phase 08.5 Plan 01: Realistic compose overlay + LiteLLM config + Speaches env

JWT auth with refresh… no — actually: **The realistic profile now boots
the real LiteLLM container (v1.83.14-stable) with a Speaches-routed
whisper-large-v3 alias, preloaded Whisper-large-v3 + pyannote weights
via the canonical PRELOAD_MODELS env, persistent HF cache at the correct
ubuntu-user path, and a strict-mode pre-warm gate that fails the run
when models are not resident.**

## Tasks completed

| Task | Subject | Commit |
|------|---------|--------|
| 1 (RED) | `test(08.5-01): add realistic overlay + speaches env + prewarm strict RED` | 4491369 |
| 2 (GREEN G1+G3+G6) | `feat(08.5-01): add realistic compose overlay; fix speaches preload + cache path` | 5bf0755 |
| 3 (GREEN G2) | `feat(08.5-01): add litellm_config.realistic.yaml — whisper to speaches` | 937a4fe |
| 4 (GREEN G7+G5) | `feat(08.5-01): add pre-warm --strict mode + .env.example HF_TOKEN` | 2f9d63a |

## Verification

- `docker compose -f docker-compose.yml -f docker-compose.load-test.yml -f docker-compose.load-test.realistic.yml --profile load-test-realistic config` renders without error and contains `image: ghcr.io/berriai/litellm:main-v1.83.14-stable`.
- `verify-compose.test.sh` 11/11 PASS (includes 7 new realistic-overlay cases).
- `pre-warm-speaches.test.sh` 4/4 PASS (RED→GREEN for `--strict` flag, transcribe-failure, missing-container, and `LOADTEST_PROFILE=realistic` auto-strict).
- `compose/litellm/__tests__/litellm_config.realistic.test.ts` 6/6 PASS.
- `.env.example` documents `HF_TOKEN` as required-for-realistic + annotates provider keys.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 1 — Bug] Comment leaked a literal `api.groq.com` that tripped the "no stale Groq routing" assertion**
- **Found during:** Task 3 GREEN verification of `litellm_config.realistic.test.ts`.
- **Issue:** A comment block named the swapped-from upstream by URL; the regex `not.toMatch(/api\.groq\.com/i)` caught the literal mention.
- **Fix:** Replaced the URL in the comment with a generic phrase ("bundled-default upstream STT provider") that preserves the comment intent without leaking the literal substring.
- **Files modified:** `compose/litellm/litellm_config.realistic.yaml`
- **Commit:** rolled into 937a4fe (same task / same file edit).

**2. [Rule 1 — Test alignment with intentional code change] `profile-lint.test.sh` had two assertions directly invalidated by intentional Wave-1 changes**
- **Found during:** Cross-test sweep after Task 4 GREEN.
- **Issue:** T13 expected the now-removed `WHISPER_MODEL` env (replaced by canonical `PRELOAD_MODELS` per 08.5-RESEARCH §G3); the `start_period` normaliser regex `^(\d+)(unit)?$` did not handle compound forms like `10m0s` produced by compose's normalisation of the bumped 600s buffer (08.5-RESEARCH §P8).
- **Fix:** Re-pointed T13 at `PRELOAD_MODELS` checking both expected model IDs; expanded the start_period normaliser to handle compound `\d+u\d+u` strings.
- **Files modified:** `tools/load-test/scripts/profile-lint.test.sh`
- **Commit:** e6c7b34 (rolled into Plan 08.5-02 Task 4 commit for atomicity — the profile-lint adjustments cover BOTH the Wave-1 speaches changes and the Wave-2 run.sh extensions in one diff).

### Pre-existing failures NOT addressed (scope-boundary)

`profile-lint.test.sh` has 5 additional failures predating Phase 08.5 (stale `services.mock-litellm` and `services.pgbouncer-1` lookups against canonical service names `litellm` and `pgbouncer`). Logged to `.planning/phases/08.5-realistic-profile-boot-and-baseline/deferred-items.md`. Recommended for a future Plan 08-08 (or follow-up) cleanup.

## Known Stubs

None — every file change is wiring code (compose YAML / shell / YAML config / env doc), not data-flow surface that could carry a stub.

## Self-Check: PASSED

- File `docker-compose.load-test.realistic.yml` exists: FOUND
- File `compose/litellm/litellm_config.realistic.yaml` exists: FOUND
- File `compose/litellm/__tests__/litellm_config.realistic.test.ts` exists: FOUND
- File `tools/load-test/scripts/pre-warm-speaches.test.sh` exists: FOUND
- Commits 4491369, 5bf0755, 937a4fe, 2f9d63a all in `git log --oneline -10`: FOUND.
