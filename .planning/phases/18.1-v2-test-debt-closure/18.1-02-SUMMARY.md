---
phase: 18.1-v2-test-debt-closure
plan: 02
title: "Rate-limit IP-tier carve-out for authed-only routes (V2-SEC-01)"
status: complete
commits:
  red:   c4c6b4b
  green: f6a42cd
start_sha: a8ec79f
files_changed: 5
requirements_closed: [V2-TD-06, V2-SEC-01]
---

# Phase 18.1 Plan 02: rate-limit security fix — SUMMARY

## One-liner

Closed the anonymous DoS vector on authed-only token routes: tagging
`config.authRequired: true` makes the IP-tier `onRequest` hook
early-return on `!req.user`, so anonymous traffic now 401s without
populating `owrl:ip:*` Valkey buckets.

## Commits (2, D-28 literal)

| Order | SHA       | Subject                                                                            |
| ----- | --------- | ---------------------------------------------------------------------------------- |
| 1 RED   | `c4c6b4b` | `test(18.1-02): red — sharpen T3 (no owrl:ip:* on anon authed-route reqs)`         |
| 2 GREEN | `f6a42cd` | `fix(18.1-02): green — authRequired tag skips IP-tier for authed-only routes (T3, security)` |

Diff stats: 5 files (1 test + 1 plugin + 3 route configs);
RED `+6/-0`, GREEN `+39/-1`.

## T3 outcome

| Phase  | T3 status | T1/T2/T4/T5/T6 |
| ------ | --------- | -------------- |
| pre-RED  | red (assertion `keysAfter.toEqual(keysBefore)` already failing — `["owrl:ip:127.0.0.1"]` vs `[]`) | skipped (T3 short-circuit) |
| post-RED | red (sharpened sibling assertion `keysAfter.filter(k => k.startsWith("owrl:ip:"))).toEqual([])` also fails) | skipped |
| post-GREEN | **PASS** (6/6 green) | **PASS** (5/5) |

GREEN run: `Test Files 1 passed (1) | Tests 6 passed (6) | Duration 3.86s`.

## Sweep audit (CONTEXT D-09)

All other `apps/api/tests/**/rate-limit*` suites re-run on GREEN SHA:

- `apps/api/tests/unit/plugins/rate-limit.test.ts` — pass
- `apps/api/tests/unit/__tests__/rate-limit-verification-status.test.ts` — pass
- `apps/api/tests/unit/__tests__/rate-limit-valkey-construction.test.ts` — pass
- `apps/api/tests/unit/__tests__/rate-limit-check-user.test.ts` — pass
- `apps/api/tests/unit/__tests__/rate-limit-health-exempt.test.ts` — pass

`Test Files 5 passed (5) | Tests 49 passed (49) | Duration 657ms`.
No regressions. The plugin-level test asserting `owrl:ip:10.70.0.1`
behavior uses an authentication-free anonymous-route fixture (still
correctly exercises the IP-tier branch) and was unaffected.

## Lefthook (W1 — proactive dry-run before GREEN commit)

```
$ pnpm exec lefthook run pre-commit (after staging 4 prod files)
✔️ biome (0.37s)
✔️ phase-tag-comments (0.54s) — lint-phase-tag-comments: clean
✔️ ui-spec (0.66s)
✔️ english (0.75s)
EXIT=0
```

The 2 stale-comment edits (`rate-limit.ts:27-30` and
`assemblyai.ts:23-26`) were body-only by design and did NOT trip the
`phase-tag-comments` regex — the existing `// Source of truth:` /
`// * [Phase 6] …` lines have non-empty prose bodies which fall under
the conservative-KEEP default. Lefthook at commit time also clean on
both RED and GREEN commits (no `--no-verify` used).

## Auth-OPEN routes (Phase 6 D-RL1 shield preserved)

Grep audit confirms `authRequired` is set on the 3 token routes ONLY:

```
apps/api/src/routes/tokens/assemblyai.ts:55:        authRequired: true,
apps/api/src/routes/tokens/deepgram.ts:34:        authRequired: true,
apps/api/src/routes/tokens/openai-realtime.ts:60:        authRequired: true,
```

`/api/auth/signin`, `/api/auth/signup`, `/api/auth/forgot-password`
remain untagged → the IP-tier `onRequest` hook still fires pre-auth on
those routes, preserving the Phase 6 D-RL1 anonymous DoS shield.

## Coverage delta

- `apps/api/src/plugins/rate-limit.ts`: +1 narrowing line (`authRequired?: boolean`) + 1 early-return + 7 comment lines. New `authRequired === true && !req.user` branch is exercised by T3 (skip-path, 35 anon requests) and T1/T2/T4/T5 (no-skip path, authed requests still IP-counted). Both branches covered → 100% line+branch on the diff.
- 3 token routes: +1 line each (`authRequired: true`) + a short comment block. Pure additive config; existing per-route tests still pass.

## Deviations from plan

None. Two-commit shape, body-only comment edits, zero `--no-verify`,
proactive lefthook dry-run exit 0, T3 RED→GREEN, T1/T2/T4/T5/T6 stable
GREEN, sweep clean, auth-OPEN routes untagged — exactly as specified.

## Forward reference (D-09 deferred)

The two-tier limiter architectural rewrite (`apps/api/src/config/rate-limits.ts`
ip-only / user-and-ip taxonomy refactor — D-09 Option C) is the proper
long-term fix this `authRequired` carve-out defers. Tracked for v3.
