---
phase: 18.1-v2-test-debt-closure
plan: 05
subsystem: cjm-lint + test-debt
tags: [repoint, lint-regex, deferred-items, sc9, atomic-mass-edit]
requires: [18.1-02]
provides:
  - "lint-cjm-doc.ts regex admits @after-phase-N.M sub-phase form"
  - "4 stale @expected-red tags repointed to 19.1/19.2/19.3/19.4"
  - "deferred-items.md §14-04 re-confirmed (SC9 closure)"
affects:
  - tools/lint-cjm-doc.ts
  - tools/lint-cjm-doc.test.ts
  - tests/e2e-cjm/features/{password-reset,transcribe,signup-verify,locale-switch}.feature
  - docs/customer-journeys.md
  - .planning/deferred-items.md
tech-stack:
  added: []
  patterns:
    - "Sub-phase tag numbering N.M (precedent: '18.1' phase id)"
    - "Append-only deferred-items.md edits (PATTERNS §6)"
key-files:
  modified:
    - tools/lint-cjm-doc.ts
    - tools/lint-cjm-doc.test.ts
    - tests/e2e-cjm/features/password-reset.feature
    - tests/e2e-cjm/features/transcribe.feature
    - tests/e2e-cjm/features/signup-verify.feature
    - tests/e2e-cjm/features/locale-switch.feature
    - docs/customer-journeys.md
    - .planning/deferred-items.md
decisions:
  - "Recorded §14-04 typecheck drift: baseline 7 → current 1 (litellm-client index.ts L171 only); six apps/api+apps/worker errors appear resolved by intervening work and need not be re-fixed by Phase 18.2"
  - "Treated 6 pre-existing mode-3 CLI lint offenders (phase17-tls.feature x3, traefik-host-split.feature x2, locale-switch.feature L12 @cjm-6.2) as OUT-OF-SCOPE — vitest test passes (33/33) and CLI offender count is unchanged vs. main HEAD"
metrics:
  duration: ~5 minutes
  completed: 2026-05-15
---

# Phase 18.1 Plan 05: REPOINT + lint regex + §14-04 re-confirm Summary

One-liner: Atomic mass-edit repointing 4 stale `@expected-red @after-phase-12/15` Gherkin tags to sub-phase `19.1/19.2/19.3/19.4` ownership, extending `tools/lint-cjm-doc.ts` regex to admit the sub-phase form in the same commit, and appending a dated §14-04 typecheck re-confirmation block to `.planning/deferred-items.md` to satisfy ROADMAP SC9.

## Commit

| SHA       | Files | Message                                                                                  |
| --------- | ----- | ---------------------------------------------------------------------------------------- |
| `42f390d` | 8     | docs(18.1-05): repoint 4 stale @expected-red tags + extend lint regex + §14-04 re-confirm |

Start SHA was `2d50d62`.

## Edits (8 files, 1 atomic commit)

1. **`tools/lint-cjm-doc.ts:162`** — inline regex extended from `/^@after-phase-\d+$/` to `/^@after-phase-\d+(\.\d+)?$/`. `afterPhase` now records the **major** numeric component (e.g. `19` for `@after-phase-19.1`); `raw` preserves the full token for downstream tooling.
2. **`tools/lint-cjm-doc.test.ts`** — 6 new cases: sub-phase admit (`19.1`), back-compat admit (`19`), reject suffix (`19a`), reject empty (`@after-phase-`), reject trailing dot (`19.`), and `extractFeatureTags` major-component recording. 33/33 tests pass.
3. **`tests/e2e-cjm/features/password-reset.feature:6`** — `@after-phase-12` → `@after-phase-19.1`.
4. **`tests/e2e-cjm/features/transcribe.feature:6`** — `@after-phase-12` → `@after-phase-19.2`.
5. **`tests/e2e-cjm/features/signup-verify.feature:27`** — `@after-phase-15` → `@after-phase-19.3`.
6. **`tests/e2e-cjm/features/locale-switch.feature:6`** — `@after-phase-15` → `@after-phase-19.4`. Line 12 (`@cjm-6.2 @after-docker-up @expected-red`) untouched per plan.
7. **`docs/customer-journeys.md`** — anchor rows at L65/L126/L160/L241 gained the `(after-phase-19.N — currently @expected-red)` suffix per the existing convention used for cjm-5.1/5.3/6.2/7.2 and SSO 1.1–1.6.
8. **`.planning/deferred-items.md`** — append-only:
   - **§14-04 re-confirm block** appended directly after the existing §14-04 "Likely fix" paragraph. Existing §14-04 prose stays byte-identical.
   - **New section "## From Phase 18.1 — stale @expected-red REPOINT"** appended at end with 4 entries naming each missing pre-req (sendResetPassword hook / mock-litellm transcribe overlay / Better Auth i18n interception / locale-e2e Playwright wiring) and the new owner phase.

## Verification

### Vitest (`pnpm vitest run tools/lint-cjm-doc.test.ts`)

```
Test Files  1 passed (1)
     Tests  33 passed (33)
  Duration  2.21s
```

### `pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red`

CLI lint surfaces 6 pre-existing offenders **unrelated to this plan** (verified via `git stash` baseline: same count on `main` HEAD before edits):

- `tests/e2e-cjm/features/locale-switch.feature:12` — `@cjm-6.2 @after-docker-up @expected-red` (no `@after-phase-N` token; the `@after-docker-up` semantic tag pre-dates the regex contract; plan explicitly says cjm-6.2 untouched).
- `tests/e2e-cjm/features/phase17-tls.feature:7,20,55` — 3 pre-existing offenders.
- `tests/e2e-cjm/features/traefik-host-split.feature:14,19` — 2 pre-existing offenders.

These are OUT OF SCOPE (SCOPE BOUNDARY rule) and logged here for visibility; the four feature files this plan owns are all clean.

### `pnpm typecheck` (for §14-04 re-confirm block)

- Exit code: **1**
- Error count: **1**
- Surviving error: `packages/litellm-client/src/index.ts:171` — `Promise<ResponseData<unknown>>` not assignable to `Promise<ResponseData<null>>` on `chatCompletionsStream`.
- **Drift since 14-04 baseline: -6** (baseline 7 → current 1). The six apps/api + apps/worker errors enumerated in §14-04 appear resolved by intervening work; recorded as a one-line `Drift since 14-04` addendum in the re-confirm block (per plan rollback section).

### Lefthook (pre-commit + commit-msg)

- `biome` — clean (1 informational warning, no errors; no fixes applied).
- `english` — clean (988 files scanned, English-only check passed).
- `commitlint` — clean (after shortening header from 129 to ≤ 100 chars; subject-case lower-cased on the verb `repoint`).

## SC9 Closure Note

The dated §14-04 re-confirm block satisfies ROADMAP Phase 18.1 SC9's "EITHER fixed in this phase OR re-confirmed deferred-with-justification (no silent rot)" path. The typecheck fix itself is deferred to Phase 18.2 OR milestone-close gate per CONTEXT.md `<deferred>`.

## Append-Only Confirmation

- §14-04 prior prose (lines 70–87) stays byte-identical (`### Pre-existing apps/api + apps/worker typecheck failures` through `vitest uses esbuild, not tsc.`). The re-confirm block was appended IMMEDIATELY AFTER the existing §14-04 "Likely fix" paragraph.
- `From Plan 12-04` and `From Plan 14-02` sections untouched.
- `From Plan 14-01 ... RESOLVED by Plan 14-03` section untouched.

## Forward References

The 4 Phase 19.x deferred-items entries are seeds for future `/gsd-discuss-phase 19.1` etc. Each names the exact missing pre-req file/symbol so the next planner has a turn-key starting point.

## Deviations

- **`pnpm test:lint-cjm-doc` script does not exist** in `package.json`. The plan's verify command referenced this script but it was never wired. I ran `pnpm vitest run tools/lint-cjm-doc.test.ts` directly — same effect, 33/33 pass.
- **6 pre-existing mode-3 CLI lint offenders** in unrelated feature files (phase17-tls, traefik-host-split, cjm-6.2 line). Confirmed pre-existing via `git stash` baseline. Treated as OUT OF SCOPE per executor SCOPE BOUNDARY rule (not caused by this task's changes). Not fixed; not logged as new deferred-items entries because the plan did not ask.
- **Typecheck drift from baseline 7 to current 1** — recorded with `Drift since 14-04: -6` addendum in the §14-04 re-confirm block per plan's rollback-section guidance. Did NOT silently update the "7" baseline.

## Self-Check: PASSED

- `tools/lint-cjm-doc.ts` regex extended: ✓
- `tools/lint-cjm-doc.test.ts` 33 tests pass: ✓
- 4 feature files repointed to 19.1/19.2/19.3/19.4: ✓
- `docs/customer-journeys.md` 4 anchor rows updated: ✓
- `.planning/deferred-items.md` gained `## From Phase 18.1` section AND `### §14-04 re-confirmed by Phase 18.1 (2026-05-15)` block: ✓
- Commit `42f390d` exists on `main`: ✓
- Lefthook clean (biome + english + commitlint): ✓
- No `--no-verify` used: ✓
