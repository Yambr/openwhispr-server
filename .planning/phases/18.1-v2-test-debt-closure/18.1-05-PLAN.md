---
phase: 18.1-v2-test-debt-closure
plan: 05
type: execute
wave: 5
depends_on: ["18.1-02"]
files_modified:
  - tools/lint-cjm-doc.ts
  - tools/lint-cjm-doc.test.ts
  - tests/e2e-cjm/features/password-reset.feature
  - tests/e2e-cjm/features/transcribe.feature
  - tests/e2e-cjm/features/signup-verify.feature
  - tests/e2e-cjm/features/locale-switch.feature
  - docs/customer-journeys.md
  - .planning/deferred-items.md
autonomous: true
requirements:
  - V2-TD-08   # Stale @expected-red REPOINT (4 tags)
  - V2-TD-09   # lint-cjm-doc regex extension for sub-phase tags
must_haves:
  truths:
    - "`tools/lint-cjm-doc.ts:162` regex extended from `/^@after-phase-\\d+$/` to `/^@after-phase-\\d+(\\.\\d+)?$/` (D-29; resolves RESEARCH §10 D-1 discrepancy via Option (c) — sub-phase numeric scheme `19.1`/`19.2`/`19.3`/`19.4` matching the `18.1` precedent we are inside)."
    - "A corresponding test in `tools/lint-cjm-doc.test.ts` (or sibling test file) covers `@after-phase-19.1` as VALID and `@after-phase-19a` / `@after-phase-` (malformed) as INVALID. Test lands in SAME commit as the regex extension (D-29)."
    - "`tests/e2e-cjm/features/password-reset.feature:6` — `@cjm-3.1` tag line re-points: `@after-phase-12` → `@after-phase-19.1` (D-11)."
    - "`tests/e2e-cjm/features/transcribe.feature:6` — `@cjm-4.1` tag line re-points: `@after-phase-12` → `@after-phase-19.2` (D-12)."
    - "`tests/e2e-cjm/features/signup-verify.feature:27` — `@cjm-1.4` tag line re-points: `@after-phase-15` → `@after-phase-19.3` (D-13)."
    - "`tests/e2e-cjm/features/locale-switch.feature:6` — `@cjm-6.1` tag line re-points: `@after-phase-15` → `@after-phase-19.4` (D-14)."
    - "`docs/customer-journeys.md` anchor rows at lines 65 (`@cjm-1.4`), 126 (`@cjm-3.1`), 160 (`@cjm-4.1`), 241 (`@cjm-6.1`) reflect new `@after-phase-N.M` ownership (D-15). `tools/lint-cjm-doc.ts` Mode-3 (doc⇄feature pairing) stays GREEN."
    - "`.planning/deferred-items.md` gains 4 new entries under section `## From Phase 18.1 — stale @expected-red REPOINT` — one per tag, each listing: missing pre-req file/symbol (sendResetPassword hook / mock-litellm transcribe overlay / Better Auth i18n interception / locale-e2e Playwright wiring), new owner phase (19.1/19.2/19.3/19.4), and the affected feature-tag. Append-only (do NOT rewrite the existing 12-04 or 14-04 sections; PATTERNS §6 risk)."
    - "All 8 file edits land in ONE atomic mass-edit commit (Claude's Discretion bullet 3 in CONTEXT). Commit shape: `docs(18.1-05): REPOINT 4 stale @expected-red tags to 19.1/19.2/19.3/19.4 + lint regex extension`."
    - "ZERO `--no-verify`; `pnpm test:lint-cjm-doc` GREEN after commit; lefthook `english` + `commitlint` + `biome` (for `lint-cjm-doc.ts` only — gherkin/markdown not in glob) clean."
  artifacts:
    - path: "tools/lint-cjm-doc.ts"
      provides: "Mode-3 regex extended to accept sub-phase tag suffix `N.M`."
      contains: "@after-phase-\\\\d+(\\\\.\\\\d+)?"
    - path: "tools/lint-cjm-doc.test.ts"
      provides: "New test cases for sub-phase tag validation."
      contains: "@after-phase-19.1"
    - path: "tests/e2e-cjm/features/password-reset.feature"
      provides: "@cjm-3.1 retagged to @after-phase-19.1."
      contains: "@after-phase-19.1"
    - path: "tests/e2e-cjm/features/transcribe.feature"
      provides: "@cjm-4.1 retagged to @after-phase-19.2."
      contains: "@after-phase-19.2"
    - path: "tests/e2e-cjm/features/signup-verify.feature"
      provides: "@cjm-1.4 retagged to @after-phase-19.3."
      contains: "@after-phase-19.3"
    - path: "tests/e2e-cjm/features/locale-switch.feature"
      provides: "@cjm-6.1 retagged to @after-phase-19.4."
      contains: "@after-phase-19.4"
    - path: "docs/customer-journeys.md"
      provides: "Anchor rows for the 4 CJMs reflect new owner-phase numbering."
      contains: "@after-phase-19."
    - path: ".planning/deferred-items.md"
      provides: "Append-only entries documenting the 4 missing pre-reqs + new owner phases."
      contains: "## From Phase 18.1 — stale @expected-red REPOINT"
  key_links:
    - from: "tools/lint-cjm-doc.ts (regex L162)"
      to: "tests/e2e-cjm/features/*.feature (4 retagged tags)"
      via: "Mode-3 doc⇄feature pairing rule; extended regex now matches `@after-phase-19.1` etc. — without this regex change, the REPOINT commit would FAIL Mode-3 lint (RESEARCH §10 D-1 BLOCKER)."
      pattern: "@after-phase-\\\\d+(\\\\.\\\\d+)?"
    - from: ".planning/deferred-items.md"
      to: "future Phase 19.1/19.2/19.3/19.4 plans"
      via: "Each entry names the missing pre-req file/symbol — future planner consumes these as the seed inputs for `/gsd-discuss-phase 19.1` etc."
      pattern: "Sub-phase 19."
---

<objective>
Per D-10..D-16, D-29, and PATTERNS §5: REPOINT the 4 stale `@expected-red`
Gherkin scenarios whose `@after-phase-12/15` ownership was speculative
scheduling. NO downstream production code lands in 18.1 — each missing
pre-req is recorded in `.planning/deferred-items.md` for a future
Phase 19.1/19.2/19.3/19.4 to deliver.

The BLOCKER (RESEARCH §10 D-1): `tools/lint-cjm-doc.ts:162` enforces
`/^@after-phase-\d+$/` (numeric only). New sub-phase tags `19.1`/`19.2`
etc. would FAIL Mode-3 unless the regex is extended FIRST. D-29 mandates
the regex extension + its test land in the SAME commit as the REPOINT
mass-edit so `pnpm test:lint-cjm-doc` never goes RED in the commit graph.

Per Discretion bullet 3 (CONTEXT.md): ONE atomic mass-edit commit covering
all 8 files (4 features + customer-journeys.md + deferred-items.md +
lint-cjm-doc.ts + its test). No logic; mechanical text edit.

Wave 5 — runs AFTER 18.1-02 (security fix) per D-27 execution order.
File-disjoint from 18.1-01/02/03/04, but D-27 sequences it. Output: 1
commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md
@CLAUDE.md

@tools/lint-cjm-doc.ts
@tools/lint-cjm-doc.test.ts
@tests/e2e-cjm/features/password-reset.feature
@tests/e2e-cjm/features/transcribe.feature
@tests/e2e-cjm/features/signup-verify.feature
@tests/e2e-cjm/features/locale-switch.feature
@tests/e2e-cjm/features/sso/keycloak-oidc.feature
@docs/customer-journeys.md
@.planning/deferred-items.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1-05-01: Atomic mass-edit — regex extension + 4 REPOINTs + docs + deferred-items</name>
  <files>
    - tools/lint-cjm-doc.ts
    - tools/lint-cjm-doc.test.ts
    - tests/e2e-cjm/features/password-reset.feature
    - tests/e2e-cjm/features/transcribe.feature
    - tests/e2e-cjm/features/signup-verify.feature
    - tests/e2e-cjm/features/locale-switch.feature
    - docs/customer-journeys.md
    - .planning/deferred-items.md
  </files>
  <action>
    Per D-10..D-16, D-29, and PATTERNS §5/§6. Mechanical mass-edit; one commit.

    **Edit 1 — `tools/lint-cjm-doc.ts:162`:**

    Locate the regex (likely a `const AFTER_PHASE_TAG_RE = /^@after-phase-\d+$/` or inline `match(/.../)` call). Replace with:

    ```ts
    /^@after-phase-\d+(\.\d+)?$/
    ```

    This admits both `@after-phase-19` and `@after-phase-19.1`. Per RESEARCH §10 D-1 Option (c) — matches the `18.1` sub-phase numbering precedent we are inside.

    **Edit 2 — `tools/lint-cjm-doc.test.ts`:**

    Add (or extend an existing test suite) with cases:

    - VALID: `@after-phase-19` — passes (back-compat).
    - VALID: `@after-phase-19.1`, `@after-phase-19.2`, `@after-phase-19.3`, `@after-phase-19.4` — pass.
    - INVALID: `@after-phase-19a` — fails (RESEARCH §10 D-1 Option (a) rejected).
    - INVALID: `@after-phase-` — fails (no digits).
    - INVALID: `@after-phase-19.` — fails (trailing dot).

    Use the existing test framework convention in the file (likely Vitest). If a `lint-cjm-doc.test.ts` does not exist, find the closest sibling test (e.g. `tools/lint-cjm-doc.spec.ts` or wherever Mode-3 unit tests live) — grep `pnpm test:lint-cjm-doc` script in `package.json` to identify.

    **Edits 3-6 — feature files (REPOINT tag-lines):**

    Each is a single-line text edit on the tag-line of the feature/scenario:

    - `tests/e2e-cjm/features/password-reset.feature:6` — `@cjm-3.1 @expected-red @after-phase-12` → `@cjm-3.1 @expected-red @after-phase-19.1`
    - `tests/e2e-cjm/features/transcribe.feature:6` — `@cjm-4.1 @expected-red @after-phase-12` → `@cjm-4.1 @expected-red @after-phase-19.2`
    - `tests/e2e-cjm/features/signup-verify.feature:27` — `@cjm-1.4 @expected-red @after-phase-15` → `@cjm-1.4 @expected-red @after-phase-19.3`
    - `tests/e2e-cjm/features/locale-switch.feature:6` — `@cjm-6.1 @expected-red @after-phase-15` → `@cjm-6.1 @expected-red @after-phase-19.4`

    Preserve any other tags on the same tag-line (e.g. `@auth`, `@a11y` — check each before editing). Only the `@after-phase-N` token swaps.

    **Edit 7 — `docs/customer-journeys.md`:**

    Update the anchor rows for each tag (lines per RESEARCH §3):
    - L65 `@cjm-1.4` row — change `@after-phase-15` text/badge to `@after-phase-19.3`
    - L126 `@cjm-3.1` row — change `@after-phase-12` to `@after-phase-19.1`
    - L160 `@cjm-4.1` row — change `@after-phase-12` to `@after-phase-19.2`
    - L241 `@cjm-6.1` row — change `@after-phase-15` to `@after-phase-19.4`

    Exact column/formatting follows the existing row template — do NOT reformat any other row. If the table uses a "Status" column with prose like "ships in Phase 12", update that prose to match the new owner phase.

    **Edit 8 — `.planning/deferred-items.md` (APPEND-ONLY):**

    Append a new section at the END of the file (preserve all existing content — PATTERNS §6 risk):

    ```markdown
    ## From Phase 18.1 — stale @expected-red REPOINT

    ### @cjm-3.1 password-reset — @after-phase-19.1 reset-mail wiring

    **Discovered:** 2026-05-15 during Phase 18.1 v2 test-debt closure.

    **Symptom:** `tests/e2e-cjm/features/password-reset.feature:6` carried `@after-phase-12` (speculative). Actual missing pre-req: `apps/api/src/auth.ts` lacks a `sendResetPassword` hook (Better Auth `forget-password` endpoint runs but never enqueues an email; worker `password_reset` template exists but is uncalled).

    **Why deferred:** Phase 12 closed without wiring the hook; not a Phase 12 bug, but a future-phase deliverable. REPOINT to `@after-phase-19.1`.

    **Likely fix:** Wire `sendResetPassword` in `apps/api/src/auth.ts` Better Auth config; enqueue via worker `password_reset` template; flip `@cjm-3.1` GREEN. Sub-phase 19.1 = reset-mail.

    ### @cjm-4.1 transcribe — @after-phase-19.2 stt-fixture

    **Discovered:** 2026-05-15 during Phase 18.1.

    **Symptom:** `tests/e2e-cjm/features/transcribe.feature:6` carried `@after-phase-12`. Actual missing pre-req: `apps/api/src/routes/transcribe.ts` proxies to LiteLLM (`provider=groq, model=whisper-large-v3`) and throws `MissingProviderKeyError → 503` when `GROQ_API_KEY` empty. CJM compose stack defaults to empty.

    **Why deferred:** Per `feedback_loadtest_cost_discipline.md` (paid cloud gets smokes only — no real GROQ key in CI). REPOINT to `@after-phase-19.2`; bias toward a mock-litellm transcribe overlay (canned `{text: ""}` response).

    **Likely fix:** Add `compose/docker-compose.cjm-mock-stt.yml` overlay that wires `mock-litellm` to respond canned `{text: ""}` for whisper requests; flip `@cjm-4.1` GREEN. Sub-phase 19.2 = stt-fixture.

    ### @cjm-1.4 signup-verify (RU locale) — @after-phase-19.3 ba-i18n

    **Discovered:** 2026-05-15 during Phase 18.1.

    **Symptom:** `tests/e2e-cjm/features/signup-verify.feature:27` carried `@after-phase-15`. Actual missing pre-req: `apps/api/src/routes/better-auth-handler.ts` is a black-box `webHandler` pass-through; Better Auth 4xx error envelope returns ENGLISH verbatim; `req.t(...)` never invoked.

    **Why deferred:** Phase 15 surface was FSL relicense + relocation — not i18n. REPOINT to `@after-phase-19.3`.

    **Likely fix:** Wrap Better Auth 4xx responses with a `req.t("better-auth:errors.<code>")` interception layer; flip `@cjm-1.4` GREEN. Sub-phase 19.3 = ba-i18n.

    ### @cjm-6.1 locale-switch — @after-phase-19.4 locale-e2e

    **Discovered:** 2026-05-15 during Phase 18.1.

    **Symptom:** `tests/e2e-cjm/features/locale-switch.feature:6` carried `@after-phase-15`. Actual missing pre-req: `tests/e2e-cjm/steps/locale.steps.ts:26-42` — all three step bodies `throw new Error("locale UI ships in Phase 15 — @cjm-6.1 stays @expected-red")`. Web-side `language-switcher.tsx` exists; cjm harness uses undici/fetch (no Playwright) and web container is not booted.

    **Why deferred:** Requires booting the web container in cjm harness AND swapping out the cucumber HTTP-only step pattern for Playwright. Non-trivial infra. REPOINT to `@after-phase-19.4`.

    **Likely fix:** Add web service to `compose/docker-compose.cjm.yml`; replace `throw` stubs in `tests/e2e-cjm/steps/locale.steps.ts:26-42` with real Playwright step-defs driving `LanguageSwitcher`; flip `@cjm-6.1` GREEN. Sub-phase 19.4 = locale-e2e.
    ```

    **Atomic commit message:**

    ```
    docs(18.1-05): REPOINT 4 stale @expected-red tags to 19.1/19.2/19.3/19.4 + lint regex extension

    Closes V2-TD-08 + V2-TD-09 (stale @after-phase-12/15 speculative scheduling):

    - tools/lint-cjm-doc.ts: AFTER_PHASE_TAG_RE extended from
      /^@after-phase-\d+$/ to /^@after-phase-\d+(\.\d+)?$/ to admit
      sub-phase numbering (matches 18.1 precedent). Per D-29, this
      regex extension lands in SAME commit as the tag swaps so
      pnpm test:lint-cjm-doc never goes RED in the commit graph.
    - tools/lint-cjm-doc.test.ts: new cases — VALID 19/19.1; INVALID
      19a/19./trailing-dot.
    - tests/e2e-cjm/features/{password-reset,transcribe,signup-verify,
      locale-switch}.feature: tag-line REPOINTs to 19.1/19.2/19.3/19.4.
    - docs/customer-journeys.md: anchor rows for @cjm-1.4/3.1/4.1/6.1
      synced to new owner-phase numbering.
    - .planning/deferred-items.md: append-only section "From Phase 18.1
      — stale @expected-red REPOINT" with 4 entries naming missing
      pre-reqs + new owner phases (19.1/19.2/19.3/19.4).

    No production code changes. Mechanical mass-edit per Discretion
    bullet 3 (CONTEXT.md). Mode-3 lint stays GREEN.

    Refs V2-TD-08, V2-TD-09, D-10..D-16, D-29.
    ```

    WHY (pitfalls):
    - Pitfall #4 (RESEARCH §8): Mode-3 linter rejects suffixed tags `19a`/`19b`. Option (c) sub-phase numeric scheme + regex extension is the resolution; Option (a) suffix would require a regex that also admits arbitrary suffixes — looser invariant.
    - Pitfall #5: extension + REPOINT MUST be same commit; if regex lands first then REPOINT lands second, intermediate HEAD would still pass lint (regex is permissive of old shape). If REPOINT lands first, intermediate HEAD fails lint. Bundle = no intermediate red state.
    - PATTERNS §6 risk: append-only on `deferred-items.md` — do NOT rewrite existing 12-04 or 14-04 sections.
    - PATTERNS §5 analog: `tests/e2e-cjm/features/sso/keycloak-oidc.feature:20` uses two forward-pointing tags (`@after-phase-19 @after-keycloak-up`). 18.1 adopts the same single-or-double tag shape — Phase 19.x tag is sufficient here (no second infra-precondition tag needed).

    Lefthook surface: `biome` (TS file), `english`, `commitlint`. `colocated-tests` does NOT fire (test is at `tools/`). No `--no-verify` (D-26).
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm test:lint-cjm-doc 2>&1 | tail -10 && grep -c "@after-phase-19\." tests/e2e-cjm/features/password-reset.feature tests/e2e-cjm/features/transcribe.feature tests/e2e-cjm/features/signup-verify.feature tests/e2e-cjm/features/locale-switch.feature && grep -c "## From Phase 18.1" .planning/deferred-items.md</automated>
  </verify>
  <done>Regex extended; test cases added; 4 feature tags REPOINTed; customer-journeys.md anchor rows synced; deferred-items.md appended with 4 entries; `pnpm test:lint-cjm-doc` GREEN; ONE atomic commit; lefthook clean.</done>
</task>

</tasks>

<test_strategy>
- Unit: `pnpm test:lint-cjm-doc` (or the underlying Vitest suite for `tools/lint-cjm-doc.ts`) — new regex test cases pass; Mode-3 doc⇄feature pairing still GREEN for all 20 `@cjm-N.M` anchors.
- Integration: none — text edits only.
- Coverage: diff on `tools/lint-cjm-doc.ts` is 1 regex char-class change; tests cover both new admit (`19.1`) and reject (`19a`) branches → 100% line/branch on the diff. ≥90/90/90/90 satisfied.
- Regression: `pnpm test` aggregate does NOT change failure count (REPOINT is forward-pointing — `@expected-red` scenarios are skipped from execution; tag-text edit doesn't flip execution status).
</test_strategy>

<verification>
```bash
cd /Users/dev/openwhispr-server

# Regex extension live
grep -n "@after-phase-" tools/lint-cjm-doc.ts | head -5
# Should show the extended regex.

# Mode-3 lint GREEN
pnpm test:lint-cjm-doc

# Tag REPOINTs landed
grep -nE "@after-phase-19\.(1|2|3|4)" tests/e2e-cjm/features/*.feature

# Customer journeys updated
grep -c "@after-phase-19\." docs/customer-journeys.md  # expect ≥ 4

# Deferred items appended
tail -50 .planning/deferred-items.md | grep "Sub-phase 19\."
```
</verification>

<success_criteria>
1. `tools/lint-cjm-doc.ts:162` regex admits `@after-phase-N.M` (sub-phase) AND `@after-phase-N` (back-compat).
2. Test cases for new regex pass (`@after-phase-19.1` valid; `@after-phase-19a` invalid).
3. All 4 feature files REPOINTed to `19.1`/`19.2`/`19.3`/`19.4` per D-11..D-14.
4. `docs/customer-journeys.md` anchor rows for the 4 CJMs match the new owner-phase.
5. `.planning/deferred-items.md` appended with 4 new entries; existing 12-04 / 14-04 sections byte-identical.
6. `pnpm test:lint-cjm-doc` exits 0.
7. NO production code changed.
8. ONE atomic `docs(18.1-05)` commit; ZERO `--no-verify`; lefthook clean.
</success_criteria>

<rollback>
If `pnpm test:lint-cjm-doc` goes RED post-commit:
- Diagnose: is it the new regex (test mistake) or Mode-3 pairing (feature/doc mismatch)?
- If regex: revert + re-author the regex test more carefully.
- If Mode-3 pairing: a feature was REPOINTed but the doc row didn't sync (or vice versa) — fix in a follow-up commit before proceeding to 18.1-06.

Plan 18.1-05 has only `18.1-02` as a DEPENDS_ON (execution order); revert affects only this plan.
</rollback>

<output>
After commit, create `.planning/phases/18.1-v2-test-debt-closure/18.1-05-SUMMARY.md` with:
- Commit SHA + diff stats (8 files).
- `pnpm test:lint-cjm-doc` output (expect PASS).
- Confirmation `deferred-items.md` is APPEND-ONLY (no existing-section rewrites).
- Lefthook result: `clean`.
- Forward-reference: Phase 19.1/19.2/19.3/19.4 deferred as future-phase deliverables; the deferred-items.md entries are the seeds for future `/gsd-discuss-phase 19.1` etc.
</output>
</content>
</invoke>