---
phase: 18.1-v2-test-debt-closure
plan: 02
type: execute
wave: 4
depends_on: ["18.1-01", "18.1-03", "18.1-04"]
files_modified:
  - apps/api/src/plugins/rate-limit.ts
  - apps/api/src/routes/tokens/assemblyai.ts
  - apps/api/src/routes/tokens/deepgram.ts
  - apps/api/src/routes/tokens/openai-realtime.ts
  - apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts
autonomous: true
requirements:
  - V2-TD-06   # Test-sweep failure F6 (T3 anon IP bucket creation — security)
  - V2-SEC-01  # Anonymous DoS-shield invariant on auth-OPEN routes (Phase 6 D-RL1)
must_haves:
  truths:
    - "`apps/api/src/plugins/rate-limit.ts:181-217` `onRequest` hook, after the existing `rateLimit === false` early-return, adds a second early-return guard: `if (routeConfig?.authRequired === true && !req.user) return;`. The narrowing cast at line 181-184 extends the `routeOptions.config` type union with `authRequired?: boolean`."
    - "The three authed-token routes carry `authRequired: true` inside their existing `config` object (NOT a new `config` key — extends the same object that already declares `rateLimit`): `apps/api/src/routes/tokens/assemblyai.ts:43`, `apps/api/src/routes/tokens/deepgram.ts` (analogous line), `apps/api/src/routes/tokens/openai-realtime.ts` (analogous line)."
    - "Auth-OPEN routes (`/api/auth/signin`, `/api/auth/signup`, `/api/auth/forgot-password`) leave `config.authRequired` UNSET → IP-tier hook still fires pre-auth on them, preserving the Phase 6 D-RL1 DoS shield contract."
    - "T3 (`apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts:248` — `unauthenticated requests 401 BEFORE the rate-limit hook fires`) is GREEN; the assertion `expect(keysAfter.sort()).toEqual(keysBefore.sort())` holds (NO `owrl:ip:127.0.0.1` bucket appears after 35 anonymous requests to an authed route)."
    - "T1, T2, T4, T5, T6 in the same integration file remain GREEN (regression-safe — they are all authed and unaffected by the skip-tag)."
    - "Stale comments at `apps/api/src/routes/tokens/assemblyai.ts:23-26` and `apps/api/src/plugins/rate-limit.ts:27-30` are synced to acknowledge the layered model (IP-tier shields anon, per-user fires post-auth, authRequired skips IP for authed routes). Bundled in the SAME commit as the prod fix (D-08, Phase 16-fix WR-01 precedent)."
    - "TWO-commit TDD shape (D-28): the existing T3 RED stands as proof (pre-existing failure on `main`); GREEN commit lands prod fix + comment sync + route tags in one atomic. No `--no-verify`."
    - "ZERO modifications to `apps/api/src/auth.ts`, `apps/api/src/index.ts` plugin registration order, or `apps/api/src/config/rate-limits.ts` (the `ip-only`/`user-and-ip` taxonomy refactor is D-09 DEFERRED to v3)."
  artifacts:
    - path: "apps/api/src/plugins/rate-limit.ts"
      provides: "Tag-driven IP-tier skip when `routeOptions.config.authRequired === true && !req.user`; layered comment sync."
      contains: "authRequired === true && !req.user"
    - path: "apps/api/src/routes/tokens/assemblyai.ts"
      provides: "Route config gains `authRequired: true` flag; T-04-04 stale header synced."
      contains: "authRequired: true"
    - path: "apps/api/src/routes/tokens/deepgram.ts"
      provides: "Route config gains `authRequired: true` flag."
      contains: "authRequired: true"
    - path: "apps/api/src/routes/tokens/openai-realtime.ts"
      provides: "Route config gains `authRequired: true` flag."
      contains: "authRequired: true"
  key_links:
    - from: "apps/api/src/plugins/rate-limit.ts:181-217 (IP-tier onRequest)"
      to: "apps/api/src/routes/tokens/{assemblyai,deepgram,openai-realtime}.ts"
      via: "`routeOptions.config.authRequired` boolean tag; Fastify exposes via `req.routeOptions.config` cast through a defensive type narrowing (analog at same file lines 181-185 for `rateLimit === false`)."
      pattern: "authRequired === true"
    - from: "apps/api/src/plugins/rate-limit.ts onRequest hook"
      to: "apps/api/src/plugins/dual-auth.ts (or wherever req.user is populated)"
      via: "Plugin registration order in `apps/api/src/index.ts` — auth plugin MUST register BEFORE rate-limit plugin so `req.user` is populated when the IP-tier `onRequest` fires (A2 confirmed this holds)."
      pattern: "req.user"
---

<objective>
Close F6 (security): unauthenticated requests to authed-token routes currently
populate `owrl:ip:127.0.0.1` buckets BEFORE the auth pre-handler returns 401 —
opening an anonymous DoS vector against the rate-limit storage. Fix per D-06
Option B: tag affected routes with `config.authRequired: true` and have the
IP-tier `onRequest` hook early-return when that tag is set AND `req.user` is
unpopulated.

The fix preserves the Phase 6 D-RL1 DoS shield for auth-OPEN routes (signin,
signup, forgot-password remain shielded by IP-tier pre-auth because they
deliberately leave `authRequired` UNSET).

This is the SECURITY fix in 18.1 (the only F* with security implications).
D-27 execution order places it AFTER paths/diarization/AccountClient — so it
runs in Wave 4, after the three independent fixes of Waves 1-3. Two-tier
limiter architectural rewrite (D-09 Option C) is DEFERRED to v3.

Output: 1 atomic commit landing all 5 file edits (rate-limit.ts + 3 route
files + 1 test file if any test-side adjustment is needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md
@CLAUDE.md

@apps/api/src/plugins/rate-limit.ts
@apps/api/src/routes/tokens/assemblyai.ts
@apps/api/src/routes/tokens/deepgram.ts
@apps/api/src/routes/tokens/openai-realtime.ts
@apps/api/src/routes/probes.ts
@apps/api/src/index.ts
@apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1-02-01: Verify T3 RED + sweep audit (no commit)</name>
  <files>
    - apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts
  </files>
  <action>
    Per D-09 + RESEARCH §2 F6.

    1. Confirm T3 is RED on `main` HEAD via `pnpm --filter @openwhispr/api test rate-limit-isolation.integration -t "unauthenticated requests 401 BEFORE the rate-limit hook fires"`. Expected: assertion fails because `owrl:ip:127.0.0.1` appears in `keysAfter`.

    2. Grep `apps/api/tests/**/rate-limit*` for IP-ceiling tests pointed at AUTHED routes. Per D-09: T1/T2/T4/T5/T6 in this file are all authed → no test fixture changes needed there. If the sweep surfaces NEW IP-ceiling tests on authed routes elsewhere, those tests must be retargeted to use an auth-OPEN route fixture (signin) before the prod fix lands — they would falsely regress otherwise.

    Expected result: only T3 is affected. NO files modified in this task. RED proof recorded for the GREEN commit body.

    WHY: D-28 requires explicit RED proof; for pre-existing failures the RED stands as-is. This audit task confirms the GREEN fix won't break sibling tests (regression-safety).
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts -t "T3" --reporter=verbose 2>&1 | grep -E "(FAIL|expect|keysAfter)" | head -10</automated>
  </verify>
  <done>T3 confirmed RED; no other IP-ceiling-on-authed-route tests surface in the sweep; ready for GREEN.</done>
</task>

<task type="auto">
  <name>Task 18.1-02-02: GREEN — rate-limit.ts skip-hook + 3 route tags + stale comments</name>
  <files>
    - apps/api/src/plugins/rate-limit.ts
    - apps/api/src/routes/tokens/assemblyai.ts
    - apps/api/src/routes/tokens/deepgram.ts
    - apps/api/src/routes/tokens/openai-realtime.ts
  </files>
  <action>
    Per D-06..D-08 + RESEARCH §1 rows D-06/D-07/D-08 + PATTERNS §2.

    **Edit 1 — `apps/api/src/plugins/rate-limit.ts:181-217` (IP-tier onRequest hook):**

    The current hook narrows `routeOptions.config` via:

    ```ts
    const routeConfig = (req as FastifyRequest & {
      routeOptions?: { config?: { rateLimit?: unknown } };
    }).routeOptions?.config;
    if (routeConfig?.rateLimit === false) return;
    ```

    Extend the union to include `authRequired?: boolean` and add a SECOND early-return immediately after the existing `rateLimit === false` check:

    ```ts
    const routeConfig = (req as FastifyRequest & {
      routeOptions?: { config?: { rateLimit?: unknown; authRequired?: boolean } };
    }).routeOptions?.config;
    if (routeConfig?.rateLimit === false) return;
    if (routeConfig?.authRequired === true && !req.user) return;
    ```

    Order matters: `rateLimit === false` (opt-out for probes etc.) MUST come first; `authRequired` skip is the layered DoS-shield carve-out for authed routes.

    **Edit 2 — `apps/api/src/plugins/rate-limit.ts:27-30` (stale comment block):**

    Sync the comment to acknowledge the layered model. Suggested replacement (keep the "Source of truth:" header line untouched to avoid `phase-tag-comments` lefthook regex hits — PATTERNS pitfall #7):

    The body should explain: (a) IP-tier fires pre-auth on auth-OPEN routes (DoS shield), (b) per-user tier fires post-auth, (c) routes tagged `config.authRequired: true` skip IP-tier so anon traffic 401s without populating `owrl:ip:*`.

    **Edit 3 — `apps/api/src/routes/tokens/assemblyai.ts:43` (config block):**

    The route already declares `config: { rateLimit: { max, timeWindow, keyGenerator } }`. EXTEND the same object — do NOT introduce a second `config` key:

    ```ts
    config: {
      authRequired: true,
      rateLimit: { max, timeWindow, keyGenerator },
    }
    ```

    Analog at `apps/api/src/routes/probes.ts:85` (`config: { auth: false, rateLimit: false }`) confirms the same-object pattern.

    Also sync the stale T-04-04 comment header at `assemblyai.ts:23-26` to note the `authRequired` flag. Touch BODY only — keep any `// Phase 04 / T-04-04 …` source-of-truth line untouched (RESEARCH §6 phase-tag-comments hook risk).

    **Edit 4 — `apps/api/src/routes/tokens/deepgram.ts`:**

    Grep for the existing `config:` block (same shape as assemblyai). Add `authRequired: true` to it.

    **Edit 5 — `apps/api/src/routes/tokens/openai-realtime.ts`:**

    Same as deepgram. Grep + extend.

    **Atomic commit message:**

    ```
    fix(18.1-02): green — authRequired tag skips IP-tier for authed-only routes (T3, security)

    Closes V2-TD-06 / V2-SEC-01 (anonymous DoS vector via rate-limit pre-auth bucket creation):

    - apps/api/src/plugins/rate-limit.ts: extend IP-tier onRequest narrowing
      with `authRequired?: boolean`; add second early-return
      `if (routeConfig?.authRequired === true && !req.user) return;` AFTER
      the existing `rateLimit === false` check.
    - apps/api/src/routes/tokens/{assemblyai,deepgram,openai-realtime}.ts:
      extend existing `config` object with `authRequired: true`.
    - apps/api/src/plugins/rate-limit.ts:27-30 + apps/api/src/routes/tokens/
      assemblyai.ts:23-26: sync stale comments to layered-model contract
      (per D-08 Phase 16-fix WR-01 precedent — comment sync in same commit
      as code change).
    - Auth-OPEN routes (/api/auth/signin, /signup, /forgot-password)
      remain UNTAGGED → IP-tier fires pre-auth (Phase 6 D-RL1 DoS shield
      preserved).

    RED proof: pre-existing T3 failure on main HEAD~1 — verified in
    18.1-02-01 audit. T1/T2/T4/T5/T6 unaffected (all authed; either
    tagged authRequired:true so they hit the skip AFTER auth populates
    req.user, or unaffected by the carve-out).

    Two-tier limiter architectural rewrite (D-09 Option C —
    apps/api/src/config/rate-limits.ts ip-only / user-and-ip taxonomy)
    DEFERRED to v3. This is a closure-phase carve-out, not a redesign.

    Refs V2-TD-06, V2-SEC-01, Phase 04 D-RL1, Phase 06 D-RL1.
    ```

    WHY (pitfalls — encode each as inline reasoning):
    - Pitfall #1 (RESEARCH §8): skip ONLY when `authRequired === true && !req.user`. Inverting (skip on auth-OPEN routes) re-opens the DoS vector. The two early-returns layer correctly: opt-out routes skip first; authed-after-auth routes skip second; everything else (auth-OPEN signin/signup) falls through to the IP increment.
    - Pitfall #5: comment sync at `assemblyai.ts:23-26` + `rate-limit.ts:27-30` is the SECOND-ORDER fix easy to forget; bundle in same commit (D-08).
    - Pitfall #7: don't reformat the comment header — only edit the body — to avoid `phase-tag-comments` lefthook hits.

    No `--no-verify`. If lefthook fires, HALT and escalate (D-26).
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>T3 GREEN; T1/T2/T4/T5/T6 still GREEN; rate-limit.ts narrowing extended; 3 route configs carry `authRequired: true`; 2 stale comments synced; ONE atomic commit; lefthook clean.</done>
</task>

</tasks>

<test_strategy>
- Unit: T3 is an integration test using a Valkey testcontainer (already wired by T1-T6). Run via `pnpm --filter @openwhispr/api test rate-limit-isolation.integration`. All 6 tests (T1-T6) must be GREEN post-fix.
- Manual sanity (optional): boot dev stack, `curl -X POST http://localhost:3000/api/tokens/assemblyai` 5x (no Authorization header) — assert no `owrl:ip:127.0.0.1` key appears in Valkey (`docker exec valkey valkey-cli KEYS 'owrl:ip:*'`).
- Coverage: diff covers `rate-limit.ts` (added 1 line + extended type union 1 line) and 3 token routes (added 1 line each). Existing tests in `apps/api/tests/unit/plugins/rate-limit*` and the integration file cover the new branch. New/modified prod LOC ≈ 4 lines; coverage delta on the changed lines is 100% via T3 (skip branch when authRequired+no-user) + T1/T2 (no-skip branch when authRequired+user-present).
- Regression: `pnpm --filter @openwhispr/api test` aggregate — confirm no new failures elsewhere.
</test_strategy>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Fastify ingress | Unauthenticated HTTP entering the IP-tier rate-limit hook |
| Fastify hook chain → `req.user` resolution | `dualAuthHook` populates `req.user`; IP-tier hook reads it |
| route config tag → IP-tier skip | `routeOptions.config.authRequired` controls the carve-out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-02-01 | Denial of Service | `apps/api/src/plugins/rate-limit.ts` IP-tier hook | mitigate | Auth-OPEN routes (`signin`/`signup`/`forgot-password`) retain pre-auth IP ceiling; `authRequired: true` ONLY skips the IP tier when `req.user` is unpopulated AND the route requires auth (so the 401 path never populates `owrl:ip:*` — closes the anonymous-storage-exhaustion vector that T3 exposed). |
| T-18.1-02-02 | Elevation of Privilege | `apps/api/src/routes/tokens/*.ts` route configs | accept | Adding `authRequired: true` is a tag, not an auth check — the actual auth gate remains the existing `dualAuthHook` pre-handler. No privilege boundary moved. |
| T-18.1-02-03 | Tampering | `routeOptions.config` cast in rate-limit hook | mitigate | Cast is defensive (`as FastifyRequest & { routeOptions?: { config?: …} }`) — same pattern as the existing `rateLimit === false` narrowing at L181-185; no untrusted input flows through `routeOptions.config` (it is set at route-registration time by trusted code). |
</threat_model>

<verification>
```bash
cd /Users/dev/openwhispr-server
# T3 GREEN proof
pnpm --filter @openwhispr/api test tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts

# Static asserts
grep -n "authRequired === true && !req.user" apps/api/src/plugins/rate-limit.ts
grep -n "authRequired: true" apps/api/src/routes/tokens/assemblyai.ts apps/api/src/routes/tokens/deepgram.ts apps/api/src/routes/tokens/openai-realtime.ts
```
</verification>

<success_criteria>
1. T3 GREEN; T1/T2/T4/T5/T6 still GREEN; no new test failures in `apps/api`.
2. `apps/api/src/plugins/rate-limit.ts` IP-tier hook has TWO early-returns; `authRequired` skip is SECOND (after `rateLimit === false`).
3. The 3 token routes (assemblyai, deepgram, openai-realtime) all carry `authRequired: true` inside their existing `config` object.
4. Auth-OPEN routes (`/api/auth/signin`, `/signup`, `/forgot-password`) UNTAGGED — verified via grep.
5. Stale comments at `rate-limit.ts:27-30` + `assemblyai.ts:23-26` reflect the layered model; comment headers/source-of-truth lines untouched.
6. NO change to `apps/api/src/auth.ts`, `apps/api/src/index.ts` plugin order, or `apps/api/src/config/rate-limits.ts`.
7. ONE atomic GREEN commit (`fix(18.1-02): green — …`); ZERO `--no-verify`; lefthook clean.
8. RED proof = pre-existing T3 failure on `main` (documented in commit body).
</success_criteria>

<rollback>
If T1/T2/T4/T5/T6 regress (e.g. authed-test asserts an IP counter increments — possible if a sibling test was actually depending on the pre-fix behaviour despite D-09 audit conclusions):
- `git revert <GREEN-SHA>`.
- Re-audit the regressed test — if it was a false-positive assertion (testing the broken behaviour), update its expectation as part of a NEW GREEN commit.
- If the regression is a real semantic break (skip leaks to auth-OPEN routes), the `authRequired` carve-out is too broad — narrow further OR fall back to D-09 Option A (reorder hooks) as a stopgap.

Plan 18.1-02 has hard dependencies on 18.1-01/03/04 only via execution order (D-27) — rolling back 02 does not regress 01/03/04 outputs.
</rollback>

<output>
After GREEN lands, create `.planning/phases/18.1-v2-test-debt-closure/18.1-02-SUMMARY.md` with:
- GREEN commit SHA + diff stats (5 files: rate-limit.ts + 3 route files + comment sync inlined).
- T3 + T1/T2/T4/T5/T6 pre/post status (RED→GREEN for T3; GREEN→GREEN for siblings).
- Confirmation auth-OPEN routes are untagged (grep output).
- Lefthook result: `clean` expected.
- Forward-reference to v3 two-tier limiter rewrite (D-09 Option C) as the proper architectural fix this carve-out defers.
</output>
</content>
</invoke>