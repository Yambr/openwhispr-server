# Phase 18.1: v2 test-debt closure - Context

**Gathered:** 2026-05-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the test-debt and audit-artefact debts surfaced by `/Users/nick/openwhispr-server/.planning/V2-MILESTONE-REVIEW.md` and the full `pnpm test` sweep run on 2026-05-15 (296 files, 2669 tests: **21 failed / 2293 passed / 355 skipped** on `main`).

**In scope:**
- Make `pnpm test` exit 0 across all workspaces (fix 7 unique failures classified in V2-MILESTONE-REVIEW).
- Re-tag (REPOINT only — no implementation) the 4 stale `@expected-red` Gherkin scenarios whose `@after-phase-12/15` ownership was speculative; record missing pre-reqs in `.planning/deferred-items.md`.
- Backfill 5 missing audit artefacts: `12-REVIEW.md`, `12-SECURITY.md`, `14-REVIEW.md`, `14-SECURITY.md`, `15-SECURITY.md` (constitutional rule #10 enforcement).
- Sync `.planning/ROADMAP.md` Progress Table (lines 819-838) with actual v2 phase closure dates.

**Out of scope (deferred):**
- FSL git-history scrub (Phase 15-04 operator-side `tools/history-scrub.sh`) — explicitly operator work.
- First real GHA `make e2e-cjm` CI run — operator work.
- UICONF-05 axe baseline live execution — operator/CI.
- Implementing the missing pre-reqs for the 4 repointed tags (`sendResetPassword` hook, mock-litellm transcribe overlay, Better Auth i18n interception, web-locale-toggle Playwright steps). Each becomes a new phase or deferred-items entry.
- Re-opening any closed v2 phase's prod code surface for refactor.

</domain>

<decisions>
## Implementation Decisions

### Source-Contract Path Fix (A1 advisor — adopted)

- **D-01:** Use **Option A** — path-rewrite to `../../../src/...` in the 3 failing tests. Smallest correct diff, matches canonical pattern at `apps/api/tests/unit/lib/sse-parser.test.ts:17-19`. Tests stay in `tests/unit/` per STRUCT-01.
- **D-02:** Exact rewrites land in same atomic commit as the test-fix:
  - `apps/worker/tests/unit/jobs/partman-maintenance.test.ts:129-132` — `__dirname` → `__dirname + "/../../../src/jobs/partman-maintenance.ts"`
  - `apps/worker/tests/unit/lib/with-system-context.test.ts:85` — `"./with-system-context.ts"` → `"../../../src/lib/with-system-context.ts"`
  - `apps/api/tests/unit/routes/agent/stream.test.ts:825-826` — `resolve(here, "stream.ts")` → `resolve(here, "..", "..", "..", "..", "src", "routes", "agent", "stream.ts")`
- **D-03:** NO changes to `tools/lint-colocated-tests.ts`, NO allowlist, NO `docs/conventions.md` edits. Option B (move tests back to `src/__tests__/`) and Option C (runtime `import()`) both REJECTED — see DISCUSSION-LOG.
- **D-04:** Add `existsSync` guard before each `readFileSync` so future tree moves fail with a path-printing error rather than bare ENOENT (defensive hardening, 2 lines per test).
- **D-05:** `__fixtures__/utf8-split.sse` is NOT missing — it lives at `apps/api/src/routes/agent/__fixtures__/utf8-split.sse` (verified). The ENOENT in V2-MILESTONE-REVIEW conflated stream.test.ts's source-read ENOENT with the fixture path. No fixture relocation.

### Rate-Limit Ordering — Security Fix (A2 advisor — adopted)

- **D-06:** Use **Option B** — tag-driven skip on `routeOptions.config.authRequired === true && !req.user` inside the IP-tier `onRequest` hook at `apps/api/src/plugins/rate-limit.ts:181-217`.
- **D-07:** Tag the three authed token routes (`assemblyai.ts:43`, `deepgram.ts`, `openai-realtime.ts`) with `config.authRequired: true`. Auth-OPEN routes (`/api/auth/signin`, `/signup`, `/forgot-password`) leave `authRequired` unset → IP-tier fires pre-auth per Phase 6 D-RL1 DoS shield contract.
- **D-08:** This is a **design conflict** between Phase 04 D-RL1 (per-user, post-auth keying) and Phase 06 D-RL1 (IP-tier pre-auth ceiling). T3 encodes the Phase 04 contract; production runs the Phase 06 contract. Reconciliation: keep both, gated by `authRequired` tag. Update stale comments at `assemblyai.ts:23-26` and `rate-limit.ts:27-30` in the same commit.
- **D-09:** Affected test audit: T3 turns GREEN. T1/T2/T4/T5/T6 in `rate-limit-isolation.integration.test.ts` are all authed — unaffected. Sweep `apps/api/tests/**/rate-limit*` for IP-ceiling tests on authed routes; update those to use auth-OPEN route fixtures (signin) if any exist. Option A (reorder hooks) REJECTED — degrades DoS shield. Option C (two-tier limiter via existing `config/rate-limits.ts:34` taxonomy) DEFERRED to v3 as proper architecture; too large for closure phase.

### Stale @expected-red Disposition (A3 advisor — REPOINT-only, all 4)

- **D-10:** All 4 stale tags → **REPOINT** only. NO real downstream code lands in 18.1. Each missing pre-req → `.planning/deferred-items.md` entry + new phase number.
- **D-11:** `@cjm-3.1` (password-reset) `@after-phase-12` → `@after-phase-19a-reset-mail`. Missing pre-req: `apps/api/src/auth.ts` lacks `sendResetPassword` hook (Better Auth `forget-password` endpoint runs but never enqueues an email; worker template `password_reset` exists but is uncalled).
- **D-12:** `@cjm-4.1` (transcribe) `@after-phase-12` → `@after-phase-19b-stt-fixture`. Missing pre-req: `apps/api/src/routes/transcribe.ts` proxies to LiteLLM `provider=groq, model=whisper-large-v3` and throws `MissingProviderKeyError → 503` when `GROQ_API_KEY` empty. CJM compose stack defaults to empty. Per `feedback_loadtest_cost_discipline.md` (paid cloud gets smokes only) — bias toward mock-litellm overlay for cjm, not real GROQ key.
- **D-13:** `@cjm-1.4` (signup-verify Russian locale) `@after-phase-15` → `@after-phase-19c-better-auth-i18n`. Missing pre-req: Better Auth handler at `apps/api/src/routes/better-auth-handler.ts` is a black-box `webHandler` pass-through; returns English error envelope verbatim. `req.t(...)` never invoked on Better Auth's 4xx responses.
- **D-14:** `@cjm-6.1` (locale-switch cookie + Russian copy) `@after-phase-15` → `@after-phase-19d-locale-e2e`. Missing pre-req: `tests/e2e-cjm/steps/locale.steps.ts:26-42` — all three step bodies are `throw new Error("locale UI ships in Phase 15 — @cjm-6.1 stays @expected-red")`. Web-side locale-toggle UI exists in `apps/web/src/components/screens/language-switcher.tsx` but no Playwright step-defs drive it, and cjm harness uses undici/fetch, not Playwright; web container not booted.
- **D-15:** Update `docs/customer-journeys.md` rows for these 4 anchors to reflect new `@after-phase-N` ownership. `tools/lint-cjm-doc.ts` Mode-3 stays GREEN (still pairs `@expected-red` with `@after-phase-N`).
- **D-16:** A3 critical finding: the **original owner phase was 13** per feature-file headers, not 12/15. The `@after-phase-12/15` tags were speculative scheduling that was never reconciled. Treat 18.1 as roadmap-tag hygiene + audit-trail repair, not as "flip what was promised."

### Diarization Status Mapping Regression

- **D-17:** Bug at `apps/api/src/routes/diarization.ts` (location TBC by planner) — for pyannote job statuses `failed` and `cancelled`, the 502 envelope returns `jobId: undefined`. Test expectation: `expect(body.jobId).toBe('job-1')`. Fix: ensure jobId propagates from upstream pyannote response through the error path. TDD: keep existing 2 RED tests in `apps/api/tests/unit/routes/__tests__/diarization.test.ts`, fix the route handler, GREEN. Same atomic commit.

### AccountClient Duplicate Copy

- **D-18:** Bug at `apps/web/src/components/screens/account/AccountClient.tsx` — text "Active sessions" appears twice (section description "Manage your profile, active sessions, and account deletion." + the section heading). Either rename the description (drop "active sessions" word) OR scope the test assertion to the heading element specifically. PREFERRED: rename description copy to avoid the word — better UX (no near-duplicate), simpler assertion. Test: `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx` uses `getByText(/Active sessions/i)` which throws on multiple matches — switch to scoped query via `within(headingsRegion).getByRole('heading', {name: /Active sessions/i})`.

### Backfill REVIEW/SECURITY Artefacts (A4 advisor — adopted)

- **D-19:** Use **Option A** — spawn `gsd-code-reviewer` and `gsd-security-auditor` as fresh agent contexts (no participation in the original phase) per-phase, NOT batched. Constitutional rule #10 ("audit trail") + adversarial-stance preservation (`13-SECURITY.md:7`).
- **D-20:** **Phase 12 commit scope:** `9763a91..5af4f6a` (first: `feat(12-01): setup_state singleton + users.role`; last: `docs(12-05b): UICONF-05 axe baseline`).
- **D-21:** **Phase 14 commit scope:** `dd44c3f..807d3dd` (first: `test(14-01): red — slim-core base conformance`; last: `docs(14-07): e2e-cjm BYOK Gherkin coverage`).
- **D-22:** **Phase 15 commit scope for 15-SECURITY.md:** TBC at plan time — full Phase 15 range (4 plans, FSL relicense + Traefik host split + history-scrub runbook). Note: 15-REVIEW.md DOES exist (`f523184` + `508041d` + `0735965` fixed 3 HIGH). Only 15-SECURITY.md is missing.
- **D-23:** **High-risk surfaces the reviewers MUST cover** (locked from A4):
  - **Phase 12:** `setup_state` singleton lifecycle/race, admin bootstrap secret handling, `users.role` enum + Better Auth `additionalFields.role` `input:false`, public `GET /api/setup-state` rate-limit, `GET /api/auth/providers` no provider-secret echo, `GET /api/capabilities` tenant-scoped ETag, `/setup` wizard CSRF/origin, role-promotion break-glass operator footgun.
  - **Phase 14:** byok-guard wiring order before SSRF + OTel, loud-fail on default/placeholder secrets in prod, `LITELLM_VIRTUAL_KEY` rotation worker removal completeness, env surface migration notes, six compose overlays + ingress `ports !reset` (no accidental `0.0.0.0` bind of admin surface), Helm BYOK toggles values.yaml secret refs, BYOK e2e-cjm scenarios actually trip the guard.
  - **Phase 15:** TLS bootstrap two-tier CA chain, FSL relicense SPDX swath, REUSE 3.3 compliance gate (`.github/workflows/reuse-lint.yml`), 220 test-file relocation correctness, host-split SSRF surface, history-scrub runbook safety (per-invocation state-dir, GPG keyring precondition).
- **D-24:** Gate "phase truly closed" claim in ROADMAP/REQUIREMENTS until all 5 artefacts land AND any HIGH findings are remediated under 18.1.x sub-phases (TDD: red test → fix → green, same commit). If new HIGHs surface, scope decision lands during execute-phase.

### Test-Run Strategy

- **D-25:** Each test-fix lands TDD in its own atomic commit (RED `git revert`-style proof via failing CI, GREEN via fix in same commit, REFACTOR if any). Per CLAUDE.md TDD constitutional rule.
- **D-26:** `--no-verify` predicted ZERO for this phase — all edits are small-surface (`*.ts`/`*.tsx` already within biome glob, but no mass-format triggers). HALT-and-escalate on any lefthook fire.
- **D-27:** Order of execution (planner will sequence): path-fixes first (cheap, clears 3 reds), then diarization fix, then AccountClient, then rate-limit (security), then stale-tag REPOINT (no code, just YAML+md edits), then backfill agents per-phase. Backfill agents can run in parallel waves.

### Claude's Discretion

- Phase plan split: 5-7 plans expected. Each independent failure becomes one plan; backfill spawns aggregated under one plan-orchestrator that delegates 5 sub-tasks to 5 agent spawns.
- Backfill artefact length cap: ≤ 250 LOC per artefact (matches `13-REVIEW.md` 202 + `15-REVIEW.md` 324 envelope).
- Stale-tag REPOINT commit shape: ONE commit covering all 4 features + customer-journeys.md row updates + deferred-items.md entry (mechanical mass-edit, no logic).
- If a backfill agent surfaces a new HIGH that requires production code change, halt execution and ask user before proceeding (deviation protocol).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### V2 Milestone State
- `.planning/V2-MILESTONE-REVIEW.md` — Full 7-phase audit; per-phase verdicts; cross-cutting findings; top-7 followups. This phase implements followups 1, 3 (partial), 4 (partial), 6, plus the test-sweep failures NOT in the review.
- `.planning/STATE.md` — Current v2 state machine; this phase appends Phase 18.1 to closed-phases list.
- `.planning/ROADMAP.md` lines 819-838 — Progress Table to sync.
- `.planning/deferred-items.md` — Where the 4 stale-tag missing pre-reqs are recorded.

### Constitutional / Process
- `.planning/PROJECT.md` §"Engineering Discipline" rules #1 (TDD), #2 (90/90/90/90 coverage), #4 (no internal mocks), #9 (no `--no-verify`), #10 (audit trail).
- `CLAUDE.md` global + project — "НЕ УПРОЩАТЬ ЗАДАЧИ" / "НЕ ИСПОЛЬЗОВАТЬ МОКИ" / strict-TDD.

### Test-Failure Cluster A (source-contract paths)
- `apps/worker/tests/unit/jobs/partman-maintenance.test.ts:129` — broken path.
- `apps/worker/src/jobs/partman-maintenance.ts` — actual source.
- `apps/worker/tests/unit/lib/with-system-context.test.ts:85` — broken path.
- `apps/worker/src/lib/with-system-context.ts` — actual source.
- `apps/api/tests/unit/routes/agent/stream.test.ts:825-826` — broken path.
- `apps/api/src/routes/agent/stream.ts` — actual source.
- `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` — **canonical pattern reference** for cross-tree path resolution.
- `docs/conventions.md` §STRUCT-01 (lines 248-273) — codified test layout, forbids `src/__tests__` colocation.
- `tools/lint-colocated-tests.ts` — enforces STRUCT-01.

### Test-Failure Cluster B (rate-limit ordering)
- `apps/api/src/plugins/rate-limit.ts:181-217` — IP-tier `onRequest` hook (the fix site).
- `apps/api/src/routes/tokens/_call-provider.ts` + token routes — auth-required routes to tag.
- `apps/api/src/routes/tokens/assemblyai.ts:23-26,43` — stale comment to sync + tag point.
- `apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts:248` — T3 RED test.
- `apps/api/src/config/rate-limits.ts:34` — `ip-only` vs `user-and-ip` taxonomy (existing but unenforced; future v3 architectural fix).
- `apps/api/src/auth.ts` — Better Auth wiring + plugin order.
- `apps/api/src/index.ts` — plugin registration order.

### Test-Failure Cluster C (diarization)
- `apps/api/tests/unit/routes/__tests__/diarization.test.ts` — 2 RED tests (failed + cancelled).
- `apps/api/src/routes/diarization.ts` — route handler to fix (path TBC).

### Test-Failure Cluster D (AccountClient)
- `apps/web/src/components/screens/account/AccountClient.tsx` — duplicate "Active sessions" copy.
- `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:117` — RED assertion.

### Stale @expected-red Tags
- `tests/e2e-cjm/features/password-reset.feature:6` (`@cjm-3.1`).
- `tests/e2e-cjm/features/transcribe.feature:6` (`@cjm-4.1`).
- `tests/e2e-cjm/features/signup-verify.feature:27` (`@cjm-1.4`).
- `tests/e2e-cjm/features/locale-switch.feature:6` (`@cjm-6.1`).
- `tests/e2e-cjm/steps/locale.steps.ts:26-42` — pending stubs (3 throws).
- `apps/api/src/auth.ts` — missing `sendResetPassword` hook (deferred pre-req).
- `apps/api/src/routes/transcribe.ts` — missing STT fixture / mock-litellm overlay (deferred pre-req).
- `apps/api/src/routes/better-auth-handler.ts` — missing i18n interception (deferred pre-req).
- `docs/customer-journeys.md` — anchor rows to update.
- `tools/lint-cjm-doc.ts` — Mode-3 pairing rule.

### Backfill Artefact Templates
- `.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-REVIEW.md` (202 LOC).
- `.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-SECURITY.md` (129 LOC, adversarial-stance template).
- `.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-REVIEW.md` (324 LOC longer-form).
- `.planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-VERIFICATION.md` + `12-0[1-5]-PLAN.md` — Phase 12 audit subject.
- `.planning/phases/14-slim-core-byok-profiles-v2/14-VERIFICATION.md` + `14-RESEARCH-loud-fail.md` + `14-RESEARCH-env-slim.md` — Phase 14 audit subject.
- `~/.claude/agents/gsd-code-reviewer.md` + `~/.claude/agents/gsd-security-auditor.md` — agent definitions.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **Cross-tree readFileSync pattern** from `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` — canonical idiom for tests in `tests/unit/` reaching into `src/`.
- **`gsd-code-reviewer` + `gsd-security-auditor` agents** — already installed and exercised by Phases 13/15/16/17.
- **Stale-tag REPOINT precedent** — Phase 18 (SSO SPEC) used `@after-phase-19` for forward-pointing reds; same pattern applies.

### Established Patterns
- **TDD atomic commits** — each fix lands with its tests in ONE commit (RED proof in CI logs, GREEN proof in commit's CI run).
- **Plan split = one plan per independent failure cluster** — Phase 02.x precedent; Phase 17 three-plan split.
- **Per-phase commit-range scoping for backfill** — A4 recommended pattern; matches what Phase 15-04 history scrub does for SHA-anchored operations.
- **Stale-comment sync in same commit as code change** — Phase 16-fix WR-01 precedent.
- **DEFERRED items in `.planning/deferred-items.md`** — append-only; each entry cites source phase + missing pre-req + new phase number.

### Integration Points
- `tools/lint-cjm-doc.ts` — runs in `lefthook.yml` pre-commit; new tag pairings MUST still pair `@expected-red` ↔ `@after-phase-N`.
- `tools/lint-phase-tag-comments.ts` — runs in CI; backfill artefacts may add new phase-tag comments to track Phase 18.1 source-of-truth. Allowlist via `tools/lint-phase-tag-comments.allowlist.txt` if needed.
- `lefthook.yml` pre-commit — biome + english + commitlint + phase-tag-comments + colocated-tests + dockerfile-tls. None of these should fire on this phase's edits.
- `.github/workflows/ci.yml` — runs `pnpm test` + lints. This phase makes that GREEN.

</code_context>

<specifics>
## Specific Ideas

- A2 finding (stale comments in `assemblyai.ts:23-26` and `rate-limit.ts:27-30`) is a SECOND-ORDER fix — easy to forget. Bundle it in the rate-limit commit.
- A3's "REPOINT all 4 to phase 19a/b/c/d" naming convention is consistent with how Phase 18 used `@after-phase-19` for SSO. Adopt the same convention.
- A4's "if new HIGH surfaces during backfill, halt-and-escalate" is the deviation protocol. Apply.
- 5-artefact backfill batched but per-phase scoped — gives 5 parallel agent spawns inside a single plan-wave; ~30-45min agent time each; total wall-time ≈ longest single run, not sum.
- Skip `lefthook` hook is `--no-verify` is FORBIDDEN per D-26.

</specifics>

<deferred>
## Deferred Ideas

- **Phase 19a-reset-mail** — wire `sendResetPassword` hook in `apps/api/src/auth.ts`, enqueue email through worker `password_reset` template, flip `@cjm-3.1` GREEN.
- **Phase 19b-stt-fixture** — add mock-litellm transcribe overlay to cjm compose stack (returns canned `{text:""}` for whisper requests), flip `@cjm-4.1` GREEN. Per `feedback_loadtest_cost_discipline.md` bias — no real GROQ key in CI.
- **Phase 19c-better-auth-i18n** — wire `req.t(...)` interception around Better Auth pass-through 4xx responses; flip `@cjm-1.4` GREEN.
- **Phase 19d-locale-e2e** — boot web container in cjm harness, replace `throw "pending"` in `tests/e2e-cjm/steps/locale.steps.ts:26-42` with real Playwright step-defs; flip `@cjm-6.1` GREEN.
- **Two-tier rate-limit** (A2 Option C) — implement `apps/api/src/config/rate-limits.ts:34` `ip-only`/`user-and-ip` taxonomy across all routes; remove the `authRequired` skip-tag hack. v3 architectural cleanup.
- **Typecheck red across 7+ files** (deferred from 14-04) — `apps/api` + `apps/worker` + `packages/litellm-client` (typed-queue BullMQ shape, with-tenant-context `unknown→AttributeValue`, transcriptions create/batch-create, etc.). Out of 18.1 scope — separate Phase 18.2 or rolled into milestone-close gate.
- **FSL history scrub** (Phase 15-04) + **first real GHA e2e-cjm run** + **UICONF-05 axe baseline** — operator-side, NOT in 18.1.

### Reviewed Todos (not folded)
- None from cross_reference_todos step (no todo system match for "18.1" key).

</deferred>

---

*Phase: 18.1-v2-test-debt-closure*
*Context gathered: 2026-05-15*
