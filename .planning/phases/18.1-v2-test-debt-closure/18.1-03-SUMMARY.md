---
phase: 18.1-v2-test-debt-closure
plan: 03
type: execute
status: complete
commits:
  red: c2e0d1c
  green: 7857827
requirements_closed: [V2-TD-04, V2-TD-05]
---

# Phase 18.1 Plan 03: Diarization status mapping fix — Summary

Closed F4 + F5 (jobId loss on pyannote terminal `failed` / `cancelled`) per D-17 / D-28 / D-30. Two-commit TDD pair, surgical single-hunk prod edit, lefthook clean on both commits.

## Commits

| SHA | Subject | Files |
|---|---|---|
| `c2e0d1c` | `test(18.1-03): red — diarization 502 envelope missing jobId for failed/cancelled` | `apps/api/tests/unit/routes/__tests__/diarization.test.ts` (+2/-2) |
| `7857827` | `fix(18.1-03): green — diarization 502 carries jobId for failed/cancelled (F4/F5)` | `apps/api/src/routes/diarization.ts` (+4/-1) |

Start SHA: `2c7102f`.

## RED → GREEN proof

**Pre-fix (post-sharpen, RED on c2e0d1c):**
```
FAIL ... status='failed'   — expected undefined to be 'job-1' at L415
FAIL ... status='cancelled' — expected undefined to be 'job-1' at L436
Tests  2 failed | 44 skipped (46)
```

**Post-fix (GREEN on 7857827):**
```
Test Files  1 passed (1)
Tests  46 passed (46)
```

Sharpening: `expect(json.error).toMatch(/.../)` → `expect(json.error).toBe("diarization job <status>")` (Option A, pins exact GREEN shape).

## Scope audit (D-17 / D-30 compliance)

- Single hunk at `apps/api/src/routes/diarization.ts:323-327` — `throw new UpstreamError(...)` replaced with `return reply.code(502).send({error, jobId})`. Mirrors 504 branch shape at L336-340.
- 504 ceiling branch (L336-340) — **byte-identical** to baseline.
- Central error handler (`errors.ts` / error-handler plugin) — **not touched**.
- `UpstreamError` import retained (still used at L365 + L369 by `mapPyannoteError`).
- No other diarization branches modified (200, 4xx, 504, Speaches branch all untouched).

## Coverage delta on `diarization.ts`

The two replaced lines map 1:1 to the two existing tests (F4 covers `failed`, F5 covers `cancelled`) — both branches of the `||` exercised. Net effect: 100% line/branch/statement coverage on the 4 new lines; pre-existing function coverage unchanged. Diff coverage ≥ 90/90/90/90 satisfied.

## Lefthook result

Both commits: `clean` (biome / phase-tag-comments / english / ui-spec / commitlint all green). Two pre-existing `useOptionalChain` warnings at L167 + L407 are unrelated to this hunk — left in place per scope discipline (CLAUDE.md rule on scope boundary; out-of-scope for v2 test-debt closure).

## Deviations

None. Plan executed exactly as written (Option A sharpen, surgical scope, 2 atomic commits, zero `--no-verify`).

## Notes

Matches PATTERNS §3 same-file 504 mirror — v3 should consider unifying envelope shape across routes so `jobId` can flow through the central handler without per-route opt-outs.
