---
phase: 18.1-v2-test-debt-closure
plan: 04
type: execute
wave: 3
depends_on: ["18.1-01", "18.1-03"]
files_modified:
  - apps/web/src/locales/en/end-user.json
  - apps/web/src/locales/ru/end-user.json
  - apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
autonomous: true
requirements:
  - V2-TD-07   # F7 — AccountClient duplicate "Active sessions" copy
must_haves:
  truths:
    - "`apps/web/src/locales/en/end-user.json` — `account.subtitle.body.text` no longer contains the phrase 'active sessions'; replacement copy is concise and excludes the noun phrase, preserving overall meaning (manage your profile + account deletion). RU sibling at `apps/web/src/locales/ru/end-user.json` drops 'активных сессиях' (or equivalent translation) for locale parity (RESEARCH §8 pitfall #8)."
    - "`apps/web/src/components/screens/account/AccountClient.tsx` is NOT modified — the component reads the subtitle via the `t(\"end-user:end-user.account.subtitle.body.text\")` i18n key (RESEARCH §1 row D-18; pitfall #3). Locale-file change is sufficient."
    - "`apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:117` switches from `getByText(/Active sessions/i)` to `getByRole(\"heading\", { name: /^Active sessions$/i, level: 2 })` (or omit `level`) — heading-scoped assertion mirrors `apps/web/src/components/screens/admin/__tests__/ConfigClient.test.tsx:165` analog."
    - "`apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:34` (or wherever the test fixture subtitle is set) is synced to the new locale string so the rendered DOM no longer carries the duplicate 'Active sessions' phrase in the subtitle."
    - "Axe baseline (UICONF-05) is NOT regressed: the `<h2>Active sessions</h2>` heading text is UNCHANGED (only the subtitle prose is renamed) — pitfall #3, RESEARCH §8."
    - "TWO-commit TDD (D-28): pre-existing F7 RED on `main` stands as proof; ONE GREEN commit lands the locale + test edits atomically. Subject: `fix(18.1-04): green — AccountClient subtitle drops 'active sessions'; assertion scoped to heading (F7)`."
    - "ZERO `--no-verify`; lefthook hooks `biome` (TS/TSX/JSON), `english`, `web-typecheck` (`apps/web/**/*.{ts,tsx}`), `commitlint` all clean."
  artifacts:
    - path: "apps/web/src/locales/en/end-user.json"
      provides: "Account subtitle no longer mentions 'active sessions' — duplicate-text removed at the source-of-copy."
      contains: "account"
    - path: "apps/web/src/locales/ru/end-user.json"
      provides: "RU parity — subtitle excludes the equivalent Russian phrase."
      contains: "account"
    - path: "apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx"
      provides: "Heading-scoped assertion + synced fixture; defence-in-depth against future duplicate-copy regressions."
      contains: "getByRole(\"heading\""
  key_links:
    - from: "apps/web/src/components/screens/account/AccountClient.tsx"
      to: "apps/web/src/locales/{en,ru}/end-user.json (account.subtitle.body.text key)"
      via: "`t(\"end-user:end-user.account.subtitle.body.text\")` i18n call — component is BYTE-IDENTICAL across this plan; only locale strings change."
      pattern: "account.subtitle.body.text"
    - from: "apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:117"
      to: "apps/web/src/components/screens/admin/__tests__/ConfigClient.test.tsx:165 (analog)"
      via: "Heading-scoped query — `screen.getByRole(\"heading\", { name: /^Active sessions$/i })` mirrors `ConfigClient`'s heading-scoping pattern; pass `level: 2` (AccountClient sections use `<h2>`) or omit `level` per defence-in-depth."
      pattern: "getByRole(\"heading\""
---

<objective>
Close F7 (RESEARCH §2): `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:117`
fails with "Found multiple elements with the text: /Active sessions/i" because
the subtitle prose ("Manage your profile, active sessions, and account
deletion.") and the section `<h2>` heading both match the regex.

Per D-18 + RESEARCH §1 row D-18: the FIX site is the i18n locale JSON (en + ru),
NOT `AccountClient.tsx`. The component renders the subtitle via the
`account.subtitle.body.text` key — drop "active sessions" from the source-of-
copy and the duplicate disappears at the rendering layer. The test is ALSO
tightened to `getByRole("heading", …)` as defence-in-depth.

Wave 3 — depends on Waves 1-2 by D-27 execution ordering only (file-disjoint
from 18.1-01/02/03). Output: 1 atomic GREEN commit landing 3 file edits
(en JSON + ru JSON + test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md
@CLAUDE.md

@apps/web/src/components/screens/account/AccountClient.tsx
@apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
@apps/web/src/locales/en/end-user.json
@apps/web/src/locales/ru/end-user.json
@apps/web/src/components/screens/admin/__tests__/ConfigClient.test.tsx
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1-04-01: Confirm F7 RED + locate i18n key (no commit)</name>
  <files>
    - apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
  </files>
  <action>
    Per D-28 pre-existing-failure rule + RESEARCH §1 row D-18.

    1. `pnpm --filter @openwhispr/web test AccountClient.test` — expect FAIL with "Found multiple elements with the text: /Active sessions/i".
    2. Grep `apps/web/src/locales/en/end-user.json` for the key `account.subtitle.body.text` (or whatever nested key holds the "Manage your profile, active sessions, and account deletion." string). Record the JSON pointer path.
    3. Confirm `AccountClient.tsx` reads via `t("end-user:end-user.account.subtitle.body.text")` (or equivalent) — NOT via inline literal. If the literal IS in JSX, D-18's "i18n key, not JSX" claim needs revision; HALT and escalate.
    4. Read `apps/web/src/components/screens/admin/__tests__/ConfigClient.test.tsx:165` for the canonical heading-scoped pattern: `expect(screen.getByRole("heading", { name: /configuration/i, level: 1 })).toBeInTheDocument();`
    5. NO file modifications. Record RED proof + key path for the GREEN commit body.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/components/screens/account/__tests__/AccountClient.test.tsx --reporter=verbose 2>&1 | grep -E "(Found multiple|FAIL|✗)" | head -5 && grep -rn "subtitle" apps/web/src/locales/en/end-user.json | head -5</automated>
  </verify>
  <done>F7 confirmed RED; i18n key path located; component verified to read via `t(...)`; heading-scoped analog noted.</done>
</task>

<task type="auto">
  <name>Task 18.1-04-02: GREEN — locale string rename + heading-scoped test + fixture sync</name>
  <files>
    - apps/web/src/locales/en/end-user.json
    - apps/web/src/locales/ru/end-user.json
    - apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
  </files>
  <action>
    Per D-18 + RESEARCH §1 row D-18 + PATTERNS §4.

    **Edit 1 — `apps/web/src/locales/en/end-user.json`:**

    Locate the JSON key holding "Manage your profile, active sessions, and account deletion." (path established in 18.1-04-01; expected at `account.subtitle.body.text` or similar). Replace with concise copy that drops "active sessions" entirely. Recommended replacement:

    > "Manage your profile and account."

    or, if preserving the deletion mention is preferred:

    > "Manage your profile and delete your account."

    The exact wording is at executor discretion; the invariant is that the regex `/Active sessions/i` no longer matches the subtitle prose. Keep the prose ≤ 60 chars to stay within the existing UICONF layout budget.

    **Edit 2 — `apps/web/src/locales/ru/end-user.json`:**

    Sibling key in RU. Current text likely contains "активных сессиях" (or "активные сессии"). Replace with a Russian rendering that DOES NOT match `/Active sessions/i` — Cyrillic translation naturally won't, but ensure the RU copy is meaning-preserving and doesn't drift from the EN intent. Suggested: "Управление профилем и удаление аккаунта." (per Phase 12 RU locale precedent — verify against existing translator conventions in the file).

    WHY (pitfall #8): keep EN+RU in lockstep — locale parity is part of the localization rule in CLAUDE.md (`en` + `ru` minimum from day one).

    **Edit 3 — `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx`:**

    Two changes in this file:

    (a) **Line 34 (or wherever the test fixture subtitle is set):** If the test mocks the i18n provider with a fixture that hard-codes the OLD subtitle text, sync the fixture to the NEW locale string. If the test uses the real `i18next` instance + actual JSON files, no fixture edit is needed (the JSON change cascades).

    (b) **Line 117 (the assertion):** Replace:

    ```ts
    expect(screen.getByText(/Active sessions/i)).toBeInTheDocument();
    ```

    with:

    ```ts
    expect(
      screen.getByRole("heading", { name: /^Active sessions$/i })
    ).toBeInTheDocument();
    ```

    Anchor regex with `^…$` (heading text is short + exact). Omit `level` (AccountClient `<h2>` is implementation detail; defence-in-depth + future-flexibility per PATTERNS §4 risk callout). If the test asserts THREE headings (Profile / Active sessions / Danger zone — per the test name "renders the three section headings"), update ALL three with the same heading-scoped pattern for consistency. If only the failing assertion is touched and Profile/Danger zone still use `getByText`, that is also acceptable (those are not currently failing).

    **Atomic GREEN commit message:**

    ```
    fix(18.1-04): green — AccountClient subtitle drops 'active sessions'; assertion scoped to heading (F7)

    Closes V2-TD-07 (duplicate "Active sessions" text causes RTL ambiguity):

    - apps/web/src/locales/en/end-user.json: account.subtitle.body.text
      replaced — "Manage your profile and account." (drops the phrase
      "active sessions" entirely; AccountClient.tsx reads via i18n key
      so no JSX edit).
    - apps/web/src/locales/ru/end-user.json: RU sibling synced for
      en/ru locale parity (CLAUDE.md i18n rule).
    - apps/web/src/components/screens/account/__tests__/AccountClient
      .test.tsx — assertion at L117 tightened from
      `getByText(/Active sessions/i)` to
      `getByRole("heading", { name: /^Active sessions$/i })`,
      mirroring the heading-scoping pattern at
      ConfigClient.test.tsx:165. Test fixture subtitle (L34) synced
      to new locale string.
    - AccountClient.tsx is BYTE-IDENTICAL (i18n-key indirection per D-18).
    - <h2>Active sessions</h2> heading text UNCHANGED — UICONF-05 axe
      baseline preserved (pitfall #3).

    RED proof: pre-existing AccountClient.test.tsx failure on main
    HEAD~1 (deferred-items.md §From Plan 12-04 entry).

    Refs V2-TD-07.
    ```

    WHY (pitfalls):
    - Pitfall #3: source-of-copy is the JSON, not the JSX; this avoids touching `AccountClient.tsx` and stays out of the UICONF-05 axe sensitivity zone.
    - Pitfall #8: RU parity is part of the duplicate-text fix — RU subtitle had its own near-duplicate that must drop in lockstep.
    - PATTERNS §4: heading-scoped query mirrors `ConfigClient.test.tsx:165` analog; pass `level: 2` or omit — AccountClient sections use `<h2>`.

    Lefthook surface: `biome` (TS/TSX/JSON formatter), `english` (English content rule applies to source artifacts; subtitle string is now shorter English text), `web-typecheck` (`AccountClient.test.tsx` edit must keep TS clean — `getByRole` is in `@testing-library/react` types), `commitlint`. No `--no-verify` (D-26).
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/components/screens/account/__tests__/AccountClient.test.tsx --reporter=verbose 2>&1 | tail -10 && ! grep -q "active sessions" apps/web/src/locales/en/end-user.json</automated>
  </verify>
  <done>F7 GREEN; en + ru JSON updated; AccountClient.tsx unchanged; test asserts via `getByRole("heading", …)`; ONE atomic commit; lefthook clean (including `web-typecheck`).</done>
</task>

</tasks>

<test_strategy>
- Unit: F7 in `AccountClient.test.tsx` becomes GREEN. The other tests in the same file (Profile heading, Danger zone heading, render-related smoke) must stay GREEN — if they assert subtitle TEXT specifically, they need fixture updates too (executor checks during Task 18.1-04-01).
- Integration / E2E: any Playwright/cucumber scenario that asserts the subtitle text (unlikely — `tests/e2e-cjm/features/*.feature` greps don't target account subtitle) must be re-verified. F7 fix is RTL-only.
- Visual / a11y: axe baseline preserved because `<h2>Active sessions</h2>` heading text is unchanged (D-18 + pitfall #3). UICONF-05 live execution is operator-side; this plan asserts the static invariant via grep ("Active sessions" still present as a heading text, just not as subtitle prose).
- Coverage: diff modifies JSON + test file. AccountClient.tsx is unchanged → no prod coverage delta. Test-file modification preserves the existing coverage of AccountClient.tsx. Coverage gate satisfied trivially (no new prod LOC).
</test_strategy>

<verification>
```bash
cd /Users/dev/openwhispr-server

# F7 GREEN
pnpm --filter @openwhispr/web test src/components/screens/account/__tests__/AccountClient.test.tsx

# Static asserts
! grep -i "active sessions" apps/web/src/locales/en/end-user.json | grep -i "subtitle"
grep -c "Active sessions" apps/web/src/components/screens/account/AccountClient.tsx  # should be 1 (just the <h2> text)
grep -n 'getByRole("heading"' apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx

# Typecheck stays clean
pnpm --filter @openwhispr/web typecheck
```
</verification>

<success_criteria>
1. F7 GREEN; sibling tests in the same file still GREEN.
2. `account.subtitle.body.text` in `en/end-user.json` no longer contains "active sessions" (case-insensitive).
3. RU sibling key updated for locale parity.
4. `AccountClient.tsx` byte-identical to pre-plan (component reads via `t(...)`).
5. Test assertion at L117 scoped to `getByRole("heading", …)`.
6. `<h2>Active sessions</h2>` heading text UNCHANGED — UICONF-05 axe baseline preserved.
7. ONE atomic GREEN commit; ZERO `--no-verify`; `web-typecheck` lefthook clean.
8. Optional but encouraged: extend the heading-scoping to Profile + Danger zone assertions if they exist in the same test (consistency).
</success_criteria>

<rollback>
If AccountClient.tsx actually contains the inline subtitle literal (D-18 wrong about i18n indirection):
- HALT before committing.
- Read AccountClient.tsx carefully and update the planner: edit the JSX instead of (or in addition to) the JSON.
- A2 pitfall #3 was the planner's hypothesis based on RESEARCH §1 row D-18 — verify before acting.

If `web-typecheck` lefthook fires on the test file edit (e.g. `getByRole` type narrowing complains):
- Add an explicit type annotation or use `screen.getByRole<HTMLHeadingElement>("heading", …)`.
- Do NOT `--no-verify`. Fix the type and re-commit.

If axe baseline regresses (heading text accidentally altered): revert and re-edit with the heading text UNTOUCHED.
</rollback>

<output>
After GREEN lands, create `.planning/phases/18.1-v2-test-debt-closure/18.1-04-SUMMARY.md` with:
- GREEN commit SHA + diff stats (3 files: 2 JSON + 1 test).
- F7 pre/post status (RED→GREEN).
- Confirmation `AccountClient.tsx` byte-identical.
- New EN + RU subtitle strings (verbatim) for future translator audit.
- Lefthook result: `clean` expected (incl. `web-typecheck`).
- Forward-reference: UICONF-05 axe baseline still operator-side; this plan does not regress it.
</output>
</content>
</invoke>