# Phase 18.1 — Discussion Log

**Date:** 2026-05-15
**Mode:** discuss (advisor-style; 4 parallel `gsd-advisor-researcher` agents)
**User pacing:** yolo — full acceptance of all 4 research recommendations; explicit choice on 2 follow-up scope questions.

## Trigger

User requested an "ultrareview по итогам второго майлстоуна" + full test sweep + integration test prep. Phase 18.1 emerged from:
1. **V2 manual review agent** (background, completed 2026-05-15) — wrote `.planning/V2-MILESTONE-REVIEW.md`; surfaced 12 follow-ups including 4 stale `@expected-red`, missing 12/14 REVIEW+SECURITY artefacts, missing 15-SECURITY.
2. **`pnpm test` full sweep** — 21 failed / 2293 passed / 355 skipped; 7 unique failures classified into 3 buckets (path-bugs from Phase 15-02 STRUCT regression, real product bugs, UI duplicate copy).

## Gray areas selected

User selected ALL FOUR gray areas (multi-select), with explicit guidance: "как обычно через advisor агента принимаем решение правильное но без оверинжиниринга и костылей без крайностей".

1. Source-contract path fix strategy (3 ENOENT tests)
2. Rate-limit ordering fix (security — anon DoS vector)
3. Stale `@expected-red` tag disposition (4 tags)
4. Backfill REVIEW/SECURITY artefacts format

## Questions asked and decisions made

### Q1. Source-contract path fix (A1 advisor)

**Options presented (after research):**
- A. Path-rewrite to `../../../src/...` (canonical pattern; smallest diff)
- B. Move 3 tests back to `src/__tests__/` with lint allowlist
- C. Refactor to runtime `import()` + `require.resolve`

**User selected (via "as advisor recommends"):** **Option A**.

**Rationale (locked from A1):**
- Already-canonical pattern at `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` — option A just applies the same idiom in 3 more files.
- Option B re-opens closed STRUCT-01 decision (`docs/conventions.md:248-273`) for trivial `readFileSync` calls; sets a "source-contract loophole" precedent.
- Option C is over-engineered for assertions that only need to read source text; workspace packages aren't published, deep-`exports` map entries are wasteful.
- ZERO lint-rule edits, ZERO allowlist edits, ZERO conventions doc edits.
- Bonus hardening: `existsSync` guard before each `readFileSync` so future tree-moves fail with path-printing error (2 extra lines per test).
- A1 verified: `__fixtures__/utf8-split.sse` is NOT missing — it exists at `apps/api/src/routes/agent/__fixtures__/utf8-split.sse`. The auditor conflated that with the stream.test.ts source-read ENOENT.

### Q2. Rate-limit ordering — security fix (A2 advisor)

**Options presented (after research):**
- A. Reorder Fastify hooks (auth before IP-tier) — REJECTED: degrades Phase 06 D-RL1 DoS shield on auth-OPEN routes (signin/signup/forgot would lose the global 600/min IP ceiling).
- B. Tag-driven skip — `routeOptions.config.authRequired === true && !req.user` → return early from IP-tier hook
- C. Two-tier limiter (global IP for auth-open, user-id for authed) using the existing-but-unenforced taxonomy at `config/rate-limits.ts:34`

**User selected (via "as advisor recommends"):** **Option B**.

**Rationale (locked from A2):**
- A2 surfaced this is a **design conflict** between Phase 04 D-RL1 (per-user, post-auth keying) and Phase 06 D-RL1 (IP-tier pre-auth DoS shield). T3 encodes the Phase 04 contract; production runs the Phase 06 contract. Both are right in their domain.
- Option B preserves the Phase 06 DoS shield on auth-OPEN routes (login brute-force protection) AND fixes T3's contract for authed routes.
- Option C is the cleaner long-term architecture (codifies the existing `config/rate-limits.ts:34` `ip-only`/`user-and-ip` taxonomy), but touches ~10-15 files — too large for a closure phase. DEFERRED to v3.
- Bundle stale-comment sync at `assemblyai.ts:23-26` and `rate-limit.ts:27-30` in the same commit so future readers don't reintroduce the conflict.

### Q3. Stale @expected-red disposition (A3 advisor)

**A3 finding:** None of the 4 stale tags can FLIP today. All 4 require real downstream code that doesn't exist:
- `@cjm-3.1` — missing `sendResetPassword` hook in `apps/api/src/auth.ts`
- `@cjm-4.1` — missing STT fixture / mock-litellm overlay (or GROQ key)
- `@cjm-1.4` — missing i18n interception in Better Auth pass-through handler
- `@cjm-6.1` — missing real Playwright step-defs (currently `throw "pending"`) + web container in cjm harness

**Critical A3 caveat:** The original owner phase per feature-file headers was **Phase 13**, not 12/15. The `@after-phase-12/15` tags were speculative scheduling that was never reconciled. This is roadmap-tag hygiene, not "flip what was promised."

**Follow-up question to user (single-select):**
- A. REPOINT only + deferred (no real code in 18.1)
- B. REPOINT + 1-2 real fixes (sendResetPassword + mock-litellm STT)
- C. All 4 real fixes (turns into mini-milestone)

**User selected:** **A — REPOINT only**. Real fixes become new phases.

**Rationale (locked from A3 + user choice):**
- 18.1 stays a CLOSURE phase, not a new-feature phase. Adding real downstream code would balloon it to ~10-12 plans (mini-milestone).
- Each missing pre-req gets a new `@after-phase-19a/b/c/d` repoint + `.planning/deferred-items.md` entry. `tools/lint-cjm-doc.ts` Mode-3 stays GREEN.
- `docs/customer-journeys.md` rows updated mechanically in same commit. ONE atomic commit for all 4 features + cjm doc + deferred-items.

### Q4. Backfill REVIEW/SECURITY format (A4 advisor)

**Options presented:**
- A. Spawn `gsd-code-reviewer` + `gsd-security-auditor` per phase (fresh contexts)
- B. Hand-written ≤200-line slim artefacts
- C. Audit-by-diff (commit messages only)

**User selected (via "as advisor recommends"):** **Option A**.

**A4 surprise finding:** `15-SECURITY.md` is ALSO missing (only `15-REVIEW.md` exists for Phase 15). 

**Follow-up question to user:**
- A. Add 15-SECURITY to 18.1 backfill (5 artefacts total)
- B. Separate Phase 18.2

**User selected:** **A — bundle in 18.1**.

**Rationale (locked):**
- Option A is the only choice that satisfies constitutional rule #10 + preserves adversarial stance modeled by `13-SECURITY.md:7`.
- Hand-written has confirmation-bias risk (author = same context that closed the phase).
- Audit-by-diff produces paperwork, not verification.
- Per-phase scoping (not batched): scopes differ (UI/admin Phase 12 vs infra/BYOK Phase 14 vs licensing/refactor Phase 15); per-phase REVIEW/SECURITY artefacts match `13-REVIEW.md` + `15-REVIEW.md` templates already in repo.
- **Phase 12 commit scope:** `9763a91..5af4f6a`. **Phase 14:** `dd44c3f..807d3dd`. **Phase 15:** TBC at plan time.
- **High-risk surfaces locked** by A4 per phase (see D-23 in CONTEXT.md).
- New HIGHs surfacing during backfill → halt-and-escalate (deviation protocol), not silent rolling-forward.

## Deferred ideas

1. **Phase 19a-reset-mail** — `sendResetPassword` hook + worker password_reset template wiring
2. **Phase 19b-stt-fixture** — mock-litellm transcribe overlay for cjm stack
3. **Phase 19c-better-auth-i18n** — wire `req.t(...)` around Better Auth pass-through 4xx envelope
4. **Phase 19d-locale-e2e** — web container in cjm harness + real Playwright step-defs for locale switch
5. **A2 Option C two-tier rate-limit** (v3 architectural cleanup; remove `authRequired` skip-tag hack)
6. **Typecheck red across 7+ files** (14-04 deferral) — separate Phase 18.2 or milestone-close gate
7. **FSL history scrub** (15-04) + **first GHA e2e-cjm run** + **UICONF-05 axe baseline** — OPERATOR-side, NOT 18.1
8. **`tools/lint-colocated-tests.ts` lint exemption for source-contract pattern** — NOT NEEDED per A1 (canonical pattern already exists, no exemption required)

## Claude's discretion items (no user input requested)

- Plan split: 5-7 plans expected (one per independent failure cluster + backfill orchestrator)
- Backfill artefact length cap: ≤ 250 LOC per artefact (matches `13-REVIEW.md` 202 + `15-REVIEW.md` 324 envelope)
- Execution order: cheap path-fixes first → diarization → AccountClient → rate-limit (security) → REPOINT (mechanical) → backfill agents (parallel)
- `--no-verify` predicted ZERO; HALT-and-escalate on any lefthook fire
- Order of priority: making `pnpm test` GREEN is the smoke gate; everything else is audit-trail hygiene

## Research artifacts

All 4 advisor researchers returned findings inline (per "do not write summary md" instruction). Key findings embedded in CONTEXT.md `<decisions>` D-01 through D-24.

- **A1** (source-contract paths): Option A; canonical pattern at `sse-parser.test.ts:17-19`; ZERO lint/allowlist changes; fixture not actually missing
- **A2** (rate-limit ordering): Option B; tag-driven skip on `authRequired === true && !req.user`; sync stale comments at `assemblyai.ts:23-26` + `rate-limit.ts:27-30`; Phase 04 vs Phase 06 design conflict surfaced
- **A3** (stale @expected-red): All 4 → REPOINT; ALL require new downstream code that doesn't exist; original owner phase was 13 per feature-file headers
- **A4** (backfill artefacts): Option A; Phase 12 range `9763a91..5af4f6a`; Phase 14 range `dd44c3f..807d3dd`; 15-SECURITY.md ALSO missing — bundle in 18.1
