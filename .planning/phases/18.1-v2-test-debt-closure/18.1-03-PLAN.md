---
phase: 18.1-v2-test-debt-closure
plan: 03
type: execute
wave: 2
depends_on: ["18.1-01"]
files_modified:
  - apps/api/src/routes/diarization.ts
autonomous: true
requirements:
  - V2-TD-04   # F4 — diarization status='failed' loses jobId
  - V2-TD-05   # F5 — diarization status='cancelled' loses jobId
must_haves:
  truths:
    - "`apps/api/src/routes/diarization.ts:323-324` no longer `throw new UpstreamError(\"DIARIZATION_JOB_FAILED\", …)` for pyannote terminal statuses `failed` and `cancelled`; instead it `return reply.code(502).send({ error: \\`diarization job ${job.status}\\`, jobId })` — explicit 502 envelope carrying `jobId`."
    - "The 504 ceiling-exceeded branch at `diarization.ts:336-340` is BYTE-IDENTICAL to pre-plan baseline (D-17 surgical-scope rule; D-30; RESEARCH §8 pitfall #2). Only the `failed`/`cancelled` lines change."
    - "Central error handler (typically `apps/api/src/plugins/error-handler.ts` or `errorEnvelope.ts`) is NOT modified (D-30 narrow-scope; central envelope discards `jobId` and that is the documented behaviour for OTHER routes)."
    - "Test F4 (`returns 502 envelope when pyannote job status='failed'`) and F5 (`returns 502 envelope when pyannote job status='cancelled'`) at `apps/api/tests/unit/routes/__tests__/diarization.test.ts` (≈ lines 415 + 436) BOTH GREEN: `expect(json.jobId).toBe('job-1')` resolves."
    - "Two-commit TDD (D-28): the existing 2 RED tests stand as RED proof on `main`; ONE GREEN commit lands the prod fix. Subject: `fix(18.1-03): green — diarization 502 carries jobId for failed/cancelled (F4/F5)`."
    - "No other diarization behaviour changes: 200 happy-path, 4xx pre-upload validation, 504 ceiling branch — all unchanged."
  artifacts:
    - path: "apps/api/src/routes/diarization.ts"
      provides: "Explicit 502 reply with `{error, jobId}` envelope for `failed` and `cancelled` pyannote statuses; matches 504 branch shape in same file."
      contains: "reply.code(502).send({"
  key_links:
    - from: "apps/api/src/routes/diarization.ts:323-324 (failed/cancelled branch)"
      to: "apps/api/src/routes/diarization.ts:336-340 (504 ceiling branch)"
      via: "Same `reply.code(N).send({error, jobId})` shape — surgical mirror, not a refactor of the central handler."
      pattern: "reply.code(50[24]).send"
    - from: "apps/api/src/routes/diarization.ts (502 fix)"
      to: "apps/api/tests/unit/routes/__tests__/diarization.test.ts (F4, F5)"
      via: "`expect(json.jobId).toBe('job-1')` — the RED→GREEN oracle for both status branches."
      pattern: "json.jobId"
---

<objective>
Close F4 + F5 (RESEARCH §2): pyannote terminal statuses `failed` and `cancelled`
currently `throw new UpstreamError("DIARIZATION_JOB_FAILED", …)` at
`apps/api/src/routes/diarization.ts:323-324`. The central error handler that
catches the throw produces a `{error, code}` envelope that DISCARDS `jobId`,
so client retry/audit logic cannot correlate the failure with the original
upstream job. The existing 504 ceiling-exceeded branch in the SAME file
(L336-340) already sends an explicit `reply.code(504).send({error, jobId})` —
this plan mirrors that shape on the 502 path.

Per D-30: REPLACE the throw with the explicit reply. Do NOT modify the
central error handler (other routes legitimately produce the canonical
`{error, code}` envelope — broadening that envelope to carry `jobId` would
balloon scope and risk other-route regressions).

Wave 2 — depends only on 18.1-01 by D-27 execution ordering (paths-first).
File-disjoint from 18.1-02/04, so 03 could run parallel with 04 by file
ownership; D-27 sequences them anyway. Output: 1 atomic GREEN commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md
@CLAUDE.md

@apps/api/src/routes/diarization.ts
@apps/api/tests/unit/routes/__tests__/diarization.test.ts
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1-03-01: Confirm F4/F5 RED + scope read (no commit)</name>
  <files>
    - apps/api/tests/unit/routes/__tests__/diarization.test.ts
  </files>
  <action>
    Per D-28 pre-existing-failure rule + RESEARCH §2.

    1. `pnpm --filter @openwhispr/api test diarization -t "failed"` — expect FAIL with `expect(json.jobId).toBe('job-1')` receiving `undefined`.
    2. `pnpm --filter @openwhispr/api test diarization -t "cancelled"` — same shape.
    3. Read `apps/api/src/routes/diarization.ts:300-345` (surrounding context for L323-324 + L336-340) to confirm:
       - L323-324 is the `failed`/`cancelled` branch with the `throw new UpstreamError(...)`.
       - L336-340 is the 504 ceiling branch with `reply.code(504).send({error, jobId})` — the shape to mirror.
       - The 200 happy-path returns above L323.
    4. Record RED proof for the GREEN commit body. NO file modifications.

    WHY: pre-existing failures don't need a NEW red commit (D-28). This task confirms RED is real on HEAD and that the surgical 2-line edit at L323-324 won't collide with adjacent logic.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/unit/routes/__tests__/diarization.test.ts -t "status='failed'" -t "status='cancelled'" --reporter=verbose 2>&1 | grep -E "(FAIL|jobId|undefined)" | head -10</automated>
  </verify>
  <done>Both F4 and F5 confirmed RED; surrounding-line context understood; ready for surgical edit.</done>
</task>

<task type="auto">
  <name>Task 18.1-03-02: GREEN — replace throw with explicit reply.code(502).send</name>
  <files>
    - apps/api/src/routes/diarization.ts
  </files>
  <action>
    Per D-17 + D-30 + RESEARCH §1 row D-17 + PATTERNS §3.

    At `apps/api/src/routes/diarization.ts:323-324`, the current code is approximately:

    ```ts
    if (job.status === "failed" || job.status === "cancelled") {
      throw new UpstreamError("DIARIZATION_JOB_FAILED", `diarization job ${job.status}`);
    }
    ```

    Replace with:

    ```ts
    if (job.status === "failed" || job.status === "cancelled") {
      return reply.code(502).send({
        error: `diarization job ${job.status}`,
        jobId,
      });
    }
    ```

    Variable names:
    - `reply` — Fastify reply object, in scope (used by surrounding handler).
    - `jobId` — already declared earlier in the handler scope (used by the 504 branch at L336-340).
    - `job.status` — the upstream pyannote response field.

    If `UpstreamError` import becomes unused in this file post-edit, REMOVE the import line to keep lint green (biome lefthook will flag unused imports). If it is used elsewhere in the file, leave the import.

    **DO NOT** also "fix" any other branch in this file (D-17 surgical scope; RESEARCH §8 pitfall #2). Specifically:
    - The 504 ceiling branch at L336-340 ALREADY sends `jobId` — touch nothing there.
    - The 200 success path — touch nothing.
    - The pre-upload 4xx validation — touch nothing.
    - The central error handler in `apps/api/src/plugins/error-handler.ts` (or equivalent) — touch nothing (D-30).

    **Atomic GREEN commit message:**

    ```
    fix(18.1-03): green — diarization 502 carries jobId for failed/cancelled (F4/F5)

    Closes V2-TD-04 + V2-TD-05 (jobId lost in upstream-failed envelope):

    - apps/api/src/routes/diarization.ts:323-324 — replace
      `throw new UpstreamError("DIARIZATION_JOB_FAILED", ...)` with
      explicit `return reply.code(502).send({error, jobId})`. Mirrors
      the 504 ceiling branch shape at L336-340 (same file).
    - Central error handler NOT modified (per D-30 narrow-scope —
      other routes legitimately produce {error, code} envelope without
      jobId; broadening the envelope is a v3 concern).

    RED proof: pre-existing failures of `returns 502 envelope when
    pyannote job status='failed'` and `... status='cancelled'` at
    apps/api/tests/unit/routes/__tests__/diarization.test.ts (≈L415,
    L436) — both fail with `expect(json.jobId).toBe('job-1')` receiving
    undefined on main HEAD~1.

    Refs V2-TD-04, V2-TD-05.
    ```

    WHY (pitfalls):
    - Pitfall #2 (RESEARCH §8): scope tight to the two terminal statuses; do NOT also "fix" the 504 branch (already correct) or refactor the central handler (cross-route blast radius).
    - PATTERNS §3 confirms the 504 branch in the same file is the canonical mirror — same `reply.code(N).send({error, jobId})` shape, intentionally bypassing the central envelope for this surgical case.

    Lefthook surface: `biome` (formatter — must stay clean; possible unused-import removal), `english` (English-only content; commit message is English), `commitlint` (subject ≤ 100 chars), `phase-tag-comments` (only fires if a phase-tag comment is moved — this edit doesn't touch headers). `--no-verify` is FORBIDDEN (D-26); HALT and escalate on any unexpected fire.
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/unit/routes/__tests__/diarization.test.ts --reporter=verbose 2>&1 | tail -20 && grep -n "reply.code(502).send" apps/api/src/routes/diarization.ts</automated>
  </verify>
  <done>F4 + F5 GREEN; 504 branch byte-identical; central handler byte-identical; ONE atomic commit; lefthook clean.</done>
</task>

</tasks>

<test_strategy>
- Unit: 2 RED tests in `apps/api/tests/unit/routes/__tests__/diarization.test.ts` (F4 + F5) become GREEN; rest of file (200 happy-path + 4xx pre-upload + 504 ceiling) unaffected.
- Integration / E2E: none — narrow-scope edit, no infra changes.
- Coverage: diff is 1 branch replacement (≈ 4 lines net). The two existing tests cover both branches of the `||` condition (`failed` + `cancelled`), so the 4 new lines reach 100% line/branch/statement coverage. Function coverage unchanged. Coverage delta on the changed lines satisfies ≥90/90/90/90.
- Regression: `pnpm --filter @openwhispr/api test diarization` aggregate — confirm 200/504/pre-upload tests stay GREEN.
</test_strategy>

<verification>
```bash
cd /Users/nick/openwhispr-server
pnpm --filter @openwhispr/api test tests/unit/routes/__tests__/diarization.test.ts
# Expected: all tests GREEN, including the F4/F5 502 jobId assertions.

# Static: confirm only L323-324 area changed
git diff apps/api/src/routes/diarization.ts
# Expected: 1 hunk only; surrounding L336-340 504 branch unmodified.
```
</verification>

<success_criteria>
1. F4 + F5 GREEN.
2. The 504 ceiling branch (L336-340) is byte-identical to pre-plan.
3. Central error handler is byte-identical to pre-plan.
4. Only ONE function (the diarization route handler) modified; only ONE conditional branch rewritten.
5. ONE atomic GREEN commit (`fix(18.1-03): green — …`).
6. ZERO `--no-verify`; lefthook clean.
7. If `UpstreamError` becomes unused, its import is removed in the SAME commit (biome lint compliance).
</success_criteria>

<rollback>
If the GREEN commit breaks other diarization tests (unlikely — surgical scope):
- `git revert <SHA>`.
- Re-read `diarization.ts:300-345` to verify the variable `jobId` is in scope at L323 (it is — used at L336-340 in same handler; if not, the rewrite needs to source `jobId` from the upstream response object).
- Re-apply the fix with corrected variable sourcing.

Plan depends on 18.1-01 by execution order (D-27) but is file-disjoint, so revert affects only this plan.
</rollback>

<output>
After GREEN lands, create `.planning/phases/18.1-v2-test-debt-closure/18.1-03-SUMMARY.md` with:
- GREEN commit SHA + diff stats (1 file, ≈ 4 lines net).
- F4/F5 pre/post status (RED→GREEN).
- Confirmation 504 branch + central handler byte-identical.
- Lefthook result: `clean` expected.
- Note: matches PATTERNS §3 same-file 504 mirror; v3 should consider unifying envelope shape across routes.
</output>
</content>
</invoke>