---
phase: 18.1-v2-test-debt-closure
plan: 01
subsystem: test-infra
tags: [path-fix, source-contract, tdd, test-only]
requirements_complete: [V2-TD-01, V2-TD-02, V2-TD-03]
key_files:
  modified:
    - apps/worker/tests/unit/jobs/partman-maintenance.test.ts
    - apps/worker/tests/unit/lib/with-system-context.test.ts
    - apps/api/tests/unit/routes/agent/stream.test.ts
metrics:
  commits: 6
  duration_min: ~5
  completed_date: 2026-05-15
---

# Phase 18.1 Plan 01: path-fix 3 source-contract ENOENTs Summary

One-liner: rewrote 3 source-contract tests from stale colocated paths
to canonical `../../../src/...` form with `existsSync` guards (D-04);
6 atomic commits (3 RED + 3 GREEN per D-28).

## Commits

Pre-plan SHA: `0270ef1`

| Pair | RED SHA  | GREEN SHA | File                                                              |
| ---- | -------- | --------- | ----------------------------------------------------------------- |
| F1   | 65cbd62  | c2c4aa3   | apps/worker/tests/unit/jobs/partman-maintenance.test.ts           |
| F2   | 1a3a666  | 2877680   | apps/worker/tests/unit/lib/with-system-context.test.ts            |
| F3   | 582b7f2  | e97b43d   | apps/api/tests/unit/routes/agent/stream.test.ts                   |

`git log --oneline -n 6` confirms alternating `test(...)/fix(...)` pairs
across 18.1-01-01..03.

## Verification

```
pnpm --filter @openwhispr/worker test partman-maintenance + with-system-context
  → Test Files  2 passed (2) | Tests  14 passed (14)

pnpm --filter @openwhispr/api test tests/unit/routes/agent/stream.test.ts
  → Test Files  1 passed (1) | Tests  17 passed (17)
```

All three previously-RED source-contract tests are GREEN. F1 reads
`../../../src/jobs/partman-maintenance.ts`; F2 reads
`../../../src/lib/with-system-context.ts`; F3 reads
`../../../../src/routes/agent/stream.ts`. Each test guards
`readFileSync` with `existsSync` (D-04). `pnpm test` aggregate
failure count drops by exactly 3 (21 → 18) per RESEARCH §2.

## Deviations from Plan

None. Plan executed exactly as written.

## Lefthook + commit discipline

- ZERO `--no-verify`. All 6 commits passed biome + phase-tag-comments +
  english + commitlint hooks cleanly.
- One commitlint reflow needed on the first RED commit message
  (header > 100 chars + body-line length); reworded and committed —
  no hook bypass.
- `colocated-tests` skip hit (tests stay under `tests/unit/`, glob is
  `{apps,packages}/*/src/**/*.test.ts`) — expected per D-26.

## Audit trail

- `tools/lint-colocated-tests.ts` byte-identical to pre-plan baseline
  (untouched, per D-03).
- No production source file under `apps/*/src/` modified.
- No fixture relocation (D-05).
- STRUCT-01 preserved.

## Coverage delta

Test-file edits only; no production LOC modified. The three tests
themselves preserve source-contract coverage on
`partman-maintenance.ts`, `with-system-context.ts`, and `stream.ts`
(regression guards now actually read the real files instead of
silently ENOENT-ing).

## Self-Check: PASSED

- 6 commit SHAs present in `git log` (verified).
- 3 modified test files all GREEN under their workspace filter (verified).
- `tools/lint-colocated-tests.ts` untouched (not in diff list).
- D-28 literal two-commit shape honored.
