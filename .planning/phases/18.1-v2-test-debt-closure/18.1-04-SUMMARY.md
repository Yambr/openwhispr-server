---
phase: 18.1-v2-test-debt-closure
plan: 04
title: AccountClient duplicate "Active sessions" copy fix (F7)
status: complete
requirements_closed: [V2-TD-07]
commits:
  red: fafda69
  green: 41e2a94
start_sha: d6491ab
red_branch: B
files_changed:
  - apps/web/src/locales/en/end-user.json
  - apps/web/src/locales/ru/end-user.json
  - apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
files_byte_identical:
  - apps/web/src/components/screens/account/AccountClient.tsx
---

# Phase 18.1 Plan 04: AccountClient F7 Duplicate Copy Fix Summary

Closes V2-TD-07. F7 (`AccountClient.test.tsx:117`) flipped RED -> GREEN via a
2-commit TDD pair following D-28 literal shape.

## Commits

| Step  | SHA       | Subject                                                                     |
| ----- | --------- | --------------------------------------------------------------------------- |
| RED   | `fafda69` | `test(18.1-04): red — AccountClient F7 sharpened (red-baseline comment)`    |
| GREEN | `41e2a94` | `fix(18.1-04): green — AccountClient subtitle drops 'active sessions' (F7)` |

Start SHA: `d6491ab`. Diff stats (GREEN): 3 files changed, 10 insertions(+), 9 deletions(-).

## Branch chosen: B (fallback)

Per PLAN's decision tree: probing Branch A (replace `getByText` with
`getByRole("heading", …)` directly in RED) would have flipped the test GREEN
immediately, because the subtitle is rendered as `<p>` while
`SessionsTable.tsx:94` renders `<h2>Active sessions</h2>` — the `<h2>` is the
only heading-role match. Branch B therefore applied:

- RED commit (`fafda69`): added a `red-baseline` comment directly above L117
  documenting the diagnosed root cause + commit reference. No assertion
  change, no locale change. Test re-confirmed RED with verbatim failure
  output `Found multiple elements with the text: /Active sessions/i`.
- GREEN commit (`41e2a94`): locale rename (EN + RU) + L117 sharpen +
  fixture sync (L34).

## Verification output

```
$ pnpm vitest run src/components/screens/account/__tests__/AccountClient.test.tsx
 Test Files  1 passed (1)
      Tests  7 passed (7)
   Duration  1.11s
```

All 7 tests GREEN. Sibling tests unaffected.

## H2 heading text NOT deleted (UICONF-05 axe-baseline preserved)

- `apps/web/src/components/screens/account/SessionsTable.tsx:94, 146` still
  render `<h2>{t("end-user:end-user.account.sessions.title.label")}</h2>`.
- `apps/web/src/locales/en/end-user.json:74` still holds
  `"label": "Active sessions"` for that h2.
- The fix removed only the lowercase phrase from the subtitle copy
  (`/end-user/account/subtitle/body/text`), NOT the heading label.

## New subtitle strings (verbatim, for translator audit)

- EN (`apps/web/src/locales/en/end-user.json:79`):
  `"text": "Manage your profile and account."`
- RU (`apps/web/src/locales/ru/end-user.json:79`):
  Russian translation of "Manage your profile and account." (Cyrillic
  literal omitted from this English-only summary per DOCS-09; see file
  directly).

## AccountClient.tsx byte-identical

`git diff d6491ab..HEAD -- apps/web/src/components/screens/account/AccountClient.tsx` = 0 lines. Component reads subtitle via `t("end-user:end-user.account.subtitle.body.text")` (D-18 i18n indirection confirmed).

## Lefthook

Both commits ran the full lefthook chain (`biome`, `phase-tag-comments`,
`english`, `web-typecheck`, `commitlint`) with NO `--no-verify`. Notes:

- RED commit-msg first attempt failed `body-no-cyrillic` (DOCS-09) due to
  quoting the original Russian subtitle verbatim — retried with a
  descriptive English paraphrase. Clean on retry.
- GREEN commit-msg first attempt failed `header-max-length` (102 > 100) —
  retried with shorter header. Clean on retry.
- Biome reformatted the `getByRole` multiline `expect` into a one-liner
  during pre-commit; re-staged and the commit landed clean.

## Forward reference

UICONF-05 axe baseline is operator-side; this plan asserts the
static invariant (heading text byte-identical, only subtitle prose
changed) — no regression risk to the visual baseline.

## Self-Check: PASSED

- RED `fafda69` exists: FOUND.
- GREEN `41e2a94` exists: FOUND.
- Test file modified: FOUND.
- Both locales modified: FOUND.
- AccountClient.tsx byte-identical: CONFIRMED.
- 7/7 tests GREEN: CONFIRMED.
