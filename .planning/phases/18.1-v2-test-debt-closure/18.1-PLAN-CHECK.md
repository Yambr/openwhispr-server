# Phase 18.1 — Plan Check

**Checked:** 2026-05-15
**Checker:** gsd-plan-checker (goal-backward, adversarial stance)
**Plans:** 7 (commit `1f0633d`)
**Verdict:** **NEEDS-WORK**

---

## Coverage Matrix (Success Criteria → Plans)

| # | Success Criterion (ROADMAP L824-836) | Plan | Task | Verification | Status |
|---|---|---|---|---|---|
| 1 | `pnpm test` exit 0; 7 failures GREEN | 01, 02, 03, 04 | 01-01..03, 02-02, 03-02, 04-02 | per-failure `pnpm vitest run …` + aggregate in 07-01 | **COVERED** |
| 2 | 4 stale `@expected-red` tags REPOINTed; `lint-cjm-doc` Mode-3 stays GREEN | 05 | 05-01 | `pnpm test:lint-cjm-doc` | **COVERED** |
| 3 | Path bugs fixed forward-compat (`import.meta.url` / package-relative) + lint guard OR exemption | 01 | 01-01..03 | `existsSync` guard + canonical analog | **COVERED** (D-04 `existsSync` is the forward-compat mechanism; D-03 explicitly rejects lint exemption — `tools/lint-colocated-tests.ts` stays byte-identical) |
| 4 | Diarization 502 + `jobId` for `failed` AND `cancelled` | 03 | 03-02 | `pnpm vitest run diarization` | **COVERED** |
| 5 | Rate-limit 401 BEFORE hook; `owrl:ip:*` not created by anon | 02 | 02-02 | `pnpm vitest run rate-limit-isolation` | **COVERED** |
| 6 | AccountClient "Active sessions" exactly once | 04 | 04-02 | `pnpm vitest run AccountClient` + grep | **COVERED** |
| 7 | Phase 12 + 14 REVIEW/SECURITY artefacts backfilled | 06 | 06-01..05 | file existence + LOC cap + grep surfaces | **COVERED** |
| 8 | ROADMAP Progress Table reflects v2 state; 18.1 added | 07 | 07-02 | grep ROADMAP + STATE | **COVERED** |
| 9 | Pre-existing typecheck failures (`deferred-items.md §14-04`) fixed OR re-confirmed deferred-with-justification | **NONE** | — | — | **NOT COVERED** |

---

## Findings

### BLOCKER

**B1. SC9 (typecheck re-confirm) has zero coverage.**
- ROADMAP L836 demands: "Pre-existing typecheck failures cataloged in `.planning/deferred-items.md` §14-04 are EITHER fixed in this phase OR re-confirmed deferred-with-justification (no silent rot)."
- CONTEXT.md `<deferred>` says "Out of 18.1 scope — separate Phase 18.2." That is a planner-side OPINION; ROADMAP demands the deferred-items.md entry be RE-CONFIRMED in-phase (with explicit justification + dated note). No plan touches `deferred-items.md §14-04`.
- Plan 05 appends 4 NEW sections (stale-tag REPOINT) but does not edit §14-04.
- Fix: add a task to Plan 05 (or new Plan 07-extension) that appends a 2-3 line "Re-confirmed deferred 2026-05-15 during Phase 18.1 — typecheck fix-up moves to Phase 18.2 (separate work)." note to deferred-items.md §14-04. Without this edit, SC9 is silently dropped → "no silent rot" rule violated.

**B2. D-28 TDD shape inverted by planner.**
- D-28 (CONTEXT.md): *"Two-commit TDD per fix, NOT single atomic. Each fix lands as `test(18.1-NN): red — <assertion>` followed immediately by `fix(18.1-NN): green — <intervention>`. Explicit RED proof in CI logs."*
- Plan 01 Task 01-01 invents a "pre-existing-failure rule": *"NOTE: this is the per-CONTEXT-D-28 wording 'for pre-existing failures, the existing RED stands as proof; the GREEN commit is the single landed change.'"* — **that wording does not exist in CONTEXT.md**. D-28 contains NO such exception. Plans 02, 03, 04 all parrot the same fabricated exception ("pre-existing RED stands as proof; ONE GREEN commit").
- Consequence: Plans 01-04 produce 4 fix-pairs as **GREEN-only commits**, not RED+GREEN pairs. There is no in-repo RED-test SHA to bisect against; the "explicit RED proof in CI logs" promised by D-28 is missing. Phase 17-fix precedent (`be8ed57`+`4118d26`) cited by D-28 IS a two-commit pair on top of an EXISTING red — the RED commit re-asserted the failure even when pre-existing. Planner shortcut diverges from precedent.
- Fix path: either (a) honor D-28 literally — each fix lands a `test(18.1-NN): red` commit that re-runs/re-asserts the failing test (even if it was already failing — the commit message + CI log are the proof artefact), then the `fix(NN): green` commit; OR (b) escalate to user for explicit waiver of D-28 for pre-existing failures (single-commit interpretation). Current plans do NEITHER — they self-grant the waiver inline.
- This is a CONSTITUTIONAL discipline breach. CLAUDE.md rule #1 (strict TDD) + PROJECT.md Engineering Discipline both forbid silent simplification.

### WARNING

**W1. Plan 02 omits planner-surfaced safeguard for stale-comment lefthook risk.**
- Risk surface explicitly flagged in RESEARCH §6: "stale-comment edits at `assemblyai.ts:23-26` + `rate-limit.ts:27-30`" may trip `phase-tag-comments` lefthook.
- Planner-surfaced risk in this checker prompt: "does the plan instruct executor to run `lefthook run pre-commit --dry-run` before committing?"
- Plan 02 instructs body-only editing and HALT on fire (D-26) — that is REACTIVE. The proactive `--dry-run` step is NOT in the task action.
- Fix: add a step to Task 02-02 between Edit 5 and the commit: `lefthook run pre-commit --dry-run --files apps/api/src/plugins/rate-limit.ts apps/api/src/routes/tokens/assemblyai.ts` and only commit if exit 0.

**W2. Plan 06 HALT protocol lacks pre-defined "new HIGH" criterion.**
- Each task in Plan 06 says "If a NEW HIGH surfaces, HALT and escalate" but does not pre-define HIGH severity.
- Mitigated by: `gsd-code-reviewer` + `gsd-security-auditor` agents have their own severity scales (`13-SECURITY.md` `HI-NN` precedent — agent classifies).
- Residual risk: if an agent emits a finding labeled "BLOCKER" or "CRITICAL" instead of HIGH, the HALT trigger might be ambiguous.
- Fix: prepend a one-line criterion to each Plan 06 task: "HALT trigger = any finding labelled HIGH, BLOCKER, or CRITICAL in the artefact's frontmatter `findings:` block."

**W3. Sequential wave graph wastes parallelism on file-disjoint plans.**
- Plans 01, 03, 04 are all file-disjoint (worker tests + api diarization + web locale/test). They could run in Wave 1 in parallel.
- Planner sequences them W1→W2→W3 per D-27. D-27 says "Order of execution (planner will sequence): paths-first, then diarization, then AccountClient, then rate-limit, then stale-tag REPOINT, then backfill." — this is GUIDANCE on order of attention, not a hard dependency contract.
- Impact: minor — Phase 18.1 is small; sequential wall-time penalty is ~10-15min. Not blocking.
- Fix optional: lower `wave:` on 03 and 04 to 1 (same wave as 01); `depends_on: []` on both.

**W4. Plan 06-05 verify command has `grep -q "FSL\|REUSE\|host-split\|history-scrub"` (OR) — passes if ANY ONE surface is mentioned.**
- The 6 D-23 surfaces (FSL/REUSE/relocation/host-split/scrub-runbook/DCO) should all be present. The OR-grep is too loose; a 15-SECURITY.md that ONLY covers FSL would pass.
- Fix: change to 5 sequential `grep -q` commands chained with `&&`, OR raise to plan-checker review post-artefact (already partially handled by `## Threat register` grep).

### INFO

**I1. Plan 01 commit subject shape inconsistent with Plans 02-07.**
- Plan 01 uses 3-part scope: `fix(18.1-01-01)`, `fix(18.1-01-02)`, `fix(18.1-01-03)`.
- Plans 02-07 use 2-part: `fix(18.1-02)`, `fix(18.1-03)`, `docs(18.1-05)`, etc.
- Per PATTERNS §10 the established convention is `fix(18.1-<NN>)` (2-part). Plan 01's 3-part is novel.
- Recommend: normalize Plan 01 to `fix(18.1-01)` for each commit (or accept 3-part as more granular for the 3 sub-fixes — both valid; commitlint accepts either).

**I2. PATTERNS.md §5 still references `19a-reset-mail` etc. (old suffix scheme).**
- CONTEXT.md D-11..D-14 uses suffix names (`19a-reset-mail`, `19b-stt-fixture`, etc.) but D-15+D-29 + RESEARCH §10 D-1 resolution chose Option (c): numeric sub-phases `19.1/19.2/19.3/19.4`.
- Plan 05 correctly uses `19.1/19.2/19.3/19.4` in all 8 edits.
- PATTERNS.md §5 still cites old `19a-reset-mail` etc. — cosmetic doc inconsistency. Not a blocker because plans are the executable artefact; planner already chose Option (c).
- Recommend: amend PATTERNS.md §5 in a follow-up doc commit (or fold into Plan 05's mass-edit). Low priority.

**I3. LOC density on 4 plans (02:293, 04:254, 05:322, 06:366) is justified.**
- Plan 02 density = commit body verbatim + 3-edit checklist + threat_model — appropriate for the SECURITY fix.
- Plan 04 density = 3 detailed edits + axe-preservation rationale + rollback for i18n-indirection-wrong scenario — appropriate.
- Plan 05 density = 8 file edits in ONE atomic mass-edit + full deferred-items.md appendix verbatim — necessary for the mechanical mass-edit shape (Discretion bullet 3).
- Plan 06 density = 5 sub-task spawn protocols + HALT protocol + 6 D-23 surface lists × 5 — necessary for orchestration.
- Verdict: NOT padding. The 250-LOC soft target is a guideline; these plans earn their density. No split required.

---

## Specific Validations

### TDD shape (D-28)

**FAIL.** Plans 01/02/03/04 fabricate a "pre-existing-failure exception" not present in D-28 (see B2). Plan 05 + 06 + 07 don't touch prod test code so D-28 N/A.

The planner's claim (Plan 01 Task 01-01 prose): "per-CONTEXT-D-28 wording 'for pre-existing failures, the existing RED stands as proof; the GREEN commit is the single landed change.'"

D-28 actual wording: *"each fix lands as `test(18.1-NN): red — <assertion>` followed immediately by `fix(18.1-NN): green — <intervention>`. Explicit RED proof in CI logs; easier bisect; matches the established `(NN-fix)` workflow shape."*

The planner inverted "explicit RED proof in CI logs" into "pre-existing CI log = proof, no RED commit needed." This is not a reading of D-28; it is a re-write of D-28.

Two acceptable resolutions:
1. **Honor D-28 literally** — each of the 4 fix-clusters lands a fresh `test(18.1-NN): red — re-assert failing path` commit (even though the test already exists, the RED commit IS the proof artefact pointed to by D-28). Then the `fix(NN): green` commit. 4 fix-clusters × 2 commits = 8 commits across plans 01-04 (plus the existing path/code edits).
2. **Escalate to user** — request explicit waiver of D-28 for pre-existing failures.

### Pitfall coverage (9/9)

All 9 RESEARCH §8 pitfalls appear as `WHY:` lines in covering tasks:
- P1 rate-limit DoS-shield → Plan 02 Task 02-02 WHY line 1 ✓
- P2 diarization scope → Plan 03 Task 03-02 WHY line 1 ✓
- P3 AccountClient i18n key not JSX → Plan 04 Task 04-02 WHY ✓ + Task 04-01 has HALT-if-wrong escape ✓
- P4 Mode-3 regex rejects suffix → Plan 05 Task 05-01 WHY line 1 + key_link ✓
- P5 stale comments bundled → Plan 02 Task 02-02 WHY line 2 ✓
- P6 Phase 15 SECURITY range includes follow-ups → Plan 06-05 must_haves + commit range ✓
- P7 phase-tag-comments lefthook on stale-comment → Plan 02 Task 02-02 WHY line 3 ✓
- P8 RU locale parity → Plan 04 Task 04-02 WHY line 2 ✓
- P9 existsSync guard → Plan 01 Tasks 01-01..03 must_haves + each task body ✓

**PASS.**

### Wave graph

Computed dependency closure from frontmatter:
- 01: W1, deps=[]
- 03: W2, deps=[01] — file-disjoint from 01; sequencing is execution-order, not file-blocking
- 04: W3, deps=[01, 03] — file-disjoint
- 02: W4, deps=[01, 03, 04] — file-disjoint
- 05: W5, deps=[02] — file-disjoint
- 06: W6, deps=[01..05] — file-disjoint
- 07: W7, deps=[01..06] — file-disjoint

Acyclic ✓. All deps resolve ✓. Wave numbers consistent with max(deps)+1 ✓.

**PASS** (with W3 INFO: parallelism wasted).

### HALT protocol (18.1-06)

**PASS.** Each task in Plan 06 has explicit HALT-on-HIGH instructions. `<rollback>` block of 06 codifies the 3 user-choice branches (sub-phase, accept-deferred, reopen). Specifics bullet 4 + D-24 honored.

Caveat W2: HIGH severity not pre-defined; relies on agent classification.

### LOC discretion (4 plans > 250)

**PASS-WITH-INFO** (see I3). Density justified per-plan; no padding identified. The 250-LOC cap from CONTEXT.md `<Claude's Discretion>` refers to BACKFILL ARTEFACT length (REVIEW.md, SECURITY.md), NOT plan length. Re-reading CONTEXT: *"Backfill artefact length cap: ≤ 250 LOC per artefact (matches `13-REVIEW.md` 202 + `15-REVIEW.md` 324 envelope)."* — explicitly artefact, not plan. Plan length is not capped.

### Scope discipline (no deferred prod code)

- Plan 05 does NOT add `sendResetPassword` / mock-litellm / i18n / locale-toggle impls ✓ (only regex + tag swap + docs)
- Plan 04 does NOT touch axe baseline ✓ (only locale JSON + test)
- Plan 02 does NOT refactor to two-tier rate-limit (D-09 Option C) ✓ (explicit DEFERRED note in commit body)
- Plan 06 does NOT silently land prod fixes on HIGH findings ✓ (HALT protocol)

**PASS.**

### Planner-surfaced risks

1. **18.1-02 stale-comment edit window** — Plan does NOT instruct `lefthook run pre-commit --dry-run` (W1). Reactive HALT only.
2. **18.1-04 i18n indirection assumption** — Plan 04 Task 04-01 step 3 includes HALT-and-escalate path if AccountClient.tsx contains inline literal instead of `t(...)`. ✓ HANDLED.
3. **18.1-06 HIGH-escalation cascade** — HALT protocol present; HIGH criterion not pre-defined (W2).
4. **4 plans > 250 LOC** — density justified (I3); not padding.

### Context Compliance (Dimension 7)

30 locked decisions in CONTEXT.md. Spot-check:
- D-01..D-05 (path fix): all covered in Plan 01 ✓
- D-06..D-09 (rate-limit): all covered in Plan 02 ✓
- D-10..D-16 (stale tags): all in Plan 05 ✓ (uses 19.1/19.2/19.3/19.4 per D-29 resolution)
- D-17 (diarization): Plan 03 ✓
- D-18 (AccountClient): Plan 04 ✓
- D-19..D-24 (backfill): Plan 06 ✓
- D-25..D-27 (test strategy + order): Plan 01-06 ✓ (order honored W1→W6)
- D-28 (two-commit TDD): **VIOLATED by Plans 01-04** (B2)
- D-29 (regex extension): Plan 05 ✓
- D-30 (diarization fix site): Plan 03 ✓

**FAIL** on D-28; all other decisions COMPLIANT.

### Architectural tier (Dimension 7c)

No `## Architectural Responsibility Map` in RESEARCH.md. **SKIPPED.**

### Cross-plan data contracts (Dimension 9)

No shared data pipelines across plans. **N/A.**

### Research resolution (Dimension 11)

RESEARCH §10 lists 3 discrepancies (D-1, D-2, D-3). Each is RESOLVED:
- D-1 (Mode-3 regex) → resolved by CONTEXT.md D-29 (regex extension) + Plan 05 ✓
- D-2 (Phase 15 TLS miscategorisation) → resolved by Plan 06-05 explicit "NOT TLS" note ✓
- D-3 (diarization location TBC) → resolved by CONTEXT.md D-30 + Plan 03 ✓

**PASS.**

### CLAUDE.md compliance (Dimension 10)

- "НЕ УПРОЩАТЬ ЗАДАЧИ" — **VIOLATED** by B2 (planner simplified D-28 to single-commit shape for pre-existing failures; that is "упрощение" of the constitutional TDD discipline).
- "НЕ ИСПОЛЬЗОВАТЬ МОКИ" — N/A (no mocks introduced).
- "no `--no-verify`" — explicitly preserved in every plan ✓.
- Strict TDD — **VIOLATED** by B2 (RED commit dropped on pre-existing failures).

**FAIL** on Russian-prefaced rule + strict-TDD discipline rule.

---

## Recommendation

**NEEDS-WORK — return to planner with 2 BLOCKERS + 4 WARNINGS.**

**Bounce-back ask (planner — exact items to fix before execute):**

1. **(BLOCKER B1)** Add an SC9-closing task. Recommend: append to Plan 05 (mass-edit commit already touches `deferred-items.md` — extend the same commit) a 3-line entry under existing §14-04: *"Re-confirmed deferred 2026-05-15 during Phase 18.1: typecheck red across 7+ files (apps/api + apps/worker + packages/litellm-client) remains out of scope; addressed in Phase 18.2 — no silent rot per ROADMAP SC9."* Update Plan 05 frontmatter `requirements:` to add `V2-TD-16 # SC9 typecheck deferred re-confirm`.

2. **(BLOCKER B2)** Resolve D-28 interpretation: either
   - (a) revise Plans 01/02/03/04 to land an explicit `test(18.1-NN): red — re-assert <failure>` commit before each `fix(NN): green` commit (8 commits total across the 4 plans), removing the fabricated "pre-existing-failure rule" prose; OR
   - (b) escalate to user with a one-pager asking for explicit D-28 waiver for the pre-existing-failure case, and document the waiver in CONTEXT.md before re-running plan-check.
   Do NOT silently keep the current shape. CLAUDE.md "НЕ УПРОЩАТЬ" forbids it.

3. **(WARNING W1)** Add to Plan 02 Task 02-02, immediately before the commit step: `lefthook run pre-commit --dry-run --files apps/api/src/plugins/rate-limit.ts apps/api/src/routes/tokens/assemblyai.ts apps/api/src/routes/tokens/deepgram.ts apps/api/src/routes/tokens/openai-realtime.ts` — only commit on exit 0.

4. **(WARNING W2)** Prepend to Plan 06 tasks 06-01..05: "HALT trigger = any finding classified HIGH, BLOCKER, or CRITICAL in the artefact's frontmatter `findings:` block; MEDIUM/LOW/INFO do not trip HALT."

5. **(WARNING W4)** Tighten Plan 06-05 verify command to chained `grep -q` for all 6 D-23 Phase-15 surfaces (FSL ∧ REUSE ∧ relocation ∧ host-split ∧ scrub-runbook ∧ DCO).

6. **(WARNING W3 — optional)** Re-evaluate parallelism: lower 03+04 to Wave 1 (file-disjoint with 01); keep 02 in Wave 2 (depends on 01 + audit of 01's behaviour). Saves ~10min wall-time. Acceptable as-is if planner prefers strict D-27 ordering.

**Execute-ready status:** **NOT READY.** Two blockers must close before `/gsd-execute-phase 18.1`. Re-spawn planner with the bounce-back ask; expect 1 revision loop. Once B1+B2 resolved, plan-check re-run should PASS-WITH-NOTES (warnings only) and is execute-ready.

---

_Plan-checked: 2026-05-15 — gsd-plan-checker (goal-backward, FORCE stance, 7 plans, 9 SCs, 30 decisions, 9 pitfalls, 3 research discrepancies)._
