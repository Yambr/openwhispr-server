---
phase: 18.1-v2-test-debt-closure
plan: 06
type: execute
wave: 6
depends_on: ["18.1-01", "18.1-02", "18.1-03", "18.1-04", "18.1-05"]
files_modified:
  - .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md
  - .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md
  - .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md
  - .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md
  - .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md
autonomous: false  # checkpoint:human-action — agent spawns require human-mediated handoff
requirements:
  - V2-TD-10  # Constitutional rule #10 (audit-trail) — Phase 12 REVIEW backfill
  - V2-TD-11  # Phase 12 SECURITY backfill
  - V2-TD-12  # Phase 14 REVIEW backfill
  - V2-TD-13  # Phase 14 SECURITY backfill
  - V2-TD-14  # Phase 15 SECURITY backfill
must_haves:
  truths:
    - "`.planning/phases/12-.../12-REVIEW.md` exists; produced by a FRESH `gsd-code-reviewer` spawn (D-19; no participation in original Phase 12 execution); scoped to commits `9763a91..5af4f6a` (D-20); ≤ 250 LOC (Discretion bullet 2; matches 13-REVIEW.md envelope, not 15-REVIEW's 324); covers the 8 high-risk surfaces from D-23 (setup_state singleton, admin bootstrap, users.role enum, /api/setup-state rate-limit, /api/auth/providers no-secret echo, /api/capabilities tenant ETag, /setup CSRF, role-promotion break-glass)."
    - "`.planning/phases/12-.../12-SECURITY.md` exists; produced by a FRESH `gsd-security-auditor` spawn (adversarial stance — every mitigation grep-verified per `13-SECURITY.md:7`); scoped to same `9763a91..5af4f6a`; ≤ 250 LOC; threat register table populated; covers the 8 D-23 surfaces with `VERIFIED` evidence lines (file:line + grep citations)."
    - "`.planning/phases/14-.../14-REVIEW.md` exists; FRESH `gsd-code-reviewer` spawn; scoped to commits `dd44c3f..807d3dd` (D-21); ≤ 250 LOC; covers the 6 D-23 Phase 14 surfaces (byok-guard wiring order, loud-fail on placeholders, virtual-key rotation worker removal completeness, six compose overlays + ingress ports, Helm BYOK secret refs, BYOK e2e-cjm scenarios actually trip the guard)."
    - "`.planning/phases/14-.../14-SECURITY.md` exists; FRESH `gsd-security-auditor` spawn; same commit range; ≤ 250 LOC."
    - "`.planning/phases/15-.../15-SECURITY.md` exists; FRESH `gsd-security-auditor` spawn; scoped to `2499435..0735965` (RESEARCH §4 note; includes 3 HIGH-fix follow-ups `f523184`+`508041d`+`0735965` so auditor sees the closed state); ≤ 250 LOC; covers Phase 15 SECURITY surface = FSL relicense + REUSE 3.3 gate + 220-test relocation + Traefik host-split + history-scrub runbook (per D-23 corrected by RESEARCH §10 D-2 — TLS belongs to Phase 17, NOT 15)."
    - "Each artefact lands as ITS OWN atomic commit (one per artefact) — 5 commits total in this plan. Commit subjects follow `docs(18.1-06): backfill <phase>-{REVIEW,SECURITY}.md (<phase>-fresh-audit)` shape."
    - "If a backfill agent surfaces ANY new HIGH finding, the executor HALTs and escalates per D-24 + Specifics bullet 4 (deviation protocol) — does NOT silently land production-code changes in 18.1. New HIGHs spin a sub-phase 18.1.x or are noted in the artefact's `## Fixes Applied` section as `pending — escalated YYYY-MM-DD`."
    - "ZERO `--no-verify` on any of the 5 artefact commits; `english` + `commitlint` clean; markdown files are out of `biome` glob so biome does NOT fire; `phase-tag-comments` does NOT fire on `.md` (glob is `{apps,packages}/**/*.{ts,tsx}`)."
  artifacts:
    - path: ".planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md"
      provides: "Adversarial code review of Phase 12; scoped 9763a91..5af4f6a; 8 high-risk surfaces audited."
      contains: "Phase 12"
    - path: ".planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md"
      provides: "Threat register verification + prompt-supplied surface verification for Phase 12; adversarial stance."
      contains: "ASVS Level"
    - path: ".planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md"
      provides: "Adversarial code review of Phase 14; scoped dd44c3f..807d3dd; BYOK + overlays + Helm + byok-guard surfaces."
      contains: "Phase 14"
    - path: ".planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md"
      provides: "Phase 14 SECURITY audit."
      contains: "ASVS Level"
    - path: ".planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md"
      provides: "Phase 15 SECURITY audit; FSL + REUSE + relocation + host-split + scrub-runbook surface (NOT TLS — Phase 17)."
      contains: "FSL"
  key_links:
    - from: ".planning/phases/12-.../12-REVIEW.md"
      to: ".planning/phases/13-.../13-REVIEW.md (analog, 202 LOC)"
      via: "Copy section structure verbatim per PATTERNS §7: frontmatter (phase/reviewed/depth/files_reviewed/findings/status) → Summary → High/Medium/Low/Info issues → Findings Above HIGH → Fixes Applied → Reviewed/Reviewer/Depth footer."
      pattern: "## High Issues"
    - from: ".planning/phases/12-.../12-SECURITY.md"
      to: ".planning/phases/13-.../13-SECURITY.md (analog, 129 LOC)"
      via: "Copy section structure verbatim per PATTERNS §8: Executive verdict → Threat register verification table → Prompt-supplied surface verification → Observations LOW → Unregistered flags → Coverage/completeness → Recommendation."
      pattern: "## Threat register verification"
    - from: "agent spawns (gsd-code-reviewer + gsd-security-auditor)"
      to: "~/.claude/agents/gsd-code-reviewer.md + ~/.claude/agents/gsd-security-auditor.md"
      via: "Fresh-context spawn per D-19 — agent must NOT have participated in original phase execution; ensures adversarial-stance preservation (13-SECURITY.md:7)."
      pattern: "fresh agent spawn"
---

<objective>
Backfill the 5 missing audit artefacts (constitutional rule #10 — audit-trail
enforcement). Per D-19: spawn `gsd-code-reviewer` and `gsd-security-auditor`
as FRESH agent contexts per-phase (NOT batched into one agent doing all 5 —
each agent gets a clean context, gets the surface scope handed to it, and
produces ONE artefact).

This is an ORCHESTRATOR plan: it does not author the artefact content
itself (that's the agents' job). It defines the spawn protocol, the scope
boundaries each agent receives, the D-23 high-risk surface lists they
must cover, and the commit shape per artefact.

Per Specifics bullet 4 + D-24: if an agent surfaces a NEW HIGH finding,
HALT and escalate. Do NOT silently land production-code changes. The
artefact lands with `## Fixes Applied` empty + a `pending — escalated`
note; the HIGH becomes a sub-phase 18.1.x (e.g. `18.1.1-phase12-hi01-fix`).

Wave 6 — last wave before ROADMAP sync (18.1-07). Depends on all prior
plans by D-27 execution order, though file-disjoint. Per Specifics
bullet 3: 5 agent spawns run in parallel (~30-45min each; total
wall-time ≈ longest single run, not sum). The 5 artefact-landing commits
are sequential (one per agent return).

Output: 5 atomic commits — one per artefact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md
@CLAUDE.md

@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-REVIEW.md
@.planning/phases/13-e2e-cjm-harness-v2-ships-first/13-SECURITY.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-REVIEW.md
@.planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-VERIFICATION.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-VERIFICATION.md
</files_to_read>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 18.1-06-00: Confirm parallel agent-spawn capacity (pre-spawn checkpoint)</name>
  <what-built>This is a pre-spawn synchronization point — 5 fresh agent contexts will be invoked next. Confirm the executor's environment supports parallel `Task`-tool spawns (or sequential if the orchestrator does not support parallelism). If sequential, expect ~5× the wall-time.</what-built>
  <how-to-verify>
    1. Verify `~/.claude/agents/gsd-code-reviewer.md` exists.
    2. Verify `~/.claude/agents/gsd-security-auditor.md` exists.
    3. Confirm with user: spawn all 5 agents in parallel, or sequentially? (Default: parallel per Specifics bullet 3.)
  </how-to-verify>
  <resume-signal>Type "parallel" or "sequential" or describe constraint.</resume-signal>
</task>

<task type="auto">
  <name>Task 18.1-06-01: Spawn fresh `gsd-code-reviewer` → 12-REVIEW.md</name>
  <files>
    - .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md
  </files>
  <action>
    Per D-19, D-20, D-23 + PATTERNS §7.

    Invoke the `gsd-code-reviewer` agent (fresh context). Prompt structure:

    ```
    You are gsd-code-reviewer. Perform an adversarial code review of Phase 12
    (admin onboarding wizard + UI-SPEC conformance audit). Scope: commits
    9763a91..5af4f6a (first: feat(12-01) setup_state singleton + users.role;
    last: docs(12-05b) UICONF-05 axe baseline). The phase shipped without a
    code review gate — this is a backfill per constitutional rule #10.

    Output: .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md
    Template: copy section structure verbatim from
    .planning/phases/13-e2e-cjm-harness-v2-ships-first/13-REVIEW.md
    (202 LOC envelope). HARD CAP: ≤ 250 LOC.

    Surfaces to audit (D-23):
    1. setup_state singleton lifecycle/race (apps/api/src/routes/setup-state.ts).
    2. Admin bootstrap secret handling (apps/api/src/routes/setup-admin.ts).
    3. users.role enum + Better Auth additionalFields.role input:false (apps/api/src/auth.ts).
    4. GET /api/setup-state public rate-limit.
    5. GET /api/auth/providers — no provider-secret echo.
    6. GET /api/capabilities — tenant-scoped ETag.
    7. /setup wizard CSRF/origin (apps/web/src/app/(public)/setup/page.tsx).
    8. Role-promotion break-glass operator footgun.

    Adversarial stance: every claim must be grep-verified against live code at HEAD.

    If a NEW HIGH finding surfaces, return it in the `## High Issues` section
    AND signal HALT — orchestrator will escalate before any production fix.
    ```

    Wait for agent return. The agent writes `12-REVIEW.md` directly to the phase dir.

    **Atomic commit:** `docs(18.1-06): backfill 12-REVIEW.md (fresh adversarial review, scoped 9763a91..5af4f6a)`

    Commit body includes: scope range, surface list, finding totals from frontmatter, escalation status (none / `## High Issues` count if any).

    **HALT protocol (D-24):** if HIGH count > 0, do NOT immediately spin a fix. Surface to user with `## ⚠ HIGH finding in 12-REVIEW.md` block in the planning return; await user direction (sub-phase 18.1.1-phase12-hiNN-fix vs. accept-as-deferred).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md && wc -l .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md | awk '$1 <= 250 {exit 0} {exit 1}' && grep -q "setup_state" .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md</automated>
  </verify>
  <done>12-REVIEW.md exists; ≤ 250 LOC; covers 8 D-23 surfaces; finding totals frontmatter populated; 1 atomic commit.</done>
</task>

<task type="auto">
  <name>Task 18.1-06-02: Spawn fresh `gsd-security-auditor` → 12-SECURITY.md</name>
  <files>
    - .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md
  </files>
  <action>
    Per D-19, D-20, D-23 + PATTERNS §8.

    Invoke `gsd-security-auditor` (fresh context, not the same agent instance as 18.1-06-01). Prompt structure:

    ```
    You are gsd-security-auditor. Adversarial security audit of Phase 12.
    Scope: commits 9763a91..5af4f6a. Stance: every mitigation grep-verified
    in implementation (per 13-SECURITY.md:7).

    Output: .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md
    Template: copy from .planning/phases/13-.../13-SECURITY.md (129 LOC envelope).
    HARD CAP: ≤ 250 LOC. ASVS target: Level 2.

    Sections required:
    - Executive verdict
    - Threat register verification (table; pull every threat ID from
      12-0[1-5]-PLAN.md <threat_model> blocks; for each: Category, Disposition,
      Status (VERIFIED/UNVERIFIED), Evidence (grep -n or file:line))
    - Prompt-supplied surface verification (numbered subsections, each ending
      "Status: VERIFIED." with 1-3 evidence bullets)
    - Observations (LOW, non-blocking)
    - Unregistered flags
    - Coverage / completeness
    - Recommendation

    Surfaces (D-23) — same 8 as 12-REVIEW.md (setup_state, admin bootstrap,
    users.role, /api/setup-state RL, /api/auth/providers, /api/capabilities,
    /setup CSRF, role-promotion break-glass).

    If a NEW HIGH surfaces, signal HALT for escalation.
    ```

    Commit: `docs(18.1-06): backfill 12-SECURITY.md (fresh adversarial security audit, scoped 9763a91..5af4f6a)`.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md && grep -q "ASVS Level" .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md && grep -q "Threat register verification" .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md && wc -l .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md | awk '$1 <= 250 {exit 0} {exit 1}'</automated>
  </verify>
  <done>12-SECURITY.md exists; ≤ 250 LOC; threat register table populated with VERIFIED evidence; 1 atomic commit.</done>
</task>

<task type="auto">
  <name>Task 18.1-06-03: Spawn fresh `gsd-code-reviewer` → 14-REVIEW.md</name>
  <files>
    - .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md
  </files>
  <action>
    Per D-19, D-21, D-23 + PATTERNS §7.

    Invoke fresh `gsd-code-reviewer`. Scope: `dd44c3f..807d3dd` (first: test(14-01) slim-core base conformance; last: docs(14-07) e2e-cjm BYOK Gherkin coverage). Same template + 250 LOC cap.

    Surfaces (D-23):
    1. byok-guard wiring order BEFORE SSRF + OTel.
    2. Loud-fail on default/placeholder secrets in prod.
    3. LITELLM_VIRTUAL_KEY rotation worker removal completeness.
    4. Env surface migration notes.
    5. Six compose overlays + ingress `ports !reset` (no accidental `0.0.0.0` bind of admin surface).
    6. Helm BYOK toggles `values.yaml` secret refs.
    7. BYOK e2e-cjm scenarios actually trip the guard (not just present in features).

    Note pre-existing typecheck failures cataloged in deferred-items.md §14-04 — these are DEFERRED, NOT a Phase 14 HIGH (V2-MILESTONE-REVIEW already notes this). The reviewer must NOT re-flag them.

    Commit: `docs(18.1-06): backfill 14-REVIEW.md (fresh adversarial review, scoped dd44c3f..807d3dd)`.

    HALT protocol on any new HIGH.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md && wc -l .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md | awk '$1 <= 250 {exit 0} {exit 1}' && grep -q "byok" .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md</automated>
  </verify>
  <done>14-REVIEW.md exists; ≤ 250 LOC; covers 7 D-23 Phase-14 surfaces; deferred typecheck failures NOT re-flagged; 1 atomic commit.</done>
</task>

<task type="auto">
  <name>Task 18.1-06-04: Spawn fresh `gsd-security-auditor` → 14-SECURITY.md</name>
  <files>
    - .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md
  </files>
  <action>
    Per D-19, D-21, D-23 + PATTERNS §8.

    Fresh `gsd-security-auditor`. Scope same as 14-REVIEW.md. Surfaces same. Template same as 12-SECURITY.md (13-SECURITY.md envelope).

    Special focus: BYOK loud-fail surface is the security-critical part of Phase 14 — confirm the guard actually fires on placeholder secrets (`PLACEHOLDER_BOOTSTRAP_WILL_REPLACE`, `change-me`, etc.) BEFORE forwarding to upstream LLM providers; confirm rotation worker removal didn't leave a key-leak path.

    Commit: `docs(18.1-06): backfill 14-SECURITY.md (fresh adversarial security audit, scoped dd44c3f..807d3dd)`.

    HALT protocol on any new HIGH.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md && grep -q "byok" .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md && grep -q "Threat register" .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md && wc -l .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md | awk '$1 <= 250 {exit 0} {exit 1}'</automated>
  </verify>
  <done>14-SECURITY.md exists; ≤ 250 LOC; threat register populated; loud-fail guard verified by grep evidence; 1 atomic commit.</done>
</task>

<task type="auto">
  <name>Task 18.1-06-05: Spawn fresh `gsd-security-auditor` → 15-SECURITY.md</name>
  <files>
    - .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md
  </files>
  <action>
    Per D-19, D-22, D-23 (CORRECTED — RESEARCH §10 D-2) + PATTERNS §8.

    Fresh `gsd-security-auditor`. Scope: `2499435..0735965` (includes 3 HIGH-fix follow-ups `f523184` + `508041d` + `0735965` so the auditor sees the closed state — RESEARCH §4 note). Template same as 12/14-SECURITY.md.

    **CRITICAL surface correction (RESEARCH §10 D-2):** D-23 originally listed "Phase 15 TLS bootstrap two-tier CA chain" — this is MISCATEGORISED. TLS belongs to Phase 17. The Phase 15 SECURITY surface is:
    1. FSL relicense SPDX swath (every applicable file received an SPDX-License-Identifier).
    2. REUSE 3.3 compliance gate (`.github/workflows/reuse-lint.yml`).
    3. 220 test-file relocation correctness (STRUCT-01 — no test left colocated in `src/__tests__/` post-relocation).
    4. Traefik host-split SSRF surface (web.localhost vs api.localhost; no path-based fallback that could be exploited for cross-host inference).
    5. History-scrub runbook safety (per-invocation state-dir, GPG keyring precondition, no destructive default).
    6. DCO bot wiring (`.github/dco.yml` cutoff_sha + `CONTRIBUTING.md`).

    **NOT in surface:** TLS bootstrap, mkcert, ACME — those are Phase 17 territory.

    Note: 15-REVIEW.md ALREADY exists; only 15-SECURITY.md is the backfill (D-22). Do NOT touch 15-REVIEW.md.

    Commit: `docs(18.1-06): backfill 15-SECURITY.md (fresh adversarial security audit, scoped 2499435..0735965)`.

    HALT protocol on any new HIGH (FSL/REUSE/relocation/host-split/runbook scope only — do NOT raise TLS issues; those belong to Phase 17-SECURITY).
  </action>
  <verify>
    <automated>test -f .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md && grep -q "FSL\|REUSE\|host-split\|history-scrub" .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md && ! grep -q "TLS bootstrap" .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md && wc -l .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md | awk '$1 <= 250 {exit 0} {exit 1}'</automated>
  </verify>
  <done>15-SECURITY.md exists; ≤ 250 LOC; covers FSL/REUSE/relocation/host-split/scrub-runbook (NOT TLS); 15-REVIEW.md untouched; 1 atomic commit.</done>
</task>

</tasks>

<test_strategy>
- Unit / Integration / E2E: NONE — this plan is documentation-only.
- Coverage: ZERO prod LOC delta. The ≥90/90/90/90 floor (CLAUDE.md rule #2) does not apply.
- Cross-task verify: After all 5 artefacts land, `ls .planning/phases/{12,14,15}-*/{REVIEW,SECURITY}.md` (5 expected) confirms the backfill set complete (12-REVIEW + 12-SECURITY + 14-REVIEW + 14-SECURITY + 15-SECURITY; 15-REVIEW pre-exists; 13-REVIEW + 13-SECURITY pre-exist).
</test_strategy>

<verification>
```bash
cd /Users/nick/openwhispr-server

# All 5 artefacts present
test -f .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-REVIEW.md && \
test -f .planning/phases/12-admin-onboarding-wizard-ui-spec-conformance-audit-v2/12-SECURITY.md && \
test -f .planning/phases/14-slim-core-byok-profiles-v2/14-REVIEW.md && \
test -f .planning/phases/14-slim-core-byok-profiles-v2/14-SECURITY.md && \
test -f .planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-SECURITY.md

# Each ≤ 250 LOC
for f in .planning/phases/12-*/12-REVIEW.md .planning/phases/12-*/12-SECURITY.md .planning/phases/14-*/14-REVIEW.md .planning/phases/14-*/14-SECURITY.md .planning/phases/15-*/15-SECURITY.md; do
  echo "$f: $(wc -l <$f) LOC"
done

# Commit count: 5 commits with subject `docs(18.1-06): backfill ...`
git log --grep="docs(18.1-06): backfill" --oneline | wc -l   # expect 5
```
</verification>

<success_criteria>
1. 5 artefacts exist at the paths in `files_modified`.
2. Each artefact ≤ 250 LOC (Discretion bullet 2).
3. Each artefact was produced by a FRESH agent context (no participation in original phase execution — D-19).
4. Phase 12 REVIEW + SECURITY scoped to `9763a91..5af4f6a` (D-20).
5. Phase 14 REVIEW + SECURITY scoped to `dd44c3f..807d3dd` (D-21).
6. Phase 15 SECURITY scoped to `2499435..0735965` (D-22 + RESEARCH §4); Phase 15 SECURITY scope is FSL/REUSE/relocation/host-split/scrub-runbook (NOT TLS — RESEARCH §10 D-2).
7. 5 atomic commits — one per artefact; subjects `docs(18.1-06): backfill <phase>-{REVIEW,SECURITY}.md (fresh adversarial …, scoped <range>)`.
8. NO new HIGH findings silently fixed in 18.1 production code (HALT protocol per D-24 + Specifics bullet 4).
9. ZERO `--no-verify`; lefthook clean.
10. 15-REVIEW.md byte-identical to pre-plan baseline (already exists; not touched).
</success_criteria>

<rollback>
If an agent surfaces a new HIGH that demands an immediate fix:
- Do NOT silently land production code in 18.1.
- Add `## Fixes Applied (pending — escalated YYYY-MM-DD)` block to the artefact.
- Commit the artefact AS-IS (with the HIGH documented but unfixed).
- Return to user with `## ⚠ HIGH finding requires escalation` block; user decides:
  - (a) sub-phase `18.1.1-phaseNN-hiMM-fix` — spin a new TDD fix-pair plan.
  - (b) accept-as-deferred — append to `.planning/deferred-items.md`; tag for v3.
  - (c) reopen the closed phase (rare; user-only decision).

Per-artefact independence means one HALT does not block the other 4 — proceed with the remaining agent spawns.
</rollback>

<output>
After all 5 commits land, create `.planning/phases/18.1-v2-test-debt-closure/18.1-06-SUMMARY.md` with:
- 5 commit SHAs + LOC counts per artefact.
- Total finding counts per artefact (critical/blocker/high/medium/low/info).
- Escalations (if any) — list HIGH IDs + status (escalated / accept-deferred / sub-phase-spawned).
- Confirmation: 15-REVIEW.md byte-identical to pre-plan baseline.
- Forward-reference: 18.1-07 syncs ROADMAP Progress Table including this plan's completion.
- Lefthook results: `clean` expected on all 5 commits.
</output>
</content>
</invoke>