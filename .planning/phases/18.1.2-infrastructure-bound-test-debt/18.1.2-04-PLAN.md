---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 04
type: execute
wave: 4
depends_on: ["18.1.2-03"]
files_modified:
  - packages/byok-guard/src/index.ts
  - packages/byok-guard/tests/byok-guard.test.ts
  - apps/api/src/index.ts
  - apps/worker/src/index.ts
  - apps/api/tests/unit/__tests__/litellm-spike-request-id.test.ts
  - apps/api/src/otel-bootstrap.ts
  - apps/api/tests/unit/otel-bootstrap.test.ts
  - apps/web/src/locales/en/end-user.json
  - apps/web/src/locales/ru/end-user.json
  - apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
autonomous: true
requirements:
  - SR-5
  - SR-7
must_haves:
  truths:
    - "D-07 (BYOK): `packages/byok-guard/src/index.ts:242` now throws `new BYOKGuardError(record.message)` instead of `process.exit(1)`. `BYOKGuardError extends Error` is a named export."
    - "D-07 callers: `apps/api/src/index.ts:54-56` + `apps/worker/src/index.ts:7-9` wrap `assertBYOKConfig()` in `try { ... } catch (e) { logger.fatal(e); process.exit(1); }` mirroring `apps/api/src/index.ts:683-687` entrypoint pattern (PATTERNS surface 5)."
    - "D-07 transitively closes Δ-3: 2 `entrypoint-db-shape` test failures GREEN after refactor (RESEARCH §3 cascade). Acceptance MUST verify `pnpm --filter @openwhispr/api test entrypoint-db-shape` GREEN."
    - "D-07 TDD pair: RED test asserts `expect(() => assertBYOKConfig()).toThrow(BYOKGuardError)`; GREEN refactors library + both call sites in SAME commit."
    - "D-08 (audio fixture, Δ-1): `apps/api/tests/unit/__tests__/litellm-spike-request-id.test.ts:104` repoFile path-depth: 4 ups → 5 ups. File `tests/fixtures/audio/sample-1s.wav` already exists (NOT `sample-1sec.wav`; CONTEXT D-08 reworded by Δ-1). Single-GREEN per Phase 18.1 §7 + D-39."
    - "D-09 (otel onSignal): `apps/api/src/otel-bootstrap.ts:144-148` changes `const onSignal` → `export const onSignal`. Test refactor at `apps/api/tests/unit/otel-bootstrap.test.ts:125-131` imports `onSignal` + invokes directly + spies `shutdownSdk`. Single atomic commit (refactor + tiny library export — both required together)."
    - "D-10 (locale): `apps/web/src/locales/en/end-user.json:79` text → `\"Manage your profile, active sessions, and account deletion.\"` (UI-SPEC oracle :588). `ru/end-user.json:79` parallel: `\"Управление профилем, активными сессиями и удалением аккаунта.\"`. `AccountClient.test.tsx:34` fixture updated. **Single atomic commit (D-21 en+ru parity + component-test in SAME commit)**."
    - "Approximately 4-5 commits total: BYOK RED, BYOK GREEN (lib+callers+entrypoint-db-shape verify), audio fixture single-GREEN, otel onSignal single-GREEN, locale single-GREEN."
    - "Coverage ≥ 90/90/90/90 on new code (`BYOKGuardError`, `onSignal` export) per D-20."
    - "ZERO `--no-verify` (D-19); ≤ 7 commits (D-18); lefthook clean."
  artifacts:
    - path: "packages/byok-guard/src/index.ts"
      provides: "BYOKGuardError export + throw-not-exit refactor."
      contains: "class BYOKGuardError extends Error"
    - path: "apps/api/src/index.ts"
      provides: "try/catch wrap around assertBYOKConfig() at line ~54."
      contains: "catch.*BYOKGuardError\\|catch.*err"
    - path: "apps/worker/src/index.ts"
      provides: "try/catch wrap around assertBYOKConfig() at line ~7."
      contains: "catch.*err"
    - path: "apps/api/src/otel-bootstrap.ts"
      provides: "Named export onSignal."
      contains: "export const onSignal"
    - path: "apps/web/src/locales/en/end-user.json"
      provides: "Updated subtitle.body.text per UI-SPEC oracle."
      contains: "active sessions"
  key_links:
    - from: "test setup for index.ts importers (entrypoint-db-shape)"
      to: "BYOK envs (test-side)"
      via: "test setup MUST set required BYOK envs OR mock assertBYOKConfig (pitfall §7 — GREEN turning RED elsewhere)"
      pattern: "OPENWHISPR_BYOK"
    - from: "otel onSignal test"
      to: "shutdownSdk spy"
      via: "vi.spyOn(mod, 'shutdownSdk') + direct mod.onSignal() invocation (NOT process.emit)"
      pattern: "mod.onSignal"
---

<objective>
Close Bucket B + Bucket C surface failures (D-07 + D-08 + D-09 + D-10) + transitive Δ-3 closure (2 `entrypoint-db-shape` tests). Sequential after Plan 03. Sequential before Plan 05.

**4-5 commits** (D-18 ≤ 7 budget):
1. `test(18.1.2-04-01): red — BYOKGuardError thrown contract` (D-39 NEW)
2. `feat(18.1.2-04-02): green — BYOK throw not exit + api+worker entrypoint catch (D-07 → Δ-3 cascade)` (D-39 NEW)
3. `fix(18.1.2-04-03): green — litellm-spike-request-id audio fixture path depth 4→5 (Δ-1, D-08)` (D-39 single GREEN; pre-existing RED)
4. `fix(18.1.2-04-04): green — otel-bootstrap export onSignal + test invokes directly (D-09)` (D-39 single GREEN; export-only surface change; library export + test refactor together — both atomic)
5. `fix(18.1.2-04-05): green — locale en+ru parity + AccountClient fixture (D-10, D-21)` (D-39 single GREEN; D-21 i18n same-commit)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-SUMMARY.md
@CLAUDE.md
@packages/byok-guard/src/index.ts
@apps/api/src/index.ts
@apps/worker/src/index.ts
@apps/api/tests/unit/__tests__/entrypoint-db-shape.test.ts
@apps/api/tests/unit/__tests__/litellm-spike-request-id.test.ts
@apps/api/src/otel-bootstrap.ts
@apps/api/tests/unit/otel-bootstrap.test.ts
@apps/web/src/locales/en/end-user.json
@apps/web/src/locales/ru/end-user.json
@apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx
@apps/web/tests/unit/locales/__tests__/coverage.test.ts
@.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18.1.2-04-01: RED — BYOKGuardError thrown contract</name>
  <files>packages/byok-guard/tests/byok-guard.test.ts</files>
  <behavior>
- Test 1 (missing envs throw): `expect(() => assertBYOKConfig({})).toThrow(BYOKGuardError)`.
- Test 2 (error carries record): thrown `BYOKGuardError.message` matches the previously-logged record's message.
- Test 3 (valid envs no-throw): `expect(() => assertBYOKConfig(validEnv)).not.toThrow()`.
  </behavior>
  <action>
Per D-07 + D-39 RED leg + PATTERNS surface 5 + RESEARCH §3.

1. Locate the existing byok-guard test file (grep `expect.*assertBYOKConfig`); if none exists at `packages/byok-guard/tests/`, create one. Mirror the workspace's vitest setup pattern (check sibling packages).
2. Add `describe("BYOKGuardError thrown contract")` with Test 1-3 above. Import `assertBYOKConfig` + `BYOKGuardError` from `../src`. `BYOKGuardError` symbol does not exist yet — import is RED trigger.
3. Run `pnpm --filter @openwhispr/byok-guard test 2>&1 | tee /tmp/04-01-red.log`. MUST fail (named export missing OR `process.exit` trap).
4. Commit: `test(18.1.2-04-01): red — BYOKGuardError thrown contract`. Body MUST include verbatim RED.

WHY: pitfall §6 — current code `process.exit(1)`s the vitest worker; vitest traps as "process.exit unexpectedly called" — the contract `expect(...).toThrow(BYOKGuardError)` cannot be satisfied without the refactor. RED pins the desired contract first (D-39).
  </action>
  <verify><automated>pnpm --filter @openwhispr/byok-guard test 2>&1 | grep -E "(FAIL|Cannot find|process.exit|toThrow)" | head -5</automated></verify>
  <done>1 commit; failing byok-guard test present; verbatim RED in commit body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.2-04-02: GREEN — BYOK throw not exit + api+worker entrypoint catch (Δ-3 transitive)</name>
  <files>packages/byok-guard/src/index.ts, apps/api/src/index.ts, apps/worker/src/index.ts</files>
  <action>
Per D-07 + D-39 GREEN leg + PATTERNS surface 5 + RESEARCH §3 cascade.

1. Edit `packages/byok-guard/src/index.ts`:
   - Add named export `export class BYOKGuardError extends Error { constructor(message: string) { super(message); this.name = "BYOKGuardError"; } }`. Place near top of module, after imports.
   - At line 242 (currently `process.exit(1);`): replace with `throw new BYOKGuardError(record.message);` (or `String(record)` if record is not a `{message}` shape — verify by Reading).
   - Keep the `logger.fatal(record, ...)` call at line 241 INTACT (library can still log before throwing).
2. Edit `apps/api/src/index.ts:54-56`:
   - Wrap `assertBYOKConfig();` in `try { assertBYOKConfig(); } catch (err) { logger.fatal({ err }, "BYOK env missing for disabled overlay; refusing to start"); process.exit(1); }`. Use the boot-logger context already in scope (verify by Reading; if not, use Pino's root logger — NOT `console`).
3. Edit `apps/worker/src/index.ts:7-9`: same try/catch wrap.
4. Run **all three** verifications:
   - `pnpm --filter @openwhispr/byok-guard test` — Plan 04-01 RED is now GREEN.
   - `pnpm --filter @openwhispr/api test entrypoint-db-shape` — **Δ-3 transitive GREEN proof** (the 2 cascading tests are no longer trapped by `process.exit`).
   - `pnpm --filter @openwhispr/api test litellm-spike-request-id 2>&1 | head -10` — verify NOT broken by this commit (audio-fixture failure handled in 04-03).
5. Coverage gate: `pnpm --filter @openwhispr/byok-guard test --coverage` ≥ 90/90/90/90 on new code (D-20).
6. Pitfall §7 mitigation: search for any **other** test file importing `apps/api/src/index.ts` that may now see a thrown `BYOKGuardError`. If any new RED surfaces, set required BYOK envs in `apps/api/tests/setup.ts` (or whichever the test bootstrap is) within THIS SAME commit. Cite pitfall §7 in body.
7. Commit: `feat(18.1.2-04-02): green — BYOK throw not exit + api+worker entrypoint catch (Δ-3 transitive)`. Body MUST cite: D-07, PATTERNS surface 5, RESEARCH §3 cascade, pitfall §7 mitigation, **verbatim entrypoint-db-shape RED→GREEN delta**.

WHY: PATTERNS surface 5 + user-memory `feedback_no_workarounds_enterprise.md` — process-boundary failure belongs at entrypoint, not in a library. WHY: pitfall §6 — current `process.exit` trapped by vitest; library throw + entrypoint catch is the correct shape. WHY: pitfall §7 — refactor may BREAK other tests; mitigate in same commit (no leave-behind RED). WHY: Δ-3 — 2 entrypoint-db-shape fails are downstream of D-07; their GREEN MUST be verified to close Δ-3.
  </action>
  <verify><automated>pnpm --filter @openwhispr/byok-guard test 2>&1 | grep -E "(✓|PASS|passed)" | head -3 && pnpm --filter @openwhispr/api test entrypoint-db-shape 2>&1 | grep -E "(✓|PASS|passed)" | head -3</automated></verify>
  <done>1 commit; byok-guard test GREEN; entrypoint-db-shape GREEN (Δ-3 closed); api+worker entrypoints wrap in try/catch; coverage ≥ 90/90/90/90 on byok-guard new code; pitfall §7 mitigated same commit.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-04-03: GREEN — litellm-spike audio fixture path depth 4→5 (Δ-1)</name>
  <files>apps/api/tests/unit/__tests__/litellm-spike-request-id.test.ts</files>
  <action>
Per D-08 + Δ-1 (RESEARCH §9 Δ-1 + §4) + D-39 single GREEN (pre-existing RED).

1. Capture RED: `pnpm --filter @openwhispr/api test litellm-spike-request-id 2>&1 | tee /tmp/04-03-red.log`.
2. Verify fixture exists: `ls -la tests/fixtures/audio/sample-1s.wav` (32078 bytes, valid RIFF/WAVE). DO NOT create or rename — Δ-1 closure is a path-depth bug only.
3. Edit `apps/api/tests/unit/__tests__/litellm-spike-request-id.test.ts:104` (around lines 96-111):
   - Current: `const repoRoot = join(here, "..", "..", "..", "..");` (4 ups → resolves to `apps/`, WRONG)
   - Replace with: `const repoRoot = join(here, "..", "..", "..", "..", "..");` (5 ups → repo root)
4. Add existsSync guard above readFileSync of the fixture (Phase 18.1.1 D-04 carryover discipline): `if (!existsSync(fixturePath)) throw new Error(\`audio fixture missing: ${fixturePath}\`);`. Extend `node:fs` import if needed.
5. Re-run: `pnpm --filter @openwhispr/api test litellm-spike-request-id`. MUST exit 0.
6. Commit: `fix(18.1.2-04-03): green — litellm-spike audio fixture path depth 4→5 (Δ-1, D-08)`. Body MUST include: (a) verbatim RED, (b) Δ-1 citation (CONTEXT D-08 was reworded; file is `sample-1s.wav` not `sample-1sec.wav`; bug is path depth NOT file creation), (c) before/after path resolution proof.

WHY: pitfall §5 — CONTEXT D-08 was misleading ("missing fixture"); live verification shows file exists, path-depth is wrong. Δ-1 honor. WHY: Phase 18.1.1 D-04 carryover — every modified test gets `existsSync` guard.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test litellm-spike-request-id 2>&1 | grep -E "(✓|PASS|passed)" | head -3</automated></verify>
  <done>1 commit; litellm-spike-request-id GREEN; path depth corrected to 5 ups; existsSync guard added; commit body cites Δ-1.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-04-04: GREEN — otel-bootstrap export onSignal + test invokes directly (D-09)</name>
  <files>apps/api/src/otel-bootstrap.ts, apps/api/tests/unit/otel-bootstrap.test.ts</files>
  <action>
Per D-09 + D-39 single GREEN (export-only surface change; refactor + tiny library change atomic together) + PATTERNS surface 6 + RESEARCH §5.

1. Capture RED: `pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | tee /tmp/04-04-red.log`.
2. Edit `apps/api/src/otel-bootstrap.ts:144-148`:
   - Change `const onSignal = (): void => { void shutdownSdk(); };` → `export const onSignal = (): void => { void shutdownSdk(); };`.
   - Leave `process.once("SIGTERM", onSignal)` + `process.once("SIGINT", onSignal)` at L147-148 INTACT (production side-effect preserved; pitfall §8).
3. Edit `apps/api/tests/unit/otel-bootstrap.test.ts:125-131`:
   - Replace `expect(() => process.emit("SIGTERM" as never)).not.toThrow();` block with:
   ```ts
   const mod = await import("../../src/otel-bootstrap");
   const spy = vi.spyOn(mod, "shutdownSdk").mockResolvedValue(undefined);
   expect(() => mod.onSignal()).not.toThrow();
   expect(spy).toHaveBeenCalled();
   ```
   - Rename the surrounding `it("emitting SIGTERM ...")` → `it("onSignal invokes shutdownSdk", ...)`.
4. Re-run: `pnpm --filter @openwhispr/api test otel-bootstrap`. MUST exit 0.
5. Coverage check (D-20): `process.once` registrations at L147-148 may no longer be hit by the new test. If coverage report shows <90 on lines 147-148, add `/* c8 ignore next 2 */` or `/* istanbul ignore next 2 */` above the two `process.once` lines (pitfall §8). Document in commit body.
6. Commit: `fix(18.1.2-04-04): green — otel-bootstrap export onSignal + test invokes directly (D-09)`. Body MUST cite: D-09, PATTERNS surface 6, RESEARCH §5, verbatim RED, any coverage-ignore-pragma applied.

WHY: A2 finding — vitest worker registers competing SIGTERM handler `process.exit(143)` BEFORE shutdownSdk resolves; direct invocation bypasses signal race. WHY: PATTERNS surface 6 — named export discipline matches `tools/global-vitest-teardown.ts:64-83`. WHY: pitfall §8 — coverage ignore-pragma may be needed for `process.once` registrations.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | grep -E "(✓|PASS|passed)" | head -3 && grep -c "export const onSignal" apps/api/src/otel-bootstrap.ts</automated></verify>
  <done>1 commit; otel-bootstrap test GREEN; `onSignal` named-exported; production process.once registrations preserved; coverage ≥ 90/90/90/90 (with pragma if needed).</done>
</task>

<task type="auto">
  <name>Task 18.1.2-04-05: GREEN — locale en+ru parity + AccountClient fixture (D-10, D-21)</name>
  <files>apps/web/src/locales/en/end-user.json, apps/web/src/locales/ru/end-user.json, apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx</files>
  <action>
Per D-10 + D-21 (i18n en+ru parity SAME commit) + D-39 single GREEN + PATTERNS surface 7 + RESEARCH §6.

1. Capture RED: `pnpm --filter @openwhispr/web test coverage 2>&1 | tee /tmp/04-05-red.log`.
2. **Read first** `apps/web/tests/unit/locales/__tests__/coverage.test.ts:106` to determine which axis fails (key parity vs oracle-source comparison) — PATTERNS surface 7 risk. Document in commit body.
3. Edit `apps/web/src/locales/en/end-user.json:79`:
   - `"text": "Manage your profile and account."` → `"text": "Manage your profile, active sessions, and account deletion."` (verbatim from UI-SPEC oracle `.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md:588`).
4. Edit `apps/web/src/locales/ru/end-user.json:79`:
   - `"text": "Управление профилем и аккаунтом."` → `"text": "Управление профилем, активными сессиями и удалением аккаунта."` (RESEARCH §6 proposed ru translation).
5. Edit `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:34`:
   - `subtitle: { body: { text: "Manage your profile and account." } },` → `subtitle: { body: { text: "Manage your profile, active sessions, and account deletion." } },`.
6. Do NOT touch other `subtitle.body.text` keys at lines 177, 354, 491, 510 (PATTERNS surface 7 risk — out of scope).
7. Re-run: `pnpm --filter @openwhispr/web test coverage 2>&1 | head -10`. MUST exit 0. Also `pnpm --filter @openwhispr/web test AccountClient 2>&1` to confirm component test still passes with new fixture.
8. Single commit (D-21 mandatory same-commit): `fix(18.1.2-04-05): green — locale en+ru parity + AccountClient fixture (D-10, D-21)`. Body MUST cite: D-10, D-21, PATTERNS surface 7, UI-SPEC oracle :588, verbatim RED, coverage-test failing-axis identified.

WHY: D-21 — en + ru drift breaks coverage.test.ts unconditionally; MUST land same commit. WHY: pitfall §9 — RU proposed translation matches en structure; locale-COVERAGE asserts key presence (not content quality), so test passes. WHY: PATTERNS surface 7 — minimal-surface diff; do NOT touch other subtitle keys (out of scope).
  </action>
  <verify><automated>pnpm --filter @openwhispr/web test coverage 2>&1 | grep -E "(✓|PASS|passed)" | head -3 && grep -c "active sessions" apps/web/src/locales/en/end-user.json apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx</automated></verify>
  <done>1 commit; locale coverage test GREEN; en + ru + AccountClient fixture all updated; ZERO touched neighboring keys.</done>
</task>

</tasks>

<verification>
After 5 commits:

```bash
cd /Users/dev/openwhispr-server
pnpm --filter @openwhispr/byok-guard test
pnpm --filter @openwhispr/api test entrypoint-db-shape    # Δ-3 transitive proof
pnpm --filter @openwhispr/api test litellm-spike-request-id    # Δ-1
pnpm --filter @openwhispr/api test otel-bootstrap          # D-09
pnpm --filter @openwhispr/web test coverage                # D-10
git log --oneline -n 5 | grep -cE "(test|feat|fix)\(18\.1\.2-04-0[1-5]\)"   # 5
```
</verification>

<success_criteria>
1. 5 atomic commits (RED, BYOK GREEN, audio GREEN, otel GREEN, locale GREEN).
2. `BYOKGuardError` thrown not exit; api+worker entrypoints catch+log+exit.
3. Δ-3 transitively closed: entrypoint-db-shape (×2) GREEN after BYOK refactor.
4. Δ-1 honored: audio path depth 4→5 (no file creation).
5. `onSignal` exported; otel-bootstrap test invokes directly (no signal race).
6. en + ru + AccountClient fixture in SAME commit (D-21).
7. Coverage ≥ 90/90/90/90 on new code (D-20).
8. ZERO `--no-verify` (D-19); lefthook clean.
</success_criteria>

<rollback>
- BYOK refactor breaks unrelated test (e.g., a hidden importer of `index.ts`) → revert 04-02; investigate hidden importer; add env-setup or mock in test bootstrap; re-apply.
- Audio path 5-ups still fails because actual repo root differs (workspace-package context drift) → use `new URL('../../../../../tests/fixtures/audio/sample-1s.wav', import.meta.url)` shape (URL-relative resolution, depth-independent). Document fallback in commit body.
- onSignal coverage stays < 90 even with pragma → restructure: move handler-registration into separate `installSignalHandlers()` function + test it in isolation. Revert and re-apply.
- RU translation flagged by native review later → re-translate in follow-up commit; this plan's test passes on key presence.
- HALT recipe — if pitfall §7 surfaces unexpected RED in 5+ unrelated tests, HALT per 18.1-06 3-branch user-choice (revert / mass-mitigate / defer); do NOT push through.
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-04-SUMMARY.md` with:
- 5 commit SHAs
- Δ-3 closure proof (entrypoint-db-shape RED→GREEN delta)
- Δ-1 closure proof (4→5 depth fix)
- Coverage report on byok-guard + onSignal
- D-21 i18n same-commit verification
- Lefthook status (clean)
</output>
