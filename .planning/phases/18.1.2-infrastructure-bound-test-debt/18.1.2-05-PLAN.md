---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 05
type: execute
wave: 5
depends_on: ["18.1.2-04"]
files_modified:
  - apps/api/tests/unit/routes/__tests__/conversations-crud.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/conversations-messages.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/conversations-search.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/conversations-list-include-messages.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/notes-crud.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/notes-list.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/notes-search.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/notes-batch-create.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/notes-delete-all.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/folders-crud.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/folders-list.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/transcriptions-crud.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/transcriptions-batch.integration.test.ts
  - apps/api/tests/unit/routes/v1/keys/__tests__/keys-crud.integration.test.ts
  - apps/api/tests/unit/routes/v1/keys/__tests__/keys-revoke.integration.test.ts
  - apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts
  - .github/workflows/ci.yml
  - docs/operations.md
autonomous: true
requirements:
  - SR-1
  - SR-3
  - SR-4
  - SR-8
must_haves:
  truths:
    - "All 18 cluster #2 `*.integration.test.ts` files (Δ-2 corrected count) migrated to `getSharedPostgres` + `bootstrapSharedRoles` + per-file schema isolation per Plan 02/03 template."
    - "Cluster #2 = 18 files entirely in `apps/api/` (NOT worker/data per Δ-2 — those have zero `*.integration.test.ts`). Live grep enumeration MUST confirm count before commit."
    - "Migration is mechanical: identical shape to Plan 03 (D-03 + D-04). One commit per sub-cluster (conversations / notes / folders / transcriptions / v1-keys / tokens) — sub-cluster batching keeps plan ≤ 7 commits per D-18 + pitfall §12."
    - "Sub-cluster commits: (1) conversations 4 files, (2) notes 5 files, (3) folders 2 files, (4) transcriptions 2 files, (5) v1/keys 2 files + tokens 1 file (batched — small)."
    - "D-11 + D-12 CI/CD adoption: `.github/workflows/ci.yml` test job at L78-95 gains job-level `env: TESTCONTAINERS_RYUK_DISABLED: \"true\"` + pre-flight `docker version && docker info` step + post-step `if: always()` cleanup (orphan rm + volume prune) per A3 + PATTERNS surface 8 (mirror of e2e-phase6-quick L453-507)."
    - "D-13 docs: `docs/operations.md` carries new \"Local development test prerequisites\" section (Docker daemon, mkcert, pnpm-workspace order, `make tls-trust`). Single commit."
    - "ZERO `services: docker:dind` (A3 rejected). ZERO `--no-verify` (D-19). ZERO new deps (D-22)."
    - "End-of-plan aggregate: `pnpm --filter @openwhispr/api test 2>&1 | tail -5` shows 0 testcontainer-port-exhaustion failures on Docker-running machine (D-23 partial proof)."
    - "Commit budget: 5 (sub-clusters) + 1 (CI) + 1 (docs) = **7 commits exactly** (D-18 ceiling). HALT if a sub-cluster needs more than 1 commit."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Updated test job with TESTCONTAINERS_RYUK_DISABLED + pre-flight + cleanup."
      contains: "TESTCONTAINERS_RYUK_DISABLED"
    - path: "docs/operations.md"
      provides: "Local development test prerequisites section."
      contains: "Local development test prerequisites"
  key_links:
    - from: "Cluster #2 18 files"
      to: "apps/api/tests/support/shared-pg.ts (Plan 02 artefact)"
      via: "import getSharedPostgres + bootstrapSharedRoles in beforeAll"
      pattern: "getSharedPostgres"
    - from: ".github/workflows/ci.yml test job"
      to: "tools/global-vitest-teardown.ts:53-62 cleanup pattern"
      via: "post-step uses identical docker container prune args (PATTERNS surface 8)"
      pattern: "label=org.testcontainers"
---

<objective>
Close Bucket-A integration cluster #2 (18 files per Δ-2, RESEARCH §2) AND adopt CI/CD pattern (D-11/12/13). Largest plan in 18.1.2. Sequential after Plan 04. Sequential before Plan 06.

**EXACTLY 7 commits (D-18 ceiling, ENFORCE):**
1. `fix(18.1.2-05-01): migrate conversations integration (4 files) to shared-pg + schema isolation`
2. `fix(18.1.2-05-02): migrate notes integration (5 files) to shared-pg + schema isolation`
3. `fix(18.1.2-05-03): migrate folders integration (2 files) to shared-pg + schema isolation`
4. `fix(18.1.2-05-04): migrate transcriptions integration (2 files) to shared-pg + schema isolation`
5. `fix(18.1.2-05-05): migrate v1/keys + tokens integration (3 files) to shared-pg + schema isolation`
6. `ci(18.1.2-05-06): green — ci.yml test job Ryuk-disabled + Docker pre-flight + cleanup (D-11/12)`
7. `docs(18.1.2-05-07): operations.md local-dev test prerequisites (D-13)`

**HALT recipe (pitfall §12)**: if any sub-cluster exceeds 1 commit (e.g., file diverges from canonical boot), HALT + partial SUMMARY + fresh executor spawn for remaining work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-04-SUMMARY.md
@CLAUDE.md
@apps/api/tests/support/shared-pg.ts
@apps/api/tests/unit/routes/__tests__/usage.integration.test.ts
@.github/workflows/ci.yml
@tools/global-vitest-teardown.ts
@docs/operations.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1.2-05-00 (pre-flight enumerate): Live grep cluster #2 file list</name>
  <files></files>
  <action>
Per RESEARCH §9 Δ-2 + pitfall §12.

1. **Before any commit**, enumerate cluster #2 verbatim:
   ```bash
   cd /Users/dev/openwhispr-server
   find apps/api -name "*.integration.test.ts" -type f | sort > /tmp/05-cluster2-actual.txt
   wc -l /tmp/05-cluster2-actual.txt   # expect 22 total
   grep -v -E "(usage|streaming-usage|web-search|web-search-ratelimit)\.integration" /tmp/05-cluster2-actual.txt > /tmp/05-cluster2-only.txt
   wc -l /tmp/05-cluster2-only.txt     # expect 18
   ```
2. If count ≠ 18: **HALT** per 18.1-06 3-branch user-choice. Update SUMMARY with actual count + propose plan-budget revision before proceeding.
3. If count = 18: cross-check against the must_haves file list. Document any new file present in actual that's missing from frontmatter — add via Edit before commit 1.
4. Re-verify worker + data carry NO `*.integration.test.ts`: `find apps/worker packages/data -name "*.integration.test.ts" | wc -l` → 0 (Δ-2 honor).

NO COMMIT for this task. Pure pre-flight enumeration.

WHY: pitfall §12 — cluster #2 size unknown at plan-time without live grep; 18-file count is from RESEARCH §2 enumeration. WHY: Δ-2 — silent expansion forbidden per D-46 spirit.
  </action>
  <verify><automated>find apps/api -name "*.integration.test.ts" -type f | grep -v -E "(usage|streaming-usage|web-search|web-search-ratelimit)\.integration" | wc -l</automated></verify>
  <done>0 commits; tmp files capture actual count; cluster #2 = 18 confirmed or HALT.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-01: Migrate conversations integration (4 files)</name>
  <files>apps/api/tests/unit/routes/__tests__/conversations-crud.integration.test.ts, apps/api/tests/unit/routes/__tests__/conversations-messages.integration.test.ts, apps/api/tests/unit/routes/__tests__/conversations-search.integration.test.ts, apps/api/tests/unit/routes/__tests__/conversations-list-include-messages.integration.test.ts</files>
  <action>
Per D-03 + D-04 + Plan 03 template + D-39 single GREEN (pre-existing RED).

1. Capture RED for the 4 conversation files in one log.
2. Apply the **identical migration shape from Plan 03-01** to each of 4 files:
   - Replace `PostgreSqlContainer.start()` → `getSharedPostgres()`. Add import.
   - Replace inline CREATE ROLE block → `await bootstrapSharedRoles(ownerPool)`.
   - Per-file SCHEMA_NAME derivation (filename-based; sanitised).
   - `CREATE SCHEMA IF NOT EXISTS ${SCHEMA_NAME}` + search_path-or-clone (MATCH Plan 03 choice exactly — read Plan 03 SUMMARY first).
   - `afterAll` → `try/finally` DROP SCHEMA CASCADE; NO `container.stop()`.
   - Skip-gate guard at top of describe.
3. Re-run aggregate: `pnpm --filter @openwhispr/api test conversations-.*integration 2>&1 | tail -10`. All 4 GREEN.
4. Single commit (4 files): `fix(18.1.2-05-01): migrate conversations integration (4 files) to shared-pg + schema isolation`. Body MUST: (a) include verbatim RED, (b) cite Plan 03 template, (c) confirm migration-applier choice match.

WHY: pitfall §3 — migration-applier choice MUST be consistent across all 18 cluster #2 files (same shared container; conflicting `__drizzle_migrations` state = breakage). WHY: pitfall §12 — 4-file batch fits one commit; do not split per-file.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test "conversations-.*integration" 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; 4 conversations files GREEN (or skip-clean); migration-applier choice consistent with Plan 03.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-02: Migrate notes integration (5 files)</name>
  <files>apps/api/tests/unit/routes/__tests__/notes-crud.integration.test.ts, apps/api/tests/unit/routes/__tests__/notes-list.integration.test.ts, apps/api/tests/unit/routes/__tests__/notes-search.integration.test.ts, apps/api/tests/unit/routes/__tests__/notes-batch-create.integration.test.ts, apps/api/tests/unit/routes/__tests__/notes-delete-all.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical Plan 03 template.

1. Capture RED for 5 notes files.
2. Apply identical migration shape (5 files).
3. Verify aggregate GREEN.
4. Single commit: `fix(18.1.2-05-02): migrate notes integration (5 files) to shared-pg + schema isolation`. Body has verbatim RED + Plan 03 template citation.

WHY: pitfall §12 — 5-file batch (largest sub-cluster); single commit per D-18.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test "notes-.*integration" 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; 5 notes files GREEN (or skip-clean).</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-03: Migrate folders integration (2 files)</name>
  <files>apps/api/tests/unit/routes/__tests__/folders-crud.integration.test.ts, apps/api/tests/unit/routes/__tests__/folders-list.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical template.

1. Capture RED.
2. Apply identical migration (2 files).
3. Verify GREEN.
4. Commit: `fix(18.1.2-05-03): migrate folders integration (2 files) to shared-pg + schema isolation`. Body has verbatim RED.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test "folders-.*integration" 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; 2 folders files GREEN (or skip-clean).</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-04: Migrate transcriptions integration (2 files)</name>
  <files>apps/api/tests/unit/routes/__tests__/transcriptions-crud.integration.test.ts, apps/api/tests/unit/routes/__tests__/transcriptions-batch.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical template.

1. Capture RED.
2. Apply identical migration (2 files).
3. Verify GREEN.
4. Commit: `fix(18.1.2-05-04): migrate transcriptions integration (2 files) to shared-pg + schema isolation`. Body has verbatim RED.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test "transcriptions-.*integration" 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; 2 transcriptions files GREEN (or skip-clean).</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-05: Migrate v1/keys + tokens integration (3 files)</name>
  <files>apps/api/tests/unit/routes/v1/keys/__tests__/keys-crud.integration.test.ts, apps/api/tests/unit/routes/v1/keys/__tests__/keys-revoke.integration.test.ts, apps/api/tests/unit/routes/tokens/__tests__/rate-limit-isolation.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical template. Closes cluster #2.

1. Capture RED for 3 files.
2. Apply identical migration shape. Note depth: `v1/keys/__tests__/` is one level deeper — import path becomes `../../../../support/shared-pg` (4 ups). Tokens path uses 3 ups (same as routes/__tests__).
3. Verify aggregate GREEN: `pnpm --filter @openwhispr/api test "(keys-|rate-limit-isolation)" 2>&1 | tail -10`.
4. Single commit: `fix(18.1.2-05-05): migrate v1/keys + tokens integration (3 files) to shared-pg + schema isolation`. Body has verbatim RED + import-depth note.
5. **Cluster #2 complete** — run full cluster #2 aggregate: `pnpm --filter @openwhispr/api test "*.integration" 2>&1 | tail -10`. Expect 22 files GREEN (cluster #1 from Plan 03 + cluster #2 18 files).

WHY: pitfall §12 — 3-file mixed-batch (different depths) fits one commit; document import-path delta in body. WHY: closes D-06 cluster #2 entirely.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test "(keys-|rate-limit-isolation)" 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; 3 v1/keys+tokens files GREEN (or skip-clean); cluster #2 aggregate verified.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-06: CI — ci.yml test job Ryuk-disabled + Docker pre-flight + cleanup</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Per D-11 + D-12 + PATTERNS surface 8 + RESEARCH §7.

1. Read `.github/workflows/ci.yml:78-95` (current test job) + `:453-507` (e2e-phase6-quick precedent) + `:486` (canonical TESTCONTAINERS_RYUK_DISABLED inline).
2. Edit `ci.yml:78-95` to mirror PATTERNS surface 8 + RESEARCH §7 exact shape:
   - Add job-level `env: TESTCONTAINERS_RYUK_DISABLED: "true"` (cleaner than per-step).
   - Add step BEFORE `pnpm test`: `name: Pre-flight Docker` / `run: docker version && docker info` (RESEARCH §7 verified ubuntu-24.04 hosted runner has Docker daemon; no sudo needed per pitfall §11).
   - Add step AFTER `pnpm test`: `name: Cleanup testcontainers` with `if: always()`, `run: |`-block doing `docker ps -a --filter "label=org.testcontainers=true" -q | xargs -r docker rm -f` + `docker volume prune -f` (mirror `tools/global-vitest-teardown.ts:28-34,53-62` argv).
   - Apply step-security `harden-runner` to job head if e2e-phase6-quick precedent does so (PATTERNS surface 8 — match precedent discipline).
3. DO NOT add `services: docker:dind` (A3 rejected). DO NOT pin actions by unfixed SHA (match precedent at L497-500 pinned SHA `ea165f8d65b6e75b540449e92b4886f43607fa02`).
4. Validate YAML locally: `pnpm exec yaml-validator .github/workflows/ci.yml` OR `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/ci.yml', 'utf8'))"` (use available validator; if neither, `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`).
5. Commit: `ci(18.1.2-05-06): green — ci.yml test job Ryuk-disabled + Docker pre-flight + cleanup (D-11/12)`. Body MUST cite: D-11, D-12, PATTERNS surface 8 mirror of L453-507, pitfall §10 (Ryuk-OFF + always-cleanup are mandatory pairing), pitfall §11 (no sudo needed).

WHY: pitfall §10 — TESTCONTAINERS_RYUK_DISABLED=true means Ryuk does NOT self-clean on parent crash; `if: always()` post-step is mandatory. WHY: pitfall §11 — GHA `runner` user has docker-group; no sudo on `docker info`. WHY: A3 — `services: docker:dind` blocked on GHA hosted runners (privileged forbidden); native Docker on ubuntu-24.04 is correct.
  </action>
  <verify><automated>grep -c "TESTCONTAINERS_RYUK_DISABLED" .github/workflows/ci.yml && grep -c "docker volume prune" .github/workflows/ci.yml</automated></verify>
  <done>1 commit; ci.yml test job has job-level env + pre-flight step + cleanup step; YAML validates; ZERO services: docker:dind.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-05-07: Docs — operations.md local-dev test prerequisites</name>
  <files>docs/operations.md</files>
  <action>
Per D-13.

1. Append new section to `docs/operations.md`:
   ```markdown
   ## Local development test prerequisites

   `pnpm test` requires the following on the developer machine:

   - **Docker daemon** running (`docker info` exits 0). The shared testcontainer fixture (`apps/api/tests/support/shared-pg.ts`) starts a single `postgres:17-alpine` container via `withReuse()`; without Docker, integration tests `describe.skip` cleanly (D-02 probe at `tools/testcontainer-availability.ts`).
   - **mkcert** installed (for `make tls-trust` + local TLS). See `tools/setup-mkcert.sh`.
   - **pnpm-workspace install order**: `pnpm install` from repo root only — never from a sub-package directory (workspace symlinks rely on root-level install).
   - **`make tls-trust`** run once to trust the local CA root for HTTPS e2e tests.
   - **Free disk + 4GB+ RAM** for parallel Postgres testcontainers (`singleThread: true` for `*.integration.test.ts` mitigates port exhaustion — see D-05).

   To skip integration tests on a Docker-less machine: `OPENWHISPR_SKIP_TESTCONTAINERS=1 pnpm test`. The Plan 01 probe (`tools/testcontainer-availability.ts`) sets this automatically when the Docker daemon is unreachable.
   ```
2. Validate markdown (if any linter exists): `pnpm lint:md docs/operations.md` — clean. Otherwise visual proof.
3. Commit: `docs(18.1.2-05-07): operations.md local-dev test prerequisites (D-13)`. Body cites D-13.

WHY: D-13 — completes Bucket D documentation surface. WHY: cross-refs Plan 01 + 02 artefacts so operators understand the test-skip path.
  </action>
  <verify><automated>grep -c "Local development test prerequisites" docs/operations.md</automated></verify>
  <done>1 commit; docs/operations.md has new section; cross-refs Plan 01 + D-05.</done>
</task>

</tasks>

<verification>
After 7 commits (+ 1 zero-commit pre-flight):

```bash
cd /Users/dev/openwhispr-server
pnpm --filter @openwhispr/api test "*.integration" 2>&1 | tail -20    # 22 GREEN cluster #1+#2 (or skip-clean)
grep -c "TESTCONTAINERS_RYUK_DISABLED" .github/workflows/ci.yml         # ≥ 2 (L486 + new test job)
grep -c "docker volume prune" .github/workflows/ci.yml                  # ≥ 1
grep -c "Local development test prerequisites" docs/operations.md       # = 1
git log --oneline -n 7 | grep -cE "(fix|ci|docs)\(18\.1\.2-05-0[1-7]\)" # = 7
```
</verification>

<success_criteria>
1. Exactly 7 atomic commits (5 sub-clusters + 1 CI + 1 docs); D-18 ceiling honored.
2. Cluster #2 (18 files) migrated to shared-pg + schema isolation; consistent migration-applier choice with Plan 03.
3. CI test job: TESTCONTAINERS_RYUK_DISABLED + Docker pre-flight + post-step cleanup (mirror of e2e-phase6-quick).
4. docs/operations.md has Local-dev-test-prerequisites section.
5. ZERO `services: docker:dind`, ZERO `--no-verify`, ZERO new deps.
6. Aggregate `*.integration` test cluster GREEN on Docker-running machine.
7. lefthook clean on all 7 commits.
</success_criteria>

<rollback>
- Sub-cluster migration breaks because file diverges from canonical boot (e.g., uses different fixture seed) → HALT per pitfall §12; partial SUMMARY; fresh spawn for remaining sub-clusters.
- CI YAML lint fires (indentation drift) → revert ci commit; re-author from e2e-phase6-quick line-for-line; re-apply.
- ci-test runs in actual GHA pipeline and fails because runner user lacks docker group (pitfall §11 wrong) → add `sudo docker info` in pre-flight step; document deviation in SUMMARY.
- HALT recipe — if aggregate test fails > 5 additional new categories after sub-cluster migration, HALT 3-branch user-choice.
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-05-SUMMARY.md` with:
- 7 commit SHAs
- Cluster #2 actual count (must = 18, Δ-2)
- Aggregate `*.integration` test result
- CI YAML diff summary
- docs/operations.md section preview
- Lefthook status (clean)
</output>
