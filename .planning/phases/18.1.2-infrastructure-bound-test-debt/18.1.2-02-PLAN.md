---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 02
type: execute
wave: 2
depends_on: ["18.1.2-01"]
files_modified:
  - apps/api/tests/support/shared-pg.ts
  - apps/api/tests/support/__tests__/shared-pg.test.ts
  - apps/api/vitest.setup.ts
  - apps/api/vitest.config.ts
  - apps/worker/vitest.config.ts
  - packages/data/vitest.config.ts
autonomous: true
requirements:
  - SR-1
  - SR-3
  - SR-4
must_haves:
  truths:
    - "`apps/api/tests/support/shared-pg.ts` exports `getSharedPostgres(): Promise<StartedPostgreSqlContainer>` memoised at module scope (D-03)."
    - "`getSharedPostgres()` lazily constructs `new PostgreSqlContainer('postgres:17-alpine').withDatabase('openwhispr').withUsername('postgres_super').withPassword('super-pw').withReuse()` — withReuse keys on identical triple → shared container."
    - "Idempotent `bootstrapSharedRoles(pool)` helper handles `CREATE ROLE` via `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` (pitfall §2 — Postgres 17 has no CREATE ROLE IF NOT EXISTS)."
    - "`apps/api/vitest.setup.ts` sets `process.env.TESTCONTAINERS_REUSE_ENABLE = 'true'` (and idempotent `OPENWHISPR_*` env where the existing file already mutates env) BEFORE any testcontainer import (pitfall §1; PATTERNS surface 2)."
    - "Each of `apps/api/vitest.config.ts`, `apps/worker/vitest.config.ts`, `packages/data/vitest.config.ts` carries the working Vitest-4-correct shape for serialising `*.integration.test.ts` (D-05). Spike a 5-line config first (PATTERNS surface 4 warning) and commit only the shape that vitest actually accepts."
    - "Worker + data packages: extend `singleThread` glob to include known testcontainer-bound `*.test.ts` files (Δ-2 honor) — explicit file list: `packages/data/tests/unit/__tests__/worker-rls-property.test.ts`, `0014-audit-log-partition.test.ts`. Recommendation (a) from RESEARCH §9 Δ-2."
    - "Sibling test `apps/api/tests/support/__tests__/shared-pg.test.ts` verifies reuse semantics: two consecutive `getSharedPostgres()` calls return same `getConnectionUri()` (D-03)."
    - "Skip-on-no-docker: if `process.env.OPENWHISPR_SKIP_TESTCONTAINERS === '1'` set by Plan 01 probe, `getSharedPostgres()` rejects with a typed error fast (allowing integration tests to `describe.skip` in beforeAll)."
    - "TDD pair for fixture (D-39 NEW); single GREEN for config edits (mechanical; pre-existing port-exhaustion RED proven by `pnpm test` baseline)."
    - "Coverage ≥ 90/90/90/90 on `shared-pg.ts` (D-20)."
    - "ZERO new top-level deps (D-22); ZERO `--no-verify` (D-19); ≤ 4 commits (D-18)."
  artifacts:
    - path: "apps/api/tests/support/shared-pg.ts"
      provides: "Shared PostgreSqlContainer fixture via withReuse() + idempotent role bootstrap."
      contains: "withReuse"
    - path: "apps/api/tests/support/__tests__/shared-pg.test.ts"
      provides: "Reuse-semantics test (same connection URI across calls)."
      contains: "getSharedPostgres"
    - path: "apps/api/vitest.setup.ts"
      provides: "TESTCONTAINERS_REUSE_ENABLE=true env wire-up (test-only; NOT package.json)."
      contains: "TESTCONTAINERS_REUSE_ENABLE"
  key_links:
    - from: "Plans 03 + 05 integration test migrations"
      to: "apps/api/tests/support/shared-pg.ts"
      via: "Import `getSharedPostgres` in `beforeAll`; replace per-file PostgreSqlContainer boot."
      pattern: "getSharedPostgres"
    - from: "Plan 01 docker probe"
      to: "shared-pg fixture"
      via: "OPENWHISPR_SKIP_TESTCONTAINERS env check at top of getSharedPostgres"
      pattern: "OPENWHISPR_SKIP_TESTCONTAINERS"
---

<objective>
Land the shared-pg testcontainer fixture (D-03) + idempotent role bootstrap (D-12 carryover pitfall §2) + `singleThread` Vitest config across api/worker/data (D-05). NEW pattern — no analog in repo (PATTERNS surface 2 + 4 both "first instance"). **Spike-first discipline** (PATTERNS surface 4): write 5-line vitest config experiment + verify it actually runs before committing.

**Budget: ≤ 4 commits.** Sequential after Plan 01. Sequential before Plan 03.

Commit shape:
1. RED: `test(18.1.2-02-01): red — shared-pg fixture reuse semantics + role bootstrap`
2. GREEN: `feat(18.1.2-02-02): green — shared-pg fixture + TESTCONTAINERS_REUSE_ENABLE wire-up`
3. GREEN (mechanical, pre-existing RED): `chore(18.1.2-02-03): apps/api+worker+data singleThread for *.integration.test.ts (D-05)`
4. (optional, only if Δ-2 worker/data extension proves needed) `chore(18.1.2-02-04): extend singleThread glob to worker-rls-property + 0014-audit-log (Δ-2)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-01-SUMMARY.md
@CLAUDE.md
@apps/api/tests/unit/routes/__tests__/usage.integration.test.ts
@apps/api/vitest.setup.ts
@apps/api/vitest.config.ts
@apps/worker/vitest.config.ts
@packages/data/vitest.config.ts
@packages/data/src/__tests__/helpers.ts
@tools/testcontainer-reaper-setup.ts
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18.1.2-02-01: RED — shared-pg fixture reuse-semantics + role bootstrap test</name>
  <files>apps/api/tests/support/__tests__/shared-pg.test.ts</files>
  <behavior>
- Test 1 (memoisation): two consecutive `getSharedPostgres()` awaits return objects with the same `getConnectionUri()`.
- Test 2 (role bootstrap idempotency): calling `bootstrapSharedRoles(pool)` twice does NOT throw `role "openwhispr_owner" already exists` (pitfall §2).
- Test 3 (skip gate): when `process.env.OPENWHISPR_SKIP_TESTCONTAINERS === '1'`, `getSharedPostgres()` rejects with typed error within ≤ 50ms (no Docker call attempted).
  </behavior>
  <action>
Per D-03 + D-39 RED leg + PATTERNS surface 2.

1. Create `apps/api/tests/support/__tests__/shared-pg.test.ts` importing `getSharedPostgres` + `bootstrapSharedRoles` from `../shared-pg`. Symbols do not exist yet — import is RED trigger.
2. Guard the testcontainer-using cases (1 + 2) under `describe.skipIf(!!process.env.OPENWHISPR_SKIP_TESTCONTAINERS)` — local dev with Docker runs them; CI w/o Docker still verifies test 3 (skip gate).
3. Run `pnpm --filter @openwhispr/api exec vitest run tests/support/__tests__/shared-pg.test.ts 2>&1 | tee /tmp/02-01-red.log` — MUST fail (module not found).
4. Commit: `test(18.1.2-02-01): red — shared-pg reuse semantics + idempotent role bootstrap`. Body MUST include verbatim RED.

WHY: pitfall §2 — `withReuse()` config-hash collision means first file's `CREATE ROLE` succeeds, all subsequent crash without idempotent guard. Phase 18.1.1 D-12 `bootstrapRoles` helper at `packages/data/src/__tests__/helpers.ts:94,193` is the proven idempotent shape to copy.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api exec vitest run tests/support/__tests__/shared-pg.test.ts 2>&1 | grep -E "(Cannot find module|MODULE_NOT_FOUND|FAIL)" | head -3</automated></verify>
  <done>1 commit; failing test present; verbatim RED in commit body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.2-02-02: GREEN — shared-pg fixture + role bootstrap + TESTCONTAINERS_REUSE_ENABLE</name>
  <files>apps/api/tests/support/shared-pg.ts, apps/api/vitest.setup.ts</files>
  <action>
Per D-03 + D-39 GREEN leg + PATTERNS surface 2 + RESEARCH §2 boot template + pitfall §2.

1. Create `apps/api/tests/support/shared-pg.ts`:
   - Module-scoped `let cached: Promise<StartedPostgreSqlContainer> | null = null`.
   - `export async function getSharedPostgres(): Promise<StartedPostgreSqlContainer>` — first checks `process.env.OPENWHISPR_SKIP_TESTCONTAINERS === '1'` → reject with `new Error('shared-pg disabled — OPENWHISPR_SKIP_TESTCONTAINERS=1 (Plan 01 probe)')`. Otherwise, assign `cached ??= new PostgreSqlContainer('postgres:17-alpine').withDatabase('openwhispr').withUsername('postgres_super').withPassword('super-pw').withReuse().start()`. Return cached promise.
   - `export async function bootstrapSharedRoles(pool: Pool): Promise<void>` — runs `DO $$ BEGIN CREATE ROLE openwhispr_owner ...; EXCEPTION WHEN duplicate_object THEN NULL; END $$;` and the parallel block for `openwhispr_app`, plus `GRANT ... TO ...` (verbatim copy from `packages/data/src/__tests__/helpers.ts:94,193` + `usage.integration.test.ts:85-92`). Plus `ALTER DATABASE openwhispr OWNER TO openwhispr_owner;` (idempotent — ALTER does not error if already owner).
2. Edit `apps/api/vitest.setup.ts`: add `process.env.TESTCONTAINERS_REUSE_ENABLE = 'true'` ABOVE any existing import that may touch testcontainers (PATTERNS surface 2 risk note). Place after the file's existing module-tag SPDX block, before re-exports.
3. Re-run sibling test — must GREEN (locally w/ Docker; or skip-branch GREEN w/o).
4. Coverage gate: `pnpm --filter @openwhispr/api exec vitest run tests/support --coverage` ≥ 90/90/90/90 on `shared-pg.ts` (D-20).
5. Commit: `feat(18.1.2-02-02): green — shared-pg fixture + TESTCONTAINERS_REUSE_ENABLE wire-up`. Body cites D-03 + pitfall §2 + PATTERNS surface 2.

WHY: pitfall §1 — `TESTCONTAINERS_REUSE_ENABLE` MUST be in `vitest.setup.ts` not `package.json` (test-only scope). WHY: pitfall §2 — Postgres 17 has no `CREATE ROLE IF NOT EXISTS`; `DO $$ ... EXCEPTION WHEN duplicate_object` is mandatory. WHY: PATTERNS surface 2 — `withReuse` is first instance in repo; spike before commit. WHY: D-22 — `@testcontainers/postgresql` already in workspace; zero new deps.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api exec vitest run tests/support/__tests__/shared-pg.test.ts 2>&1 | grep -E "(✓|PASS|passed)" | head -3 && grep -c "TESTCONTAINERS_REUSE_ENABLE" apps/api/vitest.setup.ts</automated></verify>
  <done>1 commit; sibling test GREEN; reuse env set in vitest.setup.ts; coverage ≥ 90/90/90/90 on shared-pg.ts; ZERO new deps.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-02-03: singleThread vitest config for *.integration.test.ts (api+worker+data)</name>
  <files>apps/api/vitest.config.ts, apps/worker/vitest.config.ts, packages/data/vitest.config.ts</files>
  <action>
Per D-05 + PATTERNS surface 4 (spike-first warning) + RESEARCH §8 pitfall §4.

1. **Spike first** (PATTERNS surface 4): in a scratch file (not committed), write a 5-line `vitest.config.ts` snippet using `poolOptions.threads.singleThread: true` paired with `poolMatchGlobs` OR Vitest-4 `workspace`/`projects` shape. Run `pnpm exec vitest --version` to pin the API. Verify against actual `pnpm test` invocation that integration tests serialise.
2. Once the working shape is identified, edit the three real configs:
   - `apps/api/vitest.config.ts`: extend the `mergeConfig(rootConfig, defineConfig({test: {...}}))` body to apply singleThread/singleFork to `**/*.integration.test.ts`. DO NOT touch other test fields (PATTERNS surface 4 risk).
   - `apps/worker/vitest.config.ts`: same shape — but glob may match nothing today (RESEARCH §9 Δ-2; CONTEXT D-06 wrongly says worker has integration tests).
   - `packages/data/vitest.config.ts`: same shape — also may match nothing.
3. Verify each config still loads: `pnpm --filter @openwhispr/api exec vitest --reporter=verbose --no-color list 2>&1 | head -3` (and worker/data analogs). No syntax error.
4. Single commit (pre-existing port-exhaustion RED → single GREEN per D-39 + Phase 18.1 §7): `chore(18.1.2-02-03): apps/api+worker+data singleThread for *.integration.test.ts (D-05)`. Body MUST include verbatim port-exhaustion error sample from `pnpm test` baseline (cite RESEARCH §2).

WHY: pitfall §4 — Vitest 4 deprecates `test.poolMatchGlobs`; verify exact field name with `pnpm exec vitest --version` first. WHY: pitfall §12 — singleThread is belt-and-suspenders for `withReuse()` cache warmup; without it, concurrent first-callers race the container start. WHY: pitfall §13 — worker+data glob match nothing today (harmless but doc-misleading); landing the field anyway makes future tests inherit the discipline.
  </action>
  <verify><automated>grep -l "singleThread\|singleFork" apps/api/vitest.config.ts apps/worker/vitest.config.ts packages/data/vitest.config.ts | wc -l</automated></verify>
  <done>1 commit; 3 config files contain the working serialisation shape; pnpm test still loads (no syntax error); commit body cites pitfall §4 + verbatim baseline RED.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-02-04 (CONDITIONAL): Extend singleThread to worker-rls-property + 0014-audit-log (Δ-2)</name>
  <files>packages/data/vitest.config.ts</files>
  <action>
Per RESEARCH §9 Δ-2 recommendation (a) — explicit file-list in `poolMatchGlobs` over rename.

**Only land this task if Plan 02-03 verification shows worker-rls-property.test.ts or 0014-audit-log-partition.test.ts STILL fail port-exhaustion after 02-03.** Otherwise SKIP and document VERIFIED in SUMMARY.

1. Re-run `pnpm --filter @openwhispr/data test 2>&1 | tail -30`. If `worker-rls-property` and `0014-audit-log-partition` are GREEN under Plan 02-03's glob, this task is **NO-OP — emit SUMMARY note "Δ-2 VERIFIED, no extension needed"**.
2. If they still fail with port-exhaustion / ECONNREFUSED:
   - Edit `packages/data/vitest.config.ts` poolMatchGlobs to include explicit names: `["**/worker-rls-property.test.ts", "forks"], ["**/0014-audit-log-partition.test.ts", "forks"]` with `singleFork: true`.
   - Re-run; verify GREEN.
   - Commit: `chore(18.1.2-02-04): extend singleThread glob to worker-rls-property + 0014-audit-log (Δ-2)`. Body cites RESEARCH §9 Δ-2 + verbatim RED.

WHY: pitfall §13 + RESEARCH §9 Δ-2 — naming non-conformant tests are testcontainer-bound but not `*.integration.test.ts` shape. Extension via explicit names preserves zero churn (D-22 spirit — no mass file rename).
  </action>
  <verify><automated>pnpm --filter @openwhispr/data test 2>&1 | tail -20</automated></verify>
  <done>EITHER 0 commits with VERIFIED note OR 1 commit with explicit-name glob; data tests GREEN either way (or expected-skip under no-Docker).</done>
</task>

</tasks>

<verification>
After up to 4 commits:

```bash
cd /Users/dev/openwhispr-server
pnpm --filter @openwhispr/api exec vitest run tests/support
grep -c "TESTCONTAINERS_REUSE_ENABLE" apps/api/vitest.setup.ts          # ≥ 1
grep -lc "singleThread\|singleFork" apps/api/vitest.config.ts apps/worker/vitest.config.ts packages/data/vitest.config.ts
pnpm --filter @openwhispr/api exec vitest run tests/support --coverage 2>&1 | grep -E "(Lines|Branches|Functions|Statements).*9[0-9]"
git log --oneline -n 4 | grep -cE "(test|feat|chore)\(18\.1\.2-02-(0[1-4])\)"  # 3 or 4
```
</verification>

<success_criteria>
1. 3-4 atomic commits (RED, GREEN fixture, GREEN config, optional Δ-2).
2. `getSharedPostgres()` memoised + reuses container; `bootstrapSharedRoles()` idempotent.
3. `TESTCONTAINERS_REUSE_ENABLE=true` set in `vitest.setup.ts` (NOT package.json).
4. Three vitest configs serialise `*.integration.test.ts` (Vitest-4-correct shape verified via spike).
5. Coverage ≥ 90/90/90/90 on `shared-pg.ts` (D-20).
6. ZERO new top-level deps (D-22); ZERO `--no-verify` (D-19); lefthook clean.
</success_criteria>

<rollback>
- `withReuse()` race causes intermittent test failures → switch to **explicit shared lifecycle via globalSetup/globalTeardown** instead of `withReuse()`. Revert 02-02 GREEN; re-implement with `globalSetup` starting container once, exposing connection string via env. PATTERNS surface 2 risk note.
- Vitest 4 API drift (singleThread field renamed) → HALT per 18.1-06 3-branch user-choice; document version + correct shape in SUMMARY before re-attempting.
- Schema-search-path collisions during Plan 03 dry-run → revisit `bootstrapSharedRoles` to also `RESET search_path` (PATTERNS surface 3 risk note carried).
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-02-SUMMARY.md` with:
- 3-4 commit SHAs (note Δ-2 task disposition)
- Coverage report on shared-pg.ts
- Vitest-4 field-name verified (singleThread vs poolMatchGlobs vs workspace projects)
- Δ-2 disposition (VERIFIED-NO-OP or EXTENDED)
- Lefthook status (clean)
</output>
