---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 01
title: Docker probe + setupFile correctness
status: complete
commits:
  - 1f46e4a   # test(18.1.2-01-01): red — probe contract
  - f6b4813   # feat(18.1.2-01-02): green — probe + reaper wire-up
files_created:
  - tools/testcontainer-availability.ts
  - tools/__tests__/testcontainer-availability.test.ts
files_modified:
  - tools/testcontainer-reaper-setup.ts
---

# Phase 18.1.2 Plan 01: Docker probe + setupFile correctness — Summary

Landed the Docker availability probe (D-02) and wired it into the shared
`tools/testcontainer-reaper-setup.ts` setupFile AFTER `installSignalHook()`
per PATTERNS surface 1. Foundational primitive for Plans 02/03/05.

## Commits (2, D-39 RED+GREEN pair for NEW functionality)

| SHA       | Type | Summary                                              |
| --------- | ---- | ---------------------------------------------------- |
| `1f46e4a` | test | RED — `testcontainer-availability` probe contract    |
| `f6b4813` | feat | GREEN — `assertDockerAvailable()` + reaper wire-up   |

## Coverage delta on `tools/testcontainer-availability.ts`

```
Statements : 100% (13/13)
Branches   : 100% (4/4)
Functions  : 100% (2/2)
Lines      : 100% (13/13)
```

D-20 floor (≥ 90/90/90/90) cleared on every axis.

## Reaper file delta

`tools/testcontainer-reaper-setup.ts`: 20 → 26 lines.
`grep -c "assertDockerAvailable" tools/testcontainer-reaper-setup.ts` = **3**
(1 import + 1 in JSDoc-style inline comment + 1 call site).

```ts
installSignalHook();
// Phase 18.1.2 / Plan 01 / D-02 — probe Docker daemon AFTER ...
assertDockerAvailable();
```

## Lefthook (all clean, ZERO `--no-verify`)

- biome → 0 findings (Checked 3 files)
- english → 1004 files scanned, passed
- commitlint → passed for both commits
  (initial GREEN attempt tripped `body-max-line-length` + `noConsole` —
  both fixed in-place before commit: switched `console.warn` →
  `process.stderr.write` JSON line, shortened body lines)

## Test results

5 unit cases on `tools/testcontainer-availability.test.ts`:
(a) daemon-up; (b) daemon-down ENOENT; (c) ETIMEDOUT;
(c2) non-Error throw → `String()` fallback; (d) idempotent warn.
Plus pre-existing `testcontainer-reaper-setup.test.ts` regressed clean.

## Deviations

- **+1 test case** beyond plan's 4 (added `(c2)` non-Error throw branch) —
  needed to reach 100% branch coverage on `err instanceof Error ?` ternary
  (initial 5-case run hit 75% branches → below D-20 floor). Rule 2:
  required for D-20 compliance.
- **Structured warn channel:** plan said `console.warn`; biome `noConsole`
  rule prohibited it. Switched to `process.stderr.write` with newline
  terminator — same JSON payload, same boundary discipline as
  `tools/global-vitest-teardown.ts`. Rule 3: blocking lefthook fix.

## Verification (post-merge)

```
$ docker info >/dev/null 2>&1 && echo UP            → UP
$ (unset DOCKER_HOST; docker info >/dev/null) && UP  → UP
$ pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts
  Test Files  1 passed (1)    Tests  5 passed (5)
```

## Self-Check: PASSED

- `tools/testcontainer-availability.ts` exists ✓
- `tools/__tests__/testcontainer-availability.test.ts` exists ✓
- Commits `1f46e4a` + `f6b4813` present in `git log` ✓
- Coverage ≥ 90/90/90/90 ✓
- ZERO new deps ✓ — ZERO `--no-verify` ✓
