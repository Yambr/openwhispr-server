# Phase 18.1.2: aggregate-sweep infrastructure-bound test debt - Context

**Gathered:** 2026-05-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the remaining `pnpm test` failures revealed by Phase 18.1.1-06 aggregate sweep. **Scope corrected by A1+A2 advisors:** the initial 37-failure count was stale; actual is **11 hard failures**, dominated by NON-testcontainer issues plus Docker Desktop port-exhaustion under parallel container starts.

**In scope (4 buckets):**
- **Bucket A — testcontainer port exhaustion** (~5-7 fails): Docker IS available but ~20 parallel `PostgreSqlContainer.start()` calls exhaust Docker Desktop port pool. Modem buildPayload errors. Fix: hybrid (A+C+serialize per A1) — availability probe + `withReuse(true)` shared-pg fixture + `singleThread: true` for `*.integration.test.ts`.
- **Bucket B — non-testcontainer surface failures** (~4 fails per A1 + A2): BYOK env guards (`packages/byok-guard/src/index.ts:242` calling `process.exit(1)`), missing audio fixture (`tests/fixtures/audio/sample-1sec.wav`), otel-bootstrap SIGTERM test (`apps/api/tests/unit/otel-bootstrap.test.ts:130`).
- **Bucket C — content drift** (1 fail): `apps/web` locale-coverage mismatch on `end-user.account.subtitle.body.text` per UI-SPEC oracle.
- **Bucket D — CI/CD adoption**: align `.github/workflows/ci.yml` `test` job with e2e jobs' Ryuk-disabled + post-step Docker sweep pattern (per A3 native ubuntu-24.04 strategy).

**Out of scope (deferred):**
- **SR-2 partman `lint-rls.test.ts`**: A2 found 5 stub `throw PHASE_6_NOT_YET` cases — **scheduled D-A2 forward work, not deferred regression**. Reclassify; do NOT bundle.
- **SR-6 helm-unittest**: A2 verified 163/163 already GREEN. Drop from scope; update failure ledger.
- **Phase 19.x downstream code** for repointed @cjm tags (unchanged).
- **FSL history scrub** (15-04 operator).
- **First real GHA e2e-cjm run** (operator).
- **UICONF-05 axe baseline live bake** + **Playwright visual regression GREEN bake** (operator deferrals from 18.1.1-05).

</domain>

<decisions>
## Implementation Decisions

### Bucket A — Testcontainer Port Exhaustion (A1 advisor)

- **D-01:** Use **Option D hybrid** (A+C+serialize). NOT pure-A (gate masks regressions), NOT pure-B (compose stack ≠ ephemeral testcontainers).
- **D-02:** **Docker availability probe** at `tools/testcontainer-availability.ts` exporting `assertDockerAvailable()` via `execFileSync("docker", ["info"], {timeout: 2000})`. On failure, set `process.env.OPENWHISPR_SKIP_TESTCONTAINERS=1` + log structured warning. Integration test files read env in `beforeAll` and `describe.skip` if set. Wire into `tools/testcontainer-reaper-setup.ts:19` after `installSignalHook()`. TDD pair: RED `tools/__tests__/testcontainer-availability.test.ts` with mocked `execFileSync`, GREEN implementation.
- **D-03:** **Shared-pg fixture** at `apps/api/tests/support/shared-pg.ts` exporting `getSharedPostgres()` lazily constructing `new PostgreSqlContainer("postgres:17-alpine").withReuse()` keyed on container hash. `TESTCONTAINERS_REUSE_ENABLE=true` in `apps/api/vitest.setup.ts`. Fixture test verifies reuse semantics (second call → same `getConnectionUri()`). Eventually lift to `tools/shared-pg-fixture.ts` for cross-package reuse.
- **D-04:** **Schema-per-file isolation**: `CREATE SCHEMA test_${path.basename(__filename, '.test.ts')}` in `beforeAll`, `DROP SCHEMA … CASCADE` in `afterAll`. Preserves RLS-context-leak prevention while sharing container.
- **D-05:** **`singleThread: true`** for `**/*.integration.test.ts` via vitest `test.poolMatchGlobs` in each `vitest.config.ts`. Belt-and-suspenders against port exhaustion during reuse-cache warmup. Apply to `apps/api/vitest.config.ts`, `apps/worker/vitest.config.ts`, `packages/data/vitest.config.ts`.
- **D-06:** Migrate ~22 `*.integration.test.ts` files in 2 clusters (cluster 1: usage + streaming-usage + web-search + web-search-ratelimit; cluster 2: conversations + notes + remaining + worker + data). Each cluster = one commit, atomic file group.

### Bucket B — Non-Testcontainer Surface Failures (A1 + A2)

- **D-07: BYOK env guards** (`packages/byok-guard/src/index.ts:242` calling `process.exit(1)`): test contract is "constructor throws on missing env". The actual code calls `process.exit(1)` which vitest traps as "process.exit unexpectedly called". Fix: refactor `byok-guard` to `throw new BYOKGuardError(msg)` instead of `process.exit(1)`; callers can decide whether to log+exit at entrypoint. TDD pair.
- **D-08: Missing audio fixture** (`tests/fixtures/audio/sample-1sec.wav`): grep `sample-1sec.wav` to find test sites; check fixture path. Likely Phase 15-02 STRUCT regression — fixture exists somewhere, path is wrong. Single-GREEN commit per Phase 18.1 §7 doctrine.
- **D-09: otel-bootstrap SIGTERM test** (`apps/api/tests/unit/otel-bootstrap.test.ts:130`): A2 confirmed vitest worker registers competing SIGTERM handler that calls `process.exit(143)`. Fix per A2: export `onSignal` from `apps/api/src/otel-bootstrap.ts:144-148` + call directly in test (assert no-throw + spy `shutdownSdk`). TDD pair: refactor test + tiny export-surface change.

### Bucket C — Content Drift (A2 advisor)

- **D-10: locale-coverage drift** (`apps/web/tests/unit/locales/__tests__/coverage.test.ts:106`): Phase 04 widened Account screen surface (sessions + deletion) but `en/end-user.json:79` carries pre-widen copy. UI-SPEC oracle `.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md:588` says: `"Manage your profile, active sessions, and account deletion."` Bundle has: `"Manage your profile and account."` Fix: update `en/end-user.json:79` to oracle exactly + parallel ru translation + update `AccountClient.test.tsx:34` fixture. Single commit (en+ru parity + component-test fixture together).

### Bucket D — CI/CD Adoption (A3 advisor)

- **D-11:** Adopt **Option B native Docker on ubuntu-24.04 + explicit cleanup**. Reject Option A (`services: docker:dind` incompatible with GHA hosted runners — privileged blocked). Reject Option C (violates CLAUDE.md rule 5 "no mocks of internal logic").
- **D-12:** Exact YAML diff for `.github/workflows/ci.yml` `test` job (per A3):
  - Add `env: TESTCONTAINERS_RYUK_DISABLED: "true"` (align with e2e-phase6-quick:486)
  - Add pre-flight `docker version && docker info` step (verifies Docker daemon)
  - Add post-step `if: always()` sweep removing orphan `org.testcontainers=true` containers + volume prune
- **D-13:** Documentation: `docs/operations.md` carries "Local development test prerequisites" section: Docker daemon, mkcert, pnpm-workspace order, `make tls-trust`, etc.

### Plan Split + Wave Strategy (A4 advisor)

- **D-14:** Use **Option B — 6 mid-granular plans**:
  - **18.1.2-01** Docker probe + setupFile correctness (D-02 + verify reaper-helper wire-up)
  - **18.1.2-02** Reaper-helper test hardening + shared-pg fixture + `singleThread` (D-03 + D-04 + D-05)
  - **18.1.2-03** Integration test migration cluster 1 (api: usage + streaming-usage + web-search + ratelimit)
  - **18.1.2-04** Bucket B + C: BYOK guard refactor + audio fixture + otel SIGTERM + locale-coverage (D-07 + D-08 + D-09 + D-10)
  - **18.1.2-05** CI/CD adoption (D-11 + D-12 + D-13) + Integration test migration cluster 2 (conversations + notes + worker + data)
  - **18.1.2-06** ROADMAP + STATE sync + v2.1 milestone close DECLARATION (was CLOSED-WITH-FOLLOWUP after 18.1.1)
- **D-15:** **STRICTLY SEQUENTIAL** execute per Phase 18.1.1 race-condition lesson (`7903858` collapse). No parallel waves. Plan dependency: 01 → 02 → 03 → 04 → 05 → 06.
- **D-16:** TDD per D-39 (Phase 18.1 doctrine): pre-existing RED → single GREEN with verbatim error in commit body. New functionality → 2-commit RED+GREEN pair.
- **D-17:** Estimated commit count: **~15-20 atomic commits** (reduced from initial ~28-32 estimate due to scope reduction A1+A2).
- **D-18:** Each plan must fit in ONE executor spawn (~5-7 commits) per W-3 lesson from 18.1.1. If plan size grows mid-execute → HALT + partial SUMMARY + fresh spawn.

### Constitutional / Process

- **D-19:** ZERO `--no-verify`. HALT on lefthook fire.
- **D-20:** Coverage ≥ 90/90/90/90 on new code (Docker probe + shared-pg fixture + onSignal export).
- **D-21:** D-43 i18n parity en+ru in SAME atomic commit (Bucket C).
- **D-22:** ZERO new top-level deps.
- **D-23:** End-of-phase aggregate `pnpm test` exits 0 on Docker-running machine. HALT for D-48 carve if new categories emerge.
- **D-24:** SR-2 partman + SR-6 helm OUT OF SCOPE per A2. SR-2 reclassified to D-A2 forward work; SR-6 already GREEN. Update Phase 18.1.2 success criteria in ROADMAP to drop these.

### Scope Reduction Acknowledgments (advisor findings)

- **D-25:** Initial 37-failure inventory was **stale** (came from Phase 18.1.1-06 aggregate run pre-completion of subsequent plans). Current run = **11 hard failures**. A1 and A2 independently verified.
- **D-26:** Sequential execute mandate carries from 18.1.1 lesson. Phase 18.1.2 has SIX plans — six executor spawns minimum.
- **D-27:** W-3 context-budget discipline: each plan ≤ 7 commits. Spawn FRESH executor per plan.

### Claude's Discretion

- HALT recipes follow Phase 18.1-06 three-branch user-choice pattern.
- Per-plan SUMMARY artefacts ≤ 100 LOC each.
- Final ROADMAP edits include v2.1 milestone CLOSED declaration (was CLOSED-WITH-FOLLOWUP after 18.1.1).
- Operator deferrals preserved: axe live bake, visual snapshots bake, FSL scrub, GHA e2e-cjm run.
- After D-24 scope reduction, Phase 18.1.2 closes in ~3-5h vs 18.1.1's ~5-6h. Less heroic.

</decisions>

<canonical_refs>
## Canonical References

### Phase 18.1 / 18.1.1 precedents
- `.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md` (D-01..D-30)
- `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md` (D-01..D-48)
- `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-DISCUSSION-LOG.md` (race-condition lesson)
- `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-{01..06}-SUMMARY.md`

### Bucket A — Testcontainer infra
- `tools/testcontainer-reaper-setup.ts:19` (extension target for D-02)
- `tools/global-vitest-teardown.ts:53-62` (existing prune helper)
- `apps/api/vitest.setup.ts:10` (re-export point; add TESTCONTAINERS_REUSE_ENABLE)
- `apps/api/tests/unit/routes/__tests__/usage.integration.test.ts:75-82` (per-file boot pattern to migrate)
- `apps/api/vitest.config.ts` + `apps/worker/vitest.config.ts` + `packages/data/vitest.config.ts` (singleThread sites)

### Bucket B — Non-testcontainer
- `packages/byok-guard/src/index.ts:242` (process.exit refactor target)
- `tests/fixtures/audio/sample-1sec.wav` (missing — find real location)
- `apps/api/src/otel-bootstrap.ts:144-148` (export onSignal)
- `apps/api/tests/unit/otel-bootstrap.test.ts:125-131` (refactor to direct invocation)

### Bucket C — Content drift
- `.planning/phases/07-frontend-ui-spec/UI-SPEC-end-user.md:588,1680` (oracle)
- `apps/web/src/locales/en/end-user.json:79` (bundle to fix)
- `apps/web/src/locales/ru/end-user.json` (ru parity)
- `apps/web/src/components/screens/account/__tests__/AccountClient.test.tsx:34` (component-test fixture)

### Bucket D — CI/CD
- `.github/workflows/ci.yml:78-95` (test job edit target)
- `.github/workflows/ci.yml:453-507` (e2e-phase6-quick precedent for Ryuk-disabled + cleanup)
- `feedback_testcontainers_cleanup_audit.md` (operator memory)

### Constitutional / Process
- `.planning/PROJECT.md` §Engineering Discipline rules 1-10 (TDD, coverage, no `--no-verify`, audit trail)
- CLAUDE.md root

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`tools/testcontainer-reaper-setup.ts`** (Phase 18.1.1 D-08): extend with availability probe (D-02).
- **`tools/global-vitest-teardown.ts:53-62`**: existing prune helper; aligns with CI cleanup sweep (D-12).
- **`@testcontainers/postgresql` `withReuse(true)`**: first-class feature, no new dep (D-03).
- **`apps/api/vitest.setup.ts`** (Phase 18.1.1 D-08): re-export point for shared infra (D-03 env wire-up).
- **e2e-phase6-quick CI job** (ci.yml:453-507): proven `TESTCONTAINERS_RYUK_DISABLED=true` + Docker cleanup pattern; apply to `test` job (D-12).

### Established Patterns
- **Sequential execute** (Phase 18.1.1 race lesson): no parallel waves, one executor spawn per plan.
- **W-3 context-budget discipline** (Phase 18.1.1): plan ≤ 7 commits per spawn.
- **TDD doctrine** (D-39 Phase 18.1): pre-existing RED → single GREEN; new code → 2-commit pair.
- **i18n parity en+ru** in same commit (Phase 14).
- **HALT-and-escalate** (Phase 18.1-06): 3-branch user-choice on novel failures.

### Integration Points
- Docker availability probe wired into `setupFiles` → integration tests `describe.skip` early.
- `getSharedPostgres()` accessor → `*.integration.test.ts` `beforeAll` uses shared container.
- `singleThread: true` glob → forces serial integration runs per workspace.
- CI workflow `test` job + cleanup step → matches local Docker hygiene.

</code_context>

<specifics>
## Specific Ideas

- Stale advisor count (37) vs actual (11): document scope-correction in DISCUSSION-LOG for retrospective.
- BYOK guard refactor `process.exit(1)` → `throw BYOKGuardError`: callers at api + worker entrypoints handle log+exit. Process-boundary failure should be at entrypoint, not in a library.
- Schema-per-file isolation: filename hash via `path.basename(__filename, '.test.ts')` for deterministic schema names; no collisions across files.
- `withReuse()` requires `TESTCONTAINERS_REUSE_ENABLE=true` env — set in vitest.setup.ts, NOT in package.json scripts (test-only).
- `singleThread: true` may slow tests slightly; acceptable trade-off for predictable runs.

</specifics>

<deferred>
## Deferred Ideas

- **D-A2 partman lint-rls** (per A2 reclassification): real implementation in `tools/lint-rls.ts` for partman child detection — separate phase or roadmap forward work, not 18.1.2.
- **Phase 19.1/19.2/19.3/19.4** — real downstream code for repointed @cjm tags (unchanged).
- **A2 Option C two-tier rate-limit** (Phase 18.1 deferred to v3).
- **Chromatic** (visual regression SaaS) — rejected per zero-new-deps.
- **Operator-side**: FSL scrub, GHA e2e-cjm first run, axe baseline live bake, visual snapshot bake.

### Reviewed Todos (not folded)
- None.

</deferred>

---

*Phase: 18.1.2-aggregate-sweep-test-debt*
*Context gathered: 2026-05-15*
