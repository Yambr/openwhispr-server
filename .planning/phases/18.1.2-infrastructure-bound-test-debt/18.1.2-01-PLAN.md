---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/testcontainer-availability.ts
  - tools/__tests__/testcontainer-availability.test.ts
  - tools/testcontainer-reaper-setup.ts
autonomous: true
requirements:
  - SR-1
  - SR-8
must_haves:
  truths:
    - "`tools/testcontainer-availability.ts` exports `assertDockerAvailable(): boolean` that runs `execFileSync('docker', ['info'], {timeout: 2000})` inside try/catch."
    - "On Docker-daemon-down: sets `process.env.OPENWHISPR_SKIP_TESTCONTAINERS=1` + logs structured warning + returns `false`; NEVER throws (pitfall §1)."
    - "On Docker-daemon-up: returns `true`, no env mutation."
    - "Wired into `tools/testcontainer-reaper-setup.ts:19` AFTER `installSignalHook()` (D-02 — single setupFile, do NOT add a second setupFiles entry per PATTERNS surface 1)."
    - "RED commit creates failing sibling test mocking `node:child_process.execFileSync`; GREEN commit implements probe (D-39 2-commit pair for NEW functionality)."
    - "Coverage ≥ 90/90/90/90 on `tools/testcontainer-availability.ts` (D-20)."
    - "ZERO `--no-verify` (D-19); lefthook clean."
  artifacts:
    - path: "tools/testcontainer-availability.ts"
      provides: "Docker availability probe exporting assertDockerAvailable()."
      contains: "export function assertDockerAvailable"
    - path: "tools/__tests__/testcontainer-availability.test.ts"
      provides: "Sibling vitest covering daemon-up + daemon-down + timeout branches."
      contains: "vi.mock(\"node:child_process\""
    - path: "tools/testcontainer-reaper-setup.ts"
      provides: "Wires assertDockerAvailable() after installSignalHook() at line 19."
      contains: "assertDockerAvailable"
  key_links:
    - from: "every *.integration.test.ts (consumed in Plan 02+)"
      to: "tools/testcontainer-availability.ts (via reaper setupFile)"
      via: "setupFile-set env OPENWHISPR_SKIP_TESTCONTAINERS=1 → integration tests describe.skip in beforeAll"
      pattern: "OPENWHISPR_SKIP_TESTCONTAINERS"
---

<objective>
Land the Docker availability probe (D-02) + verify setupFile wire-up correctness. Foundational primitive for Plan 02 (shared-pg fixture skips when daemon down) and Plans 03/05 (integration cluster migrations gate on env).

**2 commits (D-39 NEW functionality → RED+GREEN pair).**

Sequential per D-15. This is Plan 01 of 6 — must complete before Plan 02 spawns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@CLAUDE.md
@tools/testcontainer-reaper-setup.ts
@tools/global-vitest-teardown.ts
@tools/lint-cjm-doc.test.ts
@tools/lint-weak-assertions.test.ts
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18.1.2-01-01: RED — testcontainer-availability sibling test (mocked execFileSync)</name>
  <files>tools/__tests__/testcontainer-availability.test.ts</files>
  <behavior>
- Test 1 (daemon-up): `execFileSync` returns Buffer → `assertDockerAvailable()` returns `true`; `process.env.OPENWHISPR_SKIP_TESTCONTAINERS` is NOT set.
- Test 2 (daemon-down): `execFileSync` throws Error('connect ENOENT /var/run/docker.sock') → returns `false`; env IS set to `"1"`; structured warning logged once.
- Test 3 (timeout): `execFileSync` throws Error with `code: 'ETIMEDOUT'` → returns `false`; env set; no rethrow (pitfall §1).
- Test 4 (idempotent): two consecutive calls under daemon-down only log warning once (memoised). Optional — relaxation acceptable.
  </behavior>
  <action>
Per D-02 + D-39 RED leg + PATTERNS surface 1.

1. Create `tools/__tests__/testcontainer-availability.test.ts` mirroring `tools/lint-cjm-doc.test.ts:1-40` SPDX/JSDoc header style. Use `vi.mock("node:child_process", () => ({ execFileSync: vi.fn() }))` per `tools/lint-weak-assertions.test.ts:4` pattern.
2. Per-test reset: `delete process.env.OPENWHISPR_SKIP_TESTCONTAINERS` in `beforeEach`; restore mock in `afterEach`.
3. Import `assertDockerAvailable` from `../testcontainer-availability` — symbol will not exist; import is the RED trigger.
4. Run `pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts 2>&1 | tee /tmp/01-01-red.log` — MUST fail (module not found).
5. Commit: `test(18.1.2-01-01): red — testcontainer-availability probe contract`. Body MUST include verbatim RED output (Phase 18.1 §7).

WHY: pitfall §1 — `execFileSync` w/ `timeout: 2000` throws on timeout; the probe MUST swallow per `tools/global-vitest-teardown.ts:53-62` discipline. RED test pins the contract before implementation.
  </action>
  <verify><automated>pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts 2>&1 | grep -E "(Cannot find module|MODULE_NOT_FOUND|FAIL)" | head -3</automated></verify>
  <done>1 commit; test file present; verbatim RED captured in commit body; module-not-found / import-error visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.2-01-02: GREEN — implement assertDockerAvailable + wire into reaper setupFile</name>
  <files>tools/testcontainer-availability.ts, tools/testcontainer-reaper-setup.ts</files>
  <action>
Per D-02 + D-39 GREEN leg + PATTERNS surface 1 + RESEARCH §8 pitfall §1.

1. Create `tools/testcontainer-availability.ts` (≤ 40 LOC). Mirror SPDX/JSDoc header of `tools/testcontainer-reaper-setup.ts:1-19`. Body:
   - Import `execFileSync` from `node:child_process` + a structured logger (reuse boot-logger pattern if available; otherwise `console.warn` with JSON payload — single line, no deps per D-22).
   - Module-scoped `let warned = false` for idempotent warning.
   - `export function assertDockerAvailable(): boolean` — try `execFileSync("docker", ["info"], { timeout: 2000, stdio: "ignore" })` → return `true`. catch → `process.env.OPENWHISPR_SKIP_TESTCONTAINERS = "1"`; if `!warned` emit structured warn `{ event: "docker.unavailable", reason: err.message }`; `warned = true`; return `false`.
   - **Do NOT throw.** **Do NOT call `process.exit`.** (pitfall §1).
2. Edit `tools/testcontainer-reaper-setup.ts:19`: AFTER `installSignalHook()` call, add `assertDockerAvailable()` (single line). Add import. Keep file ≤ 25 LOC.
3. Re-run RED test — must now PASS.
4. Verify reaper file: `grep -c "assertDockerAvailable" tools/testcontainer-reaper-setup.ts` = 2 (import + call).
5. Run coverage gate: `pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts --coverage` — assert ≥ 90/90/90/90 on the new file (D-20).
6. Commit: `feat(18.1.2-01-02): green — assertDockerAvailable probe + wire into reaper setupFile`. Body cites D-02 + pitfall §1.

WHY: pitfall §1 — daemon-down MUST NOT throw or downstream setupFile chain breaks. WHY: PATTERNS surface 1 — single setupFile (`testcontainer-reaper-setup.ts`) orders setupFiles; do NOT add a second entry. WHY: D-22 — no `dockerode` import; `execFileSync` is stdlib.
  </action>
  <verify><automated>pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts 2>&1 | grep -E "(✓|PASS|4 passed)" | head -3 && grep -c "assertDockerAvailable" tools/testcontainer-reaper-setup.ts</automated></verify>
  <done>1 commit; sibling test GREEN (4 cases); reaper wires probe after installSignalHook; coverage ≥ 90/90/90/90; ZERO new deps; lefthook clean.</done>
</task>

</tasks>

<verification>
After both commits:

```bash
cd /Users/nick/openwhispr-server
pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts
pnpm exec vitest run tools/__tests__/testcontainer-availability.test.ts --coverage 2>&1 | grep -E "(Lines|Branches|Functions|Statements).*9[0-9]"
git log --oneline -n 2 | grep -cE "(test|feat)\(18\.1\.2-01-0[12]\)"  # expect 2
grep -c "assertDockerAvailable" tools/testcontainer-reaper-setup.ts    # expect 2
```
</verification>

<success_criteria>
1. 2 atomic commits (RED + GREEN); commit messages match D-16 format.
2. `assertDockerAvailable()` returns boolean; never throws; never exits process.
3. Reaper setupFile invokes probe AFTER `installSignalHook()` at line 19+.
4. Coverage ≥ 90/90/90/90 on `tools/testcontainer-availability.ts` (D-20).
5. ZERO new deps (D-22); ZERO `--no-verify` (D-19); lefthook clean.
</success_criteria>

<rollback>
- Probe accidentally throws on daemon-down → revert GREEN; re-implement with broader try/catch including `ENOENT` for missing `docker` binary entirely.
- Reaper setupFile order breaks because probe sets env BEFORE other setupFiles → move probe call to FIRST line of reaper (before `installSignalHook()`); document in SUMMARY.
- HALT recipe — if `pnpm exec vitest` cannot find `node:child_process` mock under workspace root, HALT per 18.1-06 3-branch user-choice.
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-01-SUMMARY.md` with:
- 2 commit SHAs
- Coverage report on new file
- Lefthook status (expected clean)
- Reaper file delta (line-count + grep proof of wire-up)
</output>
