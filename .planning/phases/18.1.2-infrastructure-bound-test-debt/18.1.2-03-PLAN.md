---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 03
type: execute
wave: 3
depends_on: ["18.1.2-02"]
files_modified:
  - apps/api/tests/unit/routes/__tests__/usage.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/web-search.integration.test.ts
  - apps/api/tests/unit/routes/__tests__/web-search-ratelimit.integration.test.ts
autonomous: true
requirements:
  - SR-1
must_haves:
  truths:
    - "All 4 files import `getSharedPostgres` + `bootstrapSharedRoles` from `../../../support/shared-pg` (per Plan 02 D-03 artefact)."
    - "Each `beforeAll`: replaces per-file `new PostgreSqlContainer(...).start()` with `container = await getSharedPostgres()`; runs `bootstrapSharedRoles(pool)`; then `CREATE SCHEMA IF NOT EXISTS test_<sanitised-basename>` + `SET search_path TO test_<x>, public` per-connection."
    - "Each `afterAll`: `DROP SCHEMA test_<x> CASCADE` inside `try/finally` (pitfall §3 — must run on failure); does NOT call `container.stop()` (container shared)."
    - "Schema-name sanitisation: `basename(fileURLToPath(import.meta.url), '.test.ts').replace(/[^a-z0-9]/gi,'_').toLowerCase()` — Postgres-identifier-safe (PATTERNS surface 3 risk)."
    - "Each `beforeAll` also short-circuits: `if (process.env.OPENWHISPR_SKIP_TESTCONTAINERS === '1') { describe.skip(...) }` (Plan 01 wire-up downstream)."
    - "Migration applier: each file calls `migrate(drizzle(ownerPool), { migrationsFolder: MIGRATIONS_FOLDER, migrationsSchema: '_meta', migrationsTable: '__drizzle_migrations' })` against the per-file schema (RESEARCH §2 canonical boot)."
    - "4 atomic commits, one per file (D-39 single GREEN — pre-existing testcontainer port-exhaustion is the RED on `main`)."
    - "Each commit body MUST cite verbatim port-exhaustion error from baseline + D-03/D-04 references."
    - "ZERO `--no-verify` (D-19); lefthook clean; ≤ 4 commits (D-18)."
  artifacts:
    - path: "apps/api/tests/unit/routes/__tests__/usage.integration.test.ts"
      provides: "Cluster #1 file 1 — migrated to shared-pg + schema isolation."
      contains: "getSharedPostgres"
    - path: "apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts"
      provides: "Cluster #1 file 2 — same migration."
      contains: "getSharedPostgres"
    - path: "apps/api/tests/unit/routes/__tests__/web-search.integration.test.ts"
      provides: "Cluster #1 file 3."
      contains: "getSharedPostgres"
    - path: "apps/api/tests/unit/routes/__tests__/web-search-ratelimit.integration.test.ts"
      provides: "Cluster #1 file 4."
      contains: "getSharedPostgres"
  key_links:
    - from: "Cluster #1 4 files"
      to: "apps/api/tests/support/shared-pg.ts (Plan 02 artefact)"
      via: "import getSharedPostgres in beforeAll"
      pattern: "import.*shared-pg"
    - from: "Each file's afterAll"
      to: "per-file PostgreSQL schema"
      via: "try/finally DROP SCHEMA CASCADE"
      pattern: "DROP SCHEMA.*CASCADE"
---

<objective>
Migrate Bucket-A integration cluster #1 (4 files, D-06) to the shared-pg fixture from Plan 02. Per-file `PostgreSqlContainer` boots → single shared container + schema-per-file isolation (D-03 + D-04). Closes ~4 of the 22 port-exhaustion-cascade test files.

**4 commits, one per file (D-39 single GREEN — pre-existing RED).** Sequential after Plan 02. Sequential before Plan 04.

Commit pattern (mechanical, identical per file):
1. `fix(18.1.2-03-01): migrate usage.integration.test.ts to shared-pg + schema isolation (D-03/D-04)`
2. `fix(18.1.2-03-02): migrate streaming-usage.integration.test.ts to shared-pg ...`
3. `fix(18.1.2-03-03): migrate web-search.integration.test.ts to shared-pg ...`
4. `fix(18.1.2-03-04): migrate web-search-ratelimit.integration.test.ts to shared-pg ...`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-02-SUMMARY.md
@CLAUDE.md
@apps/api/tests/support/shared-pg.ts
@apps/api/tests/unit/routes/__tests__/usage.integration.test.ts
@apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts
@apps/api/tests/unit/routes/__tests__/web-search.integration.test.ts
@apps/api/tests/unit/routes/__tests__/web-search-ratelimit.integration.test.ts
@packages/data/migrations
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1.2-03-01: Migrate usage.integration.test.ts to shared-pg + schema isolation</name>
  <files>apps/api/tests/unit/routes/__tests__/usage.integration.test.ts</files>
  <action>
Per D-03 + D-04 + RESEARCH §2 canonical boot + PATTERNS surface 3 + pitfall §3.

1. Run `pnpm --filter @openwhispr/api test usage.integration 2>&1 | tee /tmp/03-01-red.log` to capture verbatim port-exhaustion / ECONNREFUSED proof (Phase 18.1 §7).
2. Edit `usage.integration.test.ts:14-100` (boot block):
   - Replace `const container = await new PostgreSqlContainer("postgres:17-alpine").withDatabase("openwhispr").withUsername("postgres_super").withPassword("super-pw").start();` with `const container = await getSharedPostgres();`. Add import: `import { getSharedPostgres, bootstrapSharedRoles } from "../../../support/shared-pg";`.
   - Delete the inline `CREATE ROLE openwhispr_owner` / `openwhispr_app` / `GRANT` / `ALTER DATABASE` block — replaced by single `await bootstrapSharedRoles(ownerPool);` (or `superPool` per current naming).
   - After role bootstrap: compute schema name via `const SCHEMA_NAME = basename(fileURLToPath(import.meta.url), '.test.ts').replace(/[^a-z0-9]/gi,'_').toLowerCase();` (defensive: also clip to 50 chars to keep room for prefixing).
   - Execute `CREATE SCHEMA IF NOT EXISTS ${SCHEMA_NAME}` against owner pool.
   - Configure connection-pool callers to `SET search_path TO ${SCHEMA_NAME}, public` (PATTERNS surface 3 risk — choose ONE: search_path OR full qualification; document choice in commit body).
   - Run migration: `await migrate(drizzle(ownerPool), { migrationsFolder: MIGRATIONS_FOLDER, migrationsSchema: "_meta", migrationsTable: "__drizzle_migrations" });` against the per-file schema. Pitfall §3: if `migrate()` cannot scope to a non-default schema, fall back to PATTERNS option (b) — global setup migrates to `public`; per-file schemas use `CREATE TABLE ... LIKE public.* INCLUDING ALL` cloning. **Document the chosen approach in commit body.**
3. Edit `afterAll`:
   - Wrap in `try/finally`. Remove `await container.stop()` (container shared — DO NOT stop).
   - In `finally`: `await ownerPool.query(\`DROP SCHEMA IF EXISTS ${SCHEMA_NAME} CASCADE\`)`. Close `ownerPool.end()` (per-file pool ends; shared container persists).
4. Add top-of-`describe`: `if (process.env.OPENWHISPR_SKIP_TESTCONTAINERS === '1') { describe.skip('usage.integration — Docker unavailable', () => { it.todo('skipped'); }); return; }` — wire-up to Plan 01.
5. Re-run: `pnpm --filter @openwhispr/api test usage.integration 2>&1 | tee /tmp/03-01-green.log`. Must exit 0 (GREEN) on Docker-running machine; skip cleanly otherwise.
6. Single commit: `fix(18.1.2-03-01): migrate usage.integration.test.ts to shared-pg + schema isolation (D-03/D-04)`. Body MUST include: (a) verbatim RED from step 1, (b) D-03/D-04/§9 Δ-2 citations, (c) chosen migration-applier approach (search_path vs schema-clone).

WHY: pitfall §2 — `bootstrapSharedRoles` is the idempotent guard; without it second file run crashes `role already exists`. WHY: pitfall §3 — `drizzle migrate` reads `__drizzle_migrations` from a fixed schema; per-file isolation requires either search_path-scoped migrate or schema-clone fallback. WHY: PATTERNS surface 3 — schema name must be Postgres-identifier-safe; sanitise via regex.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test usage.integration 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; usage.integration GREEN (or skip-clean); commit body has verbatim RED + migration-applier choice doc; container.stop() removed; afterAll drops schema in try/finally.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-03-02: Migrate streaming-usage.integration.test.ts to shared-pg</name>
  <files>apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical pattern to 03-01.

1. Capture RED: `pnpm --filter @openwhispr/api test streaming-usage.integration 2>&1 | tee /tmp/03-02-red.log`.
2. Apply identical migration as 03-01: replace `PostgreSqlContainer.start()` → `getSharedPostgres()`; remove inline CREATE ROLE; add `bootstrapSharedRoles`; per-file schema; try/finally DROP SCHEMA; no container.stop(); skip-gate guard.
3. Use the SAME `SCHEMA_NAME` derivation pattern (filename-based).
4. Verify GREEN: `pnpm --filter @openwhispr/api test streaming-usage.integration`.
5. Commit: `fix(18.1.2-03-02): migrate streaming-usage.integration.test.ts to shared-pg + schema isolation`. Body has verbatim RED + cites Plan 03-01 as template.

WHY: pitfall §3 — IF 03-01 chose schema-clone (option b) over search_path (option a), 03-02 MUST match the same choice (consistency); if mismatched, two files in same container produce conflicting `__drizzle_migrations` state.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test streaming-usage.integration 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; streaming-usage.integration GREEN (or skip-clean); commit body cites 03-01 template.</done>
</task>

<task type="auto">
  <name>Task 18.1.2-03-03: Migrate web-search.integration.test.ts to shared-pg</name>
  <files>apps/api/tests/unit/routes/__tests__/web-search.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical pattern.

1. Capture RED.
2. Apply migration — identical shape to 03-01/02.
3. Verify GREEN.
4. Commit: `fix(18.1.2-03-03): migrate web-search.integration.test.ts to shared-pg + schema isolation`. Body has verbatim RED.

WHY: web-search.integration shares the same canonical-boot pattern (RESEARCH §2 spot-check) — identical migration applies.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test web-search.integration 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; web-search.integration GREEN (or skip-clean).</done>
</task>

<task type="auto">
  <name>Task 18.1.2-03-04: Migrate web-search-ratelimit.integration.test.ts to shared-pg</name>
  <files>apps/api/tests/unit/routes/__tests__/web-search-ratelimit.integration.test.ts</files>
  <action>
Per D-03 + D-04 + identical pattern. Closes Cluster #1.

1. Capture RED.
2. Apply migration — identical shape.
3. Verify GREEN.
4. Commit: `fix(18.1.2-03-04): migrate web-search-ratelimit.integration.test.ts to shared-pg + schema isolation`. Body has verbatim RED.
5. **Cluster #1 done** — run aggregate: `pnpm --filter @openwhispr/api test "tests/unit/routes/__tests__/(usage|streaming-usage|web-search|web-search-ratelimit).integration" 2>&1 | tail -10`. All 4 GREEN.

WHY: pitfall §12 — Cluster #1 is the 4-file mechanical batch. After Plan 03 closes, Plan 05 picks up cluster #2 (18 files). Phase budget intact (D-18 ≤ 7 commits per plan).
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test web-search-ratelimit.integration 2>&1 | grep -E "(✓|PASS|passed|SKIP)" | head -5</automated></verify>
  <done>1 commit; web-search-ratelimit.integration GREEN (or skip-clean); 4-file cluster GREEN aggregate verified.</done>
</task>

</tasks>

<verification>
After 4 commits:

```bash
cd /Users/nick/openwhispr-server
pnpm --filter @openwhispr/api test usage.integration
pnpm --filter @openwhispr/api test streaming-usage.integration
pnpm --filter @openwhispr/api test web-search.integration
pnpm --filter @openwhispr/api test web-search-ratelimit.integration
git log --oneline -n 4 | grep -cE "fix\(18\.1\.2-03-0[1-4]\)"  # expect 4
grep -l "getSharedPostgres" apps/api/tests/unit/routes/__tests__/*.integration.test.ts | wc -l  # ≥ 4
```
</verification>

<success_criteria>
1. 4 atomic commits (one per file) per D-39 single-GREEN doctrine.
2. All 4 cluster-#1 files import `getSharedPostgres` + `bootstrapSharedRoles`.
3. Each file's `afterAll` does `DROP SCHEMA ... CASCADE` in `try/finally`; does NOT call `container.stop()`.
4. Skip-gate (`OPENWHISPR_SKIP_TESTCONTAINERS=1`) honored.
5. Each commit body has verbatim RED + consistent migration-applier choice.
6. ZERO `--no-verify` (D-19); lefthook clean.
</success_criteria>

<rollback>
- Migration applier per-file schema fails because `__drizzle_migrations` lives in `_meta` schema (shared) → revert 03-01; switch to PATTERNS option (b) schema-clone; document fallback in SUMMARY; re-apply 03-01..04 with new shape.
- RLS context leaks between schemas (search_path race) → revert affected commits; switch to full-qualified SQL in each file (no search_path); re-verify per-file.
- Sibling integration tests (cluster #2) start failing because shared container's role state mutated → HALT per 18.1-06 3-branch user-choice; revert latest commit; investigate.
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-SUMMARY.md` with:
- 4 commit SHAs
- Migration-applier choice (search_path vs schema-clone) documented
- Cluster-#1 aggregate `pnpm test` result delta
- Lefthook status (clean)
</output>
