---
phase: 18.1.2-infrastructure-bound-test-debt
plan: 06
type: execute
wave: 6
depends_on: ["18.1.2-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SR-8
must_haves:
  truths:
    - "`.planning/ROADMAP.md` Phase 18.1.2 entry marked Complete; Plans table shows `6/6`; date stamp `2026-05-15`."
    - "ROADMAP Phase 18.1.2 success-criteria reflect scope reduction D-24: SR-2 partman + SR-6 helm DROPPED; SR-2 reclassified to D-A2 forward work (note carried in entry)."
    - "ROADMAP Phase 18.1.2 success criteria updated to match the actual delivered surface: Docker probe, shared-pg fixture, schema isolation, singleThread, BYOK throw-not-exit, audio path Δ-1, otel onSignal, locale parity, CI Ryuk + cleanup, ops docs."
    - "v2.1 milestone declared **CLOSED** (was `CLOSED-WITH-FOLLOWUP` after 18.1.1) in the Progress Table — replace the existing `**— v2 milestone (CLOSED-WITH-FOLLOWUP 2026-05-15) —**` divider with `**— v2.1 milestone (CLOSED 2026-05-15) —**` AND ensure the phases listed under that divider (13, 12, 14, 15, 16, 17, 18, 18.1, 18.1.1, 18.1.2) are accurate."
    - "`.planning/STATE.md` Decisions section records D-01..D-27 disposition (locked → applied) + the 3 Δ-discrepancies resolved (Δ-1 audio, Δ-2 cluster #2 count, Δ-3 BYOK cascade)."
    - "STATE.md Pending todos / blockers updated: remove items resolved by this phase; preserve operator deferrals (FSL scrub, GHA e2e-cjm first run, axe baseline live bake, visual snapshot bake) verbatim per CONTEXT Deferred Ideas."
    - "Single atomic commit (D-18 ceiling honored at 1): `docs(18.1.2-06-01): ROADMAP + STATE sync + v2.1 milestone CLOSED declaration`."
    - "ZERO `--no-verify` (D-19); lefthook clean (`docs(...)` scope passes commitlint)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18.1.2 marked Complete; v2.1 milestone CLOSED."
      contains: "v2.1 milestone (CLOSED"
    - path: ".planning/STATE.md"
      provides: "Updated decisions, pending todos, blockers."
      contains: "18.1.2"
  key_links:
    - from: "Phase 18.1.2 entry in ROADMAP"
      to: "Progress Table v2.1 milestone divider"
      via: "Both updated in same commit (D-18 1-commit budget)"
      pattern: "v2.1 milestone"
---

<objective>
Close-out plan for Phase 18.1.2. Update `.planning/ROADMAP.md` + `.planning/STATE.md` to reflect:
1. Plans 18.1.2-01..05 delivered.
2. Scope reduction D-24 (SR-2 + SR-6 dropped) reflected in success criteria.
3. v2.1 milestone state changes from `CLOSED-WITH-FOLLOWUP` → `CLOSED` (this phase closed the followup work from 18.1.1).
4. STATE.md decisions + pending todos synchronised.

**EXACTLY 1 commit (D-18 honored).** Sequential after Plan 05. Last plan in Phase 18.1.2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-CONTEXT.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-RESEARCH.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-PATTERNS.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-01-SUMMARY.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-02-SUMMARY.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-03-SUMMARY.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-04-SUMMARY.md
@.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-05-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1.2-06-01: ROADMAP + STATE sync + v2.1 milestone CLOSED declaration</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Per D-24 + CONTEXT Claude's-Discretion v2.1 milestone close note.

**Pre-flight read** all 5 prior SUMMARYs to gather actual commit counts + delivered surface.

1. Edit `.planning/ROADMAP.md` Phase 18.1.2 entry (around line 868):
   - **Goal**: rewrite to reflect actual delivery: "Resolve testcontainer port-exhaustion + 4 non-infra failures via D-02 Docker probe, D-03 shared-pg fixture, D-04 schema isolation, D-05 singleThread, D-07 BYOK throw-not-exit, D-08 audio path Δ-1, D-09 otel onSignal export, D-10 locale parity, D-11/12 CI Ryuk + cleanup, D-13 ops docs."
   - **Requirements**: update SR-1..SR-8 list to reflect D-24 (DROP SR-2 partman → mark "deferred to D-A2 forward work"; DROP SR-6 helm → mark "verified GREEN 163/163"); keep SR-1 (testcontainer), SR-3 (worker), SR-4 (data), SR-5 (locale-coverage), SR-7 (otel), SR-8 (aggregate exit 0).
   - **Success Criteria**: replace stale criteria with the 6 must-have outcomes actually delivered (probe + fixture + cluster migrations + CI adoption + ops docs + v2.1 close). Drop `services: docker:dind` mention (rejected per A3).
   - **Plans**: change `4-6 plans (TBC at /gsd-discuss-phase 18.1.2)` → `6 plans`. Append plan list (checked-off):
     ```
     Plans:
     - [x] 18.1.2-01-PLAN.md — Docker probe + setupFile correctness (D-02)
     - [x] 18.1.2-02-PLAN.md — shared-pg fixture + singleThread + reaper hardening (D-03/04/05)
     - [x] 18.1.2-03-PLAN.md — integration cluster #1 migration (4 files)
     - [x] 18.1.2-04-PLAN.md — Bucket B + C fixes (BYOK + audio Δ-1 + otel + locale)
     - [x] 18.1.2-05-PLAN.md — integration cluster #2 (18 files) + CI/CD adoption + ops docs
     - [x] 18.1.2-06-PLAN.md — ROADMAP + STATE sync + v2.1 milestone CLOSED
     ```
   - **Open question**: resolve. Replace with answer: "Docker availability is a **soft gate** via `tools/testcontainer-availability.ts` probe — sets `OPENWHISPR_SKIP_TESTCONTAINERS=1` env so integration tests `describe.skip` cleanly. Documented in `docs/operations.md` (D-13). Hard gate via CI config (D-11/12) requires Docker daemon up on the test job."

2. Edit Progress Table row for Phase 18.1.2:
   - Add row if missing OR update existing: `| 18.1.2. infrastructure-bound test debt | 6/6 | Complete | 2026-05-15 |`.

3. Edit the v2.1 milestone divider in the Progress Table (currently at line 905):
   - `**— v2 milestone (CLOSED-WITH-FOLLOWUP 2026-05-15) —**` → `**— v2.1 milestone (CLOSED 2026-05-15) —**` (follows CONTEXT Claude's-Discretion note).
   - Verify phases 13/12/14/15/16/17/18/18.1/18.1.1/18.1.2 all sit under this divider as Complete.

4. Edit `.planning/STATE.md`:
   - **Decisions** section: append a brief block stating "Phase 18.1.2 decisions D-01..D-27 locked at 2026-05-15; Δ-1 (audio path 4→5), Δ-2 (cluster #2 = 18 files, all in apps/api), Δ-3 (BYOK cascade transitively closed 2× entrypoint-db-shape) all resolved."
   - **Pending todos / blockers**: remove items closed by this phase (testcontainer port exhaustion, BYOK exit trap, audio fixture, otel SIGTERM, locale drift). Preserve operator deferrals **verbatim**: FSL history scrub, GHA e2e-cjm first run, axe baseline live bake, Playwright visual regression GREEN bake, UICONF-05 baseline.
   - **Project position cursor**: advance to "Phase 18.1.2 Complete; v2.1 milestone CLOSED" — exact wording matches the existing STATE.md cursor format (read the file first; match shape).

5. Verify markdown is well-formed: `pnpm lint:md .planning/ROADMAP.md .planning/STATE.md` (if linter available); otherwise grep-verify the divider edit landed exactly once: `grep -c "v2.1 milestone (CLOSED 2026-05-15)" .planning/ROADMAP.md` = 1.

6. Single commit: `docs(18.1.2-06-01): ROADMAP + STATE sync + v2.1 milestone CLOSED declaration`. Body MUST cite: D-24 scope reduction, CONTEXT Claude's-Discretion v2.1 close note, summary of plans 01..05 delivered (commit-count + key artefacts).

WHY: D-24 — silent stale criteria forbidden; ROADMAP MUST reflect actual delivery. WHY: v2.1 close — 18.1.1 left followup work; 18.1.2 closes it. Milestone advances to CLOSED. WHY: STATE.md cursor sync — next executor reads STATE.md; stale cursor causes mis-orientation.
  </action>
  <verify><automated>grep -c "v2.1 milestone (CLOSED 2026-05-15)" .planning/ROADMAP.md && grep -c "18.1.2" .planning/STATE.md && git log --oneline -n 1 | grep -c "18.1.2-06-01"</automated></verify>
  <done>1 commit; ROADMAP Phase 18.1.2 marked Complete; v2.1 milestone CLOSED; STATE.md decisions + cursor synced; operator deferrals preserved verbatim; lefthook clean.</done>
</task>

</tasks>

<verification>
After 1 commit:

```bash
cd /Users/dev/openwhispr-server
grep "v2.1 milestone (CLOSED" .planning/ROADMAP.md
grep "18.1.2.*Complete" .planning/ROADMAP.md
grep "18.1.2" .planning/STATE.md | head -5
git log --oneline -n 1 | grep "18.1.2-06-01"

# Phase-wide aggregate (D-23 final proof)
pnpm test 2>&1 | tail -5     # expect 0 failures on Docker-running machine (or skip-clean if no Docker)
```
</verification>

<success_criteria>
1. 1 atomic commit (D-18 honored).
2. ROADMAP Phase 18.1.2 marked Complete; Plans = 6/6; date 2026-05-15.
3. Success criteria reflect actual delivery (D-02..D-13 surface); SR-2 + SR-6 dropped per D-24.
4. v2.1 milestone CLOSED (was CLOSED-WITH-FOLLOWUP).
5. STATE.md decisions + cursor synced; operator deferrals preserved verbatim.
6. ZERO `--no-verify` (D-19); lefthook clean.
7. Phase-wide D-23 verification: `pnpm test` exits 0 on Docker-running machine (recorded in SUMMARY).
</success_criteria>

<rollback>
- Markdown linter fires on the divider edit → revert; re-author with exact bold/dashes matching existing v2 milestone divider syntax; re-apply.
- STATE.md cursor format drift (existing format differs from CONTEXT example) → read STATE.md first; match exact shape; re-apply.
- HALT recipe — if phase-wide `pnpm test` reveals NEW failure categories (D-48 carve), HALT 3-branch user-choice: (a) close 18.1.2 as-is + add 18.1.3, (b) absorb into this plan (busts D-18 — only if 1 file fix), (c) close phase with documented residual.
</rollback>

<output>
Create `.planning/phases/18.1.2-infrastructure-bound-test-debt/18.1.2-06-SUMMARY.md` with:
- 1 commit SHA
- Phase 18.1.2 final aggregate `pnpm test` result (D-23 final proof)
- Total commit count across plans 01..06 (expect ~15-18 per D-17)
- Operator deferrals carry-list (FSL scrub, GHA e2e-cjm, axe bake, visual bake)
- v2.1 milestone CLOSED declaration
- Lefthook status (clean)
</output>
