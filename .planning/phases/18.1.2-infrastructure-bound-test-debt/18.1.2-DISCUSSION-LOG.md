# Phase 18.1.2 — Discussion Log

**Date:** 2026-05-15
**Mode:** discuss (advisor-style; 4 parallel `gsd-advisor-researcher` agents)
**User pacing:** yolo — "Phase 18.1.2 теми же этапами"

## Trigger

Phase 18.1.1-06 closed v2.1 milestone CLOSED-WITH-FOLLOWUP. The 06 closure noted 37 remaining `pnpm test` failures as infrastructure-bound. User direction: close them per same etapy as 18.1/18.1.1.

## Gray areas selected

User accepted all 4 advisor scopes by default (yolo):

1. **A1** — Testcontainer infrastructure strategy (port exhaustion / withReuse / probe)
2. **A2** — Non-testcontainer failures (partman, helm, otel SIGTERM, locale)
3. **A3** — CI/CD adoption (docker:dind vs native vs gate)
4. **A4** — Plan split + execution strategy

## Critical Discovery — Scope Reduction

**A1 and A2 independently verified**: the 37-failure inventory was STALE. Current `pnpm test` shows **11 hard failures**, not 37.

Breakdown:
- **A2 SR-2 partman** (5 cases): NOT regressions — stub `throw PHASE_6_NOT_YET` cases scheduled for D-A2 forward work. **Reclassify out of 18.1.2.**
- **A2 SR-6 helm-unittest**: A2 ran `helm unittest .` and got 163/163 PASS. **Drop from 18.1.2 scope.**
- **A1**: real testcontainer failures = Docker Desktop port exhaustion under ~20 parallel `PostgreSqlContainer.start()` calls. Manifests as docker-modem buildPayload errors. NOT "Docker missing".
- **Bulk failures**: BYOK env guards (`packages/byok-guard/src/index.ts:242`), missing audio fixture, locale drift, otel SIGTERM trap.

## Questions asked and decisions made

### Q1 — Bucket A testcontainer infra strategy (A1)

**Options:**
- A. Hard Docker-availability gate
- B. Auto-boot stack in `pretest`
- C. `withReuse(true)` shared container
- **D. Hybrid (A+C+serialize)** ← selected

**Rationale:** A1 verified Docker IS available (`docker info` works). Failures are container *exhaustion*, not absence. Option D combines availability probe (CI/sandbox safety), shared container reuse (~20× faster), and `singleThread: true` for `*.integration.test.ts` (port-pool safety). 6 atomic commits, ~30 files touched.

### Q2 — Bucket B non-testcontainer failures (A2)

**Decisions:**
- D-07 BYOK guard: refactor `process.exit(1)` → `throw BYOKGuardError`. TDD pair.
- D-08 audio fixture: find real path; single-GREEN per 18.1 §7.
- D-09 otel SIGTERM: export `onSignal` from `apps/api/src/otel-bootstrap.ts:144-148`; refactor test to direct invocation. TDD pair.

**A2 also dropped:** SR-2 (partman = forward work) + SR-6 (helm = already green) from 18.1.2 scope.

### Q3 — Bucket C content drift (A2)

**Decision:** D-10 locale-coverage drift on `end-user.account.subtitle.body.text`. Update `en/end-user.json:79` + parallel ru + `AccountClient.test.tsx:34` fixture per UI-SPEC oracle. **Single commit (en+ru parity + fixture).**

### Q4 — Bucket D CI/CD adoption (A3)

**Options:**
- A. `services: docker:dind` — REJECTED (incompatible with GHA hosted runners; `privileged: true` blocked)
- **B. Native Docker on ubuntu-24.04 + cleanup** ← selected
- C. Gate on `CI && DOCKER_AVAILABLE` — REJECTED (violates CLAUDE.md rule 5)

**Rationale:** Option B is already the working pattern in `e2e-phase6-quick` job (ci.yml:453-507). Test job (lines 78-95) just needs alignment: `TESTCONTAINERS_RYUK_DISABLED=true` + pre-flight `docker version` check + post-step `if: always()` orphan sweep. Exact YAML diff provided by A3.

### Q5 — Plan split + execution strategy (A4)

**Options:**
- A. 4 plans (33/8/CI/sync) — REJECTED (Plan 01 oversize, replicates W-3 pause)
- **B. 6 plans** ← selected
- C. 3 mega-plans — REJECTED (same anti-pattern)

**Decision:**
- 6 plans, strictly sequential (per 18.1.1 race lesson)
- Each plan ≤ 7 commits (W-3 budget)
- Wave graph: 01 → 02 → 03 → 04 → 05 → 06

## Deferred ideas

- **D-A2 partman lint-rls implementation**: reclassified out per A2 — separate phase or roadmap.
- Phase 19.1/19.2/19.3/19.4 — real downstream code (unchanged).
- A2 Option C two-tier rate-limit (v3).
- Chromatic (rejected).
- Operator-side: FSL scrub, GHA e2e-cjm first run, axe live bake, visual snapshots bake.

## Claude's discretion items (no user input requested)

- Scope reduction from 37 to ~11 failures: document in CONTEXT D-25 + this log.
- 6 plans / 6 sequential spawns / ~15-20 atomic commits / ~3-5h runtime.
- BYOK guard refactor moves `process.exit` from library to entrypoint (proper layering).
- Shared-pg fixture extends Phase 18.1.1 reaper-helper pattern.

## Research artifacts

All 4 advisors returned findings inline. Key findings embedded in CONTEXT.md D-01 through D-27.

- **A1** (testcontainer): Counts stale (37→11); real issue = port exhaustion; Option D hybrid; 6 commits TDD ladder.
- **A2** (non-testcontainer): SR-2 + SR-6 OUT OF SCOPE; refactor BYOK guard + onSignal export + locale bundle.
- **A3** (CI/CD): Option B native Docker; align test job with e2e jobs' pattern; exact YAML diff.
- **A4** (plan split): Option B 6 plans; strict sequential per 18.1.1 race lesson.
