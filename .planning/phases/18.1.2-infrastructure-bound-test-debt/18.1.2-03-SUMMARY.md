# Phase 18.1.2 Plan 03: Cluster #1 integration-test container reduction — Summary

One-liner: Migrate the two `*usage*.integration.test.ts` files to the shared `getSharedPostgres()` fixture, forward-correcting Plan 02's incomplete image pin (was `postgres:17-alpine`; needed `openwhispr/postgres:17.5-pgpartman` for migration 0014) by adding `provisionPgPartman()` and a per-call extension/grant bootstrap.

## Commits

| # | SHA | Message |
|---|-----|---------|
| 1 | `6d993c1` | `test(18.1.2-03-00a): red — shared-pg pg_partman extension assertion` |
| 2 | `7513fb9` | `fix(18.1.2-03-00b): green — pgpartman image + provisionPgPartman helper` |
| 3 | `d732698` | `fix(18.1.2-03-01): green — usage.integration.test.ts to shared-pg + Option A` |
| 4 | `ba02eac` | `fix(18.1.2-03-02): green — migrate streaming-usage.integration.test.ts (Option A)` |

## RESEARCH §3 correction

Cluster #1's original count of **4 files** included the two `web-search.*.integration.test.ts` siblings. Verifier later SKIPPED those (no usage_ledger touch, no migration 0014 dependency, already share other infra). Actual scope: **2 files** — `usage.integration.test.ts` + `streaming-usage.integration.test.ts`. SUMMARY ground truth: cluster #1 = 2 files migrated.

## Plan 02 forward-correction (shared-pg image)

Plan 02 (commit `15c24c9`) shipped `apps/api/tests/support/shared-pg.ts` pinned to `postgres:17-alpine`. Production migration 0014 (`partman.create_parent` on `audit_log`) requires pg_partman ≥ 5.x. Any integration test running the full migration set against the shared container was wedged.

Forward-correction landed inline in this plan (option `a` per user direction):
- Image pinned to `openwhispr/postgres:17.5-pgpartman` (local build via `compose/postgres/Dockerfile`).
- `provisionPgPartman(superPool, ownerRole)` helper exported, mirroring the canonical grant chain from `packages/data/src/__tests__/helpers.ts::bootMigratedPostgres()`.
- `SHARED_POSTGRES_IMAGE` constant exported for downstream callers.
- All statements idempotent — safe under `.withReuse()`.

## W-2 Option A (CLAUDE.md hard rule compliance)

CLAUDE.md §Conventions #1 forbids editing production code (migration SQL, route handlers, plugin source) to fix tests. Option A respects the hard rule:
- Shared `public` schema across files.
- Drizzle migrate is idempotent on re-invocation via `_meta.__drizzle_migrations` bookkeeping.
- Per-file isolation: `TRUNCATE usage_ledger` in `beforeEach` + file-local unique user emails.
- `ON CONFLICT (tenant_id, (lower(email)))` matches the functional unique index installed by migration 0004 — vitest watch/retry safe.
- Container ownership delegated to the shared-pg module-scope cache; per-file `afterAll` no longer stops it.

## MIGRATIONS_FOLDER path fix

Both files used 5 `..` segments to climb from `apps/api/tests/unit/routes/__tests__` to repo root. Correct count is 6 (`__tests__` (0) → `routes` (1) → `unit` (2) → `tests` (3) → `api` (4) → `apps` (5) → root (6)). Fixed in both files.

## Verification

| File | Result |
|------|--------|
| `apps/api/tests/support/__tests__/shared-pg.test.ts` | 5/5 pass (reuse semantics, role bootstrap idempotency, pg_partman extension assertion, idempotency, skip gate) |
| `apps/api/tests/unit/routes/__tests__/usage.integration.test.ts` | 4/4 pass |
| `apps/api/tests/unit/routes/__tests__/streaming-usage.integration.test.ts` | 5/5 pass |
| Both integration files together (single vitest invocation) | 9/9 pass in 948ms — confirms shared-container interplay |

## Shared container count

`docker ps --filter "label=org.testcontainers.container-hash"` after both suites run: **1 openwhispr/postgres:17.5-pgpartman container** (testcontainers v11 uses `container-hash` label for reuse; the original verification recipe queried the legacy `reuse=true` label which v11 no longer emits — same semantics, new label name).

## Production code untouched

Confirmed: zero production file edits. All changes scoped to `apps/api/tests/support/shared-pg.ts`, `apps/api/tests/support/__tests__/shared-pg.test.ts`, and the two integration test files. Hard rule honored.

## Deviations

None — plan executed as scoped by user direction (option `a`).
