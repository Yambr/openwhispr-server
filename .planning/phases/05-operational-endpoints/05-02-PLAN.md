---
phase: 05-operational-endpoints
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/api/src/routes/streaming-usage.ts
  - apps/api/src/routes/usage.ts
  - apps/api/src/routes/__tests__/streaming-usage.test.ts
  - apps/api/src/routes/__tests__/streaming-usage.integration.test.ts
  - apps/api/src/routes/__tests__/streaming-usage-observability.test.ts
  - apps/api/src/routes/__tests__/usage.integration.test.ts
  - apps/api/src/routes/__tests__/ledger-idempotency.property.test.ts
  - apps/api/src/routes/index.ts
  - packages/contract-tests/src/streaming-usage.test.ts
  - packages/contract-tests/src/usage.test.ts
  - tests/e2e/phase-05-streaming-usage.spec.ts
autonomous: true
requirements: [WIRE-09, WIRE-10]
estimated_tasks: 2
must_haves:
  truths:
    - "POST /api/streaming-usage with valid body + new sessionId returns 200 + {wordsUsed, wordsRemaining: 999999999, plan: 'unlimited', limitReached: false} and inserts a usage_ledger row with request_id = sessionId, kind = 'streaming-stt', units = Math.round(audioDurationSeconds)"
    - "POST /api/streaming-usage retried with the same sessionId returns 200 (NOT 409) and does NOT duplicate the ledger row (idempotent on (request_id) per D-10)"
    - "GET /api/usage returns 200 + {wordsUsed: SUM(units), wordsRemaining: 999999999, plan: 'unlimited', limitReached: false} for authenticated user, RLS-scoped to tenant"
    - "Streaming-usage logs include SHA-256 hash + length + 200-char preview of text when sendLogs=false; 1000-char preview when sendLogs=true (D-13); NEVER full transcript in usage_ledger"
    - "401 on missing/invalid bearer; envelope {error: string} on all non-2xx (WIRE-17/18)"
    - "E2E suite phase-05-streaming-usage.spec.ts boots real compose stack and round-trips POST /api/streaming-usage twice with same sessionId — 2nd call is idempotent"
  artifacts:
    - path: "apps/api/src/routes/streaming-usage.ts"
      provides: "POST /api/streaming-usage handler with idempotent ledger insert + OTel observability"
    - path: "apps/api/src/routes/usage.ts"
      provides: "GET /api/usage handler with SUM(units) aggregator"
    - path: "packages/contract-tests/src/streaming-usage.test.ts"
      provides: "Contract conformance for WIRE-09"
    - path: "packages/contract-tests/src/usage.test.ts"
      provides: "Contract conformance for WIRE-10"
  key_links:
    - from: "apps/api/src/routes/streaming-usage.ts"
      to: "usage_ledger ON CONFLICT (request_id) DO NOTHING"
      via: "Drizzle sql template with idempotent upsert"
      pattern: "ON CONFLICT.*request_id.*DO NOTHING"
    - from: "apps/api/src/routes/usage.ts"
      to: "SUM(units) FROM usage_ledger"
      via: "withTenant Drizzle query"
      pattern: "SUM\\(units\\)"
---

<objective>
Wave 1: Implement WIRE-09 (`POST /api/streaming-usage`) idempotent ledger insert + WIRE-10 (`GET /api/usage`) SUM aggregator. Mirrors Phase 3 transcribe ledger pattern verbatim.

Purpose: The client `cloudApi.ts` calls /api/streaming-usage after every streaming-STT session; idempotency is mandatory because the client retries on network blips with the same sessionId. /api/usage is the user-facing observable for ledger state.
Output: 2 route files, 5 test files (unit + integration + observability + property + e2e), 2 contract test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-01-SUMMARY.md
@apps/api/src/routes/transcribe.ts
@apps/api/src/routes/reason.ts
@apps/api/src/routes/index.ts
@apps/api/src/error-handler.ts
@apps/api/src/middleware
@packages/data/src/schema/usage_ledger.ts
@packages/wire-schemas/src/streaming-usage.ts
@packages/contract-tests/src/schemas.ts
@~/openwhispr/src/services/cloudApi.ts
@~/openwhispr/docs/BACKEND_SPEC.md

<interfaces>
streaming-usage body (BACKEND_SPEC.md:377):
{ text?, audioDurationSeconds, sessionId, clientType?, appVersion?, clientVersion?,
  sttProvider?, sttModel?, sttProcessingMs?, sttLanguage?, audioSizeBytes?,
  audioFormat?, clientTotalMs?, sendLogs? }

streaming-usage response (BACKEND_SPEC.md:407):
{ wordsUsed: number, wordsRemaining: 999_999_999, plan: "unlimited", limitReached: false }

/api/usage response (BACKEND_SPEC.md:416):
{ wordsUsed: number, wordsRemaining: 999_999_999, plan: "unlimited", limitReached: false }

Phase 3 idempotent ledger pattern (apps/api/src/routes/transcribe.ts:130-136):
  INSERT INTO usage_ledger (tenant_id, user_id, request_id, kind, units)
  VALUES (...) ON CONFLICT (request_id) DO NOTHING
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN streaming-usage route (idempotent ledger, OTel observability, contract tests)</name>
  <files>
    apps/api/src/routes/streaming-usage.ts,
    apps/api/src/routes/__tests__/streaming-usage.test.ts,
    apps/api/src/routes/__tests__/streaming-usage.integration.test.ts,
    apps/api/src/routes/__tests__/streaming-usage-observability.test.ts,
    apps/api/src/routes/__tests__/ledger-idempotency.property.test.ts,
    apps/api/src/routes/index.ts,
    packages/contract-tests/src/streaming-usage.test.ts
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/apps/api/src/routes/transcribe.ts (Phase 3 ledger idempotency pattern — lines 130-136 specifically; full file for handler skeleton)
    - /Users/nick/openwhispr-server/apps/api/src/routes/reason.ts (for handler scaffolding)
    - /Users/nick/openwhispr-server/apps/api/src/routes/index.ts (route registration barrel)
    - /Users/nick/openwhispr-server/apps/api/src/error-handler.ts (global envelope)
    - /Users/nick/openwhispr-server/apps/api/src/middleware (bearer + tenant-context middleware to attach)
    - /Users/nick/openwhispr-server/packages/data/src/schema/usage_ledger.ts (request_id UNIQUE constraint)
    - /Users/nick/openwhispr/docs/BACKEND_SPEC.md lines 377-435 (streaming-usage spec — full card)
    - /Users/nick/openwhispr/src/services/cloudApi.ts (client call signature)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-09..D-13
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Code Examples / Idempotent Streaming-Usage Insert (full code block)
  </read_first>
  <behavior>
    - Test: POST /api/streaming-usage rejected 401 when no bearer/cookie auth
    - Test: POST with valid body + new sessionId returns 200 + {wordsUsed: number, wordsRemaining: 999999999, plan: "unlimited", limitReached: false}
    - Test: POST with same sessionId twice — both 200; usage_ledger has exactly one row with that request_id
    - Test: POST with audioDurationSeconds=120 records units = 2 (Math.round(120/60)) — wait, D-10 says units = Math.round(audioDurationSeconds), NOT divided. Verify D-10 in CONTEXT: "units = Math.round(audioDurationSeconds)". So units = Math.round(120) = 120. Document this exactly.
    - Test: POST with text="hello world" sendLogs=false — log record contains text_sha256 = sha256("hello world"), text_length = 11, text_preview = "hello world" (length ≤ 200)
    - Test: POST with 1500-char text sendLogs=false — text_preview is first 200 chars
    - Test: POST with 1500-char text sendLogs=true — text_preview is first 1000 chars
    - Test: POST with malformed body (missing sessionId) returns 400 envelope {error: string}
    - Property test (fast-check 100 pairs): random sessionId × random tenant — same sessionId from different tenants does NOT collide (different tenant_id scope; request_id UNIQUE per ledger scope per Phase 3 schema)
    - Integration test: real PG+PgBouncer testcontainer, INSERT then re-INSERT same sessionId — second insert returns existing row, ledger has 1 row
  </behavior>
  <action>
    Implement `apps/api/src/routes/streaming-usage.ts` following the verbatim Phase 3 transcribe.ts skeleton and the RESEARCH § Code Examples "Idempotent Streaming-Usage Insert" code block.

    Handler outline:
    1. Validate body with `StreamingUsageBodySchema` from `@openwhispr/wire-schemas`
    2. Apply dual-auth (Bearer or cookie) — reuse Phase 2 middleware
    3. Inside `withTenant(deps.db, tenantId, async (tx) => ...)`:
       a. `INSERT INTO usage_ledger (tenant_id, user_id, request_id, kind, units) VALUES (${tenantId}, ${userId}, ${body.sessionId}, 'streaming-stt', ${Math.round(body.audioDurationSeconds)}) ON CONFLICT (request_id) DO NOTHING`
       b. Attach OTel span attrs + structured log fields per D-11 (sttProvider, sttModel, sttLanguage, audioSizeBytes, audioFormat, sttProcessingMs, clientTotalMs, clientType, appVersion, clientVersion)
       c. Log text_sha256 = sha256(body.text ?? ''), text_length, text_preview = body.text.slice(0, body.sendLogs ? 1000 : 200) (D-13)
       d. SELECT SUM(units) FROM usage_ledger WHERE user_id = ${userId}
       e. Return `{ wordsUsed: <sum>, wordsRemaining: 999_999_999, plan: 'unlimited' as const, limitReached: false as const }` (per D-12)

    Register route in `apps/api/src/routes/index.ts` with `app.post('/api/streaming-usage', { preHandler: dualAuth }, handler)`. NO conditional registration (Pitfall #6 — negative matrix requirement).

    RED tests must precede GREEN implementation in the same atomic commit per CLAUDE.md TDD.

    Contract test `packages/contract-tests/src/streaming-usage.test.ts` uses the Phase 2-3 contract harness pattern: hits BACKEND_URL with signInFixture, posts the 14-field body shape, asserts response zod-schema match + idempotency on retry. Reference `packages/contract-tests/src/transcribe.test.ts` for the exact harness boilerplate.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/__tests__/streaming-usage && pnpm --filter @openwhispr/contract-tests test -- --run src/streaming-usage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/api/src/routes/streaming-usage.ts`
    - grep returns ≥1: `grep -E "ON CONFLICT.*request_id.*DO NOTHING" apps/api/src/routes/streaming-usage.ts`
    - grep returns ≥1: `grep -E "kind.*streaming-stt" apps/api/src/routes/streaming-usage.ts`
    - grep returns ≥1: `grep -E "wordsRemaining.*999_?999_?999|wordsRemaining: 999999999" apps/api/src/routes/streaming-usage.ts`
    - grep returns ≥1: `grep -E "text_sha256|sha256\\(.*text" apps/api/src/routes/streaming-usage.ts`
    - grep returns ≥1: `grep -E "/api/streaming-usage" apps/api/src/routes/index.ts`
    - Test command exits 0 (4 test files: unit, integration, observability, property + contract)
  </acceptance_criteria>
  <done>Streaming-usage handler land idempotent, observable, RLS-bounded, contract-conformant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED + GREEN /api/usage SUM aggregator + E2E test for both routes</name>
  <files>
    apps/api/src/routes/usage.ts,
    apps/api/src/routes/__tests__/usage.integration.test.ts,
    packages/contract-tests/src/usage.test.ts,
    tests/e2e/phase-05-streaming-usage.spec.ts,
    apps/api/src/routes/index.ts
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/apps/api/src/routes/reason.ts (auth-only GET handler pattern)
    - /Users/nick/openwhispr-server/packages/data/src/schema/usage_ledger.ts
    - /Users/nick/openwhispr-server/tests/e2e (existing e2e specs for Phase 3/4 — pattern)
    - /Users/nick/openwhispr-server/Makefile (e2e-test target)
    - /Users/nick/openwhispr/docs/BACKEND_SPEC.md lines 416-435 (/api/usage card)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-14, D-15, D-16
  </read_first>
  <behavior>
    - Test: GET /api/usage rejected 401 when no auth
    - Test: GET /api/usage returns 200 + {wordsUsed: 0, wordsRemaining: 999999999, plan: "unlimited", limitReached: false} for new user
    - Test: After inserting 3 ledger rows with units = [10, 20, 30] for user, GET /api/usage returns wordsUsed = 60
    - Test: cross-tenant — user A's wordsUsed does NOT include user B's units (RLS-enforced)
    - Test: All kinds count — insert kind = 'transcribe' (10) + 'reason' (5) + 'streaming-stt' (15) + 'web-search.tavily' (1) → wordsUsed = 31 (D-14)
    - E2E test: live compose stack — sign in via fixtures, POST /api/streaming-usage twice with same sessionId, then GET /api/usage; assert wordsUsed reflects exactly one ledger entry
  </behavior>
  <action>
    `apps/api/src/routes/usage.ts`: handler

    ```ts
    app.get('/api/usage', { preHandler: dualAuth }, async (req) => {
      const tenantId = req.tenant;
      const userId = req.user.id;
      return withTenant(deps.db, tenantId, async (tx) => {
        const result = await tx.execute(sql`
          SELECT COALESCE(SUM(units), 0)::bigint AS words_used
          FROM usage_ledger
          WHERE user_id = ${userId}::uuid
        `);
        const wordsUsed = Number(result.rows[0].words_used);
        return {
          wordsUsed,
          wordsRemaining: 999_999_999,
          plan: 'unlimited' as const,
          limitReached: false as const,
        };
      });
    });
    ```

    Register in `apps/api/src/routes/index.ts`.

    `tests/e2e/phase-05-streaming-usage.spec.ts`: e2e spec following Phase 3-4 pattern (E2E=1 env gate). Spins compose, signs in via signInFixture, hits POST /api/streaming-usage with sessionId="e2e-test-{uuid}" twice, then GET /api/usage. Asserts: both POSTs return 200, ledger row count = 1, GET /api/usage wordsUsed reflects Math.round(audioDurationSeconds) once.

    Single atomic commit `test+feat(05-02): streaming-usage + /api/usage WIRE-09 + WIRE-10`.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/__tests__/usage.integration.test.ts && pnpm --filter @openwhispr/contract-tests test -- --run src/usage.test.ts && E2E=1 make e2e-test SPEC=tests/e2e/phase-05-streaming-usage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/api/src/routes/usage.ts`
    - grep returns ≥1: `grep -E "SUM\\(units\\)" apps/api/src/routes/usage.ts`
    - grep returns ≥1: `grep -E "/api/usage" apps/api/src/routes/index.ts`
    - File exists: `tests/e2e/phase-05-streaming-usage.spec.ts`
    - Test commands exit 0; e2e exits 0
  </acceptance_criteria>
  <done>/api/usage handler + e2e proven on real compose stack.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-05-07 | Tampering | sessionId idempotency replay | high | mitigate | request_id UNIQUE constraint inherited from usage_ledger schema; INSERT … ON CONFLICT DO NOTHING; RLS prevents cross-tenant sessionId collision attack (D-10). |
| T-05-08 | Information Disclosure | text PII in usage_ledger | high | mitigate | D-13: text NEVER persisted in usage_ledger. SHA-256 hash + length + 200-char preview only to OTel logs (1000 chars only when sendLogs=true). Verified by observability test. |
| T-RATE-01 | DoS | unauthenticated /api/usage flood | medium | mitigate | dualAuth preHandler rejects 401 before handler runs; `@fastify/rate-limit` per-route (inherited Phase 4 D-19). |
</threat_model>

<verification>
- All `<automated>` commands exit 0
- E2E phase-05-streaming-usage.spec passes
- ledger-idempotency.property.test.ts confirms no cross-tenant collisions
</verification>

<success_criteria>
WIRE-09 + WIRE-10 client-observable behaviors per must_haves all green. Idempotency proven by integration + property tests. E2E proves real-compose round-trip.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-02-SUMMARY.md` recording: routes added, ledger pattern reused, contract files added, e2e spec gates idempotency, decisions implemented D-09..D-16.
</output>
