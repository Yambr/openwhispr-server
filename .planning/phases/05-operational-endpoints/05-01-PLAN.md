---
phase: 05-operational-endpoints
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/wire-schemas/package.json
  - packages/wire-schemas/tsconfig.json
  - packages/wire-schemas/src/index.ts
  - packages/wire-schemas/src/notes.ts
  - packages/wire-schemas/src/folders.ts
  - packages/wire-schemas/src/conversations.ts
  - packages/wire-schemas/src/transcriptions.ts
  - packages/wire-schemas/src/api-keys.ts
  - packages/wire-schemas/src/streaming-usage.ts
  - packages/wire-schemas/src/web-search.ts
  - packages/wire-schemas/src/settings.ts
  - packages/wire-schemas/src/__tests__/schemas.test.ts
  - packages/data/src/schema/tenant_settings.ts
  - packages/data/src/schema/user_settings.ts
  - packages/data/src/schema/notes.ts
  - packages/data/src/schema/folders.ts
  - packages/data/src/schema/conversations.ts
  - packages/data/src/schema/messages.ts
  - packages/data/src/schema/transcriptions.ts
  - packages/data/src/schema/api_keys.ts
  - packages/data/src/schema/_helpers.ts
  - packages/data/src/schema/index.ts
  - packages/data/migrations/0006_tenant_settings.sql
  - packages/data/migrations/0007_notes_folders.sql
  - packages/data/migrations/0008_conversations_messages.sql
  - packages/data/migrations/0009_transcriptions.sql
  - packages/data/migrations/0010_api_keys.sql
  - packages/data/migrations/meta/_journal.json
  - packages/data/src/__tests__/rls-property.test.ts
  - packages/data/src/__tests__/settings-rls.test.ts
  - packages/data/src/__tests__/migration-0006-backfill.test.ts
  - packages/data/src/seed/conformance.ts
  - pnpm-workspace.yaml
autonomous: true
requirements: [WIRE-22, WIRE-23, WIRE-24, WIRE-25, WIRE-26, WIRE-27, WIRE-28]
estimated_tasks: 3
must_haves:
  truths:
    - "packages/wire-schemas exports Zod schemas mirroring NoteInput / CloudNote / FolderInput / CloudFolder / ConversationInput / CloudConversation / CloudMessage / TranscriptionInput / CloudTranscription / ApiKey / CreateApiKeyResponse / StreamingUsageBody / WebSearchRequest / WebSearchResponse byte-for-byte from ~/openwhispr/src/services/*.ts"
    - "Migrations 0006..0010 apply forward and rollback successfully against the Phase 1 testcontainer Postgres+PgBouncer harness"
    - "Every new table (tenant_settings, user_settings, notes, folders, conversations, messages, transcriptions, api_keys) has ENABLE ROW LEVEL SECURITY + FORCE ROW LEVEL SECURITY + policy referencing current_setting('app.tenant_id')"
    - "AFTER INSERT trigger seed_tenant_settings inserts a tenant_settings row when a new row is committed to tenants (Pitfall #8 — AFTER not BEFORE)"
    - "Migration 0006 backfills tenant_settings for every existing tenant in the database"
    - "tools/lint-rls.ts exits 0 after migrations apply (all 8 new tables pass FORCE RLS introspection)"
    - "Extended rls-property.test.ts covers cross-tenant isolation for all 8 new tables with fast-check ≥100 pairs each"
    - "packages/data/src/seed/conformance.ts seeds deterministic rows for every new resource (notes, folders, conversations, messages, transcriptions, api_keys, tenant_settings, user_settings)"
    - "`pnpm --filter @openwhispr/data migrate` applies cleanly to a fresh PG 17 + PgBouncer testcontainer (executed by [BLOCKING] task before Wave 1)"
  artifacts:
    - path: "packages/wire-schemas/src/index.ts"
      provides: "Zod schema barrel for all Phase 5 wire shapes"
    - path: "packages/data/migrations/0006_tenant_settings.sql"
      provides: "tenant_settings + user_settings tables with RLS + AFTER INSERT trigger + backfill"
    - path: "packages/data/migrations/0007_notes_folders.sql"
      provides: "notes (with tsvector GENERATED column + GIN) + folders tables with RLS + soft-delete + client_id partial UNIQUE"
    - path: "packages/data/migrations/0008_conversations_messages.sql"
      provides: "conversations (with tsvector GIN) + messages tables with RLS + soft-delete + client_id partial UNIQUE + FK conversation_id"
    - path: "packages/data/migrations/0009_transcriptions.sql"
      provides: "transcriptions table with RLS + soft-delete + client_transcription_id partial UNIQUE"
    - path: "packages/data/migrations/0010_api_keys.sql"
      provides: "api_keys table with RLS + key_prefix UNIQUE + Argon2id key_hash + revoked_at"
  key_links:
    - from: "packages/data/migrations/0006..0010"
      to: "tools/lint-rls.ts"
      via: "FORCE RLS introspection"
      pattern: "rls.*FORCE|FORCE.*ROW LEVEL SECURITY"
    - from: "packages/wire-schemas/src/*"
      to: "~/openwhispr/src/services/*.ts"
      via: "byte-for-byte interface mirror"
      pattern: "export const \\w+Schema = z\\.object"
---

<objective>
Wave 0: Test scaffolding + Zod wire schemas + migrations 0006..0010 + RLS coverage extension. This plan establishes the failing-RED test floor and the database/schema foundation that every Wave 1+ plan depends on. After this plan lands, the migrate command is executed via the `[BLOCKING]` task before any Wave 1 plan starts.

Purpose: TDD discipline (CLAUDE.md TDD-01b) requires tests-and-schema-first. 8 new tables, 5 migrations, 7 Zod schema files all land together — any one missing breaks all downstream plans.
Output: `packages/wire-schemas/` package, 5 migrations, 8 schema files, extended RLS property test, schema-push verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-VALIDATION.md
@CLAUDE.md
@packages/data/src/schema/usage_ledger.ts
@packages/data/src/schema/tenants.ts
@packages/data/src/schema/users.ts
@packages/data/src/schema/index.ts
@packages/data/migrations/0000_initial.sql
@packages/data/src/__tests__/rls-property.test.ts
@packages/data/src/seed/conformance.ts
@tools/lint-rls.ts
@~/openwhispr/src/services/NotesService.ts
@~/openwhispr/src/services/FoldersService.ts
@~/openwhispr/src/services/ConversationsService.ts
@~/openwhispr/src/services/TranscriptionsService.ts
@~/openwhispr/src/services/ApiKeysService.ts

<interfaces>
<!-- These are the byte-for-byte canonical interfaces. Zod schemas in packages/wire-schemas/ MUST mirror these field-for-field. Read each service file in full before writing schemas. -->

From ~/openwhispr/src/services/NotesService.ts — extract: NoteInput, CloudNote, SearchResult
From ~/openwhispr/src/services/FoldersService.ts — extract: FolderInput, CloudFolder
From ~/openwhispr/src/services/ConversationsService.ts — extract: ConversationInput, CloudConversation, CloudMessage, CloudConversationWithMessages
From ~/openwhispr/src/services/TranscriptionsService.ts — extract: TranscriptionInput, CloudTranscription
From ~/openwhispr/src/services/ApiKeysService.ts — extract: ApiKey, CreateApiKeyResponse, V1Response<T>

streaming-usage body shape (14 fields per BACKEND_SPEC.md:377):
  text, audioDurationSeconds, sessionId, clientType, appVersion, clientVersion,
  sttProvider, sttModel, sttProcessingMs, sttLanguage, audioSizeBytes, audioFormat,
  clientTotalMs, sendLogs

web-search request: { query: string (≥1 char, ≤256 chars), numResults?: number (default 5, max 10) }
web-search response: { results: Array<{ title: string, url: string, snippet: string }> }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create packages/wire-schemas package + Zod schemas for all Phase 5 resources (RED tests first)</name>
  <files>
    packages/wire-schemas/package.json,
    packages/wire-schemas/tsconfig.json,
    packages/wire-schemas/src/index.ts,
    packages/wire-schemas/src/notes.ts,
    packages/wire-schemas/src/folders.ts,
    packages/wire-schemas/src/conversations.ts,
    packages/wire-schemas/src/transcriptions.ts,
    packages/wire-schemas/src/api-keys.ts,
    packages/wire-schemas/src/streaming-usage.ts,
    packages/wire-schemas/src/web-search.ts,
    packages/wire-schemas/src/settings.ts,
    packages/wire-schemas/src/__tests__/schemas.test.ts,
    pnpm-workspace.yaml
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/pnpm-workspace.yaml (current workspace members)
    - /Users/nick/openwhispr-server/packages/data/package.json (workspace package conventions)
    - /Users/nick/openwhispr/src/services/NotesService.ts (NoteInput, CloudNote, SearchResult — full file)
    - /Users/nick/openwhispr/src/services/FoldersService.ts (FolderInput, CloudFolder — full file)
    - /Users/nick/openwhispr/src/services/ConversationsService.ts (ConversationInput, CloudConversation, CloudMessage, CloudConversationWithMessages — full file)
    - /Users/nick/openwhispr/src/services/TranscriptionsService.ts (TranscriptionInput, CloudTranscription — full file)
    - /Users/nick/openwhispr/src/services/ApiKeysService.ts (ApiKey, CreateApiKeyResponse, V1Response — full file)
    - /Users/nick/openwhispr/docs/BACKEND_SPEC.md lines 345-478 (web-search, streaming-usage, usage, stt-config, note-recording-config shapes)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-22 (canonical wire shape rule)
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Architecture Patterns (file layout)
  </read_first>
  <behavior>
    - Test: each Zod schema accepts a valid example matching the TS interface
    - Test: each Zod schema rejects missing required fields
    - Test: NoteInput/FolderInput/ConversationInput accept optional client_*_id
    - Test: CloudNote/CloudFolder/CloudConversation/CloudMessage/CloudTranscription contain id, tenant_id, user_id, created_at, updated_at, deleted_at (nullable)
    - Test: ApiKey schema rejects clear-text `key` in list-response shape (only key_prefix allowed)
    - Test: CreateApiKeyResponse schema requires `key` (clear-text) AND key_prefix
    - Test: StreamingUsageBody schema accepts all 14 fields, requires only sessionId + audioDurationSeconds
    - Test: WebSearchRequest schema requires query (1-256 chars), numResults optional capped at 10
    - Test: V1Response<T> envelope: { data: T } wrapper per D-28
  </behavior>
  <action>
    Create the new workspace package `@openwhispr/wire-schemas`:

    1. `pnpm-workspace.yaml` — add `"packages/wire-schemas"` to the packages list (preserve existing entries).

    2. `packages/wire-schemas/package.json` — mirror `packages/data/package.json` structure exactly. Name: `@openwhispr/wire-schemas`. Dependencies: `zod@^4.4.3`. devDependencies: `vitest@^4`, `typescript@^5`, `@types/node`. scripts: `{"test": "vitest run", "test:watch": "vitest", "typecheck": "tsc --noEmit"}`. type: module. main: `dist/index.js`. exports map: `"./*": "./src/*.ts"`.

    3. `packages/wire-schemas/tsconfig.json` — copy `packages/data/tsconfig.json` and adjust paths.

    4. RED tests first: `packages/wire-schemas/src/__tests__/schemas.test.ts` — one describe block per resource. Each test feeds a valid example (constructed from the TS interface verbatim) through `.parse()` AND feeds an invalid example expecting `.safeParse().success === false`. Use these exact valid examples derived from the service files:
       - NoteInput: `{ title: "x", content: "y", client_note_id: "cid-1" }` (all three optional except as the interface specifies)
       - CloudNote: `{ id: "uuid", tenant_id: "uuid", user_id: "uuid", title: "x", content: "y", folder_id: null, client_note_id: "cid-1", created_at: "2026-01-01T00:00:00Z", updated_at: "2026-01-01T00:00:00Z", deleted_at: null }`
       - SearchResult: CloudNote shape PLUS `score: 0.5`
       - FolderInput / CloudFolder / ConversationInput / CloudConversation / CloudMessage: same approach; copy fields from `~/openwhispr/src/services/*.ts` interfaces verbatim
       - ApiKey: `{ id: "uuid", name: "my-key", key_prefix: "pak_abcdef", scopes: ["read"], created_at: "...", last_used_at: null, expires_at: null, revoked_at: null }` (NO `key` field — listing only shows prefix)
       - CreateApiKeyResponse (wrapped in V1Response): `{ data: { ...ApiKey, key: "pak_<clearText>" } }`
       - StreamingUsageBody: all 14 fields per BACKEND_SPEC.md:377-412 — required: sessionId (string), audioDurationSeconds (number ≥0); optional: text, clientType, appVersion, clientVersion, sttProvider, sttModel, sttProcessingMs, sttLanguage, audioSizeBytes, audioFormat, clientTotalMs, sendLogs (boolean default false)
       - WebSearchRequest: `{ query: "test", numResults: 5 }` — query: z.string().min(1).max(256); numResults: z.number().int().min(1).max(10).optional().default(5)
       - WebSearchResponse: `{ results: [{ title: "T", url: "https://x", snippet: "S" }] }`
       - SttConfigResponse: `{ defaultModel: "whisper-1", defaultLanguage: "auto", availableProviders: ["openai"] }`
       - NoteRecordingConfigResponse: `{ maxDurationSeconds: 7200, sampleRateHz: 16000, allowedFormats: ["webm"], diarizationEnabled: true }`

    5. GREEN implementation: create `packages/wire-schemas/src/{notes,folders,conversations,transcriptions,api-keys,streaming-usage,web-search,settings}.ts`. Each file exports the Zod schemas listed above. Re-export everything from `packages/wire-schemas/src/index.ts`.

    Reference D-22: schemas MUST mirror TS interfaces byte-for-byte. If an interface field is `string | null`, schema is `z.string().nullable()`. If optional `field?: T`, schema is `z.T().optional()`. Use ISO-8601 string for timestamptz fields (`z.string().datetime()`).

    Commit message: `test(05-01): RED Zod wire schemas for notes/folders/conversations/transcriptions/api-keys/streaming-usage/web-search/settings` then `feat(05-01): GREEN packages/wire-schemas exports per D-22` — single atomic commit (test+impl per CLAUDE.md TDD).
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm install && pnpm --filter @openwhispr/wire-schemas test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `packages/wire-schemas/package.json` with `"name": "@openwhispr/wire-schemas"`
    - File exists: `packages/wire-schemas/src/index.ts` re-exporting at least 8 schema modules
    - grep returns ≥1: `grep -r "export const NoteInputSchema" packages/wire-schemas/src/`
    - grep returns ≥1: `grep -r "export const StreamingUsageBodySchema" packages/wire-schemas/src/`
    - grep returns ≥1: `grep -r "V1Response" packages/wire-schemas/src/api-keys.ts`
    - `pnpm --filter @openwhispr/wire-schemas test -- --run` exits 0
    - `pnpm --filter @openwhispr/wire-schemas typecheck` exits 0
    - `pnpm-workspace.yaml` contains `packages/wire-schemas`
  </acceptance_criteria>
  <done>All Zod schemas exist, mirror TS interfaces byte-for-byte, tests pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drizzle schemas + migrations 0006..0010 + extend rls-property + seed extension (RED RLS tests first)</name>
  <files>
    packages/data/src/schema/_helpers.ts,
    packages/data/src/schema/tenant_settings.ts,
    packages/data/src/schema/user_settings.ts,
    packages/data/src/schema/notes.ts,
    packages/data/src/schema/folders.ts,
    packages/data/src/schema/conversations.ts,
    packages/data/src/schema/messages.ts,
    packages/data/src/schema/transcriptions.ts,
    packages/data/src/schema/api_keys.ts,
    packages/data/src/schema/index.ts,
    packages/data/migrations/0006_tenant_settings.sql,
    packages/data/migrations/0007_notes_folders.sql,
    packages/data/migrations/0008_conversations_messages.sql,
    packages/data/migrations/0009_transcriptions.sql,
    packages/data/migrations/0010_api_keys.sql,
    packages/data/migrations/meta/_journal.json,
    packages/data/src/__tests__/rls-property.test.ts,
    packages/data/src/__tests__/settings-rls.test.ts,
    packages/data/src/__tests__/migration-0006-backfill.test.ts,
    packages/data/src/seed/conformance.ts
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/packages/data/migrations/0000_initial.sql (RLS + FORCE RLS pattern — full file)
    - /Users/nick/openwhispr-server/packages/data/migrations/0005_session_token_plain.sql (latest migration — for journal increment pattern)
    - /Users/nick/openwhispr-server/packages/data/migrations/meta/_journal.json (current journal state)
    - /Users/nick/openwhispr-server/packages/data/src/schema/usage_ledger.ts (Drizzle pgTable + index conventions)
    - /Users/nick/openwhispr-server/packages/data/src/schema/tenants.ts + users.ts (FK reference target)
    - /Users/nick/openwhispr-server/packages/data/src/schema/index.ts (barrel export pattern)
    - /Users/nick/openwhispr-server/packages/data/src/__tests__/rls-property.test.ts (fast-check 100-pair pattern, testcontainer setup — full file)
    - /Users/nick/openwhispr-server/packages/data/src/seed/conformance.ts (deterministic ID seeding pattern)
    - /Users/nick/openwhispr-server/tools/lint-rls.ts (FORCE RLS introspection requirements)
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Architecture Patterns + § Code Examples + § Common Pitfalls #1, #7, #8 (PgBouncer + tsvector + AFTER INSERT trigger)
  </read_first>
  <behavior>
    - Test: tenant_settings table exists with PK tenant_id FK tenants(id), JSONB stt_config + note_recording_config DEFAULT '{}'
    - Test: user_settings table exists with PK user_id FK users(id), tenant_id FK tenants(id), JSONB stt_overrides + note_recording_overrides
    - Test: notes table has columns id (uuid PK), tenant_id, user_id, folder_id (uuid nullable FK folders), client_note_id (text nullable), title (text), content (text NOT NULL DEFAULT ''), content_search (tsvector GENERATED), created_at, updated_at, deleted_at (timestamptz NULL)
    - Test: notes has partial UNIQUE (tenant_id, user_id, client_note_id) WHERE client_note_id IS NOT NULL
    - Test: notes has GIN index on content_search
    - Test: notes has partial keyset index (tenant_id, created_at DESC, id DESC) WHERE deleted_at IS NULL
    - Test: folders table has columns id, tenant_id, user_id, name, parent_folder_id (self-ref nullable), client_folder_id, created_at, updated_at, deleted_at
    - Test: conversations table has columns id, tenant_id, user_id, title, content_search (tsvector GENERATED over title), client_conversation_id, created_at, updated_at, deleted_at
    - Test: messages table has columns id, conversation_id FK conversations(id), tenant_id, user_id, role (text — 'user'|'assistant'|'system'|'tool'), content, metadata (jsonb), created_at, updated_at, deleted_at
    - Test: transcriptions table has columns id, tenant_id, user_id, text, language, duration_seconds, model, provider, client_transcription_id, created_at, updated_at, deleted_at
    - Test: api_keys table has columns id, tenant_id, user_id, name, key_prefix (text UNIQUE), key_hash (text NOT NULL), scopes (text[]), last_used_at, expires_at, created_at, revoked_at
    - Test: ALL 8 new tables have ENABLE ROW LEVEL SECURITY + FORCE ROW LEVEL SECURITY
    - Test: ALL 8 new tables have an isolation policy referencing current_setting('app.tenant_id')
    - Test: AFTER INSERT trigger on tenants seeds tenant_settings (Pitfall #8 — verify with TRIGGER_TIMING = AFTER, not BEFORE)
    - Test: backfill INSERT runs against existing tenants (default tenant from 0000 must have a tenant_settings row after 0006 applies)
    - Test (property, fast-check 100 pairs per table): cross-tenant read/write of every new table FAILS under RLS
    - Test: seed.conformance.ts inserts a row per new resource with deterministic UUIDs (declared in code as exported constants)
  </behavior>
  <action>
    Implement Drizzle schemas + SQL migrations + RLS tests in one atomic test-and-impl commit per CLAUDE.md TDD.

    **Phase 1 (RED — write failing integration tests first):**

    - `packages/data/src/__tests__/settings-rls.test.ts` — testcontainer test (pattern from rls-property.test.ts):
      - Spin up Postgres 17 + PgBouncer 1.23 testcontainer
      - Apply migrations 0000..0010 via `pnpm --filter @openwhispr/data migrate` (or equivalent runner)
      - Assert pg_class.relrowsecurity = true AND pg_class.relforcerowsecurity = true for: tenant_settings, user_settings, notes, folders, conversations, messages, transcriptions, api_keys
      - Assert pg_policies exists for each new table with `current_setting('app.tenant_id'` substring
      - Assert AFTER INSERT trigger exists: `SELECT action_timing FROM information_schema.triggers WHERE event_object_table = 'tenants' AND trigger_name = 'tenants_seed_settings'` returns `'AFTER'`
      - Assert: insert a new tenant via owner db → automatically gets tenant_settings row (trigger fired)

    - `packages/data/src/__tests__/migration-0006-backfill.test.ts` — separate test verifies backfill: pre-existing tenant from 0000_initial.sql has a tenant_settings row after 0006 applies

    - Extend `packages/data/src/__tests__/rls-property.test.ts`: add fast-check property test for each new table (8 new property tests, 100 pairs each). Mirror the existing pattern for usage_ledger (which is the closest analog: tenant-scoped table with PK). For each new table, insert as tenantA via withTenant(A), then attempt SELECT as tenantB via withTenant(B) — must return zero rows. Then attempt UPDATE+DELETE — must affect zero rows. Use the existing fast-check arbitrary for uuid pairs.

    **Phase 2 (GREEN — Drizzle schemas):**

    - `packages/data/src/schema/_helpers.ts`: define `tsvector` customType helper per RESEARCH Pattern 3:
      ```ts
      import { customType } from "drizzle-orm/pg-core";
      export const tsvector = customType<{ data: string; notNull: true }>({
        dataType() { return "tsvector"; },
      });
      ```

    - Create one Drizzle schema file per table. For brevity here, exact column definitions per RESEARCH Code Examples and BEHAVIORS above. Use `uuid("id").primaryKey().defaultRandom()`, `timestamptz("created_at").notNull().defaultNow()`, `.references(() => tenants.id)` for FKs, partial UNIQUE indexes via Drizzle index API where possible (drop to raw SQL in migration for `WHERE` clauses).

    - Update `packages/data/src/schema/index.ts` to export all 8 new tables.

    **Phase 3 (GREEN — migration SQL files):**

    All 5 migrations follow this template (RLS + FORCE RLS + policy mandatory on every table):

    - `0006_tenant_settings.sql` — exactly the SQL block from RESEARCH § Code Examples "Settings Tables Migration" verbatim (it includes both tables, RLS, AFTER INSERT trigger, backfill). Hand-augment after drizzle-kit generate per Phase 1 convention.

    - `0007_notes_folders.sql`:
      ```sql
      CREATE TABLE folders (
        id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
        tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
        user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        name text NOT NULL,
        parent_folder_id uuid REFERENCES folders(id) ON DELETE SET NULL,
        client_folder_id text,
        created_at timestamptz NOT NULL DEFAULT now(),
        updated_at timestamptz NOT NULL DEFAULT now(),
        deleted_at timestamptz
      );
      ALTER TABLE folders ENABLE ROW LEVEL SECURITY;
      ALTER TABLE folders FORCE ROW LEVEL SECURITY;
      CREATE POLICY folders_isolation ON folders USING (tenant_id = current_setting('app.tenant_id', true)::uuid);
      CREATE UNIQUE INDEX folders_client_id_idx ON folders (tenant_id, user_id, client_folder_id) WHERE client_folder_id IS NOT NULL;
      CREATE INDEX folders_keyset_idx ON folders (tenant_id, created_at DESC, id DESC) WHERE deleted_at IS NULL;

      CREATE TABLE notes (
        id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
        tenant_id uuid NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
        user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        folder_id uuid REFERENCES folders(id) ON DELETE SET NULL,
        client_note_id text,
        title text,
        content text NOT NULL DEFAULT '',
        content_search tsvector GENERATED ALWAYS AS (
          setweight(to_tsvector('simple', coalesce(title, '')), 'A') ||
          setweight(to_tsvector('simple', coalesce(content, '')), 'B')
        ) STORED,
        created_at timestamptz NOT NULL DEFAULT now(),
        updated_at timestamptz NOT NULL DEFAULT now(),
        deleted_at timestamptz
      );
      ALTER TABLE notes ENABLE ROW LEVEL SECURITY;
      ALTER TABLE notes FORCE ROW LEVEL SECURITY;
      CREATE POLICY notes_isolation ON notes USING (tenant_id = current_setting('app.tenant_id', true)::uuid);
      CREATE UNIQUE INDEX notes_client_id_idx ON notes (tenant_id, user_id, client_note_id) WHERE client_note_id IS NOT NULL;
      CREATE INDEX notes_keyset_idx ON notes (tenant_id, created_at DESC, id DESC) WHERE deleted_at IS NULL;
      CREATE INDEX notes_content_search_idx ON notes USING GIN (content_search);
      ```

    - `0008_conversations_messages.sql`: conversations (with tsvector GENERATED over coalesce(title,'')) + GIN index; messages (id, conversation_id FK conversations ON DELETE CASCADE, tenant_id, user_id, role text NOT NULL CHECK (role IN ('user','assistant','system','tool')), content text NOT NULL DEFAULT '', metadata jsonb NOT NULL DEFAULT '{}', client_message_id text, created_at, updated_at, deleted_at). Both with RLS + FORCE + isolation policy + client_id partial UNIQUE + keyset partial idx. Messages: additional index on (conversation_id, created_at DESC, id DESC) WHERE deleted_at IS NULL.

    - `0009_transcriptions.sql`: id, tenant_id, user_id, text (text), language (text nullable), duration_seconds (real nullable), model (text nullable), provider (text nullable), client_transcription_id text, created_at, updated_at, deleted_at. RLS + FORCE + policy + client_id partial UNIQUE + keyset idx.

    - `0010_api_keys.sql`: id, tenant_id, user_id, name (text NOT NULL), key_prefix (text NOT NULL UNIQUE), key_hash (text NOT NULL), scopes (text[] NOT NULL DEFAULT '{}'), last_used_at timestamptz, expires_at timestamptz, created_at timestamptz NOT NULL DEFAULT now(), revoked_at timestamptz. RLS + FORCE + isolation policy on tenant_id. UNIQUE INDEX on (tenant_id, name) WHERE revoked_at IS NULL (prevent active-name dupes per tenant).

    Update `meta/_journal.json` — add entries 0006..0010 with monotonic `when` timestamps.

    **Phase 4 — seed extension:**

    Extend `packages/data/src/seed/conformance.ts` to insert one deterministic row per new resource using stable UUIDs (declared as `export const SEED_NOTE_ID = "..."` constants). CONTRACT-01 tests reference these IDs.

    Commit: single atomic `test+feat(05-01): wave-0 schemas + migrations 0006..0010 + RLS coverage`.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/data test -- --run packages/data/src/__tests__/settings-rls.test.ts packages/data/src/__tests__/migration-0006-backfill.test.ts packages/data/src/__tests__/rls-property.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `packages/data/migrations/0006_tenant_settings.sql` through `0010_api_keys.sql`
    - grep returns 8: `grep -l "FORCE ROW LEVEL SECURITY" packages/data/migrations/0006_*.sql packages/data/migrations/0007_*.sql packages/data/migrations/0008_*.sql packages/data/migrations/0009_*.sql packages/data/migrations/0010_*.sql | wc -l` returns 5 (5 files, all containing FORCE; each file may have multiple FORCE statements for tables within)
    - grep returns ≥8: `grep -c "FORCE ROW LEVEL SECURITY" packages/data/migrations/0006_*.sql packages/data/migrations/0007_*.sql packages/data/migrations/0008_*.sql packages/data/migrations/0009_*.sql packages/data/migrations/0010_*.sql | awk -F: '{sum+=$2} END {print sum}'` ≥ 8
    - grep returns ≥1: `grep -E "AFTER INSERT ON tenants" packages/data/migrations/0006_*.sql`
    - grep returns ≥1: `grep -E "websearch_to_tsquery|tsvector GENERATED|tsvector\\s+GENERATED" packages/data/migrations/0007_*.sql`
    - grep returns ≥1: `grep -E "USING GIN" packages/data/migrations/0007_*.sql`
    - grep returns ≥1: `grep -E "USING GIN" packages/data/migrations/0008_*.sql`
    - grep returns ≥1: `grep -E "client_note_id.*WHERE client_note_id IS NOT NULL|UNIQUE INDEX notes_client_id_idx" packages/data/migrations/0007_*.sql`
    - Schema files exist: `ls packages/data/src/schema/{tenant_settings,user_settings,notes,folders,conversations,messages,transcriptions,api_keys}.ts` lists all 8
    - `pnpm --filter @openwhispr/data test -- --run` exits 0 (settings-rls + migration-0006-backfill + extended rls-property tests pass)
    - `packages/data/migrations/meta/_journal.json` contains entries for tags 0006..0010
  </acceptance_criteria>
  <done>Schemas + 5 migrations + extended RLS property tests landed in one atomic TDD commit. All FORCE RLS, AFTER INSERT trigger, GENERATED tsvector verified by integration tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: [BLOCKING] Apply migrations 0006..0010 via pnpm --filter @openwhispr/data migrate + verify tools/lint-rls.ts green</name>
  <files>
    .planning/phases/05-operational-endpoints/05-01-MIGRATE-LOG.md
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/packages/data/src/migrate.ts (migration runner — to understand DATABASE_URL_OWNER requirement)
    - /Users/nick/openwhispr-server/tools/lint-rls.ts (RLS introspection runner)
    - /Users/nick/openwhispr-server/Makefile (e2e/compose targets)
  </read_first>
  <action>
    **[BLOCKING — runs after Task 2 GREEN; blocks every Wave 1 plan.]**

    Apply the new migrations against the live local Postgres (DATABASE_URL_OWNER required per Phase 1 D-*) and verify RLS lint stays green.

    Sequence:

    1. Ensure docker compose Postgres + PgBouncer are up: `docker compose ps | grep -E 'postgres|pgbouncer'` shows both running. If not, `docker compose up -d postgres pgbouncer`.

    2. Apply migrations:
       ```
       cd /Users/nick/openwhispr-server
       pnpm --filter @openwhispr/data migrate
       ```
       Expected: journal advances to entry 0010; no errors. Verify with `psql "$DATABASE_URL_OWNER" -c "\dt notes folders conversations messages transcriptions api_keys tenant_settings user_settings"` — all 8 tables listed.

    3. Run RLS introspection lint:
       ```
       pnpm --filter @openwhispr tsx tools/lint-rls.ts
       ```
       Expected exit 0. Output must report FORCE RLS for all 8 new tables.

    4. Run extended RLS property test against the live PgBouncer:
       ```
       pnpm --filter @openwhispr/data test -- --run packages/data/src/__tests__/rls-property.test.ts
       ```
       Expected: 100×8 = 800 random cross-tenant attempts, ALL deny.

    5. Capture output to `.planning/phases/05-operational-endpoints/05-01-MIGRATE-LOG.md` with timestamp, journal tail, and lint output for the SUMMARY.md trail.

    If any step fails: STOP. Do NOT proceed to Wave 1. Diagnose root cause (per CLAUDE.md no-workaround rule) and emit a `revision` checkpoint back to the orchestrator.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/data migrate && pnpm tsx tools/lint-rls.ts && pnpm --filter @openwhispr/data test -- --run packages/data/src/__tests__/rls-property.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Command `psql "$DATABASE_URL_OWNER" -c "\\dt" | grep -E 'notes|folders|conversations|messages|transcriptions|api_keys|tenant_settings|user_settings' | wc -l` returns 8
    - `tools/lint-rls.ts` exits 0 and lists all 8 new tables as FORCE RLS = true
    - `pnpm --filter @openwhispr/data test -- --run packages/data/src/__tests__/rls-property.test.ts` exits 0
    - File created: `.planning/phases/05-operational-endpoints/05-01-MIGRATE-LOG.md` with captured output
  </acceptance_criteria>
  <done>Live migrations applied; RLS lint green; cross-tenant property test green across all 8 new tables. Wave 1 unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application → Postgres (via PgBouncer transaction-mode) | All Phase 5 data reads/writes flow through `app.tenant_id` GUC + RLS policies — RLS is the safety net if app-layer scoping drifts |
| New tenant lifecycle → seed trigger | AFTER INSERT trigger seeds tenant_settings — security definer fn must not bypass RLS for other operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering / DoS | tsvector GENERATED column expressions | high | mitigate | Per Pitfall #1: tsvector expression references only own-row columns (coalesce(title,''), coalesce(content,'')) — no now(), no current_setting. Verified by integration test reading pg_attrdef. |
| T-05-05 | Elevation of Privilege | tenant_settings + user_settings tables | high | mitigate | FORCE RLS + policy on every settings table per D-31. RLS introspection lint (Phase 1) gates CI. Phase 5 ships READ-only paths; writes deferred to Phase 7. Mitigated by [BLOCKING] task 3 lint-rls verification. |
| T-05-07 | Tampering | client_note_id / client_folder_id / client_conversation_id / client_transcription_id partial UNIQUE | high | mitigate | UNIQUE scoped to (tenant_id, user_id, client_*_id) WHERE client_*_id IS NOT NULL per D-24. Cross-tenant collision impossible by index definition. Verified by extended rls-property.test.ts 100 pairs/table. |
| T-RLS-bypass-01 | Spoofing | AFTER INSERT trigger seed_tenant_settings | high | mitigate | Trigger is `SECURITY DEFINER` (per Pitfall #8) but ONLY inserts into tenant_settings with NEW.id from the parent tenant insert — no privilege escalation surface (cannot reach other tenants' data). |

Block on: high. All 4 high-severity threats are mitigated in this plan's deliverables.
</threat_model>

<verification>
- Run [BLOCKING] task 3 verify command and ensure exit 0
- Confirm `.planning/phases/05-operational-endpoints/05-01-MIGRATE-LOG.md` captures lint + property test output
- Confirm Wave 1 plans (02/03/04) can find `@openwhispr/wire-schemas` package on import
</verification>

<success_criteria>
- All `<acceptance_criteria>` of Tasks 1, 2, 3 pass
- All 8 new tables created with FORCE RLS
- AFTER INSERT trigger seeds tenant_settings (Pitfall #8 fix)
- `@openwhispr/wire-schemas` package buildable and consumable by Wave 1 plans
- Extended rls-property.test.ts covers cross-tenant isolation on all 8 new tables
- `tools/lint-rls.ts` green
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-01-SUMMARY.md` recording: schemas created, migration journal entries 0006..0010, RLS lint output, blocking task migration log path, decisions implemented (D-17, D-21 AFTER INSERT, D-22, D-23, D-24, D-25 keyset indexes, D-26 tsvector+GIN, D-27, D-28, D-29 api_keys schema, D-31).
</output>
