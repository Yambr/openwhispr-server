---
phase: 05-operational-endpoints
plan: 07
type: execute
wave: 2
depends_on: [01, 05]
files_modified:
  - apps/api/src/routes/conversations/create.ts
  - apps/api/src/routes/conversations/update.ts
  - apps/api/src/routes/conversations/delete.ts
  - apps/api/src/routes/conversations/list.ts
  - apps/api/src/routes/conversations/search.ts
  - apps/api/src/routes/conversations/messages.ts
  - apps/api/src/routes/conversations/__tests__/crud.integration.test.ts
  - apps/api/src/routes/conversations/__tests__/list-include-messages.integration.test.ts
  - apps/api/src/routes/conversations/__tests__/search.integration.test.ts
  - apps/api/src/routes/conversations/__tests__/messages.integration.test.ts
  - apps/api/src/routes/index.ts
  - packages/contract-tests/src/conversations.test.ts
  - tests/e2e/phase-05-conversations.spec.ts
autonomous: true
requirements: [WIRE-24, WIRE-25]
estimated_tasks: 3
must_haves:
  truths:
    - "POST /api/conversations/create returns CloudConversation per ~/openwhispr/src/services/ConversationsService.ts"
    - "POST /api/conversations/create with existing client_conversation_id returns existing row (200, not 409)"
    - "PATCH /api/conversations/update mutates only owner's row; cross-tenant invisible"
    - "DELETE /api/conversations/delete sets deleted_at; cascades soft-delete behavior to messages via FK (or messages remain but inaccessible — document per implementation choice)"
    - "GET /api/conversations/list?limit&before&since returns CloudConversation[] keyset-paginated"
    - "GET /api/conversations/list?include=messages returns CloudConversationWithMessages[] where each row has messages: CloudMessage[] aggregated via array_agg (D-27)"
    - "POST /api/conversations/search returns {conversations: [{...CloudConversation, score}]} using websearch_to_tsquery('simple', $1)"
    - "POST /api/conversations/messages with single CloudMessage payload returns created CloudMessage (D-22 + Claude's Discretion: single-message only for v1)"
    - "GET /api/conversations/messages?conversation_id=...&limit=&before= returns messages keyset-paginated on (created_at, id) per WIRE-25"
    - "Message metadata jsonb size-capped at 4KB per message (T-MSG-INJ mitigation)"
    - "Cross-tenant RLS isolation proven for both conversations and messages tables"
  artifacts:
    - path: "apps/api/src/routes/conversations/*.ts"
      provides: "6 route handlers (5 conversation CRUD + 1 messages handler covering POST and GET)"
    - path: "packages/contract-tests/src/conversations.test.ts"
      provides: "Contract conformance for WIRE-24 + WIRE-25"
  key_links:
    - from: "apps/api/src/routes/conversations/list.ts (include=messages branch)"
      to: "messages table"
      via: "array_agg(jsonb_build_object(...)) JOIN"
      pattern: "array_agg|jsonb_build_object"
    - from: "apps/api/src/routes/conversations/search.ts"
      to: "websearch_to_tsquery"
      via: "raw SQL"
      pattern: "websearch_to_tsquery"
---

<objective>
Wave 2: Implement WIRE-24 (Conversations CRUD + search + list w/ include=messages) and WIRE-25 (Messages CRUD: add + list).

Purpose: Conversations is the most complex CRUD family — has tsvector search + relational messages JOIN + dual sub-endpoint (`/messages` POST + GET). Mirrors Plan 05 patterns plus D-27 JOIN aggregation.
Output: 6 route files + 4 integration test files + contract + e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-05-SUMMARY.md
@apps/api/src/routes/notes/create.ts
@apps/api/src/routes/notes/list.ts
@apps/api/src/routes/notes/search.ts
@apps/api/src/lib/keyset-pagination.ts
@apps/api/src/lib/client-id-upsert.ts
@packages/data/src/schema/conversations.ts
@packages/data/src/schema/messages.ts
@packages/wire-schemas/src/conversations.ts
@~/openwhispr/src/services/ConversationsService.ts

<interfaces>
ConversationInput / CloudConversation / CloudMessage / CloudConversationWithMessages — from ~/openwhispr/src/services/ConversationsService.ts

D-27: list?include=messages → array_agg JSON aggregation (single round-trip, typed shape stability)
Cap conversations message-include at 100 messages per conversation (per Open Q#2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN conversations CRUD (create, update, delete, list-basic, search)</name>
  <files>
    apps/api/src/routes/conversations/create.ts,
    apps/api/src/routes/conversations/update.ts,
    apps/api/src/routes/conversations/delete.ts,
    apps/api/src/routes/conversations/list.ts,
    apps/api/src/routes/conversations/search.ts,
    apps/api/src/routes/conversations/__tests__/crud.integration.test.ts,
    apps/api/src/routes/conversations/__tests__/search.integration.test.ts,
    apps/api/src/routes/index.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr/src/services/ConversationsService.ts (full file)
    - /Users/dev/openwhispr-server/apps/api/src/routes/notes/ (Plan 05 to mirror — create, update, delete, list, search)
    - /Users/dev/openwhispr-server/packages/data/src/schema/conversations.ts (Plan 01 columns)
    - /Users/dev/openwhispr-server/packages/wire-schemas/src/conversations.ts
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-22..D-26
  </read_first>
  <behavior>
    - POST /api/conversations/create body ConversationInput → CloudConversation (200); client_conversation_id idempotency
    - PATCH /api/conversations/update → 200 CloudConversation
    - DELETE /api/conversations/delete → sets deleted_at
    - GET /api/conversations/list?limit&before&since → CloudConversation[] keyset paginated (no include=messages here — Task 2 adds that branch)
    - POST /api/conversations/search → {conversations: [{...CloudConversation, score}]} via websearch_to_tsquery('simple', $1)
    - Cross-tenant RLS isolation
  </behavior>
  <action>
    Mirror Plan 05 patterns 1-to-1 substituting:
    - table name: `conversations`
    - client id col: `client_conversation_id`
    - fields per ConversationInput/CloudConversation TS interface
    - search returns `{conversations: [{...CloudConversation, score: number}]}` (note plural key per service file)
    - search ts_vector source is `conversations.content_search` (over title only per Plan 01 schema)

    Register 5 routes in `apps/api/src/routes/index.ts`.

    Single atomic commit `test+feat(05-07): conversations CRUD + search WIRE-24 (sans include=messages branch)`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/conversations/__tests__/crud.integration.test.ts apps/api/src/routes/conversations/__tests__/search.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 5 files exist
    - grep ≥1 each `/api/conversations/{create,update,delete,list,search}` in `apps/api/src/routes/index.ts`
    - grep ≥1: `grep -E "websearch_to_tsquery\\('simple'" apps/api/src/routes/conversations/search.ts`
    - grep ≥1: `grep -E "client_conversation_id" apps/api/src/routes/conversations/create.ts`
    - Test command exits 0
  </acceptance_criteria>
  <done>Conversations basic CRUD + search landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED + GREEN list?include=messages JSON aggregation branch (D-27 array_agg)</name>
  <files>
    apps/api/src/routes/conversations/list.ts,
    apps/api/src/routes/conversations/__tests__/list-include-messages.integration.test.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr-server/apps/api/src/routes/conversations/list.ts (just created Task 1 — modifying)
    - /Users/dev/openwhispr/src/services/ConversationsService.ts (CloudConversationWithMessages interface)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-27
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Open Q#2 (100-message cap per conversation)
  </read_first>
  <behavior>
    - GET /api/conversations/list (no include) → CloudConversation[] (Task 1 default)
    - GET /api/conversations/list?include=messages → CloudConversationWithMessages[] where each row has `messages: CloudMessage[]`
    - Messages aggregated via array_agg up to 100 per conversation (per Open Q#2 cap)
    - Messages within each conversation ordered by (created_at ASC, id ASC); WHERE messages.deleted_at IS NULL
    - When ?include=messages NOT 'messages', treat as default list
  </behavior>
  <action>
    Extend `apps/api/src/routes/conversations/list.ts` to branch on `req.query.include === 'messages'`:

    ```ts
    if (req.query.include === 'messages') {
      const result = await tx.execute(sql`
        SELECT c.*,
          COALESCE(
            (SELECT array_agg(
               jsonb_build_object(
                 'id', m.id, 'conversation_id', m.conversation_id,
                 'tenant_id', m.tenant_id, 'user_id', m.user_id,
                 'role', m.role, 'content', m.content, 'metadata', m.metadata,
                 'client_message_id', m.client_message_id,
                 'created_at', m.created_at, 'updated_at', m.updated_at,
                 'deleted_at', m.deleted_at
               ) ORDER BY m.created_at ASC, m.id ASC
             )
             FROM (
               SELECT * FROM messages
               WHERE conversation_id = c.id AND deleted_at IS NULL
               ORDER BY created_at ASC, id ASC
               LIMIT 100
             ) m
            ),
            ARRAY[]::jsonb[]
          ) AS messages
        FROM conversations c
        WHERE c.deleted_at IS NULL
          ${before ? sql`AND (c.created_at, c.id) < (${before}, '\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff\xff'::uuid)` : sql``}
          ${since ? sql`AND (c.created_at, c.id) > (${since}, '\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00'::uuid)` : sql``}
        ORDER BY c.created_at DESC, c.id DESC
        LIMIT ${limit}
      `);
      return result.rows;
    }
    ```

    Integration test seeds 2 conversations × 5 messages each + 1 conversation × 150 messages (cap test) + 1 with soft-deleted message. Asserts:
    - Response shape conforms to CloudConversationWithMessages
    - Conversation with 150 messages returns only first 100
    - Soft-deleted message excluded

    Commit `test+feat(05-07): conversations list?include=messages array_agg branch per D-27`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/conversations/__tests__/list-include-messages.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep ≥1: `grep -E "include === 'messages'|include == 'messages'|include.*'messages'" apps/api/src/routes/conversations/list.ts`
    - grep ≥1: `grep -E "array_agg" apps/api/src/routes/conversations/list.ts`
    - grep ≥1: `grep -E "LIMIT 100" apps/api/src/routes/conversations/list.ts`
    - Test command exits 0
  </acceptance_criteria>
  <done>include=messages branch landed with 100-message cap per conversation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED + GREEN POST + GET /api/conversations/messages (WIRE-25) + contract + e2e</name>
  <files>
    apps/api/src/routes/conversations/messages.ts,
    apps/api/src/routes/conversations/__tests__/messages.integration.test.ts,
    apps/api/src/routes/index.ts,
    packages/contract-tests/src/conversations.test.ts,
    tests/e2e/phase-05-conversations.spec.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr/src/services/ConversationsService.ts (CloudMessage interface, addMessage/getMessages signatures)
    - /Users/dev/openwhispr-server/packages/data/src/schema/messages.ts (Plan 01)
    - /Users/dev/openwhispr-server/packages/wire-schemas/src/conversations.ts
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-22, D-27 + Claude's Discretion (single-message only v1)
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Security Domain (T-MSG-INJ — 4KB metadata cap)
  </read_first>
  <behavior>
    - POST /api/conversations/messages body {conversation_id, role, content, metadata?, client_message_id?} → CloudMessage (200, single message only per v1)
    - POST with metadata > 4KB → 400 envelope (T-MSG-INJ mitigation)
    - POST with existing client_message_id returns existing row (200, not 409)
    - GET /api/conversations/messages?conversation_id=<uuid>&limit=&before=&since= → CloudMessage[] keyset paginated on (created_at, id); soft-deleted excluded
    - GET requires conversation_id query param; missing → 400 envelope
    - Cross-tenant: tenant B's messages invisible to tenant A (RLS via conversations FK + messages.tenant_id)
  </behavior>
  <action>
    `apps/api/src/routes/conversations/messages.ts` registers BOTH POST and GET:
    ```ts
    app.post('/api/conversations/messages', { preHandler: dualAuth }, async (req) => {
      const body = MessageInputSchema.parse(req.body);  // single-message only
      const metadataSize = JSON.stringify(body.metadata ?? {}).length;
      if (metadataSize > 4096) throw new ValidationError('metadata exceeds 4KB');
      return withTenant(deps.db, req.tenant, async (tx) => {
        // INSERT with ON CONFLICT (tenant_id, user_id, client_message_id) DO NOTHING RETURNING
        // SELECT fallback if conflict
        // ...
      });
    });

    app.get('/api/conversations/messages', { preHandler: dualAuth }, async (req) => {
      const { conversation_id, ...listQ } = req.query as any;
      if (!conversation_id) throw new ValidationError('conversation_id required');
      const { limit, before, since } = parseListQuery(listQ);
      return withTenant(deps.db, req.tenant, async (tx) => {
        const rows = await tx.execute(sql`
          SELECT * FROM messages
          WHERE conversation_id = ${conversation_id}::uuid
            AND deleted_at IS NULL
            ${before ? sql`AND (created_at, id) < (${before}, '\xff...'::uuid)` : sql``}
            ${since ? sql`AND (created_at, id) > (${since}, '\x00...'::uuid)` : sql``}
          ORDER BY created_at DESC, id DESC
          LIMIT ${limit}
        `);
        return rows.rows;
      });
    });
    ```

    Register both endpoints in `apps/api/src/routes/index.ts`.

    Contract test `packages/contract-tests/src/conversations.test.ts`: covers all 6 endpoints (5 conversation + 1 messages dual-method) + the include=messages branch + zod-parses CloudMessage shape.

    E2E `tests/e2e/phase-05-conversations.spec.ts`: live compose round-trip — create conversation, add 5 messages, list with include=messages, list messages directly, search, soft-delete conversation.

    Single atomic commit `test+feat(05-07): conversations messages + contract + e2e WIRE-24+WIRE-25`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/conversations/__tests__/messages.integration.test.ts && pnpm --filter @openwhispr/contract-tests test -- --run src/conversations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/api/src/routes/conversations/messages.ts`
    - grep ≥1: `grep -E "app\\.post\\(.*\\/api\\/conversations\\/messages|POST.*conversations/messages" apps/api/src/routes/conversations/messages.ts`
    - grep ≥1: `grep -E "app\\.get\\(.*\\/api\\/conversations\\/messages|GET.*conversations/messages" apps/api/src/routes/conversations/messages.ts`
    - grep ≥1: `grep -E "4096|4KB" apps/api/src/routes/conversations/messages.ts`
    - grep ≥1: `grep -E "conversation_id" apps/api/src/routes/conversations/messages.ts`
    - File exists: `packages/contract-tests/src/conversations.test.ts`
    - File exists: `tests/e2e/phase-05-conversations.spec.ts`
    - Test commands exit 0
  </acceptance_criteria>
  <done>WIRE-24 + WIRE-25 client-observable shipped.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-05-03 | Tampering / DoS | tsquery injection conversations/search | high | mitigate | websearch_to_tsquery; Zod 256-char cap |
| T-05-07 | Tampering | client_conversation_id / client_message_id cross-tenant collision | high | mitigate | Partial UNIQUE INDEX scoped per Plan 01 |
| T-MSG-INJ | Tampering | message metadata jsonb injection / oversized | medium | mitigate | 4KB cap enforced in handler; Zod validates known fields; SQL-safe via parameterized inserts |
| T-AGG-MEM | DoS | array_agg memory blow-up with many messages | medium | mitigate | 100-message cap per conversation (Open Q#2); Phase 8 load probe will validate |
</threat_model>

<verification>
- All <automated> commands exit 0
- E2E conversations round-trip passes including include=messages branch
</verification>

<success_criteria>
WIRE-24 + WIRE-25 client-observable must_haves green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-07-SUMMARY.md` recording: 6 routes added, include=messages array_agg JOIN per D-27, 100-message cap rationale, decisions D-22..D-27.
</output>
