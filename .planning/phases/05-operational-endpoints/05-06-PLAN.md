---
phase: 05-operational-endpoints
plan: 06
type: execute
wave: 2
depends_on: [01, 05]
files_modified:
  - apps/api/src/routes/folders/create.ts
  - apps/api/src/routes/folders/batch-create.ts
  - apps/api/src/routes/folders/update.ts
  - apps/api/src/routes/folders/delete.ts
  - apps/api/src/routes/folders/list.ts
  - apps/api/src/routes/folders/__tests__/crud.integration.test.ts
  - apps/api/src/routes/folders/__tests__/list.integration.test.ts
  - apps/api/src/routes/index.ts
  - packages/contract-tests/src/folders.test.ts
  - tests/e2e/phase-05-folders.spec.ts
autonomous: true
requirements: [WIRE-23]
estimated_tasks: 2
must_haves:
  truths:
    - "POST /api/folders/create with FolderInput returns CloudFolder (id, tenant_id, user_id, name, parent_folder_id, client_folder_id, created_at, updated_at, deleted_at: null) — 200"
    - "POST /api/folders/create with existing client_folder_id returns existing row (200, not 409) per D-24"
    - "POST /api/folders/batch-create with ≤500 items returns array in order; >500 → 400 envelope"
    - "PATCH /api/folders/update mutates only owner's row; cross-tenant invisible (RLS)"
    - "DELETE /api/folders/delete sets deleted_at = NOW(); not returned in list"
    - "GET /api/folders/list?limit=&before=&since= keyset paginates per D-25 in (created_at DESC, id DESC); soft-deleted excluded"
    - "parent_folder_id self-FK allowed; cascade to NULL on parent delete"
    - "Cross-tenant RLS proven by rls-property extension"
  artifacts:
    - path: "apps/api/src/routes/folders/*.ts"
      provides: "5 CRUD route handlers (no search, no delete-all)"
    - path: "packages/contract-tests/src/folders.test.ts"
      provides: "Contract conformance for WIRE-23"
  key_links:
    - from: "apps/api/src/routes/folders/*.ts"
      to: "apps/api/src/lib/{keyset-pagination,soft-delete,client-id-upsert}.ts"
      via: "shared helpers from Plan 05"
      pattern: "parseListQuery|createOrReturnExisting"
---

<objective>
Wave 2: Implement WIRE-23 — Folders CRUD + list + batch-create. Mirrors Plan 05 (Notes) pattern; no search, no delete-all.

Purpose: Folders organize notes (notes.folder_id FK). Simpler than notes (no full-text search). Reuses Plan 05 helpers verbatim.
Output: 5 route handlers + 2 integration test files + contract + e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-05-SUMMARY.md
@apps/api/src/routes/notes/create.ts
@apps/api/src/routes/notes/list.ts
@apps/api/src/lib/keyset-pagination.ts
@apps/api/src/lib/client-id-upsert.ts
@packages/data/src/schema/folders.ts
@packages/wire-schemas/src/folders.ts
@~/openwhispr/src/services/FoldersService.ts

<interfaces>
FolderInput / CloudFolder — from ~/openwhispr/src/services/FoldersService.ts (read full file)

Reuse Plan 05 helpers verbatim: parseListQuery, createOrReturnExisting, soft-delete patterns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN folders CRUD routes (create, batch-create, update, delete, list)</name>
  <files>
    apps/api/src/routes/folders/create.ts,
    apps/api/src/routes/folders/batch-create.ts,
    apps/api/src/routes/folders/update.ts,
    apps/api/src/routes/folders/delete.ts,
    apps/api/src/routes/folders/list.ts,
    apps/api/src/routes/folders/__tests__/crud.integration.test.ts,
    apps/api/src/routes/folders/__tests__/list.integration.test.ts,
    apps/api/src/routes/index.ts
  </files>
  <read_first>
    - /Users/nick/openwhispr/src/services/FoldersService.ts (FolderInput, CloudFolder, all method signatures)
    - /Users/nick/openwhispr-server/apps/api/src/routes/notes/create.ts + list.ts (Plan 05 patterns to mirror)
    - /Users/nick/openwhispr-server/apps/api/src/lib/{keyset-pagination,client-id-upsert,soft-delete}.ts
    - /Users/nick/openwhispr-server/packages/data/src/schema/folders.ts
    - /Users/nick/openwhispr-server/packages/wire-schemas/src/folders.ts
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-22..D-25, D-30
  </read_first>
  <behavior>
    - POST /api/folders/create body FolderInput → CloudFolder (200); existing client_folder_id → returns existing (200)
    - POST /api/folders/batch-create ≤500 → array in order; >500 → 400 envelope
    - PATCH /api/folders/update {id, ...fields} → 200 CloudFolder; 0 rows → 404 envelope
    - DELETE /api/folders/delete {id} → sets deleted_at
    - GET /api/folders/list?limit&before&since → keyset paginated; soft-deleted excluded
    - parent_folder_id FK self-reference; deletion cascades to NULL via Plan 01 schema (ON DELETE SET NULL)
    - Cross-tenant RLS isolation enforced
  </behavior>
  <action>
    Mirror `apps/api/src/routes/notes/{create,batch-create,update,delete,list}.ts` Plan 05 implementation 1-to-1, substituting:
    - table name: `folders`
    - client id col: `client_folder_id`
    - fields per FolderInput/CloudFolder TS interface (name, parent_folder_id, client_folder_id)
    - no search route, no delete-all route

    Register 5 routes in `apps/api/src/routes/index.ts`.

    Integration test `crud.integration.test.ts` covers all 5 endpoints + cross-tenant RLS. `list.integration.test.ts` covers keyset pagination explicit edge cases (limit boundaries, before/since intersection).

    Single atomic commit `test+feat(05-06): folders CRUD WIRE-23`.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/folders</automated>
  </verify>
  <acceptance_criteria>
    - 5 files exist: `apps/api/src/routes/folders/{create,batch-create,update,delete,list}.ts`
    - grep returns ≥1 each: `/api/folders/create`, `/api/folders/batch-create`, `/api/folders/update`, `/api/folders/delete`, `/api/folders/list` in `apps/api/src/routes/index.ts`
    - grep returns ≥1: `grep -E "client_folder_id" apps/api/src/routes/folders/create.ts`
    - grep returns ≥1: `grep -E "parent_folder_id" apps/api/src/routes/folders/create.ts`
    - grep returns ≥1: `grep -E "500" apps/api/src/routes/folders/batch-create.ts`
    - Test command exits 0
  </acceptance_criteria>
  <done>Folders CRUD landed mirroring Plan 05 with table substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract conformance + E2E for folders WIRE-23</name>
  <files>
    packages/contract-tests/src/folders.test.ts,
    tests/e2e/phase-05-folders.spec.ts
  </files>
  <read_first>
    - /Users/nick/openwhispr-server/packages/contract-tests/src/notes.test.ts (Plan 05 — pattern to mirror)
    - /Users/nick/openwhispr-server/tests/e2e/phase-05-notes.spec.ts (Plan 05 e2e — pattern to mirror)
  </read_first>
  <action>
    `packages/contract-tests/src/folders.test.ts`: 5-endpoint contract suite mirroring `notes.test.ts`. Uses signInFixture + CloudFolder zod schema from `@openwhispr/wire-schemas`.

    `tests/e2e/phase-05-folders.spec.ts`: live compose round-trip — create 3 folders (one nested via parent_folder_id), list, update, soft-delete one, list (verify excluded), batch-create 5 more.

    Commit `test(05-06): folders contract + e2e WIRE-23`.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm --filter @openwhispr/contract-tests test -- --run src/folders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `packages/contract-tests/src/folders.test.ts`
    - File exists: `tests/e2e/phase-05-folders.spec.ts`
    - Test command exits 0
  </acceptance_criteria>
  <done>Folders contract + e2e green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-05-07 | Tampering | client_folder_id cross-tenant collision | high | mitigate | Partial UNIQUE INDEX scoped to (tenant_id, user_id, client_folder_id) WHERE NOT NULL per Plan 01 |
| T-05-04 | DoS | batch-create flood >500 | high | mitigate | Zod cap; transaction timeout 10s |
| T-PARENT-LOOP | Tampering | parent_folder_id self-loop / cycle | medium | accept | Application-layer cycle check deferred; ON DELETE SET NULL prevents orphan crash. Documented in SUMMARY. |
</threat_model>

<verification>
- All <automated> commands exit 0
- E2E folders round-trip passes
</verification>

<success_criteria>
WIRE-23 client-observable must_haves green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-06-SUMMARY.md` recording: 5 folders routes added, reused Plan 05 helpers, decisions D-22..D-25, D-30.
</output>
