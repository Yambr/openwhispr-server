---
phase: 05-operational-endpoints
plan: 08
type: execute
wave: 2
depends_on: [01, 05]
files_modified:
  - apps/api/src/routes/transcriptions/create.ts
  - apps/api/src/routes/transcriptions/batch-create.ts
  - apps/api/src/routes/transcriptions/list.ts
  - apps/api/src/routes/transcriptions/delete.ts
  - apps/api/src/routes/transcriptions/batch-delete.ts
  - apps/api/src/routes/transcriptions/__tests__/crud.integration.test.ts
  - apps/api/src/routes/transcriptions/__tests__/batch.integration.test.ts
  - apps/api/src/routes/index.ts
  - packages/contract-tests/src/transcriptions.test.ts
  - tests/e2e/phase-05-transcriptions.spec.ts
autonomous: true
requirements: [WIRE-26]
estimated_tasks: 2
must_haves:
  truths:
    - "POST /api/transcriptions/create with TranscriptionInput returns CloudTranscription (id, tenant_id, user_id, text, language, duration_seconds, model, provider, client_transcription_id, created_at, updated_at, deleted_at: null) — 200"
    - "POST /api/transcriptions/create with existing client_transcription_id returns existing row (200, not 409) per D-24"
    - "POST /api/transcriptions/batch-create ≤500 → array in order; >500 → 400 envelope per D-30"
    - "POST /api/transcriptions/batch-delete with array of ids (≤500) soft-deletes each in single transaction; returns {deletedCount: number}"
    - "GET /api/transcriptions/list?limit&before&since keyset paginated; soft-deleted excluded"
    - "DELETE /api/transcriptions/delete sets deleted_at"
    - "CRUD writes do NOT debit usage_ledger (D-32 — transcribe Phase 3 endpoint already debits)"
    - "Cross-tenant RLS proven"
  artifacts:
    - path: "apps/api/src/routes/transcriptions/*.ts"
      provides: "5 route handlers (no search; no update — service interface is create+list+delete+batch only)"
    - path: "packages/contract-tests/src/transcriptions.test.ts"
      provides: "Contract conformance for WIRE-26"
  key_links:
    - from: "apps/api/src/routes/transcriptions/*"
      to: "apps/api/src/lib/{keyset-pagination,client-id-upsert,soft-delete}.ts"
      via: "shared helpers from Plan 05"
      pattern: "parseListQuery|createOrReturnExisting"
---

<objective>
Wave 2: Implement WIRE-26 — Transcriptions CRUD + list + batch-create + batch-delete. Mirrors Plan 05 pattern; no search, no update (per service TS interface).

Purpose: Transcriptions are the persistent record of completed STT. Distinct from Phase 3's /api/transcribe (which is compute + ledger debit). D-32: this resource is storage-only, no ledger writes.
Output: 5 route handlers + 2 integration test files + contract + e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-05-SUMMARY.md
@apps/api/src/routes/notes/create.ts
@apps/api/src/routes/notes/batch-create.ts
@apps/api/src/routes/notes/list.ts
@apps/api/src/lib/keyset-pagination.ts
@apps/api/src/lib/client-id-upsert.ts
@packages/data/src/schema/transcriptions.ts
@packages/wire-schemas/src/transcriptions.ts
@~/openwhispr/src/services/TranscriptionsService.ts

<interfaces>
TranscriptionInput / CloudTranscription — from ~/openwhispr/src/services/TranscriptionsService.ts (read full file)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN transcriptions CRUD (create, batch-create, list, delete, batch-delete)</name>
  <files>
    apps/api/src/routes/transcriptions/create.ts,
    apps/api/src/routes/transcriptions/batch-create.ts,
    apps/api/src/routes/transcriptions/list.ts,
    apps/api/src/routes/transcriptions/delete.ts,
    apps/api/src/routes/transcriptions/batch-delete.ts,
    apps/api/src/routes/transcriptions/__tests__/crud.integration.test.ts,
    apps/api/src/routes/transcriptions/__tests__/batch.integration.test.ts,
    apps/api/src/routes/index.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr/src/services/TranscriptionsService.ts (full file)
    - /Users/dev/openwhispr-server/apps/api/src/routes/notes/{create,batch-create,list,delete}.ts (Plan 05 to mirror)
    - /Users/dev/openwhispr-server/packages/data/src/schema/transcriptions.ts (Plan 01 columns)
    - /Users/dev/openwhispr-server/packages/wire-schemas/src/transcriptions.ts
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-22..D-25, D-30, D-32
  </read_first>
  <behavior>
    - POST /api/transcriptions/create body TranscriptionInput → CloudTranscription (200); client_transcription_id idempotency
    - POST /api/transcriptions/batch-create ≤500 → array in order; >500 → 400 envelope
    - GET /api/transcriptions/list?limit&before&since → CloudTranscription[] keyset paginated, soft-deleted excluded
    - DELETE /api/transcriptions/delete {id} → sets deleted_at
    - POST /api/transcriptions/batch-delete {ids: string[]} (≤500) → soft-deletes within transaction; returns {deletedCount: number}; >500 → 400 envelope
    - NO usage_ledger writes from any CRUD path (D-32 invariant)
    - Cross-tenant RLS isolation
  </behavior>
  <action>
    Mirror Plan 05 patterns 1-to-1 substituting:
    - table name: `transcriptions`
    - client id col: `client_transcription_id`
    - fields per TranscriptionInput/CloudTranscription TS interface (text, language, duration_seconds, model, provider)
    - NO search route, NO update route, NO delete-all route

    `batch-delete.ts`:
    ```ts
    app.post('/api/transcriptions/batch-delete', { preHandler: dualAuth }, async (req) => {
      const { ids } = BatchDeleteSchema.parse(req.body);  // ids: z.array(z.string().uuid()).max(500)
      return withTenant(deps.db, req.tenant, async (tx) => {
        const result = await tx.execute(sql`
          UPDATE transcriptions SET deleted_at = NOW()
          WHERE id = ANY(${ids}::uuid[]) AND deleted_at IS NULL
        `);
        return { deletedCount: result.rowCount ?? 0 };
      });
    });
    ```

    Integration test verifies no rows appear in usage_ledger after CRUD operations (D-32 invariant).

    Register 5 routes in `apps/api/src/routes/index.ts`.

    Single atomic commit `test+feat(05-08): transcriptions CRUD + batch WIRE-26`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/api test -- --run apps/api/src/routes/transcriptions</automated>
  </verify>
  <acceptance_criteria>
    - 5 files exist: `apps/api/src/routes/transcriptions/{create,batch-create,list,delete,batch-delete}.ts`
    - grep ≥1 each `/api/transcriptions/{create,batch-create,list,delete,batch-delete}` in `apps/api/src/routes/index.ts`
    - grep ≥1: `grep -E "client_transcription_id" apps/api/src/routes/transcriptions/create.ts`
    - grep ≥1: `grep -E "500" apps/api/src/routes/transcriptions/batch-create.ts`
    - grep ≥1: `grep -E "ANY\\(.*::uuid\\[\\]\\)" apps/api/src/routes/transcriptions/batch-delete.ts`
    - Integration test asserts ZERO usage_ledger rows after transcriptions CRUD
    - Test command exits 0
  </acceptance_criteria>
  <done>Transcriptions CRUD landed; D-32 invariant proven (no ledger writes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract conformance + E2E for transcriptions WIRE-26</name>
  <files>
    packages/contract-tests/src/transcriptions.test.ts,
    tests/e2e/phase-05-transcriptions.spec.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr-server/packages/contract-tests/src/notes.test.ts (pattern)
    - /Users/dev/openwhispr-server/tests/e2e/phase-05-notes.spec.ts (pattern)
  </read_first>
  <action>
    Mirror notes contract + e2e patterns; cover 5 endpoints.

    E2E: round-trip create 3 transcriptions → list → soft-delete one → batch-delete remaining → list (empty).

    Commit `test(05-08): transcriptions contract + e2e WIRE-26`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm --filter @openwhispr/contract-tests test -- --run src/transcriptions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `packages/contract-tests/src/transcriptions.test.ts`
    - File exists: `tests/e2e/phase-05-transcriptions.spec.ts`
    - Test command exits 0
  </acceptance_criteria>
  <done>Transcriptions contract + e2e green.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-05-07 | Tampering | client_transcription_id cross-tenant collision | high | mitigate | Partial UNIQUE per Plan 01 |
| T-05-04 | DoS | batch-create / batch-delete flood >500 | high | mitigate | Zod cap; transaction timeout |
| T-LEDGER-DUP | Tampering | CRUD writing to ledger (D-32 violation) | medium | mitigate | Integration test asserts ZERO ledger rows post-CRUD |
</threat_model>

<verification>
- All <automated> commands exit 0
- E2E transcriptions round-trip passes
- D-32 invariant test green (no usage_ledger writes from any CRUD path)
</verification>

<success_criteria>
WIRE-26 client-observable must_haves green; D-32 invariant proven.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-08-SUMMARY.md` recording: 5 routes, D-32 invariant test design, decisions D-22..D-25, D-30, D-32.
</output>
