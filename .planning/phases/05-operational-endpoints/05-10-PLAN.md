---
phase: 05-operational-endpoints
plan: 10
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - packages/contract-tests/src/negative-matrix.test.ts
  - packages/contract-tests/src/__tests__/negative-matrix-enumeration.test.ts
  - docs/conventions.md
  - docs/wire-contract.md
  - .planning/REQUIREMENTS.md
  - tests/e2e/phase-05-negative-matrix.spec.ts
autonomous: true
requirements: [WIRE-16, WIRE-29]
estimated_tasks: 2
must_haves:
  truths:
    - "negative-matrix.test.ts asserts envelope conformance across every Phase 2-5 /api/* route — inventory at minimum hardcodes all Phase 5 routes; enumeration sanity-checks against fastify route table"
    - "For each route, test triggers: (a) no auth → 401, (b) malformed body on POST/PATCH → 400/422, (c) synthetic unknown sub-path → 404 via Phase 2 setNotFoundHandler"
    - "Every non-2xx response matches tolerant envelope: {error: string} (default per D-34) OR {error: {message: string, code?: string}} (structured per D-33)"
    - "Phase 5 endpoints emit ONLY {error: string} (D-34); structured envelope not introduced by Phase 5"
    - "Synthetic /api/nonexistent-{uuid} → 404 + envelope (D-35 — Phase 2 setNotFoundHandler still works)"
    - "Stripe/referrals paths (/api/stripe/checkout, /api/referrals/stats, etc.) → 404 + envelope (out-of-scope per CONTEXT.md)"
    - "Enumeration-count test surfaces missing routes (Pitfall #6 mitigation): PHASE_5_ROUTES count ≥ runtime fastify route count filtered to /api/*"
    - "docs/conventions.md documents soft-delete + client_id + keyset + tsvector + envelope conventions for future CRUD families"
    - "docs/wire-contract.md documents v1 implemented routes + v2-deferred (Stripe/referrals) + known v1 limitations"
    - "REQUIREMENTS.md WIRE-08..29 rows flipped from Pending → Complete with Plan traceability table for Phase 5"
  artifacts:
    - path: "packages/contract-tests/src/negative-matrix.test.ts"
      provides: "Runtime envelope conformance matrix across full Phase 2-5 surface"
    - path: "docs/conventions.md"
      provides: "Phase 5-established CRUD conventions"
    - path: "docs/wire-contract.md"
      provides: "Wire contract reference + v2 deferred + known limitations"
  key_links:
    - from: "packages/contract-tests/src/negative-matrix.test.ts"
      to: "EnvelopeSchema (z.union of both shapes)"
      via: "tolerant matcher per D-33"
      pattern: "z\\.union|EnvelopeSchema"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 5 plans 01-09"
      via: "WIRE-08..29 traceability rows"
      pattern: "WIRE-(22|23|24|25|26|27|28|29).*Complete"
---

<objective>
Wave 3: Implement WIRE-29 — CONTRACT-01 negative matrix proving the `cloud-api-request` envelope passthrough invariant (WIRE-16) holds across the entire Phase 2-5 wire surface. Plus convention docs and REQUIREMENTS.md traceability finalization.

Purpose: One test file proves the global envelope invariant for the WHOLE surface — instead of per-route assertions, the matrix enumerates routes and asserts envelope conformance everywhere. Locks in CONTRACT-01 as the regression net for Phase 5.
Output: Negative-matrix contract test + e2e + 2 docs + REQUIREMENTS.md traceability update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-operational-endpoints/05-CONTEXT.md
@.planning/phases/05-operational-endpoints/05-RESEARCH.md
@.planning/phases/05-operational-endpoints/05-02-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-03-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-04-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-05-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-06-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-07-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-08-SUMMARY.md
@.planning/phases/05-operational-endpoints/05-09-SUMMARY.md
@packages/contract-tests/src/conventions.test.ts
@packages/contract-tests/src/schemas.ts
@apps/api/src/error-handler.ts
@apps/api/src/routes/index.ts
@.planning/REQUIREMENTS.md
@~/openwhispr/docs/BACKEND_SPEC.md

<interfaces>
Phase 2 setNotFoundHandler emits envelope on unknown paths (D-35)

D-33: tolerant matcher accepting BOTH:
  z.object({ error: z.string() })                                   // default (Phase 5)
  z.object({ error: z.object({ message: z.string(), code: z.string().optional() }) })  // structured (BACKEND_SPEC.md:745)

Phase 5 endpoint inventory:
  POST /api/agent/web-search
  POST /api/streaming-usage
  GET  /api/usage
  GET  /api/stt-config
  GET  /api/note-recording-config
  POST /api/notes/{create,batch-create,update,delete,delete-all,list,search}  (note: update is PATCH, deletes are DELETE per service)
  POST /api/folders/{create,batch-create,update,delete,list}
  POST /api/conversations/{create,update,delete,list,search}
  POST + GET /api/conversations/messages
  POST /api/transcriptions/{create,batch-create,batch-delete}
  GET  /api/transcriptions/list
  DELETE /api/transcriptions/delete
  GET  /api/v1/keys/list
  POST /api/v1/keys/create
  POST /api/v1/keys/:id/revoke

Out-of-scope (must 404 + envelope per CONTEXT.md):
  /api/stripe/{checkout,portal,switch-plan,preview-switch}
  /api/referrals/{stats,invite,invites}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CONTRACT-01 negative matrix + enumeration sanity test + e2e</name>
  <files>
    packages/contract-tests/src/negative-matrix.test.ts,
    packages/contract-tests/src/__tests__/negative-matrix-enumeration.test.ts,
    tests/e2e/phase-05-negative-matrix.spec.ts
  </files>
  <read_first>
    - /Users/dev/openwhispr-server/packages/contract-tests/src/conventions.test.ts (existing envelope assertion pattern)
    - /Users/dev/openwhispr-server/packages/contract-tests/src/schemas.ts (ErrorEnvelope zod from Phase 2)
    - /Users/dev/openwhispr-server/packages/contract-tests/src/helpers (signInFixture, BACKEND_URL)
    - /Users/dev/openwhispr-server/apps/api/src/error-handler.ts (global handler shape)
    - /Users/dev/openwhispr-server/apps/api/src/routes/index.ts (full registered route set after Plans 02-09)
    - /Users/dev/openwhispr/docs/BACKEND_SPEC.md line 745 (cloud-api-request envelope spec, 401/503 special-cases)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md D-33, D-34, D-35, D-36
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Pitfall #6 (matrix race with route registration order)
  </read_first>
  <behavior>
    - Tolerant envelope matcher: zod union of `z.object({error: z.string()})` AND `z.object({error: z.object({message: z.string(), code: z.string().optional()})})`
    - For each Phase 5 route in PHASE_5_ROUTES inventory, run sub-cases:
      (a) no-auth call → expect 401 + envelope match
      (b) malformed body (e.g. {"invalid":true}) on POST/PATCH with cookie auth → expect 400/422 + envelope match
    - Synthetic path /api/nonexistent-${randomUUID()} → 404 + envelope (D-35 proof)
    - Stripe/referrals 7 paths → 404 + envelope (out-of-scope proof)
    - negative-matrix-enumeration.test.ts: spin up Fastify app instance, call fastify.printRoutes(), filter to /api/*, assert count ≤ PHASE_5_ROUTES.length + Phase 2-4 route count baseline. If runtime count exceeds inventory, fail with "missing route in negative matrix inventory: <route>" (Pitfall #6).
  </behavior>
  <action>
    Implement `packages/contract-tests/src/negative-matrix.test.ts` per the structure in the design code block. Use the existing `signInFixture` + `BACKEND_URL` helpers from Phase 2.

    Use zod union for tolerant envelope match. Hardcode PHASE_5_ROUTES inventory as a const array of {method, path} tuples covering all routes listed in the `<interfaces>` block above.

    Add `packages/contract-tests/src/__tests__/negative-matrix-enumeration.test.ts`:
    - Imports buildApp() from `apps/api` test entry
    - After app ready, calls fastify.printRoutes() (or iterates fastify.routes)
    - Filters to /api/* paths
    - Asserts every runtime route appears in PHASE_5_ROUTES OR in a known Phase 2-4 baseline route list (also exported from this file)
    - If runtime route NOT in inventory, fail with explicit "missing route" error

    E2E `tests/e2e/phase-05-negative-matrix.spec.ts`: runs the same negative matrix against the live compose stack via Traefik HTTPS (uses E2E=1 gate + signInFixture against live API).

    Single atomic commit `test(05-10): CONTRACT-01 negative matrix WIRE-29 + WIRE-16 envelope passthrough`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && make contract-test BACKEND_URL=https://api.localhost && E2E=1 make e2e-test SPEC=tests/e2e/phase-05-negative-matrix.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `packages/contract-tests/src/negative-matrix.test.ts`
    - grep ≥1: `grep -E "z\\.union" packages/contract-tests/src/negative-matrix.test.ts`
    - grep ≥1: `grep -E "/api/nonexistent-" packages/contract-tests/src/negative-matrix.test.ts`
    - grep ≥1: `grep -E "/api/stripe/checkout|/api/referrals/stats" packages/contract-tests/src/negative-matrix.test.ts`
    - grep ≥1: `grep -E "/api/agent/web-search.*method.*POST|method:.*POST.*web-search" packages/contract-tests/src/negative-matrix.test.ts`
    - File exists: `packages/contract-tests/src/__tests__/negative-matrix-enumeration.test.ts`
    - grep ≥1: `grep -E "printRoutes|fastify\\.routes" packages/contract-tests/src/__tests__/negative-matrix-enumeration.test.ts`
    - File exists: `tests/e2e/phase-05-negative-matrix.spec.ts`
    - Test commands exit 0
  </acceptance_criteria>
  <done>Negative matrix proves envelope invariant for entire Phase 2-5 surface; out-of-scope paths emit 404 envelope; route enumeration safety check prevents matrix gaps.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: docs/conventions.md + docs/wire-contract.md + REQUIREMENTS.md WIRE-08..29 traceability finalization</name>
  <files>
    docs/conventions.md,
    docs/wire-contract.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 218-238 (current WIRE-08..29 Pending traceability rows) + lines 317-369 (Phase 1/2 traceability table format to mirror)
    - .planning/phases/05-operational-endpoints/05-CONTEXT.md (full — for conventions wording)
    - .planning/phases/05-operational-endpoints/05-RESEARCH.md § Pitfall #9 + § Open Questions (known v1 limitations to document)
    - /Users/dev/openwhispr/docs/BACKEND_SPEC.md lines 567-720 (Stripe/referrals — v2-deferred surface)
  </read_first>
  <action>
    Create `docs/conventions.md` (or extend if exists). Document Phase 5-established conventions for future CRUD families:
    - **Soft-delete:** `deleted_at timestamptz NULL` on every CRUD table; all read paths filter `WHERE deleted_at IS NULL` via `apps/api/src/lib/soft-delete.ts` helper
    - **Client-id idempotency:** partial UNIQUE INDEX `(tenant_id, user_id, client_<resource>_id) WHERE client_<resource>_id IS NOT NULL`; duplicate create returns existing row (200, not 409) per D-24
    - **Keyset pagination:** `?limit=&before=&since=` on `(created_at, id)` tuple comparison; default limit 50, max 200; `apps/api/src/lib/keyset-pagination.ts` helper
    - **Full-text search:** Postgres `tsvector` GENERATED ALWAYS AS STORED + GIN index; `websearch_to_tsquery('simple', $1)` for safe raw input (NEVER `to_tsquery` or `plainto_tsquery`)
    - **Batch operations:** cap at 500 items; single transaction; ON CONFLICT preserves existing
    - **Envelope:** `{error: string}` default for all new endpoints; `{data: T}` envelope ONLY for `/api/v1/keys/*` per upstream client contract (D-28)
    - **RLS:** every new table FORCE ROW LEVEL SECURITY with policy on `current_setting('app.tenant_id')`; cross-tenant property test mandatory (`packages/data/src/__tests__/rls-property.test.ts`)
    - **TDD:** RED test + GREEN implementation in same atomic commit; coverage ≥90/90/90/90 on all four axes per CLAUDE.md
    - **Argon2id:** OWASP 2026 params (m=64MB t=3 p=1) via `@node-rs/argon2`; never bcrypt for new code

    Create `docs/wire-contract.md`. Document:
    - **v1 implemented:** list every Phase 2-5 route by phase, mapping to WIRE-IDs
    - **v2 deferred:**
      - Stripe: `/api/stripe/checkout`, `/api/stripe/portal`, `/api/stripe/switch-plan`, `/api/stripe/preview-switch` — 404 + envelope today via Phase 2 setNotFoundHandler
      - Referrals: `/api/referrals/stats`, `/api/referrals/invite`, `/api/referrals/invites` — same
    - **Known v1 limitations:**
      - `'simple'` tsvector config does no en/ru stemming (Pitfall #9) — search may miss morphological variants
      - BullMQ-async `notes/delete-all` deferred to Phase 6 (1000-row inline cap; >1000 returns 400)
      - `Bearer pak_*` API key middleware deferred to Phase 6 (CRUD ships; auth-path activates next phase)
      - 100-message-per-conversation cap for `?include=messages` (Open Q#2 — Phase 8 load probe will validate)
      - Yandex Search adapter confidence depends on Plan 03 Task 1 user-action outcome (live reference vs stub)
      - Settings tables ship READ paths only; PUT/PATCH deferred to Phase 7 UI per D-17

    Update `.planning/REQUIREMENTS.md`:
    - Flip WIRE-08, WIRE-09, WIRE-10, WIRE-11, WIRE-12, WIRE-16, WIRE-22, WIRE-23, WIRE-24, WIRE-25, WIRE-26, WIRE-27, WIRE-28, WIRE-29 rows in the Traceability table from `Pending` to `Complete` (Phase 5)
    - Append a new section after the Phase 2 traceability table (around line 369) titled `### Phase 5 — Operational Endpoints + CRUD Resource Families (planned 2026-05-11)` with the traceability table format from Phase 1:
      ```
      | Requirement | Plan(s) | Primary Artifacts |
      |-------------|---------|-------------------|
      | WIRE-08 | 05-03 | apps/api/src/routes/agent/web-search.ts, apps/api/src/lib/web-search/{registry,tavily-adapter,yandex-adapter}.ts, packages/contract-tests/src/web-search.test.ts |
      | WIRE-09 | 05-02 | apps/api/src/routes/streaming-usage.ts, packages/contract-tests/src/streaming-usage.test.ts |
      | WIRE-10 | 05-02 | apps/api/src/routes/usage.ts, packages/contract-tests/src/usage.test.ts |
      | WIRE-11 | 05-04 | apps/api/src/routes/stt-config.ts, apps/api/src/lib/settings-resolver.ts |
      | WIRE-12 | 05-04 | apps/api/src/routes/note-recording-config.ts, apps/api/src/lib/settings-resolver.ts |
      | WIRE-16 | 05-10 | packages/contract-tests/src/negative-matrix.test.ts |
      | WIRE-22 | 05-05 | apps/api/src/routes/notes/*.ts, packages/contract-tests/src/notes.test.ts |
      | WIRE-23 | 05-06 | apps/api/src/routes/folders/*.ts, packages/contract-tests/src/folders.test.ts |
      | WIRE-24 | 05-07 | apps/api/src/routes/conversations/{create,update,delete,list,search}.ts |
      | WIRE-25 | 05-07 | apps/api/src/routes/conversations/messages.ts |
      | WIRE-26 | 05-08 | apps/api/src/routes/transcriptions/*.ts, packages/contract-tests/src/transcriptions.test.ts |
      | WIRE-27 | 05-09 | apps/api/src/routes/v1/keys/*.ts, apps/api/src/lib/argon2-keys.ts |
      | WIRE-28 | 05-01, 05-04 | packages/data/migrations/0006_tenant_settings.sql, apps/api/src/lib/settings-resolver.ts |
      | WIRE-29 | 05-10 | packages/contract-tests/src/negative-matrix.test.ts |
      ```

    Commit `docs(05-10): conventions + wire-contract + REQUIREMENTS.md WIRE-08..29 traceability for Phase 5 close`.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && test -f docs/conventions.md && test -f docs/wire-contract.md && grep -E "WIRE-22.*05-05|WIRE-22.*Phase 5" .planning/REQUIREMENTS.md && grep -E "/api/stripe|Stripe" docs/wire-contract.md && grep -E "websearch_to_tsquery" docs/conventions.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `docs/conventions.md`
    - File exists: `docs/wire-contract.md`
    - grep ≥1: `grep -E "websearch_to_tsquery" docs/conventions.md`
    - grep ≥1: `grep -E "client_<resource>_id|client_note_id" docs/conventions.md`
    - grep ≥1: `grep -E "FORCE ROW LEVEL SECURITY" docs/conventions.md`
    - grep ≥1: `grep -E "/api/stripe" docs/wire-contract.md`
    - grep ≥1: `grep -E "WIRE-22.*Phase 5|WIRE-22.*05-05|WIRE-22.*Complete" .planning/REQUIREMENTS.md`
    - grep ≥1: `grep -E "WIRE-29" .planning/REQUIREMENTS.md`
    - Verify command exits 0
  </acceptance_criteria>
  <done>Conventions + wire-contract docs landed; REQUIREMENTS.md WIRE-08..29 traceability complete.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-ENVELOPE-DRIFT | Information Disclosure | Non-conforming error envelope leaking stack traces | high | mitigate | Negative matrix asserts every 4xx/5xx matches z.union({error: string} | {error: {message, code?}}); regression net |
| T-ROUTE-MISSING | DoS / Tampering | New route added without negative-matrix coverage (Pitfall #6) | medium | mitigate | negative-matrix-enumeration.test.ts fails CI if runtime route not in inventory |
| T-OUT-OF-SCOPE-LEAK | Information Disclosure | Stripe/referrals endpoints accidentally implemented | low | mitigate | Matrix asserts these paths return 404 + envelope; doc-locked in wire-contract.md as v2-deferred |
</threat_model>

<verification>
- `make contract-test` against live compose stack exits 0 with negative matrix included
- E2E negative-matrix.spec.ts passes
- docs/conventions.md + docs/wire-contract.md + REQUIREMENTS.md traceability all updated
</verification>

<success_criteria>
WIRE-16 + WIRE-29 client-observable must_haves green. CONTRACT-01 regression net active for Phase 2-5 surface. Docs + traceability locked.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-endpoints/05-10-SUMMARY.md` recording: negative matrix design, enumeration sanity check (Pitfall #6 mitigation), out-of-scope path verification (Stripe/referrals 404), docs added, REQUIREMENTS.md WIRE-08..29 → Complete, decisions D-33..D-36.
</output>
