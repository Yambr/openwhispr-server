<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->
# Phase 19.2 — stt-fixture (@cjm-4.1 transcribe happy-path) — CONTEXT

**Created:** 2026-05-16
**Owner:** v2.1 followup batch (re-opened 2026-05-16)
**Predecessors:** Phase 19.1 + 19a + 19b (compose harness GREEN, host-split fixed, byok-guard + Drizzle role landed)

## Scope (locked from ROADMAP)

Flip `@cjm-4.1` (transcribe happy-path, `tests/e2e-cjm/features/transcribe.feature:6`) from `@expected-red @after-phase-19.2` to GREEN. Wire `/api/transcribe` round-trip against the mock-litellm proxy with a deterministic small WAV fixture; cjm step bindings already exist and target the correct multipart contract.

## Scout findings (verified 2026-05-16)

The discuss-phase scout walked the codebase and surfaced that **most of the surface is already implemented**:

1. `tests/e2e-cjm/features/transcribe.feature` — `@cjm-4.1` tagged `@expected-red @after-phase-19.2`; `@cjm-4.2` (negative twin) already GREEN (no `@expected-red`).
2. `tests/e2e-cjm/steps/transcribe.steps.ts` — REAL bindings: signs up via Better Auth (`/api/auth/sign-up/email`), verifies via mailpit, signs in, captures session cookie, multipart-POSTs `silent.wav` as `file` field, asserts 200 + `text: string` body shape. Negative path uses `origin` + `application/octet-stream` junk bytes.
3. `tests/e2e-cjm/fixtures/silent.wav` — fixture exists.
4. `compose/mock-litellm/src/server.ts:107` — `POST /v1/audio/transcriptions` endpoint implemented; honors `transcribeMeanMs/transcribeSdMs` jitter (default 1500±400ms). Returns shape compatible with `apps/api/src/routes/audio/transcribe.ts`.
5. `apps/api/src/routes/audio/` — existing route already lives behind Phase 3 LiteLLM wiring; the `@cjm-4.2` negative-path GREEN proves the route + auth + error envelope work end-to-end.

**Likely failure root cause:** The scenario was tagged `@expected-red @after-phase-19.2` at Phase 18.1-05 (tag repointing sweep) because **at that moment the round-trip had not been proven against the live compose stack**, not because a specific code defect blocks it. Phase 19b just unblocked compose harness end-to-end; the most probable outcome of running `@cjm-4.1` today is GREEN-on-first-try. If it fails, the gap is narrow (likely fixture-size, multipart field name, or model-alias mapping) and trivially fixable inline.

## Locked decisions

**D-19.2-01 (default route): mock-litellm, NOT smoke-paid**
The cjm scenario MUST default to the hermetic mock-litellm path. Smoke-paid (OpenAI/Groq) coverage is OUT OF SCOPE for this phase per `feedback_loadtest_cost_discipline.md` (plateaus only on local models). If the user wants a paid-path scenario, it carves to Phase 19.2.1 or backlog. NO `@cjm-4.1-paid` scenario added here.

**D-19.2-02 (fixture): reuse existing `silent.wav`, do NOT add new audio**
`tests/e2e-cjm/fixtures/silent.wav` exists from Phase 13. No new audio. Phase 13's contract was "string `text` field, non-empty enough to be a string." Mock-litellm returns a stub transcript via `/v1/audio/transcriptions`. The contract is shape, not content.

**D-19.2-03 (step bindings): EXTEND, do not rewrite**
Existing step bindings work. Phase 19.2 adds:
- A vitest unit-test file `tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` per `feedback_cjm_steps_need_unit_tests.md` covering URL + multipart shape + cookie header + response-shape assertions.
- No production-side `apps/api/src/routes/audio/*.ts` changes UNLESS the live-run surfaces a defect (in which case a Hard-Rule INVERSION sub-task is added with SERVER-ERRORS ledger entry).

**D-19.2-04 (tag flip): single-commit GREEN gate**
Final commit removes `@expected-red @after-phase-19.2` from `transcribe.feature:6` AFTER local `make e2e-cjm SCENARIO="@cjm-4.1"` exits 0. ZERO `--no-verify`. Conventional commit per 19b precedent.

**D-19.2-05 (lint gate): cjm-doc lint stays green**
`tools/lint-cjm-doc.ts` regenerates customer-journeys.md doc parity; the docs entry for `@cjm-4.1` already exists at `docs/customer-journeys.md` (verified). After tag flip, lint must still pass.

**D-19.2-06 (smoke chain): L1→L1b→L2→L3 per memory rule**
Before running the full e2e scenario, run in order:
- L1: `pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` (~1s)
- L1b: `pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` (~1s)
- L2: `docker compose -f docker-compose.yml -f compose/docker-compose.ingress.yml up -d traefik api worker mock-litellm postgres valkey mailpit` + curl `https://api.localhost/api/transcribe` smoke against mock-litellm (~30s)
- L3: `E2E_CJM=1 SCENARIO="@cjm-4.1" make e2e-cjm`
Check container logs at each layer per `feedback_check_loki_after_tests.md`.

**D-19.2-07 (scope discipline): NO production refactor**
If `@cjm-4.1` fails at L3, the FIRST hypothesis is a TEST-SIDE issue (multipart field name, content-type, cookie passthrough). Only after RED-on-server-logs does a production-side fix get carved as a Hard-Rule INVERSION sub-task with explicit user approval (mirrors Phase 19a/19b pattern). Default expectation: zero production edits.

## Gray areas — none requiring user input

Every gray area for this phase has a defensible default from prior phases / existing code / memory rules / locked decisions above. No `AskUserQuestion` needed.

If the live run surfaces a **new** gray area (e.g., mock-litellm doesn't honor a specific model alias used in production config), the planner will surface it via the bounce loop, not pre-emptively here.

## Plan-phase next steps

Single plan expected:
- **Plan 19.2-01**: extend step-binding unit tests + run smoke chain + flip tag.

Estimated total: ~30-60 min real work. If first live run is RED, scope may grow by one Hard-Rule INVERSION sub-task; in that case, plan structure expands to 2 plans (test-extension + production-fix).

## Hard constraints (NON-NEGOTIABLE — inherited from CLAUDE.md + memory)

- ZERO `--no-verify` across all commits.
- Strict TDD: RED → GREEN paired atomic commits.
- English-only source artifacts.
- Conventional Commits (lowercase subject ≤100 char header).
- Co-Authored-By: Claude Opus 4.7 trailer on every commit.
- Smoke chain L1→L1b→L2→L3 before full e2e.
- Loki/docker logs first when tests fail.
- Step bindings MUST have vitest unit coverage — waivers BANNED.
- No mocks of internal logic — process-boundary only.

## Deferred ideas (NOT in this phase)

- Smoke-paid `@cjm-4.1-paid` scenario for OpenAI/Groq STT — backlog.
- `apps/api/src/routes/audio/transcribe.ts` coverage backfill — Phase 14-04 typecheck deferral already CLOSED downstream by SR-19.2/19.3.
- Audio-format coverage matrix (MP3, M4A, OGG) — Phase 5 acceptance, not v2.1 followup.
- Long-form audio (>5 min) chunking — Phase 5+ follow-up.
