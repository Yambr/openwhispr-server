---
phase: 19.2-stt-fixture-cjm-4-1-transcribe-happy-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts
  - tests/e2e-cjm/features/transcribe.feature
  - docs/customer-journeys.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SR-19.2.1
  - SR-19.2.2
  - SR-19.2.3
  - SR-19.2.4
  - SR-19.2.5
  - SR-19.2.6
must_haves:
  truths:
    - "`pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` exits 0 with ≥5 cases covering URL, multipart shape, headers, response shape, negative path."
    - "`pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` exits 0 after tag flip."
    - "`make e2e-cjm SCENARIO=\"@cjm-4.1\"` exits 0 with 1/1 scenario GREEN."
    - "`tests/e2e-cjm/features/transcribe.feature:6` no longer carries `@expected-red @after-phase-19.2`."
    - "`docs/customer-journeys.md` @cjm-4.1 row reflects CLOSED 2026-05-16, no `currently @expected-red` annotation."
    - "`.planning/ROADMAP.md` Phase 19.2 entry is `- [x]` and Phase Details heading shows `— CLOSED 2026-05-16` with commit SHAs."
    - "`.planning/STATE.md` Current Position advances to Phase 19.3 with last_activity for 19.2 logged."
  artifacts:
    - path: "tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts"
      provides: "Vitest unit coverage of @cjm-4.1 step bindings (URL, multipart, headers, response shape, negative envelope)."
    - path: "tests/e2e-cjm/features/transcribe.feature"
      provides: "@cjm-4.1 scenario stripped of @expected-red @after-phase-19.2 tags."
    - path: "docs/customer-journeys.md"
      provides: "Updated @cjm-4.1 row (CLOSED 2026-05-16)."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 19.2 marked CLOSED with commit SHAs."
    - path: ".planning/STATE.md"
      provides: "Current Position advanced to Phase 19.3."
  key_links:
    - from: "tests/e2e-cjm/steps/transcribe.steps.ts"
      to: "https://api.localhost/api/transcribe"
      via: "multipart POST (field=file, content-type=audio/wav, filename=silent.wav, with session cookie + origin header)"
      pattern: "api/transcribe.*multipart"
    - from: "apps/api/src/routes/audio/transcribe.ts"
      to: "compose/mock-litellm POST /v1/audio/transcriptions"
      via: "LITELLM_BASE_URL proxy round-trip"
      pattern: "v1/audio/transcriptions"
---

<objective>
Flip `@cjm-4.1` (transcribe happy-path) from `@expected-red @after-phase-19.2` to GREEN against the hermetic mock-litellm path, with vitest unit coverage of the step bindings landed in the same atomic flow.

Purpose: Close the last open scenario in the v2.1 followup batch by proving the live compose `/api/transcribe → mock-litellm` round-trip end-to-end. The existing surface is believed-complete (per scout findings in CONTEXT.md); this plan is a TDD-shaped verification + tag-flip, not a build.
Output: Vitest unit file, feature-file tag flip, customer-journeys.md doc parity, ROADMAP + STATE close-out commits — all under ZERO `--no-verify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.2-stt-fixture-cjm-4-1-transcribe-happy-path/19.2-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e-cjm/features/transcribe.feature
@tests/e2e-cjm/steps/transcribe.steps.ts
@compose/mock-litellm/src/server.ts
@apps/api/src/routes/audio/transcribe.ts
@docs/customer-journeys.md
</context>

<phase_context>
- D-19.2-01: mock-litellm path only — NO smoke-paid; NO `@cjm-4.1-paid` scenario added.
- D-19.2-02: reuse `tests/e2e-cjm/fixtures/silent.wav`; no new audio fixtures.
- D-19.2-03: EXTEND step bindings via vitest unit coverage; do NOT rewrite `transcribe.steps.ts`.
- D-19.2-04: tag flip lands as final atomic commit AFTER L3 GREEN, ZERO `--no-verify`.
- D-19.2-05: `tools/lint-cjm-doc.ts --check-expected-red` MUST exit 0 after flip.
- D-19.2-06: smoke chain L1→L1b→L2→L3 in strict order; check container logs at each layer.
- D-19.2-07: production code (`apps/api/src/routes/audio/*`) is OFF-LIMITS unless live RED + SERVER-ERRORS.md ledger entry + explicit user approval.
</phase_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED→GREEN): Add vitest unit coverage for transcribe.steps.ts</name>
  <files>tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts</files>
  <behavior>
    Cases asserting the CONTRACT of the binding logic (HTTP boundary mocked via fetch stub or supertest-style harness — same pattern as `locale.steps.test.ts` from Phase 19b):
    - Test 1 (URL): When `When the user POSTs the silent WAV fixture to /api/transcribe` step is invoked with `apiBaseURL=https://api.localhost`, the fetch URL is exactly `https://api.localhost/api/transcribe` and method is `POST`.
    - Test 2 (multipart shape): The outgoing request body is multipart/form-data with exactly one field named `file`, MIME `audio/wav`, filename `silent.wav`, and binary body matching the fixture bytes.
    - Test 3 (headers): The request carries `origin: https://api.localhost` AND a `cookie` header populated from the prior sign-in step's session cookie.
    - Test 4 (positive response shape): When the mock fetch returns `status=200` and `body={text: "ok"}`, the step asserts pass (`status === 200 && typeof body.text === "string"`).
    - Test 5 (negative envelope per @cjm-4.2 parity): When junk-bytes + no-cookie request is mocked to return a typed error envelope, the assertion verifies `body.code` is a string AND no `stack` field is present AND no raw stack-trace substring leaks.
    Each test must run in ≤200ms; total file ≤1s. Use `vi.fn()` / `vi.spyOn(globalThis, 'fetch')` to intercept — no real network, no real compose.
  </behavior>
  <action>Create `tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` mirroring the pattern of `tests/e2e-cjm/steps/__tests__/locale.steps.test.ts` (Phase 19b precedent). Import the step-binding functions from `../transcribe.steps.ts`, stub the global `fetch` (and any cookie-jar helper), drive each case as an isolated `it()` block. Cases enumerated above. Commit immediately after GREEN-on-first-run.</action>
  <verify>
    <automated>pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts</automated>
  </verify>
  <done>Vitest run exits 0 with ≥5 passing cases. File committed.</done>
  <commit>test(19.2-01): vitest unit coverage for cjm-4.1 transcribe step bindings</commit>
</task>

<task type="auto">
  <name>Task 2 (SMOKE gate, no commit): L1→L1b→L2 smoke chain before full e2e</name>
  <files>(no edits — verification gate only)</files>
  <action>
    Run in strict sequence; on any failure STOP and diagnose container logs before retry (per `feedback_check_loki_after_tests.md`). NO commit produced by this task.
    - L1: `pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` → expect 100% pass (re-runs Task 1 file as a gate).
    - L1b: `pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` → expect exit 0 (proves `@expected-red` accounting is intact pre-flip; @cjm-4.1 still expected-red here).
    - L2: `docker compose -p smoke-19.2 -f docker-compose.yml -f compose/docker-compose.ingress.yml up -d traefik api worker mock-litellm postgres valkey mailpit` THEN `sleep 10` THEN `curl -kfsS https://api.localhost/api/health` (or equivalent health endpoint) returns 200. Then teardown: `docker compose -p smoke-19.2 down -v`.
    - If L2 fails: `docker compose -p smoke-19.2 logs traefik api worker mock-litellm | tail -200` FIRST, then diagnose.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts && pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red</automated>
  </verify>
  <done>All three smoke layers pass. Compose stack torn down clean (no leaked volumes — verify with `docker volume ls | grep smoke-19.2` is empty).</done>
</task>

<task type="auto">
  <name>Task 3 (L3 GREEN gate): Run full e2e scenario via make e2e-cjm SCENARIO="@cjm-4.1"</name>
  <files>(no edits unless test-side bug surfaces; in that case minimal edit + regression case)</files>
  <action>
    Run `make e2e-cjm SCENARIO="@cjm-4.1"`. Expect 1/1 GREEN.

    **If GREEN-on-first-run:** Proceed directly to Task 4. No commit from this task.

    **If RED:**
    1. FIRST run `docker compose logs api worker mock-litellm traefik | tail -300` (memory rule: Loki/docker logs before trace.zip).
    2. Classify failure:
       - **TEST-SIDE** (multipart field name wrong, content-type mismatch, cookie not propagated, URL host-split wrong): Make MINIMAL edit to `tests/e2e-cjm/steps/transcribe.steps.ts`. Add a regression `it()` case to `tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` reproducing the bug at the unit level FIRST (RED), then apply the fix (GREEN). Re-run L1 + L3. Commit: `fix(19.2-02): <minimal one-line description of the test-side defect>`.
       - **PRODUCTION-SIDE** (404 from `/api/transcribe`, 500 stack leak from route handler, mock-litellm model-alias rejection that requires `apps/api/src/routes/audio/transcribe.ts` change): STOP. This is a Hard-Rule INVERSION case. Write a new entry to `.planning/phases/08-client-server-audit/SERVER-ERRORS.md` (append-only ledger, Entry N+1) documenting: failing command + exit code, container logs excerpt, hypothesis, proposed minimal fix, blast radius. Output a `## HALT — PRODUCTION-SIDE DEFECT` block to the user with the ledger entry pointer; DO NOT edit production code. Plan expands to a second plan (`19.2-02-PLAN.md`) only after explicit user approval.
  </action>
  <verify>
    <automated>make e2e-cjm SCENARIO="@cjm-4.1"</automated>
  </verify>
  <done>`make e2e-cjm SCENARIO="@cjm-4.1"` exits 0 with 1/1 GREEN. If a test-side fix was needed, regression case in `__tests__/transcribe.steps.test.ts` is GREEN and committed in same atomic commit.</done>
</task>

<task type="auto">
  <name>Task 4 (GREEN tag flip): Remove @expected-red @after-phase-19.2 from @cjm-4.1 + update doc parity</name>
  <files>tests/e2e-cjm/features/transcribe.feature, docs/customer-journeys.md</files>
  <action>
    1. Edit `tests/e2e-cjm/features/transcribe.feature` line 6: change `@cjm-4.1 @expected-red @after-phase-19.2` → `@cjm-4.1`. Preserve indentation and trailing newline.
    2. Edit `docs/customer-journeys.md` @cjm-4.1 row: remove any `currently @expected-red` annotation; append/update `CLOSED 2026-05-16` marker per the `@cjm-4.2` row format that already exists.
    3. Re-run lint gate: `pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red` → expect exit 0.
    4. Re-run scenario: `make e2e-cjm SCENARIO="@cjm-4.1"` → still GREEN (defense in depth: prove flip didn't break anything).
    5. Single atomic commit (ZERO `--no-verify`).
  </action>
  <verify>
    <automated>pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red && make e2e-cjm SCENARIO="@cjm-4.1"</automated>
  </verify>
  <done>Feature file shows bare `@cjm-4.1` on line 6. customer-journeys.md @cjm-4.1 row is CLOSED 2026-05-16. Lint exit 0. e2e exit 0. Commit landed on HEAD.</done>
  <commit>feat(19.2-02): flip @cjm-4.1 transcribe happy-path to GREEN</commit>
</task>

<task type="auto">
  <name>Task 5 (DOCS close-out): Update ROADMAP + STATE</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-client-server-audit/SERVER-ERRORS.md (conditional)</files>
  <action>
    1. `.planning/ROADMAP.md`:
       - Change Phase 19.2 main-list checkbox from `- [ ]` to `- [x]`.
       - Update Phase 19.2 Details heading: append ` — CLOSED 2026-05-16` and list the closing commit SHAs from Task 1 + Task 4 (and Task 3 if a fix commit landed).
    2. `.planning/STATE.md`:
       - Update Current Position to next phase (Phase 19.3 per ROADMAP ordering — verify against ROADMAP before writing).
       - Log `last_activity` for Phase 19.2 with date 2026-05-16 and closing commit SHAs.
    3. `.planning/phases/08-client-server-audit/SERVER-ERRORS.md`: append Entry N+1 ONLY IF Task 3 surfaced a production-side issue that was approved + fixed. Otherwise SKIP this file (default expectation per D-19.2-07).
    4. Single atomic commit.
  </action>
  <verify>
    <automated>grep -q "19.2.*CLOSED 2026-05-16" .planning/ROADMAP.md && grep -q "19.3" .planning/STATE.md</automated>
  </verify>
  <done>ROADMAP shows 19.2 CLOSED with SHAs. STATE advances to 19.3. Commit landed. `git status --short` clean.</done>
  <commit>docs(19.2): close phase 19.2 in roadmap + state</commit>
</task>

</tasks>

<verification>
**Final aggregate verification (run all, all must exit 0):**

```bash
# L1 — unit coverage of step bindings
pnpm exec vitest run tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts

# L1b — cjm-doc lint with expected-red accounting
pnpm exec tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red

# L3 — full e2e scenario
make e2e-cjm SCENARIO="@cjm-4.1"

# Artifact sanity
grep -E '^\s*@cjm-4\.1\s*$' tests/e2e-cjm/features/transcribe.feature   # bare tag, no @expected-red
grep -q "19.2.*CLOSED 2026-05-16" .planning/ROADMAP.md
git log --oneline -5    # confirms 3-4 atomic commits, no --no-verify
git status --short      # working tree clean
```

**Smoke chain (already executed in Task 2 + Task 3, re-runnable for re-verification):**
L1 → L1b → L2 (compose up + health curl + teardown) → L3 (`make e2e-cjm SCENARIO="@cjm-4.1"`).
At every red layer: `docker compose logs <service> | tail -200` FIRST, then diagnose.
</verification>

<success_criteria>
- SR-19.2.1 ✅ — vitest unit file lands at `tests/e2e-cjm/steps/__tests__/transcribe.steps.test.ts` with ≥5 cases.
- SR-19.2.2 ✅ — `@cjm-4.1` scenario GREEN against compose+mock-litellm via `make e2e-cjm SCENARIO="@cjm-4.1"`.
- SR-19.2.3 ✅ — `@expected-red @after-phase-19.2` removed from `transcribe.feature:6`.
- SR-19.2.4 ✅ — `docs/customer-journeys.md` @cjm-4.1 row CLOSED 2026-05-16, lint exit 0.
- SR-19.2.5 ✅ — ROADMAP Phase 19.2 marked `- [x]` + CLOSED 2026-05-16 + commit SHAs; STATE advanced to Phase 19.3.
- SR-19.2.6 ✅ — ZERO `--no-verify` across all commits; Conventional Commits with `Co-Authored-By: Claude Opus 4.7` trailer.
</success_criteria>

<hard_constraints>
Inherited from CONTEXT.md (NON-NEGOTIABLE):
- ZERO `--no-verify` across all commits.
- Strict TDD: RED → GREEN paired atomic commits.
- English-only source artifacts.
- Conventional Commits (lowercase subject ≤100 char header) + `Co-Authored-By: Claude Opus 4.7` trailer.
- Smoke chain L1→L1b→L2→L3 before full e2e.
- Loki/docker logs FIRST when tests fail.
- Step bindings MUST have vitest unit coverage — waivers BANNED (`feedback_cjm_steps_need_unit_tests.md`).
- No mocks of internal logic — process-boundary only.
- NO production-side edits to `apps/api/src/routes/audio/*` without Hard-Rule INVERSION + SERVER-ERRORS.md ledger + explicit user approval (D-19.2-07).
- NO new audio fixtures; reuse `silent.wav` (D-19.2-02).
- NO `@cjm-4.1-paid` scenario; smoke-paid is OUT OF SCOPE (D-19.2-01).
</hard_constraints>

<deferred_items>
Carried forward (NOT in this phase):
- Smoke-paid `@cjm-4.1-paid` scenario for OpenAI/Groq STT → backlog (gate behind `OPENWHISPR_LOADTEST_ALLOW_PAID` per `feedback_loadtest_cost_discipline.md`).
- Audio-format coverage matrix (MP3/M4A/OGG) → Phase 5 acceptance, not v2.1 followup.
- Long-form audio (>5 min) chunking → Phase 5+ follow-up.
- `apps/api/src/routes/audio/transcribe.ts` coverage backfill → already CLOSED downstream by SR-19.2/19.3 per CONTEXT.md.
Pointer: see `.planning/deferred-items.md` and CONTEXT.md `## Deferred ideas`.
</deferred_items>

<output>
After completion, create `.planning/phases/19.2-stt-fixture-cjm-4-1-transcribe-happy-path/19.2-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Final commit SHAs (3-4 expected: test-coverage, optional fix, tag-flip, docs-closeout).
- Whether Task 3 hit any RED + classification (test-side fix vs HALT).
- Confirmation that ZERO `--no-verify` was used.
- Pointer to deferred items.
</output>
