---
phase: 00-repo-bootstrap-constitutional-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .nvmrc
  - .tool-versions
  - tsconfig.base.json
  - biome.json
  - lefthook.yml
  - commitlint.config.cjs
  - .gitignore
autonomous: true
requirements: [DEVEX-01]
requirements_addressed: [DEVEX-01]
must_haves:
  truths:
    - "A fresh clone runs `pnpm install` successfully"
    - "`pnpm exec biome check .` exits 0 on a clean tree"
    - "`pnpm exec tsc --noEmit` runs against the base config without errors"
    - "Lefthook hooks install on `pnpm install` (`prepare` script)"
    - "Conventional Commits enforced on commit-msg via commitlint"
  artifacts:
    - path: package.json
      provides: "Workspace root with packageManager pin, scripts, devDependencies"
      contains: '"packageManager": "pnpm@11.0.8"'
    - path: pnpm-workspace.yaml
      provides: "pnpm workspace declaration"
      contains: "apps/*"
    - path: tsconfig.base.json
      provides: "Strict TS base config (TS 6.x compatible)"
      contains: '"strict": true'
    - path: biome.json
      provides: "Biome 2.4.14 lint+format config with i18n locale ignore"
      contains: "biomejs.dev/schemas/2.4.14"
    - path: lefthook.yml
      provides: "Pre-commit (biome+lint:english) and commit-msg (commitlint) hooks"
      contains: "commitlint"
    - path: commitlint.config.cjs
      provides: "Conventional Commits + Cyrillic-ban custom rules"
      contains: "subject-no-cyrillic"
  key_links:
    - from: package.json#scripts.prepare
      to: lefthook
      via: "lefthook install runs on pnpm install"
      pattern: "lefthook install"
    - from: lefthook.yml#commit-msg.commitlint
      to: commitlint.config.cjs
      via: "pnpm exec commitlint --edit {1}"
      pattern: "commitlint --edit"
---

<objective>
Bootstrap the workspace foundation: pnpm 11 monorepo, Node 24 pin, TypeScript 6 strict, Biome 2.4.14, Lefthook 2.1.6 git hooks, and commitlint 21 with custom Cyrillic-ban rules. This plan establishes the "config layer" — every other plan depends on these files existing.

Purpose: Anchor the constitutional disciplines in committed config from PR #1 — no bolted-on retrofits.
Output: A repo that responds to `pnpm install` cleanly and refuses non-conventional / Cyrillic commits at the local level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-repo-bootstrap-constitutional-ci/00-CONTEXT.md
@.planning/phases/00-repo-bootstrap-constitutional-ci/00-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Workspace root scaffold (pnpm + Node + TS + .gitignore)</name>
  <files>
    package.json,
    pnpm-workspace.yaml,
    .nvmrc,
    .tool-versions,
    tsconfig.base.json,
    .gitignore
  </files>
  <read_first>
    - .planning/phases/00-repo-bootstrap-constitutional-ci/00-RESEARCH.md (sections: Standard Stack, Code Examples / package.json, Code Examples / pnpm-workspace.yaml, Code Examples / tsconfig.base.json)
    - .planning/phases/00-repo-bootstrap-constitutional-ci/00-CONTEXT.md (D-01..D-05, D-23)
    - CLAUDE.md (no Cyrillic in committed files; preserve existing structure)
    - existing /Users/dev/openwhispr-server/.gitignore (preserve, do not clobber)
  </read_first>
  <action>
Create `package.json` at repo root EXACTLY per RESEARCH.md §Code Examples (with all devDependencies from §Standard Stack pinned versions): pnpm@11.0.8, typescript@6.0.3, @types/node@25.6.2, @biomejs/biome@2.4.14, vitest@4.1.5, @vitest/coverage-v8@4.1.5, @playwright/test@1.59.1, @stryker-mutator/core@9.6.1, @stryker-mutator/vitest-runner@9.6.1, tsup@8.5.1, lefthook@2.1.6, @commitlint/cli@21.0.0, @commitlint/config-conventional@21.0.0, tsx (latest).

Required `package.json` fields:
- `"name": "openwhispr-server"`, `"private": true`, `"version": "0.0.0"`
- `"packageManager": "pnpm@11.0.8"`
- `"engines": { "node": "24.x", "pnpm": "11.x" }`
- `"scripts"`: prepare (lefthook install), lint, lint:fix, format, lint:english (tsx tools/lint-english.ts), lint:tdd (tsx tools/lint-tdd.ts), typecheck (pnpm -r exec tsc --noEmit), test (vitest run --coverage), test:watch (vitest), test:e2e (playwright test), test:contract (pnpm --filter ./packages/contract-tests test), test:mutation (stryker run), test:mutation:incremental (stryker run --incremental), build (pnpm -r build)

Create `pnpm-workspace.yaml` containing:
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

Create `.nvmrc` with single line: `24`
Create `.tool-versions` with single line: `nodejs 24`

Create `tsconfig.base.json` EXACTLY per RESEARCH §Code Examples — strict mode, NodeNext, ES2024, noUncheckedIndexedAccess, exactOptionalPropertyTypes, isolatedModules, verbatimModuleSyntax. This is the base every workspace `tsconfig.json` will extend.

Update `.gitignore` (file already exists, append if missing): node_modules, dist, coverage, reports, .stryker-tmp, .DS_Store, *.log, .env, .env.local, playwright-report, .turbo. Do NOT remove existing entries.

Run `corepack enable && corepack prepare pnpm@11.0.8 --activate && pnpm install`. Commit `pnpm-lock.yaml`.

Constraints:
- All identifiers, comments, scripts in English (DOCS-09)
- Use exact versions from RESEARCH §Standard Stack — these are verified live as of 2026-05-08
- Do NOT add devDependencies not in the verified list
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.base.json && test -f .nvmrc && test -f pnpm-lock.yaml && grep -q '"packageManager": "pnpm@11.0.8"' package.json && grep -q '"strict": true' tsconfig.base.json && pnpm install --frozen-lockfile && pnpm exec tsc --version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"packageManager": "pnpm@11.0.8"` exactly
    - `package.json` contains `"engines": { "node": "24.x", "pnpm": "11.x" }`
    - `package.json` `scripts` block contains keys: prepare, lint, lint:fix, format, lint:english, lint:tdd, typecheck, test, test:watch, test:e2e, test:contract, test:mutation, test:mutation:incremental, build
    - `package.json` `devDependencies` lists the 13 packages above with the EXACT versions from RESEARCH §Standard Stack
    - `pnpm-workspace.yaml` lists `apps/*` and `packages/*`
    - `.nvmrc` contains `24`
    - `tsconfig.base.json` contains `"strict": true` AND `"noUncheckedIndexedAccess": true` AND `"module": "NodeNext"`
    - `pnpm install --frozen-lockfile` exits 0
    - `pnpm-lock.yaml` is committed
    - No file in this task contains Cyrillic codepoints (U+0400–U+052F)
  </acceptance_criteria>
  <done>
    Workspace root exists; `pnpm install --frozen-lockfile` succeeds; lockfile committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Biome + Lefthook + commitlint config (lint/format/git-hook layer)</name>
  <files>
    biome.json,
    lefthook.yml,
    commitlint.config.cjs
  </files>
  <read_first>
    - Files created in Task 1 (package.json, tsconfig.base.json)
    - .planning/phases/00-repo-bootstrap-constitutional-ci/00-RESEARCH.md (sections: Code Examples / biome.json, Code Examples / lefthook.yml, Code Examples / commitlint.config.cjs, Pattern 3 English-only enforcement, Pattern 4 TDD enforcement, Pitfall 6 Lefthook auto-install, Pitfall 8 Biome version pin)
    - .planning/phases/00-repo-bootstrap-constitutional-ci/00-CONTEXT.md (D-06, D-18, D-19, D-27)
  </read_first>
  <action>
Create `biome.json` EXACTLY per RESEARCH §Code Examples / biome.json:
- `$schema` pinned to `https://biomejs.dev/schemas/2.4.14/schema.json` (matches devDependency)
- `vcs.useIgnoreFile: true`
- `files.ignore` MUST list: `node_modules/**`, `dist/**`, `coverage/**`, `.stryker-tmp/**`, `reports/**`, `**/*.gen.ts`, `packages/i18n/locales/**`
- `formatter`: enabled, indentStyle space, indentWidth 2, lineWidth 100
- `linter.rules.recommended: true`, plus `style.useImportType: error`, `style.useNodejsImportProtocol: error`, `suspicious.noConsoleLog: warn`
- `organizeImports.enabled: true`

Create `lefthook.yml` per RESEARCH §Code Examples / lefthook.yml:
```yaml
pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: pnpm exec biome check --apply {staged_files}
      stage_fixed: true
    english:
      run: pnpm exec tsx tools/lint-english.ts
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
```

Note: `tools/lint-english.ts` is created in Plan 03 — that's fine; pre-commit hook will fail until then, which is correct because Plan 03 is in Wave 1 (parallel) and ALL Wave 1 plans must be merged together to land Phase 0. The hook is dormant on this commit; activates after `pnpm install` runs `prepare → lefthook install` which only happens once Plan 03 also exists in the tree.

Create `commitlint.config.cjs` per RESEARCH §Code Examples / commitlint.config.cjs — extend `@commitlint/config-conventional`, add custom `subject-no-cyrillic` and `body-no-cyrillic` rules with regex `/[Ѐ-ԯ]/` (covers U+0400–U+052F per RESEARCH Pattern 3).

Run `pnpm exec biome check .` to confirm lint passes on the existing tree. Run `pnpm exec lefthook install` to verify hooks register.

Constraints:
- Use the EXACT regex `/[Ѐ-ԯ]/` for Cyrillic detection (RESEARCH Pattern 3 verified Unicode block range)
- Do NOT add `commitlint-plugin-no-cyrillic` as a dep — it does not exist on npm (RESEARCH §Pattern 3 verified)
- Implement the rule INLINE in `commitlint.config.cjs` `plugins[0].rules`
- All comments in English
  </action>
  <verify>
    <automated>test -f biome.json && test -f lefthook.yml && test -f commitlint.config.cjs && grep -q 'biomejs.dev/schemas/2.4.14' biome.json && grep -q 'packages/i18n/locales' biome.json && grep -q 'subject-no-cyrillic' commitlint.config.cjs && grep -q 'commitlint --edit' lefthook.yml && pnpm exec biome check . && pnpm exec lefthook version</automated>
  </verify>
  <acceptance_criteria>
    - `biome.json` `$schema` contains the literal string `2.4.14`
    - `biome.json` `files.ignore` array contains `packages/i18n/locales/**` AND `**/*.gen.ts` AND `.stryker-tmp/**`
    - `biome.json` `linter.rules.recommended` is `true`
    - `lefthook.yml` defines a `pre-commit` block with `biome` command running `pnpm exec biome check --apply {staged_files}` and an `english` command running `pnpm exec tsx tools/lint-english.ts`
    - `lefthook.yml` defines a `commit-msg` block with command `pnpm exec commitlint --edit {1}`
    - `commitlint.config.cjs` `extends` array contains `@commitlint/config-conventional`
    - `commitlint.config.cjs` defines a custom rule named `subject-no-cyrillic` with regex matching U+0400–U+052F
    - `commitlint.config.cjs` defines a custom rule named `body-no-cyrillic`
    - `commitlint.config.cjs` `rules` block sets both custom rules to severity `2` (error)
    - `pnpm exec biome check .` exits 0 on the post-task tree
    - No Cyrillic characters in any of biome.json, lefthook.yml, commitlint.config.cjs (the regex `/[Ѐ-ԯ]/` is the codepoint range as a JS regex literal — those bracket characters ARE Cyrillic; this is the documented exception in RESEARCH §Pattern 3 — note the regex must use the Unicode escape syntax `/[Ѐ-ӿԀ-ԯ]/u` to keep the source file ASCII-only)
    - The Cyrillic regex in `commitlint.config.cjs` uses Unicode escapes (`Ѐ-ӿԀ-ԯ`) NOT literal Cyrillic characters
  </acceptance_criteria>
  <done>
    Biome 2.4.14 config validated; Lefthook hooks installable; commitlint enforces Conventional Commits + ASCII-source-only commit messages.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `pnpm install --frozen-lockfile` exits 0
- `pnpm exec biome check .` exits 0
- `pnpm exec lefthook install` exits 0
- `pnpm exec commitlint --version` exits 0
- No file in this plan's `files_modified` contains literal Cyrillic codepoints (commitlint.config.cjs uses `\u` escapes only)
</verification>

<success_criteria>
1. Workspace root files exist with verified versions from RESEARCH.md.
2. Lefthook + commitlint enforce Conventional Commits and Cyrillic-free messages locally.
3. Biome lints the entire repo cleanly.
4. The "config layer" is in place for Plans 02–06 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/00-repo-bootstrap-constitutional-ci/00-01-SUMMARY.md`.
</output>
