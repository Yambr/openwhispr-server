---
phase: 31-constitutional-lockers
plan: 07
type: execute
wave: 3
depends_on: [31-01, 31-02, 31-03, 31-04, 31-05, 31-06]
files_modified:
  - .planning/DISCIPLINE.md
  - CLAUDE.md
  - lefthook.yml
  - .github/workflows/ci.yml
  - .github/workflows/nightly.yml
  - Makefile
  - package.json
  - tests/e2e/lockers.spec.ts
  - tools/lockers-allowlist-diff.ts
  - tools/lockers-allowlist-diff.test.ts
autonomous: true
requirements: [LOCKER-07, LOCKER-08, LOCKER-09]
mode: SAME-COMMIT-DISCIPLINE-AND-LOCKERS
must_haves:
  truths:
    - "DISCIPLINE.md Rules 11–14 exist with concrete locker-tool cross-references (LOCKER-01..06 cited by file path)."
    - "CLAUDE.md § Engineering discipline mirrors DISCIPLINE.md Rules 11–14 verbatim — the two files agree on text + ordering."
    - "lefthook.yml has a `lockers` pre-commit block invoking `make lint:lockers`."
    - "`.github/workflows/ci.yml` has a `lockers` job + an `lockers-allowlist-diff` step that fails on net allowlist additions without `Allowlist-grow-approved: issue-NNNN` in commit body (or PR body)."
    - "`.github/workflows/nightly.yml` runs `make lint:lockers` without `--warn-only` flags (nightly is the early-warning channel for the WARN-only-on-land lockers)."
    - "`Makefile` has a `lint:lockers` target invoking `pnpm lint:lockers`."
    - "`package.json` has `lint:lockers` aggregate script chaining all six per-locker scripts."
    - "`tests/e2e/lockers.spec.ts` exists with 6 cases — one per locker — invoking the real binary via `child_process.execFileSync` against a temp fixture and asserting exit code + stderr regex (DISCIPLINE Rules 3 + 4: real binary, no mocks)."
    - "`tools/lockers-allowlist-diff.ts` exists + tested at ≥ 90/90/90/90, runnable via `pnpm lint:lockers-allowlist-diff`."
    - "Single atomic commit: `docs(31-07): DISCIPLINE Rules 11–14 + locker integration (LOCKER-07/08/09)` — DISCIPLINE.md + CLAUDE.md + ALL wiring + e2e + allowlist-diff tool ship together. Verifier rejects split commits per LOCKER-07."
  artifacts:
    - { path: .planning/DISCIPLINE.md, provides: "Rules 11–14 prose appended after Rule 10; 'Retroactive enforcement' section preserved; v2.2-effective note added" }
    - { path: CLAUDE.md, provides: "§ Engineering discipline updated to mirror Rules 11–14" }
    - { path: lefthook.yml, provides: "`lockers` pre-commit block with `run: make lint:lockers`; `glob:` filters by `{apps,packages}/*/src/**/*.{ts,tsx}`" }
    - { path: .github/workflows/ci.yml, provides: "`lockers` job (or stacked steps in `lint-english` job) + `lockers-allowlist-diff` step gating PRs" }
    - { path: .github/workflows/nightly.yml, provides: "Nightly `lockers` job WITHOUT `--warn-only` flags so 31-04/05/06 surface findings nightly even when pre-commit is WARN-only" }
    - { path: Makefile, provides: "`lint:lockers` PHONY target invoking `pnpm lint:lockers`" }
    - { path: package.json, provides: "`lint:lockers` chained-AND of six per-locker scripts + `lint:lockers-allowlist-diff` script + `test:lockers-allowlist-diff` coverage gate" }
    - { path: tests/e2e/lockers.spec.ts, provides: "6 vitest cases (CONTEXT permits .spec.ts — using vitest, NOT Playwright, per 31-RESEARCH Q1) invoking each locker binary via execFileSync against a temp dir" }
    - { path: tools/lockers-allowlist-diff.ts, provides: "CI helper: base-vs-HEAD allowlist diff; refuses net additions lacking `Allowlist-grow-approved: issue-NNNN`" }
  key_links:
    - { from: lefthook.yml, to: Makefile, via: "run: make lint:lockers" }
    - { from: ".github/workflows/ci.yml", to: Makefile, via: "run: make lint:lockers + run: pnpm lint:lockers-allowlist-diff" }
    - { from: Makefile, to: package.json, via: "lint:lockers: pnpm lint:lockers" }
    - { from: package.json, to: "tools/lint-*.ts", via: "lint:lockers aggregate script" }
    - { from: ".planning/DISCIPLINE.md", to: "CLAUDE.md", via: "verbatim Rules 11–14 mirror — same words, same ordering" }
---

# Phase 31 / Plan 07 — Integration: DISCIPLINE + CLAUDE + lefthook + CI + Makefile + E2E + allowlist-diff (LOCKER-07/08/09)

**Closes:** LOCKER-07, LOCKER-08, LOCKER-09.
**Mode:** **SINGLE atomic commit** per LOCKER-07: DISCIPLINE.md edit + CLAUDE.md mirror + all wiring + e2e + allowlist-diff tool ship together. Verifier rejects split commits.

## Goal

Wire the six lockers from 31-01..06 into the developer + CI flow as a single coherent gate. DISCIPLINE.md gains Rules 11–14 + CLAUDE.md mirrors them. lefthook + ci.yml + nightly.yml + Makefile + package.json invoke the lockers consistently via `make lint:lockers`. E2E suite at `tests/e2e/lockers.spec.ts` proves the binaries refuse violations end-to-end. LOCKER-09's net-addition guard ships as `tools/lockers-allowlist-diff.ts` + CI step.

## Depends On

- 31-01, 31-02, 31-03, 31-04, 31-05, 31-06 — all six lockers + their `package.json` scripts must exist before this plan can chain them.

## Inherits

- DISCIPLINE Rules 1–10 (this plan amends with 11–14).

## Tasks

### Task 1 — RED: E2E spec scaffold + allowlist-diff test

- **Files:** `tests/e2e/lockers.spec.ts`, `tools/lockers-allowlist-diff.test.ts`.
- **Before:** `pnpm test:e2e` fails — spec file missing or cases reference unimplemented `lockers-allowlist-diff` module.
- **Action:**
  1. Write `tests/e2e/lockers.spec.ts` with 6 vitest `it()` cases — one per locker — using `child_process.execFileSync` (zero new deps per 31-RESEARCH A3) to spawn `pnpm exec tsx tools/lint-<name>.ts <tempDir>` against a `mkdtempSync`-created fixture directory containing a single known violation. Each case asserts: non-zero exit code via `expect(() => execFileSync(...)).toThrow(/<expected-stderr-regex>/)`. Cases:
     - LOCKER-01 — temp `src/handler.ts` with `if (process.env.NODE_ENV === 'production') {}`; expect `NODE_ENV-read`.
     - LOCKER-02 — temp file with `const x = foo as any;`; expect `as-any`.
     - LOCKER-03 — temp file with `const URL = "http://localhost:3000";`; expect `localhost-string`.
     - LOCKER-04 — temp file with `app.route({ method: "POST", url: "/api/x", handler: () => {} });`; expect `LOCKER-04-NO-SCHEMA`. Run WITHOUT `--warn-only` so the test asserts BLOCKING behaviour even though production flag is WARN.
     - LOCKER-05 — temp file with leaking Error class; expect `LOCKER-05-LEAK`.
     - LOCKER-06 — temp file with `spawn("bash", ["-c", \`...${DATABASE_URL}...\`]);`; expect `shell-credential-interpolation`.
  2. Spec also includes 2 documentation-grep cases: `expect(readFileSync('.planning/DISCIPLINE.md', 'utf8')).toContain('Rule 11')` through `Rule 14`; same for `CLAUDE.md`. Closes LOCKER-07 from the test side.
  3. `tools/lockers-allowlist-diff.test.ts`: covers cases (a) clean diff → exit 0, (b) net addition WITHOUT `Allowlist-grow-approved:` → exit 1, (c) net addition WITH `Allowlist-grow-approved: issue-1234` in COMMIT_MESSAGE env or PR body → exit 0, (d) net removal → exit 0 (removals always allowed).
- **Atomic commit:** part of the single 31-07 commit (see "Atomic Commit Boundaries").

### Task 2 — GREEN: DISCIPLINE Rules 11–14 + CLAUDE.md mirror + all wiring + allowlist-diff tool

- **Files:** all files listed in `files_modified`.
- **Action:**
  1. **`.planning/DISCIPLINE.md`** — append after Rule 10, before "Retroactive enforcement":
     ```markdown
     11. **No NODE_ENV branches in runtime paths.** `process.env.NODE_ENV` and `NODE_ENV` comparisons MAY appear ONLY in `bootstrap.ts`, `config/*.ts`, `otel-bootstrap.ts`, `*.config.ts`. Anywhere else in `apps/**/src/**` + `packages/**/src/**` is REFUSED. Enforced by `tools/lint-no-env-branches.ts` (LOCKER-01).

     12. **No type-suppression.** `as any`, `as unknown as`, `@ts-ignore`, `@ts-nocheck` are REFUSED in production code. `@ts-expect-error` is permitted ONLY in the format `@ts-expect-error issue-NNNN: <reason>`. Allowlist exists for pre-existing debt; net additions REFUSE the PR. Enforced by `tools/lint-no-suppressions.ts` (LOCKER-02). The related defence-in-depth invariant — Error subclasses MUST truncate `bodyText|responseBody|upstreamPayload|response|body` string fields at construction — is enforced by `tools/lint-secret-shape-in-error.ts` (LOCKER-05).

     13. **No hardcoded localhost / UUID / test-token shapes.** `localhost`, `127.0.0.1`, ports `:3000|:4000|:8080`, UUID literals, and secret-shape literals (`sk-…`, `sk-ant-…`, `AIza…`, `AKIA…`, `Bearer ey…`) are REFUSED outside `tests/`, `.env.*.example`, `compose/`, `docs/`, `charts/`, `tools/`. The canonical `DEFAULT_TENANT_ID = '00000000-0000-0000-0000-000000000000'` is permanently allowlisted. Enforced by `tools/lint-no-hardcode.ts` (LOCKER-03).

     14. **Production-readiness invariants.** Every Fastify route declaration (`app.route|get|post|put|patch|delete`) MUST carry `schema: { body|querystring|params: <ZodSchema> }` AND `config: { rateLimit: ... }`. `rateLimit: false` is permitted ONLY for `/api/health|/api/ready|/api/healthz|/api/_test/*` URLs. Every exported symbol MUST have at least one non-test importer. The related defence-in-depth invariant — `child_process.spawn('bash', ['-c', ...])` / `execSync` / `exec` / `execFileSync` MUST NOT interpolate template-literal credentials referencing `*_URL|*_KEY|*_PASSWORD|*_SECRET|*_TOKEN` bindings — is enforced by `tools/lint-shell-credential-interpolation.ts` (LOCKER-06). Use argv-array form `spawn(cmd, [arg, ...], { shell: false })` instead. Enforced by `tools/lint-prod-readiness.ts` (LOCKER-04) + LOCKER-06.

     **Locker WARN→BLOCKING ledger.** LOCKER-04 ships WARN-only-on-land; flips to BLOCKING in the closing commit of phase 31-08 (bulk-fix). LOCKER-05 ships WARN-only-on-land; flips in the closing commit of Phase 37 (CRIT-FIX-09). LOCKER-06 ships WARN-only-on-land; flips in the closing commit of Phase 36.a (CRIT-FIX-05). LOCKER-01/02/03 ship BLOCKING from day one.
     ```
  2. **`CLAUDE.md`** — Update § "Engineering discipline (constitutional, NON-NEGOTIABLE)" to mirror DISCIPLINE.md Rules 11–14 verbatim. Same ordering. Same wording. (Per LOCKER-07: the two files MUST agree.)
  3. **`lefthook.yml`** — append inside `pre-commit.commands` (after `dockerfile-tls`):
     ```yaml
     # Phase 31 / Plan 07 / LOCKER-08 — fail-fast on any locker violation.
     # The lockers fan out under `make lint:lockers` so lefthook + ci.yml +
     # nightly.yml + Makefile share one source of truth (Pitfall 5).
     lockers:
       glob: "{apps,packages}/*/src/**/*.{ts,tsx}"
       run: make lint:lockers
     ```
  4. **`Makefile`** — add target:
     ```makefile
     lint\:lockers:
     	pnpm lint:lockers
     ```
     (escape `:` in target name per Make rules; mirror `lint:` precedent at line 21.)
     Also add to `.PHONY` list at line 5.
  5. **`package.json`** — add aggregate + diff scripts (after `lint:dockerfile-tls`):
     ```json
     "lint:lockers": "pnpm lint:no-env-branches && pnpm lint:no-suppressions && pnpm lint:no-hardcode && pnpm lint:prod-readiness && pnpm lint:secret-shape-in-error && pnpm lint:shell-credential-interpolation",
     "lint:lockers-allowlist-diff": "tsx tools/lockers-allowlist-diff.ts",
     "test:lockers-allowlist-diff": "vitest run tools/lockers-allowlist-diff.test.ts --coverage --coverage.include=tools/lockers-allowlist-diff.ts --coverage.exclude= --coverage.all=false --coverage.thresholds.lines=90 --coverage.thresholds.branches=90 --coverage.thresholds.functions=90 --coverage.thresholds.statements=90"
     ```
  6. **`.github/workflows/ci.yml`** — extend `lint-english` job (after the `dockerfile-tls` line at :41) with two new run steps:
     ```yaml
     - run: make lint:lockers
     - run: pnpm lint:lockers-allowlist-diff
     ```
     Rationale: stacking matches the existing pattern (`ci.yml:38-41`); separate job is OK too but introduces redundant `actions/checkout` + `pnpm install` cost.
  7. **`.github/workflows/nightly.yml`** — add a `lockers-nightly` job that runs `pnpm lint:no-env-branches`, `pnpm lint:no-suppressions`, `pnpm lint:no-hardcode`, `pnpm exec tsx tools/lint-prod-readiness.ts` (no `--warn-only`), `pnpm exec tsx tools/lint-secret-shape-in-error.ts` (no `--warn-only`), `pnpm exec tsx tools/lint-shell-credential-interpolation.ts` (no `--warn-only`). Failure posts a `gh issue create --title "Nightly locker fail: <date>"` step (or surfaces via Slack — defer Slack to ops). Nightly is the early-warning channel for WARN-only lockers.
  8. **`tools/lockers-allowlist-diff.ts`** — implement:
     - Read each of the six allowlist files at `git rev-parse HEAD~1` (base — actually `origin/main` in CI; resolve via `GITHUB_BASE_REF` env when set, else `HEAD~1`).
     - Read each at HEAD (working tree).
     - Compute net additions per file.
     - If any net additions exist AND neither `process.env.COMMIT_MESSAGE` nor `process.env.PR_BODY` contains `Allowlist-grow-approved: issue-\d+`, exit 1.
     - Otherwise exit 0.
  9. **`tests/e2e/lockers.spec.ts`** — implement the 6 cases drafted in Task 1, plus the doc-grep cases.
- **Atomic commit:** `docs(31-07): DISCIPLINE Rules 11–14 + locker integration (LOCKER-07/08/09)` — ALL files above land in ONE commit. Verifier rejects split commits per LOCKER-07.
- **Threat-model implications:** Closes V14 (Configuration), reinforces V5 (Input Validation) and V7 (Error Handling/Logging) via mechanical enforcement. The single-commit invariant prevents discipline-doc drift.

### Task 3 — REFACTOR (optional)

If a shared `tools/lib/allowlist.ts` clearly emerges from 31-01..06, extract it here. Coverage requirement: ≥ 90/90/90/90 on the new shared util; rerun all six per-locker tests to confirm none regress.

## Atomic Commit Boundaries

**Single atomic commit** for Task 1+2 combined per LOCKER-07. Task 3 is a separate refactor commit if and only if extraction is performed.

Per DISCIPLINE Rule 1 + LOCKER-07: tests + production wiring + DISCIPLINE prose + CLAUDE mirror must travel together. Splitting would cause the verifier to reject the phase.

## Verification Gate

- `pnpm test:lockers-allowlist-diff` ≥ 90/90/90/90.
- `pnpm exec vitest run tests/e2e/lockers.spec.ts` — all 8 cases (6 locker e2e + 2 doc-grep) GREEN.
- `make lint:lockers` exit 0 on a clean tree.
- `make lint:lockers` exit 1 on a temp working-tree that contains a deliberately-broken fixture (manually verified by executor; documented in `31-07-DECISIONS.md`).
- `diff <(grep -A 50 'Rule 11' .planning/DISCIPLINE.md) <(grep -A 50 'Rule 11' CLAUDE.md)` — output limited to trailing context lines outside Rules 11–14 block. Rule prose itself MUST match verbatim.
- `git log -1 --name-only` on the 31-07 commit lists ALL files in `files_modified` and ONLY those — verifier confirms single-commit atomicity.

## Risks + Mitigations

| Risk | From research | Mitigation |
|---|---|---|
| Single-commit atomicity vs RED→GREEN cadence tension | Q12 | The RED→GREEN cadence is preserved within 31-01..06 individually; 31-07's role is WIRING which IS one logical unit. The test scaffold (Task 1) lives in the same commit as the wiring (Task 2) because the test verifies the wiring exists. |
| lefthook + CI drift | Pitfall 5 | `make lint:lockers` is the single source of truth invoked by both. |
| Allowlist-tampering inside same PR | Pitfall 6 | `tools/lockers-allowlist-diff.ts` diffs base-vs-HEAD; CI step refuses unless approved token in commit/PR body. |
| Glob performance with biome already running pre-commit | Pitfall 3 | lefthook `parallel: true` is set; `glob:` filter limits to staged-files; full-tree scan deferred to CI. |
| `tests/e2e/lockers.spec.ts` is `.spec.ts` but uses vitest, not Playwright | Open Question 1 | Document the framework choice in the spec's top-of-file comment. CONTEXT.md says `.spec.ts`; framework is planner's call; vitest avoids Playwright bootstrap cost for pure-CLI cases. If the executor finds vitest doesn't pick up `tests/e2e/*.spec.ts` by default, rename to `tests/e2e/lockers.test.ts` (RESEARCH §Q13 explicitly notes either works). |
| CLAUDE.md mirror drift over time | LOCKER-07 verifier rule | Add a future-phase TODO (NOT this plan) for a `lint-discipline-mirror.ts` that grep-asserts DISCIPLINE Rule 11–14 prose appears in CLAUDE.md. Out of scope here; record in `31-07-DECISIONS.md`. |

## Out of Scope

- Adding a `lint-discipline-mirror.ts` (future phase).
- Touching the existing Rules 1–10 prose.
- Modifying lefthook's `parallel: true` setting.
- Touching the `commit-msg` hook.
- Slack notification for nightly failures (ops decision).
- Any production-code fix (fixes belong to 31-08 + Phases 32–41).
- Renaming `tests/e2e/lockers.spec.ts` to `.test.ts` (only IF the executor confirms vitest picks up `.spec.ts` in this repo's config; otherwise renamed and noted in DECISIONS.md).
