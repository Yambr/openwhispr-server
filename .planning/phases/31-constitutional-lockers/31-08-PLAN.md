---
phase: 31-constitutional-lockers
plan: 08
type: execute
wave: 4
depends_on: [31-07]
files_modified:
  - "apps/api/src/routes/** (bulk-fix LOCKER-04 violations — multiple atomic commits ≤ 50 files each)"
  - "apps/api/src/lib/** (bulk-fix MEDIUM hardcode / suppression debt)"
  - "apps/api/src/plugins/**"
  - "apps/api/src/middleware/**"
  - "apps/worker/src/** (LOCKER-04 + LOCKER-02 db/app-pool suppressions IF not blocked by Phase 32 RLS scope)"
  - "apps/web/src/** (LOCKER-03 port-literal MEDIUM debt)"
  - "packages/**/src/** (residual LOCKER-02/03/04 MEDIUM/LOW)"
  - tools/lint-prod-readiness.allowlist.txt
  - package.json
  - lefthook.yml
  - .github/workflows/ci.yml
  - .github/workflows/nightly.yml
  - Makefile
  - .planning/phases/31-constitutional-lockers/31-08-DEFERRED.md
autonomous: true
requirements: [LOCKER-09]
mode: MULTI-COMMIT-PER-AREA-CAP-50-FILES
must_haves:
  truths:
    - "Each area's MEDIUM/LOW violations are fixed in a SEPARATE atomic commit; each commit's diff is ≤ 50 files."
    - "Each commit's fix lands with its TDD test(s) — production code edit + tests in the SAME commit per DISCIPLINE Rule 1."
    - "CRITICAL/HIGH violations belonging to Phases 32–41 are NOT touched here — they are logged with their owning-phase number in `31-08-DEFERRED.md`."
    - "Final commit of 31-08 flips LOCKER-04 from `--warn-only` to BLOCKING: drops `--warn-only` from `package.json` `lint:prod-readiness`, regenerates `tools/lint-prod-readiness.allowlist.txt` to empty-modulo-permanent (only legitimate `/api/health` shape entries remain), updates DISCIPLINE Rule 14 prose adding `BLOCKING since <sha>` note, refreshes lefthook + ci.yml + nightly.yml + Makefile."
    - "`pnpm lint:lockers` exits 0 (BLOCKING) on the final 31-08 tree."
    - "Each commit passes ≥ 90/90/90/90 coverage on diff per DISCIPLINE Rule 2 + 7."
  artifacts:
    - { path: ".planning/phases/31-constitutional-lockers/31-08-DEFERRED.md", provides: "Per-violation ledger of items SKIPPED by 31-08 and their owning Phase 32–41 number" }
    - { path: "tools/lint-prod-readiness.allowlist.txt", provides: "Cleared to empty-modulo-permanent on final commit; pre-commit/CI invocations drop `--warn-only` in the SAME final commit" }
  key_links:
    - { from: "tools/lint-prod-readiness.allowlist.txt", to: "package.json + lefthook.yml + ci.yml + nightly.yml + Makefile", via: "WARN→BLOCKING flip happens atomically — same commit clears allowlist + drops `--warn-only` everywhere" }
---

# Phase 31 / Plan 08 — Bulk-fix MEDIUM/LOW + LOCKER-04 BLOCKING flip

**Closes:** LOCKER-09 (verified-non-empty operational state); also operationally closes the WARN-only carry from LOCKER-04 by flipping it to BLOCKING in the final commit.

## Goal

Bulk-fix pre-existing MEDIUM/LOW violations across the codebase one area at a time, with ≤ 50 files per atomic commit and tests + production edits in the SAME commit. Pre-existing CRITICAL/HIGH violations belonging to Phases 32–41 are NOT touched here; they are logged in `31-08-DEFERRED.md` with their owning phase number. Final commit flips LOCKER-04 to BLOCKING.

## Depends On

- 31-07 (DISCIPLINE.md + CLAUDE.md + lefthook + ci.yml + nightly.yml + Makefile + e2e all wired and committed).

## Inherits

- DISCIPLINE Rules 1–14 (Rules 11–14 in effect after 31-07 lands).

## Pre-Flight: Triage Pass (Task 0)

- **File:** `.planning/phases/31-constitutional-lockers/31-08-DEFERRED.md`.
- **Action:** Run `pnpm lint:no-env-branches`, `pnpm lint:no-suppressions`, `pnpm lint:no-hardcode`, `pnpm exec tsx tools/lint-prod-readiness.ts` (no `--warn-only`), `pnpm exec tsx tools/lint-secret-shape-in-error.ts` (no `--warn-only`), `pnpm exec tsx tools/lint-shell-credential-interpolation.ts` (no `--warn-only`). Capture the full finding set. For each finding, classify:
  - **MEDIUM/LOW + in-scope here** → schedule into one of Tasks 1–7 by area.
  - **CRITICAL/HIGH + owned by Phases 32–41** → log to `31-08-DEFERRED.md` with `<file>:<line>  LOCKER-XX  → Phase <N>` row.
  - **Phase 32 area (`apps/api/src/index.ts`, `apps/worker/src/db/app-pool.ts` — RLS-touching)** → log; do NOT fix in 31-08 (avoids merge conflicts with Phase 32).
  - **Phase 36.a area (`apps/worker/src/jobs/audit-archive.ts:106-127`)** → log to deferred; LOCKER-06 flip is Phase 36.a's responsibility.
  - **Phase 37 area (`packages/litellm-client/src/errors.ts:31`)** → log to deferred; LOCKER-05 flip is Phase 37's responsibility.
  - **Phase 38 area (dead `@openwhispr/auth` exports)** → log to deferred; LOCKER-04 dead-export entries here clear when Phase 38 retires the package.
  - **Phase 41 area** (most apps/web port hardcodes, several LOCKER-04 routes per ROADMAP 1220-1225) → log to deferred.
- **Atomic commit:** `docs(31-08): triage MEDIUM/LOW vs CRITICAL/HIGH deferred to Phases 32–41`.

## Tasks (one atomic commit per task, ≤ 50 files each)

### Task 1 — `apps/api/src/routes/**` LOCKER-04 fixes (subset NOT owned by Phase 41.b)

- **Before (RED):** `pnpm exec tsx tools/lint-prod-readiness.ts` (without `--warn-only`) reports N route-shape findings under `apps/api/src/routes/**`. Existing tests for the touched routes do NOT yet assert zod-schema-rejection of malformed input.
- **Action (GREEN):** For each MEDIUM/LOW route in scope: add `schema: { body: <zod> }` / `schema: { querystring: <zod> }` / `schema: { params: <zod> }` importing from `@openwhispr/wire-schemas` or `@openwhispr/contract-tests/schemas`; add `config: { rateLimit: { max: <reasonable>, timeWindow: "1 minute" } }`. For each touched route, the SAME commit adds/extends a unit test asserting `request({ body: <malformed> }) → 400` (zod rejection). Coverage ≥ 90/90/90/90 on the diff.
- **Cap:** ≤ 50 files per commit. If the touched-routes set exceeds 50, split into Task 1a / 1b / 1c.
- **Atomic commit(s):** `fix(31-08): apps/api/src/routes — zod + rateLimit on MEDIUM routes (LOCKER-04 bulk-fix)`.
- **Verify per commit:** `pnpm --filter @openwhispr/api test` GREEN; `pnpm exec tsx tools/lint-prod-readiness.ts <touched-files>` shows N-X findings (X = files closed in this commit); coverage delta ≥ 90/90/90/90 on diff.

### Task 2 — `apps/api/src/lib/**` MEDIUM debt closure

- **Before:** Hardcode/suppression debt in `apps/api/src/lib/ssrf-dispatcher.ts`, `apps/api/src/lib/default-tenant.ts` (UUID-zero already permanent-allowlisted; nothing to fix there), and others surfaced by triage.
- **Action:** For each MEDIUM finding, refactor source to remove the violation AND remove the allowlist entry in the SAME commit. Tests covering the new (non-suppressing / non-hardcoded) shape land in the SAME commit per DISCIPLINE Rule 1.
- **Cap:** ≤ 50 files.
- **Atomic commit:** `fix(31-08): apps/api/src/lib — MEDIUM hardcode/suppression closure`.

### Task 3 — `apps/api/src/plugins/**` + `apps/api/src/middleware/**` MEDIUM debt closure

- **Action:** Same shape as Task 2 for these subtrees.
- **Cap:** ≤ 50 files.
- **Atomic commit:** `fix(31-08): apps/api/src/{plugins,middleware} — MEDIUM debt closure`.

### Task 4 — `apps/worker/src/**` MEDIUM debt closure (EXCLUDING `db/app-pool.ts` and `jobs/audit-archive.ts`)

- **Before:** Triage identifies which `apps/worker/src/**` files have MEDIUM debt NOT owned by Phase 32 or 36.a.
- **Action:** Close MEDIUM debt on the eligible subset. `db/app-pool.ts` and `jobs/audit-archive.ts` stay deferred (Phase 32 / Phase 36.a respectively).
- **Cap:** ≤ 50 files.
- **Atomic commit:** `fix(31-08): apps/worker/src — MEDIUM debt closure (Phase 32/36 carveouts deferred)`.

### Task 5 — `apps/web/src/**` MEDIUM port-literal closure (EXCLUDING Phase 41.c page.tsx files)

- **Action:** Close `apps/web/src/lib/auth-actions.ts:22` and `apps/web/src/lib/auth-server.ts:47` port hardcodes (route through env). The 6 `(auth)/app/**/page.tsx` port hardcodes belong to Phase 41.c (per ROADMAP :1221) — leave deferred.
- **Cap:** ≤ 50 files.
- **Atomic commit:** `fix(31-08): apps/web/src/lib — port-literal closure (page.tsx files deferred to 41.c)`.

### Task 6 — `packages/**/src/**` MEDIUM debt closure

- **Action:** Close MEDIUM debt in `packages/byok-guard/src/`, `packages/email/src/`, `packages/litellm-client/src/config.ts:29` (port literal), etc. EXCLUDING `packages/litellm-client/src/errors.ts:31` (Phase 37) and `packages/auth/**` (Phase 38).
- **Cap:** ≤ 50 files.
- **Atomic commit:** `fix(31-08): packages/**/src — MEDIUM debt closure (Phase 37/38 carveouts deferred)`.

### Task 7 — LOCKER-04 BLOCKING flip (FINAL commit)

- **Before:** Tasks 1–6 reduce `tools/lint-prod-readiness.allowlist.txt` to "permanent-only" entries (legitimate `/api/health|/api/ready|/api/_test/*` shapes if any are still flagged) + Phase-41-deferred entries (each tagged `# issue-31-debt-LOCKER-04-route-bulkfix-phase-41`).
- **Action:**
  1. **Decision point:** If the residual allowlist contains ANY entries NOT owned by Phase 41 / Phase 38 (i.e., entries that should have been closed by 31-08 but weren't), spawn `gsd-advisor-researcher` for the disposition. Record verdict in `31-08-DECISIONS.md`. The user is offline.
  2. Drop `--warn-only` from `package.json` `lint:prod-readiness` script: from `"tsx tools/lint-prod-readiness.ts --warn-only"` to `"tsx tools/lint-prod-readiness.ts"`.
  3. Update lefthook.yml + ci.yml + nightly.yml + Makefile to drop `--warn-only` (Makefile already invokes via `pnpm`; only the script form changes).
  4. Update `.planning/DISCIPLINE.md` Rule 14 prose to add a closing line: `LOCKER-04 flipped from WARN-only to BLOCKING on <SHA> via Phase 31-08 final commit.` Mirror to `CLAUDE.md`.
  5. Run `pnpm lint:lockers` → MUST exit 0.
  6. Run full E2E `tests/e2e/lockers.spec.ts` → all cases GREEN.
- **Atomic commit:** `feat(31-08): flip LOCKER-04 to BLOCKING — Phase 31 closure`.

## Atomic Commit Boundaries

| # | Task | Commit message | File-cap | Notes |
|---|---|---|---|---|
| 0 | Triage | `docs(31-08): triage ...` | n/a | docs-only — creates `31-08-DEFERRED.md` |
| 1 | apps/api/src/routes | `fix(31-08): apps/api/src/routes ...` | ≤ 50 | split into 1a/1b/1c if needed |
| 2 | apps/api/src/lib | `fix(31-08): apps/api/src/lib ...` | ≤ 50 | |
| 3 | apps/api/src/{plugins,middleware} | `fix(31-08): ...` | ≤ 50 | |
| 4 | apps/worker/src | `fix(31-08): apps/worker/src ...` | ≤ 50 | excludes 32/36.a carveouts |
| 5 | apps/web/src/lib | `fix(31-08): apps/web/src/lib ...` | ≤ 50 | page.tsx → 41.c |
| 6 | packages/**/src | `fix(31-08): packages/**/src ...` | ≤ 50 | excludes 37/38 carveouts |
| 7 | BLOCKING flip | `feat(31-08): flip LOCKER-04 to BLOCKING ...` | ~5 files | DISCIPLINE.md + CLAUDE.md + lefthook + ci.yml + nightly.yml + Makefile + package.json + allowlist |

Each fix commit lands tests + production edits together per DISCIPLINE Rule 1.

## Verification Gate

- After Task 7: `pnpm lint:lockers` exits 0 with NO `--warn-only` on LOCKER-04.
- `pnpm test:lint-prod-readiness` ≥ 90/90/90/90; `pnpm test` (full suite) GREEN.
- `pnpm exec vitest run tests/e2e/lockers.spec.ts` — all cases GREEN (including the LOCKER-04 BLOCKING case re-run).
- `wc -l tools/lint-prod-readiness.allowlist.txt` shows only permanent + Phase-41/38-deferred entries (each tagged).
- `git log --oneline 31-08` shows ≤ 8 commits (1 triage + ≤ 6 area commits + 1 flip; areas may split into 1a/b/c).
- `31-08-DEFERRED.md` lists every deferred violation with its owning phase number.
- Coverage delta `31-COVERAGE.md` shows per-file before/after on the four axes per DISCIPLINE Rule 10.

## Risks + Mitigations

| Risk | Mitigation |
|---|---|
| A single area exceeds 50 files | Split into Xa/Xb/Xc with the same commit-subject prefix. |
| Phase 32 area accidentally touched | Triage task identifies the Phase 32 / 36.a / 37 / 38 / 41 zones; executor double-checks each commit's diff against `31-08-DEFERRED.md` before staging. |
| Fixing a route reveals a missing contract schema in `@openwhispr/wire-schemas` | Add the schema as part of the SAME commit (TDD red→green: failing test asserts new schema exists; greens by adding the schema + the route fix). |
| BLOCKING flip surprises remaining allowlisted entries | Task 7 step 1 mandates advisor-researcher consultation for any unexplained residual entry. |
| Coverage drops below 90 on a touched file | Per DISCIPLINE Rule 7, the commit FAILS verification — executor must extend the test commit-internally before push. |
| Bulk-fix scope creep | Strict CRITICAL/HIGH exclusion list (deferred ledger); no commit may carry a CR-class fix — those belong to Phases 32–41. |

## Out of Scope

- Any CRITICAL/HIGH fix owned by Phases 32–41 (logged in `31-08-DEFERRED.md`).
- `apps/api/src/index.ts` NODE_ENV short-circuits (`:494,498`) — production architecture fix; not 31-08's scope.
- `apps/worker/src/db/app-pool.ts` `as any` suppressions — Phase 32 RLS touches the same file; deferred.
- `apps/worker/src/jobs/audit-archive.ts` — Phase 36.a.
- `packages/litellm-client/src/errors.ts` — Phase 37.
- `packages/auth/**` — Phase 38.
- Phase 41-owned apps/web `(auth)/app/**/page.tsx` and `apps/api/src/routes/agent/stream.ts` — Phase 41.b/c.
- New DISCIPLINE rules beyond the WARN→BLOCKING closing note.
