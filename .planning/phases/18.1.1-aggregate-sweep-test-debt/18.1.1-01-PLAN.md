---
phase: 18.1.1-aggregate-sweep-test-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/tests/unit/lib/sse-parser.test.ts
  - apps/api/tests/unit/routes/tokens/assemblyai.test.ts
  - apps/api/tests/unit/routes/tokens/deepgram.test.ts
  - apps/api/tests/unit/routes/tokens/openai-realtime.test.ts
  - apps/api/tests/unit/i18n/__tests__/i18n-completeness.test.ts
  - apps/api/tests/unit/otel-bootstrap.test.ts
  - apps/api/tests/unit/__tests__/entrypoint-db-shape.test.ts
  - apps/api/tests/unit/lib/redact-url-bootstrap-usage.test.ts
  - apps/api/tests/unit/auth.test.ts
  - packages/data/tests/unit/__tests__/migrate.test.ts
  - packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts
  - packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts
  - packages/data/tests/unit/__tests__/migration-rollback.test.ts
  - packages/data/tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts
  - packages/data/tests/unit/__tests__/settings-rls.test.ts
  - packages/data/tests/unit/__tests__/rls-property.test.ts
  - packages/data/tests/unit/__tests__/worker-rls-property.test.ts
  - apps/api/tests/unit/__tests__/boot-order.test.ts
  - .planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md
  - tools/lint-colocated-tests.ts
  - tools/lint-colocated-tests.test.ts
  - tools/__fixtures__/lint-stale-path-literals/
autonomous: true
requirements:
  - SR-A1
  - SR-A5
  - SR-A6
must_haves:
  truths:
    - "`pnpm --filter @openwhispr/api test sse-parser` exits 0; sse-parser.test.ts resolves `../../../src/routes/agent/__fixtures__/*.sse` with existsSync guard (D-04)."
    - "Three tokens fixture tests (assemblyai, deepgram, openai-realtime) resolve `../../../../src/routes/tokens/__fixtures__/*.json` and pass."
    - "i18n-completeness.test.ts resolves `API_SRC = ../../../../src` from `apps/api/tests/unit/i18n/__tests__/`."
    - "otel-bootstrap.test.ts resolves `../../src/index.ts` + `../../src` (P6/P7)."
    - "entrypoint-db-shape.test.ts uses `new URL('../../../src/index.ts', import.meta.url)`."
    - "redact-url-bootstrap-usage.test.ts uses `new URL('../../../src/index.ts', import.meta.url)`."
    - "auth.test.ts reads `.env.slim.example` (NOT `.env.example`) per Δ-2; depth stays `../../..` from `apps/api/tests/unit/` (already correct)."
    - "migrate.test.ts resolves `../../../src/migrate.ts`."
    - "Seven packages/data tests with MIGRATIONS_FOLDER refs use `../../../migrations` (Cluster 9 enumerated to 7 files per Δ-bonus / pitfall §8)."
    - "boot-order.test.ts REPO_ROOT uses `../../../../..` (5 hops, not 4)."
    - "UI-SPEC-admin.md `apps/` prefix strip — applied IFF `lint-cjm-doc` fires; otherwise VERIFY-AND-SKIP per RESEARCH §1 P18."
    - "tools/lint-colocated-tests.ts exports `findStalePathLiterals(rootDir)` (AST-based, ts.createSourceFile) that fails on missing readFileSync/path.resolve/new URL literal targets; wired into `pnpm lint:colocated-tests`."
    - "Each cluster lands as a SINGLE GREEN commit (D-39); verbatim error output anchored in commit body (Phase 18.1 §7)."
    - "lint-extension lands as 2-commit RED+GREEN pair (D-39 — NEW functionality)."
    - "ZERO `--no-verify`; lefthook clean on every commit (D-41)."
  artifacts:
    - path: "apps/api/tests/unit/lib/sse-parser.test.ts"
      provides: "Cluster 1 — SSE fixture path corrected + existsSync guard."
      contains: "../../../src/routes/agent/__fixtures__"
    - path: "tools/lint-colocated-tests.ts"
      provides: "findStalePathLiterals AST-based linter extension."
      contains: "export function findStalePathLiterals"
  key_links:
    - from: "all path-fix tests"
      to: "apps/api/tests/unit/lib/sse-parser.test.ts:17-19"
      via: "Canonical cross-tree pattern: `dirname(fileURLToPath(import.meta.url))` + `existsSync` guard (D-04)."
      pattern: "dirname(fileURLToPath(import.meta.url))"
    - from: "tools/lint-colocated-tests.ts findStalePathLiterals"
      to: "pre-commit hook (lefthook colocated-tests)"
      via: "`pnpm lint:colocated-tests` script extension picks up automatically."
      pattern: "lint:colocated-tests"
---

<objective>
Close Bucket A path-fix mass-sweep (Clusters 1-11) + extend `tools/lint-colocated-tests.ts` with `findStalePathLiterals` so future Phase 15-02-style regressions fail at pre-commit time.

**12 atomic commits total:**
- 11 cluster commits (per D-02 / D-39 single-GREEN doctrine — pre-existing RED on `main`)
- 1 lint-extension RED+GREEN pair (D-05 + D-39 NEW functionality)

Wave 1 — runs parallel with 18.1.1-02 and 18.1.1-04 (disjoint files per D-38).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-RESEARCH.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-PATTERNS.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-01-PLAN.md
@CLAUDE.md
@apps/api/tests/unit/lib/sse-parser.test.ts
@tools/lint-colocated-tests.ts
@tools/lint-colocated-tests.test.ts
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1.1-01-01: Cluster 1 — sse-parser fixture paths (single GREEN)</name>
  <files>apps/api/tests/unit/lib/sse-parser.test.ts</files>
  <action>
Per D-02 Cluster 1 + D-03 single-GREEN doctrine + RESEARCH §1 P1.

1. Run `pnpm --filter @openwhispr/api test sse-parser 2>&1 | tee /tmp/01-01-red.log` to capture verbatim ENOENT (RED proof).
2. Edit `fixturePath` builder at line 17-19: replace `resolve(HERE, "..", "routes", "agent", "__fixtures__", ...)` with `resolve(HERE, "..", "..", "..", "src", "routes", "agent", "__fixtures__", ${name}.sse)`.
3. Add `existsSync` guard immediately above readFileSync (D-04): `if (!existsSync(p)) throw new Error(\`source-contract path moved: ${p}\`);` — extend `node:fs` import for `existsSync`.
4. Re-run: must exit 0 (7 .sse fixtures resolve).
5. Commit: `fix(18.1.1-01-01): repoint sse-parser fixture paths to src/routes/agent/__fixtures__`. Body MUST include verbatim error output from step 1.

WHY: pitfall §1 — vitest setup inheritance not relevant here; pitfall covers analog. This is canonical RED → GREEN per Phase 18.1 §7.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test tests/unit/lib/sse-parser.test.ts 2>&1 | grep -E "(✓|PASS)" | head -5</automated></verify>
  <done>1 commit; sse-parser GREEN; existsSync guard present; commit body has verbatim RED proof.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-02: Cluster 2 — tokens fixtures (single GREEN, 3 files)</name>
  <files>apps/api/tests/unit/routes/tokens/assemblyai.test.ts, apps/api/tests/unit/routes/tokens/deepgram.test.ts, apps/api/tests/unit/routes/tokens/openai-realtime.test.ts</files>
  <action>
Per D-02 Cluster 2 + RESEARCH §1 P2-P4.

1. Capture verbatim RED for all three: `pnpm --filter @openwhispr/api test tokens 2>&1 | tee /tmp/01-02-red.log`.
2. In each of the 3 files, replace `resolve(dirname(fileURLToPath(import.meta.url)), "__fixtures__")` with `resolve(dirname(fileURLToPath(import.meta.url)), "..", "..", "..", "..", "src", "routes", "tokens", "__fixtures__")`. 4 ups (`tests/unit/routes/tokens/` → repo root) + 4 down into `apps/api/src/routes/tokens/__fixtures__/`.
3. Add `existsSync` guard above each readFileSync site (D-04).
4. Single commit: `fix(18.1.1-01-02): repoint tokens fixture paths (assemblyai+deepgram+openai-realtime)`. Body has verbatim RED.

WHY: 3 sibling files = same depth math = one cluster per D-02. Don't split into 3 commits — Phase 18.1 §7 cluster-by-source-tree.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test tests/unit/routes/tokens 2>&1 | grep -E "✓ .*(assemblyai|deepgram|openai-realtime)" | wc -l</automated></verify>
  <done>1 commit; 3 token tests GREEN; existsSync guard present in all 3.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-03: Cluster 3 — i18n-completeness (single GREEN)</name>
  <files>apps/api/tests/unit/i18n/__tests__/i18n-completeness.test.ts</files>
  <action>
Per D-02 Cluster 3 + RESEARCH §1 P5.

1. Capture RED via `pnpm --filter @openwhispr/api test i18n-completeness`.
2. From `apps/api/tests/unit/i18n/__tests__/` (4 hops to repo root, then into src): set `API_SRC = resolve(HERE, "..", "..", "..", "..", "src")`. Adjust locales builder to `resolve(API_SRC, "i18n", "locales", \`${lng}.json\`)`.
3. Add existsSync guard before each readFileSync.
4. Commit: `fix(18.1.1-01-03): repoint i18n-completeness API_SRC to apps/api/src`. Body has verbatim RED.

WHY: depth math is irregular per cluster (D-01 — no codemod possible).
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test tests/unit/i18n 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; i18n-completeness GREEN.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-04: Cluster 4 — otel-bootstrap P6/P7 (single GREEN)</name>
  <files>apps/api/tests/unit/otel-bootstrap.test.ts</files>
  <action>
Per D-02 Cluster 4 + D-10 + RESEARCH §1 P6/P7.

1. Capture RED for otel-bootstrap.
2. Line 79: `path.join(__dirname, "index.ts")` → `path.join(__dirname, "..", "..", "src", "index.ts")`. Line 194: `path.resolve(__dirname, "routes")` → `path.resolve(__dirname, "..", "..", "src")`.
3. Add existsSync guard above each read (D-04). Preserve first-line invariant assertion semantics (D-10 — paths-only fix).
4. Commit: `fix(18.1.1-01-04): repoint otel-bootstrap to apps/api/src/{index.ts,routes}`. Body has verbatim RED.

WHY: pitfall §1 — D-10 forbids semantic change to "first-line" assertion (Phase 14 supersede).
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test otel-bootstrap 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; otel-bootstrap GREEN; first-line assertion semantics unchanged.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-05: Cluster 5 — entrypoint-db-shape Cat-C double-apps fix (single GREEN)</name>
  <files>apps/api/tests/unit/__tests__/entrypoint-db-shape.test.ts</files>
  <action>
Per D-02 Cluster 5 + RESEARCH §1 P8.

1. Capture RED. From `apps/api/tests/unit/__tests__/` (3 hops to `apps/api/`, +1 to repo, +1 down to `src/`): replace `new URL("../index.ts", import.meta.url)` with `new URL("../../../src/index.ts", import.meta.url)`.
2. Add existsSync guard via `fileURLToPath` + `existsSync`.
3. Commit: `fix(18.1.1-01-05): repoint entrypoint-db-shape new URL to apps/api/src/index.ts`. Body has verbatim RED.

WHY: Cat-C double-`apps/` regression (`apps/apps/api/`) is irrecoverable by codemod (D-01).
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test entrypoint-db-shape 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; entrypoint-db-shape GREEN.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-06: Cluster 6 — redact-url-bootstrap-usage (single GREEN)</name>
  <files>apps/api/tests/unit/lib/redact-url-bootstrap-usage.test.ts</files>
  <action>
Per D-02 Cluster 6 + RESEARCH §1 P9.

1. Capture RED. From `apps/api/tests/unit/lib/` (3 hops to `apps/api/`, +1 down to `src/`): replace `new URL("../index.ts", import.meta.url)` with `new URL("../../../src/index.ts", import.meta.url)`.
2. Add existsSync guard.
3. Commit: `fix(18.1.1-01-06): repoint redact-url-bootstrap-usage to apps/api/src/index.ts`. Body has verbatim RED.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test redact-url-bootstrap-usage 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; redact-url-bootstrap-usage GREEN.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-07: Cluster 7 — auth.test.ts .env filename change (single GREEN, Δ-2 honor)</name>
  <files>apps/api/tests/unit/auth.test.ts</files>
  <action>
Per D-02 Cluster 7 + RESEARCH §9 Δ-2 + pitfall §2, §6.

1. Capture RED. Δ-2 verified: depth `../../../` from `apps/api/tests/unit/` is CORRECT (3 hops = repo root). Only the FILENAME is wrong — `.env.example` does not exist on disk; only `.env.slim.example`, `.env.full.example`, `.env.embedded.example`, `.env.e2e.example` are present.
2. Line 147: change `.env.example` → `.env.slim.example` (keep `resolve(__dirname, "..", "..", "..", ".env.slim.example")`). Do NOT touch depth.
3. Add existsSync guard BEFORE the assertion — verifies the filename swap actually hits a real file.
4. Commit: `fix(18.1.1-01-07): repoint auth.test.ts .env.example → .env.slim.example (Δ-2)`. Body MUST cite RESEARCH §9 Δ-2 + verbatim RED.

WHY: pitfall §2 — Cluster 7 is filename-only fix; depth is already correct. pitfall §6 — auth.test asserts `OPENWHISPR_DISABLE_RATE_LIMIT` doc lives in `.env.slim.example` specifically.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test auth.test 2>&1 | grep -E "(env.slim|✓|PASS)" | head -3</automated></verify>
  <done>1 commit; auth.test.ts GREEN; depth unchanged; only filename changed; existsSync guard hits `.env.slim.example` on disk.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-08: Cluster 8 — migrate.test.ts dynamic-import target (single GREEN)</name>
  <files>packages/data/tests/unit/__tests__/migrate.test.ts</files>
  <action>
Per D-02 Cluster 8 + RESEARCH §1 P11.

1. Capture RED. From `packages/data/tests/unit/__tests__/` (3 hops to `packages/data/`, +1 down to `src/`): replace `resolve(HERE, "..", "migrate.ts")` with `resolve(HERE, "..", "..", "..", "src", "migrate.ts")`.
2. Add existsSync guard before dynamic import (D-04 + Cat-E coverage).
3. Commit: `fix(18.1.1-01-08): repoint migrate.test.ts to packages/data/src/migrate.ts`. Body has verbatim RED.

WHY: Cat-E (dynamic-import) requires existsSync before `await import(target)` — different shape from readFileSync but same defensive uplift.
  </action>
  <verify><automated>pnpm --filter @openwhispr/data test migrate.test 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; migrate.test GREEN.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-09: Cluster 9 — packages/data MIGRATIONS_FOLDER batch (single GREEN, 7 files per Δ-bonus)</name>
  <files>packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts, packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts, packages/data/tests/unit/__tests__/migration-rollback.test.ts, packages/data/tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts, packages/data/tests/unit/__tests__/settings-rls.test.ts, packages/data/tests/unit/__tests__/rls-property.test.ts, packages/data/tests/unit/__tests__/worker-rls-property.test.ts</files>
  <action>
Per D-02 Cluster 9 + RESEARCH §1 P12-P16 + pitfall §8 (Δ-bonus: 7 files, NOT 5).

1. Re-enumerate first: `grep -rln "MIGRATIONS_FOLDER\|migrations" packages/data/tests/unit/__tests__/ | xargs grep -l "resolve(__dirname"`. Confirm 7 candidates (or expand if drift since RESEARCH).
2. For EACH file: from `packages/data/tests/unit/__tests__/`, replace `resolve(__dirname, "..", "..", "migrations")` with `resolve(__dirname, "..", "..", "..", "migrations")` (4 hops up + 1 down to `packages/data/migrations/`).
3. Add existsSync guard to each.
4. Capture RED for all 7 in one log file.
5. Single commit: `fix(18.1.1-01-09): repoint 7 packages/data tests to ../../../migrations (Δ-bonus 5→7 files)`. Body has verbatim RED + enumerated filenames + Δ-bonus citation.

WHY: pitfall §8 — CONTEXT said 5, live grep shows 7 (`migration-rollback`, `worker-rls-property` extra). Batch as ONE cluster per D-02 to preserve cluster-by-source-tree doctrine.
  </action>
  <verify><automated>pnpm --filter @openwhispr/data test 2>&1 | grep -E "✓ .*(pgbouncer|migration-0006|migration-rollback|0003_better_auth|settings-rls|rls-property|worker-rls-property)" | wc -l</automated></verify>
  <done>1 commit touching ≥7 files; all GREEN; existsSync in each; commit body enumerates filenames.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-10: Cluster 10 — boot-order REPO_ROOT 5-hop (single GREEN)</name>
  <files>apps/api/tests/unit/__tests__/boot-order.test.ts</files>
  <action>
Per D-02 Cluster 10 + RESEARCH §1 P17.

1. Capture RED. From `apps/api/tests/unit/__tests__/`: REPO_ROOT needs 5 hops (not 4): `path.resolve(__dirname, "../../../../..")` — pitfall: easy off-by-one because `__tests__` segment is sometimes elided in mental count.
2. Add existsSync guard against the resolved REPO_ROOT (assert e.g. `package.json` exists at root).
3. Commit: `fix(18.1.1-01-10): repoint boot-order REPO_ROOT to 5-hop from __tests__`. Body has verbatim RED.

WHY: Cluster 10 boot-order REPO_ROOT off-by-one classic — pitfall is `__tests__` segment.
  </action>
  <verify><automated>pnpm --filter @openwhispr/api test boot-order 2>&1 | grep -E "(✓|PASS)" | head -3</automated></verify>
  <done>1 commit; boot-order GREEN; REPO_ROOT existsSync-validates against repo `package.json`.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-01-11: Cluster 11 — UI-SPEC-admin.md apps/ prefix (VERIFY-AND-SKIP)</name>
  <files>.planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md</files>
  <action>
Per D-02 Cluster 11 + RESEARCH §1 P18 (VERIFY-AND-SKIP path).

1. Run `pnpm test:lint-cjm-doc 2>&1 | tee /tmp/01-11-cjm.log` (if script exists). If script absent, run `pnpm exec node tools/lint-cjm-doc.ts` directly.
2. If NO `apps/`-prefix violations surface: do NOT edit the doc. Instead emit a SUMMARY note "Cluster 11 VERIFIED — no standalone `apps/` lines in UI-SPEC-admin.md (per RESEARCH §1 P18 grep verification)" and SKIP commit.
3. If violations surface: surgically strip the `apps/` prefix from offending lines only (table cells in prose stay). Commit: `fix(18.1.1-01-11): strip apps/ prefix from UI-SPEC-admin.md`.
4. If lint-cjm-doc itself errors due to script-wiring drift: HALT per D-09 (operator recipe).

WHY: pitfall §15 — RESEARCH grep returned zero standalone `apps/` lines; cluster may be a no-op. D-46 forbids silent expansion.
  </action>
  <verify><automated>pnpm test:lint-cjm-doc 2>&1 | tail -10</automated></verify>
  <done>EITHER 0 commits with verified-skip note OR 1 commit stripping prefix; lint-cjm-doc exits 0 either way.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-01-12: D-05 findStalePathLiterals lint extension (RED+GREEN pair)</name>
  <files>tools/lint-colocated-tests.ts, tools/lint-colocated-tests.test.ts, tools/__fixtures__/lint-stale-path-literals/exists/sample.test.ts, tools/__fixtures__/lint-stale-path-literals/missing/sample.test.ts</files>
  <behavior>
- Test 1 (RED expectation): `findStalePathLiterals("tools/__fixtures__/lint-stale-path-literals/missing/")` returns array containing the stale literal path → assertion fails because function doesn't exist yet.
- Test 2: Same function called against `exists/` fixture tree → returns empty array (no violations).
- Test 3: Function tolerates `// lint-stale-path-literals-ignore` marker (skips that line).
- Test 4: Function exits non-zero from CLI invocation when violations present.
  </behavior>
  <action>
Per D-05 + D-39 (NEW functionality → 2-commit RED+GREEN pair) + PATTERNS A2.

**RED commit:**
1. Create fixtures:
   - `tools/__fixtures__/lint-stale-path-literals/exists/sample.test.ts` — contains `readFileSync(resolve(__dirname, "./real-file.ts"))` where `real-file.ts` exists next to it.
   - `tools/__fixtures__/lint-stale-path-literals/missing/sample.test.ts` — contains `readFileSync(resolve(__dirname, "./does-not-exist.ts"))`.
2. Extend `tools/lint-colocated-tests.test.ts` with new `describe("findStalePathLiterals")` block per Test 1-4 above. Import `findStalePathLiterals` from `./lint-colocated-tests` (will fail — symbol missing).
3. Run `pnpm exec vitest run tools/lint-colocated-tests.test.ts` — must FAIL (RED).
4. Commit: `test(18.1.1-01-12): red — findStalePathLiterals against fixture tree`. Body has verbatim RED output.

**GREEN commit:**
1. In `tools/lint-colocated-tests.ts`: add `export function findStalePathLiterals(rootDir: string): StaleLiteral[]` using `ts.createSourceFile` + `forEachChild` walk. Detect:
   - `CallExpression` where callee is `readFileSync`/`path.resolve`/`new URL` constructor.
   - First-arg `StringLiteral` (skip template strings / dynamic — these are runtime).
   - Resolve statically relative to test file's directory.
   - Skip if `// lint-stale-path-literals-ignore` comment present on prior line.
   - Push `{ file, line, target }` when target does not exist (`existsSync(target) === false`).
2. Wire into CLI: extend the `main()` function so `pnpm lint:colocated-tests` runs BOTH checks. Exit code 0 = both clean; 1 = stale path; 2 = colocated violation. Update `package.json` script if needed.
3. Re-run test: GREEN.
4. Commit: `chore(18.1.1-01-12): green — wire findStalePathLiterals into lint-colocated-tests`.

WHY: pitfall §11 — `phase-tag-comments` lint forbids `// Phase 18.1.1` inline; use bare SPDX-only header on new fixture files. PATTERNS A2 — `typescript` is transitive dep via Vitest 4, no new top-level dep required (D-44).
  </action>
  <verify><automated>pnpm exec vitest run tools/lint-colocated-tests.test.ts 2>&1 | grep -E "findStalePathLiterals" | head -5 && pnpm lint:colocated-tests 2>&1 | tail -5</automated></verify>
  <done>2 commits (test:red + chore:green); findStalePathLiterals exported + tested; pnpm lint:colocated-tests exits 0 on clean tree; ts.createSourceFile-based AST walk in place; coverage ≥ 90/90/90/90 on new code (D-42).</done>
</task>

</tasks>

<verification>
After all 12 commits:

```bash
cd /Users/nick/openwhispr-server

# Each cluster runs GREEN
pnpm --filter @openwhispr/api test tests/unit/lib/sse-parser.test.ts
pnpm --filter @openwhispr/api test tests/unit/routes/tokens
pnpm --filter @openwhispr/api test tests/unit/i18n
pnpm --filter @openwhispr/api test otel-bootstrap
pnpm --filter @openwhispr/api test entrypoint-db-shape
pnpm --filter @openwhispr/api test redact-url-bootstrap-usage
pnpm --filter @openwhispr/api test auth.test
pnpm --filter @openwhispr/data test migrate.test
pnpm --filter @openwhispr/data test 2>&1 | grep -E "✓ .*(pgbouncer|migration|rls-property)"
pnpm --filter @openwhispr/api test boot-order
pnpm lint:colocated-tests

# Commit shape audit: 12 atomic commits, 11 single-GREEN + 1 RED+GREEN pair
git log --oneline -n 13 | grep -cE "(fix|test|chore)\(18\.1\.1-01-(0[1-9]|1[0-2])\)" # expect 12 (cluster-11 may be 0 commits, then 11)

# Coverage gate (D-42) — only D-05 lint extension is NEW code
pnpm --filter ./tools test --coverage 2>&1 | grep -E "(Lines|Branches|Functions|Statements).*9[0-9]"
```

Expected: all path-fix tests exit 0; lint:colocated-tests exits 0; `findStalePathLiterals` coverage ≥ 90/90/90/90.
</verification>

<success_criteria>
1. 11 Bucket A clusters all GREEN (Cluster 11 may be VERIFY-AND-SKIP).
2. 12 atomic commits (or 11 if Cluster 11 skipped) — single GREEN per pre-existing-RED cluster; 2-commit RED+GREEN for D-05.
3. existsSync guard (D-04) added to every modified test.
4. Coverage on D-05 new code: ≥ 90/90/90/90 (D-42).
5. ZERO `--no-verify` (D-41); lefthook clean.
6. Δ-2 honored (Cluster 7 filename-only).
7. Δ-bonus honored (Cluster 9 enumerates 7 files, not 5).
</success_criteria>

<rollback>
- Cluster commit breaks a sibling assertion → `git revert <SHA>`; re-read file; re-apply with narrower diff.
- D-05 lint-extension fires false positives on real codebase (post-merge) → revert chore: green commit; add allow-list file `tools/lint-stale-path-literals.allowlist.txt`; re-implement with allow-list honored.
- HALT recipe (Cluster 11) — if `lint-cjm-doc` script missing entirely, emit operator recipe (`pnpm install` + Docker check + retry) and present user 3 branches: (a) wire script in this plan, (b) defer to Phase 18.1.2, (c) declare Cluster 11 N/A and document in SUMMARY.
</rollback>

<output>
Create `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-01-SUMMARY.md` with:
- 11-12 commit SHAs (note any cluster skipped)
- `pnpm test` aggregate failure-count delta vs Phase 18.1-07 baseline
- D-05 coverage report (≥ 90/90/90/90)
- Lefthook status: clean (expected)
- Δ-2 + Δ-bonus citations
</output>
</content>
</invoke>