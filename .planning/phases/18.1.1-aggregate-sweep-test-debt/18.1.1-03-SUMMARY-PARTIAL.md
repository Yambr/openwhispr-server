---
phase: 18.1.1
plan: 03
status: PARTIAL (D-08 reaper-extract + session-id done; D-11 triaged; D-12 PENDING)
completed_tasks: [D-08-reaper-extract, D-08-session-id]
pending_tasks: [D-12-bootstrap-roles-helper]
triaged: [D-11-lint-rls]
last_activity: 2026-05-15 commit 360d918
---

# Phase 18.1.1 Plan 03: testcontainer + misc — PARTIAL SUMMARY

## One-liner

D-08 reaper-helper extracted to `tools/testcontainer-reaper-setup.ts`
and wired into apps/worker + packages/data; teardown prune narrowed by
`OPENWHISPR_TESTCONTAINER_SESSION_ID` env. D-11 triaged → CREATEROLE
bug (covered by pending D-12). D-12 helper-extract not started — paused
at W-3 context budget for next spawn.

## Commits (this executor session)

| Task | Gate | Commit | Notes |
| ---- | ---- | ------ | ----- |
| D-08 reaper-extract | RED | `8d7c35a` | `tools/__tests__/testcontainer-reaper-setup.test.ts` listener-count assertion |
| D-08 reaper-extract | GREEN | `65f7722` | NEW `tools/testcontainer-reaper-setup.ts`; apps/api/vitest.setup.ts reduced to pure import; apps/worker + packages/data vitest configs gain setupFiles |
| D-08 session-id | RED | `0f6189f` | global-vitest-teardown.test.ts gains 2 assertions on session-id label filter |
| D-08 session-id | GREEN | `360d918` | `buildPruneArgv()` reads `OPENWHISPR_TESTCONTAINER_SESSION_ID`; legacy fallback preserved |

## D-11 triage (not committed)

`pnpm test tools/lint-rls.test.ts` fails with PG error
**42501 — permission denied to alter role openwhispr_app**:

```
Only roles with the CREATEROLE attribute and the ADMIN option on
role "openwhispr_app" may alter this role.
SQL: ALTER ROLE openwhispr_app SET app.tenant_id TO '<uuid>'
```

The owner role is bootstrapped with `BYPASSRLS` only — missing the
`CREATEROLE` attribute AND the `GRANT openwhispr_app TO openwhispr_owner
WITH ADMIN OPTION` line that `pgbouncer-interleave.test.ts:104-107`
already includes. This is the **same root cause** as Δ-1 / D-12 in the
plan. Disposition: **transitively fixed by D-12 helper extraction**.

## Pending — D-12 bootstrap-roles helper (NEXT SPAWN)

Extract from `packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts:99-112`:

```sql
CREATE ROLE openwhispr_owner WITH LOGIN BYPASSRLS CREATEROLE PASSWORD ...
CREATE ROLE openwhispr_app   WITH LOGIN PASSWORD ...
GRANT openwhispr_app TO openwhispr_owner WITH ADMIN OPTION
GRANT SET, ALTER SYSTEM ON PARAMETER "app.tenant_id" TO openwhispr_owner
ALTER DATABASE <db> OWNER TO openwhispr_owner
ALTER SCHEMA public OWNER TO openwhispr_owner
```

→ `packages/data/tests/unit/__helpers__/bootstrap-roles.ts` exporting
`async function bootstrapRoles(superPool: Pool, opts: { dbName: string;
ownerPassword: string; appPassword: string }): Promise<void>`.

**Callers to update (7 sites):**

- packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts (template source)
- packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts
- packages/data/tests/unit/__tests__/settings-rls.test.ts
- packages/data/tests/unit/__tests__/rls-property.test.ts
- packages/data/tests/unit/__tests__/migrate-litellm-db.test.ts
- packages/data/tests/unit/__tests__/worker-rls-property.test.ts
- tools/lint-rls.test.ts (transitively fixes D-11)

**TDD shape:**
1. RED: `packages/data/tests/unit/__tests__/bootstrap-roles.test.ts` asserts
   `bootstrapRoles(superPool)` runs against a real test container, then
   `SELECT rolcreaterole FROM pg_roles WHERE rolname='openwhispr_owner'`
   returns true and `SELECT rolname FROM pg_roles WHERE rolname='openwhispr_app'`
   returns one row.
2. GREEN: extract helper + update all 7 callers in one commit.
3. Caveat in commit body: prod-parity drift (CREATEROLE on owner is
   non-prod). Planner risk #2 calls for a future split into
   `bootstrapMinimalRoles` + `grantCreateRoleForTesting` if Phase 19.x
   prod-tests fail.

## Self-Check

All 4 commits present in `git log` between `8d7c35a` and `360d918`.
Listener-count and session-id filter assertions verified locally. D-11
triage findings recorded above (not yet code-committed; resolved by D-12).
