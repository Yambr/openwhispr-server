# Phase 18.1.1: aggregate-sweep test debt + UICONF auth drift - Context

**Gathered:** 2026-05-15
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the two debt buckets surfaced by Phase 18.1-07's aggregate `pnpm test` smoke + user feedback on auth UI:

**Bucket A — System test debt** (~20 ENOENT path bugs across apps/api + apps/worker + packages/data, wire-schemas undeclared dep, ESBuild/oxc await PARSE_ERROR, testcontainer cleanup hygiene, otel-bootstrap first-line invariant, lint-rls, postgres role bootstrap drift).

**Bucket B — UICONF visual drift** (auth screens diverge from `screens-user.jsx` oracle: AuthShell missing, password show/hide eye toggle missing, "Remember this device" checkbox missing, OIDC button ordering wrong, password strength meter missing, terms checkbox missing in sign-up, status badge icon missing in verify-email, UICONF-05 axe baseline never committed).

**In scope:**
- All Bucket A failure clusters (~16 path-fix sites + 4 cluster-2-3-4 fixes).
- AuthShell component scaffold + 4 auth screens (sign-in/sign-up/verify-email/setup) to oracle parity.
- UICONF-05 axe baseline JSON + Playwright visual regression snapshots committed.
- ROADMAP + STATE.md sync + v2.1 milestone declaration.
- Docker garbage hygiene (already done in pre-phase: 15.61 GB reclaimed).
- Phase 18.2 (originally carved separately for auth redesign) merged into 18.1.1 Bucket B per user direction "не отложить — исправить сейчас".

**Out of scope:**
- Phase 19.1-19.4 (real downstream code for repointed @cjm tags — sendResetPassword, mock-litellm STT, BA i18n, locale-toggle e2e).
- Two-tier rate-limit (Phase 18.1 deferred to v3).
- New auth features (passkeys, TOTP, magic-link, social-login beyond Google/GitHub/SSO).
- Reset-mail wiring (D-UX2 / SR-B5 defer).
- FSL history scrub (operator).
- First real GHA e2e-cjm CI run (operator).

</domain>

<decisions>
## Implementation Decisions

### Bucket A — System test debt

#### A1 — ENOENT path regression mass-fix (~16 net sites)

- **D-01:** Apply **Option A** path-rewrite per Phase 18.1-01 precedent. NO codemod (mixed categories Cat-A/B/C/E preclude uniform transform; depth math is irregular). NO mirror (contradicts Phase 18.1 D-05). Each test file edited surgically.
- **D-02:** Cluster into ~9 commits (per A1 advisor table P1-P20):
  - **Cluster 1** (`fix(18.1.1-01-01)`): SSE-parser fixture paths → `apps/api/src/routes/agent/__fixtures__/*.sse` (P1, 7 fixtures, 1 file).
  - **Cluster 2** (`fix(18.1.1-01-02)`): tokens fixture paths (P2-P4, 3 sibling test files).
  - **Cluster 3** (`fix(18.1.1-01-03)`): i18n-completeness `API_SRC` repoint (P5, 1 file).
  - **Cluster 4** (`fix(18.1.1-01-04)`): otel-bootstrap `index.ts` + `apiRoot` repoint (P6, P7, 1 file).
  - **Cluster 5** (`fix(18.1.1-01-05)`): entrypoint-db-shape `new URL` depth fix (P8, 1 file, Cat-C double-`apps/` bug).
  - **Cluster 6** (`fix(18.1.1-01-06)`): redact-url-bootstrap-usage `index.ts` repoint (P9, 1 file).
  - **Cluster 7** (`fix(18.1.1-01-07)`): `auth.test.ts` `.env.example` → `.env.slim.example` + path (P10, 1 file, content fix).
  - **Cluster 8** (`fix(18.1.1-01-08)`): `migrate.test` dynamic-import → `packages/data/src/migrate.ts` (P11, 1 file, Cat-E).
  - **Cluster 9** (`fix(18.1.1-01-09)`): `packages/data` 5 migration-folder depth fixes (P12-P16, 5 sibling files, one commit).
  - **Cluster 10** (`fix(18.1.1-01-10)`): boot-order `REPO_ROOT` depth (P17, 1 file).
  - **Cluster 11** (`fix(18.1.1-01-11)`): `UI-SPEC-admin.md` `apps/` prefix strip (P18, 1 file — locate first).
- **D-03:** TDD shape per Phase 18.1 D-25 + A1 finding: all 16+ failures are **pre-existing RED on `main`**. Each path-fix lands as a **single GREEN commit** (the existing failing test IS the RED proof — re-stating it via `pnpm test` exit code is sufficient per Phase 18.1 §7). Commit body MUST include verbatim error output to anchor the RED proof. Atomic-commit doctrine preserved.
- **D-04:** Add `existsSync`-guard hardening per Phase 18.1 D-04 to every path-fix.
- **D-05:** Extend `tools/lint-colocated-tests.ts` with NEW exported function `findStalePathLiterals(rootDir)` that AST-greps test files for `readFileSync`/`path.resolve`/`new URL` arguments containing string literals, statically resolves the target relative to test file, reports non-existent target. Wire into `pnpm lint:colocated-tests` script. This makes Phase 15-02-style regressions impossible at pre-commit hook time. **TDD: write failing test for `findStalePathLiterals` first against fixture tree, then implement.** Land as `chore(18.1.1-01-12)`.

#### A2 — wire-schemas + ESBuild/oxc + testcontainer hygiene

- **D-06:** **Cluster 1 (`@openwhispr/wire-schemas` resolution):** Empirically NOT broken (resolves via pnpm hoisting). However `packages/contract-tests/package.json` lacks declared dep. Fix: add `"@openwhispr/wire-schemas": "workspace:*"` to dependencies. RED test: new `packages/contract-tests/tests/__tests__/dep-graph.test.ts` (lint-style) reads its own package.json + walks `tests/**/*.ts` imports + asserts every `@openwhispr/*` import has matching dep entry. GREEN: declare dep. **1 atomic commit (`fix(18.1.1-02-01)`).**
- **D-07:** **Cluster 2 (ESBuild/oxc `await` PARSE_ERROR):** Vite 8.0.11 + Vitest 4.1.5 swapped TS transformer to `vite:oxc`. Stricter than esbuild about TLA contexts. The offending idiom is `expect(() => Schema.parse(await res.json())).not.toThrow()` — `await` inside non-async arrow. Fix: refactor each occurrence to `const body = await res.json(); expect(() => Schema.parse(body)).not.toThrow()`. Affected files include `packages/contract-tests/tests/unit/transcriptions.test.ts:131,175` + 4 more (grep `await .*\.json\(\)` inside `()=>` to locate). RED test: new `tools/lint-await-in-non-async.ts` AST linter (uses `ts.createSourceFile`) wired into `tools` vitest project. Scans `*.test.ts` for `AwaitExpression` inside non-async `ArrowFunctionExpression`/`FunctionExpression`. GREEN: refactor 5 test files. **1 atomic commit (`fix(18.1.1-02-02)`).**
- **D-08:** **Cluster 3 (testcontainer cleanup hygiene):** Infra partially exists at `tools/global-vitest-teardown.ts` + `apps/api/vitest.setup.ts:1-14` (`installSignalHook()`). Missing: shared reaper-helper is NOT referenced by `apps/worker/vitest.config.ts` or `packages/data/vitest.config.ts` (other testcontainer users). Also: vitest crashes BEFORE testcontainers SDK loads (pre-cluster-1 ENOENT failures), so Ryuk never gets handshake. Fix: promote `apps/api/vitest.setup.ts` to root-shared `tools/testcontainer-reaper-setup.ts`, reference from missing workspace configs. Harden `tools/global-vitest-teardown.ts` to prune via `--filter label=org.testcontainers.session-id=<vitest-pid>` (narrower blast radius). **2 atomic commits (`fix(18.1.1-02-03)` extract reaper-helper + setupFiles wire-up + RED tests; `fix(18.1.1-02-04)` session-id label hardening + integration test).**
- **D-09:** **HALT protocol** per Phase 18.1-06 precedent: if `docker ps -a | grep testcontainers` shows orphans after `pnpm test` exits, OR if Ryuk container is not running, plan 18.1.1-02 HALTs and emits operator recipe (`docker system prune` + Docker Desktop restart + re-run). Three-branch user-choice on HALT.

#### A3 — Misc (otel-bootstrap, lint-rls, postgres role)

- **D-10:** **`otel-bootstrap` first-line invariant** (`apps/api/tests/unit/otel-bootstrap.test.ts:79,194`): paths P6/P7 covered by Cluster 4 in D-02. The "first-line" assertion semantics (Phase 14 supersede) remain as-is — only paths fix.
- **D-11:** **`lint-rls.test.ts`** (`tools/lint-rls.test.ts`): root cause TBC by executor — run failing test, capture error, classify. If path-bug → fold into D-02 Cluster 10/11. If logic bug → standalone fix commit `fix(18.1.1-03-01)`.
- **D-12:** **Postgres CREATEROLE permission drift** (`packages/data` migrate tests): testcontainer postgres image default `postgres` user is superuser. If tests need a non-superuser `openwhispr_app` role with CREATEROLE, the fixture migration `00-roles.sh` must run first. Verify via `compose/postgres/init-scripts/` or equivalent. Fix: ensure `globalSetup` for `packages/data` runs the role-bootstrap SQL before test suite. RED: standalone test asserting `pg_has_role('openwhispr_app','CREATEROLE','member')` against fresh testcontainer. GREEN: wire bootstrap. **1 atomic commit (`fix(18.1.1-03-02)`).**

### Bucket B — UICONF auth drift

#### B1 — AuthShell scaffold

- **D-13:** Create NEW `apps/web/src/components/screens/auth/AuthShell.tsx` mapping oracle `ui.jsx:229-316` to Tailwind 4 + shadcn/ui v2. Two-column split layout: left = branding panel (logo, kicker, side title, side quote, footer with version + Status/Docs/GitHub links); right = form panel. Mobile (`<lg`): side hidden, form fills viewport. Props: `children: ReactNode`, `sideTitle?: string`, `sideKicker?: string`, `sideQuote?: string`.
- **D-14:** Update `apps/web/src/app/(public)/layout.tsx:17-23` — drop centered wrapper (AuthShell now owns layout); LanguageSwitcher header stays absolute-positioned over right column.
- **D-15:** New i18n keys in `apps/web/src/locales/en/common.json` + `ru/common.json` for default shell copy (`common.auth.shell.kicker.default.text`, `common.auth.shell.title.default.text`, `common.auth.shell.quote.default.text`). en+ru parity in same atomic commit.

#### B2 — Sign-in form

- **D-16:** Wrap `SignInForm.tsx` in `<AuthShell>`. Drop `<Card max-w-md>` outer.
- **D-17:** Reorder: render `<OidcButtons>` BEFORE "Or with email" separator BEFORE email/password Form.
- **D-18:** Update `OidcButtons.tsx:66-80`: add `size="lg" className="w-full"` + branch `variant={p.id === "oidc" ? "ghost" : "outline"}` for SSO de-emphasis.
- **D-19:** Add "Or with email" separator label via text-in-rule pattern (Tailwind absolute-positioned span).
- **D-20:** Add password show/hide eye toggle using `lucide-react` `Eye`/`EyeOff` (already in deps). Local `useState<boolean>`. `aria-label` from new i18n key.
- **D-21:** Add "Remember this device" checkbox. Requires installing shadcn `checkbox` (vendoring Radix-based source; NO new top-level deps — `radix-ui` already at line 33 of package.json). Wire as RHF field `rememberDevice: boolean`. Better Auth `signIn.email` accepts `rememberMe` — pass through.
- **D-22:** KEEP D-UX2 forgot-password as muted static text. SR-B5 disposition: DEFER to Phase 19.1 reset-mail. Add sentinel comment `// Source of truth: D-UX2 (Phase 18.1.1) — anchor lands with Phase 19.1 reset-mail`. Document in `.planning/deferred-items.md`.
- **D-23:** Refactor footer "No account? Sign up" to centered text + accent link (drop CardFooter underline-link).

#### B3 — Sign-up form

- **D-24:** Wrap in `<AuthShell sideTitle={t("end-user.signup.shell.sideTitle.text")} sideQuote={t("end-user.signup.shell.sideQuote.text")}>`.
- **D-25:** Add password strength meter (oracle lines 117-131): 4px bar + label "Weak/Fair/Strong" derived from `signUpSchema` rule (12+ chars, mixed). Inline `passwordStrength(value)` helper colocated in component — no new dependency.
- **D-26:** Add "I agree to terms and privacy" checkbox. Same shadcn checkbox install as D-21. RHF field `acceptedTerms: z.literal(true)` in `signUpSchema`. Link `/terms` `/privacy`. **If routes don't exist:** scope-out to deferred-items (do not author placeholder pages — out of 18.1.1 scope).
- **D-27:** KEEP OIDC row (current code is additive feature beyond oracle — annotate in comment, not drift).
- **D-28:** Refactor footer same as D-23.

#### B4 — Verify-email screen

- **D-29:** Wrap in `<AuthShell sideTitle={t("end-user.verify.shell.sideTitle.text")} sideQuote={t("end-user.verify.shell.sideQuote.text")}>`.
- **D-30:** Add centered 56×56 status badge icon. Branch per `status` state (`Mail` / `Loader2` / `CheckCircle2` / `AlertCircle` from `lucide-react`). `text-center` on CardContent.
- **D-31:** Current 3 variants (`loading` / `success` / `error`) acceptable — oracle's `pending` variant is sign-up success-branch territory which already exists. No new variant.

#### B5 — Setup screen

- **D-32:** Wrap `SetupForm.tsx` in `<AuthShell sideTitle={t("end-user.setup.shell.sideTitle.text")} sideQuote={t("end-user.setup.shell.sideQuote.text")}>`. NO other internal changes — D-20 deviation stands (no oracle for setup screen).

#### B6 — UICONF-05 axe baseline + visual regression

- **D-33:** **(a) JSON baseline artifact** — extend `apps/web/tests/e2e/fixtures/axe.ts` `runAxe()` to optionally write `tests/e2e/__axe-baselines__/<screen>.json` when `AXE_UPDATE_BASELINE=1` is set, and assert no-regression diff on normal runs. Call sites: `await runAxe(page, "u1-sign-in")` etc. Commit baselines for u1/u2/u3 + new u-setup.
- **D-34:** **(b) Visual regression** — NEW `apps/web/tests/e2e/auth-shell-visual.spec.ts` using Playwright built-in `toHaveScreenshot()` (zero new deps — `@playwright/test@1.60.0` already in devDeps). 4 screens × full-page screenshot. `maxDiffPixelRatio: 0.01`. Mask `[data-testid='locale-switcher']`. Snapshots committed under `tests/e2e/auth-shell-visual.spec.ts-snapshots/`.
- **D-35:** Bake baselines via `pnpm --filter @openwhispr/web exec playwright test auth-shell-visual --update-snapshots` AFTER all B1-B5 fixes land.
- **D-36:** Do NOT add Chromatic — violates "ZERO new deps" constraint.

### Plan Split & Wave Structure (per A4 advisor)

- **D-37:** Use **Option B — 6 mid-granular plans**:
  - **18.1.1-01** Bucket A path-fix codemod (10-12 commits via cluster RED+GREEN; cluster-by-source-tree per A4)
  - **18.1.1-02** Bucket A pkg/transform (wire-schemas + esbuild/oxc; 2 commits)
  - **18.1.1-03** Bucket A testcontainer + misc (otel/lint-rls/pg-role); 4-6 commits
  - **18.1.1-04** Bucket B AuthShell scaffold + sign-in + sign-up (~6 commits)
  - **18.1.1-05** Bucket B verify-email + setup + UICONF-05 axe baseline + visual regression (~5 commits)
  - **18.1.1-06** ROADMAP/STATE sync + v2.1 milestone declaration (1 atomic commit)
- **D-38:** **Wave graph (parallel parts marked):**
  ```
  Wave 1 (parallel): 18.1.1-01 ‖ 18.1.1-02 ‖ 18.1.1-04
  Wave 2:            18.1.1-03 (depends on 01,02)
                     18.1.1-05 (depends on 04)
  Wave 3:            18.1.1-06 (depends on 01..05)
  ```
- **D-39:** **TDD commit shape for mass codemod** (D-02 cluster pairs): all Bucket A path-fix failures are pre-existing RED on `main` → single GREEN commit per cluster with verbatim error output in commit body (per Phase 18.1 §7 atomic-commit doctrine). For new functionality (lint extension D-05, await-AST linter D-07, dep-graph linter D-06, testcontainer reaper test D-08, AuthShell component D-13, baseline infra D-33-34): true 2-commit RED+GREEN pair per D-28.
- **D-40:** Estimated atomic-commit count: **~28-32**. Runtime estimate: **~4-4.5 hours**.

### Constitutional / Process

- **D-41:** ZERO `--no-verify`. HALT-and-escalate on any unexpected lefthook fire (especially `english`, `phase-tag-comments`, `web-typecheck`, `colocated-tests` after D-05 extension).
- **D-42:** Coverage ≥ 90/90/90/90 on diff per CLAUDE.md rule #2. For path-fix-only commits: coverage delta on changed test files only (no prod code touched). For Bucket B: AuthShell + 4 screens new code, must hit coverage floor.
- **D-43:** i18n en+ru parity in same atomic commit (Bucket B i18n keys per A3 §7).
- **D-44:** ZERO new top-level deps. shadcn `checkbox` install vendors source (Radix already at `radix-ui` package.json:33). `lucide-react` already line 30. `@axe-core/playwright` + `@playwright/test` already lines 43-44.
- **D-45:** Verifier gate (end-of-phase): aggregate `pnpm test` exits 0 across all 296 files; UICONF-05 axe + visual baselines green; ROADMAP table synced; STATE.md milestone status updated to `closed` (was `closed-with-followup`); v2.1 milestone declared per A4 D-39.

### Scope guards

- **D-46:** If executor encounters oracle divergence beyond SR-B1..B6, HALT and escalate — do not silently expand.
- **D-47:** If `/terms` or `/privacy` routes don't exist for D-26, scope-out the terms checkbox to deferred-items (do not author placeholder pages).
- **D-48:** Pre-existing `.test.ts` failures NOT in Bucket A inventory: capture but DO NOT fix in this phase (carve out to Phase 18.1.2 if any surface).

### Claude's Discretion

- HALT recipes (testcontainer cluster, oracle divergence, missing prereqs) follow Phase 18.1-06 three-branch user-choice pattern.
- Per-plan SUMMARY artefacts mirror Phase 18.1 shape (≤ 100 LOC each, executor-authored).
- Final ROADMAP edits include v2.1 milestone declaration (was CLOSED-WITH-FOLLOWUP after Phase 18.1; this phase brings to clean CLOSED).
- `tools/lint-colocated-tests.ts` extension (D-05) adds defensive rule that prevents future STRUCT-style regressions — high-value durable artefact.
- Mobile-first AuthShell (D-13) per A3 §6 — side panel hidden on `<lg`; oracle was desktop-mocked but mobile parity is implicit constitutional UX rule.
- 4 advisors all returned within the same wall-clock window; bounce loop budget tight.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 18.1 precedents
- `.planning/phases/18.1-v2-test-debt-closure/18.1-CONTEXT.md` (D-01..D-30 — patterns to inherit)
- `.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md` §1, §7 (path-rewrite canonical pattern)
- `.planning/phases/18.1-v2-test-debt-closure/18.1-PATTERNS.md` (analog mapping playbook)
- `.planning/phases/18.1-v2-test-debt-closure/18.1-PLAN-CHECK-2.md` (plan-check rigor)
- `.planning/phases/18.1-v2-test-debt-closure/18.1-07-SUMMARY.md` (closure shape + 14-failure carve-out)

### Bucket A — System test debt
- `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` (canonical cross-tree path pattern reference)
- `apps/api/src/routes/agent/__fixtures__/` (real fixture location; 7 .sse files)
- `apps/api/src/routes/tokens/__fixtures__/` (real token fixture location; 3 .json files)
- `apps/api/src/i18n/locales/{en,ru}.json` (real i18n location)
- `apps/api/src/index.ts` (real api entrypoint)
- `packages/data/src/migrate.ts` (real migrate location)
- `packages/data/migrations/` (real migrations directory)
- `tools/lint-colocated-tests.ts` (extension target for D-05 `findStalePathLiterals`)
- `packages/contract-tests/package.json` (D-06 dep-graph fix)
- `packages/wire-schemas/package.json` (exports map; resolves via pnpm hoist)
- `packages/contract-tests/tests/unit/transcriptions.test.ts:131,175` (D-07 await-in-non-async sites)
- `tools/global-vitest-teardown.ts` (D-08 reaper extension)
- `apps/api/vitest.setup.ts:1-14` (D-08 promote-to-shared source)
- `apps/worker/vitest.config.ts` + `packages/data/vitest.config.ts` (D-08 wire-up targets)
- `feedback_testcontainers_cleanup_audit.md` (operator memory)

### Bucket B — UICONF auth drift
- `.planning/phases/07-frontend-ui-spec/design/screens-user.jsx:7-260` (`ScreenSignIn`/`ScreenSignUp`/`ScreenVerify` oracle)
- `.planning/phases/07-frontend-ui-spec/design/ui.jsx:229-316` (`AuthShell` oracle)
- `apps/web/src/components/screens/auth/SignInForm.tsx` (drift locus: 114, 189-191, 197-198)
- `apps/web/src/components/screens/auth/SignUpForm.tsx` (drift locus: 81, 96, 177-178)
- `apps/web/src/components/screens/auth/VerifyEmailClient.tsx` (drift locus: 65, 78, 94)
- `apps/web/src/components/screens/auth/SetupForm.tsx:5-9` (D-20 deviation locked)
- `apps/web/src/components/screens/auth/OidcButtons.tsx:66-80` (size/variant fix targets)
- `apps/web/src/app/(public)/layout.tsx:17-23` (centered-wrapper drop target)
- `apps/web/src/locales/en/end-user.json:420-525` (existing i18n keys; extend per A3 §7)
- `apps/web/tests/e2e/fixtures/axe.ts:15-19` (UICONF-05 baseline extension target)
- `apps/web/tests/e2e/u1-sign-in.spec.ts:66-70` (existing axe call site)
- `apps/web/package.json:30,33,43-44` (deps verification — no new top-level deps)
- `apps/web/src/components/ui/` (NO checkbox present — `npx shadcn@latest add checkbox` required)

### Constitutional / Process
- `.planning/PROJECT.md` §Engineering Discipline rules 1-10
- CLAUDE.md root (НЕ УПРОЩАТЬ, НЕ МОКАТЬ, no `--no-verify`, no `--legacy`)
- `.planning/V2-MILESTONE-REVIEW.md` (top-7 followups; UICONF-05 axe gap recorded)

### Stack pinning
- Next.js 15.5.18, React 19.1.0, Tailwind 4, shadcn/ui v2 (Phase 14 stack — DO NOT bump)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`apps/api/tests/unit/lib/sse-parser.test.ts:17-19`** — canonical cross-tree readFileSync pattern. ALL Bucket A path-fix sites use the same shape (`resolve(HERE, "..", "..", "..", "src", "<dir>", "<file>")`).
- **`apps/api/vitest.setup.ts`** — installSignalHook() pattern. Promote to `tools/testcontainer-reaper-setup.ts` (D-08).
- **`tools/global-vitest-teardown.ts:30-46`** — docker container prune by label. Extend with session-id filter (D-08).
- **`apps/web/tests/e2e/fixtures/axe.ts:15-19`** — runtime axe call already in place. Extend with baseline JSON write (D-33).
- **`@playwright/test@1.60.0`** built-in `toHaveScreenshot()` — visual regression without Chromatic (D-34).
- **`lucide-react` icons** (`Eye`/`EyeOff`/`Mail`/`Loader2`/`CheckCircle2`/`AlertCircle`) — already in deps (D-20, D-30).
- **`radix-ui`** package — already in deps; shadcn `checkbox` is Radix wrapper, no top-level dep bump.

### Established Patterns

- **TDD atomic-commit doctrine** (Phase 18.1 §7): pre-existing RED failures → single GREEN commit with verbatim error in body. New code → 2-commit RED+GREEN pair (D-28 literal honor).
- **HALT-and-escalate** (Phase 18.1-06): operator-recipe-style halt with 3-branch user choice (operator clears + retries / pin reuse=false / defer to sub-phase).
- **i18n parity en+ru** in same atomic commit (Phase 14 D-i18n-01).
- **`@expected-red @after-phase-N` regex** allows sub-phase numbering (Phase 18.1 D-29 regex extension); Phase 19.1 carries SR-B5 forgot-password.
- **shadcn install** = vendor source (no top-level dep), `npx shadcn@latest add <component>`.
- **AuthShell mobile-first**: side panel hidden `<lg` is implicit constitutional UX rule (no explicit decision but consistent with Tailwind 4 mobile-first).

### Integration Points

- **`pnpm test` exit-0 gate** at end-of-phase (D-45).
- **UICONF-05 axe baseline file** committed under `apps/web/tests/e2e/__axe-baselines__/` (D-33).
- **Playwright visual snapshots** committed under `apps/web/tests/e2e/auth-shell-visual.spec.ts-snapshots/` (D-34).
- **`tools/lint-colocated-tests.ts` `findStalePathLiterals`** wired into pre-commit hook (D-05).
- **`tools/lint-await-in-non-async.ts`** wired into `tools` vitest project (D-07).
- **`tools/__tests__/dep-graph.test.ts`** for contract-tests workspace dep correctness (D-06).
- **Better Auth `signIn.email` `rememberMe` param** carries D-21 checkbox state.

</code_context>

<specifics>
## Specific Ideas

- Mobile AuthShell: side panel hidden `<lg`, form fills viewport. Side panel `relative` with absolute footer links + version (matches oracle `ui.jsx:229-316`).
- Password show/hide eye toggle: positioned absolute, `right: 5, top: 5, height: 30, width: 30` per oracle (Tailwind: `absolute right-1.5 top-1.5 size-8`).
- Password strength meter (sign-up): 4 score bands (0-25 / 26-50 / 51-75 / 76-100) → "Weak" / "Fair" / "Good" / "Strong" + colors (red-500/orange-500/yellow-500/green-500). Inline `passwordStrength(value: string): {score: number; label: string}` helper.
- "Remember this device" checkbox: pre-checked? Oracle shows unchecked default. Better Auth `rememberMe` default also false. Keep unchecked.
- D-UX2 forgot-password sentinel: literal comment `// Source of truth: D-UX2 (Phase 18.1.1) — anchor lands with Phase 19.1 reset-mail` colocated with the muted text JSX.

</specifics>

<deferred>
## Deferred Ideas

- **Phase 18.1.2** — if Bucket A surfaces new pre-existing failures NOT in D-02 inventory (per A4 SR-B5 scope guard).
- **Phase 19.1/19.2/19.3/19.4** — real downstream code for repointed @cjm tags (Phase 18.1 D-11..D-14 carve-out stands).
- **Terms + Privacy pages** — if `/terms` or `/privacy` routes don't exist (D-26 scope-out).
- **A2 Option C two-tier rate-limit** — v3 architectural cleanup (Phase 18.1 deferred).
- **Chromatic** — rejected per zero-new-deps constraint (D-36).
- **Confirm-password field on sign-up** — i18n key dormant; oracle doesn't show; leave.
- **Operator-side**: FSL history scrub (15-04), first real GHA e2e-cjm CI run, DCO bot wiring.
- **`pnpm test:lint-cjm-doc` script wiring** — Phase 18.1-05 deviation (planner can wire if trivial; otherwise defer).
- **Setup screen oracle** — D-20 deviation already documented; not in scope to author retrospective oracle.

### Reviewed Todos (not folded)
- None from cross_reference_todos step.

</deferred>

---

*Phase: 18.1.1-aggregate-sweep-test-debt*
*Context gathered: 2026-05-15*
