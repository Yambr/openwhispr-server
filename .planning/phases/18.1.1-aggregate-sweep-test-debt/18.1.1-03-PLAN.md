---
phase: 18.1.1-aggregate-sweep-test-debt
plan: 03
type: execute
wave: 2
depends_on: [18.1.1-01, 18.1.1-02]
files_modified:
  - tools/testcontainer-reaper-setup.ts
  - apps/api/vitest.setup.ts
  - apps/worker/vitest.config.ts
  - packages/data/vitest.config.ts
  - tools/global-vitest-teardown.ts
  - tools/lint-rls.test.ts
  - packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts
  - packages/data/tests/unit/__tests__/role-bootstrap.test.ts
  - packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts
  - packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts
autonomous: true
requirements:
  - SR-A4
  - SR-A6
  - SR-A7
must_haves:
  truths:
    - "`tools/testcontainer-reaper-setup.ts` is the shared reaper-helper, 14-line body matching `apps/api/vitest.setup.ts:1-14`."
    - "`apps/worker/vitest.config.ts` and `packages/data/vitest.config.ts` declare `setupFiles: ['../../tools/testcontainer-reaper-setup.ts']` (pitfall §1 — mergeConfig doesn't inherit setupFiles)."
    - "`apps/api/vitest.setup.ts` imports from the shared file OR is replaced by direct reference."
    - "`tools/global-vitest-teardown.ts` PRUNE_ARGV includes `--filter label=org.testcontainers.session-id=${process.pid}` for narrower blast radius."
    - "`tools/lint-rls.test.ts` failure mode classified (executor runs failing test, captures error). IF path-bug → folded into 18.1.1-01 (re-open); IF logic bug → standalone fix commit here (`fix(18.1.1-03-01)`)."
    - "`packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts` extracts inline `CREATE ROLE openwhispr_owner/_app` SQL from `pgbouncer-interleave.test.ts:101-110` + `migration-0006-backfill.test.ts:54-57` into idempotent helper (Δ-1 reshape — NO globalSetup wire-up since no compose/postgres/init-scripts/ exists)."
    - "Helper is idempotent via `DO $$ BEGIN … EXCEPTION WHEN duplicate_object THEN NULL; END $$;` per pitfall §7 (Postgres pre-16 has no CREATE ROLE IF NOT EXISTS)."
    - "Atomic commit count: 3-4 (reaper extract + session-id hardening + role-helper extract + optional lint-rls fix)."
    - "If executor encounters orphan testcontainers post-run → HALT per D-09 (3-branch operator recipe)."
    - "ZERO `--no-verify` (D-41)."
  artifacts:
    - path: "tools/testcontainer-reaper-setup.ts"
      provides: "Shared reaper-setup module for cross-workspace vitest configs."
      contains: "installSignalHook()"
    - path: "packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts"
      provides: "Idempotent CREATE ROLE helper for migration test bootstrap."
      contains: "EXCEPTION WHEN duplicate_object"
    - path: "tools/global-vitest-teardown.ts"
      provides: "Session-id label filter on testcontainer prune."
      contains: "org.testcontainers.session-id"
  key_links:
    - from: "apps/worker/vitest.config.ts + packages/data/vitest.config.ts"
      to: "tools/testcontainer-reaper-setup.ts"
      via: "`setupFiles: ['../../tools/testcontainer-reaper-setup.ts']` (pitfall §1 explicit re-declare)."
      pattern: "testcontainer-reaper-setup"
    - from: "role-bootstrap.ts"
      to: "pgbouncer-interleave.test.ts + migration-0006-backfill.test.ts"
      via: "Call `await bootstrapRoles(superPool)` in place of inline CREATE ROLE SQL."
      pattern: "bootstrapRoles"
---

<objective>
Close Bucket A misc fixes (Wave 2, depends on 18.1.1-01 path-fix completion + 18.1.1-02 transform fixes so vitest can actually start):

- **D-08 testcontainer reaper promotion** (2 commits — extract + session-id hardening).
- **D-11 lint-rls** (1 commit IF logic bug; else fold into 18.1.1-01 retroactively).
- **D-12 postgres CREATEROLE** (1 commit per Δ-1 — extract inline SQL to test-helper, NOT globalSetup wire-up).

**3-4 atomic commits** depending on D-11 disposition.

Wave 2 (after 18.1.1-01 and 18.1.1-02 complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-RESEARCH.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-PATTERNS.md
@apps/api/vitest.setup.ts
@apps/worker/vitest.config.ts
@packages/data/vitest.config.ts
@tools/global-vitest-teardown.ts
@tools/lint-rls.test.ts
@packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts
@packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts
@packages/data/migrations/__tests__/0017-setup-state.test.ts
@feedback_testcontainers_cleanup_audit.md
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18.1.1-03-01: D-08 reaper-helper promote + wire workspace configs (RED+GREEN pair)</name>
  <files>tools/testcontainer-reaper-setup.ts, apps/api/vitest.setup.ts, apps/worker/vitest.config.ts, packages/data/vitest.config.ts</files>
  <behavior>
- Test 1 (RED): integration test asserts that running `pnpm --filter @openwhispr/worker test` registers a signal hook (proxy: spy on process.on or verify `tools/testcontainer-reaper-setup.ts` was loaded as setupFile). Initially fails because worker config has no setupFiles.
- Test 2: same for `packages/data`.
- Test 3: `apps/api/vitest.setup.ts` references shared file (no duplicate body).
  </behavior>
  <action>
Per D-08 + PATTERNS A5 + pitfall §1.

**RED step:**
1. Add integration test `tools/testcontainer-reaper-setup.test.ts` that asserts each workspace config's `setupFiles` array contains the shared reaper path. Walk each config file via `readFileSync` + regex `setupFiles.*testcontainer-reaper-setup`.
2. Run test — must FAIL (worker + data configs lack setupFiles per RESEARCH §2).
3. Commit RED: `test(18.1.1-03-01): red — workspace vitest configs missing shared reaper setupFile`. Body has RED log.

**GREEN step:**
4. Create `tools/testcontainer-reaper-setup.ts` (bare SPDX header per pitfall §11): copy 14-line body from `apps/api/vitest.setup.ts:1-14` — `import { installSignalHook } from "./global-vitest-teardown"; installSignalHook();`.
5. Update `apps/api/vitest.setup.ts`: replace body with `import "../../tools/testcontainer-reaper-setup";` (or change root `vitest.config.ts` setupFiles to point directly at the shared file).
6. Update `apps/worker/vitest.config.ts`: inside `mergeConfig(rootConfig, { test: { … } })` add `setupFiles: ["../../tools/testcontainer-reaper-setup.ts"]`. Reaper MUST be FIRST in array (pitfall §5: Ryuk handshake order).
7. Update `packages/data/vitest.config.ts`: same setupFiles addition.
8. Re-run RED test — must GREEN.
9. Run `pnpm --filter @openwhispr/worker test 2>&1 | head -50` AND `pnpm --filter @openwhispr/data test 2>&1 | head -50` — expect no "no such file" on the setupFile path.
10. Commit GREEN: `fix(18.1.1-03-01): green — promote reaper-helper to tools/ + wire worker+data setupFiles`. Body cites pitfall §1.

WHY: pitfall §1 — `mergeConfig` does NOT auto-inherit `setupFiles` arrays; explicit re-declare mandatory in each child config.
  </action>
  <verify><automated>grep -l "testcontainer-reaper-setup" apps/worker/vitest.config.ts packages/data/vitest.config.ts && pnpm exec vitest run tools/testcontainer-reaper-setup.test.ts 2>&1 | tail -5</automated></verify>
  <done>2 commits (test:red + fix:green); shared reaper present; 2 workspace configs reference it; api config refactored to import shared; coverage on new helper ≥ 90/90/90/90.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-03-02: D-08 session-id label hardening (RED+GREEN pair)</name>
  <files>tools/global-vitest-teardown.ts, tools/global-vitest-teardown.test.ts</files>
  <behavior>
- Test 1 (RED): `PRUNE_ARGV` array contains `--filter label=org.testcontainers.session-id=${process.pid}`. Initially fails (filter absent).
- Test 2: prune is idempotent (re-invocation does not error).
- Test 3: existing label filter (`org.testcontainers.lang=node`) preserved.
  </behavior>
  <action>
Per D-08 + PATTERNS A5 + RESEARCH §2.

**RED step:**
1. Extend (or create) `tools/global-vitest-teardown.test.ts`: assert PRUNE_ARGV contains both existing filter + new session-id filter. Must FAIL initially.
2. Commit RED: `test(18.1.1-03-02): red — PRUNE_ARGV lacks session-id filter`.

**GREEN step:**
3. Edit `tools/global-vitest-teardown.ts` (lines 28-46 per PATTERNS A5): extend PRUNE_ARGV with second `--filter` arg pair: `"--filter", \`label=org.testcontainers.session-id=${process.pid}\``. Keep existing `try/catch` swallow-errors discipline at L36-45.
4. Re-run test — GREEN.
5. Commit GREEN: `fix(18.1.1-03-02): green — narrow prune to session-id label`. Body cites `feedback_testcontainers_cleanup_audit.md`.

WHY: feedback_testcontainers_cleanup_audit.md — current prune is wide; session-id narrowing prevents cross-test-run prune of unrelated containers.
  </action>
  <verify><automated>pnpm exec vitest run tools/global-vitest-teardown.test.ts 2>&1 | tail -5 && grep -c "session-id" tools/global-vitest-teardown.ts</automated></verify>
  <done>2 commits; session-id filter present; existing filter preserved; coverage ≥ 90/90/90/90.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-03-03: D-11 lint-rls classify-and-fix (conditional, 0-1 commits)</name>
  <files>tools/lint-rls.test.ts</files>
  <action>
Per D-11 (classify-then-act).

1. Run `pnpm exec vitest run tools/lint-rls.test.ts -t "exit 0" --reporter=verbose 2>&1 | tee /tmp/03-03-classify.log`.
2. Inspect output:
   - **Path-bug indicators**: `ENOENT`, `Cannot find module`, `no such file`. → re-open 18.1.1-01 (Cluster 10 or 11) to add this file. NO commit here. Note in SUMMARY: "Cluster 10/11 should have included lint-rls.test.ts — folded retroactively."
   - **Logic-bug indicators**: Drizzle/pg ESM resolution failure, Vitest 4 transformer error, testcontainer SDK init failure. → fix here.
   - **Pre-cluster-1 abort indicator**: prior failure aborts whole project. → already fixed by 18.1.1-01 path-fixes. Re-run; if now GREEN, no action.
3. IF logic-bug surfaces requiring fix here: capture verbatim RED, surgically fix root cause (most likely: pin Drizzle/pg version constraint or wrap `await import("@testcontainers/postgresql")` in try-with-helpful-error), commit `fix(18.1.1-03-03): green — lint-rls.test.ts <root-cause-shortname>` with verbatim RED in body.
4. IF NO action needed (path or pre-abort): SUMMARY records classification + no commit.

WHY: pitfall §1 + RESEARCH §3 — empirically path math in lint-rls.test.ts is correct; most likely root cause is pre-cluster-1 abort already cleared by Wave-1 plan 01. Conditional task = honor D-11 classify-then-act.
  </action>
  <verify><automated>pnpm exec vitest run tools/lint-rls.test.ts 2>&1 | tail -10</automated></verify>
  <done>EITHER 0 commits with classification note OR 1 commit with verbatim RED proof + root-cause citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-03-04: D-12 postgres role-bootstrap helper extract (RED+GREEN pair, Δ-1 reshape)</name>
  <files>packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts, packages/data/tests/unit/__tests__/role-bootstrap.test.ts, packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts, packages/data/tests/unit/__tests__/migration-0006-backfill.test.ts</files>
  <behavior>
- Test 1 (RED): standalone test asserts `pg_has_role('openwhispr_app','CREATEROLE','member')` against fresh testcontainer after `bootstrapRoles(superPool)`. Initially fails — helper doesn't exist.
- Test 2: idempotency — `bootstrapRoles` called twice does not error.
- Test 3: `pgbouncer-interleave.test.ts` post-refactor calls `bootstrapRoles(superPool)` instead of inline SQL.
  </behavior>
  <action>
Per D-12 + RESEARCH §9 Δ-1 + PATTERNS A6 + pitfall §7.

**Δ-1 reshape:** NO `compose/postgres/init-scripts/` exists. Bootstrap is INLINE per-test (pgbouncer-interleave.test.ts:101-110, migration-0006-backfill.test.ts:54-57). Fix is helper extraction, NOT globalSetup wire-up.

**RED step:**
1. Create `packages/data/tests/unit/__tests__/role-bootstrap.test.ts`:
   - Spin up `PostgreSqlContainer().start()`.
   - Connect as `postgres` (superuser).
   - Import `bootstrapRoles` from `./_helpers/role-bootstrap` (will fail).
   - `await bootstrapRoles(pool)`.
   - Assert `SELECT pg_has_role('openwhispr_app','CREATEROLE','member')` returns true.
   - Assert idempotency (second call no-op).
   - Stop container.
2. Run test — must FAIL (helper missing).
3. Commit RED: `test(18.1.1-03-04): red — role-bootstrap helper missing; CREATEROLE assertion ENOENT-on-import`.

**GREEN step:**
4. Create `packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts`:
   ```ts
   import type { Pool } from "pg";
   export async function bootstrapRoles(superPool: Pool): Promise<void> {
     await superPool.query(`
       DO $$ BEGIN
         CREATE ROLE openwhispr_owner WITH LOGIN BYPASSRLS CREATEROLE PASSWORD 'owner-pw';
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;`);
     await superPool.query(`
       DO $$ BEGIN
         CREATE ROLE openwhispr_app WITH LOGIN CREATEROLE PASSWORD 'app-pw';
       EXCEPTION WHEN duplicate_object THEN NULL; END $$;`);
     await superPool.query(`GRANT openwhispr_app TO openwhispr_owner WITH ADMIN OPTION`);
   }
   ```
   (Note CREATEROLE on `openwhispr_app` per PATTERNS A6 risk — test parity may drift from prod; document in commit body.)
5. Refactor `pgbouncer-interleave.test.ts:101-110` to `await bootstrapRoles(superPool)` instead of inline SQL.
6. Refactor `migration-0006-backfill.test.ts:54-57` same.
7. Run all three: `pnpm --filter @openwhispr/data test role-bootstrap pgbouncer-interleave migration-0006-backfill`. GREEN.
8. Commit GREEN: `fix(18.1.1-03-04): green — extract role-bootstrap helper (Δ-1 reshape) + idempotent + CREATEROLE`. Body cites Δ-1 + PATTERNS A6 + pitfall §7 + prod-drift caveat.

WHY: pitfall §7 — Postgres pre-16 lacks `CREATE ROLE IF NOT EXISTS`; exception-swallow `DO $$ EXCEPTION WHEN duplicate_object` is canonical idempotent shape. Δ-1 — globalSetup wire-up is wrong target since no init-scripts dir exists.
  </action>
  <verify><automated>pnpm --filter @openwhispr/data test role-bootstrap 2>&1 | grep -E "(✓|PASS|CREATEROLE)" | head -5 && grep -c "bootstrapRoles" packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts</automated></verify>
  <done>2 commits; helper exists + tested + used by 2 test files; idempotent; CREATEROLE assertion passes; coverage on helper ≥ 90/90/90/90; prod-drift caveat documented.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
cd /Users/nick/openwhispr-server

# Reaper wire-up
grep -l "testcontainer-reaper-setup" apps/worker/vitest.config.ts packages/data/vitest.config.ts apps/api/vitest.setup.ts
pnpm --filter @openwhispr/worker test 2>&1 | head -20
pnpm --filter @openwhispr/data test 2>&1 | head -20

# Session-id label
grep "session-id" tools/global-vitest-teardown.ts

# Role-bootstrap helper
pnpm --filter @openwhispr/data test role-bootstrap pgbouncer-interleave migration-0006-backfill
grep -c "bootstrapRoles" packages/data/tests/unit/__tests__/{pgbouncer-interleave,migration-0006-backfill}.test.ts

# Testcontainer hygiene — POST-test docker check (D-09 HALT recipe)
docker ps -a --filter "label=org.testcontainers" --format "{{.Names}}\t{{.Status}}" # expect empty

# Commit count: 3-4 (depending on D-11 disposition)
git log --oneline -n 8 | grep -cE "(fix|test|chore)\(18\.1\.1-03-0[1-4]\)"
```
</verification>

<success_criteria>
1. Shared reaper-helper present + wired into 2 workspace configs (D-08).
2. session-id label filter present in PRUNE_ARGV.
3. lint-rls classified; commit issued IFF logic-bug surfaced.
4. role-bootstrap helper extracted; pgbouncer-interleave + migration-0006-backfill refactored to use it; CREATEROLE assertion GREEN.
5. Coverage ≥ 90/90/90/90 on all new helpers (D-42).
6. Δ-1 reshape honored (helper extraction, NOT globalSetup).
7. ZERO `--no-verify` (D-41).
8. POST-run `docker ps -a` shows zero orphan testcontainers OR HALT recipe emitted (D-09).
</success_criteria>

<rollback>
- D-09 HALT recipe (testcontainer orphans post-run):
  - **Branch A**: operator runs `docker system prune -af --volumes` + Docker Desktop restart + re-run.
  - **Branch B**: pin testcontainers `reuse=false` for all suites in this plan; document in SUMMARY.
  - **Branch C**: carve to Phase 18.1.2 (cleanup hygiene as own phase).
- If `bootstrapRoles` CREATEROLE on `_app` breaks prod-parity tests (PATTERNS A6 risk): split helper into `bootstrapMinimalRoles` (no CREATEROLE) + `grantCreateRoleForTesting` (CREATEROLE) — call latter only when D-12 RED test runs.
</rollback>

<output>
Create `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-03-SUMMARY.md` with:
- Commit SHAs (3-4 depending on D-11)
- D-11 classification result + disposition
- Δ-1 citation
- POST-run docker ps result
- Coverage report on new helpers
- HALT branch chosen (if any)
</output>
</content>
</invoke>