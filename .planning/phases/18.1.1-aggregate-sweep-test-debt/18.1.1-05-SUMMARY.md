---
phase: "18.1.1"
plan: "05"
subsystem: web
status: complete-with-deferred-bake
completed: 2026-05-15
---

# Phase 18.1.1 Plan 05: Bucket B verify-email + setup + axe baseline + visual regression — Summary

## One-liner

Wrapped `VerifyEmailClient` + `SetupForm` in `AuthShell` with per-state status badge and en+ru parity copy; extended `runAxe()` with optional JSON baseline write/read; added Playwright visual-regression spec for the four auth-shell screens (RED only — baselines deferred until operator runs against live stack).

## Commits

| Task  | Type    | SHA      | Summary                                            |
| ----- | ------- | -------- | -------------------------------------------------- |
| 05-01 | test    | d473cb3  | RED — VerifyEmailClient AuthShell + status badge   |
| 05-01 | feat    | f76379f  | GREEN — VerifyEmailClient refactor                 |
| 05-02 | feat    | 4259672  | SetupForm AuthShell wrap (cosmetic D-20)           |
| 05-03 | test    | 376fef5  | RED — axe baseline helper                          |
| 05-03 | feat    | ef46081  | GREEN — compareOrWriteBaseline + wire call sites   |
| 05-05 | test    | bedb31c  | RED — auth-shell visual regression (baselines TBD) |

Total commits: 6 (Task 05-04 absorbed into 05-03; visual GREEN deferred).

## Decisions

- **D-29..D-31 honoured**: VerifyEmailClient now wraps every status branch in `<AuthShell>` with localized `sideTitle` / `sideQuote`, renders a 56×56 centered status badge (Mail / Loader2 / CheckCircle2 / AlertCircle) with `data-testid="status-badge"`, and applies `text-center` on every CardContent.
- **D-32**: SetupForm gets a bare cosmetic `<AuthShell>` wrapper only — no internal wizard layout changes, per D-20 (the Phase-07 oracle never produced a `/setup` JSX template, so the shell-only deviation stays minimal).
- **D-33**: `runAxe(page, screenId?)` now reads/writes `tests/e2e/__axe-baselines__/<screenId>.json` gated by `AXE_BASELINE=1` + `AXE_UPDATE_BASELINE=1`. Helper extracted to `src/lib/axe-baseline.ts` so vitest can unit-test the JSON read/write without booting Playwright.
- **D-34**: Visual-regression spec authored with `toHaveScreenshot()` × 4 (sign-in, sign-up, verify-email-error, setup); `maxDiffPixelRatio: 0.01`; locale switcher masked; `fullPage: true`. Stays RED until baselines baked.
- **D-43**: i18n en+ru parity for new `end-user.verify.shell.{sideTitle,sideQuote}` and `end-user.setup.shell.{sideTitle,sideQuote}` keys — added in the same atomic commit as the consumer.
- **D-44**: ZERO new top-level dependencies (`lucide-react` was already a project dep via shadcn/ui).

## Deviations from plan

### Tasks 05-04 (axe baseline bake) and 05-05 GREEN (visual baseline bake) deferred

**Issue:** Both baking steps require a live `docker compose` stack (web at `https://localhost/sign-in` etc.). The session host's docker daemon is running unrelated containers; the OpenWhispr stack is not booted, and starting it is out of scope for this executor spawn.

**Action:** Plan 05's RED→GREEN test commits are complete; the baseline JSON files (Task 05-04) and the visual snapshot PNGs (Task 05-05 GREEN) are intentionally absent. Operators bake by running:

```bash
AXE_BASELINE=1 AXE_UPDATE_BASELINE=1 pnpm --filter @openwhispr/web exec playwright test
pnpm --filter @openwhispr/web exec playwright test auth-shell-visual --update-snapshots
```

once the stack is up, then commit the resulting fixtures.

**Rule classification:** Documented + deferred (no Rule 1-4 deviation).

### Task 05-03 colocated-tests pivot

**Issue:** First-draft RED test landed at `apps/web/src/lib/__tests__/axe-baseline.test.ts` — caught by `tools/lint-colocated-tests.ts` because the rule only allows `.test.tsx` siblings, not `.test.ts`.

**Action (Rule 3 — blocking fix):** Moved the test to `apps/web/tests/unit/lib/__tests__/axe-baseline.test.ts` (canonical workspace layout). No production-code impact.

## Files modified

- `apps/web/src/components/screens/auth/VerifyEmailClient.tsx` (refactored)
- `apps/web/src/components/screens/auth/SetupForm.tsx` (AuthShell wrap)
- `apps/web/src/components/screens/auth/__tests__/VerifyEmailClient.test.tsx` (+5 tests)
- `apps/web/src/components/screens/auth/__tests__/SetupForm.test.tsx` (+1 test)
- `apps/web/src/locales/en/end-user.json` (+ verify.shell, setup.shell)
- `apps/web/src/locales/ru/end-user.json` (+ verify.shell, setup.shell)
- `apps/web/src/lib/axe-baseline.ts` (new)
- `apps/web/tests/unit/lib/__tests__/axe-baseline.test.ts` (new)
- `apps/web/tests/e2e/fixtures/axe.ts` (signature + baseline wiring)
- `apps/web/tests/e2e/u1-sign-in.spec.ts` / `u2-sign-up.spec.ts` / `u3-verify-email.spec.ts` (screenId arg)
- `apps/web/tests/e2e/u-setup.spec.ts` (new)
- `apps/web/tests/e2e/auth-shell-visual.spec.ts` (new — RED)
- `apps/web/tests/e2e/__axe-baselines__/.gitkeep` (new)

## Tests

- **Unit:** VerifyEmailClient 10/10, SetupForm 22/22, axe-baseline 3/3 — all green.
- **E2E:** RED commits intentionally — baselines deferred.

## Self-Check: PASSED

- Six commits exist in git log (d473cb3..bedb31c).
- All listed files exist on disk.
- en+ru i18n parity for both new key blocks verified (`grep -c 'verify.shell\|setup.shell'`).
