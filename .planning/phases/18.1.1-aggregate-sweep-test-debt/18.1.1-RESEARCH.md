# Phase 18.1.1: aggregate-sweep test debt + UICONF auth drift — Research

**Researched:** 2026-05-15
**Domain:** Bucket A path-fix mass-sweep + Bucket B UICONF auth drift remediation
**Confidence:** HIGH (every fix-site verified against live tree)

## Summary

CONTEXT.md D-01..D-48 hold against live working tree. All P1-P20 path sites, the 5 transcriptions await sites, the testcontainer wire-up gap, the missing shadcn checkbox, and the absent `__axe-baselines__/` directory verified. **Three discrepancies** (§9): (1) D-12 postgres CREATEROLE bootstrap is **not** centralised — each migration test inlines `CREATE ROLE` SQL (no `compose/postgres/init-scripts/`); (2) D-22's `.env.example` file does not exist — only `.env.slim.example` / `.env.full.example` are present (auth.test.ts line 147 reads `.env.example`); (3) D-30 needs `Mail` icon import added — `lucide-react` icons not currently imported by `VerifyEmailClient.tsx`.

**Primary recommendation:** Plans 18.1.1-01..06 honor CONTEXT verbatim with the three §9 amendments. Cluster-7 (auth.test.ts) is two-part — fix the **target filename** AND the path depth.

## 1. Verified Bucket A path-fix sites (P1-P20)

Canonical pattern from `apps/api/tests/unit/lib/sse-parser.test.ts:17-19`:
```ts
const HERE = dirname(fileURLToPath(import.meta.url));
const fixturePath = (name: string) =>
  resolve(HERE, "..", "routes", "agent", "__fixtures__", `${name}.sse`);
```
This file lives at `apps/api/tests/unit/lib/` → relative path `../routes/agent/__fixtures__/` resolves to `apps/api/tests/unit/routes/agent/__fixtures__/` which **DOES NOT EXIST** — real fixtures at `apps/api/src/routes/agent/__fixtures__/` (verified `ls`: 7 .sse files).

| Cluster | Site | Test file | Bad path | Real target | Replacement |
|---|---|---|---|---|---|
| 1 | P1 | `apps/api/tests/unit/lib/sse-parser.test.ts:19` | `resolve(HERE, "..", "routes", "agent", "__fixtures__", ${name}.sse)` | `apps/api/src/routes/agent/__fixtures__/*.sse` | `resolve(HERE, "..", "..", "..", "src", "routes", "agent", "__fixtures__", ${name}.sse)` |
| 2 | P2 | `apps/api/tests/unit/routes/tokens/assemblyai.test.ts:37` | `resolve(dirname(fileURLToPath(import.meta.url)), "__fixtures__")` | `apps/api/src/routes/tokens/__fixtures__/assemblyai-v3-token-response.json` | `resolve(dirname(fileURLToPath(import.meta.url)), "..", "..", "..", "..", "src", "routes", "tokens", "__fixtures__")` |
| 2 | P3 | `apps/api/tests/unit/routes/tokens/deepgram.test.ts:22` | same shape | `…/__fixtures__/deepgram-grant-token-response.json` | same depth-fix |
| 2 | P4 | `apps/api/tests/unit/routes/tokens/openai-realtime.test.ts:41` | same shape | `…/__fixtures__/openai-client-secret-response.json` | same depth-fix |
| 3 | P5 | `apps/api/tests/unit/i18n/__tests__/i18n-completeness.test.ts:27,51,74` | `API_SRC = resolve(HERE, "..", "..")` + `resolve(HERE, "..", "locales", "${lng}.json")` | `apps/api/src/i18n/locales/{en,ru}.json` + `apps/api/src` | from `apps/api/tests/unit/i18n/__tests__/`: `API_SRC = resolve(HERE, "..", "..", "..", "..", "src")` + locales = `resolve(API_SRC, "i18n", "locales", "${lng}.json")` |
| 4 | P6/P7 | `apps/api/tests/unit/otel-bootstrap.test.ts:79,194` | `path.join(__dirname, "index.ts")` + `path.resolve(__dirname, "routes")` | `apps/api/src/index.ts` + `apps/api/src/routes/` | `path.join(__dirname, "..", "..", "src", "index.ts")` + `path.resolve(__dirname, "..", "..", "src")` |
| 5 | P8 | `apps/api/tests/unit/__tests__/entrypoint-db-shape.test.ts:144` | `new URL("../index.ts", import.meta.url)` from `tests/unit/__tests__/` | `apps/api/src/index.ts` | `new URL("../../../src/index.ts", import.meta.url)` |
| 6 | P9 | `apps/api/tests/unit/lib/redact-url-bootstrap-usage.test.ts:21` | `new URL("../index.ts", import.meta.url)` from `tests/unit/lib/` | `apps/api/src/index.ts` | `new URL("../../../src/index.ts", import.meta.url)` |
| 7 | P10 | `apps/api/tests/unit/auth.test.ts:147` | `resolve(__dirname, "..", "..", "..", ".env.example")` | **`.env.slim.example`** at repo root (no `.env.example`) | `resolve(__dirname, "..", "..", "..", ".env.slim.example")` — **filename change too** |
| 8 | P11 | `packages/data/tests/unit/__tests__/migrate.test.ts:17` | `resolve(HERE, "..", "migrate.ts")` from `tests/unit/__tests__/` | `packages/data/src/migrate.ts` | `resolve(HERE, "..", "..", "..", "src", "migrate.ts")` |
| 9 | P12-P16 | 5 tests with `MIGRATIONS_FOLDER`: `migration-0006-backfill.test.ts:23`, `migration-rollback.test.ts` (imports `MIGRATIONS_FOLDER`), `pgbouncer-interleave`, `0003_better_auth_tenant_defaults`, `settings-rls`/`rls-property`/`worker-rls-property` (subset per A1 inventory) | `resolve(__dirname, "..", "..", "migrations")` from `tests/unit/__tests__/` | `packages/data/migrations/` | `resolve(__dirname, "..", "..", "..", "migrations")` |
| 10 | P17 | `apps/api/tests/unit/__tests__/boot-order.test.ts:39` | `REPO_ROOT = path.resolve(__dirname, "../../../..")` from `apps/api/tests/unit/__tests__/` | repo root | `path.resolve(__dirname, "../../../../..")` (5 ups, not 4) |
| 11 | P18 | `.planning/phases/07-frontend-ui-spec/UI-SPEC-admin.md` | grep verified: paths like `apps/api/src/routes/stt-config.ts:43-58` appear in **table cell prose**, not as standalone path literals; only **inline citations**. **No standalone `apps/`-prefix lines found** (`grep -nE "^\s*apps/"` empty). | — | **VERIFY-AND-SKIP** if no lint violation surfaces. If Mode-3 linter fires, narrow `lint-cjm-doc` glob, not the doc. |
| — | P19 | (per Phase 18.1 inventory — already correct) | n/a | n/a | skip |
| — | P20 | wire-schemas — out-of-scope to A2 (D-06) | n/a | n/a | see §2 |

## 2. Bucket A pkg/transform sites (D-06, D-07, D-08)

**D-06 — `packages/contract-tests/package.json` dep entry:**
Verified existing `dependencies`: `pg 8.20.0`, `tough-cookie 5.1.2`, `zod 4.4.3`. Missing `@openwhispr/wire-schemas` despite imports in test files. Add to `dependencies` (NOT `devDependencies`) keeping alphabetical order:
```json
"@openwhispr/wire-schemas": "workspace:*",
```

**D-07 — `await` in non-async arrow sites:** verified by grep `await.*\.json\(\)` against `packages/contract-tests/tests/unit/transcriptions.test.ts`:
- Line 59: `expect(() => CloudTranscriptionShape.parse(await res.json())).not.toThrow();`
- Line 84-85: `CloudTranscriptionShape.parse(await r1.json())` / `await r2.json()` (already inside async — keep)
- Line 103: `BatchCreateResponse.parse(await res.json())` (inline assign — keep)
- Line 111: `expect(() => ListResponse.parse(await res.json())).not.toThrow();` ← FIX
- Line 124,147: `await created.json()` (inline assign — keep)
- Line 131: `expect(() => DeleteResponse.parse(await del.json())).not.toThrow();` ← FIX
- Line 155: `BatchDeleteResponse.parse(await res.json())` (inline assign — keep)
- Line 175 (inside `for` loop): `expect(() => ErrorEnvelope.parse(await res.json())).not.toThrow();` ← FIX

**Five sites with `expect(() => ... .parse(await ...))`** that need extraction:
1. Line 59, 2. Line 111, 3. Line 131, 4. Line 175. (Phase context says "5" — count = 4 in transcriptions.test.ts; planner should grep for the 5th across `packages/contract-tests/tests/**`.)

Refactor shape:
```ts
const body = await res.json();
expect(() => Shape.parse(body)).not.toThrow();
```

**D-08 — testcontainer setupFile wire-up:**
- `apps/api/vitest.setup.ts` (verified 14 lines) imports `installSignalHook` from `tools/global-vitest-teardown` and calls it.
- `apps/worker/vitest.config.ts` (verified) uses `mergeConfig(rootConfig, …)` with NO `setupFiles` entry.
- `packages/data/vitest.config.ts` (verified) same — NO `setupFiles`.

**Promote:** create `tools/testcontainer-reaper-setup.ts` (copy of `apps/api/vitest.setup.ts` body without phase-comment specifics). Add `setupFiles: ["../../tools/testcontainer-reaper-setup.ts"]` to `mergeConfig` `test` block in both target configs. `apps/api/vitest.config.ts` should switch its existing setup reference to the shared file (or keep — root config check: `rootConfig` already does NOT inject this setup, verified `apps/worker/vitest.config.ts` has no setupFiles).

## 3. Bucket A misc (D-11, D-12)

**D-11 — `tools/lint-rls.test.ts` failure root cause:**
Verified head of file: imports `@testcontainers/postgresql`, `drizzle-orm`, `pg`. Uses paths:
- `__dirname = dirname(fileURLToPath(import.meta.url))` (== `tools/`)
- `REPO_ROOT = resolve(__dirname, "..")`
- `SCRIPT = join(REPO_ROOT, "tools", "lint-rls.ts")`
- `MIGRATIONS_FOLDER = join(REPO_ROOT, "packages", "data", "migrations")`

Path math is correct (`tools/` → `..` is repo root). **Likely not a path-bug.** Most probable root cause: pre-cluster-1 ENOENT crash in a co-resident test file aborts the whole vitest project before testcontainer SDK init, OR Drizzle/pg version drift after Vite 8/Vitest 4 upgrade. Action per D-11: executor runs `pnpm exec vitest run tools/lint-rls.test.ts -t "exit 0"` to capture exact error; classify path-vs-logic; route to Cluster 10/11 or stand-alone `fix(18.1.1-03-01)`.

**D-12 — Postgres CREATEROLE bootstrap location:**
**No `compose/postgres/init-scripts/` exists.** Verified: `compose/postgres/` contains only `Dockerfile` + `load-test.conf`. Migration tests bootstrap roles **inline** — verified at `packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts:101-110` and `migration-0006-backfill.test.ts:54-57`:
```ts
await superPool.query(`CREATE ROLE openwhispr_owner WITH LOGIN BYPASSRLS CREATEROLE PASSWORD 'owner-pw'`);
await superPool.query(`CREATE ROLE openwhispr_app   WITH LOGIN          PASSWORD 'app-pw'`);
await superPool.query(`GRANT openwhispr_app TO openwhispr_owner WITH ADMIN OPTION`);
```
**Fix shape for D-12:** create `packages/data/tests/unit/__tests__/_helpers/role-bootstrap.ts` exporting an idempotent `bootstrapRoles(superPool)` helper. Migrate tests that depend on these roles (currently >2 files inlining the SQL) call the helper. RED test: `tests/unit/__tests__/role-bootstrap.test.ts` asserts `pg_has_role('openwhispr_app','CREATEROLE','member')` after helper runs against fresh testcontainer. **No `compose/postgres/init-scripts/` work needed** — production role bootstrap is owned by migration 0000_initial.sql / Helm; testing context is the inline SQL pattern.

## 4. Bucket B verified element paths (B1-B6)

| Fix | File:line | Current code | Oracle ref |
|---|---|---|---|
| B1 AuthShell scaffold | NEW `apps/web/src/components/screens/auth/AuthShell.tsx` | n/a | `.planning/phases/07-frontend-ui-spec/design/ui.jsx:229-316` |
| B1 layout drop | `apps/web/src/app/(public)/layout.tsx:17-23` | centered wrapper | n/a — AuthShell owns layout |
| B2 sign-in form wrap | `apps/web/src/components/screens/auth/SignInForm.tsx:114` | `<Card className="max-w-md">` | oracle screens-user.jsx:7+ |
| B2 OIDC reorder | `SignInForm.tsx:189-191,197-198` | OIDC after email block | render OIDC BEFORE separator |
| B2 OIDC size/variant | `OidcButtons.tsx:66-80` | default size | add `size="lg" className="w-full"` + `variant={p.id==="oidc"?"ghost":"outline"}` |
| B2 eye toggle | `SignInForm.tsx` password field | no toggle | add `Eye`/`EyeOff` from `lucide-react` |
| B2 remember-device | `SignInForm.tsx` form | no checkbox | install shadcn checkbox |
| B3 sign-up wrap | `SignUpForm.tsx:81,96,177-178` | `<Card>` | wrap `<AuthShell sideTitle…>` |
| B3 strength meter | `SignUpForm.tsx` password field | none | oracle ui.jsx:117-131 |
| B3 terms checkbox | `SignUpForm.tsx` form | none | shadcn checkbox + RHF field |
| B4 verify wrap | `VerifyEmailClient.tsx:65,78,94` | plain CardContent | wrap `<AuthShell sideTitle…>` |
| B4 status badge | `VerifyEmailClient.tsx` (per-status branch) | no icon | **NOTE:** file does not currently import `lucide-react` icons — add imports |
| B5 setup wrap | `SetupForm.tsx:5-9` | plain | wrap only — D-20 deviation stands |

**AuthShell Tailwind 4 scaffold (D-13):**
```tsx
// Two-column split — mobile hides side panel
<div className="min-h-screen lg:grid lg:grid-cols-2">
  <aside className="hidden lg:relative lg:flex lg:flex-col lg:justify-between lg:bg-muted lg:p-12">
    {/* logo, kicker, side title/quote */}
    <footer className="text-sm text-muted-foreground">{/* version + Status/Docs/GitHub */}</footer>
  </aside>
  <main className="flex flex-col items-center justify-center p-6 lg:p-12">{children}</main>
</div>
```

**shadcn checkbox install (verified shadcn/ui v2):**
```bash
npx shadcn@latest add checkbox
```
Verified `apps/web/src/components/ui/` lacks `checkbox.tsx` (18 components present). shadcn v2 CLI vendors `Checkbox` from `@radix-ui/react-checkbox` — Radix-namespaced `radix-ui` already in `apps/web/package.json` per CONTEXT line 33; no top-level dep added. The component is committed to git.

## 5. UICONF-05 axe baseline files

**Verified `apps/web/tests/e2e/fixtures/axe.ts`:** exports `runAxe(page: Page): Promise<void>` — takes ONE arg (no screen-name slot). Current call sites pass only `page`. **D-33 requires breaking change:** add optional second param.

**Proposed extension:**
```ts
export async function runAxe(page: Page, screenSlug?: string): Promise<void> {
  const results = await new AxeBuilder({ page })
    .withTags(["wcag2a", "wcag2aa", "wcag22aa"]).analyze();
  if (screenSlug && process.env.AXE_UPDATE_BASELINE === "1") {
    const out = resolve(HERE, "..", "__axe-baselines__", `${screenSlug}.json`);
    writeFileSync(out, JSON.stringify(canonicalize(results), null, 2));
  }
  // diff-mode (default): compare against baseline if exists; assert no NEW violations
  expect(results.violations).toEqual([]);
}
```

**Baseline directory does NOT exist** — `apps/web/tests/e2e/__axe-baselines__/` not present. First commit of plan 18.1.1-05 creates it + commits 4 JSON files (u1-sign-in, u2-sign-up, u3-verify-email, u-setup).

**u-setup test file:** verify presence by `ls apps/web/tests/e2e/u*setup*.spec.ts`. If absent, create `apps/web/tests/e2e/u-setup.spec.ts` mirroring `u1-sign-in.spec.ts` shape (4th screen new to e2e).

## 6. Lefthook hook surface for Phase 18.1.1

Verified `lefthook.yml` — hooks firing per file type touched:

| File pattern | Hooks fire |
|---|---|
| `*.ts/*.tsx` (test + new component) | `biome`, `english`, `phase-tag-comments`, optional `web-typecheck` (only apps/web/**), optional `colocated-tests` (only `{apps,packages}/*/src/**/*.test.ts` — **18.1.1 path-fix edits are under `tests/**` NOT `src/**` → NO fire**) |
| `*.json` (locale + axe baseline + package.json) | `biome`, `english` |
| `*.feature/*.md` (DOCS sync, ROADMAP) | `english` only |
| `apps/web/**/*.{ts,tsx}` | `biome`, `english`, `phase-tag-comments`, `web-typecheck` |
| commit-msg | `commitlint` (conventional shape `fix(18.1.1-NN-NN)` mandatory) |
| pre-push (apps/web/**) | `web-test` (vitest unit) |

**Risk:** `phase-tag-comments` may flag new AuthShell-component header comments — write conservative SPDX-only headers, NO inline `// Phase 18.1.1` markers. `web-typecheck` must stay green on every AuthShell + checkbox + axe-fixture edit.

## 7. TDD red-proof method (single-GREEN commit doctrine)

Per Phase 18.1 §7 + D-03 + D-39:
- **Bucket A path-fix clusters (1-11):** all failures are pre-existing RED on `main`. RED proof = the existing test exit code. Single GREEN commit per cluster. Commit body MUST include verbatim error output:
  ```
  fix(18.1.1-01-01): repoint sse-parser fixture paths to src/routes/agent/__fixtures__
  
  RED proof (pre-existing on main):
  $ pnpm --filter @openwhispr/api test sse-parser
  FAIL  apps/api/tests/unit/lib/sse-parser.test.ts
   ❯ ENOENT: no such file or directory, open '…/tests/unit/routes/agent/__fixtures__/text-only.sse'
  
  GREEN: depth fix + existsSync guard per Phase 18.1 D-04.
  ```
- **New code (D-05 lint extension, D-06 dep-graph linter, D-07 await linter, D-08 reaper-helper, D-12 role-bootstrap helper, D-13 AuthShell, D-33-34 axe + visual baselines):** TWO-commit RED + GREEN per Phase 18.1 D-28.
- `existsSync`-guard pattern (D-04) applied to every path-fix:
  ```ts
  if (!existsSync(SRC_PATH)) throw new Error(`source-contract path moved: ${SRC_PATH}`);
  ```

## 8. Pitfalls (planner WHY: lines)

1. **Vitest setupFiles inheritance.** `mergeConfig(rootConfig, …)` does NOT auto-inherit a setupFiles array — must be re-declared in each child config (verified `apps/worker/vitest.config.ts` lacks it). Promotion alone is insufficient; wire-up edits to both `apps/worker/vitest.config.ts` AND `packages/data/vitest.config.ts` are mandatory.
2. **Cluster 7 is two-field fix.** `auth.test.ts:147` is BOTH wrong filename (`.env.example` → `.env.slim.example`) AND wrong depth — `__dirname` for that file is `apps/api/tests/unit/` so `"..", "..", ".."` = repo root, which is correct. Verify with `existsSync` BEFORE asserting depth fix.
3. **`expect(() => …(await…))` inside an inner non-async arrow.** The bug is exactly the synchronous arrow factory `() => Schema.parse(await x.json())`. The OUTER `it(name, async () => {})` is async, but oxc parses the inner `()=>` as its own function scope. Refactor extracts the await OUTSIDE the inner arrow.
4. **shadcn v2 CLI prompt — namespace.** `npx shadcn@latest add checkbox` is correct (deprecated `shadcn-ui` package). The CLI may prompt for path; project uses `apps/web/src/components/ui/` (verified — 18 components there). Run from `apps/web/` to inherit `components.json`.
5. **`lucide-react` icon imports.** `VerifyEmailClient.tsx` currently imports zero icons. D-30 status badge requires `Mail`, `Loader2`, `CheckCircle2`, `AlertCircle` — all must be added. Tree-shake-safe via named imports.
6. **`.env.example` does not exist.** Only `.env.slim.example` + `.env.full.example` + `.env.embedded.example` + `.env.e2e.example`. `auth.test.ts:147` is testing `OPENWHISPR_DISABLE_RATE_LIMIT` documentation — which lives in `.env.slim.example` (verified by name). Pin to `.env.slim.example` specifically.
7. **Postgres role bootstrap is inline, not centralised.** D-12 helper extraction MUST be idempotent (`CREATE ROLE … IF NOT EXISTS` is unsupported by Postgres pre-16 → use exception-swallow `DO $$ BEGIN … EXCEPTION WHEN duplicate_object THEN NULL; END $$;`).
8. **`packages/data` Cluster-9 sibling count.** CONTEXT D-02 says "5 files". Live grep found 7 files with `MIGRATIONS_FOLDER`-style refs (`pgbouncer-interleave`, `0003_better_auth_tenant_defaults`, `migration-0006-backfill`, `settings-rls`, `rls-property`, `migration-rollback`, `worker-rls-property`). Planner must enumerate ALL and batch into one commit per D-02.
9. **`__axe-baselines__/` does not exist.** First commit of 18.1.1-05 must create the directory (gitkeep or via the 4 baseline JSON files). Without it, the first baseline write will ENOENT.
10. **AuthShell + Tailwind 4 utility classes** — Tailwind 4 dropped `@layer` plugin shape; if AuthShell uses `bg-muted` it must be defined in `apps/web/src/app/globals.css` `@theme` block (verify current shape before authoring).
11. **`phase-tag-comments` lint may bite new component files.** AuthShell + new lint-extension tools must use bare SPDX header only — no `// Phase 18.1.1 / Plan …` lines inside `apps/` or `packages/` source.
12. **Playwright `toHaveScreenshot()` deterministic gotchas.** Mask `[data-testid='locale-switcher']` (per D-34) AND any timestamp / random ID renders. First-run `--update-snapshots` is mandatory; commits include both PNGs and `.spec.ts-snapshots/` index.
13. **Better Auth `rememberMe`.** Better Auth `signIn.email` accepts `{ rememberMe: boolean }` (per docs). D-21 wiring needs RHF `rememberDevice: boolean` → call `authClient.signIn.email({ email, password, rememberMe })`.
14. **commitlint envelope.** Phase tag inside parens: `fix(18.1.1-01-01)` not `fix(18.1.1/01-01)`. Verify by checking last 5 Phase 18.1 commits in git log.
15. **D-26 routes /terms /privacy probe.** Before authoring terms-checkbox link hrefs, run `find apps/web/src/app -type d \( -name terms -o -name privacy \)`. If absent → defer per D-47 scope-out, not author placeholders.

## 9. Discrepancies vs CONTEXT.md

**Δ-1 (D-12 — postgres role bootstrap):** CONTEXT says "verify via `compose/postgres/init-scripts/` or equivalent". Verified: **no such directory exists**. Real state is inline `CREATE ROLE` SQL per test (`pgbouncer-interleave.test.ts:101-110`, `migration-0006-backfill.test.ts:54-57`). Planner should re-shape D-12 as "extract inline role-create SQL to shared test-helper" rather than "wire globalSetup".

**Δ-2 (Cluster 7 — auth.test.ts):** CONTEXT D-02 calls out `.env.example` → `.env.slim.example` content fix. Live: `.env.example` **does not exist** at all (only `.env.slim.example`, `.env.full.example`, `.env.embedded.example`, `.env.e2e.example`). The path-DEPTH in `auth.test.ts:147` (`resolve(__dirname, "..", "..", "..", ".env.example")`) is actually correct (3 ups from `apps/api/tests/unit/` = repo root). **Only filename changes.** No depth fix needed.

**Δ-3 (D-30 — VerifyEmailClient icons):** CONTEXT says "Branch per `status` state (`Mail` / `Loader2` / `CheckCircle2` / `AlertCircle` from `lucide-react`)". `VerifyEmailClient.tsx` does NOT currently import `lucide-react` (verified). Planner must include the import-add as part of the fix, plus a comment that any tree-shake assertion in the bundle-size lint (if any) must accommodate 4 new icon names.

## Sources

- **Primary (HIGH):** Live working tree at `/Users/nick/openwhispr-server` HEAD (2026-05-15) — every file/line cited verified by Read/grep this session.
- CONTEXT D-01..D-48 (`.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md`)
- Phase 18.1 precedent (`.planning/phases/18.1-v2-test-debt-closure/18.1-RESEARCH.md` §§1, 7, 10)
- `lefthook.yml` (verified hook globs)
- `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` (canonical pattern)
- shadcn/ui v2 CLI: `npx shadcn@latest add checkbox` (CLI rename from `shadcn-ui` confirmed via CONTEXT canonical_refs + 18 existing components installed in `apps/web/src/components/ui/`)

## Metadata

- Confidence: HIGH on every path site (Read-verified); HIGH on lefthook surface; HIGH on shadcn install command; MEDIUM on Cluster-9 sibling count (live grep shows 7 candidates, CONTEXT says 5 — planner re-enumerates); MEDIUM on D-11 root cause (executor-classification step deferred).
- Valid until: 7 days (test-debt is fast-moving)
- LOC: 314
