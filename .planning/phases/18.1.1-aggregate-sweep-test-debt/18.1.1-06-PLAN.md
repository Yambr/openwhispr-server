---
phase: 18.1.1-aggregate-sweep-test-debt
plan: 06
type: execute
wave: 3
depends_on: [18.1.1-01, 18.1.1-02, 18.1.1-03, 18.1.1-04, 18.1.1-05]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/V2-MILESTONE-REVIEW.md
autonomous: true
requirements:
  - SR-A1
  - SR-A2
  - SR-A3
  - SR-A4
  - SR-A5
  - SR-A6
  - SR-A7
  - SR-B1
  - SR-B2
  - SR-B3
  - SR-B4
  - SR-B5
  - SR-B6
must_haves:
  truths:
    - "End-of-phase aggregate `pnpm test` exits 0 across all 296 test files (D-45)."
    - "ROADMAP.md Phase 18.1.1 row: status `complete`; success criteria all checked; commit-count summary present."
    - "STATE.md milestone v2.1 declared (D-45) — v2 transitions from `closed-with-followup` (per Phase 18.1-07 SUMMARY) to clean `closed`."
    - "V2-MILESTONE-REVIEW.md updated: top-7 followups checked off; UICONF-05 axe gap closed; testcontainer-cleanup-audit checked off."
    - "Aggregate metrics captured: total commit count for Phase 18.1.1 (target ~28-32 per D-40); per-plan SUMMARY hyperlinks; coverage gate verified ≥ 90/90/90/90 across all new code."
    - "Single atomic commit `docs(18.1.1-06-01): close phase + declare v2.1 milestone`."
    - "If aggregate `pnpm test` does NOT exit 0: HALT — do NOT mark phase complete; emit operator recipe + carve outstanding failures to Phase 18.1.2 per D-48."
    - "ZERO `--no-verify` (D-41)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18.1.1 row marked complete; v2.1 milestone declared."
      contains: "Phase 18.1.1"
    - path: ".planning/STATE.md"
      provides: "v2.1 milestone status entry."
      contains: "v2.1"
    - path: ".planning/V2-MILESTONE-REVIEW.md"
      provides: "Followup table reconciled."
      contains: "UICONF-05"
  key_links:
    - from: "ROADMAP.md Phase 18.1.1 row"
      to: "5 plan SUMMARYs (18.1.1-01..05)"
      via: "Hyperlinks in plan-list table."
      pattern: "18.1.1-0"
---

<objective>
Close the phase: sync ROADMAP, declare v2.1 milestone, reconcile V2-MILESTONE-REVIEW followups, verify end-of-phase aggregate `pnpm test` exits 0 (D-45).

**1 atomic commit** (`docs(18.1.1-06-01)`).

Wave 3 — depends on ALL of 18.1.1-01..05 completing successfully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/V2-MILESTONE-REVIEW.md
@.planning/phases/18.1-v2-test-debt-closure/18.1-07-SUMMARY.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 18.1.1-06-01: Aggregate verify + ROADMAP/STATE sync + v2.1 milestone declaration (single commit)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/V2-MILESTONE-REVIEW.md</files>
  <action>
Per D-45 + D-48 (HALT-on-residual).

**Step 1 — Aggregate verify gate (D-45):**

1. Run `pnpm test 2>&1 | tee /tmp/18.1.1-06-aggregate.log` from repo root.
2. Inspect:
   - Total test files: expect 296.
   - Failures: expect 0.
   - If failures > 0:
     - Classify each: in-D-02-inventory (re-open offending plan) OR outside (D-48 carve to Phase 18.1.2).
     - HALT this task. Emit operator recipe (3-branch user choice per D-09 pattern):
       - **Branch A**: re-open 18.1.1-NN plan to fix in-scope failures; re-run aggregate.
       - **Branch B**: carve all outstanding to Phase 18.1.2; mark this phase `closed-with-followup-18.1.2`.
       - **Branch C**: full HALT pending user investigation.
     - Do NOT proceed to Step 2.
3. If exit 0: proceed.

**Step 2 — ROADMAP.md update:**

4. Edit `.planning/ROADMAP.md` Phase 18.1.1 row:
   - Status: `complete` (was `planned` or `in-progress`).
   - Success criteria checkboxes: tick all (1-N).
   - Append commit-count summary: `**Commits**: ~28-32 atomic (5 plans + this docs commit).`
   - Add plan-list table hyperlinks:
     ```
     - [18.1.1-01-SUMMARY.md](phases/18.1.1-aggregate-sweep-test-debt/18.1.1-01-SUMMARY.md) — Bucket A path-fix codemod (11 clusters + lint extension)
     - [18.1.1-02-SUMMARY.md](…) — Bucket A pkg/transform (wire-schemas dep + await-AST linter)
     - [18.1.1-03-SUMMARY.md](…) — Bucket A testcontainer + misc (reaper + session-id + role-bootstrap)
     - [18.1.1-04-SUMMARY.md](…) — Bucket B AuthShell + sign-in + sign-up
     - [18.1.1-05-SUMMARY.md](…) — Bucket B verify-email + setup + UICONF-05 axe + visual regression
     ```
   - Mark Bucket A SR-A1..A7 + Bucket B SR-B1..B6 all checked.

**Step 3 — STATE.md v2.1 milestone:**

5. Edit `.planning/STATE.md`:
   - v2 milestone status: `closed-with-followup` → `closed` (was set by Phase 18.1-07 SUMMARY; this phase clears the followup).
   - Append new milestone block:
     ```markdown
     ## Milestone v2.1 — declared 2026-05-15
     **Status**: open
     **Scope**: post-v2 hardening; new features TBD by user.
     **Predecessor**: v2 (closed via Phase 18.1.1 — aggregate-sweep + UICONF auth drift).
     ```

**Step 4 — V2-MILESTONE-REVIEW.md reconcile:**

6. Edit `.planning/V2-MILESTONE-REVIEW.md` top-7 followups table:
   - UICONF-05 axe gap → ✅ closed by 18.1.1-05.
   - testcontainer cleanup audit (feedback_testcontainers_cleanup_audit.md) → ✅ closed by 18.1.1-03.
   - Aggregate `pnpm test` failures → ✅ closed by 18.1.1-01..03.
   - Any others matching: mark closed with phase + plan citation.
   - Unresolved followups: stamp "carved to Phase 19.x" or "open".

**Step 5 — Atomic commit:**

7. Single commit: `docs(18.1.1-06-01): close phase 18.1.1 + declare v2.1 milestone + reconcile V2-MILESTONE-REVIEW`. Body includes:
   - Aggregate `pnpm test` exit code + summary line (296 files / 0 failures).
   - Commit-count summary (`git log --oneline --grep="18.1.1" | wc -l` value).
   - v2.1 declaration rationale (v2 closed-with-followup → closed).
   - Citations: D-45, D-48, Phase 18.1-07 SUMMARY.

WHY: D-45 mandates aggregate green before closure; D-48 mandates carve-not-fix for out-of-inventory residuals; pitfall §14 — commitlint envelope `docs(18.1.1-06-01)` shape.
  </action>
  <verify><automated>pnpm test 2>&1 | tail -10 && grep -c "complete\|closed" .planning/ROADMAP.md && grep -c "v2.1" .planning/STATE.md</automated></verify>
  <done>EITHER 1 commit landing closure (aggregate green) OR HALT recipe emitted with carve-to-18.1.2 disposition documented.</done>
</task>

</tasks>

<verification>
After task:

```bash
cd /Users/nick/openwhispr-server

# Aggregate green (D-45)
pnpm test 2>&1 | tail -20

# ROADMAP sync
grep -A 5 "Phase 18.1.1" .planning/ROADMAP.md | head -20

# v2.1 milestone declared
grep -c "v2.1" .planning/STATE.md

# V2-MILESTONE-REVIEW reconciled
grep -E "UICONF-05|testcontainer" .planning/V2-MILESTONE-REVIEW.md | head -5

# Phase-wide commit count
git log --oneline --grep="18.1.1-" --since="2026-05-15" | wc -l # expect 28-32 (D-40)

# Single closure commit
git log --oneline -n 2 | grep -c "docs(18.1.1-06-01)" # expect 1
```
</verification>

<success_criteria>
1. Aggregate `pnpm test` exits 0 (D-45).
2. ROADMAP Phase 18.1.1 row marked complete with all SR-A/SR-B success criteria checked.
3. STATE.md v2 transitioned to `closed`; v2.1 milestone declared.
4. V2-MILESTONE-REVIEW.md followups reconciled.
5. Single atomic commit `docs(18.1.1-06-01)`.
6. Phase-wide commit count 28-32 (D-40 target).
7. ZERO `--no-verify` (D-41).
</success_criteria>

<rollback>
- D-45 aggregate fails: emit 3-branch HALT recipe (Branch A re-open in-scope plan / Branch B carve to 18.1.2 / Branch C full HALT). Do NOT mark phase complete until Branch A or B resolves.
- If unexpected lefthook fires on docs commit (e.g., `english` flags Russian-only side-note): re-word in English (constitutional rule per CLAUDE.md root — source-artifact English only).
- If `git log --grep="18.1.1-"` shows < 25 commits, investigate whether some plans skipped commits silently — re-audit per-plan SUMMARYs against expected count in this plan's body.
</rollback>

<output>
Create `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-06-SUMMARY.md` with:
- 1 commit SHA
- Aggregate `pnpm test` exit code + 296-file/0-failure verification
- Phase-wide commit count
- v2.1 milestone declaration line
- V2-MILESTONE-REVIEW followups reconciled list
- Carve-to-18.1.2 disposition (if any)
</output>
</content>
</invoke>