# Phase 18.1.1 — Pattern Map

**Mapped:** 2026-05-15
**Scope:** Bucket A (16 path-fix sites + 6 new tooling/infra artefacts) + Bucket B (1 NEW shell + 4 screen wraps + 2 baselines).
**Stance:** every NEW/MODIFIED file cites a concrete in-repo analog file:line. Inherits Phase 18.1 playbook; appends 13 new entries.

---

## A1. Bucket A path-rewrites (×16 sites, D-01..D-04)

**New diffs:** 16 test files across `apps/api/tests/unit/**`, `packages/data/tests/**`, `tools/lint-rls.test.ts`.

**Analog:** `apps/api/tests/unit/lib/sse-parser.test.ts:17-19` — **canonical cross-tree pattern** (inherited from Phase 18.1 §1):
```ts
const HERE = dirname(fileURLToPath(import.meta.url));
const fixturePath = (name: string) =>
  resolve(HERE, "..", "..", "..", "src", "<dir>", "<file>");
```

**What to copy:** `dirname(fileURLToPath(import.meta.url))` + relative hops to sibling `src/` tree. D-04 mandates `existsSync(target)` guard BEFORE every `readFileSync` / dynamic import (defensive uplift over the analog — same as Phase 18.1).

**What's different:** Cat-A (fixture moves) ≠ Cat-B (depth math wrong) ≠ Cat-C (double `apps/`) ≠ Cat-E (dynamic-import targets) — no codemod possible (D-01). Each cluster's depth recomputed individually. Cluster 5 (P8 entrypoint-db-shape) and Cluster 9 (`packages/data` ×5) need fresh `..`-counts.

**Risk:** sse-parser.test.ts has NO existsSync guard — DO NOT copy that omission. STRUCT-01 mirror layout (tests/unit/<mirror>/) holds; cluster 11 (UI-SPEC-admin.md `apps/` prefix) is **content fix**, not path fix — different pattern (string replace inside a markdown literal).

---

## A2. `tools/lint-colocated-tests.ts` extension — `findStalePathLiterals` (D-05)

**New diff:** add exported function + wire into `pnpm lint:colocated-tests`.

**Analog A (host file shape):** `tools/lint-colocated-tests.ts:34-48` — header pattern (shebang + SPDX + JSDoc rationale + exported `ALLOWED_PREFIXES` const + POSIX normaliser).

**Analog B (AST scan precedent):** none of the existing `tools/lint-*.ts` use TS AST today — `lint-tdd.ts:35-58` only walks `git log`; `lint-colocated-tests.ts` uses `glob` + suffix match. **First AST-based linter in repo** → use `ts.createSourceFile` + `forEachChild` walk (vendored from TS compiler API, no new dep — `typescript` is at root).

**Analog C (RED test colocated):** `tools/lint-colocated-tests.test.ts` (sibling pattern) — fixture tree under `tools/__fixtures__/lint-colocated-tests/`. Mirror that layout: `tools/__fixtures__/lint-stale-path-literals/{exists,missing}/...`.

**What to copy:** exit codes 0/1/2 contract from `lint-colocated-tests.ts:26-31`; allowlist file convention from line 46 (`LEGACY_ALLOWLIST_FILE` const).

**Risk:** `findStalePathLiterals` must resolve **statically** — runtime `path.join(__dirname, dyn)` w/ template-string vars are NOT in scope (skip with a `// lint-stale-path-literals-ignore` marker, mirroring `// biome-ignore`). Don't fall into eval-style resolution. Pre-commit hook (lefthook) wire-up: `pnpm lint:colocated-tests` already runs in `lefthook.yml` — extension picked up automatically. Do NOT add a second hook entry.

---

## A3. `tools/lint-await-in-non-async.ts` NEW (D-07)

**New diff:** AST linter + RED test + wire into `tools` vitest project.

**Closest analog:** `tools/lint-tdd.ts:1-58` (file header, exit-code contract, `execFileSync` boundary). Use `ts.createSourceFile` walk to find `AwaitExpression` nodes inside non-async `ArrowFunctionExpression`/`FunctionExpression`.

**Analog (similar pure-AST tool):** none exists. **First in repo.** Use TS compiler API directly (already a transitive dep via Vitest 4 + Drizzle). DO NOT introduce `@typescript-eslint/parser` — would breach D-44 zero-new-deps.

**What to copy:** header + JSDoc rationale block (`lint-colocated-tests.ts:1-33`); RED test sibling-file convention; allow-list file pattern (`LEGACY_ALLOWLIST_FILE` const).

**What's different:** scope is `*.test.ts` only (production code already async-aware via Biome). Walk skips files matching pre-existing test allow-list under `tools/lint-await-in-non-async.allowlist.txt` (if any baseline is needed).

**Risk:** false positives on `async function () { await x }` inside a non-async outer arrow — must check **immediate parent** function context, not nearest-ancestor. TypeScript AST `FunctionLikeDeclaration.modifiers` carries `AsyncKeyword`. Don't conflate with `await` in top-level module scope (Node 24 TLA legal).

---

## A4. `packages/contract-tests/tests/__tests__/dep-graph.test.ts` NEW (D-06)

**New diff:** vitest spec reading own `package.json` + walking `tests/**/*.ts` imports, asserting every `@openwhispr/*` import has a matching `dependencies` entry.

**Closest analog:** `tools/lint-colocated-tests.test.ts` style of "lint-as-test" — load source, run assertion, report violations. Also adjacent: `tools/lint-migrations.test.ts` (reads `packages/data/migrations/*.sql` and asserts shape).

**What to copy:** `glob` of `tests/**/*.ts` + per-file `readFileSync` + regex `/@openwhispr\/([a-z-]+)/g` to extract imports. Set-diff against `Object.keys({...pkg.dependencies, ...pkg.devDependencies})`. Single `expect(missing).toEqual([])` assertion.

**Risk:** must walk **only** test files within `packages/contract-tests/tests/**`, NOT re-resolve transitive imports. peerDeps don't count (contract-tests is a leaf). Workspace protocol `workspace:*` must be respected — don't compare against published version.

---

## A5. `tools/testcontainer-reaper-setup.ts` extract from `apps/api/vitest.setup.ts` (D-08)

**New diff:** promote 14-line setup to shared root tool + wire from `apps/worker/vitest.config.ts` + `packages/data/vitest.config.ts`.

**Closest analog (promotion precedent):** `tools/global-vitest-teardown.ts:1-77` — already a shared cross-workspace utility imported by `apps/api/vitest.setup.ts:12`. Mirror the same SPDX + JSDoc header style + named-export shape (`installSignalHook` is already at `tools/global-vitest-teardown.ts:53-66`).

**What to copy:** verbatim 14-line body of `apps/api/vitest.setup.ts:1-14` (one-liner `installSignalHook()` call inside a tiny setup module). Then update `apps/api/vitest.setup.ts` to import from new location (or delete and reference the shared file directly via `setupFiles: ["../../tools/testcontainer-reaper-setup.ts"]`).

**Analog (session-id label hardening):** `tools/global-vitest-teardown.ts:28-34` — extend the `PRUNE_ARGV` filter list with a second `--filter label=org.testcontainers.session-id=${process.pid}` entry. Same `execFileSync` boundary, same swallow-errors `try/catch` discipline at `:36-45`.

**Risk:** Ryuk handshake needs to fire BEFORE the broader signal handler — vitest crash before testcontainers SDK load means signal hook prunes nothing (D-09 HALT recipe). Setup-file order matters: reaper must be FIRST in `setupFiles` array.

---

## A6. Postgres CREATEROLE bootstrap migration (D-12)

**New diff:** ensure `openwhispr_app` role has `CREATEROLE` membership BEFORE migrate suite runs against testcontainer.

**Closest analog (existing role-bootstrap):** `packages/data/migrations/init/00-roles.sh` + `00-roles.sql.tpl` — production bootstrap via Postgres entrypoint's `/docker-entrypoint-initdb.d/`. **Not directly reusable** for testcontainers — init-scripts run once on first volume init, but testcontainers create ephemeral DBs.

**Closest test analog:** `packages/data/migrations/__tests__/0017-setup-state.test.ts:81-84` — in-test role bootstrap:
```ts
`CREATE ROLE openwhispr_owner WITH LOGIN BYPASSRLS CREATEROLE PASSWORD '${ownerPassword}'`,
`CREATE ROLE openwhispr_app   WITH LOGIN          PASSWORD '${appPassword}'`,
```
Also `packages/data/tests/unit/__tests__/pgbouncer-interleave.test.ts:101-103` (same shape, used in 2+ test files already).

**What to copy:** the two `CREATE ROLE` statements verbatim, executed as superuser via `superPool.query()` against fresh testcontainer postgres in `globalSetup`. **NEW:** add explicit `CREATEROLE` to `openwhispr_app` if any test asserts `pg_has_role('openwhispr_app','CREATEROLE','member')` (D-12 RED).

**Risk:** prod bootstrap deliberately gives CREATEROLE only to `openwhispr_owner`; test parity vs prod is the user-relevant truth — if D-12 RED needs CREATEROLE on `openwhispr_app`, that drifts from prod. Verify RED first; if prod-parity required, fix is to grant CREATEROLE conditionally only in test bootstrap.

---

## B1. `apps/web/src/components/screens/auth/AuthShell.tsx` NEW (D-13)

**Closest analog:** `apps/web/src/components/screens/AppShell.tsx:1-40` — existing sibling shell. Same anatomy: `"use client"` directive, SPDX header + Phase rationale block, `import Link from "next/link"`, `useTranslation` for i18n, `ReactNode` children prop, named export `function AuthShell(...): React.JSX.Element`.

**What to copy:**
- File header pattern (`AppShell.tsx:1-12`) — SPDX + Phase/Plan citation + mobile-breakpoint note.
- `"use client"` directive at L12.
- Import order: `next/link` → `next/navigation` → `react` types → `react-i18next` → `@/components/ui/*` → `@/lib/*` → siblings.
- `useTranslation(["common"])` shape (vs end-user-namespace in AppShell:39).
- ReactNode children typing + named export.

**What's different:** two-column split (oracle `ui.jsx:229-316`) — left branding/right form, side hidden `<lg`. AppShell is row+sidebar; AuthShell is grid. Layout primitives: Tailwind `grid grid-cols-1 lg:grid-cols-2` instead of AppShell's flex+sidebar. Optional props `sideTitle?`/`sideKicker?`/`sideQuote?` for per-screen variance (sign-up, verify, setup pass distinct copy per D-24/D-29/D-32).

**Risk:** AppShell.tsx is end-user-only — AuthShell wraps `(public)` routes (D-14 drops the centred wrapper at `apps/web/src/app/(public)/layout.tsx:17-23`). LanguageSwitcher header stays absolute-positioned over the RIGHT column only. Don't reproduce AppShell's `NAV` array — auth has no nav.

---

## B2. Password show/hide eye toggle (D-20)

**Closest analog:** none in repo (`grep` for `Eye|EyeOff|showPassword` returned zero hits in `apps/web/src/`). **First instance.**

**Closest stylistic analog:** `apps/web/src/components/ui/input.tsx:6-20` — `Input` primitive component. NEW behaviour wraps it: parent `<div className="relative">`, child absolute-positioned `<Button variant="ghost" size="icon">` with `Eye`/`EyeOff` from `lucide-react`.

**What to copy:** wrap pattern only — input remains untouched, `type` becomes `type={shown ? "text" : "password"}` driven by local `useState<boolean>(false)`. `Button` variant precedent: `OidcButtons.tsx:66-80` (D-18 uses `variant="ghost"` for SSO de-emphasis — same variant here for the toggle).

**What's different:** new icon-only button, not part of existing kit. Per specifics: `absolute right-1.5 top-1.5 size-8`. `aria-label` from i18n key (D-20).

**Risk:** lefthook `web-typecheck` will fail if `lucide-react` imports are wrong path; verify `Eye`/`EyeOff` exports exist (they do — already imported elsewhere via deps). Don't reach for a new `@radix-ui/react-toggle` dep (zero-new-deps D-44).

---

## B3. Password strength meter (D-25)

**Closest analog:** none in repo (`grep` for `passwordStrength|strength` in `apps/web/src/` returned zero hits). **First instance.**

**Closest structural analog:** any inline derived-state helper colocated in a form component — e.g., `apps/web/src/components/screens/auth/SignUpForm.tsx` already inlines schema-driven validation via `useZodForm`. Add `passwordStrength(value: string): {score: 0|1|2|3; label: "weak"|"fair"|"good"|"strong"}` as a **module-scoped pure function** above the component.

**What to copy:** Tailwind colour ramp inline-class pattern from `apps/web/src/components/ui/badge.tsx` (variant-driven `bg-*-500`). 4-band score → `bg-red-500 / bg-orange-500 / bg-yellow-500 / bg-green-500` div widths (`w-1/4 / w-2/4 / w-3/4 / w-full`).

**Risk:** zero-new-deps (D-44) — DO NOT add `zxcvbn` or `@zxcvbn-ts/core`. Score derives from the `signUpSchema` rules (12+ chars / mixed case / digit / symbol) — keep simple count-based banding. i18n keys for labels (D-43 en+ru parity).

---

## B4. Shadcn `Checkbox` install (D-21, D-26)

**Closest analog:** `apps/web/src/components/ui/stepper.tsx:1-22` — sole vendored shadcn primitive in repo:
```ts
// Vendored from https://github.com/damianricobelli/shadcn-stepper
// (MIT License, …). Adapted to apps/web's shadcn/ui v2 + Tailwind 4 + Radix-UI conventions
```

**What to copy:** vendoring discipline only — `npx shadcn@latest add checkbox` writes `apps/web/src/components/ui/checkbox.tsx` from the canonical shadcn registry (Radix-based). NO top-level dep bump (`radix-ui` umbrella already at `package.json:33`).

**Analog for SPDX/header retrofit:** every `apps/web/src/components/ui/*.tsx` carries `// SPDX-License-Identifier: FSL-1.1-ALv2` as L1 (see `input.tsx:1`, `stepper.tsx:1-2`) — MUST be added post-`shadcn add` because shadcn's generator does NOT emit SPDX (will trip `lint-english` + repo SPDX gate).

**Risk:** shadcn registry pulls from `@radix-ui/react-checkbox` — verify that import resolves through the umbrella `radix-ui` package (subpath exports). If shadcn writes `import { Checkbox } from "@radix-ui/react-checkbox"` and that subpath is missing, swap to `radix-ui/checkbox` form (matches stepper.tsx precedent). Land in SAME atomic commit as the consumer (SignInForm + SignUpForm) — orphaned ui files trigger `lint-colocated-tests` style gates.

---

## B5. UICONF-05 axe baseline JSON (D-33)

**New artefact:** `apps/web/tests/e2e/__axe-baselines__/<screen>.json` files + extension of `apps/web/tests/e2e/fixtures/axe.ts`.

**Closest analog (host file):** `apps/web/tests/e2e/fixtures/axe.ts:15-19` — current `runAxe(page: Page): Promise<void>` shape:
```ts
const results = await new AxeBuilder({ page })
  .withTags(["wcag2a", "wcag2aa", "wcag22aa"])
  .analyze();
expect(results.violations).toEqual([]);
```

**Closest baseline-write analog:** none in repo — first JSON-snapshot artefact for e2e suite. Closest neighbour for **persisted JSON fixtures**: `apps/api/src/routes/agent/__fixtures__/*.sse` directory convention (canonical `__fixtures__` segment between source and test).

**What to copy:** extend signature to `runAxe(page: Page, screenId?: string): Promise<void>`. When `screenId` + `AXE_UPDATE_BASELINE=1` env → `writeFileSync(resolve(HERE, "../__axe-baselines__", `${screenId}.json`), JSON.stringify(...))`. Otherwise → `readFileSync` + diff `results.violations` against baseline. Existing assertion at L19 stays as `toEqual([])` for screens without a baseline.

**Risk:** writing baselines from a test run breaks idempotency — gate behind env var (D-33 spec). Existing call site at `apps/web/tests/e2e/u1-sign-in.spec.ts:66-70` is `await runAxe(page)` — backward-compat the optional `screenId` arg. Commit baselines under `__axe-baselines__/` MUST include u-setup (D-33 lists 4 screens).

---

## B6. Playwright visual regression `toHaveScreenshot` (D-34)

**Closest analog:** **none in repo** (`grep toHaveScreenshot|toMatchSnapshot` returned zero hits in `apps/web/tests/`). **First instance.**

**Closest stylistic analog:** `apps/web/tests/e2e/u1-sign-in.spec.ts` — typical Playwright spec shape, `await page.goto("/sign-in")` + `await runAxe(page)`. Mirror that imports + setup; add `await expect(page).toHaveScreenshot('sign-in.png', { maxDiffPixelRatio: 0.01, mask: [page.locator("[data-testid='locale-switcher']")] })`.

**What to copy:** Playwright spec scaffold from `u1-sign-in.spec.ts:1-30`; `mask:` parameter is the per-spec convention for hiding LanguageSwitcher (which carries dynamic locale labels). New file `auth-shell-visual.spec.ts` co-located with existing u-numbered specs. Snapshots auto-land under `auth-shell-visual.spec.ts-snapshots/` (Playwright convention — D-34 explicit).

**Risk:** snapshot dir collides with `.gitignore` patterns if any blanket `*-snapshots/` rule exists — verify before baking. `--update-snapshots` invocation only AFTER B1-B5 land (D-35). Cross-platform diff variance (macOS vs Linux CI) is the canonical Playwright headache — use `playwright.config.ts` `expect.toHaveScreenshot.maxDiffPixelRatio: 0.01` (already explicit in D-34).

---

## B7. Commit-message convention `(NN-MM)` (D-02, D-39)

**Analog (precedent, Phase 18.1):** recent git log shape:
- `fix(18.1-02): green — authRequired tag skips IP-tier for authed-only routes (T3, security)` (f6a42cd)
- `test(18.1-02): red — sharpen T3 (no owrl:ip:* on anon authed-route reqs)` (c4c6b4b)
- `docs(18.1-05): summary — REPOINT 4 tags + lint regex + §14-04 reconfirm` (c715877)
- `docs(18.1.1): capture phase context — aggregate-sweep test debt + UICONF auth drift` (2299ab4)

**18.1.1 adoption per D-02:** `fix(18.1.1-NN-MM): <subject>` where NN = plan (01..06) and MM = cluster within plan (e.g., 18.1.1-01-09 for D-02 cluster 9). Each path-fix cluster lands as a **single GREEN commit** (D-03) with verbatim error output anchored in commit body — pre-existing RED proof.

**Two-commit RED+GREEN pairs** (D-39) for NEW functionality only:
- `test(18.1.1-01-12): red — findStalePathLiterals against fixture tree`
- `chore(18.1.1-01-12): green — wire findStalePathLiterals into lint-colocated-tests`
Both share the `01-12` identifier (mirrors Phase 17/16-fix `wr-05` style).

**Risk:** commitlint at lefthook pre-commit ≤ 100 chars subject; D-41 forbids `--no-verify`. The `18.1.1-NN-MM` segment is **20 chars** of scope alone — keep subjects ≤ 72 to be safe. `phase-tag-comments` hook will scan for `// Phase 18.1.1 / Plan 01` markers — match exact precedent in source comments.

---

## Inherited from Phase 18.1 (no change)

- **Path-rewrite pattern** (§1) — `sse-parser.test.ts:17-19` canonical; D-04 existsSync uplift mandatory.
- **TDD atomic-commit pair** (§9) — `test(...): red` then `fix(...): green` adjacent commits with shared ID. Single-GREEN allowed for pre-existing RED clusters per D-03 / 18.1 §7.
- **`.planning/deferred-items.md` entry format** (§6) — append-only under new heading `## From Phase 18.1.1 — <bucket>`.
- **HALT recipe shape** (Phase 18.1-06) — three-branch user choice on testcontainer / oracle-divergence / missing-prereq.

---

## Summary table

| New / modified surface | Closest analog | File:line |
|---|---|---|
| ×16 path-rewrite tests (Bucket A1) | sse-parser.test.ts | apps/api/tests/unit/lib/sse-parser.test.ts:17-19 |
| lint-colocated-tests `findStalePathLiterals` | self + TS AST (first) | tools/lint-colocated-tests.ts:34-48 |
| lint-await-in-non-async.ts NEW | lint-tdd.ts header shape | tools/lint-tdd.ts:1-58 |
| contract-tests dep-graph.test.ts NEW | lint-migrations.test.ts style | tools/lint-migrations.test.ts |
| testcontainer-reaper-setup.ts (promote) | global-vitest-teardown.ts | tools/global-vitest-teardown.ts:1-77 |
| Postgres CREATEROLE bootstrap | 0017-setup-state.test.ts L81-84 | packages/data/migrations/__tests__/0017-setup-state.test.ts:81-84 |
| AuthShell.tsx NEW | AppShell.tsx | apps/web/src/components/screens/AppShell.tsx:1-40 |
| Password show/hide eye toggle | none — first; OidcButtons ghost variant | apps/web/src/components/screens/auth/OidcButtons.tsx:66-80 |
| Password strength meter | none — first; badge.tsx variant ramp | apps/web/src/components/ui/badge.tsx |
| Shadcn `checkbox` install | stepper.tsx vendoring | apps/web/src/components/ui/stepper.tsx:1-22 |
| UICONF-05 axe baseline JSON | axe.ts current runAxe shape | apps/web/tests/e2e/fixtures/axe.ts:15-19 |
| Playwright `toHaveScreenshot` NEW | u1-sign-in.spec.ts skeleton | apps/web/tests/e2e/u1-sign-in.spec.ts |
| `fix(18.1.1-NN-MM)` commit shape | docs(18.1-05) precedent | commit c715877 |

---

_Mapped: 2026-05-15 — gsd-pattern-mapper for Phase 18.1.1_
