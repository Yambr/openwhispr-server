---
phase: 18.1.1-aggregate-sweep-test-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/screens/auth/AuthShell.tsx
  - apps/web/src/app/(public)/layout.tsx
  - apps/web/src/components/screens/auth/SignInForm.tsx
  - apps/web/src/components/screens/auth/SignUpForm.tsx
  - apps/web/src/components/screens/auth/OidcButtons.tsx
  - apps/web/src/components/ui/checkbox.tsx
  - apps/web/src/locales/en/common.json
  - apps/web/src/locales/ru/common.json
  - apps/web/src/locales/en/end-user.json
  - apps/web/src/locales/ru/end-user.json
  - apps/web/tests/unit/components/screens/auth/AuthShell.test.tsx
  - apps/web/tests/unit/components/screens/auth/SignInForm.test.tsx
  - apps/web/tests/unit/components/screens/auth/SignUpForm.test.tsx
  - .planning/deferred-items.md
autonomous: true
requirements:
  - SR-B1
  - SR-B2
  - SR-B3
  - SR-B4
  - SR-B5
must_haves:
  truths:
    - "`apps/web/src/components/screens/auth/AuthShell.tsx` exists; two-column split (Tailwind `lg:grid lg:grid-cols-2`); side panel hidden `<lg`; optional props `sideTitle`/`sideKicker`/`sideQuote`."
    - "`apps/web/src/app/(public)/layout.tsx` drops centered wrapper (lines 17-23); AuthShell owns layout; LanguageSwitcher stays absolute over right column."
    - "i18n keys for shell defaults (`common.auth.shell.kicker.default.text`, `…title.default.text`, `…quote.default.text`) added in BOTH `en/common.json` AND `ru/common.json` in SAME commit (D-43)."
    - "SignInForm.tsx wrapped in `<AuthShell>`; `<Card className=\"max-w-md\">` outer dropped (D-16)."
    - "OIDC buttons render BEFORE 'Or with email' separator BEFORE email/password Form (D-17)."
    - "OidcButtons.tsx:66-80 adds `size=\"lg\" className=\"w-full\"` + `variant={p.id === \"oidc\" ? \"ghost\" : \"outline\"}` (D-18)."
    - "Password show/hide eye toggle using lucide-react `Eye`/`EyeOff` + `useState<boolean>(false)` + `aria-label` from i18n key (D-20)."
    - "shadcn `checkbox` vendored via `npx shadcn@latest add checkbox` (run from `apps/web/`); file at `apps/web/src/components/ui/checkbox.tsx`; SPDX header retrofit `// SPDX-License-Identifier: FSL-1.1-ALv2` as L1 (PATTERNS B4 risk)."
    - "'Remember this device' checkbox + RHF field `rememberDevice: boolean` + Better Auth `signIn.email({ rememberMe })` wired (D-21 + pitfall §13)."
    - "Forgot-password stays muted static text with sentinel comment `// Source of truth: D-UX2 (Phase 18.1.1) — anchor lands with Phase 19.1 reset-mail` (D-22)."
    - "SignUpForm.tsx wrapped in AuthShell with `sideTitle`/`sideQuote` from i18n (D-24)."
    - "Password strength meter present: 4-band `passwordStrength(value): {score, label}` helper colocated module-scope; 4px bar with color ramp `bg-red-500/orange-500/yellow-500/green-500`; labels from i18n with en+ru parity (D-25)."
    - "Terms checkbox: gated by D-47 routes probe. IF `apps/web/src/app/terms/` AND `…/privacy/` exist → wire `acceptedTerms: z.literal(true)` RHF field + checkbox + links (D-26). IF MISSING → defer to `.planning/deferred-items.md`, do NOT author placeholder pages."
    - "Sign-in footer + sign-up footer refactored to centered text + accent link (D-23 + D-28)."
    - "ZERO new top-level deps (D-44); `radix-ui` already at apps/web/package.json:33; `lucide-react` at L30."
    - "Coverage on AuthShell + Sign{In,Up}Form refactors ≥ 90/90/90/90 (D-42)."
    - "TDD shape: NEW AuthShell + NEW shadcn checkbox = 2-commit RED+GREEN; refactor of existing screens = single-GREEN where pre-existing assertions already covered the behavior, OR 2-commit when new behavior added (e.g., eye-toggle, remember-device, strength-meter)."
  artifacts:
    - path: "apps/web/src/components/screens/auth/AuthShell.tsx"
      provides: "Two-column branded auth layout shell."
      contains: "lg:grid lg:grid-cols-2"
    - path: "apps/web/src/components/ui/checkbox.tsx"
      provides: "Vendored shadcn Checkbox primitive."
      contains: "SPDX-License-Identifier: FSL-1.1-ALv2"
  key_links:
    - from: "SignInForm + SignUpForm"
      to: "AuthShell"
      via: "`<AuthShell sideTitle={…} sideQuote={…}>` wraps each screen content."
      pattern: "<AuthShell"
    - from: "Remember-device checkbox"
      to: "Better Auth signIn.email"
      via: "RHF `rememberDevice` → `authClient.signIn.email({ email, password, rememberMe })` (pitfall §13)."
      pattern: "rememberMe"
---

<objective>
Close Bucket B B1+B2+B3 — AuthShell scaffold + sign-in refactor + sign-up refactor (D-13..D-28).

**~6-8 atomic commits:**
1. AuthShell component RED+GREEN (D-13) — 2 commits
2. Public-layout centered-wrapper drop (D-14) — 1 commit
3. shadcn `checkbox` vendor + SPDX retrofit (D-21/D-26 prereq) — 1 commit (SAME as D-21 consumer per PATTERNS B4 risk)
4. SignInForm full refactor (D-16..D-23) — 2-3 commits depending on bundling:
   - Wrap + reorder (single)
   - Eye toggle + remember-device + i18n keys (RED+GREEN pair — NEW behavior)
5. SignUpForm full refactor (D-24..D-28) — 2 commits:
   - Wrap + strength meter (RED+GREEN — NEW behavior)
   - Terms checkbox CONDITIONAL on D-47 routes probe

Wave 1 — parallel with 18.1.1-01 + 18.1.1-02 (disjoint files: apps/web/* only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-CONTEXT.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-RESEARCH.md
@.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-PATTERNS.md
@.planning/phases/07-frontend-ui-spec/design/screens-user.jsx
@.planning/phases/07-frontend-ui-spec/design/ui.jsx
@apps/web/src/components/screens/auth/SignInForm.tsx
@apps/web/src/components/screens/auth/SignUpForm.tsx
@apps/web/src/components/screens/auth/OidcButtons.tsx
@apps/web/src/components/screens/AppShell.tsx
@apps/web/src/app/(public)/layout.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/stepper.tsx
@apps/web/package.json
@apps/web/src/locales/en/end-user.json
</files_to_read>

<tasks>

<task type="auto" tdd="true">
  <name>Task 18.1.1-04-01: D-13 AuthShell component scaffold (RED+GREEN pair)</name>
  <files>apps/web/src/components/screens/auth/AuthShell.tsx, apps/web/tests/unit/components/screens/auth/AuthShell.test.tsx, apps/web/src/locales/en/common.json, apps/web/src/locales/ru/common.json</files>
  <behavior>
- Test 1 (RED): render `<AuthShell><div data-testid="form">x</div></AuthShell>` → expect `[data-testid="form"]` visible AND sidebar `aside` element present.
- Test 2: pass `sideTitle="Welcome"` → assert text appears in side panel.
- Test 3: viewport `<lg` (e.g., 1023px width via Testing Library window mock) → side panel has `hidden` class OR not in DOM.
- Test 4: footer links Status/Docs/GitHub present with correct hrefs (i18n-driven).
- Test 5: defaults render i18n keys `common.auth.shell.kicker.default.text` etc.
  </behavior>
  <action>
Per D-13 + D-15 + D-43 + PATTERNS B1 + pitfall §10, §11.

**RED step:**
1. Create `apps/web/tests/unit/components/screens/auth/AuthShell.test.tsx` with React Testing Library — Tests 1-5 per behavior block. Will fail (component missing).
2. Commit RED: `test(18.1.1-04-01): red — AuthShell two-column layout + i18n props`. Body has RED log.

**GREEN step:**
3. Create `apps/web/src/components/screens/auth/AuthShell.tsx`:
   - L1: `// SPDX-License-Identifier: FSL-1.1-ALv2` (bare SPDX per pitfall §11 — NO `// Phase 18.1.1` markers).
   - L2: `"use client";` directive.
   - Imports: `next/link`, `react`, `react-i18next`, `@/components/ui/*` per import order from PATTERNS B1.
   - `useTranslation(["common"])`.
   - JSX skeleton from RESEARCH §4 (`<div className="min-h-screen lg:grid lg:grid-cols-2">…`).
   - Props: `{ children: ReactNode; sideTitle?: string; sideKicker?: string; sideQuote?: string }`.
   - Footer with `version + Status/Docs/GitHub` from i18n.
4. Add i18n keys IN SAME COMMIT (D-43):
   - `apps/web/src/locales/en/common.json`: `common.auth.shell.kicker.default.text`, `…title.default.text`, `…quote.default.text`, `…footer.status.text`, `…footer.docs.text`, `…footer.github.text`.
   - `apps/web/src/locales/ru/common.json`: same keys, Russian copy.
5. Verify `apps/web/src/app/globals.css` `@theme` block defines `bg-muted` (pitfall §10). If not, define it.
6. Run `pnpm --filter @openwhispr/web test AuthShell` — GREEN.
7. Commit GREEN: `feat(18.1.1-04-01): green — AuthShell two-column branded layout`. Body has GREEN log + i18n parity citation.

WHY: pitfall §10 — Tailwind 4 dropped `@layer plugin` shape; `bg-muted` needs `@theme` definition. pitfall §11 — bare SPDX only on new component files. D-43 — i18n parity in same commit.
  </action>
  <verify><automated>pnpm --filter @openwhispr/web test AuthShell 2>&1 | grep -E "(✓|PASS)" | head -5 && diff <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/en/common.json | sort) <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/ru/common.json | sort)</automated></verify>
  <done>2 commits; AuthShell component + tests + i18n parity; coverage ≥ 90/90/90/90; `web-typecheck` lefthook clean.</done>
</task>

<task type="auto">
  <name>Task 18.1.1-04-02: D-14 drop centered wrapper from (public)/layout.tsx (single GREEN)</name>
  <files>apps/web/src/app/(public)/layout.tsx</files>
  <action>
Per D-14.

1. Read `apps/web/src/app/(public)/layout.tsx` lines 17-23 (centered wrapper).
2. Replace centered wrapper with bare `{children}` (AuthShell now owns layout); keep LanguageSwitcher header absolute-positioned (will overlay right column of AuthShell).
3. Re-run `pnpm --filter @openwhispr/web test` — must stay GREEN (no test regressions).
4. Commit: `fix(18.1.1-04-02): drop centered wrapper from (public)/layout.tsx — AuthShell owns layout`.

WHY: D-14 — centered wrapper double-centers the AuthShell grid and breaks the two-column split.
  </action>
  <verify><automated>grep -c "max-w-md\|mx-auto.*items-center" apps/web/src/app/\(public\)/layout.tsx</automated></verify>
  <done>1 commit; centered wrapper removed; layout passes existing tests; LanguageSwitcher header preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-04-03: D-21/D-26 prereq — shadcn checkbox vendor + SPDX retrofit (single commit, RED+GREEN combined)</name>
  <files>apps/web/src/components/ui/checkbox.tsx</files>
  <behavior>
- Test: `apps/web/src/components/ui/checkbox.tsx` exists; first line is SPDX header; default export is Radix-based Checkbox.
  </behavior>
  <action>
Per D-21 + D-26 prereq + PATTERNS B4 + pitfall §4.

1. From `apps/web/` directory: `cd apps/web && npx shadcn@latest add checkbox` (note: `shadcn@latest`, NOT deprecated `shadcn-ui`).
2. If CLI prompts for path: answer with default (`src/components/ui/`).
3. shadcn writes `apps/web/src/components/ui/checkbox.tsx` from Radix-based registry.
4. **SPDX retrofit (PATTERNS B4 risk):** shadcn does NOT emit SPDX. Edit the file to prepend `// SPDX-License-Identifier: FSL-1.1-ALv2` as L1 (matches `input.tsx:1`, `stepper.tsx:1-2`).
5. **Radix subpath check (PATTERNS B4 risk):** If shadcn writes `import { ... } from "@radix-ui/react-checkbox"` and that subpath fails to resolve through umbrella `radix-ui`, swap to `radix-ui/checkbox` form (stepper.tsx precedent). Verify with `pnpm --filter @openwhispr/web typecheck`.
6. Verify NO new entry in `apps/web/package.json` `dependencies` block (D-44).
7. Commit: `feat(18.1.1-04-03): vendor shadcn Checkbox primitive + SPDX retrofit`. Body cites PATTERNS B4 risk + verifies no new top-level dep.

WHY: pitfall §4 — shadcn v2 CLI is `shadcn`, not `shadcn-ui`. PATTERNS B4 risk — shadcn output may need subpath adjustment.

Note: Single GREEN-only commit acceptable here per D-39 because the artefact is a vendored 3rd-party primitive, not "new functionality" in the testable sense. Test arrives via downstream consumers (Task 04 + 05).
  </action>
  <verify><automated>test -f apps/web/src/components/ui/checkbox.tsx && head -2 apps/web/src/components/ui/checkbox.tsx | grep -q "SPDX-License-Identifier" && pnpm --filter @openwhispr/web typecheck 2>&1 | tail -3 && git diff apps/web/package.json | grep -c "^+" # should be 0 (no new deps)</automated></verify>
  <done>1 commit; checkbox.tsx vendored + SPDX L1; typecheck clean; ZERO new top-level deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-04-04: D-16..D-23 SignInForm full refactor (RED+GREEN pair for new behaviors)</name>
  <files>apps/web/src/components/screens/auth/SignInForm.tsx, apps/web/src/components/screens/auth/OidcButtons.tsx, apps/web/tests/unit/components/screens/auth/SignInForm.test.tsx, apps/web/src/locales/en/end-user.json, apps/web/src/locales/ru/end-user.json, .planning/deferred-items.md</files>
  <behavior>
- Test 1 (RED): render SignInForm → assert `<AuthShell>` is ancestor (no outer `<Card max-w-md>`).
- Test 2: OIDC buttons render BEFORE "Or with email" separator BEFORE email/password Form (DOM order).
- Test 3: password show/hide eye toggle: click toggle → input type changes from `password` to `text` and aria-label updates.
- Test 4: "Remember this device" checkbox renders + RHF field `rememberDevice` toggles.
- Test 5: submit form with `rememberDevice: true` → `authClient.signIn.email` called with `rememberMe: true`.
- Test 6: forgot-password is muted text + sentinel comment exists in source.
  </behavior>
  <action>
Per D-16..D-23 + PATTERNS B2 + pitfall §13.

**RED step:**
1. Extend `apps/web/tests/unit/components/screens/auth/SignInForm.test.tsx` with Tests 1-6. Run — fails on multiple assertions.
2. Commit RED: `test(18.1.1-04-04): red — SignInForm AuthShell wrap + OIDC order + eye toggle + remember-device + sentinel`. Body has RED log.

**GREEN step:**
3. Edit `apps/web/src/components/screens/auth/SignInForm.tsx`:
   - L114: drop `<Card className="max-w-md">` outer; wrap content in `<AuthShell>` (D-16).
   - Reorder DOM: `<OidcButtons />` BEFORE "Or with email" separator BEFORE `<Form>` (D-17).
   - Add separator using text-in-rule Tailwind pattern: `<div className="relative"><Separator /><span className="absolute inset-0 flex items-center justify-center text-xs uppercase"><span className="bg-background px-2">{t("end-user.signin.separator.text")}</span></span></div>` (D-19).
   - Add password show/hide (D-20): wrap password Input in `<div className="relative">`; child `<Button variant="ghost" size="icon" className="absolute right-1.5 top-1.5 size-8" aria-label={t("end-user.signin.password.toggle.aria-label")}>` with `{shown ? <EyeOff /> : <Eye />}` from `lucide-react`. `useState<boolean>(false)`. `type={shown ? "text" : "password"}`.
   - Add "Remember this device" checkbox (D-21): import `Checkbox` from `@/components/ui/checkbox`; add RHF field `rememberDevice: z.boolean().default(false)` in schema; render `<Checkbox checked={field.value} onCheckedChange={field.onChange} />` + `<Label>`. Wire submit to call `authClient.signIn.email({ email, password, rememberMe: data.rememberDevice })` per pitfall §13.
   - Forgot-password (D-22): keep current muted text; ADD sentinel comment `// Source of truth: D-UX2 (Phase 18.1.1) — anchor lands with Phase 19.1 reset-mail` colocated above the JSX.
   - Footer (D-23): replace CardFooter-underline link with centered text + accent link `<p className="text-center text-sm"><Link className="text-primary">…</Link></p>`.
4. Edit `apps/web/src/components/screens/auth/OidcButtons.tsx:66-80` (D-18): add `size="lg" className="w-full"` props; change variant prop to `variant={p.id === "oidc" ? "ghost" : "outline"}`.
5. Add new i18n keys to `end-user.json` en+ru in same commit (D-43): separator text, eye-toggle aria-label, remember-device label.
6. Append `.planning/deferred-items.md` entry: `### From Phase 18.1.1 — Bucket B SR-B5` with disposition note (D-22 sentinel + Phase 19.1 anchor).
7. Re-run tests — GREEN.
8. Commit GREEN: `feat(18.1.1-04-04): green — SignInForm AuthShell wrap + OIDC reorder + eye toggle + remember-device + sentinel`. Body cites pitfall §13 + D-22 sentinel.

WHY: pitfall §13 — Better Auth `signIn.email` accepts `rememberMe` param. PATTERNS B2 — eye toggle is first-instance, uses Button ghost variant from OidcButtons precedent. D-22 — keep muted, defer to Phase 19.1.
  </action>
  <verify><automated>pnpm --filter @openwhispr/web test SignInForm 2>&1 | grep -E "(✓|PASS)" | head -10 && grep -c "Source of truth: D-UX2" apps/web/src/components/screens/auth/SignInForm.tsx && grep -c "AuthShell\|Eye\|EyeOff" apps/web/src/components/screens/auth/SignInForm.tsx</automated></verify>
  <done>2 commits (test:red + feat:green); 6 behaviors covered; sentinel present; deferred-items.md updated; i18n en+ru parity; coverage on SignInForm ≥ 90/90/90/90.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 18.1.1-04-05: D-24..D-28 SignUpForm refactor + D-47 routes probe (RED+GREEN pair)</name>
  <files>apps/web/src/components/screens/auth/SignUpForm.tsx, apps/web/tests/unit/components/screens/auth/SignUpForm.test.tsx, apps/web/src/locales/en/end-user.json, apps/web/src/locales/ru/end-user.json, .planning/deferred-items.md</files>
  <behavior>
- Test 1 (RED): SignUpForm wrapped in `<AuthShell sideTitle={…} sideQuote={…}>`.
- Test 2: password strength meter renders 4-band bar; varies width + color per `passwordStrength(value)` result.
- Test 3: `passwordStrength("weak")` returns `{score: 0, label: "weak"}`; `passwordStrength("Str0ngP@ssword123!")` returns `{score: 3, label: "strong"}`.
- Test 4 (CONDITIONAL on D-47 probe): terms checkbox renders + RHF `acceptedTerms: z.literal(true)` blocks submit unchecked. SKIP if D-47 routes absent.
- Test 5: footer centered text + accent link (D-28).
- Test 6: OIDC row present (D-27 — additive feature beyond oracle, kept).
  </behavior>
  <action>
Per D-24..D-28 + D-47 + PATTERNS B3 + pitfall §15.

**D-47 routes probe FIRST:**
1. Run `find apps/web/src/app -type d \( -name terms -o -name privacy \) 2>/dev/null`.
2. IF both `apps/web/src/app/terms/` AND `apps/web/src/app/privacy/` exist → implement Test 4 + terms checkbox.
3. IF EITHER missing → DEFER per D-47: append `.planning/deferred-items.md` entry `### From Phase 18.1.1 — Bucket B D-26 terms checkbox: routes absent, defer with phase 19.x anchor`. Do NOT author placeholder pages. Test 4 SKIPPED with `it.skip("terms checkbox — defer per D-47", …)`.

**RED step:**
4. Extend SignUpForm.test.tsx with Tests 1-6 (Test 4 conditional). Run — fails.
5. Commit RED: `test(18.1.1-04-05): red — SignUpForm AuthShell wrap + strength meter [+ terms if routes present]`.

**GREEN step:**
6. Edit `apps/web/src/components/screens/auth/SignUpForm.tsx`:
   - L81/96/177-178: wrap content in `<AuthShell sideTitle={t("end-user.signup.shell.sideTitle.text")} sideQuote={t("end-user.signup.shell.sideQuote.text")}>` (D-24).
   - Add module-scope `passwordStrength(value: string): {score: 0|1|2|3; label: "weak"|"fair"|"good"|"strong"}` helper above the component (D-25). Count-based banding: length ≥ 12 + mixed case + digit + symbol → score 3; partial criteria → 2/1/0. Inline pure function — NO new dep (D-44 + PATTERNS B3 risk).
   - Render 4-band bar: `<div className="h-1 w-full bg-muted"><div className={cn("h-full transition-all", strength.score === 0 && "w-1/4 bg-red-500", strength.score === 1 && "w-2/4 bg-orange-500", strength.score === 2 && "w-3/4 bg-yellow-500", strength.score === 3 && "w-full bg-green-500")} /></div>` + label `<p className="text-xs">{t(\`end-user.signup.strength.${strength.label}.text\`)}</p>`.
   - IF D-47 routes present: import `Checkbox`; add RHF `acceptedTerms: z.literal(true, { errorMap: () => ({ message: t("end-user.signup.terms.required") })})` to `signUpSchema`; render `<Checkbox /> + <Label>I agree to <Link href="/terms">Terms</Link> and <Link href="/privacy">Privacy</Link></Label>`.
   - Keep OIDC row (D-27); add comment `// D-27 — additive beyond oracle, NOT drift`.
   - Footer (D-28): same shape as D-23 from Task 04-04.
7. Add new i18n keys to en+ru in same commit (D-43): shell sideTitle/sideQuote, strength labels (weak/fair/good/strong), terms-required.
8. Run tests — GREEN.
9. Commit GREEN: `feat(18.1.1-04-05): green — SignUpForm AuthShell wrap + strength meter + [terms checkbox|terms defer]`. Body cites D-47 probe result + PATTERNS B3 + D-44.

WHY: pitfall §15 — D-47 routes probe BEFORE authoring terms link. PATTERNS B3 — NO `zxcvbn` (D-44 zero-new-deps); inline count-based banding.
  </action>
  <verify><automated>pnpm --filter @openwhispr/web test SignUpForm 2>&1 | grep -E "(✓|PASS|skip)" | head -10 && grep -c "passwordStrength\|AuthShell" apps/web/src/components/screens/auth/SignUpForm.tsx</automated></verify>
  <done>2 commits (test:red + feat:green); strength meter inline helper present; AuthShell wrap; D-47 probe result documented (in commit body OR deferred-items.md); i18n en+ru parity; coverage ≥ 90/90/90/90.</done>
</task>

</tasks>

<verification>
After all tasks:

```bash
cd /Users/dev/openwhispr-server

# Component tests
pnpm --filter @openwhispr/web test AuthShell SignInForm SignUpForm

# Typecheck (web-typecheck lefthook gate)
pnpm --filter @openwhispr/web typecheck

# i18n parity (D-43)
diff <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/en/end-user.json | sort) <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/ru/end-user.json | sort) | wc -l # expect 0
diff <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/en/common.json | sort) <(jq -r 'paths(scalars) | join(".")' apps/web/src/locales/ru/common.json | sort) | wc -l # expect 0

# Zero-new-deps (D-44)
git diff apps/web/package.json | grep -E "^\+.*\":" | grep -v "^+++" # expect empty

# Sentinel comment (D-22)
grep -c "Source of truth: D-UX2" apps/web/src/components/screens/auth/SignInForm.tsx

# Commit count: ~6-8 atomic
git log --oneline -n 12 | grep -cE "(fix|test|feat|chore)\(18\.1\.1-04-0[1-5]\)"

# Coverage gate
pnpm --filter @openwhispr/web test --coverage 2>&1 | grep -E "AuthShell|SignInForm|SignUpForm" | grep -E "9[0-9]\."
```
</verification>

<success_criteria>
1. AuthShell present with tests + i18n parity.
2. Public-layout centered wrapper dropped.
3. shadcn checkbox vendored + SPDX retrofit; zero new top-level deps.
4. SignInForm: AuthShell wrap, OIDC reorder, eye toggle, remember-device, sentinel — all GREEN.
5. SignUpForm: AuthShell wrap, strength meter, terms-checkbox (or defer note), centered footer — all GREEN.
6. ~6-8 atomic commits per D-39 (NEW behavior gets RED+GREEN pair; non-test refactors single commit).
7. i18n en+ru parity in every i18n-touching commit (D-43).
8. ZERO new top-level deps (D-44).
9. Coverage on AuthShell + SignInForm + SignUpForm ≥ 90/90/90/90 (D-42).
10. ZERO `--no-verify` (D-41).
</success_criteria>

<rollback>
- shadcn checkbox vendor breaks typecheck (PATTERNS B4 risk): swap import from `@radix-ui/react-checkbox` to `radix-ui/checkbox` matching stepper.tsx precedent; re-run typecheck.
- Tailwind 4 `bg-muted` undefined (pitfall §10): add `@theme { --color-muted: …; }` to `globals.css`; verify via `pnpm --filter @openwhispr/web build`.
- D-46 oracle divergence — HALT and emit 3-branch user choice (rebase to oracle / lock current spec / escalate to phase 18.1.3).
- Better Auth `rememberMe` param not present in installed version (pitfall §13): downgrade to local-only `localStorage.setItem("rememberDevice", true)` workaround? NO — that's a scope-reduction; instead HALT and check Better Auth version pin.
</rollback>

<output>
Create `.planning/phases/18.1.1-aggregate-sweep-test-debt/18.1.1-04-SUMMARY.md` with:
- 6-8 commit SHAs grouped by task
- D-47 routes probe result (present / deferred)
- i18n parity verification output
- Coverage report ≥ 90/90/90/90
- Zero-new-deps verification
- Sentinel + deferred-items.md citation
</output>
</content>
</invoke>