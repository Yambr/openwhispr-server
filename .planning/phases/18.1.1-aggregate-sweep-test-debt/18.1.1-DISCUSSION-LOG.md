# Phase 18.1.1 — Discussion Log

**Date:** 2026-05-15
**Mode:** discuss (advisor-style; 4 parallel `gsd-advisor-researcher` agents)
**User pacing:** yolo — "поднимай 18.1.1 теми же этапами" + "все этапы до конца последовательно" + escalation midflight: "блять куча мусора в докере и дизай авторизации старая хуйня до сих пор" + "нихуя не исправлено что я просил по дизайну во втором этапе".

## Trigger

Phase 18.1-07 CHECKPOINT (HALT) revealed 14 newly-surfaced test failures outside V2-MILESTONE-REVIEW inventory after Plans 01-06 closed their scoped 7 V2-REVIEW failures. User chose CLOSED-WITH-FOLLOWUP for v2 milestone + carve-out to Phase 18.1.1. Aggregate `pnpm test` re-run discovered scope is bigger: **54 failed test files**, dominated by Phase 15-02 STRUCT regression masked by per-workspace filter sweeps.

User pivoted mid-discuss to add Bucket B (UICONF auth drift): "дизай авторизации старая хуйня до сих пор" — Phase 12 left UICONF-05 axe baseline never executed + visual drift on 4 auth screens vs `screens-user.jsx` oracle. Phase 18.2 originally carved separately; user direction "не отложить — исправить сейчас" merged it into 18.1.1 as Bucket B.

User pivoted again on docker: "блять куча мусора в докере" — `docker system df` showed 30+ orphan testcontainers + 102 volumes + 10.37 GB build cache + 5 dangling images (2.83 GB). Aggressive prune freed 15.61 GB. Stack re-upped post-prune.

## Gray areas selected

User selected ALL by default ("yolo поднимай теми же этапами"). 4 advisor agents spawned in parallel:

1. **A1** — ENOENT path regression mass-fix scope + strategy
2. **A2** — wire-schemas resolution + ESBuild/oxc await PARSE_ERROR + testcontainer cleanup hygiene
3. **A3** — UICONF visual drift on 4 auth screens
4. **A4** — plan split + execution strategy

## Questions asked and decisions made

### Q1 — Bucket A path-fix strategy (A1 advisor)

**Options presented:**
- A. Per-test path-rewrite (Phase 18.1-01 precedent)
- B. Codemod via ts-morph sweep (mass mechanical fix)
- C. Wholesale mirror — copy `src/__fixtures__/` → `tests/unit/__fixtures__/`

**Decision (D-01..D-05):** **Option A.** Mixed categories (Cat-A/B/C/E) preclude uniform codemod. Mirror contradicts Phase 18.1 D-05.

**A1 advisor inventory:** 20 ENOENT failure sites P1-P20. P19 verified safe (already correct), P20 wire-schemas out-of-scope to A2. Net: 16 path-fix sites + 1 lint-extension commit (D-05 `findStalePathLiterals`).

**Cluster shape (D-02):** 11 commits across 9 clusters (Cluster 9 batches 5 packages/data sibling files). All pre-existing RED on `main` → single GREEN commit per cluster with verbatim error in body (per Phase 18.1 §7).

### Q2 — Bucket A pkg/transform/testcontainer (A2 advisor)

**Decisions (D-06..D-09):**

- **wire-schemas:** NOT broken — undeclared dep resolves via pnpm hoist. Fix: 1-line `workspace:*` dep entry + dep-graph lint test. 1 atomic commit.
- **ESBuild/oxc `await` PARSE_ERROR:** Vite 8 + Vitest 4 swapped to `vite:oxc` (stricter). Idiom `expect(() => Schema.parse(await res.json()))` invalid (await in non-async arrow). Fix: refactor to extract `const body = await res.json()` first + new `tools/lint-await-in-non-async.ts` AST linter. 1 atomic commit.
- **Testcontainer:** Reaper-helper exists at `apps/api/vitest.setup.ts` but not promoted to apps/worker + packages/data configs. Crash-before-SDK-init hole exists. Fix: promote to shared `tools/testcontainer-reaper-setup.ts` + session-id label filter. 2 atomic commits.
- **HALT protocol** per Phase 18.1-06: operator recipe if testcontainer orphans persist.

Total Bucket A pkg/transform/testcontainer: **4 atomic commits**.

### Q3 — Bucket B UICONF auth drift (A3 advisor)

**Decisions (D-13..D-36):**

Per-screen drift table:
- **U1 SignIn:** AuthShell missing, OIDC ordering wrong, "Or with email" separator label missing, password eye toggle missing, "Remember this device" checkbox missing, footer cosmetic. 5 fixes.
- **U2 SignUp:** AuthShell missing, password strength meter missing, terms checkbox missing, footer cosmetic. 4 fixes.
- **U3 VerifyEmail:** AuthShell missing, status badge icon missing. 2 fixes.
- **U-Setup:** AuthShell missing only (D-20 deviation stands). 1 fix.
- **AuthShell scaffold:** NEW component mapping oracle `ui.jsx:229-316` to Tailwind 4 + shadcn/ui v2.

**SR-B5 forgot-password disposition:** DEFER to Phase 19.1 reset-mail per A4. D-UX2 locked. Add sentinel comment.

**UICONF-05 axe baseline:** Already runtime via `runAxe()`. Missing: JSON baseline artefact + Playwright visual regression snapshots. Both ZERO new deps (Playwright built-in `toHaveScreenshot`).

**i18n parity:** 14 new keys (en+ru) per A3 §7.

**Shadcn checkbox:** install via `npx shadcn@latest add checkbox` — vendors source, NO top-level dep (radix-ui already at line 33).

### Q4 — Plan split (A4 advisor)

**Options presented:**
- A. 10 granular plans
- B. 6 mid-granular plans (RECOMMENDED)
- C. 4 mega-plans

**Decision (D-37..D-40):** **Option B.** Wave graph:
```
Wave 1 (parallel): 01 ‖ 02 ‖ 04
Wave 2:            03 (depends 01,02), 05 (depends 04)
Wave 3:            06 (depends 01..05)
```

**TDD commit shape (D-39):** Pre-existing RED → single GREEN commit per cluster. New functionality → 2-commit RED+GREEN pair.

**Atomic-commit estimate:** ~28-32. Runtime: ~4-4.5 hours.

### Q5 — Docker garbage (mid-discuss user pivot)

**Decision:** Aggressive `docker system prune -af --volumes`. Freed 15.61 GB. Stack re-upped post-prune (`make up-with-observability` exit 0).

### Q6 — Phase 18.2 fate

**Decision:** MERGED into 18.1.1 Bucket B per user direction "не отложить — исправить сейчас". Phase 18.2 removed from queue.

## Deferred ideas

1. **Phase 18.1.2** — for Bucket A surfaces not in D-02 inventory (per A4 scope guard).
2. **Phase 19.1/19.2/19.3/19.4** — real downstream code for repointed @cjm tags (unchanged).
3. **Terms + Privacy pages** — if routes don't exist (D-26 scope-out).
4. **A2 Option C two-tier rate-limit** — v3 (unchanged).
5. **Chromatic** — rejected, Playwright built-in screenshots cover it.
6. **Confirm-password field on sign-up** — i18n key dormant; oracle doesn't show; leave.
7. **Operator-side**: FSL history scrub, first real GHA e2e-cjm CI run, DCO bot wiring.
8. **`pnpm test:lint-cjm-doc` script wiring** — Phase 18.1-05 deviation; planner can wire if trivial.

## Claude's discretion items (no user input requested)

- Plan split: 6 plans (Option B) per A4 advisor — mirrors Phase 18.1's 7-plan cardinality, respects cluster orthogonality.
- Cluster commit shape for Bucket A: single GREEN per cluster (pre-existing RED on main); 2-commit pair only for new lint/AST tools.
- Mobile-first AuthShell: side panel hidden `<lg` (oracle was desktop-mocked, but Tailwind 4 implies mobile-first as constitutional UX rule).
- v2.1 milestone declaration at 18.1.1-06 close (was CLOSED-WITH-FOLLOWUP after Phase 18.1; this phase brings to clean CLOSED).
- 30 new test surfaces (lint rules + dep-graph + reaper integration + axe baseline diff + visual screenshot tests) — coverage on diff requirement still 90/90/90/90.
- HALT-and-escalate recipes follow Phase 18.1-06 three-branch user-choice pattern.
- 4 advisors returned within wall-clock window — bounce loop budget tight.

## Research artifacts

All 4 advisors returned findings inline. Key findings embedded in CONTEXT.md `<decisions>` D-01 through D-48.

- **A1** (path regression): 20 sites P1-P20 categorized A/B/C/E; 13 atomic commits including lint-extension; existsSync-guard hardening per Phase 18.1 D-04.
- **A2** (pkg/transform/testcontainer): wire-schemas NOT broken (just undeclared dep); ESBuild→oxc transformer regression; reaper-helper promotion + session-id label hardening.
- **A3** (UICONF auth): per-screen drift tables + AuthShell scaffold shape + UICONF-05 baseline 2-part plan (axe JSON + Playwright snapshots).
- **A4** (plan split): Option B 6 plans, cluster-RED+cluster-GREEN for mass codemod, SR-B5 forgot-password DEFER confirmed.
