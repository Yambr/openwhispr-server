---
phase: 33-envelope-encrypt-credentials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/data/migrations/0019_envelope_encrypt_secret_columns_add.sql
  - packages/data/migrations/0019_envelope_encrypt_secret_columns_add.down.sql
  - packages/data/migrations/__tests__/0019-envelope-encrypt-secret-columns-add.test.ts
  - packages/data/migrations/meta/_journal.json
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CRIT-FIX-02
must_haves:
  truths:
    - "Migration 0019 applies forward cleanly on a fresh PG 17 testcontainer with all prior migrations + 0018 RLS pattern preserved"
    - "Every one of the 8 credential columns gains exactly 6 nullable bytea sidecars (dek_wrapped, dek_iv, dek_auth_tag, value_iv, value_auth_tag, value_ciphertext)"
    - "sessions.token_fp + sessions.previous_token_fp bytea(32) columns exist with UNIQUE INDEX on token_fp (NOT NULL deferred to 0020) and partial INDEX on previous_token_fp"
    - "Plaintext columns are untouched (additive only) — sessions.token + previous_token remain text with sessions_token_unique still active"
    - "Down migration rolls back cleanly on the same testcontainer"
    - "ROADMAP Phase 33 entry corrected from '0018_envelope_encrypt_secret_columns.sql' to '0019_envelope_encrypt_secret_columns_add.sql' + '0020_envelope_encrypt_secret_columns_drop_plaintext.sql'"
  artifacts:
    - path: "packages/data/migrations/0019_envelope_encrypt_secret_columns_add.sql"
      provides: "Additive bytea sidecar columns + token_fp / previous_token_fp + indexes"
      contains: "ALTER TABLE \"account\" ADD COLUMN \"access_token_value_ciphertext\" bytea"
    - path: "packages/data/migrations/0019_envelope_encrypt_secret_columns_add.down.sql"
      provides: "Rescue rollback DDL (not in journal)"
    - path: "packages/data/migrations/__tests__/0019-envelope-encrypt-secret-columns-add.test.ts"
      provides: "Forward + rollback verification on real PG 17 testcontainer"
  key_links:
    - from: "0019.sql"
      to: "bootMigratedPostgres"
      via: "drizzle-orm migrator picks up journal entry"
      pattern: "_journal.json.*0019"
---

# Phase 33 / Plan 01 — Migration 0019: additive bytea sidecars + fingerprints

**Requirement closed (partial):** CRIT-FIX-02 — additive migration step.

## Goal

Land migration `0019_envelope_encrypt_secret_columns_add.sql` introducing 48 new nullable bytea sidecar columns (6 per credential × 8 credentials) plus `sessions.token_fp` / `sessions.previous_token_fp` bytea(32) fingerprint sidecars and their indexes. **Additive only:** plaintext columns and existing indexes stay intact so 33-02..04 can land in any rollback-safe order before 33-05 drops plaintext.

## Depends on

- Phase 32 (0018 fail-closed RLS) — landed.
- DISCIPLINE Rules 1-14.
- ROADMAP locks (migration filename corrected this plan).

## Authoritative corrections incorporated

- **6 bytea per credential, not 4** (research §Q2; CONTEXT 4-bytea claim is wrong). Matches `EncryptedRow` shape in `packages/data/src/encryption/envelope.ts:37-44`.
- **`sessions.token_fp` SHA-256 fingerprint sidecar** required to preserve O(log N) lookupByToken once plaintext drops (research §Q4 / pitfall #3). Mirror for `previous_token`.
- **Migration filename: 0019, not 0018** (Phase 32 took 0018).

## Column matrix (48 + 2 sidecars + 2 indexes)

| Table | Column | 6 sidecars added | Fingerprint sidecar |
|---|---|---|---|
| `account` | `access_token` | `access_token_{dek_wrapped,dek_iv,dek_auth_tag,value_iv,value_auth_tag,value_ciphertext}` | — |
| `account` | `refresh_token` | `refresh_token_{…}` (6 cols) | — |
| `account` | `id_token` | `id_token_{…}` (6 cols) | — |
| `account` | `password` | `password_{…}` (6 cols) | — |
| `verification` | `value` | `value_{…}` (6 cols) | — |
| `sessions` | `token` | `token_{…}` (6 cols) | `token_fp` bytea(32) — UNIQUE INDEX (nullable in 0019, NOT NULL flipped in 0020) |
| `sessions` | `previous_token` | `previous_token_{…}` (6 cols) | `previous_token_fp` bytea(32) — partial INDEX WHERE NOT NULL |
| `oauth_state` | `code_verifier` | `code_verifier_{…}` (6 cols) | — |

**Total:** 48 bytea sidecars + 2 fingerprint cols + 2 new indexes.

## Task breakdown (strict RED → GREEN → REFACTOR per DISCIPLINE Rule 1)

### Task 1 — RED: migration test asserts new columns exist

**Files:** `packages/data/migrations/__tests__/0019-envelope-encrypt-secret-columns-add.test.ts` (NEW)

**What fails:** Test boots PG 17 via `bootMigratedPostgres` (Phase 32 pattern), then queries `information_schema.columns` asserting each of the 48 bytea columns + `sessions.token_fp` + `sessions.previous_token_fp` exists with `data_type='bytea'` and `is_nullable='YES'`. Asserts `pg_indexes` contains `sessions_token_fp_unique` + `sessions_previous_token_fp_idx`. Asserts plaintext columns (`account.access_token`, etc.) STILL present (additive only). Test fails RED because migration file does not yet exist — `bootMigratedPostgres` raises during journal replay.

**Commit:** `test(33-01): red — 0019 migration test asserts 48 bytea sidecars + 2 fingerprints`

### Task 2 — GREEN: migration 0019 forward DDL

**Files:**
- `packages/data/migrations/0019_envelope_encrypt_secret_columns_add.sql` (NEW)
- `packages/data/migrations/meta/_journal.json` (journal entry appended)

**What passes:** Forward DDL adds the 48 bytea cols + 2 fingerprint cols + 2 indexes. Uses `ALTER TABLE … ADD COLUMN IF NOT EXISTS` for idempotency on re-runs. Index creation uses `CREATE UNIQUE INDEX IF NOT EXISTS "sessions_token_fp_unique" ON sessions(token_fp) WHERE token_fp IS NOT NULL` (partial-unique to permit pre-backfill NULLs). `previous_token_fp` index identical pattern. Header documents the 6-bytea shape per research §Q2 with citation. Test from Task 1 turns GREEN.

**Commit:** `feat(33-01): green — 0019 additive bytea sidecars + sha256 fingerprints`

### Task 3 — GREEN: down migration + rollback test path

**Files:**
- `packages/data/migrations/0019_envelope_encrypt_secret_columns_add.down.sql` (NEW; rescue-only, NOT journaled)
- extension to Task 1 test file: rollback assertion block

**What passes:** Down DDL drops the 48+2 columns + 2 indexes. Test extension: after forward, apply down via `psql -f`, re-query `information_schema.columns`, assert all 50 cols gone, plaintext cols still intact. Mirrors Phase 32 0018 rescue-down pattern (SUMMARY §"Companion `0018_rls_fail_closed.down.sql`").

**Commit:** `feat(33-01): green — 0019 down migration (rescue, not journaled)`

### Task 4 — REFACTOR: ROADMAP filename correction

**Files:** `.planning/ROADMAP.md`

**What passes:** Replace every `0018_envelope_encrypt_secret_columns.sql` occurrence with the canonical two-file pair `0019_envelope_encrypt_secret_columns_add.sql` + `0020_envelope_encrypt_secret_columns_drop_plaintext.sql`. Same edit lands in `.planning/REQUIREMENTS.md` CRIT-FIX-02 entry (currently says `0018_envelope_encrypt_secret_columns.sql`). Both edits in ONE refactor commit (no production code change).

**Commit:** `refactor(33-01): roadmap+requirements — migration 0018 → 0019/0020 split`

## Verification gate

```bash
pnpm --filter @openwhispr/data exec vitest run \
  migrations/__tests__/0019-envelope-encrypt-secret-columns-add.test.ts
# Expect: 1 file passed / N tests passed, exit 0

pnpm --filter @openwhispr/data exec vitest run --coverage \
  migrations/__tests__/0019-envelope-encrypt-secret-columns-add.test.ts
# Expect: lines/branches/functions/statements ≥ 90/90/90/90 on the new test file

pnpm lint:lockers
# Expect: exit 0 (no new violations)
```

## Risks + mitigations (from research §15 pitfalls)

| Risk | Mitigation |
|---|---|
| Pitfall #1 (drizzle-kit cannot emit bytea sidecar DDL from schema) | Hand-author migration; schema declarations NOT updated in this plan — that lands in 33-05 alongside plaintext drop. |
| Pitfall #3 (UNIQUE INDEX on `sessions.token` dies under ciphertext) | Pre-create `sessions_token_fp_unique` partial-unique index in 0019; 33-03 backfills `token_fp`; 33-05 flips NOT NULL + drops `sessions_token_unique`. |
| Pitfall #5 (journal drift) | Journal entry appended in the SAME commit as the SQL file; test boots via `bootMigratedPostgres` which replays journal — drift caught immediately. |
| ROADMAP "0018" drift causing reviewer confusion | Task 4 corrects ROADMAP + REQUIREMENTS in atomic commit. |

## Out of scope (this plan)

- Lens implementation (33-02).
- Backfill script (33-03).
- Better Auth wiring (33-04).
- Plaintext column DROP + schema declarations (33-05).
- `lookup_session_by_previous_token(text)` function rewrite (33-04).
- Test deletion in `tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts` (33-05).
