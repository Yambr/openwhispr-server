---
phase: 33-envelope-encrypt-credentials
plan: 05
type: execute
wave: 5
depends_on: [33-01, 33-02, 33-03, 33-04]
files_modified:
  - packages/data/migrations/0020_envelope_encrypt_secret_columns_drop_plaintext.sql
  - packages/data/migrations/0020_envelope_encrypt_secret_columns_drop_plaintext.down.sql
  - packages/data/migrations/__tests__/0020-drop-plaintext.test.ts
  - packages/data/migrations/meta/_journal.json
  - packages/data/src/schema/accounts.ts
  - packages/data/src/schema/sessions.ts
  - packages/data/src/schema/verifications.ts
  - packages/data/src/schema/oauth_state.ts
  - tools/lint-no-plaintext-secret-columns.ts
  - tools/__tests__/lint-no-plaintext-secret-columns.test.ts
  - lefthook.yml
  - Makefile
  - package.json
  - .github/workflows/ci.yml
  - .github/workflows/nightly.yml
  - .planning/DISCIPLINE.md
  - CLAUDE.md
  - docs/security.md
  - tests/e2e/encryption-at-rest.test.ts
  - packages/data/tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/33-envelope-encrypt-credentials/33-SUMMARY.md
  - .planning/phases/33-envelope-encrypt-credentials/33-COVERAGE.md
autonomous: true
requirements:
  - CRIT-FIX-02
  - LOCKER-PLAINTEXT-COLS
must_haves:
  truths:
    - "Migration 0020 drops the 8 plaintext columns AND sessions_token_unique index AND flips sessions.token_fp to NOT NULL + makes sessions_token_fp_unique a full UNIQUE INDEX"
    - "Drizzle schema declarations (accounts.ts, sessions.ts, verifications.ts, oauth_state.ts) updated to bytea-only sidecars + fingerprint cols — plaintext columns removed from schema"
    - "tools/lint-no-plaintext-secret-columns.ts AST-scans packages/data/src/schema/** and REFUSES (exit non-zero) any text|varchar|char column whose name matches /^(access_token|refresh_token|password|id_token|value|token|previous_token|code_verifier)$/"
    - "Locker ships BLOCKING from day one (no WARN flip) — refuses introduction of new plaintext credential columns"
    - "DISCIPLINE.md Rule 15 inserted (LOCKER-08 / LOCKER-PLAINTEXT-COLS)"
    - "CLAUDE.md § Engineering Discipline mirror updated to include Rule 15"
    - "lefthook.yml + Makefile + package.json + ci.yml + nightly.yml all wire the new locker into pnpm lint:lockers + pre-commit + CI matrix"
    - "docs/security.md §12 added: scope, MASTER_KEK env requirements, KEK rotation runbook, KMS recipes (AWS/GCP/Azure/Vault)"
    - "tests/e2e/encryption-at-rest.test.ts boots real docker compose stack, signs in, asserts raw DB ciphertext-on-disk, and asserts the linter exits non-zero on a deliberately-broken fixture"
    - "5 obsolete tests in 0003_better_auth_tenant_defaults.test.ts deleted (Phase 32 DEFERRED + research §Q13)"
    - "ALL of the above land in ONE atomic commit per LOCKER-07 precedent — verifier rejects splits"
    - "Coverage on tools/lint-no-plaintext-secret-columns.ts + 0020 migration test + e2e test ≥ 90/90/90/90"
  artifacts:
    - path: "packages/data/migrations/0020_envelope_encrypt_secret_columns_drop_plaintext.sql"
      provides: "DROP COLUMN × 8 + DROP INDEX sessions_token_unique + ALTER COLUMN token_fp SET NOT NULL + recreate sessions_token_fp_unique as full UNIQUE"
    - path: "tools/lint-no-plaintext-secret-columns.ts"
      provides: "AST scan refusing plaintext credential column declarations"
    - path: ".planning/DISCIPLINE.md"
      provides: "Rule 15 (LOCKER-PLAINTEXT-COLS) inserted after Rule 14"
    - path: "CLAUDE.md"
      provides: "Mirror of Rule 15"
    - path: "docs/security.md"
      provides: "§12 Encryption at rest — operator runbook"
    - path: "tests/e2e/encryption-at-rest.test.ts"
      provides: "Compose-stack end-to-end ciphertext-on-disk verification"
  key_links:
    - from: "0020.sql"
      to: "0019.sql + backfill"
      via: "Phase 33 deploy sequence: 0019 → 33-04 deploy → backfill → 0020"
      pattern: "_journal.json.*0020"
    - from: "tools/lint-no-plaintext-secret-columns.ts"
      to: "pnpm lint:lockers"
      via: "Makefile target lint:lockers aggregates all LOCKER-* binaries"
      pattern: "lint-no-plaintext-secret-columns"
    - from: ".planning/DISCIPLINE.md Rule 15"
      to: "CLAUDE.md mirror"
      via: "constitutional rule replication"
      pattern: "LOCKER-PLAINTEXT-COLS"
---

# Phase 33 / Plan 05 — Atomic closure: 0020 drop-plaintext + schema flip + LOCKER-PLAINTEXT-COLS + DISCIPLINE Rule 15 + docs + e2e + obsolete-test cleanup

**Requirement closed:** CRIT-FIX-02 (final closure) + LOCKER-PLAINTEXT-COLS (Rule 15 introduction).

## Goal

The closing commit of Phase 33. Per LOCKER-07 atomic-invariant precedent (DISCIPLINE Rule 14), this plan lands the **entire encryption-at-rest closure bundle in ONE atomic commit**: drop-plaintext migration 0020, Drizzle schema flip, LOCKER-PLAINTEXT-COLS source + tests, DISCIPLINE Rule 15 + CLAUDE.md mirror, lefthook/Makefile/CI wiring, docs/security.md §12, the encryption-at-rest e2e test, deletion of 5 obsolete Phase-32 tests, and the SUMMARY + COVERAGE reports. The verifier MUST reject any attempt to split this into multiple commits.

## Depends on

- 33-01 (sidecar columns exist).
- 33-02 (lens + boot validator exist).
- 33-03 (backfill exists; operator MUST have invoked it on every environment before 0020 lands).
- 33-04 (lens wired into production code; lookup function rewritten; oauth_state hooks landed).
- DISCIPLINE Rules 1-14 + LOCKER-07 atomic-invariant precedent.
- CLAUDE.md Hard Rule 1 (no test-only edits to production).
- Phase 32 SUMMARY §"Open items moved out of Phase 32" — references the 5 obsolete tests this plan deletes.

## Task breakdown (RED → GREEN per DISCIPLINE Rule 1; bundled into ONE atomic commit per LOCKER-07)

> **Atomicity note.** Per LOCKER-07 precedent (`.planning/DISCIPLINE.md` rule 14 final paragraph), the closure commit ships every artefact below in ONE commit so the locker + the rule + the CI wiring + the schema state cannot drift apart. The "RED → GREEN" structure below is per-subtask test ordering within the developer's working tree; the final commit boundary is singular.

### Task 1 — RED: locker test refuses plaintext credential column

**Files:** `tools/__tests__/lint-no-plaintext-secret-columns.test.ts` (NEW)

**What fails:** Cases:
1. Fixture: a TS file declaring `accessToken: text("access_token")` → linter exits non-zero; stderr mentions the field name.
2. Fixture: `password: text("password")` → exit non-zero.
3. Fixture: `value: varchar("value", { length: 256 })` → exit non-zero (varchar variant).
4. Fixture: `token: char("token", { length: 32 })` → exit non-zero (char variant).
5. Negative fixture: `accessTokenValueCiphertext: bytea("access_token_value_ciphertext")` → exit 0.
6. Negative fixture: a non-credential text column (`email: text("email")`) → exit 0.
7. Real-codebase run after schema flip: linter against `packages/data/src/schema/**` exits 0 (Task 4 must complete first; this assertion runs in Task 7 verification).

All fail RED — `tools/lint-no-plaintext-secret-columns.ts` does not exist.

### Task 2 — GREEN: locker implementation

**Files:** `tools/lint-no-plaintext-secret-columns.ts` (NEW)

**What passes:** TypeScript AST scan via `ts-morph` (existing project pattern from LOCKER-01..06). Walks `packages/data/src/schema/**/*.ts`. For each property assignment of shape `<name>: <fn>("<colname>", ...)`: if `fn.name` ∈ {`text`,`varchar`,`char`} and `<colname>` matches `/^(access_token|refresh_token|password|id_token|value|token|previous_token|code_verifier)$/`, emit violation. Exit 1 on any violation; exit 0 otherwise. CLI flags: `--warn-only` honored (locker ships BLOCKING — no project allowlist; flag exists for nightly noise budget only). All Task 1 cases GREEN.

### Task 3 — RED: 0020 migration test

**Files:** `packages/data/migrations/__tests__/0020-drop-plaintext.test.ts` (NEW)

**What fails:** Test boots PG via `bootMigratedPostgres` extended to apply migrations through 0019 + backfill (synthetic-seed step) + 0020. Asserts:
1. `information_schema.columns` no longer contains `account.access_token`, `account.refresh_token`, `account.id_token`, `account.password`, `verification.value`, `sessions.token`, `sessions.previous_token`, `oauth_state.code_verifier` (text columns).
2. `pg_indexes` no longer contains `sessions_token_unique`; instead contains `sessions_token_fp_unique` as a non-partial UNIQUE INDEX.
3. `sessions.token_fp` and `sessions.previous_token_fp` `is_nullable='NO'`.
4. The 48 bytea sidecars are still present.
5. Down migration reverses cleanly (rescue path).

RED until 0020.sql exists.

### Task 4 — GREEN: 0020 forward + down migration

**Files:**
- `packages/data/migrations/0020_envelope_encrypt_secret_columns_drop_plaintext.sql` (NEW)
- `packages/data/migrations/0020_envelope_encrypt_secret_columns_drop_plaintext.down.sql` (NEW, rescue)
- `packages/data/migrations/meta/_journal.json` (appended)

**What passes:** Forward DDL:
- `ALTER TABLE … DROP COLUMN access_token` (×8 across 4 tables)
- `DROP INDEX IF EXISTS sessions_token_unique`
- `DROP INDEX IF EXISTS sessions_previous_token_idx`
- `DROP INDEX IF EXISTS sessions_token_fp_unique` (the partial-unique from 0019) → `CREATE UNIQUE INDEX sessions_token_fp_unique ON sessions(token_fp)` (full UNIQUE)
- `ALTER TABLE sessions ALTER COLUMN token_fp SET NOT NULL`
- `ALTER TABLE sessions ALTER COLUMN previous_token_fp DROP NOT NULL` (stays nullable — overlap-window optional)

Down migration: ADD COLUMN with `text` type and NULL values; recreate `sessions_token_unique`. Operator restores plaintext via rescue script (not automated — documented in `docs/security.md` §12 rotation/rollback subsection). Task 3 GREEN.

### Task 5 — GREEN: Drizzle schema declarations flip

**Files:** `packages/data/src/schema/accounts.ts`, `sessions.ts`, `verifications.ts`, `oauth_state.ts`

**What passes:** For each plaintext credential column: remove the `text(...)` declaration; add 6 `bytea` columns + optional `tokenFp: customType<{data: Buffer}>("token_fp").notNull()` (sessions only). Replace `tokenUnique: uniqueIndex("sessions_token_unique").on(t.token)` with `tokenFpUnique: uniqueIndex("sessions_token_fp_unique").on(t.tokenFp)`. The `previousTokenIdx` becomes `previousTokenFpIdx`. **Locker run AT THIS POINT exits 0** — the closure invariant. If the locker exits non-zero, the bundle cannot land; executor must investigate which column slipped through.

### Task 6 — GREEN: DISCIPLINE Rule 15 + CLAUDE.md mirror + integration wiring

**Files:**
- `.planning/DISCIPLINE.md` — append Rule 15 after Rule 14: *"15. **No plaintext credential columns.** `text|varchar|char` columns whose name matches `/^(access_token|refresh_token|password|id_token|value|token|previous_token|code_verifier)$/` are REFUSED in `packages/data/src/schema/**`. Enforced by `tools/lint-no-plaintext-secret-columns.ts` (LOCKER-PLAINTEXT-COLS / LOCKER-08). BLOCKING from day one — no WARN flip, no allowlist. Defence-in-depth for envelope-encryption-at-rest (CRIT-FIX-02, Phase 33)."*
- `CLAUDE.md` § Engineering Discipline — same paragraph mirrored.
- `Makefile` — add invocation to `lint:lockers` aggregate.
- `package.json` — add scripts `lint:no-plaintext-secret-columns` + `test:lint-no-plaintext-secret-columns`.
- `lefthook.yml` — add to pre-commit `lockers` group.
- `.github/workflows/ci.yml` — invocation in the lockers job; BLOCKING.
- `.github/workflows/nightly.yml` — invocation in the `lockers-nightly` job (already exists).

### Task 7 — GREEN: docs/security.md §12 + e2e test

**Files:**
- `docs/security.md` — append §12 "Encryption at rest":
  - Scope (8 columns / 4 tables / 48 bytea sidecars + 2 fingerprints).
  - `MASTER_KEK` env requirements (32 bytes, base64url-encoded, never logged, rotated via overlap-window provider chain).
  - KEK rotation runbook (generate KEK_v2 → deploy with `MASTER_KEK_NEW` + `MASTER_KEK` overlap → re-wrap migrator (future v2 phase) → retire KEK_v1).
  - KMS provisioning recipes for AWS KMS / GCP KMS / Azure Key Vault / HashiCorp Vault (each: ≤ 10 lines, env-var output).
  - Rollback rescue procedure pointing at `0020_…down.sql`.
  - LOCKER-PLAINTEXT-COLS defence-in-depth note.
- `tests/e2e/encryption-at-rest.test.ts` (NEW) — boots `docker compose up -d` (real stack), runs a sign-in flow via HTTP, opens a raw `pg.Client` against the compose-exposed PG port, asserts `account.password_value_ciphertext` non-null + `account.password` column DOES NOT EXIST (post-0020). Second assertion in same test: deliberately introduce a broken schema fixture, run `pnpm lint:no-plaintext-secret-columns` via `execFileSync` (argv-array form per LOCKER-06), assert exit code ≠ 0. Gated on `E2E=1` per DISCIPLINE Rule 3.

### Task 8 — GREEN: delete obsolete Phase-32-deferred tests

**Files:** `packages/data/tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts`

**What passes:** Delete the 5 cases listed in Phase 32 `32-DEFERRED.md` Category A (rolconfig assertions + 4 default-tenant DEFAULT assertions). These cases assert OBSOLETE pre-Phase-32 fail-open posture AND further break under Phase 33 ciphertext storage (they INSERT into `account.password` as text). Per research §Q13: deletion preferred over rewrite — they assert behavior that is now anti-invariant. A single replacement assertion is added to the same file: *"Phase 32+33 net effect — rolconfig empty + column DEFAULTs dropped + credential cols are bytea-only"* — a 3-line introspection check.

### Task 9 — GREEN: REQUIREMENTS + ROADMAP closure + 33-SUMMARY + 33-COVERAGE

**Files:**
- `.planning/REQUIREMENTS.md` — flip CRIT-FIX-02 status `Pending` → `Closed`; add LOCKER-PLAINTEXT-COLS entry as Phase 33 deliverable.
- `.planning/ROADMAP.md` — Phase 33 line `[ ]` → `[x]`; correct any remaining "0018" → "0019/0020" drift (33-01 caught it but full-file grep here for completeness).
- `.planning/phases/33-envelope-encrypt-credentials/33-SUMMARY.md` (NEW) — mirrors Phase 32 SUMMARY format: what landed, atomic commit log, verification commands, decisions worth flagging, open items.
- `.planning/phases/33-envelope-encrypt-credentials/33-COVERAGE.md` (NEW) — per-file before/after on 4 axes per DISCIPLINE Rule 10.

### Single atomic closure commit

After all 9 tasks are working in the dev tree (locker green on real schema, e2e green on real compose stack, all unit/integration suites green, coverage ≥ 90/90/90/90 on diff), execute ONE commit:

```
git add packages/data/migrations/0020*.sql \
        packages/data/migrations/__tests__/0020-drop-plaintext.test.ts \
        packages/data/migrations/meta/_journal.json \
        packages/data/src/schema/{accounts,sessions,verifications,oauth_state}.ts \
        tools/lint-no-plaintext-secret-columns.ts \
        tools/__tests__/lint-no-plaintext-secret-columns.test.ts \
        lefthook.yml Makefile package.json \
        .github/workflows/ci.yml .github/workflows/nightly.yml \
        .planning/DISCIPLINE.md CLAUDE.md docs/security.md \
        tests/e2e/encryption-at-rest.test.ts \
        packages/data/tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts \
        .planning/REQUIREMENTS.md .planning/ROADMAP.md \
        .planning/phases/33-envelope-encrypt-credentials/33-SUMMARY.md \
        .planning/phases/33-envelope-encrypt-credentials/33-COVERAGE.md
git commit -m "feat(33-05): atomic closure — drop plaintext + LOCKER-PLAINTEXT-COLS + Rule 15 + docs/security §12 + e2e"
```

Verifier check (LOCKER-07 atomic invariant): the commit MUST include the locker source + the DISCIPLINE rule + the CI wiring + the schema flip + the migration. If any artefact is missing, the commit is rejected by the verifier.

## Verification gate

```bash
pnpm --filter @openwhispr/data exec vitest run --coverage \
  migrations/__tests__/0020-drop-plaintext.test.ts \
  tests/unit/__tests__/0003_better_auth_tenant_defaults.test.ts
# Expect: 2 files passed; coverage on 0020 migration test ≥ 90/90/90/90

pnpm exec vitest run --coverage \
  tools/__tests__/lint-no-plaintext-secret-columns.test.ts
# Expect: 1 file passed; coverage on tools/lint-no-plaintext-secret-columns.ts ≥ 90/90/90/90

pnpm lint:no-plaintext-secret-columns
# Expect: exit 0 (no violations after schema flip)

pnpm lint:lockers
# Expect: exit 0 (all 8 lockers green)

E2E=1 pnpm exec vitest run --config tests/e2e/vitest.e2e.config.ts \
  tests/e2e/encryption-at-rest.test.ts
# Expect: 1 file passed, exit 0
```

## Risks + mitigations (from research §15 pitfalls + LOCKER-07 precedent)

| Risk | Mitigation |
|---|---|
| Pitfall #1 (drizzle-kit emits text-column DDL when introspecting bytea sidecars) | Schema declarations use `bytea(...)` explicitly; drizzle-kit `generate` regression caught by Task 1 locker — exits non-zero if it slips. |
| Pitfall #7 (rolling back 0020 destroys ciphertext) | Down migration is rescue-only (NOT journaled); operator runbook in `docs/security.md` §12 documents the cost (must run reverse-backfill before 0020 down can preserve data). |
| Pitfall #12 (atomic commit splits) | LOCKER-07 precedent: single commit gates verifier; verifier rejects splits by inspecting the commit DAG for the artefact set. |
| Pitfall #13 (locker exempt-allowlist gradually accumulates) | Day-one BLOCKING with NO allowlist. Any future legitimate exception requires a phase carve-out + DISCIPLINE amendment — not a flag flip. |
| Lefthook + Makefile + CI workflows + DISCIPLINE rule drift | All 5 wiring points edited in the same commit. CI workflow runs the locker as a separate job — orchestrator inspects job result. |
| Deleting tests that some other phase relies on | Phase 32 32-DEFERRED.md Category A explicitly names these 5 cases as obsolete; no other phase references them. Grep confirms. |
| CLAUDE.md Hard Rule 1 (no production edits for test reasons) | Schema flip + migration 0020 + route hooks are CORRECT production behavior for Phase 33's end state — not test workarounds. |
| Phase 32 NULLIF pattern accidentally regressed by 0020 | 0020 touches NO RLS policy bodies. Migration test asserts policy bodies on the 4 affected tables remain NULLIF form. |

## Out of scope (this plan)

- KEK rotation re-wrap migrator (future v2 phase per RESEARCH).
- Encryption of non-credential columns (notes content, transcriptions) — future phase.
- HSM bundled support — documented in §12 as KMS-derivable but not shipped.
- Phase 34 (tenantPlugin retirement).
- Any further Phase-32-deferred test cleanup beyond the 5 cases in Category A — Categories B/C remain Phase 41 work per `32-DEFERRED.md`.
