---
phase: 69-sso-jit-live-keycloak
plan: 06
type: execute
wave: 4
depends_on: [69-03, 69-04, 69-05]
files_modified:
  - tests/e2e-cjm/steps/sso.steps.ts
  - tests/e2e-cjm/steps/__tests__/sso.steps.test.ts
  - tests/e2e-cjm/features/sso/keycloak-oidc.feature
  - tests/self-tests/sso-step-drift.test.ts
autonomous: true
requirements: [SSO-IMPL-05]
must_haves:
  truths:
    - "All 7 @cjm-sso-1.* scenarios pass against the live Keycloak container (the @expected-red @after-phase-19 @after-keycloak-up tags are removed)"
    - "sso.steps.ts drives real OIDC flows: JIT create, name/role re-sync, role downgrade, email-domain tenant, sign-in 403 tenant-mismatch (1.5a), read 404 cross-tenant (1.5b), empty-realm loud-fail (1.6)"
    - "The full OIDC login e2e completes: authorize redirect to live Keycloak, browser login of the seeded user, callback, opaque bearer mint, desktop channel-scheme deep-link echo"
    - "The @cjm-sso-1.6 empty-realm step asserts a NON-ZERO api boot exit code (via bootStack expectExit) plus the structured sso.jit.rejected boot log — not a log-only check"
    - "tests/self-tests/sso-step-drift.test.ts no longer asserts placeholder-only; it asserts real Given/When/Then-vs-feature text equivalence"
    - "Each @cjm-sso step binding has sibling vitest unit coverage with the HTTP/DOM boundary mocked"
  artifacts:
    - path: "tests/e2e-cjm/steps/sso.steps.ts"
      provides: "real wire + DOM step implementations for the 7 @cjm-sso scenarios"
      contains: "undici"
    - path: "tests/e2e-cjm/steps/__tests__/sso.steps.test.ts"
      provides: "sibling unit tests for the step bindings (HTTP/DOM boundary mocked)"
      contains: "sso"
  key_links:
    - from: "tests/e2e-cjm/steps/sso.steps.ts"
      to: "live Keycloak (realm acme)"
      via: "Playwright page.goto authorize redirect + login-form fill; undici wire assertions for JIT row + audit + bearer"
      pattern: "page.goto|undici"
    - from: "tests/e2e-cjm/steps/sso.steps.ts"
      to: "compose-harness bootStack expectExit"
      via: "the @cjm-sso-1.6 empty-realm step boots the api expecting a non-zero exit and reads stderr (byok misconfig-boot precedent)"
      pattern: "expectExit|bootStack"
    - from: "tests/self-tests/sso-step-drift.test.ts"
      to: "tests/e2e-cjm/steps/sso.steps.ts"
      via: "drift sentinel now asserts step-text vs feature-text equivalence (placeholder-only checks removed)"
      pattern: "extractStepBindings"
---
<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->

<objective>
Flip the SSO scenarios GREEN against the live Keycloak: replace the all-stub `sso.steps.ts` with real wire+DOM implementations for the 7 `@cjm-sso-1.*` scenarios, remove the `@expected-red @after-phase-19 @after-keycloak-up` tags, update the drift sentinel `sso-step-drift.test.ts` (which currently asserts placeholder-only and WILL trip), and add sibling unit tests for the step bindings (per cjm-steps-need-unit-tests). This is the phase gate: `make e2e-cjm SCENARIO="@sso"` => 7/7 GREEN proving a freshly-ordered Keycloak works tomorrow for BOTH web and desktop SSO.

Purpose: Req-5b + Req-7 — the live-IdP proof that closes the phase. Everything before this plan is unit/integration; this is the end-to-end truth against a real Keycloak 26 container with JIT tenant/role provisioning + the desktop bearer-mint deep-link.
Output: real sso.steps.ts + sibling unit tests + the un-redded feature + the updated drift sentinel; the @sso e2e run is GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-sso-jit-live-keycloak/69-DECISIONS.md
@.planning/phases/69-sso-jit-live-keycloak/69-RESEARCH.md
@.planning/phases/69-sso-jit-live-keycloak/69-PATTERNS.md

<interfaces>
From Plan 03/04: web genericOAuth mapProfileToUser + 4 databaseHooks; desktop mint-bearer JIT projection
(both new-user createOAuthUser and returning-user re-sync/reject branches);
5 rejection codes mapped to 403/400; 3 sso.jit.* audit events. From Plan 05: realm acme seeded
(client openwhispr-backend, user alice@acme.example/openwhispr-engineering => member); keycloak.yml
wired into make e2e-cjm under @sso; @cjm-sso-1.5 split into 1.5a (sign-in 403) + 1.5b (read 404).

Step analogs (VERIFIED):
- DOM: tests/e2e-cjm/steps/oidc.steps.ts (page.goto api.localhost/sign-in + waitForLoadState networkidle + locator counts) — use page.goto authorize + fill the Keycloak login form + assert callback.
- Wire: tests/e2e-cjm/steps/signin.steps.ts (undici Agent rejectUnauthorized:false for *.localhost; postJsonRaw; getSetCookie()).
- Cross-tenant 404 clone: tests/e2e-cjm/steps/rls-cross-tenant.steps.ts:168-230 (readTranscribeJob cross-tenant by id => 404 not_found; "the body MUST NOT leak the resource's existence" asserts code === "not_found").
- Boot-exit (misconfig) precedent: tests/e2e-cjm/steps/byok.steps.ts:159-167 ("the api container boots expecting exit code {int}" => bootStack({ expectExit: code })). This is the EXACT mechanism @cjm-sso-1.6 reuses.
- Harness: tests/e2e-cjm/support/compose-harness.ts bootStack({ expectExit }) returns BootStackResult { exitCode: number|null, stderr?: string } — exitCode is the observed api exit, stderr is captured via `compose logs api` (only populated when expectExit is set). The doc-comment region at lines 8-29 + the expectExit field at line 187 document the short-budget fast-fail boot.
- Helpers: support/fixtures.ts signedInAs(apiBaseURL,email,password) => {cookieHeader,status}; fetchWithCookie(url,{cookie,...}); postJsonRaw; freshTenant.

sso-step-drift.test.ts (VERIFIED): tests/self-tests/sso-step-drift.test.ts:95-106 asserts the file is
placeholder-only via `expect(src).not.toMatch(/\bundici\b/)` + `not.toMatch(/\bfetch\b\s*\(/)` — these
INVERT when real steps land. Keep extractStepBindings/extractFeatureSteps (lines 39-68) + the
text-equivalence it (line 79); raise the coverage floor (currently 0.3, line 92) toward strict equality;
drop the placeholder-only it (95-106); update the header comment (currently "ships in Phase 19").

@cjm-sso scenario behaviors (feature, post-split): 1.1 first-time JIT (tenant acme/role member + audit
sso.jit.user.created); 1.2 returning user name re-sync; 1.3 admin group revoked => role downgrade +
sso.jit.role.updated; 1.4 email-domain tenant derivation; 1.5a returning user CHANGED tenant claim =>
403 forbidden_tenant_mismatch; 1.5b tenant-A user reads tenant-B fail-closed row => 404 not_found;
1.6 empty-realm boot loud-fail => sso.jit.rejected + non-zero exit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real sso.steps.ts (7 scenarios, live Keycloak) + sibling unit tests + un-red feature</name>
  <read_first>
    - tests/e2e-cjm/steps/sso.steps.ts (FULL — the current 171-line all-stub file to replace; the Given/When/Then text strings to KEEP byte-identical to the feature)
    - tests/e2e-cjm/steps/oidc.steps.ts:13-55 (DOM page.goto + waitForLoadState + locator pattern for the authorize/login-form drive)
    - tests/e2e-cjm/steps/signin.steps.ts:1-60 (undici Agent rejectUnauthorized for *.localhost; postJsonRaw; getSetCookie idiom)
    - tests/e2e-cjm/steps/rls-cross-tenant.steps.ts:168-230 (the cross-tenant 404 clone for @cjm-sso-1.5b; "not_found" code assertion)
    - tests/e2e-cjm/steps/byok.steps.ts:155-185 (the misconfig-boot precedent: bootStack({ expectExit: code }), reading bootResult.exitCode + bootResult.stderr — the EXACT pattern for @cjm-sso-1.6)
    - tests/e2e-cjm/support/compose-harness.ts:8-29,180-211 (bootStack expectExit doc + BootStackResult { exitCode, stderr } shape)
    - tests/e2e-cjm/support/fixtures.ts:73-130 (signedInAs, fetchWithCookie, postJsonRaw signatures)
    - tests/e2e-cjm/features/sso/keycloak-oidc.feature (the 7 scenarios + their exact step text after the Plan 05 1.5a/1.5b split)
    - 69-RESEARCH.md "Code Examples" (cross-tenant 404 clone note) + Validation Architecture (SCENARIO grep command) + feedback cjm-steps-need-unit-tests
  </read_first>
  <action>
    Replace the stub bodies in tests/e2e-cjm/steps/sso.steps.ts with real implementations, keeping every Given/When/Then text string byte-identical to the feature (drift sentinel pins them). Implement against the live Keycloak realm acme seeded by Plan 05:
    - 1.1: drive the OIDC authorize redirect (page.goto the api sign-in / authorize URL), fill the Keycloak login form for alice@acme.example, land the callback; assert via undici a User row with tenant acme + role member exists and a sso.jit.user.created audit_log row was written (query through an authenticated admin/test read or a dedicated assertion endpoint already used by the rls-cross-tenant steps).
    - 1.2: returning user signs in with an updated name claim; assert the User row name re-synced (and the sso.jit.role.updated audit on re-sync as the feature states).
    - 1.3: admin group removed from claims => role downgraded to default + sso.jit.role.updated audit.
    - 1.4: email-domain tenant derivation => User row tenant acme.
    - 1.5a: returning user presents a CHANGED tenant claim => the sign-in is rejected with HTTP 403 and error code forbidden_tenant_mismatch (assert the status + envelope code).
    - 1.5b: clone rls-cross-tenant.steps.ts — a tenant-A JIT user issues an authenticated read scoped to tenant B's row in a fail-closed app table => 404 not_found, asserting code === "not_found" (existence not leaked).
    - 1.6: empty-realm loud-fail — boot the api with OIDC_ISSUER_URL pointed at a non-existent realm (the empty mounted-dir path; this scenario does NOT seed the realm) using the compose-harness bootStack `expectExit` support (compose-harness.ts:187) following the byok.steps misconfig-boot precedent (byok.steps.ts:159-167 — `bootStack({ ..., expectExit: <nonzero> })`). Assert TWO things on the returned BootStackResult: (a) `bootResult.exitCode` is NON-ZERO (the api crashed loud, did not silently disable JIT) — assert exitCode !== 0 (and === the expected non-zero code), NOT a weaker log-only check; (b) the captured `bootResult.stderr` contains the structured `sso.jit.rejected`-equivalent boot-validator FATAL log (the validateJitBoot loud-fail line naming the offending OIDC_* var / unreachable realm). The exit-code assertion is the primary gate; the structured-log assertion is corroborating — both must hold.
    Use the undici localhost dispatcher (rejectUnauthorized:false for *.localhost — LOCKER-03 allows localhost literals in tests/). For the desktop bearer leg (Req-7), drive authorize => callback => opaque bearer mint => assert the channel-scheme deep-link echo (reuse the desktop deep-link contract). Remove `@expected-red @after-phase-19 @after-keycloak-up` from all @sso scenarios in the feature file; update the feature header comment.
    Create tests/e2e-cjm/steps/__tests__/sso.steps.test.ts: sibling vitest unit tests for the step bindings with the HTTP/DOM boundary mocked (assert each step issues the correct URL/payload; no live stack needed) — mandatory per cjm-steps-need-unit-tests.
    English-only. LOCKER-02 no as-any. The PENDING throw and the "ships in Phase 19" comment are removed.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm test sso.steps 2>&1 | tail -15 && grep -c "expected-red" tests/e2e-cjm/features/sso/keycloak-oidc.feature</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test sso.steps exits 0 (sibling unit tests GREEN with mocked boundary)
    - grep -c "expected-red" tests/e2e-cjm/features/sso/keycloak-oidc.feature == 0 (all @sso scenarios un-redded)
    - grep -c "ships in Phase 19" tests/e2e-cjm/steps/sso.steps.ts == 0 (PENDING removed)
    - grep -c "undici" tests/e2e-cjm/steps/sso.steps.ts is >= 1 (real wire impl)
    - The @cjm-sso-1.6 binding calls bootStack with expectExit and asserts bootResult.exitCode !== 0 (non-zero boot exit) — `grep -c "expectExit" tests/e2e-cjm/steps/sso.steps.ts` >= 1 AND the step asserts the exit code, not only a log substring
    - The @cjm-sso-1.6 binding ALSO asserts the structured sso.jit.rejected-equivalent FATAL boot log in bootResult.stderr (corroborating the non-zero exit)
    - tests/e2e-cjm/steps/__tests__/sso.steps.test.ts exists and covers every Given/When/Then binding (incl. the 1.6 expectExit branch with bootStack mocked)
    - 1.5b uses the not_found (404) code assertion (clone of rls-cross-tenant); 1.5a asserts 403 forbidden_tenant_mismatch
    - pnpm lint:lockers exits 0; pnpm tsx tools/lint-steps-have-unit-tests.ts shows no violation for sso.steps
  </acceptance_criteria>
  <done>sso.steps.ts drives the 7 real scenarios; @cjm-sso-1.6 asserts a non-zero boot exit (bootStack expectExit) + the structured sso.jit.rejected FATAL log; sibling unit tests GREEN; feature un-redded; PENDING throw removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update sso-step-drift.test.ts + live @sso e2e GREEN (phase gate)</name>
  <read_first>
    - tests/self-tests/sso-step-drift.test.ts (FULL — the 3 it() blocks; keep extractStepBindings/extractFeatureSteps + the text-equivalence it; raise the 0.3 floor; DROP the placeholder-only it at 95-106; rewrite the header comment)
    - tests/e2e-cjm/steps/sso.steps.ts (Task 1 output — the real bindings the drift test now validates)
    - Makefile e2e-cjm target + 69-RESEARCH.md Validation Architecture (the SCENARIO="@sso" grep command; the readiness + seed wiring from Plan 05)
    - 69-RESEARCH.md Pitfall 4 (the drift sentinel is a tripwire that MUST be updated in the same change as the real steps)
  </read_first>
  <action>
    Update tests/self-tests/sso-step-drift.test.ts: remove the "step file is still placeholder-only" it() (lines 95-106 — the not.toMatch(/undici/) + not.toMatch(/fetch/) checks that now invert). Keep extractStepBindings/extractFeatureSteps helpers and the binding-count it; raise the text-equivalence coverage floor from 0.3 toward strict equality now that the steps are real and normalized (assert every feature step has a matching binding, modulo the And-keyword normalization). Rewrite the header comment to describe the post-implementation drift sentinel (drop the "ships in Phase 19" rationale).
    Then run the phase gate: boot the live Keycloak stack and run the @sso scenarios. This is a real docker compose + Playwright run (live Keycloak 26 container, no IdP mock — constitutional). Read the exit code + the playwright summary line with your own eyes; 7/7 must be GREEN. After the @sso run is green, run the full grep-invert sweep to confirm no @expected-red regression elsewhere.
    English-only. Do not weaken any other scenario's gating.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm test sso-step-drift 2>&1 | tail -10 && E2E_CJM=1 SCENARIO="@sso" make e2e-cjm 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test sso-step-drift exits 0 with the placeholder-only it() removed and the text-equivalence floor raised
    - grep -c "placeholder-only" tests/self-tests/sso-step-drift.test.ts == 0
    - grep -c "ships in Phase 19" tests/self-tests/sso-step-drift.test.ts == 0
    - E2E_CJM=1 SCENARIO="@sso" make e2e-cjm => 7/7 scenarios PASS against the live Keycloak (exit 0; the playwright summary line shows 7 passed, 0 failed — read it directly)
    - The full e2e-cjm grep-invert run shows no new @expected-red regression
    - The Req-7 desktop bearer leg (authorize => live KC login => callback => bearer => channel-scheme deep-link) is covered by a passing scenario
    - pnpm lint:lockers exits 0
  </acceptance_criteria>
  <done>Drift sentinel updated to real-step mode; make e2e-cjm SCENARIO="@sso" is 7/7 GREEN against live Keycloak incl. the desktop bearer deep-link; no @expected-red regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> live Keycloak -> callback | the e2e drives a real OIDC authorize/login across the IdP trust boundary |
| desktop callback -> channel-scheme deep-link | the bearer is echoed to a custom-protocol URL (scheme-allowlist validated) |
| operator misconfig -> api boot | an unreachable/empty realm must crash the api loud (non-zero exit), never silently disable JIT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-17 | Spoofing | mocked IdP masking a real failure | mitigate | the e2e uses a REAL Keycloak 26 container (constitutional no-mock-of-IdP); 7/7 must pass against it |
| T-69-18 | Tampering | open redirect on desktop callback | mitigate | scheme-allowlist validation on the deep-link; the e2e asserts the exact channel-scheme echo |
| T-69-19 | Information disclosure | cross-tenant existence leak | mitigate | @cjm-sso-1.5b asserts code === not_found (404), never a forbidden_* code revealing existence |
| T-69-20 | DoS | silent JIT disable on misconfig (fail-open) | mitigate | @cjm-sso-1.6 asserts a NON-ZERO boot exit (bootStack expectExit) + the FATAL sso.jit.rejected log — a misconfigured realm fails loud, never serves with JIT silently off |
</threat_model>

<verification>
- pnpm test sso.steps sso-step-drift GREEN
- E2E_CJM=1 SCENARIO="@sso" make e2e-cjm => 7/7 GREEN against live Keycloak (exit 0)
- @cjm-sso-1.6 asserts a non-zero api boot exit code (bootStack expectExit) + the structured FATAL boot log
- Full grep-invert e2e-cjm sweep: no new @expected-red regression
- pnpm lint:lockers exits 0; lint-steps-have-unit-tests clean for sso.steps
</verification>

<success_criteria>
- 7 @cjm-sso scenarios GREEN against the live Keycloak container; @expected-red tags removed
- Full OIDC login e2e (web + desktop bearer deep-link) proven against the live IdP
- @cjm-sso-1.6 proves the empty-realm misconfig fails loud (non-zero exit + structured log), not log-only
- sso-step-drift.test.ts updated to real-step drift mode
</success_criteria>

<output>
After completion, create `.planning/phases/69-sso-jit-live-keycloak/69-06-SUMMARY.md`
</output>
</output>
