---
phase: 69-sso-jit-live-keycloak
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - compose/test/keycloak-realms/realm-openwhispr-test.json
  - scripts/seed-keycloak-realm.sh
  - tests/e2e-cjm/features/sso/keycloak-oidc.feature
  - Makefile
  - .env.slim.example
  - scripts/__tests__/seed-keycloak-realm.test.ts
autonomous: true
requirements: [SSO-IMPL-05]
must_haves:
  truths:
    - "The realm JSON lives OUTSIDE the mounted ./compose/test/keycloak/ dir; that dir stays empty (only .gitkeep) so @cjm-sso-1.6 still observes an empty import dir"
    - "scripts/seed-keycloak-realm.sh imports realm 'acme' via the Keycloak Admin REST API after the container is healthy"
    - "The realm 'acme' client 'openwhispr-backend' emits groups in BOTH id_token AND userinfo (A1) so the desktop path sees groups"
    - "compose/test/keycloak.yml (+ --profile sso) is wired into the make e2e-cjm compose stack with a Keycloak readiness wait + seed step for the @sso run"
    - "The @cjm-sso-1.5 scenario is split into 1.5a (sign-in 403 forbidden_tenant_mismatch) + 1.5b (read-time 404 on a fail-closed table) per D-69-3"
    - "The seed script uses no credential interpolation (LOCKER-06)"
  artifacts:
    - path: "compose/test/keycloak-realms/realm-openwhispr-test.json"
      provides: "realm acme + client openwhispr-backend + seeded user with groups+email claims + group-membership protocol mapper targeting userinfo"
      contains: "openwhispr-backend"
    - path: "scripts/seed-keycloak-realm.sh"
      provides: "Admin REST API realm import (token acquire + POST /admin/realms)"
      contains: "admin/realms"
    - path: "tests/e2e-cjm/features/sso/keycloak-oidc.feature"
      provides: "7 scenarios (1.5 split into 1.5a + 1.5b)"
      contains: "cjm-sso-1.5a"
  key_links:
    - from: "Makefile"
      to: "compose/test/keycloak.yml"
      via: "e2e-cjm compose stack adds the keycloak override + readiness wait + seed step when the run targets the SSO scenarios"
      pattern: "keycloak.yml"
    - from: "scripts/seed-keycloak-realm.sh"
      to: "compose/test/keycloak-realms/realm-openwhispr-test.json"
      via: "POST the realm json to admin/realms from a separate path; mounted dir stays empty"
      pattern: "keycloak-realms/realm-openwhispr-test.json"
---
<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->

<objective>
Author the live-Keycloak fixtures with the load-bearing realm-path separation (D-69-3 / SPEC Req-5): ship realm-openwhispr-test.json in a SEPARATE dir (compose/test/keycloak-realms/), import it at runtime via the Admin REST API in scripts/seed-keycloak-realm.sh so the mounted compose/test/keycloak/ dir stays empty (keeping @cjm-sso-1.6 loud-fail-on-empty honest). Wire compose/test/keycloak.yml (profile sso) into make e2e-cjm with a Keycloak readiness wait, and split @cjm-sso-1.5 into 1.5a (sign-in 403) + 1.5b (read 404) per D-69-3.

Purpose: this is the infrastructure the e2e plan (06) runs against. The realm-path trick + the empty mounted dir are explicitly what a verifier would reject if done wrong (SPEC Failure Analyst round). The realm client MUST emit groups in userinfo (A1) so the desktop path (Plan 04) sees them.
Output: realm JSON + seed script (LOCKER-06 safe) + Makefile e2e-cjm wiring + the 1.5a/1.5b feature split + a seed-script unit test (HTTP boundary mocked, per cjm-steps-need-unit-tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-sso-jit-live-keycloak/69-DECISIONS.md
@.planning/phases/69-sso-jit-live-keycloak/69-RESEARCH.md
@.planning/phases/69-sso-jit-live-keycloak/69-PATTERNS.md
@.planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md

<interfaces>
keycloak.yml fixture (VERIFIED): image quay.io/keycloak/keycloak:26.0, profiles sso, start-dev import-realm,
ports 8089:8080 + 9000:9000, mount compose/test/keycloak/ to /opt/keycloak/data/import:ro,
container_name e2e-cjm-keycloak, network openwhispr_internal (external), admin user/password admin/admin.
compose/test/keycloak/ holds ONLY .gitkeep — its emptiness is @cjm-sso-1.6's assertion. KEEP EMPTY.

Makefile e2e-cjm target (VERIFIED, around lines 520-561): gated on E2E_CJM=1; runs
tools/lint-cjm-doc.ts --check-expected-red; docker compose -p e2e-cjm with
docker-compose.yml + compose/docker-compose.embedded-litellm.yml + compose/docker-compose.storage.yml
+ compose/docker-compose.ingress.yml + tests/e2e-cjm/compose-overrides.yml --profile default up -d --build --wait;
then wait-for-readiness.ts; bddgen; playwright (SCENARIO grep OR grep-invert @expected-red).

Worked acme realm (SPEC-ldap-keycloak.md:78-104): realm acme, client openwhispr-backend,
user alice@acme.example with groups openwhispr-engineering + okta-everyone,
OIDC_ROLE_MAPPING openwhispr-engineering to member => role member.

Seed-script analog: scripts/verify-images.sh (set -uo pipefail NOT set -e; bash-3.2;
command -v guard exit 127; input-safety regex before invoking external tools).
LOCKER-06: NO credential interpolation into a bash -c eval string; pass admin password via env,
use curl --data fields. Admin-REST import: POST /realms/master/protocol/openid-connect/token
(client_id=admin-cli, grant_type=password) to get a token; POST /admin/realms with
Authorization Bearer + --data @realm.json.

D-69-3 feature split: @cjm-sso-1.5 to 1.5a (returning user CHANGED tenant claim => sign-in 403
forbidden_tenant_mismatch, resolver mode 6) + 1.5b (tenant-A JIT user reads tenant-B's row in a
FAIL-CLOSED app table => 404 not_found, clone of @cjm-15.* / rls-cross-tenant.steps.ts:206-216).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author realm JSON (separate path) + seed-keycloak-realm.sh (Admin REST, LOCKER-06) + unit test</name>
  <read_first>
    - compose/test/keycloak.yml (FULL — image pin, ports, mount, admin env, network; the fixture the realm imports against)
    - scripts/verify-images.sh:1-40 (bash-3.2 header, set -uo pipefail, command -v guard exit 127, input-safety regex)
    - 69-RESEARCH.md Pitfall 1 (Admin-REST import recipe; mounted dir stays empty) + Assumption A1 (groups in userinfo) + Open Question 3 (Group Membership protocol mapper)
    - 69-PATTERNS.md seed-keycloak-realm.sh + realm JSON sections (LOCKER-06 argv-array; SEPARATE path)
    - SPEC-ldap-keycloak.md:78-104 (the worked acme realm contents)
  </read_first>
  <action>
    Create compose/test/keycloak-realms/realm-openwhispr-test.json (NOT in compose/test/keycloak/): realm acme, enabled; a confidential client openwhispr-backend with the e2e redirect URIs (api.localhost callback + the desktop-callback path) and a client scope carrying a Group Membership protocol mapper (groups claim) configured to add to BOTH id.token.claim=true AND userinfo.token.claim=true (A1 — desktop path reads userinfo); a seeded user alice@acme.example (password set) with group membership openwhispr-engineering so the resolver yields tenant acme / role member. Seed the additional fixtures 1.5a/1.5b need (a user whose tenant claim can change for the sign-in-403 path, and a fail-closed-table row owned by a second tenant for the read-404 path). Keycloak image stays pinned 26.0.
    Create scripts/seed-keycloak-realm.sh: bash-3.2, set -uo pipefail, command -v curl/docker guards (exit 127 if missing). Acquire an admin token from KC_URL/realms/master/protocol/openid-connect/token (client_id=admin-cli, grant_type=password, username/password read from env — NEVER interpolated into a bash -c eval string; pass as curl --data fields). POST the realm JSON to KC_URL/admin/realms with Authorization Bearer token and --data @compose/test/keycloak-realms/realm-openwhispr-test.json. Idempotent: a 409 (realm exists) is treated as success. Input-safety regex on any KC_URL/realm arg before use. LOCKER-06: no template-literal credential interpolation; if ever invoked from TS use argv-array spawn shell:false.
    Create scripts/__tests__/seed-keycloak-realm.test.ts (per cjm-steps-need-unit-tests): mock ONLY the HTTP boundary (token endpoint + /admin/realms), assert the script posts the realm to the correct URL, handles 409 idempotently, and never embeds the password in a shell command string.
    LOCKER-03: localhost/admin literals are allowed in compose/ + scripts/ + tests/. English-only.
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm test seed-keycloak-realm 2>&1 | tail -12 && bash -n scripts/seed-keycloak-realm.sh && test -f compose/test/keycloak-realms/realm-openwhispr-test.json && echo REALM_JSON_OK && (ls compose/test/keycloak/ | grep -qv gitkeep && echo MOUNTED_DIR_NOT_EMPTY || echo MOUNTED_DIR_STILL_EMPTY)</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test seed-keycloak-realm exits 0
    - bash -n scripts/seed-keycloak-realm.sh exits 0 (syntactically valid)
    - compose/test/keycloak-realms/realm-openwhispr-test.json exists; the verify command prints MOUNTED_DIR_STILL_EMPTY
    - grep -c "userinfo.token.claim" compose/test/keycloak-realms/realm-openwhispr-test.json is >= 1 (groups mapper targets userinfo — A1)
    - grep -n "admin/realms" scripts/seed-keycloak-realm.sh matches (Admin REST import)
    - pnpm tsx tools/lint-shell-credential-interpolation.ts shows no new violation for the seed script (LOCKER-06)
    - pnpm lint:lockers exits 0
  </acceptance_criteria>
  <done>Realm JSON in the separate path with userinfo group mapper; seed script imports via Admin REST without credential interpolation; mounted dir still empty; unit test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire keycloak.yml + seed into make e2e-cjm; split @cjm-sso-1.5 into 1.5a + 1.5b; add JIT env to .env.slim.example</name>
  <read_first>
    - Makefile:520-561 (the e2e-cjm target — compose -f list, readiness-wait + bddgen + playwright steps, SCENARIO grep branch)
    - tests/e2e-cjm/support/wait-for-readiness.ts:1-25 (the readiness-wait pattern to extend for Keycloak health on :9000/health/ready)
    - tests/e2e-cjm/features/sso/keycloak-oidc.feature (FULL — the 6 scenarios + tags; @cjm-sso-1.5 is the one to split)
    - 69-DECISIONS.md D-69-3 (the exact 1.5a sign-in-403 + 1.5b read-404 split text)
    - tools/lint-cjm-doc.ts behavior (e2e-cjm runs it with --check-expected-red — keep the @expected-red to @after-phase pairing satisfied while @sso scenarios stay red until Plan 06)
    - .env.slim.example (FORMAT — where to append the 7 commented JIT env vars)
  </read_first>
  <action>
    Edit the Makefile e2e-cjm target so that when the run targets the SSO scenarios (SCENARIO matches @sso), the compose stack adds -f compose/test/keycloak.yml --profile sso, then polls Keycloak health (:9000/health/ready) and runs scripts/seed-keycloak-realm.sh BEFORE bddgen/playwright. Keep the default (non-SSO) path unchanged so Keycloak is NOT booted for the base run — preserving @cjm-sso-1.6's empty-realm observation and leaving the base run unaffected. Add the Keycloak readiness poll (extend wait-for-readiness or a dedicated step) so the seed does not race the container.
    Edit tests/e2e-cjm/features/sso/keycloak-oidc.feature: replace the single @cjm-sso-1.5 scenario with TWO per D-69-3 — @cjm-sso-1.5a (Returning OIDC user presents a CHANGED tenant claim => sign-in rejected with 403 forbidden_tenant_mismatch) and @cjm-sso-1.5b (tenant-A JIT user issues an authenticated read scoped to tenant B's row in a fail-closed app table => 404 not_found; the resource's existence is not leaked). Keep @expected-red @after-phase-19 @after-keycloak-up on all @sso scenarios for now (Plan 06 removes them) so lint-cjm-doc --check-expected-red stays satisfied. Update the feature header comment to describe 7 scenarios.
    Append the 7 JIT env vars (OIDC_TENANT_CLAIM, OIDC_TENANT_MAPPING, OIDC_GROUP_CLAIM, OIDC_ROLE_MAPPING, OIDC_ROLE_PRIORITY, OIDC_DEFAULT_ROLE, OIDC_REVOCATION_MODE) to .env.slim.example, commented, with the SPEC defaults documented.
    English-only. LOCKER-03 compliant (test/compose literals).
  </action>
  <verify>
    <automated>cd /Users/dev/openwhispr-server && pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red 2>&1 | tail -8 && grep -cE "cjm-sso-1.5a|cjm-sso-1.5b" tests/e2e-cjm/features/sso/keycloak-oidc.feature && grep -c "keycloak.yml" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red exits 0
    - grep -cE "cjm-sso-1.5a|cjm-sso-1.5b" tests/e2e-cjm/features/sso/keycloak-oidc.feature == 2 (the scenario was split)
    - grep -c "keycloak.yml" Makefile is >= 1 (the SSO compose override is wired)
    - The feature file has 7 scenarios; 1.5a asserts 403 forbidden_tenant_mismatch and 1.5b asserts 404 not_found
    - grep -c "OIDC_TENANT_CLAIM" .env.slim.example is >= 1 (JIT env documented)
    - The default (non-SSO) e2e-cjm compose list is unchanged (Keycloak only added under the @sso branch)
    - pnpm lint:lockers exits 0
  </acceptance_criteria>
  <done>make e2e-cjm boots Keycloak + seeds the realm for @sso runs (default run unchanged); @cjm-sso-1.5 split into 1.5a/1.5b; JIT env documented; cjm-doc lint passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script to Keycloak Admin API | admin credentials cross into the seed at runtime (test-only; LOCKER-06 forbids interpolation) |
| realm config to IdP | the realm authors the trust contract (client, redirect URIs, group mapper) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-14 | Information disclosure | admin password in shell | mitigate | LOCKER-06: no credential interpolation; password passed via curl --data field, never in a bash -c eval string |
| T-69-15 | Tampering | realm import populating the empty dir | mitigate | realm JSON lives in a SEPARATE path; Admin-REST import keeps the mounted dir empty so @cjm-sso-1.6 stays honest |
| T-69-16 | Spoofing | open redirect via realm client | accept | redirect URIs scoped to api.localhost + the desktop-callback path in the test realm only |
</threat_model>

<verification>
- pnpm test seed-keycloak-realm GREEN; bash -n scripts/seed-keycloak-realm.sh clean
- pnpm tsx tools/lint-cjm-doc.ts --features tests/e2e-cjm/features --check-expected-red exits 0
- compose/test/keycloak/ still holds only .gitkeep; realm JSON in compose/test/keycloak-realms/
- pnpm lint:lockers exits 0 (LOCKER-06 clean for the seed script)
</verification>

<success_criteria>
- Realm fixture + Admin-REST seed (no credential interpolation) with the mounted dir kept empty
- keycloak.yml wired into make e2e-cjm for @sso; @cjm-sso-1.5 split into 1.5a/1.5b
</success_criteria>

<output>
After completion, create `.planning/phases/69-sso-jit-live-keycloak/69-05-SUMMARY.md`
</output>
