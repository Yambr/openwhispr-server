---
phase: 69-sso-jit-live-keycloak
plan: 04
type: tdd
wave: 3
depends_on: [69-01, 69-03]
files_modified:
  - apps/api/src/lib/mint-bearer.ts
  - apps/api/src/routes/desktop-signin.ts
  - apps/api/tests/unit/__tests__/mint-bearer-jit.test.ts
  - apps/api/tests/integration/__tests__/desktop-jit.test.ts
autonomous: true
requirements: [SSO-IMPL-01, SSO-IMPL-03]
must_haves:
  truths:
    - "The desktop authorize request and the account.scope both request the group scope so Keycloak emits groups in userinfo"
    - "The desktop userinfo fetch parses groups + the tenant claim (OidcUserinfo widened)"
    - "mint-bearer calls resolveJitDecision and passes the resolved tenantId+role into createOAuthUser's user arg"
    - "A first-time desktop SSO user lands with the resolved tenant+role (NOT default-tenant + null-role)"
    - "A desktop rejection (e.g. unknown tenant) surfaces the rejection without minting a bearer"
    - "Token exchange, SSRF discovery guards, set-auth-token rotation, and channel-scheme echo are UNCHANGED"
  artifacts:
    - path: "apps/api/src/lib/mint-bearer.ts"
      provides: "widened OidcUserinfo (groups + tenant claim); resolveJitDecision call; tenantId/role into createOAuthUser"
      contains: "resolveJitDecision"
    - path: "apps/api/src/routes/desktop-signin.ts"
      provides: "group scope added to the authorize redirect scope param"
      contains: "scope"
  key_links:
    - from: "apps/api/src/lib/mint-bearer.ts"
      to: "apps/api/src/lib/oidc-jit-resolver.ts"
      via: "pre-createOAuthUser projection (D-69-1 desktop call-site); shares the SAME resolver as the web path"
      pattern: "resolveJitDecision"
    - from: "apps/api/src/routes/desktop-signin.ts"
      to: "Keycloak authorize endpoint"
      via: "scope param widened so userinfo carries groups (A1 residual risk)"
      pattern: "scope"
---
<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->

<objective>
Close the desktop bearer-mint seam (D-69-1 Option C, second call-site): the desktop path bypasses `genericOAuth` so `mapProfileToUser` never fires there. Add the three narrow ADDITIVE edits — (1) request the group scope in `desktop-signin.ts` + the `account.scope` in `mint-bearer.ts`; (2) widen `OidcUserinfo` to carry `groups` + the tenant claim; (3) call the SAME `resolveJitDecision` and pass `tenantId`/`role` into `createOAuthUser` — so desktop SSO users JIT-provision identically to the web path. The shared databaseHooks (Plan 03) fire because `createOAuthUser` routes through `createWithHooks`.

Purpose: Without this, Req-7's desktop login lands a default-tenant + null-role user (69-RESEARCH Pitfall 2). This is the central seam called out in D-69-1. Token exchange, discovery SSRF guards (`assertEndpointAffiliated`), `set-auth-token` rotation, and channel-scheme deep-link echo MUST stay byte-for-byte unchanged.
Output: edited mint-bearer.ts + desktop-signin.ts + a unit test (boundary-mocked userinfo) + an integration test (real PG) asserting a first-time desktop profile lands the resolved tenant+role.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/69-sso-jit-live-keycloak/69-DECISIONS.md
@.planning/phases/69-sso-jit-live-keycloak/69-RESEARCH.md
@.planning/phases/69-sso-jit-live-keycloak/69-PATTERNS.md

<interfaces>
<!-- From Plan 01: resolveJitDecision(claims, cfg, existing?), readJitConfig(env) -->
<!-- From Plan 03: the 4 databaseHooks + tenantId additionalField already wired in auth.ts; -->
<!--   createOAuthUser routes through createWithHooks so the hooks fire on the desktop path too. -->

<!-- mint-bearer.ts current state (VERIFIED): -->
<!--   interface OidcUserinfo { sub; email; name?; picture? } at :96 (NO groups, NO tenant claim — widen) -->
<!--   profile = (await uiRes.json()) as OidcUserinfo at :316 -->
<!--   createOAuthUser({email,name,emailVerified,image}, {providerId, accountId, accessToken, idToken, scope:"openid email profile"}) at :330-348 -->
<!--   account.scope hardcoded "openid email profile" at the createOAuthUser account arg (~:346) -->
<!--   discoverOidc + OidcDiscoveryDocSchema + assertEndpointAffiliated — UNCHANGED (SSRF guard) -->
<!-- desktop-signin.ts current state (VERIFIED): -->
<!--   idpUrl.searchParams.set("scope", "openid email profile") at :192 (widen to include the group scope) -->

<!-- A1 residual risk (D-69-1): the Keycloak client must emit `groups` in USERINFO (not only id_token) -->
<!--   since the desktop path reads claims via the userinfo fetch. The realm JSON (Plan 05) authors the -->
<!--   userinfo group mapper. If groups land only in the id_token, extend minimally to decode the id_token -->
<!--   JWT in mint-bearer — still NOT a fallback to routing desktop through genericOAuth. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN widen scope + OidcUserinfo + call resolveJitDecision in mint-bearer (unit, boundary-mocked)</name>
  <read_first>
    - apps/api/src/lib/mint-bearer.ts:95-110 (OidcUserinfo interface + OidcDiscoveryDocSchema), :185-200 (discovery region), :316-350 (the userinfo fetch + createOAuthUser call — the exact seam)
    - apps/api/src/routes/desktop-signin.ts:185-200 (the authorize redirect scope param)
    - apps/api/src/lib/oidc-jit-resolver.ts + oidc-jit-config.ts (Plan 01 — the resolver + readJitConfig)
    - 69-DECISIONS.md D-69-1 (three additive edits; UNCHANGED list: token exchange / SSRF guards / set-auth-token / channel-scheme)
    - 69-RESEARCH.md Pitfall 2 (desktop silently provisions default-tenant — the failure this closes) + Assumption A1
    - feedback (memory): cjm-steps-need-unit-tests / characterization-test-real-surface — boundary-mock only the HTTP userinfo fetch, never the resolver/db logic
  </read_first>
  <behavior>
    - Test: OidcUserinfo now parses {sub,email,name?,picture?,groups?:string[], <tenantClaim>?} — a userinfo payload with groups is captured
    - Test: when readJitConfig is non-null, mint-bearer calls resolveJitDecision(profile, jitConfig) and on ok passes {tenantId, role} into createOAuthUser's user arg (assert the arg shape via a spy on the internalAdapter boundary)
    - Test: account.scope passed to createOAuthUser includes the group scope (no longer the hardcoded "openid email profile")
    - Test: on a rejection decision, mint-bearer does NOT call createOAuthUser and surfaces the rejection (no bearer minted)
    - Test: when readJitConfig is null (JIT disabled), mint-bearer behaves exactly as today (default createOAuthUser arg, no resolver call) — backward-compat
    - Test: the discovery/token-exchange/assertEndpointAffiliated path is untouched (existing mint-bearer tests still green)
  </behavior>
  <action>
    Edit `apps/api/src/lib/mint-bearer.ts`: widen `interface OidcUserinfo` to add `groups?: string[]` and an index signature / explicit field for the configured tenant claim so the userinfo JSON carries claims (keep sub/email/name/picture). Read `const jitConfig = readJitConfig()` once. After parsing `profile` and before/at the `createOAuthUser` call (~:330-348): when jitConfig non-null, call `resolveJitDecision(profile as Record<string,unknown>, jitConfig, existing?)`; on `{ok:true}` pass `tenantId`+`role` into the createOAuthUser USER arg (alongside email/name/emailVerified/image); on `{ok:false}` surface the rejection (throw JitRejectionError, do not mint). Change the hardcoded `scope: "openid email profile"` in the account arg to include the group scope (e.g. derive from jitConfig.groupClaim or append "groups"). Leave `discoverOidc`, `OidcDiscoveryDocSchema`, `assertEndpointAffiliated`, token exchange, set-auth-token rotation, and the channel-scheme deep-link echo UNCHANGED.
    Edit `apps/api/src/routes/desktop-signin.ts:192`: append the group scope to the authorize `scope` param so Keycloak is asked to emit groups in userinfo (A1).
    Boundary-mock ONLY the HTTP userinfo/token fetch (network boundary, allowed); do NOT mock the resolver or the adapter logic. Land RED unit tests first, then GREEN. Atomic TDD commits. LOCKER-02 no as-any (cast to Record<string,unknown> via a typed coercion, not `as any`). English-only.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm test mint-bearer-jit mint-bearer desktop-signin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test mint-bearer-jit mint-bearer desktop-signin` exits 0
    - `grep -n "resolveJitDecision" apps/api/src/lib/mint-bearer.ts` matches
    - `grep -n "groups" apps/api/src/lib/mint-bearer.ts` shows OidcUserinfo carries groups
    - `grep -c "openid email profile" apps/api/src/routes/desktop-signin.ts` shows the scope was widened (the bare string is replaced/augmented with the group scope)
    - `grep -n "assertEndpointAffiliated\|set-auth-token\|discoverOidc" apps/api/src/lib/mint-bearer.ts` STILL present (guards/rotation untouched)
    - Pre-existing mint-bearer tests stay GREEN (no regression in token exchange / SSRF path)
    - Coverage on the mint-bearer diff >= 90/90/90/90; `pnpm lint:lockers` exits 0
  </acceptance_criteria>
  <done>Desktop path widens scope+userinfo, calls the shared resolveJitDecision, and passes tenantId/role into createOAuthUser; SSRF/token/rotation/channel-scheme unchanged; backward-compat when JIT disabled.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN desktop JIT integration (real PG — first-time desktop user lands resolved tenant+role)</name>
  <read_first>
    - apps/api/src/lib/mint-bearer.ts (Task 1 output — the projection seam)
    - apps/api/src/lib/oidc-jit-hooks.ts (Plan 03 — the databaseHooks that fire via createWithHooks on createOAuthUser)
    - an existing mint-bearer / desktop integration test (mirror the real-PG createOAuthUser + session-mint assertion shape; testcontainers PG/PgBouncer/Valkey)
    - 69-RESEARCH.md System Data Flow "DESKTOP bearer-mint path" diagram (createOAuthUser -> createWithHooks -> SAME databaseHooks fire)
  </read_first>
  <behavior>
    - Test (real PG/PgBouncer/Valkey): a first-time desktop OIDC profile (acme example, groups carrying member) drives mint-bearer -> createOAuthUser -> hooks; assert the persisted User row has tenantId=<acme> and role="member" (NOT default-tenant/null) via a real SELECT
    - Test: the create.after hook fired (a sso.jit.user.created audit row exists for the new user) — proving the desktop path shares the same hook lifecycle as web
    - Test: a desktop profile resolving an unknown tenant -> no User row created, no bearer minted
    - Test: a valid desktop sign-in still mints an opaque bearer + the channel-scheme deep-link is unchanged (assert the deep-link shape)
  </behavior>
  <action>
    Add an integration test that boots the real adapter (testcontainers PG+PgBouncer+Valkey, no internal mocks) and exercises the desktop mint-bearer projection end-to-end through createOAuthUser into the hooks from Plan 03. Mock only the userinfo/token HTTP boundary. Assert persisted tenantId/role via SELECT, the sso.jit.user.created audit row, the unknown-tenant rejection (no row), and that a valid sign-in still returns a bearer + unchanged channel-scheme deep-link. Land RED first, then GREEN. Atomic TDD commits. LOCKER-02 no as-any. English-only.
  </action>
  <verify>
    <automated>cd /Users/nick/openwhispr-server && pnpm test desktop-jit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test desktop-jit` exits 0 (real testcontainers)
    - Test asserts persisted User.tenantId + User.role via SELECT == resolved values (NOT default/null)
    - A sso.jit.user.created audit row exists for the desktop-provisioned user (shared hook lifecycle proven)
    - Unknown-tenant desktop profile creates NO User row and mints NO bearer
    - Valid sign-in returns an opaque bearer + the unchanged channel-scheme deep-link
    - Coverage >= 90/90/90/90 on the diff; `pnpm lint:lockers` exits 0
  </acceptance_criteria>
  <done>First-time desktop SSO user lands resolved tenant+role on real PG with a matching audit row; unknown-tenant rejected; bearer + deep-link unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keycloak userinfo -> mint-bearer | the desktop path reads claims via a server-side userinfo fetch (SSRF-guarded by assertEndpointAffiliated, UNCHANGED) |
| mint-bearer -> createOAuthUser | JIT tenant/role projection crosses into the persisted row via the shared hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-11 | Elevation | desktop default-tenant footgun | mitigate | resolveJitDecision projects tenant/role before createOAuthUser; no silent default-tenant provisioning (closes Pitfall 2) |
| T-69-12 | Tampering | poisoned discovery -> secret exfil | mitigate | assertEndpointAffiliated + OidcDiscoveryDocSchema UNCHANGED (D-69-1) |
| T-69-13 | Spoofing | forged groups in userinfo | accept | claims are signed by Keycloak; trust boundary is the verified userinfo; mapping only via operator config |
</threat_model>

<verification>
- `pnpm test mint-bearer-jit desktop-jit mint-bearer desktop-signin` GREEN (unit + real-PG integration)
- SSRF suite + token-exchange tests stay GREEN (guards untouched): `pnpm test mint-bearer 2>&1 | tail -5`
- `pnpm lint:lockers` exits 0
</verification>

<success_criteria>
- Desktop SSO user JIT-provisions with the resolved tenant+role via the shared resolver (D-69-1)
- SSRF guards / token exchange / set-auth-token / channel-scheme deep-link unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/69-sso-jit-live-keycloak/69-04-SUMMARY.md`
</output>
