---
phase: 14-slim-core-byok-profiles-v2
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - tests/e2e-cjm/features/byok-storage.feature
  - tests/e2e-cjm/features/byok-observability.feature
  - tests/e2e-cjm/features/loud-fail-misconfig.feature
  - tests/e2e-cjm/support/compose-harness.ts
  - tests/e2e-cjm/support/byok-steps.ts
  - tests/e2e-cjm/support/world.ts
autonomous: true
requirements:
  - SLIM-01
  - SLIM-02
  - SLIM-03
  - SLIM-04
  - BYOK-01
  - BYOK-02
  - BYOK-03
must_haves:
  truths:
    - "`tests/e2e-cjm/features/byok-storage.feature` exists with the 3 scenarios from RESEARCH §E proposal, tagged `@cjm-byok-storage`."
    - "`tests/e2e-cjm/features/byok-observability.feature` exists with 3 scenarios tagged `@cjm-byok-observability` (unset → fatal, =disabled → no-op, BYOK URL → corp export)."
    - "`tests/e2e-cjm/features/loud-fail-misconfig.feature` exists with 2 scenarios tagged `@cjm-loud-fail-misconfig` (fatal precedes SSRF+OTel; credential redaction)."
    - "Step definitions live in `tests/e2e-cjm/support/byok-steps.ts` and reuse the existing `bootStack(opts)` API from compose-harness.ts (which already accepts `composeFiles` and — extended by this plan — `envOverrides`)."
    - "`bootStack()` is extended to accept `envOverrides?: Record<string, string | undefined>` that injects env vars into the `docker compose up` invocation (via a temp `.env.test-<scenarioId>` file or `--env-file` flag, NOT mutation of `process.env`)."
    - "`bootStack()` is extended to capture stderr (via `docker compose logs api --no-color` after a deterministic timeout) so the misconfig scenarios can assert on the Pino fatal record."
    - "All Phase 13 cjm scenarios still pass after these additions — no regression."
    - "Phase 14 success criteria #1 through #5 are ALL covered by at least one scenario across the cjm feature files (after this plan ships) and by the unit/integration tests from plans 14-01 through 14-06."
  artifacts:
    - path: "tests/e2e-cjm/features/byok-storage.feature"
      provides: "BYOK storage overlay refusal scenarios"
      contains: "@cjm-byok-storage"
    - path: "tests/e2e-cjm/features/byok-observability.feature"
      provides: "BYOK observability sentinel + corp endpoint scenarios"
      contains: "@cjm-byok-observability"
    - path: "tests/e2e-cjm/features/loud-fail-misconfig.feature"
      provides: "Boot-order + credential-redaction scenarios"
      contains: "@cjm-loud-fail-misconfig"
    - path: "tests/e2e-cjm/support/byok-steps.ts"
      provides: "Given/When/Then implementations for the 3 features"
      min_lines: 150
    - path: "tests/e2e-cjm/support/compose-harness.ts"
      provides: "Extended bootStack signature with envOverrides + stderr capture"
      contains: "envOverrides"
  key_links:
    - from: "tests/e2e-cjm/features/byok-storage.feature scenario 'api refuses to start'"
      to: "apps/api/src/lib/byok-guard.ts assertBYOKConfig"
      via: "Pino fatal stderr capture + JSON parse"
      pattern: "BYOK_STORAGE_REQUIRED"
---

<objective>
Author the three Gherkin feature files that CONTEXT.md and ROADMAP claimed
Phase 13 shipped but did not (RESEARCH §E correction), wire step
definitions, and extend `bootStack()` to support per-scenario env overrides
+ stderr capture for misconfig assertions. After this plan, Phase 14
success criterion #5 ("Phase 13 Gherkin scenarios `@cjm-byok-storage`,
`@cjm-byok-observability`, `@cjm-loud-fail-misconfig` are GREEN") is finally
satisfiable in reality.

Output: All 7 Phase 14 requirements covered with at least one Gherkin
scenario, end-to-end against a real-overlay-composed stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-CONTEXT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-PATTERNS.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-01-PLAN.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-02-PLAN.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-03-PLAN.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-04-PLAN.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-06-PLAN.md

@tests/e2e-cjm/features/transcribe.feature
@tests/e2e-cjm/features/signin.feature
@tests/e2e-cjm/support/compose-harness.ts
@tests/e2e-cjm/support/world.ts
@apps/api/src/lib/byok-guard.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GREEN — extend bootStack() with envOverrides + stderr capture</name>
  <files>
    - tests/e2e-cjm/support/compose-harness.ts
    - tests/e2e-cjm/support/compose-harness.test.ts (new — unit test for the new harness behavior)
  </files>
  <behavior>
    - `bootStack(opts)` accepts `envOverrides?: Record<string, string | undefined>`. When present, the harness writes a temp env file (`.cjm.env.<scenarioId>`) merging current `.env` + `envOverrides` (undefined value = explicit unset), invokes `docker compose --env-file <temp> -f … up`, and cleans up the temp file on `stopStack()`.
    - `bootStack(opts)` accepts `expectExit?: number` (default `null` = wait for healthy). When `expectExit` is a number, the harness `--wait-timeout` is reduced to a short window (15s) and the harness collects stderr via `docker compose logs api --no-color --tail=200` after either (a) the api container reports `exited <code>` matching `expectExit`, or (b) the timeout fires. Returned `BootStackResult` shape extended with `stderr?: string` and `exitCode?: number`.
    - The harness does NOT mutate `process.env` of the test runner (per CLAUDE.md no-internal-mocks; env injection happens via docker compose's `--env-file`).
  </behavior>
  <action>
    1. Author `tests/e2e-cjm/support/compose-harness.test.ts` (RED): cases for `envOverrides` writing the temp file, `expectExit` returning stderr, cleanup on stop. Mock `execa` at the process boundary only (the harness shells out to `docker compose` — the test asserts the argv passed to `execa`, not the docker behavior).
    2. Extend `bootStack()` to accept the two new opts. Keep the existing `composeFiles` opt intact. Author the temp `.env.<scenarioId>` file at `tests/e2e-cjm/.scratch/` (gitignored — add to `.gitignore` if not already covered). Use `fs.mkdtemp()` for scratch dirs.
    3. Run vitest — harness test GREEN. Commit as `feat(14-07): bootStack envOverrides + stderr capture`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/e2e-cjm/support/compose-harness.test.ts</automated>
  </verify>
  <done>bootStack accepts the two new opts; harness suite green; no `process.env` mutation in the harness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED → GREEN — author byok-storage + byok-observability features</name>
  <files>
    - tests/e2e-cjm/features/byok-storage.feature (new)
    - tests/e2e-cjm/features/byok-observability.feature (new)
    - tests/e2e-cjm/support/byok-steps.ts (new)
    - tests/e2e-cjm/support/world.ts (extend if needed for parsing fatal JSON)
  </files>
  <behavior>
    Use the Gherkin shapes from RESEARCH §E verbatim for the proposed scenarios. Tags `@cjm-byok-storage` and `@cjm-byok-observability` per CONTEXT.md.

    **byok-storage.feature** scenarios:
    1. `api refuses to start when storage overlay is OFF and S3_ENDPOINT is unset`
       - boot without `compose/docker-compose.storage.yml`; envOverrides: `{S3_ENDPOINT: undefined, OTEL_EXPORTER_OTLP_ENDPOINT: "disabled", INGRESS_BASE_URL: "http://api.localhost", DATABASE_URL: "postgresql://...", SMTP_HOST: undefined}` and NODE_ENV != production; expectExit: 1.
       - Assert stderr contains a JSON line matching `{"level":60,"event":"byok.required","code":"BYOK_STORAGE_REQUIRED","overlay":"storage"}`.
    2. `api boots when storage overlay is OFF but S3_ENDPOINT is set to corporate BYOK`
       - envOverrides: `{S3_ENDPOINT: "https://s3.corp.example.com", S3_ACCESS_KEY: "ak", S3_SECRET_KEY: "sk", S3_BUCKET: "ow"}`; expectExit: null (wait for healthy).
    3. `api boots when storage overlay is ON`
       - composeFiles includes `compose/docker-compose.storage.yml`; expectExit: null.

    **byok-observability.feature** scenarios:
    1. `api refuses to start when observability overlay OFF and OTEL_EXPORTER_OTLP_ENDPOINT unset`
       - expectExit: 1; stderr matches `BYOK_OBSERVABILITY_REQUIRED`.
    2. `api boots in no-op telemetry mode when OTEL_EXPORTER_OTLP_ENDPOINT=disabled`
       - envOverrides: `{OTEL_EXPORTER_OTLP_ENDPOINT: "disabled"}`; expectExit: null; assert via `/api/health` 200 AND log line introspection that no `OTLPExporter` initialization log appears.
    3. `api boots and exports to corp OTLP when overlay OFF but BYOK endpoint set`
       - envOverrides: `{OTEL_EXPORTER_OTLP_ENDPOINT: "http://localhost:14317"}` (a fake but syntactically-valid endpoint; api boots but exporter will error in background — that's fine, scenario asserts boot health not exporter delivery).

    Tests RED at first (feature files exist but step defs don't / are stubs). Then write step defs that drive `bootStack({composeFiles, envOverrides, expectExit})` and parse stderr as NDJSON, asserting matching records.
  </behavior>
  <action>
    1. Author the two `.feature` files using `tests/e2e-cjm/features/transcribe.feature` as the structural analog (Feature/Background/Scenario/Given/When/Then). Tag the entire feature with `@cjm-byok-storage` / `@cjm-byok-observability`. RED commit: `test(14-07): RED @cjm-byok-storage + @cjm-byok-observability feature files`.
    2. Author `tests/e2e-cjm/support/byok-steps.ts`:
       - `Given('the slim-core compose stack with(out)? `-f compose\\/docker-compose\\.([\\w-]+)\\.yml`', …)` to control which overlays go in `composeFiles`.
       - `Given('`(\\w+)` is unset', …)` and `Given('`(\\w+)=(.+?)`', …)` to populate `envOverrides`.
       - `When('the api container boots', …)` calls `await world.bootResult = bootStack({composeFiles, envOverrides, expectExit})`.
       - `Then('the api process exits with code (\\d+)', …)` asserts `world.bootResult.exitCode === code`.
       - `Then('stderr contains a Pino fatal record `(.+?)`', …)` parses stderr NDJSON and asserts at least one record matches the JSON-fragment expectation (using a `subset` matcher).
       - `Then('the api becomes healthy within (\\d+)s', …)` waits for `/api/health` 200 via polling.
       - `Then('no `byok.required` fatal record is emitted', …)` asserts no level-60 `event:"byok.required"` line in collected logs after a 10s grace period.
    3. Run `pnpm test:cjm -t "@cjm-byok-storage"` and `…@cjm-byok-observability`. Expect failures (the underlying impl from plans 14-01..06 should make these pass; if it doesn't, file a bug against the responsible plan — DO NOT relax assertions).
    4. Fix any wire-up bugs uncovered (these often surface real misconfigurations — RESEARCH §C.1 documented the `MINIO_ENDPOINT` injection gate as the storage-side hook for BYOK; if the api doesn't loud-fail in the storage-OFF case, it likely means the env wasn't truly unset). GREEN. Commit as `feat(14-07): byok-storage + byok-observability step defs + GREEN`.
  </action>
  <verify>
    <automated>pnpm test:cjm -t "@cjm-byok-storage|@cjm-byok-observability"</automated>
  </verify>
  <done>All 6 scenarios across the two features GREEN. Stderr fatal records match the JSON shape from CONTEXT.md decision 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED → GREEN — author loud-fail-misconfig feature + final sweep</name>
  <files>
    - tests/e2e-cjm/features/loud-fail-misconfig.feature (new)
    - tests/e2e-cjm/support/byok-steps.ts (extend)
  </files>
  <behavior>
    Two scenarios from RESEARCH §E:
    1. `misconfig fatal precedes installGlobalSSRF and otel-bootstrap`
       - Boot with storage misconfigured (S3_ENDPOINT unset); collect stderr; parse first ~5 log lines.
       - Assert the FIRST level-60 record on stderr has `event: "byok.required"`.
       - Assert NO log line with `installGlobalSSRF` or `OTel SDK starting` appears anywhere in the captured stderr.
    2. `credential-bearing strings in the fatal record are redacted`
       - envOverrides: `S3_ENDPOINT=https://access:secret@s3.corp/` (deliberately credential-bearing, no other storage envs to maintain the storage-misconfig state — the `missing: ["S3_ACCESS_KEY", "S3_SECRET_KEY", "S3_BUCKET"]` path).
       - Assert the fatal record's `hint` field matches `https://*****@s3.corp/` (redacted via redactUrl).
       - Assert the raw substring `secret` does NOT appear anywhere in the captured stderr.
  </behavior>
  <action>
    1. Author the feature file. Tag `@cjm-loud-fail-misconfig`. RED commit: `test(14-07): RED @cjm-loud-fail-misconfig feature`.
    2. Extend `byok-steps.ts` with:
       - `Then('the very first log line on stderr is the Pino fatal `event: byok.required` record', …)` — parse stderr NDJSON, find first level≥60 line, assert event field.
       - `Then('no SSRF dispatcher initialization log appears', …)` — assert no line matches `/SSRF|installGlobalSSRF/`.
       - `Then('no OTel SDK initialization log appears', …)` — assert no line matches `/OTel SDK starting|NodeSDK/`.
       - `Then('the `hint` field contains the redacted form `(.+?)`', …)` — find the fatal record, JSON parse, assert `.hint.includes(expected)`.
       - `Then('the raw password substring `(\\w+)` does not appear anywhere on stderr', …)` — assert `stderr.indexOf(secret) === -1`.
    3. Run `pnpm test:cjm -t "@cjm-loud-fail-misconfig"`. GREEN. Commit as `feat(14-07): loud-fail-misconfig step defs + GREEN`.
    4. Final sweep: run the FULL cjm suite (Phase 13 + Phase 14) to confirm no regression. `pnpm test:cjm` from repo root. All GREEN. Commit as `test(14-07): full cjm suite green with Phase 14 additions` (if any non-trivial changes were needed to fix Phase 13 scenarios under the new compose layering).
  </action>
  <verify>
    <automated>pnpm test:cjm</automated>
  </verify>
  <done>Full cjm suite green. The 3 Phase 14 feature files contribute 8 scenarios total. Credential redaction verified end-to-end.</done>
</task>

</tasks>

<verification>
1. `pnpm test:cjm -t "@cjm-byok-storage"` GREEN (3 scenarios).
2. `pnpm test:cjm -t "@cjm-byok-observability"` GREEN (3 scenarios).
3. `pnpm test:cjm -t "@cjm-loud-fail-misconfig"` GREEN (2 scenarios).
4. `pnpm test:cjm` (full suite) GREEN — no Phase 13 regression.
5. `pnpm vitest run tests/e2e-cjm/support/compose-harness.test.ts` GREEN.
6. Coverage on `tests/e2e-cjm/support/byok-steps.ts` + the harness extension ≥ 90/90/90/90 on diff.
</verification>

<success_criteria>
Phase 14 success criterion #5 satisfied (`@cjm-byok-storage`, `@cjm-byok-observability`, `@cjm-loud-fail-misconfig` GREEN). All 7 requirements (SLIM-01..04, BYOK-01..03) have at least one Gherkin scenario verifying them. Phase verifier can mark Phase 14 PASSED.
</success_criteria>

<output>
After completion, create `.planning/phases/14-slim-core-byok-profiles-v2/14-07-SUMMARY.md` — quote the 3 feature files in full, show the bootStack signature diff, list the new step regexes, and confirm the goal-backward audit: each of phase success criteria #1-#5 maps to ≥ 1 plan + ≥ 1 verifying test (link the specific test names).
</output>
