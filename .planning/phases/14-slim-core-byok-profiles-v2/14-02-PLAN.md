---
phase: 14-slim-core-byok-profiles-v2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.slim.example
  - .env.full.example
  - .env.example
  - tools/bootstrap.sh
  - tools/bootstrap.test.sh
  - docs/operations.md
autonomous: true
requirements:
  - SLIM-03
  - SLIM-04
must_haves:
  truths:
    - "`.env.slim.example` exists at repo root and contains exactly the 5 user-visible operator keys from CONTEXT.md decision 4 (POSTGRES_APP_PASSWORD, BETTER_AUTH_SECRET, LITELLM_MASTER_KEY, BETTER_AUTH_URL, OPENROUTER_API_KEY)."
    - "`.env.slim.example` also contains bootstrap-generated invisible keys as `PLACEHOLDER_BOOTSTRAP_WILL_REPLACE` (POSTGRES_OWNER_PASSWORD, VALKEY_PASSWORD, MASTER_KEK, BACKUP_AGE_IDENTITY) so bootstrap.sh can fill them on first run without operator knowledge — RESEARCH §D.2."
    - "`.env.slim.example` contains a commented overlay appendix with one section per overlay (observability/storage/ingress/pgbouncer/dev-tools), each banner-marked `# REQUIRES: docker-compose.<overlay>.yml` per CONTEXT.md decision 4 — RESEARCH §D.2 buckets B."
    - "`.env.example` is renamed to `.env.full.example` (git-rename, content preserved verbatim)."
    - "`tools/bootstrap.sh` line 39 reads the template path from `BOOTSTRAP_ENV_TEMPLATE` env var, defaulting to `${REPO_ROOT}/.env.slim.example` (NOT `.env.example`) — RESEARCH §D.1."
    - "`docs/operations.md` has a new section titled `## BYOK Environment Matrix` documenting which env var each overlay unlocks and which loud-fail code fires when overlay is OFF and the env is unset."
  artifacts:
    - path: ".env.slim.example"
      provides: "Slim quickstart template — 5 visible keys + bootstrap-invisible secrets + commented overlay appendix"
      contains: "POSTGRES_APP_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE"
    - path: ".env.full.example"
      provides: "Full 88-key reference template (rename of .env.example)"
      contains: "POSTGRES_OWNER_PASSWORD="
    - path: "tools/bootstrap.sh"
      provides: "Env-overridable template selection"
      contains: "BOOTSTRAP_ENV_TEMPLATE"
    - path: "docs/operations.md"
      provides: "BYOK matrix section"
      contains: "## BYOK Environment Matrix"
  key_links:
    - from: "tools/bootstrap.sh"
      to: ".env.slim.example"
      via: "BOOTSTRAP_ENV_TEMPLATE default"
      pattern: "BOOTSTRAP_ENV_TEMPLATE.*\\.env\\.slim\\.example"
    - from: "docs/operations.md BYOK Environment Matrix"
      to: "Loud-fail codes from byok-guard"
      via: "matrix row per overlay"
      pattern: "BYOK_STORAGE_REQUIRED|BYOK_OBSERVABILITY_REQUIRED|BYOK_INGRESS_REQUIRED|BYOK_DATABASE_REQUIRED|BYOK_SMTP_REQUIRED"
---

<objective>
Author `.env.slim.example` per CONTEXT.md decision 4 + RESEARCH §D, rename
`.env.example` → `.env.full.example`, make `tools/bootstrap.sh` template
path env-overridable, and document the BYOK env matrix in
`docs/operations.md`. This plan is parallel-safe with 14-01 (different file
sets; no overlap).

Purpose: Closes SLIM-03 (slim env template) and SLIM-04 (BYOK matrix doc).
Pre-requisite for plan 14-07's Gherkin scenarios which assert against the
slim template.

Output: A clean, opinionated 5-input slim template that bootstrap can fill
without operator intervention beyond the OpenRouter key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-CONTEXT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH-env-slim.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-PATTERNS.md

@.env.example
@.env.embedded.example
@tools/bootstrap.sh
@docs/operations.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN — bootstrap.sh env-overridable template path</name>
  <files>
    - tools/bootstrap.sh
    - tools/bootstrap.test.sh (new — bats-style or pure-bash assertions)
  </files>
  <behavior>
    - Test 1: With `BOOTSTRAP_ENV_TEMPLATE` unset, `bootstrap.sh --print-template` (new dry-run flag) prints the absolute path to `${REPO_ROOT}/.env.slim.example`.
    - Test 2: With `BOOTSTRAP_ENV_TEMPLATE=/tmp/custom.env.example`, the same command prints `/tmp/custom.env.example`.
    - Test 3: With `BOOTSTRAP_ENV_TEMPLATE` pointing at a non-existent file, bootstrap exits non-zero with a clear "template not found" error (do not silently fall back).
  </behavior>
  <action>
    1. Author `tools/bootstrap.test.sh` (bash-only, no bats dependency) that exercises the 3 behaviors above. Use `mktemp -d` for scratch dirs. Run the script via `bash tools/bootstrap.test.sh`. Commit RED as `test(14-02): RED bootstrap env-overridable template`.
    2. Edit `tools/bootstrap.sh:39` per RESEARCH §D.1:
       ```
       readonly ENV_EXAMPLE="${BOOTSTRAP_ENV_TEMPLATE:-${REPO_ROOT}/.env.slim.example}"
       ```
       Add a `--print-template` flag handler at the top of `main()` (or wherever args are parsed): if `$1 == "--print-template"` print `"$ENV_EXAMPLE"` and exit 0. Add an existence guard: if `[[ ! -f "$ENV_EXAMPLE" ]]` then `printf 'bootstrap: template not found: %s\n' "$ENV_EXAMPLE" >&2; exit 1`.
    3. Run `bash tools/bootstrap.test.sh` — must GREEN. Commit as `feat(14-02): bootstrap.sh BOOTSTRAP_ENV_TEMPLATE override`.
  </action>
  <verify>
    <automated>bash tools/bootstrap.test.sh</automated>
  </verify>
  <done>3 behaviors GREEN. bootstrap.sh exits 1 with a useful error on missing template.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED + GREEN — author .env.slim.example, rename .env.example → .env.full.example</name>
  <files>
    - .env.slim.example (new)
    - .env.full.example (git-rename from .env.example)
    - tests/integration/env-slim-example.test.ts (new)
  </files>
  <behavior>
    - Test 1: `.env.slim.example` exists at repo root.
    - Test 2: `.env.full.example` exists at repo root; `.env.example` does NOT exist (renamed).
    - Test 3: Parsing `.env.slim.example` as KEY=VALUE lines, the UNCOMMENTED keys equal exactly: `POSTGRES_APP_PASSWORD`, `BETTER_AUTH_SECRET`, `LITELLM_MASTER_KEY`, `BETTER_AUTH_URL`, `OPENROUTER_API_KEY`, `POSTGRES_OWNER_PASSWORD`, `VALKEY_PASSWORD`, `MASTER_KEK`, `BACKUP_AGE_IDENTITY`, `OTEL_EXPORTER_OTLP_ENDPOINT`. (5 user-visible + 4 bootstrap-invisible + OTEL_EXPORTER_OTLP_ENDPOINT=disabled sentinel per CONTEXT decision 5.)
    - Test 4: The 4 bootstrap-invisible keys have value `PLACEHOLDER_BOOTSTRAP_WILL_REPLACE` (so bootstrap.sh `gen_secret` fills them).
    - Test 5: `OPENROUTER_API_KEY` has empty value (not a placeholder — operator-supplied or 503 envelope).
    - Test 6: `BETTER_AUTH_URL` has value `http://localhost:3000`.
    - Test 7: `OTEL_EXPORTER_OTLP_ENDPOINT` has value `disabled` (the sentinel per CONTEXT.md decision 5).
    - Test 8: The file contains commented-out overlay sections marked with header `# REQUIRES: docker-compose.<overlay>.yml` for each of: observability, storage, ingress, pgbouncer, dev-tools (5 banner strings).
    - Test 9: Each overlay section contains the expected env keys per CONTEXT decision 2 and RESEARCH §D.2 bucket B (e.g. storage section commented-lists `S3_ENDPOINT=`, `S3_ACCESS_KEY=`, `S3_SECRET_KEY=`, `S3_BUCKET=`; observability section the corp OTLP override hint; ingress section `INGRESS_BASE_URL=`, `TRAEFIK_ADMIN_PASSWORD=`; pgbouncer section `PGBOUNCER_ADMIN_PASSWORD=`; dev-tools section `SMTP_HOST=`, `SMTP_PORT=`, `SMTP_USER=`, `SMTP_PASSWORD=`, `SMTP_FROM=`).
  </behavior>
  <action>
    1. Author `tests/integration/env-slim-example.test.ts` with the 9 assertions above. Use Node fs + a simple regex parser that ignores `^\s*#` lines. Run; RED. Commit as `test(14-02): RED .env.slim.example conformance`.
    2. `git mv .env.example .env.full.example` (preserves history). No content edit.
    3. Author `.env.slim.example` per RESEARCH §D and CONTEXT.md decision 4 + decision 5:
       - Header comment block explaining: "This is the slim-core quickstart template. 5 user-visible keys; the rest are bootstrap-generated. Uncomment an overlay section below AFTER adding `-f compose/docker-compose.<overlay>.yml` to your `docker compose` invocation."
       - 10 active KEY=VALUE lines per Test 3.
       - 5 commented overlay sections per Test 8/9, each opening with `# REQUIRES: docker-compose.<overlay>.yml`.
    4. Run vitest — GREEN. Commit as `feat(14-02): .env.slim.example + rename .env.example to .env.full.example`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/env-slim-example.test.ts</automated>
  </verify>
  <done>9 assertions GREEN. `.env.full.example` exists with original 88-key content. `.env.slim.example` exists with the 10-key slim contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — docs/operations.md BYOK matrix section</name>
  <files>
    - docs/operations.md
    - tests/integration/docs-operations-byok-matrix.test.ts (new)
  </files>
  <behavior>
    - Test 1: `docs/operations.md` contains the literal H2 heading `## BYOK Environment Matrix`.
    - Test 2: Under that heading there is a markdown table with header columns `Overlay | BYOK env(s) when OFF | Loud-fail code | Compose overlay file | Helm toggle`.
    - Test 3: The table has one row per overlay (5 rows): storage, observability, ingress, pgbouncer, dev-tools. Each row's loud-fail code matches CONTEXT.md decision 2's table EXACTLY: `BYOK_STORAGE_REQUIRED`, `BYOK_OBSERVABILITY_REQUIRED`, `BYOK_INGRESS_REQUIRED`, `BYOK_DATABASE_REQUIRED`, `BYOK_SMTP_REQUIRED`.
    - Test 4: Each row's compose-overlay-file column matches `compose/docker-compose.<overlay>.yml`.
    - Test 5: Each row's Helm toggle matches per CONTEXT.md decision 6: `storage.enabled`, `observability.enabled`, `tls.enabled`, `pooler.enabled`, `mailpit.enabled`.
    - Test 6: There is a paragraph BEFORE the matrix linking the section to `apps/api/src/lib/byok-guard.ts` (the implementing module from plan 14-04).
  </behavior>
  <action>
    1. Author the vitest test file with the 6 assertions. Read `docs/operations.md` as a string; use regex / line scanning. RED. Commit as `test(14-02): RED operations.md BYOK matrix`.
    2. Edit `docs/operations.md` to add the `## BYOK Environment Matrix` section. Place it logically (e.g. after the "Self-hosting quickstart" section if one exists, else at the end before any "Troubleshooting" section). Include the paragraph: "The slim-core base ships without `minio`, `traefik`, `otel-collector`, `pgbouncer`, or `mailpit`. When you do NOT enable an overlay AND the corresponding BYOK env var is unset, the api refuses to start with a Pino fatal record (see `apps/api/src/lib/byok-guard.ts`)." Then the markdown table per behaviors.
    3. GREEN. Commit as `docs(14-02): BYOK environment matrix in operations.md`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/docs-operations-byok-matrix.test.ts</automated>
  </verify>
  <done>6 assertions GREEN. Matrix section is concise, accurate, and points operators at the loud-fail module.</done>
</task>

</tasks>

<verification>
1. `bash tools/bootstrap.test.sh` GREEN (3 behaviors).
2. `pnpm vitest run tests/integration/env-slim-example.test.ts` GREEN (9 assertions).
3. `pnpm vitest run tests/integration/docs-operations-byok-matrix.test.ts` GREEN (6 assertions).
4. `ls .env.full.example` exits 0; `ls .env.example 2>/dev/null` exits non-zero.
5. `BOOTSTRAP_ENV_TEMPLATE=/tmp/nonexistent bash tools/bootstrap.sh --print-template` exits 1 with a clear stderr message.
6. Coverage on the three test files ≥ 90/90/90/90 (trivially — they're file readers).
</verification>

<success_criteria>
SLIM-03 (`.env.slim.example` with 5 keys) and SLIM-04 (BYOK matrix in operations.md) are observable in the filesystem and machine-verifiable. Bootstrap.sh now defaults to the slim template; power users can opt back into `.env.full.example` via `BOOTSTRAP_ENV_TEMPLATE`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-slim-core-byok-profiles-v2/14-02-SUMMARY.md` — list the 10 keys in slim, the 5 overlay banner sections, the bootstrap.sh edit, the BYOK matrix structure, and quote the matrix table.
</output>
