---
phase: 14-slim-core-byok-profiles-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - SLIM-01
must_haves:
  truths:
    - "Bare `docker compose up` (no flags, no `--profile`, no extra `-f`) brings up exactly 6 long-running services (api, web, worker, postgres, valkey, litellm) plus migrate as init-container."
    - "No base service in `docker-compose.yml` declares a `profiles:` key (the TD-14.f / deferred-items #3a inversion is closed)."
    - "13 non-slim services (pgbouncer, minio, traefik, otel-collector, loki, tempo, mimir, grafana, mailpit, fixture-idp, seed, contract-test-runner — and pgbouncer/otel-collector/mailpit `depends_on` edges off api/worker/migrate) are REMOVED from base; they will be re-declared by overlays in Wave 2 plan 14-03."
    - "Slim-core works without Traefik because api exposes `4000:3000` and web exposes `3000:3000` directly to the host."
    - "`OTEL_EXPORTER_OTLP_ENDPOINT` in api/worker env blocks has NO `:-http://otel-collector:4317` fallback (unset propagates → loud-fail in 14-04)."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Slim-core base — 6 long-running services + migrate, no profiles, no overlay-resident services, no overlay-coupled depends_on, host ports on api/web."
      contains: "service: api / web / worker / postgres / valkey / litellm / migrate"
      excludes: "profiles:, minio:, traefik:, otel-collector:, loki:, tempo:, mimir:, grafana:, mailpit:, pgbouncer:, fixture-idp:, seed:, contract-test-runner:"
  key_links:
    - from: "docker-compose.yml api service"
      to: "host port 4000"
      via: "ports: 4000:3000"
      pattern: "ports:.*4000:3000"
    - from: "docker-compose.yml web service"
      to: "host port 3000"
      via: "ports: 3000:3000"
      pattern: "ports:.*3000:3000"
---

<objective>
Convert `docker-compose.yml` from a 19-service profile-gated megafile into a
slim-core base of 7 services (6 long-running + migrate init). Remove every
`profiles:` declaration, remove every reference to services that will live in
Wave-2 overlays, and add host-port publishing to api and web so the slim path
works without Traefik.

Purpose: This is the **pre-requisite for every other Phase 14 plan.** Until
slim-core is the base, overlays cannot be authored and the loud-fail guard
cannot be wired (the guard's behavior depends on knowing which services are
"overlay-OFF" — which is unknowable while base still carries them).

Output: A `docker-compose.yml` that satisfies success-criterion #1 of the
phase verbatim, and an integration test that proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-CONTEXT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-PATTERNS.md

@docker-compose.yml
@compose/e2e/docker-compose.e2e.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — author slim-core conformance test</name>
  <files>
    - tests/integration/slim-core-base.test.ts (new)
  </files>
  <behavior>
    - Test 1: `yaml.parse(readFileSync("docker-compose.yml"))` produces an object whose `services` keys equal exactly `{api, web, worker, postgres, valkey, litellm, migrate}` (set equality — no extras, no omissions).
    - Test 2: No service in `services` has a `profiles` key (assert `service.profiles === undefined` for all 7).
    - Test 3: `services.api.ports` contains `"4000:3000"` (string form) or `{published: 4000, target: 3000}` (long form).
    - Test 4: `services.web.ports` contains `"3000:3000"`.
    - Test 5: `services.api.depends_on` keys are a subset of `{migrate, litellm, valkey}` (no mailpit, no otel-collector, no pgbouncer).
    - Test 6: `services.worker.depends_on` keys are a subset of `{litellm, valkey, migrate}` (no otel-collector).
    - Test 7: `services.migrate.depends_on` keys are a subset of `{postgres}` (no pgbouncer).
    - Test 8: `services.api.environment.OTEL_EXPORTER_OTLP_ENDPOINT` (string) does NOT contain `:-http://otel-collector:4317`. Allowed forms: bare `${OTEL_EXPORTER_OTLP_ENDPOINT}` OR absent.
    - Test 9: Same for `services.worker.environment.OTEL_EXPORTER_OTLP_ENDPOINT`.
    - Test 10: `services.api.depends_on.migrate.condition === "service_completed_successfully"` (migrate gate preserved).
  </behavior>
  <action>
    Create `tests/integration/slim-core-base.test.ts` using Vitest + `yaml` package (`pnpm add -D yaml` if not already a workspace dep; verify via `node_modules/.pnpm`). Import `readFileSync` from `node:fs` and `parse` from `yaml`. Read `docker-compose.yml` at repo root via `resolve(__dirname, "../../docker-compose.yml")`. Write the 10 assertions above. No mocks, no testcontainers (this is a static YAML conformance test). Run the test; it MUST fail at this stage (RED) because base still has 19 services and profiles. Commit as `test(14-01): RED slim-core base conformance`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/slim-core-base.test.ts</automated>
    Expected: test FAILS with assertions about extra services / profiles present.
  </verify>
  <done>Test file committed; running it reports failures aligned to current 19-service state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — slim-core base edit</name>
  <files>
    - docker-compose.yml
  </files>
  <behavior>
    After edit, `pnpm vitest run tests/integration/slim-core-base.test.ts` passes all 10 assertions from Task 1.
  </behavior>
  <action>
    Edit `docker-compose.yml` in-place (per D-01 / D-02):
    1. DELETE these top-level services entirely (they move to overlays in plan 14-03): `pgbouncer`, `minio`, `traefik`, `otel-collector`, `loki`, `tempo`, `mimir`, `grafana`, `mailpit`, `fixture-idp`, `seed`, `contract-test-runner` (12 deletions).
    2. DELETE every `profiles:` line on the 7 surviving services (`postgres`, `valkey`, `litellm`, `migrate`, `api`, `worker`, `web`) per RESEARCH §G.3.
    3. From `api.depends_on` REMOVE keys `pgbouncer`, `otel-collector`, `mailpit`. Keep `migrate` (condition: service_completed_successfully), `litellm` (service_healthy), `valkey` (service_healthy).
    4. From `worker.depends_on` REMOVE key `otel-collector`. Keep `litellm`, `valkey`, `migrate`.
    5. From `migrate.depends_on` REMOVE key `pgbouncer`. Keep `postgres` (service_healthy). (Comment at lines 309-314 already states migrate goes direct to postgres:5432 — see RESEARCH §A.1.)
    6. ADD `ports: ["4000:3000"]` to `api` service. ADD `ports: ["3000:3000"]` to `web` service.
    7. In `api.environment` and `worker.environment` blocks: replace `OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}` with `OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT}` (no fallback).
    8. DELETE the top-level named volumes that backed deleted services: `minio_data`, `loki_data`, `tempo_data`, `mimir_data`, `grafana_data` (5 volume entries). Keep `postgres_data`, `valkey_data` (still in use by surviving postgres/valkey).
    9. Do NOT touch the `networks:` block; both surviving and overlay services share `openwhispr_internal`.
    10. Preserve all healthchecks, env_files, build contexts, image pins on the 7 surviving services exactly as-is.
    Run the conformance test from Task 1 — it MUST now pass (GREEN). Commit as `feat(14-01): slim-core base — 6 services + migrate, no profiles`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/slim-core-base.test.ts</automated>
    Also run: `docker compose config --services | sort` and verify output is exactly `api\nlitellm\nmigrate\npostgres\nvalkey\nweb\nworker`.
    Also run: `docker compose config | grep -c "profiles:" | grep -v '^#' || true` → expect `0` (no `profiles:` lines outside comments).
  </verify>
  <done>All 10 assertions GREEN. `docker compose config --services` lists exactly the 7 slim-core service names. No `profiles:` keys remain.</done>
</task>

<task type="auto">
  <name>Task 3: docker-compose lint + boot smoke (no overlays)</name>
  <files>
    - (no source files — invocation-only)
  </files>
  <action>
    Smoke-test the slim-core base end-to-end without any overlay file:
    1. `docker compose config -q` (lints the merged config; exits non-zero on syntax error).
    2. `docker compose up -d` (bare `up` per success-criterion #1 wording — no `--profile`, no extra `-f`).
    3. Poll `docker compose ps` until 6 long-running services report `running (healthy)` and `migrate` reports `exited (0)`, with a 180s ceiling.
    4. `docker compose down -v` to clean up.
    Do NOT add a real-API e2e test here; full Gherkin-driven boot tests live in plan 14-07. This task is the manual smoke executors run before committing Task 2. Document any failure as a blocker — DO NOT downgrade the test by relaxing assertions. Per CLAUDE.md: no workarounds; if a service fails to come up without an overlay, fix the base, not the test.
  </action>
  <verify>
    <automated>
    bash -lc 'set -euo pipefail; docker compose config -q && docker compose up -d --wait --wait-timeout 180 && docker compose ps --format json | jq -e "[.[] | select(.State == \"running\" or .State == \"exited\")] | length == 7" && docker compose down -v'
    </automated>
  </verify>
  <done>Smoke passes: 6 healthy + 1 exited(0); `docker compose down -v` returns 0.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run tests/integration/slim-core-base.test.ts` → all assertions GREEN.
2. `docker compose config --services | sort` → exactly the 7 slim-core names.
3. Bare `docker compose up -d --wait` → 6 healthy + migrate exited(0) within 180s.
4. `grep -v '^#' docker-compose.yml | grep -c '^[[:space:]]*profiles:'` → `0`.
5. Coverage on the new test file ≥ 90/90/90/90 (trivially — it's a pure YAML reader).
</verification>

<success_criteria>
Phase success criterion #1 covered (slim default = 6 services + migrate, bare `docker compose up`, profiles inversion closed). All other success criteria stay open for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/14-slim-core-byok-profiles-v2/14-01-SUMMARY.md` per the get-shit-done summary template — list the 12 deleted services, the 5 deleted volumes, the host-port additions, the depends_on prunes, and the OTel env fallback strip.
</output>
