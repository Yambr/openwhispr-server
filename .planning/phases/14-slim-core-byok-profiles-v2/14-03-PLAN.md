---
phase: 14-slim-core-byok-profiles-v2
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - compose/docker-compose.observability.yml
  - compose/docker-compose.storage.yml
  - compose/docker-compose.ingress.yml
  - compose/docker-compose.pgbouncer.yml
  - compose/docker-compose.dev-tools.yml
  - compose/docker-compose.contract-test.yml
  - compose/grafana/provisioning/datasources/postgres.yaml
  - tests/e2e-cjm/support/compose-harness.ts
  - Makefile
  - scripts/run.sh
  - tools/lint-compose-chart-parity.test.ts
autonomous: true
requirements:
  - SLIM-02
must_haves:
  truths:
    - "Six new overlay files exist under `compose/`: `docker-compose.observability.yml`, `.storage.yml`, `.ingress.yml`, `.pgbouncer.yml`, `.dev-tools.yml`, `.contract-test.yml`."
    - "`compose/docker-compose.observability.yml` declares exactly 5 services: otel-collector, loki, tempo, mimir, grafana — extracted verbatim from pre-Phase-14 base — AND re-declares `api.depends_on.otel-collector: service_started` and `worker.depends_on.otel-collector: service_started` AND re-injects `OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}` into api+worker environment blocks."
    - "`compose/docker-compose.storage.yml` declares minio + `api.depends_on.minio: service_healthy` + storage env defaults."
    - "`compose/docker-compose.ingress.yml` declares traefik + Traefik router labels deltas AND removes the slim-core host ports `3000:3000` (web) and `4000:3000` (api) using compose 2.20+ `ports: !reset []` override syntax — so when the ingress overlay is layered, only Traefik (`:80`/`:443`/`:8443`/`:8080`) is reachable on the host. Without the overlay, the bare slim-core continues to bind `localhost:3000` (web) and `localhost:4000` (api) per Phase-14 OSS quickstart contract."
    - "`compose/docker-compose.pgbouncer.yml` declares pgbouncer + `api.depends_on.pgbouncer: service_healthy` + `migrate.depends_on.pgbouncer: service_healthy` (sequencing barrier per RESEARCH §A.1) + overlay-side `DATABASE_URL=postgresql://...@pgbouncer:6432/...` re-injection."
    - "`compose/docker-compose.dev-tools.yml` declares ONLY mailpit (TD-14.a closure — CONTEXT decision 1)."
    - "`compose/docker-compose.contract-test.yml` declares fixture-idp + seed + contract-test-runner."
    - "`compose/grafana/provisioning/datasources/postgres.yaml:32` points at `postgres:5432` direct (not `pgbouncer:6432`) per RESEARCH §A.3 — Grafana datasource works regardless of pgbouncer overlay state."
    - "`tests/e2e-cjm/support/compose-harness.ts` `COMPOSE_FILES` constant includes the new overlays needed by Phase 13 happy-path scenarios (observability, pgbouncer, dev-tools, ingress) per RESEARCH §B.3. Phase 13 scenarios already address services via Traefik front URL (`https://api.localhost/...`, see `compose-harness.ts:71`), so the ingress overlay's `ports: !reset []` host-port removal does NOT regress them."
    - "Makefile targets `up`, `up-with-observability`, `up-with-storage`, `up-with-ingress`, `up-with-pgbouncer`, `up-with-dev-tools`, `up-full` exist and use explicit `-f` chains per RESEARCH §G.1."
    - "`tools/lint-compose-chart-parity.test.ts` allowlist recognizes overlay-resident services (fixture-idp, seed, contract-test-runner, mailpit) as compose-only / no-Helm."
  artifacts:
    - path: "compose/docker-compose.observability.yml"
      provides: "Observability overlay (5 services)"
      contains: "otel-collector:"
    - path: "compose/docker-compose.storage.yml"
      provides: "Storage overlay (minio)"
      contains: "minio:"
    - path: "compose/docker-compose.ingress.yml"
      provides: "Ingress overlay (traefik) + host-port reset on api/web"
      contains: "traefik:"
    - path: "compose/docker-compose.pgbouncer.yml"
      provides: "Pooler overlay (pgbouncer)"
      contains: "pgbouncer:"
    - path: "compose/docker-compose.dev-tools.yml"
      provides: "Dev-tools overlay (mailpit only)"
      contains: "mailpit:"
      excludes: "fixture-idp:|seed:|contract-test-runner:"
    - path: "compose/docker-compose.contract-test.yml"
      provides: "Contract-test overlay (fixture-idp + seed + contract-test-runner)"
      contains: "contract-test-runner:"
  key_links:
    - from: "compose/docker-compose.observability.yml api service env block"
      to: "OTEL_EXPORTER_OTLP_ENDPOINT default http://otel-collector:4317"
      via: "overlay env-default re-injection (RESEARCH §A.3)"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT.*:-http://otel-collector:4317"
    - from: "compose/docker-compose.pgbouncer.yml api service env block"
      to: "DATABASE_URL pointing at pgbouncer:6432"
      via: "overlay DATABASE_URL re-injection"
      pattern: "pgbouncer:6432"
    - from: "compose/docker-compose.ingress.yml api + web service definitions"
      to: "host-port unbinding via ports: !reset []"
      via: "compose 2.20+ override-merge `!reset` operator"
      pattern: "ports:\\s*!reset\\s*\\[\\]"
---

<objective>
Author the six compose overlays that re-introduce every non-slim service the
base lost in plan 14-01. Each overlay re-injects the `depends_on` edges and
environment defaults that the base stripped, so that `docker compose -f
docker-compose.yml -f compose/docker-compose.<overlay>.yml up` reproduces
the original behavior of that service set.

Update the Grafana datasource (no longer assumes pgbouncer), the e2e-cjm
compose-harness `COMPOSE_FILES` list, the Makefile invocation chains, and
the compose-chart-parity linter allowlist.

Output: SLIM-02 satisfied. The slim-core overlay layering works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-CONTEXT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH-slim-core-map.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-PATTERNS.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-01-PLAN.md

@docker-compose.yml
@compose/e2e/docker-compose.e2e.yml
@compose/grafana/provisioning/datasources/postgres.yaml
@tests/e2e-cjm/support/compose-harness.ts
@Makefile
@scripts/run.sh
@tools/lint-compose-chart-parity.test.ts

<interfaces>
<!-- COMPOSE_FILES current shape — extract from tests/e2e-cjm/support/compose-harness.ts:60-63 -->
<!-- ```ts
<!-- export const COMPOSE_FILES: readonly string[] = [
<!--   "docker-compose.yml",
<!--   "docker-compose.embedded-litellm.yml",
<!-- ] as const;
<!-- ``` -->
<!-- bootStack already accepts opts.composeFiles override per compose-harness.ts:177-181 — no API change needed -->
<!-- Phase 13 readiness URL is https://api.localhost/api/health (Traefik-fronted) per compose-harness.ts:71 — Phase 13 e2e never depends on bare host ports 3000/4000, so removing them via ports: !reset [] in the ingress overlay is safe -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — overlay conformance tests (one consolidated suite)</name>
  <files>
    - tests/integration/compose-overlays.test.ts (new)
  </files>
  <behavior>
    A single Vitest suite with one `describe()` per overlay. Each describe asserts:
    - the overlay file exists at `compose/docker-compose.<name>.yml`
    - `yaml.parse` of the file produces the expected `services` keys (set equality with the rows in CONTEXT.md decision 1's routing table)
    - the overlay re-injects the `depends_on` and `environment` deltas from RESEARCH §A.3 (per the must_haves.truths above)
    - `docker compose -f docker-compose.yml -f compose/docker-compose.<name>.yml config -q` exits 0 (lints the merged config) — invoked via `execaCommand` from `execa` with `reject: false`.

    Plus integration assertions:
    - `compose/grafana/provisioning/datasources/postgres.yaml` `url` field equals `jdbc:postgresql://postgres:5432/${POSTGRES_DB}` (no `pgbouncer`).
    - `tests/e2e-cjm/support/compose-harness.ts` `COMPOSE_FILES` constant (read as source text, parse with simple regex) contains the 4 overlay paths required for cjm happy-path: observability, pgbouncer, dev-tools, ingress.
    - `Makefile` contains targets `up`, `up-with-observability`, `up-with-storage`, `up-with-ingress`, `up-with-pgbouncer`, `up-with-dev-tools`, `up-full`.
    - `tools/lint-compose-chart-parity.test.ts` allowlist (read as source) lists `mailpit`, `fixture-idp`, `seed`, `contract-test-runner` as overlay-only-no-helm.
    - **Ingress overlay host-port reset:** parse `compose/docker-compose.ingress.yml` and assert that under `services.api.ports` and `services.web.ports` the merged value uses the compose 2.20+ `!reset []` override (the YAML tag-resolved value will be an empty list with the `!reset` tag); equivalently, the raw file text contains `ports: !reset []` (or `ports:\n  !reset []`) under both the `api:` and `web:` service blocks. After merge: `docker compose -f docker-compose.yml -f compose/docker-compose.ingress.yml config --format json | jq '.services.api.ports'` returns `[]` (or omits the key entirely).
  </behavior>
  <action>
    Author `tests/integration/compose-overlays.test.ts` with the assertions above. Use `yaml` package + `execa` for the `docker compose config -q` smoke (execa already a workspace dep — verify via `grep execa package.json`). Mark the `docker compose config -q` and `config --format json` portions with `it.skipIf(process.env.SKIP_DOCKER === "1")` so CI without docker can still typecheck. Use the `yaml` package with `customTags`-aware parsing OR fall back to substring assertion on the raw file text for the `!reset` tag (the `yaml` package preserves YAML tags via the `Node` API — see `yaml` docs). Run the suite — RED. Commit as `test(14-03): RED compose overlay conformance + grafana datasource + harness + Makefile + parity allowlist + ingress port-reset`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/compose-overlays.test.ts</automated>
  </verify>
  <done>Suite RED with assertions pointing at the missing overlay files, datasource still on pgbouncer, ingress overlay missing the `!reset` directive, etc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — author the six overlays + grafana datasource fix</name>
  <files>
    - compose/docker-compose.observability.yml (new)
    - compose/docker-compose.storage.yml (new)
    - compose/docker-compose.ingress.yml (new)
    - compose/docker-compose.pgbouncer.yml (new)
    - compose/docker-compose.dev-tools.yml (new)
    - compose/docker-compose.contract-test.yml (new)
    - compose/grafana/provisioning/datasources/postgres.yaml
  </files>
  <behavior>
    Authored overlays match RESEARCH §A.1 service definitions verbatim (including image pin, healthcheck, env block, depends_on intra-overlay edges, named volumes, host ports). Each overlay also adds the cross-base re-declarations per RESEARCH §G.2 worked example.
  </behavior>
  <action>
    For each overlay, read the corresponding service block in the pre-edit `docker-compose.yml` (use `git show HEAD~N:docker-compose.yml` to recover if the file is already mid-edit, but plan 14-01 must be merged FIRST per `depends_on: [01]`).

    Per overlay specifics:

    **observability.yml** (per CONTEXT decision 1 + RESEARCH §A.1 rows 6-10):
    - Top-level `services:` containing otel-collector, loki, tempo, mimir, grafana with the exact image pins / healthchecks / depends_on / env_files from base.
    - Top-level `volumes:` containing `loki_data`, `tempo_data`, `mimir_data`, `grafana_data`.
    - Service-level overrides on `api` and `worker` (declared at top level alongside the new services): `depends_on.otel-collector: {condition: service_started}` and `environment.OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-http://otel-collector:4317}` (overlay re-adds the `:-` default that plan 14-01 stripped from base).

    **storage.yml** (RESEARCH §A.1 row 4):
    - `services:` minio + `api.depends_on.minio: {condition: service_healthy}` + `api.environment` re-injection of `S3_ENDPOINT: ${S3_ENDPOINT:-http://minio:9000}`, `S3_ACCESS_KEY: ${S3_ACCESS_KEY:-${MINIO_ROOT_USER}}`, `S3_SECRET_KEY: ${S3_SECRET_KEY:-${MINIO_ROOT_PASSWORD}}`, `S3_BUCKET: ${S3_BUCKET:-openwhispr}`.
    - Top-level `volumes: { minio_data: {} }`.

    **ingress.yml** (RESEARCH §A.1 row 5):
    - `services:` traefik (with all four host ports 80/443/8443/8080).
    - `api` and `web` service overrides: ADD `labels` block per `docker-compose.yml:676-699` Traefik label pattern (use the exact router rules `Host(api.localhost)` for api and the web router as already configured pre-Phase-14), AND set `ports: !reset []` to unbind the slim-core base host ports (`4000:3000` on api, `3000:3000` on web). With the ingress overlay layered, only Traefik (`:80`/`:443`/`:8443`/`:8080`) is reachable on the host — clean production posture. Without the overlay, slim-core base keeps `localhost:3000`/`:4000` for the OSS quickstart.
    - YAML syntax: under each service, write `ports: !reset []` (compose 2.20+; confirmed available on the dev host running Docker Compose v2.23.0-desktop.1). Do NOT use `ports: []` (override-merge would prepend, not replace).

    **pgbouncer.yml** (RESEARCH §A.1 row 2):
    - `services:` pgbouncer + `api.depends_on.pgbouncer: {condition: service_healthy}` + `migrate.depends_on.pgbouncer: {condition: service_healthy}` (sequencing barrier per RESEARCH §A.1 comment) + `api.environment.DATABASE_URL: postgresql://${POSTGRES_APP_USER}:${POSTGRES_APP_PASSWORD}@pgbouncer:6432/${POSTGRES_DB}` (overlay re-points DATABASE_URL through pooler).

    **dev-tools.yml** (RESEARCH §A.1 row 16) — ONLY mailpit:
    - `services:` mailpit with host port `127.0.0.1:8025:8025` and the env block from base. NO api `depends_on.mailpit` re-add (per RESEARCH §A.3 mailpit hard-codes table: recommend dropping the dep from base; `EmailSender.ts` loud-fail handles missing SMTP at runtime, not boot).

    **contract-test.yml** (RESEARCH §A.1 rows 17-19):
    - `services:` fixture-idp + seed + contract-test-runner with their `restart: "no"`, build contexts, `depends_on` edges as in base.

    **grafana datasource fix** — `compose/grafana/provisioning/datasources/postgres.yaml:32`: change `url: pgbouncer:6432` → `url: postgres:5432` (per RESEARCH §A.3 first table — Grafana datasource doesn't need pooling).

    GREEN. Commit as `feat(14-03): six compose overlays + grafana direct-pg datasource + ingress ports !reset`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/compose-overlays.test.ts -t "overlay file structure|grafana|ingress port-reset"</automated>
    Also: `for o in observability storage ingress pgbouncer dev-tools contract-test; do docker compose -f docker-compose.yml -f compose/docker-compose.$o.yml config -q || exit 1; done`
    Also: `docker compose -f docker-compose.yml -f compose/docker-compose.ingress.yml config --format json | jq -e '.services.api.ports == [] and .services.web.ports == []'`
  </verify>
  <done>All 6 overlays lint clean when merged with slim-core base; Grafana datasource points at postgres direct; ingress overlay strips api+web host ports via `!reset`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — update Makefile, scripts/run.sh, cjm compose-harness, parity linter allowlist</name>
  <files>
    - Makefile
    - scripts/run.sh
    - tests/e2e-cjm/support/compose-harness.ts
    - tools/lint-compose-chart-parity.test.ts
  </files>
  <behavior>
    Wire the overlays into the developer-facing entry points so they're discoverable.
  </behavior>
  <action>
    1. **Makefile** — add the seven `up*` targets per RESEARCH §G.1:
       - `up:` invokes bare `docker compose up -d --wait`.
       - `up-with-<overlay>:` adds the matching `-f compose/docker-compose.<overlay>.yml`.
       - `up-full:` chains all five non-contract-test overlays in order: observability → storage → ingress → pgbouncer → dev-tools.
       - Preserve any existing `up`/`up-*` target body that does additional work (e.g. waiting on healthchecks); only replace the `docker compose` invocation portion.
       - Update any `db-only` / `obs-only` / `dev` / `contract-test` profile-based targets to use the equivalent `-f` overlay chain (per RESEARCH §G.3 "After Phase 14, these profile-based invocations replace with explicit `-f` selectors").

    2. **scripts/run.sh** — apply the same `-f`-chain pattern if the script currently honors `COMPOSE_PROFILES=…`. Preserve any unrelated logic.

    3. **tests/e2e-cjm/support/compose-harness.ts** — line 60-63: extend `COMPOSE_FILES` per RESEARCH §B.3:
       ```ts
       export const COMPOSE_FILES: readonly string[] = [
         "docker-compose.yml",
         "docker-compose.embedded-litellm.yml",
         "compose/docker-compose.observability.yml",
         "compose/docker-compose.pgbouncer.yml",
         "compose/docker-compose.dev-tools.yml",
         "compose/docker-compose.ingress.yml",
       ] as const;
       ```
       Note: Phase 13 readiness/healthcheck flows already use the Traefik front URL `https://api.localhost/api/health` (`compose-harness.ts:71`), so the ingress overlay's `ports: !reset []` is compatible — no scenario regresses from host-port removal. Per CLAUDE.md no-workarounds: if a Phase 13 scenario regresses due to overlay layering, fix the overlay or the scenario; do NOT add scenario-specific overrides here.

    4. **tools/lint-compose-chart-parity.test.ts** — extend the allowlist (lines around 152/165/167/170/210/212/214/272 per RESEARCH §A.3) with the new "overlay-only-no-helm" classification: add `mailpit`, `fixture-idp`, `seed`, `contract-test-runner` to whatever data structure the linter uses to recognize compose services that have no Helm template counterpart. Mirror the existing `cluster-prereq` classification mechanism — do not invent a new schema.

    Run the conformance suite (Task 1) and the parity linter — both must pass. Commit as `chore(14-03): wire overlays into Makefile, run.sh, cjm harness, parity allowlist`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/compose-overlays.test.ts && pnpm vitest run tools/lint-compose-chart-parity.test.ts</automated>
    Also: `make -n up-with-observability | grep -F 'compose/docker-compose.observability.yml'`
  </verify>
  <done>All conformance assertions GREEN. Parity linter green with overlay services classified. Makefile dry-run shows correct `-f` chains.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run tests/integration/compose-overlays.test.ts` GREEN.
2. `pnpm vitest run tools/lint-compose-chart-parity.test.ts` GREEN.
3. `for o in observability storage ingress pgbouncer dev-tools contract-test; do docker compose -f docker-compose.yml -f compose/docker-compose.$o.yml config -q; done` exits 0 for all six.
4. `make -n up-full` shows the chain `-f docker-compose.yml -f compose/docker-compose.observability.yml -f compose/docker-compose.storage.yml -f compose/docker-compose.ingress.yml -f compose/docker-compose.pgbouncer.yml -f compose/docker-compose.dev-tools.yml`.
5. `docker compose -f docker-compose.yml -f compose/docker-compose.ingress.yml config --format json | jq -e '.services.api.ports == [] and .services.web.ports == []'` exits 0 (ingress overlay strips base host ports).
6. Coverage on the new test file ≥ 90/90/90/90.
</verification>

<success_criteria>
SLIM-02 (opt-in compose overlay files) observable in the filesystem and lint-clean. Phase 14 success criterion #2 covered (additive layering, mailpit ONLY in dev-tools, 6 overlay files under `compose/`).
</success_criteria>

<output>
After completion, create `.planning/phases/14-slim-core-byok-profiles-v2/14-03-SUMMARY.md` — list the 6 overlay files with their service rosters, the grafana datasource change, the Makefile targets added, the `COMPOSE_FILES` extension, the parity allowlist additions, and the ingress overlay's `ports: !reset []` directive on api/web.
</output>
