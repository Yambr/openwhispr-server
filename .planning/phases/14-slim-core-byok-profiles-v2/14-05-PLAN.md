---
phase: 14-slim-core-byok-profiles-v2
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - apps/worker/src/index.ts
  - apps/worker/src/queues.ts
  - apps/worker/src/scheduler.ts
  - apps/worker/src/queues.test.ts
  - apps/worker/src/scheduler.test.ts
  - apps/worker/src/jobs/virtual-key-rotation.ts (DELETE)
  - apps/worker/src/jobs/virtual-key-rotation.test.ts (DELETE)
  - tests/e2e/log-scrub-sentinel.test.ts
  - docs/architecture.md
  - docs/operations.md
autonomous: true
requirements:
  - BYOK-03
must_haves:
  truths:
    - "`apps/worker/src/jobs/virtual-key-rotation.ts` and its `.test.ts` are deleted from the tree (verified by `git ls-files`)."
    - "`apps/worker/src/index.ts` no longer references `noopLitellmKeyClient`, `noopUserKeyLookup`, `LiteLlmKeyClient`, `UserKeyLookup`, `buildVirtualKeyRotationHandler`, or `vkrWorker`. The internal-mock-in-production constitutional violation is closed."
    - "`apps/worker/src/queues.ts` no longer registers `virtualKeyRotation` in `QUEUE_NAMES` or the registry."
    - "`apps/worker/src/scheduler.ts` no longer enqueues the weekly virtual-key-rotation cron tick."
    - "`apps/worker/src/queues.test.ts` and `scheduler.test.ts` still pass — the virtual-key-rotation expectations are removed; no fake-success substitutions added."
    - "`tests/e2e/log-scrub-sentinel.test.ts:109-123` is rewritten to use the `email-delivery` queue (a surviving real queue with a deterministic payload schema) instead of `virtual-key-rotation` — RESEARCH §A.5."
    - "`docs/architecture.md:263` no longer shows `Q2[virtual-key-rotation]` in the queue diagram."
    - "`docs/operations.md` documents a one-shot `valkey-cli DEL bull:virtual-key-rotation:*` cleanup step for upgrade-in-place operators (RESEARCH §A.5 side-effect note)."
    - "An idempotent transient cleanup at worker boot (`apps/worker/src/index.ts`) DELs `bull:virtual-key-rotation:*` keys via the existing Valkey connection — safe to re-run, easy to remove later when stragglers stop appearing."
  artifacts:
    - path: "apps/worker/src/index.ts"
      provides: "Worker entrypoint — vkr removed, transient cleanup wired"
      excludes: "noopLitellmKeyClient|noopUserKeyLookup|vkrWorker|buildVirtualKeyRotationHandler"
      contains: "bull:virtual-key-rotation"
    - path: "apps/worker/src/queues.ts"
      provides: "Queue registry — virtualKeyRotation removed"
      excludes: "virtualKeyRotation"
    - path: "apps/worker/src/scheduler.ts"
      provides: "Scheduler — vkr cron removed"
      excludes: "virtualKeyRotation"
    - path: "tests/e2e/log-scrub-sentinel.test.ts"
      provides: "Log-scrub e2e using email-delivery queue"
      contains: "email-delivery"
      excludes: "virtual-key-rotation"
  key_links:
    - from: "apps/worker/src/index.ts boot"
      to: "valkey DEL bull:virtual-key-rotation:*"
      via: "transient cleanup call wrapped in try/catch"
      pattern: "DEL.*bull:virtual-key-rotation"
---

<objective>
Execute the full virtual-key-rotation removal per CONTEXT.md decision 3 and
RESEARCH §A.5. This is the cleanest path to closing BYOK-03 — replacing
the two internal-mock adapters (`noopLitellmKeyClient`, `noopUserKeyLookup`)
with real implementations would require ~4 new pieces of infra (litellm-client
methods, DB migration, admin route, dispatcher) that are NOT in Phase 14
scope. CONTEXT decision 3 chose "remove the dead path entirely" — that is
the work this plan executes.

Output: BYOK-03 satisfied. The `noopX` constitutional violation is gone.
A transient cleanup at worker boot drains stale BullMQ keys for
upgrade-in-place operators. `log-scrub-sentinel.test.ts` is rewritten to
hit the `email-delivery` queue instead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-CONTEXT.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-RESEARCH-noopx.md
@.planning/phases/14-slim-core-byok-profiles-v2/14-PATTERNS.md

@apps/worker/src/index.ts
@apps/worker/src/queues.ts
@apps/worker/src/scheduler.ts
@apps/worker/src/queues.test.ts
@apps/worker/src/scheduler.test.ts
@apps/worker/src/jobs/virtual-key-rotation.ts
@tests/e2e/log-scrub-sentinel.test.ts
@docs/architecture.md
@docs/operations.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — assertions that confirm vkr is gone</name>
  <files>
    - tests/integration/virtual-key-rotation-removed.test.ts (new)
  </files>
  <behavior>
    - Test 1: `existsSync("apps/worker/src/jobs/virtual-key-rotation.ts") === false`.
    - Test 2: same for `.test.ts`.
    - Test 3: `grep -rln "virtualKeyRotation\\|noopLitellmKeyClient\\|noopUserKeyLookup\\|buildVirtualKeyRotationHandler\\|vkrWorker"` over `apps/worker/src` and `apps/api/src` returns ZERO matches (uses `execa` to shell out). EXCEPT in `apps/worker/src/index.ts` where the literal string `bull:virtual-key-rotation` is permitted in the transient cleanup block.
    - Test 4: `apps/worker/src/queues.ts` source does NOT contain `virtualKeyRotation`.
    - Test 5: `apps/worker/src/scheduler.ts` source does NOT contain `virtualKeyRotation` or `0 3 * * 0`.
    - Test 6: `tests/e2e/log-scrub-sentinel.test.ts` no longer contains `virtual-key-rotation`; instead contains `email-delivery`.
    - Test 7: `docs/architecture.md` does NOT contain `Q2[virtual-key-rotation]`.
    - Test 8: `docs/operations.md` contains the literal `valkey-cli DEL bull:virtual-key-rotation:*` cleanup instruction.
  </behavior>
  <action>
    Author `tests/integration/virtual-key-rotation-removed.test.ts` with the 8 assertions above. Run; FAIL on every assertion (RED — pre-edit state). Commit as `test(14-05): RED virtual-key-rotation removal conformance`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/virtual-key-rotation-removed.test.ts</automated>
  </verify>
  <done>All 8 assertions RED, pointing at the current vkr presence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — delete vkr wiring + transient Valkey cleanup</name>
  <files>
    - apps/worker/src/index.ts
    - apps/worker/src/queues.ts
    - apps/worker/src/scheduler.ts
    - apps/worker/src/queues.test.ts
    - apps/worker/src/scheduler.test.ts
    - apps/worker/src/jobs/virtual-key-rotation.ts (DELETE)
    - apps/worker/src/jobs/virtual-key-rotation.test.ts (DELETE)
  </files>
  <behavior>
    Per the file:line removal map in RESEARCH §A.5 (full 8-file, 30-LOC scope), execute every deletion. After edits:
    - All assertions from Task 1 GREEN (except #6 which is Task 3).
    - `pnpm typecheck` GREEN.
    - `pnpm vitest run apps/worker` GREEN (queue + scheduler suites lose the vkr cases but otherwise unchanged).
    - Transient cleanup block in `apps/worker/src/index.ts` boot path: after the Valkey connection is established, call `await connection.eval("local k = redis.call('KEYS', ARGV[1]); for i=1,#k do redis.call('DEL', k[i]) end; return #k", 0, "bull:virtual-key-rotation:*")` (or simpler `connection.keys("bull:virtual-key-rotation:*")` + `connection.del(...)`) wrapped in `try { … } catch (err) { logger.warn({err}, "transient vkr-key cleanup failed; non-fatal"); }`. Idempotent — does nothing on subsequent boots after keys drained.
  </behavior>
  <action>
    Execute the deletion map from RESEARCH §A.5 line-by-line in this order:
    1. `git rm apps/worker/src/jobs/virtual-key-rotation.ts apps/worker/src/jobs/virtual-key-rotation.test.ts`.
    2. Edit `apps/worker/src/index.ts`: delete lines per RESEARCH §A.5 rows 1-7 (imports, noop adapters, vkrWorker registration, drain-list entry, header comments). ADD the transient Valkey cleanup block right after the `connection` is constructed and before the Worker registrations.
    3. Edit `apps/worker/src/queues.ts`: delete lines per RESEARCH §A.5 rows 8-12.
    4. Edit `apps/worker/src/scheduler.ts`: delete lines per RESEARCH §A.5 rows 13-17.
    5. Edit `apps/worker/src/queues.test.ts:41`: remove the `virtualKeyRotation` line from the QUEUE_NAMES expectation.
    6. Edit `apps/worker/src/scheduler.test.ts:41,90-130`: remove the registry stub entry and the two vkr cron tests.
    7. Run `pnpm typecheck && pnpm vitest run apps/worker`. Both GREEN.
    8. Run the Task 1 conformance test — assertions 1-5, 7 GREEN; 6, 8 still pending Task 3.
    Commit as `feat(14-05)!: remove virtual-key-rotation wiring (constitutional: no internal mocks)`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm vitest run apps/worker tests/integration/virtual-key-rotation-removed.test.ts -t "file removed|index.ts|queues.ts|scheduler.ts|docs/architecture"</automated>
  </verify>
  <done>vkr files gone; worker suites green; typecheck green; transient cleanup wired and try-catched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — rewrite log-scrub-sentinel.test.ts + docs updates</name>
  <files>
    - tests/e2e/log-scrub-sentinel.test.ts
    - docs/architecture.md
    - docs/operations.md
  </files>
  <behavior>
    - `tests/e2e/log-scrub-sentinel.test.ts:109-123` enqueues an `email-delivery` job with a payload that contains a known credential-bearing string (e.g. an SMTP password sentinel like `__SCRUB_SENTINEL__pwd-secret-zzz__END__`) and asserts the log scrubber redacts the secret substring in the captured log line.
    - `docs/architecture.md:263` queue diagram: `Q2[virtual-key-rotation]` node removed; remaining nodes (Q1, Q3, …) renumbered or relabeled minimally so the diagram still parses (mermaid syntax check).
    - `docs/operations.md`: under an "Upgrade notes" or "Operator runbooks" section, add a subsection titled `### Upgrade from Phase 13 — virtual-key-rotation removal`. Body: brief explanation of the removal (link to CONTEXT.md decision 3 rationale), then the one-shot cleanup command `valkey-cli -h <valkey-host> -p 6379 -a "${VALKEY_PASSWORD}" DEL bull:virtual-key-rotation:*` (also note the worker's transient cleanup makes this optional for most operators).
  </behavior>
  <action>
    1. Edit `tests/e2e/log-scrub-sentinel.test.ts:109-123` per RESEARCH §A.5: swap the queue from `virtualKeyRotation` → `emailDelivery`, swap the payload to a valid email-delivery payload (read `apps/worker/src/jobs/email-delivery.ts` for the schema and use a minimal-valid object — to/from/subject/body — with the credential sentinel embedded in the body). Per CLAUDE.md: do not add a "loop with retry" workaround; if the new queue's payload schema rejects the credential sentinel for SMTP-validation reasons, embed the sentinel in a free-form field like `body` that is already redacted by the log scrubber.
    2. Edit `docs/architecture.md:263`: read 10 lines of context around line 263; remove `Q2[virtual-key-rotation]` node; verify the diagram fence is still valid mermaid (`pnpm exec mermaid-cli ...` if available; else manual syntax check).
    3. Edit `docs/operations.md`: add the new subsection under an appropriate parent. Cite the worker-side transient cleanup so operators know it's a belt-and-braces measure.
    4. Run Task 1 conformance — all 8 assertions GREEN. Run `pnpm test:e2e -t log-scrub-sentinel` (if e2e gated by `E2E=1` per PROJECT.md, run with `E2E=1 pnpm test:e2e -t log-scrub-sentinel`).
    Commit as `test(14-05): rewrite log-scrub-sentinel to use email-delivery queue` and `docs(14-05): remove vkr from architecture.md, add upgrade note to operations.md`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/integration/virtual-key-rotation-removed.test.ts</automated>
    Plus: `E2E=1 pnpm test:e2e -t log-scrub-sentinel` (smoke).
  </verify>
  <done>All 8 conformance assertions GREEN. log-scrub-sentinel e2e GREEN against email-delivery. Diagram and operator note in place.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run tests/integration/virtual-key-rotation-removed.test.ts` — 8/8 GREEN.
2. `pnpm typecheck` GREEN.
3. `pnpm vitest run apps/worker --coverage` GREEN; coverage on touched worker files ≥ 90/90/90/90 on diff.
4. `E2E=1 pnpm test:e2e -t log-scrub-sentinel` GREEN.
5. `git ls-files | grep -F virtual-key-rotation` returns NO matches (file deletion verified).
6. `grep -rln "noopLitellmKeyClient\\|noopUserKeyLookup" apps/` returns NO matches.
</verification>

<success_criteria>
BYOK-03 satisfied (vkr removal closes the noopX audit). Phase 14 success criterion #4 covered (all three noops resolved: noopSender already replaced in Phase 13, noopLitellmKeyClient + noopUserKeyLookup deleted here). Constitutional "no internal mocks in production code" rule honored.
</success_criteria>

<output>
After completion, create `.planning/phases/14-slim-core-byok-profiles-v2/14-05-SUMMARY.md` — quote the 8-file removal map, show the transient cleanup block diff, summarize the log-scrub-sentinel rewrite, and quote the operations.md upgrade note.
</output>
