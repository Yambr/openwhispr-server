---
phase: 03
plan: 09
slug: docs-makefile-e2e-and-readme
type: execute
wave: 3
status: locked
depends_on: ["03-01", "03-02", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - docs/litellm-target-spec.md
  - docs/litellm-mock-mode.md
  - Makefile
  - README.md
  - tools/lint-docs-headings.ts
autonomous: true
requirements: [LITELLM-05, LITELLM-06]
estimated_tokens: 25000

must_haves:
  truths:
    - "docs/litellm-target-spec.md exists, derived from speaches-audio.md, generalized for bundled-default + corporate-override topologies (LITELLM-06)"
    - "docs/litellm-target-spec.md documents env-override path: LITELLM_BASE_URL + corporate-LiteLLM-runs-its-own-virtual-keys posture (LITELLM-05)"
    - "docs/litellm-mock-mode.md explains the contract-test mock_response approach"
    - "Makefile `make e2e-test` target uses `.env.e2e` (operator-supplied real keys per CONTEXT D-05B)"
    - "README.md Quickstart section updated to reference Phase 3 endpoints + provider-key onboarding"
  artifacts:
    - path: docs/litellm-target-spec.md
      provides: "Bundled + corporate-override LiteLLM topology, model definitions, request_id propagation, env-override path"
      min_lines: 150
    - path: docs/litellm-mock-mode.md
      provides: "Mock-mode contract test architecture"
      min_lines: 40
    - path: Makefile
      provides: "e2e-test target"
      contains: "e2e-test:"
  key_links:
    - from: docs/litellm-target-spec.md
      to: speaches-audio.md
      via: "References upstream pattern as the corporate-operator example"
      pattern: "speaches-audio.md"
---

<objective>
Documentation + Makefile glue closing LITELLM-05 + LITELLM-06.

Purpose:
- Author `docs/litellm-target-spec.md` as the operator-facing reference for both bundled-default and corporate-override LiteLLM topologies. Generalizes `speaches-audio.md` (which is the corporate-LiteLLM-with-Speaches example) into a topology-agnostic spec.
- Document the mock-mode contract-test architecture in `docs/litellm-mock-mode.md`.
- Add `make e2e-test` target that runs the contract suite against real provider APIs from `.env.e2e` (CONTEXT D-05B).
- Update README quickstart to mention Phase 3 endpoints + provider-key opt-in.

Output: docs that an operator can read and self-host with confidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-litellm-integration-bundled-oss-models/03-CONTEXT.md
@.planning/phases/03-litellm-integration-bundled-oss-models/03-01-SUMMARY.md
@.planning/phases/03-litellm-integration-bundled-oss-models/03-02-SUMMARY.md
@.planning/phases/03-litellm-integration-bundled-oss-models/03-03-SUMMARY.md
@.planning/phases/03-litellm-integration-bundled-oss-models/03-08-SUMMARY.md
@speaches-audio.md
@README.md
@Makefile
@.env.example

<interfaces>
Existing docs lint: tools/lint-docs-headings.ts (extend if needed for new doc).

Existing make contract-test target — pattern to mirror for make e2e-test.

speaches-audio.md is the corporate-operator example; we generalize, NOT copy-verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author docs/litellm-target-spec.md + docs/litellm-mock-mode.md + lint pass</name>
  <files>docs/litellm-target-spec.md, docs/litellm-mock-mode.md, tools/lint-docs-headings.ts (extend allowlist if needed)</files>
  <behavior>
    - Test (lint): `pnpm exec tsx tools/lint-docs-headings.ts docs/litellm-target-spec.md docs/litellm-mock-mode.md` exits 0.
    - Test (English-only): `pnpm exec tsx tools/lint-english.ts docs/litellm-target-spec.md docs/litellm-mock-mode.md` (existing tool from Phase 0) exits 0.
    - Test (sections): docs/litellm-target-spec.md contains H2 sections: "Topologies", "Bundled-Default Configuration", "Corporate-Override Configuration", "Request ID Propagation", "Env Override Path", "Diarization Caveats", "Realtime WSS Topology", "Spend Log Ingestion".
    - Test (sections): docs/litellm-mock-mode.md contains H2 sections: "What This Solves", "How It Works", "Per-Model mock_response", "Diarization Mock", "Running Locally".
    - Test (links): no broken markdown links (use markdown-link-check or simple grep against existing files).
  </behavior>
  <action>
    1. Author `docs/litellm-target-spec.md` (≥150 lines). Sections:
       - Overview / why this exists (LITELLM-06 purpose: bundled-default + corporate-override single source of truth).
       - **Topologies**: 2 supported — (A) bundled-default (compose litellm sidecar, public APIs via .env keys), (B) corporate-override (LITELLM_BASE_URL → operator's existing LiteLLM Proxy; Speaches+pyannote behind it per speaches-audio.md pattern).
       - **Bundled-Default Configuration**: model_list excerpt from compose/litellm/litellm_config.yaml (Plan 01); operator opens .env, sets OPENROUTER_API_KEY/GROQ_API_KEY/OPENAI_API_KEY/PYANNOTE_API_KEY (D-11: Groq powers Whisper-large-v3 STT; D-12: OpenAI powers `WSS /v1/realtime` via `gpt-realtime` model entry with `mode: realtime`); missing keys → 503 envelope (or close-with-error on WSS upgrade for missing OPENAI_API_KEY).
       - **Corporate-Override Configuration**: operator sets `LITELLM_BASE_URL=https://litellm.corp.example.com` + `LITELLM_MASTER_KEY=<their-master-key>`; bundled litellm container can be disabled via `docker compose --profile default up -d --scale litellm=0` (or compose profile exclusion). Reference speaches-audio.md as the canonical corporate setup (Speaches CUDA image, internal pyannote nginx pass-through, 3600s realtime timeouts).
       - **Request ID Propagation**: API injects `x-litellm-spend-logs-metadata: {"openwhispr_request_id":"..."}` header; LiteLLM persists in spend log metadata column; spend ingest worker (apps/worker) reads `metadata->>'openwhispr_request_id'` for usage_ledger row idempotency. Plan 02 spike result confirms shape.
       - **Env Override Path**: list every Phase-3 env var (LITELLM_BASE_URL, LITELLM_MASTER_KEY, LITELLM_DATABASE_URL, OPENROUTER_API_KEY, GROQ_API_KEY, OPENAI_API_KEY (D-12 — Realtime), PYANNOTE_API_KEY, LITELLM_DEFAULT_CHAT_MODEL); operator-override matrix.
       - **Diarization Caveats**: D-07 outcome — bundled-default uses single-hop pyannote-compatible endpoint (chosen in Plan 01) OR 503-only; corporate operator wires their pyannote behind LiteLLM pass_through_endpoints per speaches-audio.md.
       - **Realtime WSS Topology**: Traefik → Fastify wsUpstream → LiteLLM → **OpenAI Realtime API direct** (D-12, `wss://api.openai.com/v1/realtime?model=gpt-realtime` via LiteLLM `mode: realtime`); 3600s ingress timeouts; opaque-bearer preserved (master key + OPENAI_API_KEY never exposed to desktop). Default model: `gpt-realtime` (GA). Aliases: `gpt-realtime-mini`, `gpt-4o-realtime-preview` (legacy). Pricing: $0.06/min audio in + $0.24/min audio out. Corporate operator with Speaches/Azure overrides via LITELLM_BASE_URL.
       - **Spend Log Ingestion**: BullMQ Job Scheduler every 30s; Postgres co-tenant read; idempotent UPSERT on request_id; pass-through endpoints unmetered (LITELLM-07 acknowledgment).
       - **Migration Path from Phase 2 (HIGH-1 fix)**: operators upgrading from Phase 2 do NOT need any manual action. The `litellm` database is auto-created by the Drizzle migrate runner on every `docker compose up` (idempotent — see Plan 01 Task 2 step 1a). The `make clean-stack` target remains AVAILABLE as a development/test convenience for fully resetting a local stack but is EXPLICITLY NOT REQUIRED for upgrades and EXPLICITLY NOT RECOMMENDED for production operators (it destroys all postgres data). Document this migration path as zero-action: `git pull && docker compose up -d` is sufficient — the migrate service ensures the litellm database exists before LiteLLM starts.

    2. Author `docs/litellm-mock-mode.md` (≥40 lines). Sections:
       - **What This Solves**: hermetic CI without internet/quotas; LiteLLM `mock_response` is a native feature, not a workaround.
       - **How It Works**: contract-test profile sets `LITELLM_CONFIG_FILE=litellm_config.contract.yaml` env, switching the litellm volume mount to the mock config. Each model in mock config has `litellm_params.mock_response: '<JSON>'`.
       - **Per-Model mock_response**: examples for whisper-large-v3 (Groq, D-11) + qwen3.6-plus. Document that `mode: realtime` entries (D-12 OpenAI realtime) are exempt — LiteLLM does not honor `mock_response` for WSS realtime; Plan 07's contract handshake test asserts proxy reach only, real realtime is e2e/Phase 4.
       - **Diarization Mock**: contract YAML has no pass_through_endpoints; `MOCK_DIARIZATION=true` env on api service short-circuits the diarization route (Plan 06).
       - **Running Locally**: `make contract-test` is the canonical entry; troubleshooting tips.

    3. Extend `tools/lint-docs-headings.ts` if needed to recognize new docs (existing tool already validates heading structure — add new files to its known list if it has one).
  </action>
  <verify>
    <automated>pnpm exec tsx tools/lint-docs-headings.ts docs/litellm-target-spec.md docs/litellm-mock-mode.md && pnpm exec tsx tools/lint-english.ts docs/litellm-target-spec.md docs/litellm-mock-mode.md && grep -c '^## ' docs/litellm-target-spec.md | awk '$1 < 8 {exit 1}'</automated>
  </verify>
  <done>Both docs exist, pass English + heading lints, contain all required sections, no broken links.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `make e2e-test` Makefile target + update README quickstart</name>
  <files>Makefile, README.md</files>
  <behavior>
    - Test (Makefile): `make e2e-test` target exists; uses `.env.e2e` not `.env`; refuses to run when `.env.e2e` missing (clear error message).
    - Test (Makefile): target sets `LITELLM_CONFIG_FILE=litellm_config.yaml` (real config), brings up compose, runs contract suite with real provider key paths exercised.
    - Test (Makefile help): `make help` lists e2e-test alongside contract-test.
    - Test (README): quickstart references Phase 3 endpoints; "Provider Keys" subsection lists OPENROUTER_API_KEY / GROQ_API_KEY / PYANNOTE_API_KEY with operator-actionable instructions.
  </behavior>
  <action>
    1. Update Makefile `.PHONY` line to include `e2e-test`. Add target:
       ```makefile
       # Phase 3 / D-05B — E2E contract suite against REAL provider APIs.
       # Operator supplies .env.e2e with OPENROUTER_API_KEY + GROQ_API_KEY +
       # PYANNOTE_API_KEY (real values, NOT bootstrap-generated). Costs real
       # money — runs locally or via scheduled (NOT main) CI.
       e2e-test:
       	@test -f .env.e2e || (echo "Refusing to run: .env.e2e not found. Create it with real OPENROUTER_API_KEY + GROQ_API_KEY + PYANNOTE_API_KEY values." && exit 1)
       	@grep -q '^OPENROUTER_API_KEY=' .env.e2e || (echo "OPENROUTER_API_KEY missing in .env.e2e" && exit 1)
       	@grep -q '^GROQ_API_KEY=' .env.e2e || (echo "GROQ_API_KEY missing in .env.e2e" && exit 1)
       	OPENWHISPR_TEST_ROUTES=true LITELLM_CONFIG_FILE=litellm_config.yaml \
       	  docker compose --env-file .env --env-file .env.e2e \
       	  --profile default --profile contract-test up -d --wait
       	@OPENWHISPR_TEST_ROUTES=true docker compose --env-file .env --env-file .env.e2e \
       	  --profile default --profile contract-test run --rm seed; \
       	rc=$$? ; \
       	if [ $$rc -ne 0 ]; then docker compose down -v ; exit $$rc ; fi ; \
       	OPENWHISPR_TEST_ROUTES=true docker compose --env-file .env --env-file .env.e2e \
       	  --profile default --profile contract-test run --rm contract-test-runner ; \
       	rc=$$? ; docker compose down -v ; exit $$rc
       ```

       Add a `.env.e2e.example` file at repo root listing the three provider keys with placeholder comments. Add `.env.e2e` to `.gitignore`.

    2. Update README.md:
       - Quickstart: after "first authenticated /api/transcribe" reference, add a "Provider Keys (optional)" subsection listing OPENROUTER_API_KEY / GROQ_API_KEY / OPENAI_API_KEY / PYANNOTE_API_KEY (D-11: STT via Groq; D-12: Realtime WSS via OpenAI direct) with one-line link to docs/litellm-target-spec.md.
       - Add a "Testing Modes" section briefly: `make test` (unit), `make contract-test` (hermetic CI), `make e2e-test` (real APIs, requires .env.e2e).

    3. Verify make help output (existing target `help` greps all colon-suffixed names).
  </action>
  <verify>
    <automated>make help 2>&1 | grep -q 'e2e-test' && grep -q 'Provider Keys' README.md && (test -f .env.e2e || (echo "expected refuse" && make e2e-test 2>&1 | grep -q 'Refusing to run'))</automated>
  </verify>
  <done>e2e-test target works (refuses without .env.e2e; runs with it); README quickstart updated; .env.e2e.example committed; .gitignore covers .env.e2e.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator ↔ docs | docs reference real env var names; no secrets in docs |
| `.env.e2e` ↔ git | gitignored; operator handles secrets out-of-band |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Information Disclosure | docs/litellm-target-spec.md leaks an example API key | mitigate | All examples use placeholder strings (`<your-key-here>`); lint already catches `sk-` patterns. |
| T-03-09-02 | Information Disclosure | .env.e2e accidentally committed | mitigate | Added to .gitignore; pre-commit hook (lefthook) already gitleaks-scans staged files. |
| T-03-09-03 | DoS | e2e-test run loops in CI burning quotas | mitigate | Manual / scheduled trigger only (not on every PR); operator-owned cadence. |

</threat_model>

<verification>
- Lint passes on both docs.
- Makefile help lists e2e-test.
- README has Provider Keys + Testing Modes sections.
</verification>

<success_criteria>
1. docs/litellm-target-spec.md ≥150 lines, all 8 H2 sections, English-only, lint-clean.
2. docs/litellm-mock-mode.md ≥40 lines, 5 H2 sections.
3. make e2e-test target works (refuses without .env.e2e).
4. README quickstart updated.
</success_criteria>

<output>
Create `.planning/phases/03-litellm-integration-bundled-oss-models/03-09-SUMMARY.md` with: docs ToCs, e2e-test invocation example, README diff summary.
</output>
