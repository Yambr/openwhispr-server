---
phase: 01-core-infra-multi-tenant-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - compose/otel-collector/config.yaml
  - compose/grafana/provisioning/datasources/tempo.yaml
  - compose/grafana/provisioning/datasources/loki.yaml
  - compose/grafana/provisioning/datasources/mimir.yaml
  - compose/traefik/traefik.yml
  - compose/traefik/dynamic.yml
  - compose/traefik/certs/.gitkeep
  - compose/pgbouncer/pgbouncer.ini
  - compose/pgbouncer/userlist.txt.example
  - Makefile
  - tests/infra/smoke.sh
  - tests/infra/wait-healthy.sh
  - tests/infra/otel-roundtrip.sh
  - tests/infra/loki-roundtrip.sh
  - tests/infra/mimir-roundtrip.sh
  - tests/infra/compose-schema.test.ts
autonomous: true
requirements: [DATA-06, PROVIDER-02]
tags: [infra, docker-compose, observability, traefik, minio, otel]
must_haves:
  truths:
    - "make up brings 10 services to Healthy state in <60s"
    - "Traefik routes api.localhost, grafana.localhost, minio-console.localhost over HTTPS"
    - "OTel Collector receives OTLP and forwards to Tempo, Loki, Mimir"
    - "Grafana datasources for Tempo, Loki, Mimir auto-provisioned"
    - "MinIO bundled and reachable on internal network with documented bucket-prefix convention"
    - "PgBouncer in transaction mode with default_pool_size=100, max_prepared_statements=200"
  artifacts:
    - path: "docker-compose.yml"
      provides: "10-service compose stack with healthchecks and profiles"
      contains: "edoburu/pgbouncer:1.23.1"
    - path: "compose/otel-collector/config.yaml"
      provides: "OTLP receiver -> Tempo/Loki/Mimir exporters"
      contains: "X-Scope-OrgID"
    - path: "compose/traefik/traefik.yml"
      provides: "File-provider Traefik 3 with 1h responding timeouts"
      contains: "readTimeout: 3700s"
    - path: "compose/pgbouncer/pgbouncer.ini"
      provides: "Transaction-mode pooler for 1000 concurrent"
      contains: "pool_mode = transaction"
  key_links:
    - from: "OTel Collector exporter"
      to: "Mimir /api/v1/push"
      via: "X-Scope-OrgID header"
      pattern: "X-Scope-OrgID"
    - from: "Traefik dynamic.yml api-svc"
      to: "api:3000"
      via: "Phase 0 placeholder Fastify"
      pattern: "url:.*http://api:3000"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Expand the Phase 0 placeholder docker-compose.yml into the full 10-service data plane (Postgres, PgBouncer, Valkey, MinIO, Traefik, OTel Collector, Loki, Tempo, Mimir, Grafana) with verified 2026-05 image pins, healthchecks on every service, named volumes for state, the `default` / `obs-only` / `db-only` Compose profiles, and the per-service config files mounted in (`compose/`). Ship `tests/infra/*.sh` validation scripts and a `compose-schema` Vitest unit test that lints the YAML structurally without booting Docker.

Purpose: Without the data plane up, nothing else in Phase 1 (migrations, RLS lint, property tests) can run. This plan is the load-bearing infra slice that Plans 03 and 05 sit on top of.

Output: A repository where `make up` boots the full data plane to Healthy in <60s on amd64 and arm64, and Plan 03's `make migrate` can connect to Postgres on the next wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-core-infra-multi-tenant-data/01-CONTEXT.md
@.planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-INFRA.md
@.planning/phases/01-core-infra-multi-tenant-data/01-VALIDATION.md
@docker-compose.yml
@Makefile

<interfaces>
Phase 0 Fastify placeholder (apps/api/src/index.ts) listens on :3000.
Traefik must route api.localhost -> http://api:3000.

Verified 2026-05-09 image pins (override D-02):
- postgres:17-alpine
- edoburu/pgbouncer:1.23.1     (NOT bitnami — bitnami free image retired 2025)
- valkey/valkey:8.1-alpine
- minio/minio:RELEASE.2026-03-25T00-00-00Z   (pin by digest; document coollabsio/minio fallback)
- traefik:v3.6
- otel/opentelemetry-collector-contrib:0.151.0
- grafana/grafana:11.6.0
- grafana/loki:3.5.0
- grafana/tempo:2.8.0
- grafana/mimir:2.16.0

PgBouncer mandatory config:
- pool_mode = transaction
- default_pool_size = 100
- max_prepared_statements = 200
- ignore_startup_parameters = extra_float_digits,search_path
- auth_type = scram-sha-256

OTel Collector pipelines: traces->Tempo (otlp), logs->Loki, metrics->Mimir (with X-Scope-OrgID: openwhispr).

Network: single bridge network `openwhispr_internal`. Only Traefik publishes 80/443/8080. No other host port-publish.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author compose-schema unit test + smoke shell scripts (Wave 0 substrate)</name>
  <files>tests/infra/compose-schema.test.ts, tests/infra/wait-healthy.sh, tests/infra/smoke.sh, tests/infra/otel-roundtrip.sh, tests/infra/loki-roundtrip.sh, tests/infra/mimir-roundtrip.sh</files>
  <read_first>
    - .planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-INFRA.md (§3, §10)
    - .planning/phases/01-core-infra-multi-tenant-data/01-VALIDATION.md (§Wave 0)
    - tests/self-tests/cyrillic-injection.test.ts (existing Vitest pattern)
    - vitest.config.ts (Phase 0 thresholds)
  </read_first>
  <behavior>
    - Test 1 (compose-schema): parses docker-compose.yml as YAML; asserts NO `version:` top-level key; asserts every of [postgres, pgbouncer, valkey, minio, traefik, otel-collector, loki, tempo, mimir, grafana] exists under `services:`; asserts each has an `image:` matching the verified pin AND a `healthcheck:` block AND is attached to network `openwhispr_internal`.
    - Test 2 (compose-schema): asserts only `traefik` has any host-`ports:` mapping; postgres/pgbouncer/valkey/minio/loki/tempo/mimir/grafana/otel-collector have NO host port-publish.
    - Test 3 (compose-schema): asserts `services.pgbouncer.depends_on.postgres.condition == "service_healthy"`.
    - Test 4 (compose-schema): asserts named volumes [postgres_data, valkey_data, minio_data, loki_data, tempo_data, mimir_data, grafana_data] declared.
    - Test 5 (compose-schema): asserts `services.otel-collector.environment` or its mounted config file references `X-Scope-OrgID`.
    - smoke.sh / wait-healthy.sh / *-roundtrip.sh: callable bash scripts (set -euo pipefail) that exit 0 on success, non-zero with a clear message otherwise. Use docker compose CLI + curl. wait-healthy.sh takes a timeout-in-seconds arg; polls `docker compose ps --format json` and asserts every service `.Health == "healthy"`. otel/loki/mimir-roundtrip.sh use a tiny Node OTLP emitter (10-line script via @opentelemetry/sdk-node — write inline using here-doc) since `telemetrygen` may not be on the runner; query Tempo/Loki/Mimir HTTP APIs and assert non-empty results.
  </behavior>
  <action>
    Write the Vitest test file at tests/infra/compose-schema.test.ts using the existing tests/self-tests/cyrillic-injection.test.ts shape (English-only, ASCII source, exact `describe`/`it` style, `import { describe, it, expect } from 'vitest'`). Parse YAML with `yaml` (already a transitive dep — verify or pnpm add -D yaml). The five tests above MUST FAIL initially because the new docker-compose.yml does not yet exist with these properties (TDD red).

    Then write the five bash scripts in tests/infra/. Each starts with `#!/usr/bin/env bash` + `set -euo pipefail`, comments are English-only, exit codes 0/1/2 (0=success, 1=assertion failed, 2=internal/dependency error). wait-healthy.sh signature: `wait-healthy.sh <timeout-seconds>`; default 60 if missing. smoke.sh runs `make up` then calls wait-healthy.sh 60 then `curl -fkS https://api.localhost/api/health` (Phase 0 health route through Traefik) — note: smoke.sh expects `api.localhost` to resolve; rely on `*.localhost` RFC 6761 reservation, document in script header that operators may need `/etc/hosts` if their resolver doesn't honor RFC 6761. Make scripts executable (chmod +x via filesystem perms or via `bash` invocation in CI).

    For OTLP emit, use this minimal Node script embedded as a heredoc inside otel-roundtrip.sh:
    ```
    node -e "
    const { NodeSDK } = require('@opentelemetry/sdk-node');
    const { OTLPTraceExporter } = require('@opentelemetry/exporter-trace-otlp-http');
    const { trace } = require('@opentelemetry/api');
    const sdk = new NodeSDK({ traceExporter: new OTLPTraceExporter({ url: 'http://localhost:4318/v1/traces' }) });
    sdk.start();
    trace.getTracer('roundtrip').startSpan('probe').end();
    setTimeout(() => sdk.shutdown().then(() => process.exit(0)), 2000);
    "
    ```
    Then `sleep 5 && curl -fsS 'http://localhost:3200/api/search?tags=service.name=unknown_service' | jq -e '.traces | length > 0'`. Add @opentelemetry/sdk-node + exporter-trace-otlp-http as devDependencies of the workspace root.

    English-only enforced (CI lint:english covers .ts; bash files end in .sh and `tools/lint-english.ts` glob already covers .sh per Phase 0 — verify).

    Per Conventional Commits: commit as `test(01-01): add compose-schema test and infra smoke scripts` (TDD red commit).
  </action>
  <verify>
    <automated>pnpm vitest run tests/infra/compose-schema.test.ts</automated>
    <expected>5 tests RED (docker-compose.yml does not yet declare the required services/healthchecks/volumes — that is Task 2). After this task, only the test file + bash scripts exist. The verify command MUST exit non-zero — that is the TDD red signal.</expected>
  </verify>
  <done>
    - tests/infra/compose-schema.test.ts exists, parses YAML, contains 5 test cases that all fail because the compose file isn't expanded yet
    - tests/infra/{smoke,wait-healthy,otel-roundtrip,loki-roundtrip,mimir-roundtrip}.sh exist, are executable bash, set -euo pipefail, English-only
    - `grep -q 'X-Scope-OrgID' tests/infra/mimir-roundtrip.sh` passes
    - `bash -n tests/infra/smoke.sh` exits 0 (syntax-OK)
    - Test file commit precedes implementation commit (TDD discipline; lefthook commit-msg gate inspects)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand docker-compose.yml + per-service config files (10 services, healthchecks, profiles)</name>
  <files>docker-compose.yml, .env.example, compose/otel-collector/config.yaml, compose/grafana/provisioning/datasources/tempo.yaml, compose/grafana/provisioning/datasources/loki.yaml, compose/grafana/provisioning/datasources/mimir.yaml, compose/traefik/traefik.yml, compose/traefik/dynamic.yml, compose/traefik/certs/.gitkeep, compose/pgbouncer/pgbouncer.ini, compose/pgbouncer/userlist.txt.example, Makefile</files>
  <read_first>
    - .planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-INFRA.md (§3 full template, §4 PgBouncer, §5 Traefik, §6 OTel, §7 Grafana, §8 MinIO, §10.5 success criteria)
    - .planning/phases/01-core-infra-multi-tenant-data/01-CONTEXT.md (D-01..D-06, D-29..D-31)
    - existing docker-compose.yml (Phase 0 placeholder — replace, do not delete the api service hook)
    - existing Makefile (Phase 0 stubs — extend, do not delete)
    - tests/infra/compose-schema.test.ts (Task 1 — written contract this task fulfills)
  </read_first>
  <action>
    Author docker-compose.yml from scratch with NO `version:` key (Compose Spec v2 — silently ignored anyway, but lint-clean). Top-level keys: `name: openwhispr`, `networks: { openwhispr_internal: { driver: bridge } }`, `volumes:` block listing the 7 named volumes from `must_haves`. Then 10 services using EXACTLY the verified image pins from `<interfaces>` above.

    Per-service config (literal, not "match research"):

    1. **postgres** (`postgres:17-alpine`, profiles `[default, db-only]`): env `POSTGRES_USER=${POSTGRES_OWNER_USER}` `POSTGRES_PASSWORD=${POSTGRES_OWNER_PASSWORD}` `POSTGRES_DB=openwhispr`; volume `postgres_data:/var/lib/postgresql/data`; mount `./packages/data/migrations/init:/docker-entrypoint-initdb.d:ro` (Plan 03 fills this dir); healthcheck `test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]`, interval 5s, timeout 3s, retries 10, start_period 10s.

    2. **pgbouncer** (`edoburu/pgbouncer:1.23.1`, profiles `[default, db-only]`): env `DB_HOST=postgres` `DB_PORT=5432` `DB_USER=${POSTGRES_APP_USER}` `DB_PASSWORD=${POSTGRES_APP_PASSWORD}` `DB_NAME=openwhispr` `POOL_MODE=transaction` `AUTH_TYPE=scram-sha-256` `MAX_CLIENT_CONN=2000` `DEFAULT_POOL_SIZE=100` `RESERVE_POOL_SIZE=5` `MAX_PREPARED_STATEMENTS=200` `IGNORE_STARTUP_PARAMETERS=extra_float_digits,search_path`; mount `./compose/pgbouncer/pgbouncer.ini:/etc/pgbouncer/pgbouncer.ini:ro`; depends_on postgres service_healthy; healthcheck `pg_isready -h 127.0.0.1 -p 5432`.

    3. **valkey** (`valkey/valkey:8.1-alpine`, profile `[default]`): command `valkey-server --requirepass $$VALKEY_PASSWORD`; env `VALKEY_PASSWORD=${VALKEY_PASSWORD}`; volume `valkey_data:/data`; healthcheck `valkey-cli -a $$VALKEY_PASSWORD ping | grep PONG`.

    4. **minio** (`minio/minio:RELEASE.2026-03-25T00-00-00Z`, profile `[default]`): command `server /data --console-address ":9001" --address ":9000"`; env `MINIO_ROOT_USER=${MINIO_ROOT_USER}` `MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}`; volume `minio_data:/data`; healthcheck `curl -fsS http://127.0.0.1:9000/minio/health/live`.

    5. **traefik** (`traefik:v3.6`, profile `[default]`): host-publish 80, 443, 8080 (only service with ports:); mount `./compose/traefik/traefik.yml:/etc/traefik/traefik.yml:ro`, `./compose/traefik/dynamic.yml:/etc/traefik/dynamic.yml:ro`, `./compose/traefik/certs:/certs:ro`; command `--ping=true`; healthcheck `traefik healthcheck --ping`.

    6. **otel-collector** (`otel/opentelemetry-collector-contrib:0.151.0`, profiles `[default, obs-only]`): mount `./compose/otel-collector/config.yaml:/etc/otelcol-contrib/config.yaml:ro`; command `--config=/etc/otelcol-contrib/config.yaml`; depends_on loki/tempo/mimir service_healthy; expose 4317/4318/13133; healthcheck `wget -qO- http://127.0.0.1:13133/`.

    7. **loki** (`grafana/loki:3.5.0`, profiles `[default, obs-only]`): volume `loki_data:/loki`; healthcheck `wget -qO- http://127.0.0.1:3100/ready`. (Anonymous mode — no auth_enabled flag.)

    8. **tempo** (`grafana/tempo:2.8.0`, profiles `[default, obs-only]`): volume `tempo_data:/tmp/tempo`; healthcheck `wget -qO- http://127.0.0.1:3200/ready`.

    9. **mimir** (`grafana/mimir:2.16.0`, profiles `[default, obs-only]`): volume `mimir_data:/data`; healthcheck `wget -qO- http://127.0.0.1:9009/ready`.

    10. **grafana** (`grafana/grafana:11.6.0`, profiles `[default, obs-only]`): env `GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}`; mount `./compose/grafana/provisioning:/etc/grafana/provisioning:ro`; volume `grafana_data:/var/lib/grafana`; depends_on loki/tempo/mimir service_healthy; healthcheck `wget -qO- http://127.0.0.1:3000/api/health | grep -q '"database":"ok"'`.

    Write `compose/otel-collector/config.yaml` with the literal pipeline from RESEARCH-INFRA §6.1: receivers.otlp(grpc:4317, http:4318); processors.batch(timeout:5s, send_batch_size:512); exporters.otlp/tempo(tempo:4317, tls.insecure:true), exporters.loki(http://loki:3100/loki/api/v1/push), exporters.prometheusremotewrite(http://mimir:9009/api/v1/push, headers.X-Scope-OrgID: openwhispr); extensions.health_check(0.0.0.0:13133); service.pipelines: traces/logs/metrics.

    Write `compose/grafana/provisioning/datasources/{tempo,loki,mimir}.yaml` exactly as RESEARCH-INFRA §7.2/§7.3 — tempo with tracesToLogsV2/tracesToMetrics/serviceMap UIDs, mimir with `httpHeaderName1: 'X-Scope-OrgID'` and `secureJsonData.httpHeaderValue1: 'openwhispr'`, loki anonymous.

    Write `compose/traefik/traefik.yml` (static config) per RESEARCH-INFRA §5.1 — api.dashboard:true, ping entryPoint, web/websecure entryPoints with respondingTimeouts.readTimeout: 3700s and writeTimeout: 3700s and idleTimeout: 180s, providers.file (filename: /etc/traefik/dynamic.yml, watch: true), NO `providers.docker`. Write `compose/traefik/dynamic.yml` (dynamic config) per §5.2 — three routers (api.localhost, grafana.localhost, minio-console.localhost) with TLS, three services (api -> http://api:3000, grafana -> http://grafana:3000, minio-console -> http://minio:9001), tls.certificates referencing /certs/local.crt and /certs/local.key. Add `compose/traefik/certs/.gitkeep` so the dir is committed empty (Plan 02's bootstrap.sh generates the cert).

    Write `compose/pgbouncer/pgbouncer.ini` exactly as RESEARCH-INFRA §4.4 (the `[databases]` and `[pgbouncer]` sections). Write `compose/pgbouncer/userlist.txt.example` as a comment-only template explaining bootstrap regenerates it.

    Write `.env.example` containing every key bootstrap.sh will populate (Plan 02 cross-ref):
    ```
    # OpenWhispr Server — environment template. Run tools/bootstrap.sh.
    POSTGRES_OWNER_USER=openwhispr_owner
    POSTGRES_OWNER_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    POSTGRES_APP_USER=openwhispr_app
    POSTGRES_APP_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    POSTGRES_DB=openwhispr
    PGBOUNCER_ADMIN_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    VALKEY_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    MINIO_ROOT_USER=openwhispr
    MINIO_ROOT_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    TRAEFIK_ADMIN_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    GRAFANA_ADMIN_PASSWORD=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    MASTER_KEK=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    BACKUP_AGE_IDENTITY=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    BETTER_AUTH_SECRET=PLACEHOLDER_BOOTSTRAP_WILL_REPLACE
    OPENWHISPR_KEY_PROVIDER=env
    DATABASE_URL=postgres://openwhispr_app:${POSTGRES_APP_PASSWORD}@pgbouncer:5432/openwhispr
    DATABASE_URL_OWNER=postgres://openwhispr_owner:${POSTGRES_OWNER_PASSWORD}@postgres:5432/openwhispr
    ```
    English-only comments. No real secret values.

    Extend Makefile with idempotent targets (do not remove Phase 0 stubs):
    ```
    .PHONY: up down logs ps restart
    up:
    	docker compose --profile default up -d
    down:
    	docker compose down
    logs:
    	docker compose logs -f
    ps:
    	docker compose ps
    restart:
    	$(MAKE) down && $(MAKE) up
    ```
    (Tabs are required in Makefile recipes — verify.)

    Per D-02 we override bitnami/pgbouncer:1.23 to edoburu/pgbouncer:1.23.1 (research-validated; bitnami free image retired 2025). Document this decision inline in docker-compose.yml as a comment on the pgbouncer image line.

    Per D-04 no bind mounts for state — config files mounted ro is OK, state is named volumes.

    Per CLAUDE.md "no separate projects": every new service goes into the SINGLE existing docker-compose.yml; do NOT create a second compose file.

    Commit (TDD green): `feat(01-01): expand compose stack to 10 services with healthchecks`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/infra/compose-schema.test.ts && grep -q 'edoburu/pgbouncer:1.23.1' docker-compose.yml && grep -q 'pool_mode = transaction' compose/pgbouncer/pgbouncer.ini && grep -q 'X-Scope-OrgID' compose/otel-collector/config.yaml && grep -q 'readTimeout: 3700s' compose/traefik/traefik.yml && grep -q 'MASTER_KEK=PLACEHOLDER' .env.example && test -f compose/grafana/provisioning/datasources/tempo.yaml && test -f compose/grafana/provisioning/datasources/loki.yaml && test -f compose/grafana/provisioning/datasources/mimir.yaml && bash -n tests/infra/smoke.sh</automated>
  </verify>
  <done>
    - All 5 compose-schema tests GREEN
    - 10 services declared with verified image pins, all have healthchecks, all attached to openwhispr_internal
    - Only traefik publishes host ports
    - Per-service config files exist with the exact directives above
    - .env.example contains every bootstrap-managed key
    - `make up` target present in Makefile
    - English-only lint passes (`pnpm lint:english`)
    - Commit message follows conventional-commits + lefthook commit-msg
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> Traefik :80/:443/:8080 | Only externally reachable surface; rest of stack is on internal network |
| Traefik -> service backends | Internal-network plaintext OK; TLS terminates at ingress |
| OTel Collector -> Mimir | X-Scope-OrgID header asserts tenant identity (single-tenant in v1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | postgres / pgbouncer / valkey / minio host-port leak | mitigate | No `ports:` block on stateful services; only Traefik publishes — compose-schema test 2 enforces |
| T-01-01-02 | Tampering | OTel Collector receiving unauthenticated OTLP from network | accept | Internal network only; not host-published; v1 dev compose; Phase 9 Helm adds mTLS |
| T-01-01-03 | Denial of Service | PgBouncer exhausting Postgres backend connections | mitigate | DEFAULT_POOL_SIZE=100 sized for SCALE-02 1000-concurrent target; RESERVE_POOL_SIZE=5 |
| T-01-01-04 | Spoofing | Traefik dashboard exposed on :8080 | accept | Dev-only; PROJECT documents prod must add basic-auth middleware (Phase 9) |
| T-01-01-05 | Information Disclosure | MinIO root credentials in environment | mitigate | Generated by bootstrap.sh (Plan 02); never committed; .env gitignored |
| T-01-01-06 | Elevation of Privilege | Loki/Tempo anonymous read enabled | accept | Single-tenant dev posture; auth_enabled remains false; documented in operations.md (Plan 06) |
</threat_model>

<verification>
1. `pnpm vitest run tests/infra/compose-schema.test.ts` — 5 tests green
2. `pnpm lint:english` — no Cyrillic/non-ASCII offenses
3. `bash -n tests/infra/smoke.sh tests/infra/wait-healthy.sh tests/infra/otel-roundtrip.sh tests/infra/loki-roundtrip.sh tests/infra/mimir-roundtrip.sh` — all syntax-OK
4. `grep -q 'edoburu/pgbouncer:1.23.1' docker-compose.yml` — pin override applied
5. Phase 0 coverage thresholds (85/80/80/85) not regressed: `pnpm test --coverage`
</verification>

<success_criteria>
- compose-schema.test.ts: 5/5 green
- 10 services with verified image pins + healthchecks present
- Profiles `default`, `obs-only`, `db-only` defined
- Only Traefik publishes host ports
- All Mimir traffic carries `X-Scope-OrgID: openwhispr`
- PgBouncer config: `pool_mode = transaction`, `default_pool_size = 100`, `max_prepared_statements = 200`, `ignore_startup_parameters = extra_float_digits,search_path`
- Traefik responding timeouts >= 3700s (covers 1h streaming sessions per SCALE-05)
- `.env.example` enumerates every bootstrap-managed key
- Two commits: `test(01-01)` red, `feat(01-01)` green; coverage non-regressed; English-only lint green
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infra-multi-tenant-data/01-01-SUMMARY.md` summarizing: services landed, image pins, profiles, key config decisions (edoburu override, X-Scope-OrgID, 3700s timeouts), files added/modified, follow-ups (mkcert vs openssl self-signed cert is Plan 02's decision; bash 3.2 macOS gotcha is Plan 02; observability dashboards deferred to Phase 6 per D-29).
</output>
