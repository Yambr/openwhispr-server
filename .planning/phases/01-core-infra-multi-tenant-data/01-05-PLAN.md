---
phase: 01-core-infra-multi-tenant-data
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - tools/lint-rls.ts
  - tools/lint-rls.test.ts
  - tests/self-tests/rls-introspection.test.ts
  - packages/data/src/__tests__/rls-property.test.ts
  - .github/workflows/ci.yml
  - scripts/branch-protection.json
  - Makefile
  - package.json
autonomous: true
requirements: [TEST-RLS-01, TEST-MIGRATION-01]
tags: [lint, rls-introspection, property-test, fast-check, ci, branch-protection]
must_haves:
  truths:
    - "tools/lint-rls.ts mirrors lint-english shape: shebang #!/usr/bin/env -S pnpm exec tsx, exit codes 0/1/2, file:line:col stderr-style diagnostics"
    - "lint-rls flags any tenant_id-bearing table with relrowsecurity=false, RLS-on table without policy, and policy not referencing app.tenant_id"
    - "rls-introspection.test.ts (self-test) injects bad_table without RLS into a fresh-migrated DB and asserts lint exits non-zero with bad_table in stderr"
    - "rls-property.test.ts uses fast-check 4.7.0 + @fast-check/vitest 0.4.1 to run 100 random tenant pairs through real PgBouncer; zero cross-tenant rows observed"
    - "GHA jobs lint-rls + test-migration land in ci.yml using services: postgres:17-alpine block, with healthcheck and SHA-pinned actions"
    - "scripts/branch-protection.json adds lint-rls + test-migration to required contexts"
  artifacts:
    - path: "tools/lint-rls.ts"
      provides: "Standalone tsx lint script introspecting pg_class.relrowsecurity and pg_policies"
      contains: "current_setting"
    - path: "packages/data/src/__tests__/rls-property.test.ts"
      provides: "fast-check 100-tenant-pair RLS property test through PgBouncer testcontainer"
      contains: "fc.uuid"
    - path: "tools/lint-rls.test.ts"
      provides: "Vitest unit test of lint-rls.ts using @testcontainers/postgresql"
      contains: "PostgreSqlContainer"
    - path: ".github/workflows/ci.yml"
      provides: "lint-rls + test-migration jobs added"
      contains: "lint-rls"
  key_links:
    - from: ".github/workflows/ci.yml lint-rls job"
      to: "tools/lint-rls.ts"
      via: "pnpm lint:rls in GHA services-block postgres"
      pattern: "lint:rls"
    - from: ".github/workflows/ci.yml test-migration job"
      to: "packages/data/migrations/0000_initial.sql"
      via: "forward+drop+forward schema diff + rollback"
      pattern: "pg_dump --schema-only"
    - from: "scripts/branch-protection.json"
      to: ".github/workflows/ci.yml job names"
      via: "harness-self-check / branch-protection-contexts test"
      pattern: "lint-rls"
---

<objective>
Lock down regression: ship the RLS-introspection lint (`tools/lint-rls.ts` + unit test) that fails any future migration adding an unguarded tenant_id-bearing table; the constitutional self-test (`tests/self-tests/rls-introspection.test.ts`) that asserts the lint actually fires; the TEST-RLS-01 property test (`packages/data/src/__tests__/rls-property.test.ts`) using fast-check 4.7.0 to fuzz 100 random tenant pairs through real PgBouncer transaction-mode; the two new GHA jobs (`lint-rls` + `test-migration`); and the branch-protection.json update so PRs can't bypass them.

Purpose: Phase 1 success criteria #2 ("an RLS-introspection lint blocks any future migration that adds an unguarded table") and #3 ("a property test runs random tenant pairs against every queryable model and observes zero cross-tenant reads or writes"). Without this plan, future phases can silently drop RLS coverage and nothing fires.

Output: A repository where adding a new tenant_id table without RLS fails CI; the property test drives 100 random pairs through PgBouncer with zero leakage; and branch protection rejects PRs that don't pass these checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-infra-multi-tenant-data/01-CONTEXT.md
@.planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-DB.md
@.planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-TOOLING.md
@.planning/phases/01-core-infra-multi-tenant-data/01-03-PLAN.md
@.planning/phases/01-core-infra-multi-tenant-data/01-04-PLAN.md
@tools/lint-english.ts
@tests/self-tests/cyrillic-injection.test.ts
@.github/workflows/ci.yml
@scripts/branch-protection.json

<interfaces>
Phase 0 patterns (constitutional):
- Standalone tsx scripts: shebang `#!/usr/bin/env -S pnpm exec tsx`, exit 0/1/2, English-only stderr diagnostics
- Self-tests live in tests/self-tests/, mirror cyrillic-injection.test.ts pattern
- All third-party GHA actions SHA-pinned with `# vX.Y.Z` trailer (Trivy 2026-03-19 incident response)
- Branch-protection-contexts self-test (existing, Phase 0) auto-detects drift between scripts/branch-protection.json and ci.yml job names
- harness-self-check job exists; new jobs land alongside

Plan 03 already shipped:
- packages/data/migrations/0000_initial.sql with FORCE RLS DDL
- packages/data/src/schema/* with TENANT_SCOPED_TABLES const

Plan 04 already shipped:
- packages/data/src/tenant-context.ts withTenant<T>
- PgBouncer-interleave smoke test pattern (re-use the testcontainer scaffold)

Lint queries (from RESEARCH-TOOLING §"Pattern 1"):
- Q_NO_RLS: tables with tenant_id column AND relrowsecurity=false
- Q_RLS_NO_POLICY: relrowsecurity=true AND no entry in pg_policies
- Q_BAD_POLICY: pg_policies row whose `qual` does NOT contain 'app.tenant_id'

Property test setup (from RESEARCH-DB §"TEST-RLS-01 property test (concrete)"):
- @fast-check/vitest 0.4.1 (pinned; works with Vitest 4)
- fast-check 4.7.0
- testcontainers Postgres + edoburu/pgbouncer:1.23.1 sidecar (re-use Plan 04's pattern)
- 100 numRuns, fc.uuid({version:4}) for tenants, fc.array(fc.emailAddress()) for inserts
- Auto-discovery from TENANT_SCOPED_TABLES (Plan 03 export); v1 covers 'users' explicitly + smoke probes for sessions/audit_log/usage_ledger

GHA service-block pattern (from RESEARCH-TOOLING §"Example: .github/workflows/ci.yml additions"):
- runs-on: ubuntu-24.04
- services.postgres: image: postgres:17-alpine, env, ports 5432:5432, options with --health-cmd pg_isready
- step-security/harden-runner@<sha> # v2.x — re-use the SHA already pinned in Phase 0 ci.yml
- pnpm/action-setup@v4 with version: 11.0.8 (matches Phase 0)
- actions/setup-node@v5 with node-version: '24', cache: 'pnpm'
- actions/checkout@v5

For the test-migration job: connect directly to Postgres on 5432 (NOT through PgBouncer — RESEARCH-TOOLING anti-pattern: migrations need session features). Skip PgBouncer entirely in this job.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools/lint-rls.ts + tools/lint-rls.test.ts + tests/self-tests/rls-introspection.test.ts</name>
  <files>tools/lint-rls.ts, tools/lint-rls.test.ts, tests/self-tests/rls-introspection.test.ts, Makefile, package.json</files>
  <read_first>
    - tools/lint-english.ts (exact pattern shape — shebang, exit codes, stderr style)
    - tests/self-tests/cyrillic-injection.test.ts (self-test pattern)
    - .planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-TOOLING.md (§"Pattern 1: Standalone tsx-runnable lint script", §"Example: tools/lint-rls.test.ts skeleton")
    - packages/data/migrations/0000_initial.sql (the schema lint runs against)
  </read_first>
  <behavior>
    - lint-rls.test.ts (unit, testcontainers Postgres):
      • Test 1: clean schema (apply 0000_initial.sql) → lint exits 0
      • Test 2: inject `CREATE TABLE bad_table (id uuid, tenant_id uuid)` (no RLS) → lint exits 1, stderr contains 'bad_table' and 'has tenant_id column but RLS is disabled'
      • Test 3: inject table with ENABLE RLS but no policy → exits 1, stderr matches 'no policy attached'
      • Test 4: inject policy referencing wrong setting (e.g., USING (id IS NOT NULL)) → exits 1, stderr matches 'does not reference app.tenant_id'
    - rls-introspection.test.ts (self-test, mirrors cyrillic-injection):
      • Test: as a constitutional gate, run lint-rls against a temp postgres with an injected bad migration; assert non-zero exit + 'bad_table' in stderr. Spawns its own PostgreSqlContainer (full integration, not a unit test).
  </behavior>
  <action>
    Write `tools/lint-rls.ts` per RESEARCH-TOOLING §"Pattern 1" verbatim. Header docstring (English-only) lists the three failure modes and the three Q_* SQL queries. Use `pg` (not Drizzle — keep the lint dep-light). Connection from DATABASE_URL env. The CRITICAL warning in the header: `DATABASE_URL must point to Postgres directly, not through PgBouncer` (RESEARCH-TOOLING Pitfall 3).

    Refinement on the Q_BAD_POLICY query: pg_policies has `qual` (USING expr text) and `with_check` (WITH CHECK expr text). Tighten the query to:
    ```sql
    SELECT tablename, policyname, qual, with_check
    FROM pg_policies
    WHERE schemaname = 'public'
      AND ((qual IS NULL OR qual NOT LIKE '%app.tenant_id%')
        OR (with_check IS NOT NULL AND with_check NOT LIKE '%app.tenant_id%'));
    ```
    (The 0000_initial.sql policies set both USING and WITH CHECK to the same predicate, so this is the right shape.) Skip false positives on the `_meta` schema (where __drizzle_migrations lives, no tenant_id column).

    Write `tools/lint-rls.test.ts` per RESEARCH-TOOLING §"Example skeleton" — uses @testcontainers/postgresql (already added by Plan 03's package.json). Apply 0000_initial.sql at setup time using a connection-uri owner pool. Each test spawns its own connection or shares the container.

    Write `tests/self-tests/rls-introspection.test.ts` mirroring `tests/self-tests/cyrillic-injection.test.ts`'s shape but spawning a real Postgres testcontainer; inject `CREATE TABLE bad_table (id uuid PRIMARY KEY, tenant_id uuid NOT NULL)` post-migration, run lint-rls, assert exit=1 and stderr contains 'bad_table'. This is the CI-blocking gate that proves the lint constitutional rule is wired.

    Extend Makefile with:
    ```
    .PHONY: lint-rls
    lint-rls:
    	pnpm exec tsx tools/lint-rls.ts
    ```
    Add to root package.json scripts: `"lint:rls": "tsx tools/lint-rls.ts"`.

    Per CLAUDE.md "не использовать моки": real Postgres testcontainer; no pg-mem; no stubs. Per "не упрощать": all 3 failure modes covered, not just "missing RLS".

    Commit in TDD discipline: red commit (`test(01-05): add lint-rls and rls-introspection self-test`) then green (`feat(01-05): tools/lint-rls.ts RLS-introspection lint`).
  </action>
  <verify>
    <automated>pnpm vitest run tools/lint-rls.test.ts tests/self-tests/rls-introspection.test.ts && grep -q 'app.tenant_id' tools/lint-rls.ts && grep -q '#!/usr/bin/env -S pnpm exec tsx' tools/lint-rls.ts && pnpm lint:rls --help 2>&1 | grep -qi 'database_url\|usage' || true</automated>
  </verify>
  <done>
    - 4 lint-rls tests + 1 self-test all green
    - tools/lint-rls.ts exists with shebang, English-only docstring, the three Q_* queries, exit codes 0/1/2
    - Makefile + package.json `lint:rls` wired
    - English-only lint green; coverage non-regressed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST-RLS-01 property test (fast-check 100 tenant pairs through PgBouncer)</name>
  <files>packages/data/src/__tests__/rls-property.test.ts, packages/data/package.json</files>
  <read_first>
    - .planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-DB.md (§"TEST-RLS-01 property test (concrete)" — copy literally with edoburu pin override)
    - packages/data/src/__tests__/pgbouncer-interleave.test.ts (Plan 04 — re-use the container setup helper)
    - packages/data/src/tenant-context.ts (Plan 04 — withTenant)
    - packages/data/src/schema/index.ts (TENANT_SCOPED_TABLES)
  </read_first>
  <behavior>
    - rls-property.test.ts:
      • beforeAll: spin testcontainer Postgres + edoburu/pgbouncer:1.23.1 sidecar; apply 0000_initial.sql via owner; CREATE ROLE openwhispr_app; appPool with max:5 via PgBouncer.
      • Property 1 (numRuns: 100): for arbitrary tenantA, tenantB (distinct UUIDv4), and arbitrary array of emails, seed tenants A and B as owner, insert users under tenant A, read under tenant B → expect zero rows from tenant A; UPDATE under tenant B → expect zero rows of tenantA touched; DELETE under tenant B → zero of tenantA touched.
      • Property 2 (numRuns: 50, smaller sessions table): same shape but exercises sessions table — insert sessions under A with random expiresAt; under B, SELECT/UPDATE/DELETE never sees A.
      • Property 3 (numRuns: 30): exercise audit_log inserts (action+payload) — no cross-tenant.
      • Property 4 (numRuns: 30): exercise usage_ledger with random request_id (per-tenant unique-namespacing handled by RLS, not request_id) — no cross-tenant.
      • Test 5 (smoke, not property): SELECT * FROM users with NO withTenant call → returns 0 rows (fail-closed proof).
  </behavior>
  <action>
    Add to `packages/data/package.json` devDependencies (Plan 03 already added testcontainers):
    ```json
    "fast-check": "4.7.0",
    "@fast-check/vitest": "0.4.1"
    ```
    Run `pnpm install --filter @openwhispr/data`.

    Write `packages/data/src/__tests__/rls-property.test.ts` per RESEARCH-DB §"TEST-RLS-01 property test (concrete)". Two changes from the RESEARCH stub:
    1. Replace `bitnami/pgbouncer:1.23` with `edoburu/pgbouncer:1.23.1` and use the env-var shape from RESEARCH-INFRA §3.3 (DB_HOST=postgres, DB_USER=openwhispr_app, etc.) — bitnami env vars don't apply to edoburu.
    2. The `seedTenants` helper inserts both tenant rows via the OWNER pool (un-RLS'd tenants table accepts owner writes).

    Each property uses `test.prop([...generators], { numRuns: N })` from `@fast-check/vitest`. Use `fc.pre(tenantA !== tenantB)` to discard duplicate-pair runs (UUIDv4 collisions effectively zero but cheap to guard).

    For auto-discovery beyond `users`: import TENANT_SCOPED_TABLES from schema/index.ts; for each table, run a simplified property: insert one synthetic row via owner under tenantA's tenant_id directly, then read under tenantB via withTenant + tx.execute(sql\`SELECT * FROM ${sql.identifier(tableName)}\`); assert empty.

    Per CLAUDE.md "no mocks": real Postgres + real PgBouncer; per "не упрощать": full property suite across all 4 tenant-scoped tables, not just users.

    Test runtime caveat: 100 + 50 + 30 + 30 = 210 property runs may exceed default Vitest timeout. Set `testTimeout: 180_000` at file level (3min). beforeAll/afterAll already have 60s timeouts.

    Commit (TDD red->green): `test(01-05): TEST-RLS-01 property test 100 random tenant pairs through PgBouncer`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/data/src/__tests__/rls-property.test.ts && grep -q 'fc.uuid' packages/data/src/__tests__/rls-property.test.ts && grep -q 'edoburu/pgbouncer:1.23.1' packages/data/src/__tests__/rls-property.test.ts && grep -q 'numRuns: 100' packages/data/src/__tests__/rls-property.test.ts && grep -q '"fast-check": "4.7.0"' packages/data/package.json</automated>
  </verify>
  <done>
    - All properties green; 210 random runs total; zero cross-tenant rows observed
    - fast-check 4.7.0 + @fast-check/vitest 0.4.1 pinned in packages/data/package.json
    - edoburu/pgbouncer:1.23.1 used (consistent with Plan 01-01 + Plan 04)
    - Coverage non-regressed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GHA jobs lint-rls + test-migration + branch-protection.json update</name>
  <files>.github/workflows/ci.yml, scripts/branch-protection.json</files>
  <read_first>
    - .github/workflows/ci.yml (Phase 0 — preserve existing jobs; harden-runner, pnpm/action-setup, setup-node, checkout SHA pins to re-use)
    - scripts/branch-protection.json (Phase 0 — append, do not replace)
    - .planning/phases/01-core-infra-multi-tenant-data/01-RESEARCH-TOOLING.md (§"Example: .github/workflows/ci.yml additions" — use literally with SHA pins from Phase 0 ci.yml)
    - tests/self-tests/branch-protection-contexts.test.ts (Phase 0 — auto-detects drift)
  </read_first>
  <behavior>
    No new test files in this task. The contracts are:
    - Phase 0 self-test `tests/self-tests/branch-protection-contexts.test.ts` already runs against any change to scripts/branch-protection.json or ci.yml; if a new context appears in one but not the other, that test fails. Adding to both keeps it green.
    - Existing Phase 0 `harness-self-check` job will exercise the new lint-rls and test-migration jobs as part of CI (they show up as new required contexts).
  </behavior>
  <action>
    Re-read the existing `.github/workflows/ci.yml`. Note the SHA-pinned action versions for `step-security/harden-runner`, `actions/checkout`, `pnpm/action-setup`, `actions/setup-node`. Use the EXACT SAME pinned SHAs in the new jobs (no version drift across the file).

    Append two new jobs to ci.yml. Use the literal pattern from RESEARCH-TOOLING §"Example: .github/workflows/ci.yml additions" with these adjustments:

    - Action SHA pins: copy from existing jobs in the same file. Example: `step-security/harden-runner@<sha>  # v2.x.y` (the version trailer comment is constitutional per Phase 0 PR-#1 Trivy lessons).
    - For `lint-rls` job: env vars POSTGRES_OWNER_PASSWORD + POSTGRES_APP_PASSWORD set to `test` strings (acceptable in CI; not deny-listed values per `tools/bootstrap/default-secrets.txt`); first run a setup SQL block via `psql` to create the openwhispr_owner BYPASSRLS role + openwhispr_app role + alter database owner — mirroring init/00-roles.sql contents. Then `pnpm migrate` (uses DATABASE_URL_OWNER pointing at the postgres service). Then `pnpm lint:rls` with DATABASE_URL_OWNER as the introspection target (lint connects directly, NOT via PgBouncer per RESEARCH-TOOLING Pitfall 3 — and PgBouncer isn't in CI services anyway).
    - For `test-migration` job: same role setup; forward apply; pg_dump --schema-only > /tmp/schema-pre.sql; DROP DATABASE + CREATE DATABASE (need a separate "postgres" maintenance DB connection — connect to default `postgres` DB via `PGDATABASE=postgres`); re-run pnpm migrate; pg_dump again; `diff -u /tmp/schema-pre.sql /tmp/schema-post.sql` (must be empty). Then test rollback: `pnpm migrate:rollback` exits 0.

    Both jobs `runs-on: ubuntu-24.04`. Both depend implicitly on the same setup steps (harden-runner audit, checkout, pnpm setup, node setup, install --frozen-lockfile).

    Update `scripts/branch-protection.json`: append `"lint-rls"` and `"test-migration"` to `required_status_checks.contexts` (the existing `branch-protection-contexts` self-test catches drift, so do NOT also add to the test fixture — let the Phase 0 self-test fail on un-synced state and force this update).

    Per CLAUDE.md "не упрощать": full forward+drop+forward+rollback in CI, not "v1 forward only".

    Commit: `ci(01-05): add lint-rls and test-migration GHA jobs + branch protection`.
  </action>
  <verify>
    <automated>grep -q 'lint-rls:' .github/workflows/ci.yml && grep -q 'test-migration:' .github/workflows/ci.yml && grep -q '"lint-rls"' scripts/branch-protection.json && grep -q '"test-migration"' scripts/branch-protection.json && grep -q 'postgres:17-alpine' .github/workflows/ci.yml && grep -q 'pg_isready' .github/workflows/ci.yml && grep -q 'pg_dump --schema-only' .github/workflows/ci.yml && pnpm vitest run tests/self-tests/branch-protection-contexts.test.ts</automated>
  </verify>
  <done>
    - lint-rls and test-migration jobs in ci.yml; service block postgres:17-alpine with healthcheck options
    - Both required contexts in scripts/branch-protection.json
    - Phase 0 branch-protection-contexts self-test green (no drift)
    - All third-party action SHAs match existing pins; version-tag comments present
    - English-only lint green
    - On a real PR (manual smoke after merge), both jobs run and pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR -> CI lint-rls + test-migration | Mandatory checks gate merge to main |
| Lint script -> Postgres metadata views | Read-only introspection; no DML/DDL |
| Property test -> real PgBouncer transaction-mode | Stress test: small pool forces physical-connection reuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Future migration adds tenant_id table without RLS | mitigate | lint-rls + GHA job + branch protection — three layers; rls-introspection self-test asserts the lint actually fires |
| T-01-05-02 | Tampering | Lint script bypassed by adding _meta-schema tables | accept | Lint excludes _meta schema by design (drizzle migrations table lives there); RESEARCH-DB Pitfall 8 |
| T-01-05-03 | Information Disclosure | Property test discovers actual cross-tenant leak | mitigate | Test fails CI; fix-forward; this is the test EXISTING is the mitigation |
| T-01-05-04 | Tampering | Operator drops a required status check from branch protection | mitigate | Phase 0 branch-protection-contexts self-test fails CI on JSON-vs-workflow drift |
| T-01-05-05 | Spoofing | Third-party GHA action compromised between PRs | mitigate | SHA-pinned actions; version-trailer comments enable audit; matches Phase 0 hardening |
| T-01-05-06 | Denial of Service | Property test runtime explodes in CI | mitigate | testTimeout 180s; numRuns budgeted (100/50/30/30); container reuse if vitest workers cooperate |
</threat_model>

<verification>
1. `pnpm vitest run tools/lint-rls.test.ts tests/self-tests/rls-introspection.test.ts packages/data/src/__tests__/rls-property.test.ts` — all green
2. `grep -q 'lint-rls:' .github/workflows/ci.yml` and `grep -q 'test-migration:' .github/workflows/ci.yml`
3. `grep -q '"lint-rls"' scripts/branch-protection.json` and `grep -q '"test-migration"' scripts/branch-protection.json`
4. Phase 0 branch-protection-contexts self-test green
5. English-only lint, coverage 85/80/80/85 non-regressed
6. Conventional Commits + lefthook gates green for all commits
</verification>

<success_criteria>
- 5 lint-rls/self-test cases green
- 4 property-test properties green (210+ runs, zero cross-tenant rows)
- 2 new GHA jobs land with full forward+drop+forward+rollback for test-migration and full lint-rls drive against real Postgres
- branch-protection.json updated; Phase 0 drift self-test green
- All third-party actions SHA-pinned; English-only; TDD red->green commits visible
</success_criteria>

<output>
Create `.planning/phases/01-core-infra-multi-tenant-data/01-05-SUMMARY.md` summarizing: lint script's three queries; self-test that proves the lint fires; property-test runtime budget (~3min); GHA service-block pattern (postgres only, no PgBouncer in migration jobs); branch-protection drift detection mechanism; follow-ups (Phase 6+ extends lint to also forbid bare `SET app.tenant_id` outside withTenant).
</output>
