---
phase: 02.10
status: locked
date: 2026-05-10
mode: yolo-mechanical
---

# Phase 02.10 — CONTEXT

## Goal

Close cascade tail Group A: `packages/contract-tests/src/helpers/sign-in-fixture.ts` POSTs to `/api/auth/sign-in/email` without an `Origin` header → Better Auth's CSRF gate rejects with `403 MISSING_OR_NULL_ORIGIN` → 4 contract tests RED. Mirror the proven pattern from `packages/data/src/seed/conformance.ts:46-58` (Phase 02.3).

## Locked Decisions

### D-01 — Forward Origin: AUTH_URL on signInFixture POST

`packages/contract-tests/src/helpers/sign-in-fixture.ts:33` adds `origin: AUTH_URL` to the request headers (mirrors the seed pattern). One-line change.

### D-02 — TDD

1. Failing test FIRST: `packages/contract-tests/src/helpers/__tests__/sign-in-fixture-origin.test.ts` — mock fetch, capture request init, assert headers contain `origin` with the configured AUTH_URL. RED before fix.
2. Reverse-patch: revert origin line → test red.

### D-03 — Out of scope

- Other fetch sites in contract-tests/helpers — investigate only if E2E surfaces another MISSING_OR_NULL_ORIGIN
- Refactor to a shared origin-aware fetch wrapper — defer

## Files

Production: `packages/contract-tests/src/helpers/sign-in-fixture.ts`
Tests: `packages/contract-tests/src/helpers/__tests__/sign-in-fixture-origin.test.ts`

## Acceptance gate

1. New test RED before fix, GREEN after
2. Re-run contract-test E2E: 4 Group A tests advance past 403; new failures (if any) classified for Phase 02.11+
3. Single atomic commit
