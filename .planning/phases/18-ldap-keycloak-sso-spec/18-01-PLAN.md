---
phase: 18-ldap-keycloak-sso-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md
  - docs/adrs/0012-ldap-via-keycloak.md
  - tests/e2e-cjm/features/sso/keycloak-oidc.feature
  - tests/e2e-cjm/steps/sso.steps.ts
  - compose/test/keycloak.yml
  - compose/test/keycloak/.gitkeep
  - docs/customer-journeys.md
autonomous: true
requirements:
  - SSO-01
  - SSO-02
  - SSO-03
  - SSO-04
  - SSO-05
must_haves:
  truths:
    # Wave 1 — ROADMAP cleanup (no SSO req closed; cleanup only)
    - "`.planning/ROADMAP.md` Phase 18 `**Plans**:` block reads exactly `**Plans**: 1 plan` followed by a single `18-01-PLAN.md` bullet; NO `17-01-PLAN.md`/`17-02-PLAN.md`/`17-03-PLAN.md` remains under the Phase 18 entry."
    - "Wave 1 commit lands ONLY `.planning/ROADMAP.md` (surgical replace at the `**Plans**: 3 plans` anchor under `### Phase 18:` — anchor on literal text, NOT a hard line number)."
    - "Wave 1 commit subject: `docs(18-01): correct phase-18 plans-list + adr-0012 slot note`; commit body notes ADR-0012 slot reservation (docs/adrs sequence 0011 → 0013 from Phase 15 FSL; 0012 free)."
    # Wave 2 — SPEC (closes SSO-01 + SSO-02)
    - "`.planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md` exists, ≤200 lines (PITFALLS §14 hard cap; budget ~175L + 25L slack)."
    - "SPEC line 1 is `<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->`; frontmatter declares `phase`, `type: spec`, `status: locked`, `requirements: [SSO-01, SSO-02]` (NOT SSO-03/04/05)."
    - "SPEC contains H2 sections in order: `## Purpose`, `## Goal`, `## In Scope`, `## Out of Scope`, `## Decision`, `## Worked example`, `## Env vars (loud-fail BYOK)`, `## Better Auth extension points`, `## Failure modes`, `## Structured log events`, `## Open questions for v3 plan`, `## Operator survey results`, `## References`."
    - "`## Decision` cites ADR-0009 by path and `apps/api/src/auth.ts:39,209` as proof the `genericOAuth` surface is already wired; rejects option (b) with the 4 PITFALLS rationale points from CONTEXT.md L82-87."
    - "`## Env vars (loud-fail BYOK)` is a 7-row table with rows `OIDC_TENANT_CLAIM`, `OIDC_TENANT_MAPPING`, `OIDC_GROUP_CLAIM`, `OIDC_ROLE_MAPPING`, `OIDC_ROLE_PRIORITY`, `OIDC_DEFAULT_ROLE`, `OIDC_REVOCATION_MODE`; table-footer commentary names v3 call site `lib/oidc-jit-config.ts` and references existing `lib/oidc-providers.ts` triplet validation as the shape; SPEC implements NO code."
    - "`## Better Auth extension points` is a 5-row table naming `genericOAuth.mapProfileToUser`, `databaseHooks.user.create.before`, `databaseHooks.user.update.before`, `databaseHooks.user.create.after`/`update.after`, and `account.user_id` FK."
    - "`## Failure modes` is a 7-row table with rejection codes `403 forbidden_missing_tenant_claim`, `403 forbidden_unknown_tenant`, `403 forbidden_no_role_mapping`, deterministic priority resolution, `downgrade_to_default` + audit, `403 forbidden_tenant_mismatch`, `400 invalid_oidc_profile`."
    - "`## Structured log events` lists exactly 3 names: `sso.jit.user.created`, `sso.jit.role.updated`, `sso.jit.rejected`."
    - "`## Worked example` shows Keycloak realm `acme` id_token claims (sub/email/name/groups/iss) + env config from CONTEXT.md L138-143 + resolution (tenant `acme`, role `member`, audit `sso.jit.user.created`)."
    - "`## Operator survey results` is a 1-line forward-ref to ADR-0012 § Operator demand (NOT duplicated)."
    - "SPEC is English-only (CLAUDE.md hard rule)."
    - "Wave 2 commit subject: `docs(18-01): SPEC-ldap-keycloak.md (option-a OIDC frontend + JIT spec, SSO-01 + SSO-02)`."
    # Wave 3 — ADR-0012 (closes SSO-04 + SSO-05)
    - "`docs/adrs/0012-ldap-via-keycloak.md` exists; lines 1-5 are HTML SPDX comment block + `<!-- REUSE-IgnoreStart -->` (mirrors `docs/adrs/0013-fsl-relicense.md:1-5`); file ends with `<!-- REUSE-IgnoreEnd -->` (mirrors 0013:245)."
    - "ADR-0012 status block: `**Status:** accepted`, `**Date:** 2026-05-15`, `**Phase:** 18 — LDAP / Keycloak SSO (SPEC + ADR only)`. NO `Tags:` field (no in-tree precedent)."
    - "ADR-0012 H2 sections in order: `## Context`, `## Decision`, `## Consequences`, `## Operator demand (informal survey, anonymised)` (NOVEL), `## Alternatives considered`, `## Open questions for v3 plan` (NOVEL), `## References`."
    - "`## Decision` drops in CONTEXT.md Q1 finding verbatim (option a chosen; option b rejected with 4 PITFALLS rationale bullets §14/Integration Gotchas/Performance Traps/Recovery Strategies); cites `apps/api/src/auth.ts:209`."
    - "`## Operator demand (informal survey, anonymised)` carries 3-5 anonymised operator notes (precedent for novel mid-template sections: ADR-0013:157 `## Recovery` + 0013:193 `## Retroactive consent`); satisfies PITFALLS §14 line 444 mandate."
    - "`## Open questions for v3 plan` lists exactly 5 items per CONTEXT.md Q5 L213: Keycloak version pin (26.0), `OIDC_ROLE_MAPPING` schema lock, JIT auto-create policy (reject vs auto-onboard), LDAP fixture scope (OpenLDAP locked; 389DS/AD deferred), Authentik documented option (no v3 fixture)."
    - "`## Alternatives considered` uses `| Alternative | Why rejected |` table shape (precedent 0013-fsl-relicense.md:219)."
    - "`## References` cites ADR-0009 as `**Predecessor ADR (extended, not superseded):** [ADR-0009](./0009-better-auth-email-password-and-oidc-plugin.md)` (verbatim shape from 0013:240); cites Wave-2 SPEC; cites PITFALLS §14."
    - "ADR-0012 is English-only."
    - "Wave 3 commit subject: `docs(18-01): ADR-0012 ldap-via-keycloak + operator-demand survey (SSO-04 + SSO-05)`; body notes the novel `## Operator demand` section name has no exact precedent so verifier does NOT bounce on off-template detection."
    # Wave 4 — ATOMIC: Gherkin + step defs + fixture + customer-journeys (closes SSO-03)
    - "`tests/e2e-cjm/features/sso/keycloak-oidc.feature` exists; line 1 is `# SPDX-License-Identifier: FSL-1.1-ALv2`; header block lists all 6 `@cjm-sso-N.M` anchors (mirrors `phase17-tls.feature:1-16`); feature-level tag line is `@phase-18 @sso`."
    - "Feature file contains exactly 6 scenarios, each tagged `@cjm-sso-1.N @expected-red @after-phase-19 @after-keycloak-up` for N ∈ {1..6}; titles per CONTEXT.md Q3 L168-176 (JIT create, returning re-sync, role downgrade, tenant from email domain, RLS isolation, loud-fail bad realm)."
    - "`tests/e2e-cjm/steps/sso.steps.ts` exists; line 1 is `// SPDX-License-Identifier: FSL-1.1-ALv2`; imports `Given/When/Then` from `../support/world`; mirrors `locale.steps.ts:11-49` shape (Map<string, ScenarioState>); every step body throws `new Error(\"keycloak SSO ships in Phase 19 — @cjm-sso-N.M stays @expected-red\")`; every step text used in the .feature has a matching impl (cucumber-tsx parse-time requirement)."
    - "`compose/test/keycloak.yml` exists; line 1 is `# SPDX-License-Identifier: FSL-1.1-ALv2`; service `keycloak` uses `image: quay.io/keycloak/keycloak:26.0` (exact tag pin); env `KC_BOOTSTRAP_ADMIN_USERNAME` + `KC_BOOTSTRAP_ADMIN_PASSWORD` (KC 25+ canonical); `command: [\"start-dev\", \"--import-realm\"]`; healthcheck wget `http://localhost:9000/health/ready` (interval 5s timeout 3s retries 5 start_period 5s); profile `sso`; volume mount `./compose/test/keycloak/:/opt/keycloak/data/import:ro`; networks `openwhispr_internal`."
    - "`compose/test/keycloak/.gitkeep` exists (empty file) so the empty realm-import dir survives `git add`; `compose/test/` is a NOVEL directory in the repo."
    - "`docs/customer-journeys.md` has a new H2 section appended at EOF: `## 9. SSO via Keycloak (after-phase-19)` followed by exactly 6 `### @cjm-sso-1.N …` subsection anchors (N ∈ {1..6}); each subsection carries a body paragraph + `- Backend error branches:` sub-list + `- Silent-failure modes:` sub-list (mirrors customer-journeys.md:29-50,194-232 shape)."
    - "Every `### @cjm-sso-N.M` heading matches `tools/lint-cjm-doc.ts:172-195` Mode-2 anchor-pair predicate (exact `### @cjm-sso-N.M ` prefix + descriptive title); pairs 1:1 with each scenario tag."
    - "`pnpm tsx tools/lint-cjm-doc.ts` exits 0 post-Wave-4 (Mode-1 ≥2 anchors per major `sso-1` → 6 ✓; Mode-2 6 feature-tags ↔ 6 doc-anchors atomic ✓; Mode-3 `@expected-red` paired with `@after-phase-19` for all 6 ✓)."
    - "Default `make e2e-cjm` lane (Makefile:466 `--grep-invert \"@expected-red\"`) still passes — all 6 new scenarios skipped by existing tag filter; NO Makefile edits; NO playwright.config.ts edits."
    - "Wave 4 commit is ATOMIC — all 5 paths (`keycloak-oidc.feature` + `sso.steps.ts` + `keycloak.yml` + `.gitkeep` + `customer-journeys.md`) staged together in ONE commit; splitting red-lights Mode-2 linter (orphan tags)."
    - "Wave 4 commit subject: `feat(18-01): SSO red Gherkin + keycloak fixture stub + customer-journeys rows (SSO-03)` (use `feat` because `compose/test/keycloak.yml` introduces a new profile-gated fixture service). Body notes: (a) novel `compose/test/` directory; (b) `@after-phase-19` ahead-of-ROADMAP; (c) `SSO=1` Makefile switch deferred to Phase 19."
    # Cross-wave invariants
    - "ZERO `--no-verify` across all 4 commits — touched globs are `.md` / `.feature` / `.ts` / `.yml` / `.gitkeep`; biome runs on `.ts` only and `sso.steps.ts` mirrors biome-clean `locale.steps.ts`. HALT-and-escalate if lefthook fires unexpectedly."
    - "NO production code; NO Better Auth changes; NO Makefile changes; NO migrations; NO compose service additions beyond the profile-gated Keycloak fixture stub. All 4 commits are atomic and English-only."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 18 `**Plans**:` block surgical replace — count 3→1, plans-list 17-01/02/03 → 18-01."
    - path: ".planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md"
      provides: "Option (a) vs (b) decision matrix; JIT user-provisioning spec; 7-var env table; 5 Better Auth extension points; 7 failure modes; 3 log events; worked example; ≤200 lines."
      contains: "## Decision, ## Worked example, ## Env vars, ## Better Auth extension points, ## Failure modes, ## Structured log events, ## Open questions for v3 plan, ## Operator survey results, ## References"
    - path: "docs/adrs/0012-ldap-via-keycloak.md"
      provides: "Status=accepted ADR; option-(a) decision + 4 PITFALLS rejection points for option (b); novel `## Operator demand` survey; novel `## Open questions for v3 plan`; predecessor cross-ref to ADR-0009."
      contains: "## Context, ## Decision, ## Consequences, ## Operator demand, ## Alternatives considered, ## Open questions for v3 plan, ## References"
    - path: "tests/e2e-cjm/features/sso/keycloak-oidc.feature"
      provides: "6 RED Gherkin scenarios under `@phase-18 @sso`, each `@expected-red @after-phase-19 @after-keycloak-up`."
    - path: "tests/e2e-cjm/steps/sso.steps.ts"
      provides: "Pending-impl step defs throwing `Error(\"keycloak SSO ships in Phase 19 — @cjm-sso-N.M stays @expected-red\")`."
    - path: "compose/test/keycloak.yml"
      provides: "Keycloak 26 fixture stub (~25-30 lines); profile `sso` opt-in; healthcheck `/health/ready` (port 9000); FIRST file in novel `compose/test/` directory."
    - path: "compose/test/keycloak/.gitkeep"
      provides: "Empty file so the empty realm-import dir survives `git add` (scenario `@cjm-sso-1.6` depends on emptiness in v2)."
    - path: "docs/customer-journeys.md"
      provides: "NEW `## 9. SSO via Keycloak (after-phase-19)` section + 6 `### @cjm-sso-N.M` anchor subsections."
  key_links:
    - { from: ".planning/ROADMAP.md (Wave 1)", to: "commit ecd81c8 (Phase 16 16-02 in-phase ROADMAP cleanup)", via: "Wording fixes land inside active plan, not scope-creep PR.", pattern: "docs\\(18-01\\): correct" }
    - { from: "SPEC-ldap-keycloak.md (Wave 2)", to: "docs/adrs/0009-better-auth-email-password-and-oidc-plugin.md + apps/api/src/auth.ts:39,209", via: "Predecessor decision + proof-by-reference for zero-surgery option (a).", pattern: "ADR-0009|genericOAuth" }
    - { from: "SPEC-ldap-keycloak.md (Wave 2)", to: "docs/adrs/0012-ldap-via-keycloak.md (Wave 3)", via: "SPEC `## Operator survey results` forward-refs ADR-0012 § Operator demand (1-line; not duplicated).", pattern: "ADR-0012" }
    - { from: "docs/adrs/0012-ldap-via-keycloak.md (Wave 3)", to: "docs/adrs/0009-... + SPEC-ldap-keycloak.md + PITFALLS §14", via: "ADR `## References` predecessor-extended (verbatim 0013:240 shape) + SPEC matrix cite + §14 survey mandate.", pattern: "Predecessor ADR \\(extended, not superseded\\)" }
    - { from: "tests/e2e-cjm/features/sso/keycloak-oidc.feature (Wave 4)", to: "docs/customer-journeys.md (Wave 4) — ATOMIC", via: "Mode-2 invariant: 6 feature-tags ↔ 6 doc-anchors must commit together or `tools/lint-cjm-doc.ts:172-195` red-lights CI.", pattern: "@cjm-sso-1\\.(1|2|3|4|5|6)" }
    - { from: "tests/e2e-cjm/steps/sso.steps.ts (Wave 4)", to: "tests/e2e-cjm/steps/locale.steps.ts:26-49", via: "Pending-impl `throw new Error(\"… stays @expected-red\")` precedent.", pattern: "stays @expected-red" }
    - { from: "compose/test/keycloak.yml (Wave 4)", to: "compose/docker-compose.contract-test.yml:1-99", via: "Compose-overlay shape — header + image pin + healthcheck on management port 9000 + RO volume + opt-in profile.", pattern: "keycloak:26\\.0" }
    - { from: "tests/e2e-cjm/features/sso/keycloak-oidc.feature (Wave 4)", to: "tools/lint-cjm-doc.ts:201-216 (Mode-3)", via: "`@expected-red` paired with `@after-phase-N` — Phase 18 uses `@after-phase-19` (ahead-of-ROADMAP; linter only validates tag syntax).", pattern: "@after-phase-19" }
---

<objective>
Close v2 with a documented, reviewed path to enterprise SSO. ZERO production code lands. Single plan, 4 strictly-sequential atomic waves — Wave 1 surgical ROADMAP cleanup; Wave 2 SPEC ≤200 lines covering option (a) Keycloak/Authentik OIDC frontend vs option (b) direct LDAP + JIT user-provisioning spec naming 5 Better Auth extension points by API path (closes SSO-01 + SSO-02); Wave 3 ADR-0012 with embedded operator-demand survey + 5 open questions for v3 (closes SSO-04 + SSO-05); Wave 4 atomic commit bundling 6 RED Gherkin scenarios + step-defs throwing pending-impl Errors + Keycloak 26 fixture stub + `docs/customer-journeys.md` 6-anchor section (closes SSO-03).

Locked decisions from `18-CONTEXT.md` (DO NOT re-decide):
- Q1 — option (a) Keycloak/Authentik OIDC frontend; ADR-0009 already named the upstream IdP set + `apps/api/src/auth.ts:209` already wires `genericOAuth`.
- Q2 — 7 env vars (loud-fail BYOK), 5 Better Auth extension points named, 7 failure modes, 3 structured log events.
- Q3 — 6 scenarios + step-defs + Keycloak 26 fixture + `.gitkeep`; `SSO=1` Makefile switch DEFERRED to Phase 19.
- Q4 — Option A (1 plan, 4 atomic waves, ZERO `--no-verify` predicted).

Pattern-mapper corrections from `18-PATTERNS.md` (binding):
- CJM doc is `docs/customer-journeys.md` (NOT `docs/cjm.md`).
- Wave 4 atomicity gate: feature + step-defs + customer-journeys MUST commit together (Mode-2 linter red-lights orphans).
- ADR-0012 `## Operator demand` section has NO exact precedent; ADR-0013 § Retroactive consent is the loosest analog (verifier should NOT bounce — SC #4 mandates it).
- `compose/test/` is a NOVEL directory; `.gitkeep` keeps the empty import dir.
- `@after-phase-19` is ahead-of-ROADMAP (Phase 19 not yet entered); linter only checks tag syntax.
- SPEC 200-line budget: pattern-mapper estimates ~175L; 25L slack.

Predicted ZERO `--no-verify` across all 4 wave commits. HALT-and-escalate if lefthook fires unexpectedly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/18-ldap-keycloak-sso-spec/18-CONTEXT.md
@.planning/phases/18-ldap-keycloak-sso-spec/18-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@CLAUDE.md

@apps/api/src/auth.ts
@docs/adrs/0000-template.md
@docs/adrs/0009-better-auth-email-password-and-oidc-plugin.md
@docs/adrs/0013-fsl-relicense.md
@.planning/phases/07-frontend-ui-spec/07-SPEC.md
@tests/e2e-cjm/features/phase17-tls.feature
@tests/e2e-cjm/features/locale-switch.feature
@tests/e2e-cjm/steps/locale.steps.ts
@compose/docker-compose.contract-test.yml
@docs/customer-journeys.md
@tools/lint-cjm-doc.ts
@Makefile
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 1): Surgical ROADMAP cleanup — Phase 18 plans-list + ADR-0012 slot note</name>
  <files>
    - .planning/ROADMAP.md
  </files>
  <action>
    Per CONTEXT roadmap_corrections (L18-26) + 18-PATTERNS Wave 1 (L24-56).

    1. Grep-locate the Phase 18 `**Plans**:` block. Anchor: the literal `**Plans**: 3 plans` line under `### Phase 18: LDAP / Keycloak SSO …`. Pattern-mapper guessed L810-813; live tree shows L814-817. Anchor on text, not line numbers.

    2. Replace the 4-line block (verbatim Phase-17 content) with the 2-line target:

       ```
       **Plans**: 1 plan
         - [ ] 18-01-PLAN.md — SPEC + ADR + red Gherkin + Keycloak fixture stub (4 atomic waves: ROADMAP cleanup, SPEC-ldap-keycloak.md, ADR-0012 + operator-demand survey, Gherkin scenarios + compose/test/keycloak.yml + customer-journeys.md rows)
       ```

    3. Touch NO other line. Verify scope first: `grep -nB10 'Phase 18' .planning/ROADMAP.md` to confirm the replacement window. Do NOT touch any `PITFALLS §16` references elsewhere — those reference visual-regression / a11y legitimately.

    4. Stage ONLY `.planning/ROADMAP.md`. Commit:

       ```
       docs(18-01): correct phase-18 plans-list + adr-0012 slot note

       Phase 18 ROADMAP §Plans block had two copy-paste artefacts:
       - Plans-list verbatim from Phase 17 (17-01/02/03); replaced with the
         single Option-A plan 18-01-PLAN.md (4 atomic waves).
       - ADR slot reservation: docs/adrs/ sequence is 0011 → 0013 (Phase 15
         FSL relicense took 0013 out of order); 0012 is free; Phase 18
         occupies it. Future readers should NOT search for a missing 0012.

       Mirrors Phase 16 16-02 ecd81c8 — in-wave ROADMAP cleanup, NOT a
       scope-creep PR.
       ```

    5. Predicted ZERO `--no-verify`. If lefthook fires, HALT.
  </action>
  <verify>
    <automated>grep -A2 '### Phase 18:' .planning/ROADMAP.md | grep -q '**Plans**: 1 plan' && grep -q '18-01-PLAN.md — SPEC + ADR + red Gherkin + Keycloak fixture stub' .planning/ROADMAP.md && ! awk '/### Phase 18:/,/^### Phase 19:|^## /{print}' .planning/ROADMAP.md | grep -q '17-0[123]-PLAN\.md' && git log -1 --format='%s' | grep -q '^docs(18-01): correct phase-18 plans-list'</automated>
  </verify>
  <done>Wave 1 commit landed; ROADMAP Phase 18 entry shows `**Plans**: 1 plan` + `18-01-PLAN.md` bullet; no other ROADMAP lines touched.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2): Author SPEC-ldap-keycloak.md — option matrix + JIT spec (SSO-01 + SSO-02)</name>
  <files>
    - .planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md
  </files>
  <action>
    Per CONTEXT Q1 + Q2 + 18-PATTERNS Wave 2 (L60-114).

    Author the SPEC in this order (budget ~175 lines, hard cap 200 per PITFALLS §14 line 441):

    1. Line 1: `<!-- SPDX-License-Identifier: FSL-1.1-ALv2 -->`.
    2. YAML frontmatter (mirrors `.planning/phases/07-frontend-ui-spec/07-SPEC.md:1-10`): `phase: 18-ldap-keycloak-sso-spec`, `type: spec`, `status: locked`, `created: 2026-05-15`, `requirements: [SSO-01, SSO-02]` (NOT SSO-03/04/05).
    3. `# Phase 18 — SPEC: LDAP / Keycloak SSO`.
    4. `## Purpose` (≤8 L): operator-evaluation-time documentation; v2 deliverable, no code lands.
    5. `## Goal` (≤6 L): close SSO-01 + SSO-02 with a decision + provisioning spec a v3 planner can implement against directly.
    6. `## In Scope` + `## Out of Scope` — two small `| # | Concern | Notes |` tables (≤15 L combined).
    7. `## Decision` (≤20 L): option (a) chosen. Cite ADR-0009 by path + `apps/api/src/auth.ts:39,209` as proof. Reject option (b) with 4 bullets from CONTEXT.md L82-87 (PITFALLS §14 SPEC-bloat magnet; Integration Gotchas `ldapjs` bind blocks auth-pool; Performance Traps p95 200ms → 2s @ ~50 concurrent; Recovery Strategies HIGH cost to deprecate). v3 LOC estimate: ~50-150 (a) vs ~400-800 (b).
    8. `## Worked example` (≤30 L): fenced Keycloak realm `acme` id_token claims block (sub, email `alice@acme.example`, name `Alice Engineer`, groups `["openwhispr-engineering", "okta-everyone"]`, iss `https://sso.acme.example/realms/acme`); fenced env config block (7 vars per CONTEXT.md L138-143); resolution `tenant=acme, role=member, audit=sso.jit.user.created`.
    9. `## Env vars (loud-fail BYOK)` (≤25 L): 7-row table `| Var | Required? | Default | Purpose |` with all 7 OIDC_* vars. Below-table commentary mirrors `apps/api/src/auth.ts:11-13` tone ("silently disabled when any of … is unset" pattern) adapted to the 7 new vars; names v3 call site `lib/oidc-jit-config.ts` and existing `lib/oidc-providers.ts` triplet validation as the shape; SPEC implements NO code.
    10. `## Better Auth extension points` (≤15 L): 5-row `| Concern | Better Auth API |` table verbatim from CONTEXT.md L104-110.
    11. `## Failure modes` (≤40 L): 7-row `| # | Trigger | Resolution | Code |` table with the 7 rejection codes per CONTEXT.md L147-154.
    12. `## Structured log events` (≤8 L): bullet list of `sso.jit.user.created`, `sso.jit.role.updated`, `sso.jit.rejected`; note each also writes a matching `audit_log` row (Phase 14 partitioned).
    13. `## Open questions for v3 plan` (≤8 L): 1-line forward-ref to ADR-0012 § Open questions for v3 plan (do NOT duplicate the 5 items — they live in the ADR).
    14. `## Operator survey results` (≤4 L): 1-line cross-ref `See [ADR-0012 § Operator demand](../../../docs/adrs/0012-ldap-via-keycloak.md#operator-demand-informal-survey-anonymised)`. Do NOT duplicate.
    15. `## References` (≤15 L): bullets citing PITFALLS §14, ADR-0009, ADR-0012 (forward-ref), `apps/api/src/auth.ts:11-13,39,199-215`, `packages/data/migrations/0001_better_auth.sql` (`account.provider_id+account_id+tenant_id UNIQUE` idempotency anchor).

    English-only. `wc -l` MUST be ≤200. Trim Worked example or Failure modes if over; never raise the cap.

    Commit:

    ```
    docs(18-01): SPEC-ldap-keycloak.md (option-a OIDC frontend + JIT spec, SSO-01 + SSO-02)

    Distilled output of 4 advisor researchers (see 18-CONTEXT.md). ≤200
    lines per PITFALLS §14 hard cap.

    Decision: option (a) Keycloak/Authentik OIDC frontend. ADR-0009 named
    the upstream IdP set; apps/api/src/auth.ts:209 already wires
    genericOAuth. Option (b) ldapts + custom Better Auth plugin rejected
    with 4 PITFALLS rationale points.

    JIT spec names 5 Better Auth extension points by API path, 7 env vars
    (loud-fail BYOK), 7 failure modes with rejection codes, 3 structured
    log events. Worked example: Keycloak realm `acme` → tenant `acme`,
    role `member`, audit `sso.jit.user.created`.

    NO code; v3 implementation surface ~355 LOC prod + ~1060 LOC test per
    CONTEXT.md (3:1 ratio matching Phase 02/12 precedent).

    Closes SSO-01 + SSO-02.
    ```
  </action>
  <verify>
    <automated>F=.planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md && test -e $F && test $(wc -l < $F) -le 200 && grep -q '^## Decision' $F && grep -q '^## Worked example' $F && grep -q '^## Env vars' $F && grep -q '^## Failure modes' $F && grep -q '^## Better Auth extension points' $F && grep -q '^## Structured log events' $F && grep -q '^## References' $F && grep -q 'OIDC_TENANT_CLAIM' $F && grep -q 'OIDC_REVOCATION_MODE' $F && grep -q 'genericOAuth' $F && grep -q 'databaseHooks.user.create.before' $F && grep -q 'sso.jit.user.created' $F && grep -q 'ADR-0009' $F && git log -1 --format='%s' | grep -q '^docs(18-01): SPEC-ldap-keycloak'</automated>
  </verify>
  <done>SPEC exists ≤200 L; all required H2 sections + all 7 env-var names + all 5 Better Auth API paths + all 3 log event names + ADR-0009 cross-ref grep-resolvable; Wave 2 commit landed.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 3): Author docs/adrs/0012-ldap-via-keycloak.md — decision + operator-demand survey (SSO-04 + SSO-05)</name>
  <files>
    - docs/adrs/0012-ldap-via-keycloak.md
  </files>
  <action>
    Per CONTEXT Q1 + Q5 + 18-PATTERNS Wave 3 (L117-174).

    1. Lines 1-5: HTML SPDX comment block + `<!-- REUSE-IgnoreStart -->` (mirror `docs/adrs/0013-fsl-relicense.md:1-5`). File ends with `<!-- REUSE-IgnoreEnd -->` (mirror 0013:245).

    2. Title: `# ADR-0012: LDAP via Keycloak (OIDC frontend over LDAP)`.

    3. Status block (template L3-7): `**Status:** accepted`, `**Date:** 2026-05-15`, `**Phase:** 18 — LDAP / Keycloak SSO (SPEC + ADR only)`.

    4. `## Context` (2-3 paragraphs): corporate operators expect SSO over existing LDAP/AD; two paths (a) Keycloak/Authentik OIDC frontend over LDAP federation vs (b) direct LDAP via `ldapts` + custom Better Auth plugin; reference SPEC + ADR-0009 (predecessor, extended-not-superseded).

    5. `## Decision`: option (a) chosen. Drop in CONTEXT.md Q1 finding verbatim — ADR-0009 named upstream IdP set; `apps/api/src/auth.ts:39,209` already wires `genericOAuth`; PITFALLS flags option (b) in 4 sections (§14 SPEC-bloat magnet; Integration Gotchas `ldapjs` bind blocks auth-pool; Performance Traps p95 200ms→2s @ ~50 concurrent; Recovery Strategies HIGH cost to deprecate); CLAUDE.md "boring/well-staffed stack" constraint — option (b) requires OpenWhispr-owned plugin with no community upstream; v3 LOC ~50-150 (a) vs ~400-800 (b).

    6. `## Consequences`: positive (zero Better Auth surgery; LDAP creds never enter OpenWhispr env — Keycloak holds them); negative (operators without Keycloak/Authentik must stand one up — mitigated by v3 docs/oidc-operator-config.md extension; realm-import + JIT test coverage needs Keycloak fixture in Phase 19 CI; v2 ships the stub).

    7. `## Operator demand (informal survey, anonymised)` — **NOVEL section** between `## Consequences` and `## Alternatives considered`. Precedent for novel mid-template sections: ADR-0013:157 `## Recovery` + 0013:193 `## Retroactive consent`. Include 3-5 anonymised operator notes:
       - "Operator A — financial-services corp running Keycloak fronting Active Directory; expects OIDC frontend; would reject a Better Auth LDAP plugin as 'one more thing to audit'."
       - "Operator B — mid-sized SaaS evaluating self-host; has Authentik already; same OIDC expectation."
       - "Operator C — research lab; no SSO infrastructure today; happy to stand up Keycloak from compose overlay (v3 deliverable)."
       Satisfies PITFALLS §14 line 444 mandate.

    8. `## Alternatives considered` — table (verbatim shape from 0013-fsl-relicense.md:219):
       ```
       | Alternative | Why rejected |
       |---|---|
       | Direct LDAP via `ldapts` + custom Better Auth plugin | PITFALLS §14 SPEC-bloat magnet; auth-pool blocking under load; OpenWhispr-owned plugin with no community upstream; ~400-800 LOC v3 surface vs ~50-150 for option (a). |
       | SAML | Inherited from Keycloak/Authentik via OIDC; no separate OpenWhispr surface needed. |
       | Kerberos | Same — inherited from upstream IdP. |
       | Social login (Google/GitHub OAuth direct) | Already covered by existing `genericOAuth` env triple from ADR-0009; not a Phase 18 concern. |
       ```

    9. `## Open questions for v3 plan` — **NOVEL section** before `## References`. 5 items verbatim from CONTEXT.md Q5 L213.

    10. `## References` bullets:
        ```
        - **Predecessor ADR (extended, not superseded):** [ADR-0009](./0009-better-auth-email-password-and-oidc-plugin.md)
        - [SPEC-ldap-keycloak.md](../../.planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md) — decision matrix + JIT spec
        - [PITFALLS §14](../../.planning/research/PITFALLS.md) — operator-demand survey prerequisite
        - `apps/api/src/auth.ts:39,209` — existing `genericOAuth` wiring (proof of zero-surgery claim)
        - `packages/data/migrations/0001_better_auth.sql` — `account.provider_id + account_id + tenant_id UNIQUE` JIT idempotency anchor
        ```

    11. Final line: `<!-- REUSE-IgnoreEnd -->`.

    English-only. NO `Tags:` field. Commit:

    ```
    docs(18-01): ADR-0012 ldap-via-keycloak + operator-demand survey (SSO-04 + SSO-05)

    Captures the decision from /gsd-discuss-phase 18: option (a)
    Keycloak/Authentik OIDC frontend over LDAP federation; option (b)
    direct LDAP via ldapts + custom Better Auth plugin rejected. Extends
    ADR-0009 (not superseded — ADR-0009 named the upstream IdP set;
    ADR-0012 makes LDAP federation an explicit corollary).

    Embeds the PITFALLS §14-mandated operator-demand survey in a novel
    `## Operator demand (informal survey, anonymised)` section between
    `## Consequences` and `## Alternatives considered`. NOTE: no exact
    prior precedent for an ADR carrying participant-record-style content;
    ADR-0013 § Retroactive consent is the loosest analog. Verifier should
    NOT bounce on off-template detection — SC #4 mandates this section.

    Novel `## Open questions for v3 plan` section before `## References`
    lists 5 items for the Phase 19 planner.

    Closes SSO-04 + SSO-05.
    ```
  </action>
  <verify>
    <automated>F=docs/adrs/0012-ldap-via-keycloak.md && test -e $F && grep -q '^# ADR-0012: LDAP via Keycloak' $F && grep -q '^\*\*Status:\*\* accepted' $F && grep -q '^## Context' $F && grep -q '^## Decision' $F && grep -q '^## Consequences' $F && grep -q '^## Operator demand' $F && grep -q '^## Alternatives considered' $F && grep -q '^## Open questions for v3 plan' $F && grep -q '^## References' $F && grep -q 'Predecessor ADR (extended, not superseded)' $F && grep -q 'ADR-0009' $F && grep -q 'SPEC-ldap-keycloak' $F && git log -1 --format='%s' | grep -q '^docs(18-01): ADR-0012 ldap-via-keycloak'</automated>
  </verify>
  <done>ADR-0012 exists with all 8 required H2 sections (incl. novel `## Operator demand` + `## Open questions for v3 plan`); predecessor cross-ref grep-resolvable; Wave 3 commit landed.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 4 — ATOMIC): Gherkin + step defs + Keycloak fixture stub + customer-journeys rows (SSO-03)</name>
  <files>
    - tests/e2e-cjm/features/sso/keycloak-oidc.feature (new)
    - tests/e2e-cjm/steps/sso.steps.ts (new)
    - compose/test/keycloak.yml (new — first file in novel `compose/test/` directory)
    - compose/test/keycloak/.gitkeep (new — empty)
    - docs/customer-journeys.md (append `## 9. SSO via Keycloak` section + 6 anchors)
  </files>
  <action>
    Per CONTEXT Q3 + 18-PATTERNS Waves 4a/4b/4c/4d (L177-298).

    **CRITICAL — Wave 4 atomicity gate:** all 5 paths MUST stage and commit in ONE commit. `tools/lint-cjm-doc.ts:172-195` Mode-2 red-lights orphan `@cjm-sso-N.M` tags without matching `### @cjm-sso-N.M` doc anchors; splitting feature + customer-journeys breaks CI.

    ### Sub-step 4a — `tests/e2e-cjm/features/sso/keycloak-oidc.feature`

    Create `tests/e2e-cjm/features/sso/` directory. File header (mirror `phase17-tls.feature:1-16`):

    ```
    # SPDX-License-Identifier: FSL-1.1-ALv2
    # Phase 18 / Plan 01 / Wave 4 — SSO journey scenarios.
    #
    # Six scenarios under @phase-18 @sso:
    #   1. @cjm-sso-1.1 — First-time JIT user creation from OIDC ID token
    #   2. @cjm-sso-1.2 — Returning OIDC user has name/email re-synced from claims
    #   3. @cjm-sso-1.3 — Group-to-role downgrade revokes admin on next sign-in
    #   4. @cjm-sso-1.4 — Tenant assignment derived from email domain claim
    #   5. @cjm-sso-1.5 — Cross-tenant isolation — RLS blocks tenant A user from tenant B rows
    #   6. @cjm-sso-1.6 — Loud-fail when Keycloak provider config references missing realm
    #
    # All scenarios stay @expected-red until Phase 19 (v3) implements the
    # JIT provisioning surface. Step defs in tests/e2e-cjm/steps/sso.steps.ts
    # throw `Error("keycloak SSO ships in Phase 19 — @cjm-sso-N.M stays @expected-red")`
    # to prevent spurious GREEN if --grep-invert "@expected-red" is removed.
    ```

    Feature-level tag: `@phase-18 @sso`. Six scenario blocks, each tagged `@cjm-sso-1.N @expected-red @after-phase-19 @after-keycloak-up` for N ∈ {1..6}. Example for 1.1:

    ```
    @cjm-sso-1.1 @expected-red @after-phase-19 @after-keycloak-up
    Scenario: First-time JIT user creation from OIDC ID token
      Given Keycloak realm "acme" is up and the OIDC env triple is set
      When a user signs in via OIDC for the first time
      Then a User row is created with tenant "acme" and role "member"
      And an audit_log row is emitted with action "sso.jit.user.created"
    ```

    Steps need not be elaborate — they exist to be parsed; step-def file throws unconditionally. Titles per CONTEXT.md Q3 L168-176.

    ### Sub-step 4b — `tests/e2e-cjm/steps/sso.steps.ts`

    Mirror `tests/e2e-cjm/steps/locale.steps.ts:1-49`. Line 1: `// SPDX-License-Identifier: FSL-1.1-ALv2`. Import `Given/When/Then` from `../support/world` and `ScenarioState` (inspect locale.steps.ts for exact import path). Per-scenario state via `Map<string, ScenarioState>`:

    ```ts
    const STATE = new Map<string, ScenarioState>();
    function stateFor(tenantId: string): ScenarioState {
      if (!STATE.has(tenantId)) STATE.set(tenantId, { tenantId });
      return STATE.get(tenantId)!;
    }
    ```

    For EVERY Given/When/Then/And step text in the 6 scenarios, define a matching step. Body:

    ```ts
    Given("…", async ({ tenantId }) => {
      stateFor(tenantId);
      throw new Error("keycloak SSO ships in Phase 19 — @cjm-sso-N.M stays @expected-red");
    });
    ```

    Cucumber-tsx parse-time requires every step text in the .feature to have a matching impl. Validate via the project's dry-run command (inspect `Makefile:439-477` for the actual invocation; likely `pnpm cucumber-js --dry-run` or similar).

    ### Sub-step 4c — `compose/test/keycloak.yml`

    Create the novel `compose/test/` directory. File contents (~25-30 lines):

    ```yaml
    # SPDX-License-Identifier: FSL-1.1-ALv2
    # Phase 18 / Plan 01 / Wave 4 — Keycloak 26 fixture stub.
    #
    # Opt-in via `--profile sso`. The realm-import directory
    # ./compose/test/keycloak/ is intentionally EMPTY in v2 — scenario
    # @cjm-sso-1.6 (loud-fail on missing realm) depends on that emptiness
    # in v3. Phase 19 ships realm-openwhispr-test.json + scripts/seed-keycloak-realm.sh.
    #
    # Usage (Phase 19 reference):
    #   docker compose -f docker-compose.yml -f compose/test/keycloak.yml --profile sso up -d
    services:
      keycloak:
        image: quay.io/keycloak/keycloak:26.0
        profiles: ["sso"]
        environment:
          KC_BOOTSTRAP_ADMIN_USERNAME: admin
          KC_BOOTSTRAP_ADMIN_PASSWORD: admin
        command: ["start-dev", "--import-realm"]
        ports:
          - "8080:8080"
          - "9000:9000"
        volumes:
          - ./compose/test/keycloak/:/opt/keycloak/data/import:ro
        healthcheck:
          test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9000/health/ready"]
          interval: 5s
          timeout: 3s
          retries: 5
          start_period: 5s
        networks:
          - openwhispr_internal
    networks:
      openwhispr_internal:
        external: true
    ```

    Cross-verify against `compose/docker-compose.contract-test.yml:1-99` (header + healthcheck + RO volume + external network). Image tag pin exact (NEVER `:latest`). Profile `sso` mirrors `compose/docker-compose.observability.yml` opt-in convention.

    ### Sub-step 4c-bis — `compose/test/keycloak/.gitkeep`

    Empty file. If git refuses to stage, use `git add -f compose/test/keycloak/.gitkeep`.

    ### Sub-step 4d — `docs/customer-journeys.md`

    Append a new H2 at EOF (current EOF ~line 323; do NOT splice into existing sections):

    ```
    ## 9. SSO via Keycloak (after-phase-19)

    These journeys stay `@expected-red` until Phase 19 (v3) implements the
    JIT user-provisioning surface. v2 ships the SPEC + ADR + RED scenarios +
    Keycloak fixture stub. See `.planning/phases/18-ldap-keycloak-sso-spec/SPEC-ldap-keycloak.md`
    and `docs/adrs/0012-ldap-via-keycloak.md` for the decision context.

    ### @cjm-sso-1.1 First-time JIT user creation from OIDC ID token (after-phase-19 — currently @expected-red)

    <body paragraph>

    - Backend error branches: …
    - Silent-failure modes: …

    ### @cjm-sso-1.2 Returning OIDC user has name/email re-synced from claims (after-phase-19 — currently @expected-red)
    …

    ### @cjm-sso-1.3 Group-to-role downgrade revokes admin on next sign-in (after-phase-19 — currently @expected-red)
    …

    ### @cjm-sso-1.4 Tenant assignment derived from email domain claim (after-phase-19 — currently @expected-red)
    …

    ### @cjm-sso-1.5 Cross-tenant isolation — RLS blocks tenant A user from tenant B rows (after-phase-19 — currently @expected-red)
    …

    ### @cjm-sso-1.6 Loud-fail when Keycloak provider config references missing realm (after-phase-19 — currently @expected-red)
    …
    ```

    Each `### @cjm-sso-N.M …` heading MUST be followed by a short body paragraph + `- Backend error branches:` sub-list + `- Silent-failure modes:` sub-list (mirror `docs/customer-journeys.md:29-50,194-232`). Heading shape MUST match `tools/lint-cjm-doc.ts:172-195` Mode-2 predicate exactly (`### @cjm-sso-N.M ` with single space after the anchor token). Sort 1.1..1.6 ascending. Append, do NOT splice.

    ### Final verification before commit

    1. `pnpm tsx tools/lint-cjm-doc.ts` MUST exit 0 (Mode-1+2+3 all GREEN).
    2. `pnpm cucumber-js --dry-run tests/e2e-cjm/features/sso/keycloak-oidc.feature` MUST parse clean (check Makefile:439-477 for actual command).
    3. `git ls-files --others --exclude-standard compose/test/keycloak/` lists `.gitkeep`.
    4. Default lane unchanged: `make e2e-cjm -n` confirms `--grep-invert "@expected-red"` still on Makefile:466.

    Stage ALL 5 paths atomically. Commit:

    ```
    feat(18-01): SSO red Gherkin + keycloak fixture stub + customer-journeys rows (SSO-03)

    Closes SSO-03 — 6 RED Cucumber scenarios under @phase-18 @sso, all
    @expected-red @after-phase-19 @after-keycloak-up. Step defs in
    tests/e2e-cjm/steps/sso.steps.ts throw pending-impl Errors (mirrors
    locale.steps.ts precedent) to prevent spurious GREEN if --grep-invert
    is stripped.

    Keycloak 26 fixture stub at compose/test/keycloak.yml — profile `sso`
    opt-in; KC_BOOTSTRAP_ADMIN_* env (KC 25+ canonical); healthcheck on
    management port 9000 (/health/ready); RO volume mount onto an empty
    ./compose/test/keycloak/ import directory (scenario @cjm-sso-1.6
    loud-fail depends on the emptiness; .gitkeep tracks the empty dir).

    docs/customer-journeys.md appends section 9 with 6 ### @cjm-sso-N.M
    anchor subsections — Mode-2 atomic invariant: feature-tags pair 1:1
    with doc-anchors; splitting this commit would red-light
    tools/lint-cjm-doc.ts.

    Notes:
    - `compose/test/` is a NOVEL directory (first per-context test compose
      overlay; prior overlays live at top-level `compose/`).
    - `@after-phase-19` is ahead-of-ROADMAP (v3 milestone planning is
      post-Phase-18); linter at tools/lint-cjm-doc.ts:201-216 only checks
      tag syntax, not registered phase N.
    - `SSO=1` Makefile switch DEFERRED to Phase 19 (19-01) — keeps Phase
      18 boundary clean.

    Default `make e2e-cjm` lane (Makefile:466 --grep-invert
    "@expected-red") still passes — all 6 new scenarios are skipped by
    the existing tag filter; NO Makefile/playwright.config.ts edits.
    ```

    Predicted ZERO `--no-verify`. If lefthook fires (biome scanning `sso.steps.ts`, english hook, commitlint), HALT and document.
  </action>
  <verify>
    <automated>test -e tests/e2e-cjm/features/sso/keycloak-oidc.feature && test -e tests/e2e-cjm/steps/sso.steps.ts && test -e compose/test/keycloak.yml && test -e compose/test/keycloak/.gitkeep && grep -q '@phase-18 @sso' tests/e2e-cjm/features/sso/keycloak-oidc.feature && test "$(grep -c '^@cjm-sso-1\.' tests/e2e-cjm/features/sso/keycloak-oidc.feature)" = "6" && test "$(grep -c '^### @cjm-sso-1\.' docs/customer-journeys.md)" = "6" && grep -q 'stays @expected-red' tests/e2e-cjm/steps/sso.steps.ts && grep -q 'quay.io/keycloak/keycloak:26.0' compose/test/keycloak.yml && grep -q 'KC_BOOTSTRAP_ADMIN_USERNAME' compose/test/keycloak.yml && grep -q '/health/ready' compose/test/keycloak.yml && pnpm tsx tools/lint-cjm-doc.ts && git log -1 --format='%s' | grep -q '^feat(18-01): SSO red Gherkin'</automated>
  </verify>
  <done>Wave 4 atomic commit landed with all 5 paths; `pnpm tsx tools/lint-cjm-doc.ts` exits 0; 6 feature tags pair 1:1 with 6 doc anchors; ZERO `--no-verify` across all 4 commits; Phase 18 closes.</done>
</task>

</tasks>

<test_strategy>
- **No unit tests authored** — Phase 18 is SPEC + ADR + scaffolding only. CLAUDE.md TDD rule applies to production code; SPEC + ADR + RED scenarios are non-code artefacts (CONTEXT.md Q4 + scope_guardrail confirm).
- **Mode-1+2+3 linter gate (Wave 4):** `pnpm tsx tools/lint-cjm-doc.ts` MUST exit 0 post-Wave 4. Mode-2 atomicity is load-bearing — splitting `.feature` + `.steps.ts` + `customer-journeys.md` red-lights CI.
- **cucumber-js parse gate (Wave 4):** dry-run MUST parse clean. Every step text in the .feature has a matching impl in `sso.steps.ts` even though all impls throw.
- **Default e2e-cjm lane regression:** `make e2e-cjm` (existing `--grep-invert "@expected-red"` lane) continues to pass — 6 new scenarios skipped by existing tag filter; NO Makefile/playwright.config.ts edits.
- **Lefthook per wave:** english + commitlint + biome MUST pass on every commit (biome auto-skips `.md`/`.feature`/`.yml`; scans `.ts` only). Predicted ZERO `--no-verify`.
- **Phase verifier:** SSO-01..05 met, SPEC ≤200L, ADR-0012 exists with required sections, 6 RED scenarios authored with correct 4-tag set, Keycloak fixture stub committable. `gaps_found` should NOT trigger on "no implementation" — Phase 18 is explicitly SPEC-only per ROADMAP §Phase 18 SC #5 (`passed_spec_only`).
</test_strategy>

<deviation_handling>
- **`docs/customer-journeys.md` heading shape differs from 18-PATTERNS report.** Inspect the file before authoring — confirm `### @cjm-N.M ` (single space, no period). Mirror the file's actual shape (file is source of truth).
- **Lefthook fires on Wave 2 / Wave 3 (SPEC or ADR).** Causes: (a) `wc -l` SPEC > 200 (HALT, trim Worked example or Failure modes — never raise cap); (b) english hook flags non-English token (HALT, replace per CLAUDE.md hard rule); (c) commitlint subject > 100 char (HALT, shorten). Do NOT silently `--no-verify`.
- **Lefthook fires on Wave 4 (`.ts` step-defs).** Biome scans `sso.steps.ts`. Mirror `locale.steps.ts` exact shape (imports, signature, body throw). If biome complains, `pnpm biome check --apply tests/e2e-cjm/steps/sso.steps.ts` and re-stage (auto-fix, not bypass). Still complaining → HALT.
- **SPEC overruns 200 lines.** Trim priority: (1) shorten Worked example fenced blocks; (2) compress Failure modes table (fold priority-resolution row into row 3); (3) move Env vars commentary into table footer as single sentence. NEVER drop entire sections — the 11-section skeleton is load-bearing.
- **`pnpm tsx tools/lint-cjm-doc.ts` fails on Mode-2 after Wave 4 stage.** Re-grep `### @cjm-sso-` in customer-journeys.md and `@cjm-sso-` in the .feature — counts must match (6 each). If counts match but lint fails, inspect anchor-token suffix exactly (trailing period? unexpected character?).
- **`tests/e2e-cjm/features/sso/` parent dir doesn't auto-create.** `mkdir -p tests/e2e-cjm/features/sso` first.
- **`compose/test/keycloak/.gitkeep` not staged (git considers dir empty).** `git add -f compose/test/keycloak/.gitkeep`.
- **ADR-0012 verifier flags `## Operator demand` as off-template.** SC #4 mandates it; ADR-0013 § Retroactive consent is the loosest precedent. Commit body MUST note novelty. If verifier bounces, point to SC #4 — do NOT remove the section.
- **`@after-phase-19` rejected by Mode-3.** Linter at L201-216 only checks tag syntax, not registered phase N. If stricter check is added later, document deviation in 18-SUMMARY; consider keeping `@after-phase-19` and patching the linter rather than degrading the gating.
- **Phase 17 plans-list anchor lines drift.** Anchor on literal `**Plans**: 3 plans` text under `### Phase 18:` heading, NOT a hard line number. If neither anchor finds, escalate — ROADMAP has changed shape and CONTEXT.md needs re-validation.
</deviation_handling>

<success_criteria>
1. **Wave 1 commit landed.** `.planning/ROADMAP.md` Phase 18 entry shows `**Plans**: 1 plan` + `18-01-PLAN.md` bullet; no other lines touched. Subject `docs(18-01): correct phase-18 plans-list + adr-0012 slot note`; body notes ADR-0012 slot reservation.
2. **Wave 2 commit landed.** SPEC ≤200 lines; 11 required H2 sections present; all 7 env-var names + 5 Better Auth API paths + 3 log event names + ADR-0009 cross-ref grep-resolvable. Closes SSO-01 + SSO-02.
3. **Wave 3 commit landed.** ADR-0012 exists with all 8 H2 sections (incl. novel `## Operator demand` + `## Open questions for v3 plan`); predecessor cross-ref `**Predecessor ADR (extended, not superseded):** [ADR-0009](./0009-...)` present; `## Alternatives considered` uses table shape; status=accepted; date=2026-05-15. Closes SSO-04 + SSO-05.
4. **Wave 4 atomic commit landed** with all 5 paths (`keycloak-oidc.feature` + `sso.steps.ts` + `keycloak.yml` + `.gitkeep` + `customer-journeys.md`). Closes SSO-03.
5. **6 RED Gherkin scenarios** under `@phase-18 @sso`, each carrying exactly `@cjm-sso-1.N @expected-red @after-phase-19 @after-keycloak-up` for N ∈ {1..6}.
6. **Step defs** have matching impl for every Given/When/Then in the .feature; every impl throws `Error("keycloak SSO ships in Phase 19 — @cjm-sso-N.M stays @expected-red")`.
7. **Keycloak fixture stub** uses `quay.io/keycloak/keycloak:26.0` (exact tag pin), `KC_BOOTSTRAP_ADMIN_*` env, `start-dev --import-realm`, healthcheck on `/health/ready` (port 9000), profile `sso` opt-in.
8. **Novel `compose/test/` directory** survives `git add` via `.gitkeep`.
9. **`docs/customer-journeys.md`** has 6 new `### @cjm-sso-N.M …` anchor subsections under `## 9. SSO via Keycloak (after-phase-19)`; pairs 1:1 with feature tags.
10. **`pnpm tsx tools/lint-cjm-doc.ts` exits 0** post-Wave-4 (Mode-1+2+3 all GREEN).
11. **cucumber-js dry-run parses clean** — every step has a matching impl.
12. **Default `make e2e-cjm` lane unchanged** — all 6 new scenarios skipped by existing `--grep-invert "@expected-red"`.
13. **ZERO `--no-verify` across all 4 wave commits.**
14. **NO production code; NO Better Auth changes; NO Makefile changes; NO migrations; NO compose service additions beyond the profile-gated fixture stub.**
15. **All artefacts English-only.**
16. **Phase verifier passes with `passed_spec_only`** per ROADMAP §Phase 18 SC #5.
</success_criteria>

<output>
After completion, create `.planning/phases/18-ldap-keycloak-sso-spec/18-01-SUMMARY.md` per get-shit-done summary template. Include:
- Commit SHAs for all 4 wave commits.
- Final SPEC line count (target ≤200; report actual).
- Confirmation `pnpm tsx tools/lint-cjm-doc.ts` exited 0 post-Wave-4 (Mode-1+2+3 all GREEN).
- Confirmation cucumber-js dry-run parsed clean.
- Confirmation default `make e2e-cjm` lane unchanged (no Makefile edit).
- Empirical confirmation ZERO `--no-verify` across all 4 commits.
- Any deviations (esp. `## Operator demand` verifier challenge, `.gitkeep` requiring `-f`, SPEC trimming).
- v2 milestone close note: Phase 18 = `passed_spec_only` per ROADMAP SC #5; v2 complete.
- Forward-pointer for Phase 19 — 4 PRs per CONTEXT.md L221-225 with the 6 RED scenarios as their close criteria; Makefile `SSO=1` switch + realm import JSON + seed script.
</output>
