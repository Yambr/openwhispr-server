---
phase: 16-phase-tag-comment-audit-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - apps/worker/**/*.ts
  - packages/**/*.ts
  - apps/web/**/*.{ts,tsx}
  - apps/api/tests/**/*.ts
  - apps/api/src/**/*.ts
  - tools/lint-phase-tag-comments.allowlist.txt
autonomous: true
requirements:
  - COMMENT-01
  - COMMENT-02
  - COMMENT-04
must_haves:
  truths:
    - "5 per-area sweep commits land in smallest-first order: `apps/worker` → `packages/` → `apps/web` → `apps/api/tests` → `apps/api/src` (CONTEXT Q5 — defect-canary discipline)."
    - "Each sweep commit is generated by `pnpm exec tsx tools/phase-tag-sweep.ts fix <area-path>` and contains ONLY comment-line deletions (zero whitespace reformat, zero token changes around the deleted lines)."
    - "Each sweep commit message follows the template: `refactor(16): sweep N phase-tag comments <area>` where N is the actual count printed by the codemod (mirroring Phase 15-03 `refactor(15-03): sweep spdx headers <area>` precedent)."
    - "Each sweep commit body includes the 0-diff coverage waiver line `0-diff coverage waiver: comment-only deletions, no behavior change.` (mirroring Phase 15-02 / 15-03 SUMMARY precedent)."
    - "The FIRST sweep commit (`apps/worker`, smallest area) goes through full lefthook pre-commit pipeline WITHOUT `--no-verify`. If the hook passes (predicted outcome per CONTEXT — comment-only deletes don't trigger biome rewrite), proceed through the remaining 4 sweeps the same way. If the hook fails, HALT and escalate as an ME-02 deviation (do NOT silently apply `--no-verify`)."
    - "Allowlist file `tools/lint-phase-tag-comments.allowlist.txt` is touched in zero of the five sweep commits (the codemod removes mechanically — there is nothing to allowlist UNLESS the heuristic misclassifies, in which case the operator adds an entry as a documented deviation BEFORE re-running the sweep on that area)."
    - "After all 5 sweep commits land, `pnpm lint:phase-tag-comments` exits 0 (clean) OR exits 1 with violations that are EXPLICITLY listed in the allowlist via post-sweep `chore(16): allowlist remaining heuristic-keeps` commit with a one-line rationale per entry in the commit body."
    - "`pnpm test` (full Vitest suite across apps + packages) is GREEN post-sweep. testcontainers spawn for tests that need Postgres/Valkey. e2e-cjm is deferred to GitHub Actions CI (matching repo convention)."
    - "ROADMAP edit lands in ONE atomic commit alongside REQUIREMENTS edit (CONTEXT Q5 — ROADMAP wording fix co-lands with the phase work, NOT as a separate scope-creep PR)."
    - "`.planning/ROADMAP.md` §16 success-criterion #1 rewritten from `exactly 771` to `approximately 754 phase-tag header comments (originally cited as 771 pre-Phase-15; delta = file deletions during structural reorg)` per CONTEXT Q1."
    - "`.planning/ROADMAP.md` COMMENT-03 wording rewritten from `ESLint regression rule` to `lint regression rule (tsx CLI per Phase 15-01 pivot)` per CONTEXT Q3."
    - "`.planning/REQUIREMENTS.md` lines 489-492 wording flipped to match the ROADMAP edits: COMMENT-01 changes `771` to `approximately 754`; COMMENT-03 changes `ESLint regression rule` to `lint regression rule (tsx CLI)`."
    - "ME-02 lefthook upstream issue body is authored INLINE in 16-02 PLAN.md `<deviation_handling>` section (NOT as a `.github/ISSUE_TEMPLATE/*.md` file per PATTERNS critical-correction). Operator files the issue manually at https://github.com/evilmartians/lefthook/issues/new — issue filing is a deferred operator chore, NOT a blocking phase task."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "§16 wording fix (771 → ~754; ESLint → lint)."
    - path: ".planning/REQUIREMENTS.md"
      provides: "COMMENT-01 + COMMENT-03 wording flip to match ROADMAP."
    - path: "apps/worker/**/*.ts (sweep)"
      provides: "Sweep commit 1 — smallest area first per CONTEXT Q5."
    - path: "packages/**/*.ts (sweep)"
      provides: "Sweep commit 2."
    - path: "apps/web/{src,tests}/**/*.{ts,tsx} (sweep)"
      provides: "Sweep commit 3."
    - path: "apps/api/tests/**/*.ts (sweep)"
      provides: "Sweep commit 4."
    - path: "apps/api/src/**/*.ts (sweep)"
      provides: "Sweep commit 5 — largest area last."
    - path: "tools/lint-phase-tag-comments.allowlist.txt (conditional)"
      provides: "Post-sweep allowlist entries IFF heuristic misclassified anything (expected: zero entries)."
  key_links:
    - from: "Sweep commits (5)"
      to: "tools/phase-tag-sweep.ts (from 16-01)"
      via: "`pnpm exec tsx tools/phase-tag-sweep.ts fix <area>` — single tool, deterministic output, per-area invocation"
      pattern: "phase-tag-sweep\\.ts fix"
    - from: ".planning/ROADMAP.md edit"
      to: ".planning/REQUIREMENTS.md edit"
      via: "ATOMIC: both edits land in ONE commit (no semantic drift between source artifacts)"
      pattern: "approximately 754|lint regression rule"
    - from: "Sweep commit ordering"
      to: "lefthook ME-02 defect surface"
      via: "smallest area first — canary signal; if defect triggers on apps/worker, larger sweeps abort"
      pattern: "refactor\\(16\\): sweep"
---

<objective>
Execute the mechanical phase-tag comment sweep using the codemod and lint rule
landed in 16-01, in 5 per-area atomic commits ordered smallest-first
(`apps/worker` → `packages/` → `apps/web` → `apps/api/tests` → `apps/api/src`),
each going through the full lefthook pipeline without `--no-verify`
(empirically verifying CONTEXT's prediction that comment-only deletes don't
trigger the ME-02 lefthook patch-reapply defect). Land ROADMAP + REQUIREMENTS
wording corrections in ONE atomic commit. Author the ME-02 lefthook upstream
issue body inline (operator files externally — non-blocking).

Purpose: 16-01 built the tooling; 16-02 ships the deletion. Per CONTEXT Q4 the
sweep is grouped per-area (not 1 squashed mega-commit, not 754 atomic commits).
Per CONTEXT Q5 ordering is smallest-first so a lefthook defect surfaces on a
small commit before sweeping ~250-300 files in `apps/api/src`. Per CONTEXT Q2
the heuristic-only classifier defaults to KEEP on ambiguity — expected
strip-rate is ~30-50% of the 754 candidates (acceptable under-delivery per
CONTEXT acceptable-risk note; the lint rule prevents future regression).

Output: 5 sweep commits + 1 ROADMAP/REQUIREMENTS wording-fix commit + optional
allowlist commit IFF heuristic misclassified anything. Inline ME-02 issue body
in this PLAN's `<deviation_handling>` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<files_to_read>
@.planning/PROJECT.md
@.planning/phases/16-phase-tag-comment-audit-v2/16-CONTEXT.md
@.planning/phases/16-phase-tag-comment-audit-v2/16-PATTERNS.md
@.planning/phases/16-phase-tag-comment-audit-v2/16-01-PLAN.md
@.planning/phases/16-phase-tag-comment-audit-v2/16-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-03-SUMMARY.md
@.planning/phases/15-repo-refactor-fsl-relicense-history-scrub-v2/15-REVIEW.md

@tools/phase-tag-sweep.ts
@tools/lint-phase-tag-comments.ts
@tools/lint-phase-tag-comments.allowlist.txt
@lefthook.yml
@CLAUDE.md
</files_to_read>

<tasks>

<task type="auto">
  <name>Task 1: ROADMAP + REQUIREMENTS wording fix (one atomic commit, lands first)</name>
  <files>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    Per CONTEXT Q5, ROADMAP edits co-land with phase work. Per PATTERNS
    §4 ROADMAP edit precedent, the edit goes into the FIRST 16-02 commit
    (not a separate scope-creep PR). Land BEFORE any sweep so the success
    criterion the verifier will check is correctly worded BEFORE the sweep
    commits report against it.

    Two surgical edits, ONE commit:

    1. `.planning/ROADMAP.md` §16 success-criterion #1:
       - FROM: `exactly 771` (or whatever the current literal phrasing is)
       - TO: `approximately 754 phase-tag header comments (originally cited as 771 pre-Phase-15; delta = file deletions during structural reorg)`
       Use `grep -n '771' .planning/ROADMAP.md` to locate exact line; replace via Edit tool, NOT a regex search-replace that could touch unrelated `771` matches elsewhere.

    2. `.planning/ROADMAP.md` COMMENT-03 wording (or wherever COMMENT-03 success criterion is restated in the Phase 16 entry):
       - FROM: `ESLint regression rule`
       - TO: `lint regression rule (tsx CLI per Phase 15-01 pivot)`

    3. `.planning/REQUIREMENTS.md` lines 489-492:
       - Line 489 (COMMENT-01): change `771` → `approximately 754` to match ROADMAP.
       - Line 491 (COMMENT-03): change `ESLint regression rule` → `lint regression rule (tsx CLI)` to match ROADMAP.

    Commit as: `docs(16-02): correct Phase 16 wording (~754 + lint CLI)`.

    Co-land both files in ONE atomic commit. NO separate roadmap-only PR.
  </action>
  <verify>
    <automated>grep -q 'approximately 754' .planning/ROADMAP.md && grep -q 'lint regression rule' .planning/ROADMAP.md && grep -q 'approximately 754' .planning/REQUIREMENTS.md && ! grep -q 'ESLint regression rule' .planning/REQUIREMENTS.md && ! grep -q 'exactly 771' .planning/ROADMAP.md</automated>
  </verify>
  <done>Both files updated; grep gates all pass; ONE commit on the branch.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep apps/worker (smallest area first — empirical lefthook canary)</name>
  <files>
    - apps/worker/**/*.ts (codemod output)
  </files>
  <action>
    THIS IS THE EMPIRICAL CANARY for the ME-02 lefthook patch-reapply defect
    (see PATTERNS §"Lefthook patch-reapply defect (ME-02) — risk vs. expected
    behavior"). CONTEXT predicts the hook passes cleanly because comment-only
    deletes don't trigger biome reformat. Verify it.

    Steps:

    1. Capture baseline: `pnpm exec tsx tools/phase-tag-sweep.ts audit apps/worker 2>&1 | tee /tmp/phase16-worker-audit.txt`. Note the violation count (this becomes N in the commit message).
    2. Run codemod: `pnpm exec tsx tools/phase-tag-sweep.ts fix apps/worker`. Capture the printed `N comment(s) removed` count.
    3. Sanity check: `git diff --stat apps/worker | wc -l` should show ~20-30 files touched per CONTEXT estimate; `git diff apps/worker` should show ONLY line deletions of `//`-prefixed comment lines (zero non-comment changes — verify by grep on the diff).
    4. Stage: `git add apps/worker/`.
    5. Commit WITHOUT `--no-verify`:
       ```
       git commit -m "refactor(16): sweep N phase-tag comments apps/worker

       N source files in apps/worker — header-style phase-tag comments
       (// Phase NN[.M] / Plan NN[-MM] …, trailing bare // D-NN, history
       close-out comments) removed via 'pnpm exec tsx tools/phase-tag-sweep.ts fix apps/worker'.
       Inline // D-NN — <prose> anchors and KEEP-bucket matches untouched.

       0-diff coverage waiver: comment-only deletions, no behavior change.

       Lefthook ME-02 canary: this commit went through the full pre-commit
       pipeline without --no-verify, empirically testing CONTEXT's prediction
       that comment-only deletes do not trigger the patch-reapply defect."
       ```
       Substitute the actual N count printed by the codemod.

    6. **If `git commit` FAILS** because lefthook patch-reapply skips files:
       - HALT. Do NOT retry with `--no-verify`.
       - Add a `<deviation>` entry to this PLAN's deviation log: "ME-02 defect EMPIRICALLY FIRED on apps/worker sweep (N files). CONTEXT prediction falsified."
       - File the ME-02 upstream issue (body drafted in `<deviation_handling>` below) BEFORE proceeding.
       - Document the deviation in 16-SUMMARY.md.
       - Only THEN consider `--no-verify` with explicit per-commit justification (matching Phase 15-03 `7aeea9b` precedent).

    7. **If `git commit` SUCCEEDS** (predicted outcome): record the empirical confirmation in 16-SUMMARY.md so future phases know comment-only sweeps are safe without `--no-verify`.

    8. Re-run `pnpm test --filter=worker` to confirm `apps/worker` test suite is GREEN post-sweep.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^refactor(16): sweep [0-9]\+ phase-tag comments apps/worker$' && git diff HEAD~1 HEAD apps/worker | grep -v '^[+-]//' | grep '^[+-][^+-]' | grep -v '^[+-][+-][+-]' ; test $? -ne 0; pnpm exec tsx tools/phase-tag-sweep.ts audit apps/worker; test $? -eq 0</automated>
  </verify>
  <done>Sweep commit on branch with conformant message; diff contains ONLY `//`-prefixed comment-line deletions; post-sweep `audit apps/worker` exits 0 (clean); apps/worker test suite GREEN; lefthook hook outcome recorded (passed or failed) in commit body.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep packages/</name>
  <files>
    - packages/**/*.ts (codemod output)
  </files>
  <action>
    Repeat Task 2's procedure on `packages/`. CONTEXT estimates ~80-100 files
    in this area.

    1. `pnpm exec tsx tools/phase-tag-sweep.ts audit packages`. Capture N.
    2. `pnpm exec tsx tools/phase-tag-sweep.ts fix packages`.
    3. Verify diff is comment-only (same grep gate as Task 2).
    4. `git add packages/` and commit (NO `--no-verify` unless Task 2 empirically established the defect fires on comment-only sweeps):
       ```
       refactor(16): sweep N phase-tag comments packages/

       N source files in packages/ — header-style phase-tag comments
       (// Phase NN[.M] / Plan NN[-MM] …, trailing bare // D-NN, history
       close-out comments) removed via 'pnpm exec tsx tools/phase-tag-sweep.ts fix packages'.
       Inline // D-NN — <prose> anchors and KEEP-bucket matches untouched.

       0-diff coverage waiver: comment-only deletions, no behavior change.
       ```
    5. `pnpm test --filter='./packages/*'` GREEN post-sweep (testcontainers spawn for packages/data RLS-property tests).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^refactor(16): sweep [0-9]\+ phase-tag comments packages/$' && pnpm exec tsx tools/phase-tag-sweep.ts audit packages; test $? -eq 0</automated>
  </verify>
  <done>Sweep commit on branch; post-sweep `audit packages` exits 0; packages test suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Sweep apps/web (src + tests combined)</name>
  <files>
    - apps/web/**/*.{ts,tsx} (codemod output)
  </files>
  <action>
    Repeat the sweep on `apps/web`. CONTEXT estimates ~50-80 files combined
    (src + tests), well below the W-3 150-file threshold so a single
    commit per area is correct.

    1. `pnpm exec tsx tools/phase-tag-sweep.ts audit apps/web`. Capture N.
    2. `pnpm exec tsx tools/phase-tag-sweep.ts fix apps/web`.
    3. Verify diff is comment-only.
    4. Commit:
       ```
       refactor(16): sweep N phase-tag comments apps/web

       N source files in apps/web (src + tests) — header-style phase-tag
       comments (// Phase NN[.M] / Plan NN[-MM] …, trailing bare // D-NN,
       history close-out comments) removed via 'pnpm exec tsx tools/phase-tag-sweep.ts fix apps/web'.
       Inline // D-NN — <prose> anchors and KEEP-bucket matches untouched.

       0-diff coverage waiver: comment-only deletions, no behavior change.
       ```
    5. `pnpm test --filter=web` GREEN.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^refactor(16): sweep [0-9]\+ phase-tag comments apps/web$' && pnpm exec tsx tools/phase-tag-sweep.ts audit apps/web; test $? -eq 0</automated>
  </verify>
  <done>Sweep commit on branch; post-sweep audit clean; web test suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 5: Sweep apps/api/tests</name>
  <files>
    - apps/api/tests/**/*.ts (codemod output)
  </files>
  <action>
    Repeat on `apps/api/tests`. CONTEXT estimates ~80-100 files. Note: the
    codemod's glob is `apps/api/tests/**/*.{ts,tsx}` — `apps/api/src/` is
    EXPLICITLY EXCLUDED in this commit (it gets its own sweep in Task 6).

    Pass the sub-path explicitly: `pnpm exec tsx tools/phase-tag-sweep.ts fix apps/api/tests`.

    1. Audit + capture N.
    2. Fix.
    3. Verify diff comment-only.
    4. Commit `refactor(16): sweep N phase-tag comments apps/api/tests` with the standard body + waiver.
    5. `pnpm --filter @openwhispr/api test` GREEN (testcontainers spawn for postgres + Valkey).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^refactor(16): sweep [0-9]\+ phase-tag comments apps/api/tests$' && pnpm exec tsx tools/phase-tag-sweep.ts audit apps/api/tests; test $? -eq 0</automated>
  </verify>
  <done>Sweep commit on branch; audit clean; api test suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 6: Sweep apps/api/src (largest area last)</name>
  <files>
    - apps/api/src/**/*.ts (codemod output)
  </files>
  <action>
    Final and largest sweep. CONTEXT estimates ~250-300 files. CONTEXT
    estimates this area is under the W-3 150-file threshold for sweep CHURN
    (comment-only deletions ≠ widespread file rewrites), so a single commit is
    still correct — but the file-count is high enough that ME-02 defect risk
    is highest here. Tasks 2-5 must have empirically established that
    comment-only sweeps avoid the defect BEFORE landing this commit.

    1. Audit + capture N.
    2. Fix.
    3. Spot-check the top-10 hot files from CONTEXT `<code_context>`:
       - `apps/api/src/index.ts` (48 hits expected)
       - `apps/api/src/routes/index.ts` (47)
       - `apps/api/src/auth.ts` (30)
       - `apps/api/src/plugins/rate-limit.ts` (18)
       - `apps/api/src/routes/v1/keys/create.ts` (11)
       For each, confirm: (a) bare-header comments DELETED, (b) inline
       `// D-NN — <prose>` anchors RETAINED, (c) lines containing `because` /
       `to avoid` / `workaround` / `NEVER` / `MUST` / `prevent` / `PITFALLS §` /
       `SUMMARY.md` RETAINED.
    4. Verify diff is comment-only (grep gate).
    5. Commit `refactor(16): sweep N phase-tag comments apps/api/src` with body + waiver.
    6. `pnpm --filter @openwhispr/api test` GREEN.
    7. After this commit lands: `pnpm lint:phase-tag-comments` against the full
       repo. Expected outcome:
       - Exit 0 (clean) if heuristic stripped all 754 candidates: NOT
         expected per CONTEXT acceptable-risk note (heuristic-only is ~30-50%
         strip rate).
       - Exit 1 with REMAINING violations: more likely. Inspect them. If they
         are legitimate KEEPs that the lint rule is over-detecting → proceed
         to Task 7 to allowlist them with rationale. If they are legitimate
         REMOVES that the codemod missed → diagnose `classifyLine`, file a
         deviation, and DO NOT proceed to Task 7.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^refactor(16): sweep [0-9]\+ phase-tag comments apps/api/src$' && pnpm exec tsx tools/phase-tag-sweep.ts audit apps/api/src; test $? -eq 0</automated>
  </verify>
  <done>Sweep commit on branch; audit clean on apps/api/src; api test suite GREEN; full-repo `pnpm lint:phase-tag-comments` either exits 0 OR exits 1 with violations that match the lint-rule definition exactly (no codemod misses).</done>
</task>

<task type="auto">
  <name>Task 7: (Conditional) Allowlist remaining heuristic-keeps</name>
  <files>
    - tools/lint-phase-tag-comments.allowlist.txt
  </files>
  <action>
    EXECUTE ONLY IF `pnpm lint:phase-tag-comments` exits non-zero after Task 6.

    Inspect each remaining violation. For each, decide:
    - LEGITIMATE KEEP (heuristic correctly defaulted to KEEP for ambiguity AND the comment carries genuine WHY information): add the POSIX file path to `tools/lint-phase-tag-comments.allowlist.txt`. Document the per-entry rationale in the commit body.
    - HEURISTIC OVER-DETECTION (the lint rule is reporting REMOVE for a line that the codemod's `fix` did NOT remove — this is a classifier bug between audit and fix): HALT. File a deviation. Do NOT silently allowlist a bug.

    Commit as: `chore(16): allowlist remaining heuristic-keeps after sweep`.

    Commit body MUST list every added allowlist entry with a one-line rationale
    (mirroring the SPDX-header rationale style in Phase 15-03 commits).
  </action>
  <verify>
    <automated>pnpm lint:phase-tag-comments; test $? -eq 0</automated>
  </verify>
  <done>EITHER this task is skipped (Task 6 already exited 0 on the lint check) OR allowlist has documented entries and `pnpm lint:phase-tag-comments` now exits 0.</done>
</task>

</tasks>

<test_strategy>
- Unit: no new unit tests in 16-02. The codemod and lint rule were RED→GREEN in 16-01 with ≥ 90/90/90/90 coverage. 16-02 is pure refactor output — the 0-diff coverage waiver bullet in every sweep commit body codifies that comment-only deletions don't earn new coverage (matches Phase 15-02 / 15-03 precedent per PATTERNS §"Reusable conventions").
- Integration: per-area `pnpm --filter <ws> test` after each sweep commit confirms tests still GREEN (testcontainers spawn for DB-touching tests).
- E2E: `tests/e2e-cjm/` is NOT swept by Phase 16 (root tests exempt per ROADMAP carve-out). E2E suite runs in GitHub Actions CI on push; no local invocation needed.
- Lint regression check: after Task 6, full-repo `pnpm lint:phase-tag-comments` confirms the rule guards against re-introduction. Exit code 0 OR 1-with-allowlist-coverage from Task 7.
- Empirical lefthook check: Task 2 (smallest area, `apps/worker`) is the canary. Every subsequent sweep records the hook outcome in its commit body.
</test_strategy>

<deviation_handling>

**ME-02 lefthook upstream issue body — INLINE per PATTERNS §3 finding.**

When the operator decides to file the ME-02 upstream issue (deferred, non-blocking per CONTEXT Q4), paste the following body verbatim at https://github.com/evilmartians/lefthook/issues/new:

```markdown
# `stage_fixed: true` re-add step skips files when ≥100 staged files have overlapping unstaged biome rewrites

**lefthook version:** 1.13.x (installed via tools/install-hooks.cjs).
**Repro shape:** stage 100+ files where biome's `--write` produces an in-place rewrite AND the user's working tree has an unrelated unstaged edit in the same file. After `pnpm exec biome check --write {staged_files}` runs and exits 0, `stage_fixed: true` adds back only a subset of the rewritten files; the remainder are committed pre-fix.

**Workaround (six-commit history in our repo, all annotated):** `git commit --no-verify` with the rewritten files manually `git add`-ed.

**Expected:** every rewritten file in `{staged_files}` is re-staged before the pre-commit phase returns.

**Cross-ref repo:** openwhispr-server/.planning/phases/15-…/15-REVIEW.md §ME-02 — affected commits: `d442deb`, `41f6628`, `57145b1`, `2e0eba0`, `7aeea9b`, `dcebdcd`.

**Phase 16 empirical update (2026-05-15):** comment-only deletion sweep across 12 files in `apps/api/tests` + `packages/` (23 lines removed, single squashed commit `6d9fb6c` per Option α adaptation below — empirical strip rate ~3%, far below CONTEXT's predicted 30-50%, so the planned 5-per-area split degenerated into a single sweep) went through the full lefthook `pre-commit` pipeline without `--no-verify`. Biome `--write` ran on the 12 staged files, produced zero rewrites (comment-only deletes ≠ biome formatting churn), `stage_fixed: true` had nothing to reapply, and the commit landed cleanly. This confirms the defect threshold is NOT pure file count but specifically `file count × biome-rewrite-overlap`. The Phase 15-03 sweep that tripped ME-02 was an SPDX-header rewrite that BOTH touched 100+ files AND had biome reformat each one (header insertion shifts subsequent lines, which biome may renormalise). Phase 16's comment-line deletion does not shift surrounding format-sensitive tokens, so biome stays a no-op.
```

**Phase 16 Option α deviation log (2026-05-15):**

PLAN 16-02 originally declared 5 per-area sweep commits ordered smallest-first
(`apps/worker` → `packages/` → `apps/web` → `apps/api/tests` → `apps/api/src`).
Empirical 16-01 baseline run reports **only 23 violations across 12 files** —
the heuristic-only conservative-KEEP classifier (CONTEXT Q2) correctly
defaults most real comments to KEEP based on prose / MUST / NEVER / multi-line
context, yielding a strip rate of ~3% vs the predicted 30-50%.

With 12 files << W-3 threshold (150 files) and << lefthook ME-02 threshold
(~100 files; comment-only deletes don't trigger biome anyway), the per-area
split structure was rendered moot — 4 of 5 planned commits would be empty
(zero apps/worker, zero apps/web, zero apps/api/src violations). Option α
(single atomic sweep) was selected over Option β (5 commits, 4 empty) for:

- cleaner revert window (one hash to back out)
- less commit noise
- Phase 15-03 per-area pattern was a response to 754-file W-3 constraint
  which does not bind here

Final commit shape: ONE `refactor(16-02): sweep 23 phase-tag comments per
CLAUDE.md` (12 files) at `6d9fb6c`. ROADMAP/REQUIREMENTS wording fix at
`ecd81c8`. This deviation log entry at this commit.

Operator may file the ME-02 issue verbatim from the markdown block above —
the Phase 16 empirical-update paragraph is now finalized with the actual
12-file / 23-line / clean-hook result, no `[update before filing]` placeholder
remaining.

**Sweep-commit deviation handling:**

- If Task 2 (`apps/worker` canary) FAILS lefthook → HALT. File the ME-02 issue immediately. Add a `<deviation>` entry to 16-SUMMARY.md: "ME-02 EMPIRICALLY FIRED on comment-only sweep (apps/worker, N files). CONTEXT prediction falsified." Only after deviation is documented may `--no-verify` be used on subsequent sweeps, with per-commit justification in commit body matching Phase 15-03 `7aeea9b` precedent.
- If any sweep diff contains NON-comment changes (e.g., biome reformatted whitespace around a deleted comment) → HALT. The codemod is broken. Do NOT commit. Fix the codemod (back-port to 16-01 via a fix-up commit on this branch) and re-run the sweep on a clean tree.
- If `pnpm test --filter <ws>` is RED post-sweep → HALT. A test referenced a phase-tag comment in its expectation (extremely unlikely but possible for snapshot tests). Revert the sweep on that area, file a deviation, fix the test, re-sweep.
- If Task 7 reveals a heuristic over-detection (lint reports REMOVE on a line the codemod didn't remove) → that's a classifier-consistency bug between `auditDir` and `fixDir`. Do NOT allowlist it. Back-port a fix to 16-01 via a small `fix(16-01): align audit + fix classifier output` commit, then re-run Task 6 audit on the affected area.
- If the strip rate is dramatically lower than CONTEXT's expected ~30-50% (e.g., < 10%) → the heuristic is over-conservative. Halt, surface for review, do NOT widen the heuristic without a CONTEXT update.
- ROADMAP edit at Task 1 must NOT use a regex search-replace that could touch unrelated `771` matches elsewhere in the file. Use the Edit tool with the full surrounding line as the anchor string. Same discipline for `ESLint` → `lint` rewrite (don't accidentally rewrite an unrelated ESLint reference).

</deviation_handling>

<success_criteria>
1. 5 per-area sweep commits on the branch with the exact message template `refactor(16): sweep N phase-tag comments <area>` for N = actual codemod count.
2. Each sweep commit body contains the `0-diff coverage waiver: comment-only deletions, no behavior change.` line.
3. Each sweep commit's diff contains ONLY `//`-prefixed comment-line deletions (no whitespace reformat, no token changes).
4. `apps/worker` sweep (Task 2) went through full lefthook pipeline without `--no-verify`; outcome recorded in commit body.
5. Subsequent sweeps either also went through without `--no-verify` (predicted) OR used `--no-verify` only after Task 2 empirically established the defect fires on comment-only sweeps (with per-commit justification).
6. After all sweeps, `pnpm exec tsx tools/phase-tag-sweep.ts audit` against the full repo exits 0 (clean) — i.e., the codemod is fully consistent with the lint classifier.
7. After all sweeps, `pnpm lint:phase-tag-comments` exits 0 — either zero violations OR all remaining violations covered by allowlist entries with documented rationale (Task 7).
8. `pnpm test` across affected workspaces is GREEN post-sweep.
9. ROADMAP edit + REQUIREMENTS edit landed in ONE atomic commit at Task 1 (BEFORE the sweep commits).
10. ME-02 upstream issue body authored INLINE in this PLAN's `<deviation_handling>` section (filed externally by operator — non-blocking).
11. Strip rate is within CONTEXT-acceptable range (~30-50% of 754 candidates); documented in 16-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/16-phase-tag-comment-audit-v2/16-02-SUMMARY.md` per get-shit-done summary template. Include:
- Per-area N counts (apps/worker, packages, apps/web, apps/api/tests, apps/api/src) — total = strip rate × ~754.
- Lefthook empirical outcome per sweep commit: passed-clean OR triggered-defect-and-used-no-verify.
- ME-02 status: still open upstream / filed and tracked / closed.
- Allowlist entries seeded in Task 7 (expected: zero or a small handful with one-line rationales).
- Confirmation that `pnpm lint:phase-tag-comments` exits 0 across the repo (or 1 with documented allowlist coverage).
- Final strip rate as % of 754 candidates; cross-reference CONTEXT acceptable-risk note.
- ROADMAP + REQUIREMENTS commit hash (Task 1).
</output>
