---
phase: 02.5
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/api/src/auth.ts
autonomous: true
requirements: [AUTH-01, AUTH-04, TDD-01b]
must_haves:
  truths:
    - "apps/api/src/auth.ts replaces the wildcard `import * as schema` with named imports of `{ users, sessions, accounts, verifications }` from `@openwhispr/data/schema`."
    - "drizzleAdapter is invoked with an explicit `schema: { user: users, session: sessions, account: accounts, verification: verifications }` map."
    - "Plan 01's unit test (`auth-schema-mapping.test.ts`) goes from 3 RED → 3 GREEN."
    - "All other API-package tests remain GREEN — no regression to better-auth handler bridge, trusted-origins, email-mailpit, rate-limit, or openwhispr-source-log tests."
    - "Coverage for the changed lines in auth.ts is ≥ 90% (TDD-01b per-phase floor on touched surface)."
  artifacts:
    - path: "apps/api/src/auth.ts"
      provides: "Better Auth instance factory with explicit canonical-name schema map"
      contains: "schema: {"
  key_links:
    - from: "apps/api/src/auth.ts buildAuth()"
      to: "packages/data/src/schema/{users,sessions,accounts,verifications}.ts"
      via: "named drizzle table imports"
      pattern: "import \\{ users, sessions, accounts, verifications \\} from \"@openwhispr/data/schema\""
    - from: "apps/api/src/auth.ts drizzleAdapter call"
      to: "Better Auth's model lookup at INSERT time"
      via: "schema object with canonical Better Auth keys"
      pattern: "schema: \\{[\\s\\S]*user: users[\\s\\S]*session: sessions[\\s\\S]*account: accounts[\\s\\S]*verification: verifications"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Land the production code change for D-01: explicit Better-Auth-canonical schema-key map. Turns Plan 01's RED unit test GREEN. Combined with Plan 02 (tenant default binding), the next contract-test signup attempt should succeed end-to-end.

Purpose: bridge the naming gap between Better Auth's canonical model names (`user`/`session`/`account`/`verification`) and our pluralized drizzle table exports (`users`/`sessions`/`accounts`/`verifications`) — without renaming the drizzle exports (which would ripple through every consumer including the RLS lint and the TENANT_SCOPED_TABLES literal).

Output:
- Modified `apps/api/src/auth.ts` with named imports + explicit schema map.
- Plan 01 unit test goes 3 RED → 3 GREEN.
- All other API-package tests stay GREEN.
- TDD-01b coverage gate ≥ 90% on the changed lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-CONTEXT.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-01-PLAN.md
@apps/api/src/auth.ts
@apps/api/src/__tests__/auth-trusted-origins.test.ts
@packages/data/src/schema/index.ts
@packages/data/src/schema/users.ts
@packages/data/src/schema/sessions.ts
@packages/data/src/schema/accounts.ts
@packages/data/src/schema/verifications.ts

<interfaces>
Current code in apps/api/src/auth.ts (lines 19-25 and 137-140 — exact strings to replace):

Line 24:
```ts
import * as schema from "@openwhispr/data/schema";
```

Lines 137-140:
```ts
    database: drizzleAdapter(db, {
      provider: "pg",
      schema,
    }),
```

Drizzle exports (named, pluralized) from `@openwhispr/data/schema`:
- `users` (pgTable name "users")
- `sessions` (pgTable name "sessions")
- `accounts` (pgTable name "account" — singular SQL)
- `verifications` (pgTable name "verification" — singular SQL)

Better Auth canonical model lookup keys (from Better Auth's drizzle adapter source):
- `user` / `session` / `account` / `verification` (all singular)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace wildcard schema import with explicit Better-Auth-canonical map</name>
  <files>apps/api/src/auth.ts</files>
  <read_first>
    - apps/api/src/auth.ts (full file — lines 1-188; pay special attention to lines 19-25 imports and 136-140 drizzleAdapter call)
    - apps/api/src/__tests__/auth-schema-mapping.test.ts (Plan 01's test — defines the contract this code must satisfy)
  </read_first>
  <behavior>
    - The `import * as schema from "@openwhispr/data/schema"` line is removed.
    - A named import `import { users, sessions, accounts, verifications } from "@openwhispr/data/schema"` is added in its place.
    - The `drizzleAdapter(db, { provider: "pg", schema })` call becomes `drizzleAdapter(db, { provider: "pg", schema: { user: users, session: sessions, account: accounts, verification: verifications } })`.
    - No other behavior in `buildAuth()` changes (plugins, session config, trustedOrigins, sendVerificationEmail all preserved).
    - A header comment block above the new imports / map references CONTEXT D-01 and explains why we don't rename the drizzle exports.
  </behavior>
  <action>
    Apply the following two edits to `apps/api/src/auth.ts`:

    **Edit 1** — replace line 24 (`import * as schema from "@openwhispr/data/schema";`) with the LITERAL block:

    ```ts
    // Phase 02.5 / Plan 03 / D-01 — Explicit Better-Auth-canonical schema map.
    //
    // Better Auth's drizzle adapter looks up models by their canonical (singular)
    // names: user / session / account / verification. Our drizzle exports are
    // pluralized (users / sessions / accounts / verifications). Renaming the
    // exports would ripple through TENANT_SCOPED_TABLES, the Plan 05 RLS lint,
    // every consumer of `@openwhispr/data/schema`, and the migration test fixtures
    // — not worth the blast radius for a name-mapping problem.
    //
    // Instead, bind the canonical names to the pluralized table objects below,
    // scoped only to the drizzleAdapter call. The drizzle exports keep their
    // pluralized names everywhere else.
    import {
      users,
      sessions,
      accounts,
      verifications,
    } from "@openwhispr/data/schema";
    ```

    **Edit 2** — replace lines 137-140 (`database: drizzleAdapter(db, { provider: "pg", schema })`) with the LITERAL block:

    ```ts
        database: drizzleAdapter(db, {
          provider: "pg",
          // Better Auth canonical model names (left) ↔ our pluralized drizzle
          // table objects (right). Phase 02.5 / Plan 03 / D-01.
          schema: {
            user: users,
            session: sessions,
            account: accounts,
            verification: verifications,
          },
        }),
    ```

    Then verify Plan 01's unit test goes GREEN:
    `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts 2>&1 | tee /tmp/02.5-03-task1-green.log`

    Then run the full API-package test suite as a regression gate:
    `pnpm -F @openwhispr/api test --run 2>&1 | tee /tmp/02.5-03-task1-regression.log`

    Pre-existing tests that MUST remain green:
    - apps/api/src/__tests__/auth-trusted-origins.test.ts
    - apps/api/src/__tests__/email-mailpit.test.ts
    - apps/api/src/__tests__/openwhispr-source-log.test.ts
    - apps/api/src/__tests__/rate-limit-*.test.ts
    - All other vitest files under apps/api/src/

    If any pre-existing test breaks because it relied on the wildcard `schema` import (e.g. used `schema.something` indirectly), do NOT revert; investigate and either (a) update the test to use named imports too, or (b) add the missing named export to the destructured list. Document the choice in the commit message body.
  </action>
  <verify>
    <automated>grep -q 'import \{$' apps/api/src/auth.ts && grep -qE 'users,\s*$' apps/api/src/auth.ts && grep -qE 'verifications,?\s*\}\s*from\s*"@openwhispr/data/schema"' apps/api/src/auth.ts && grep -q "user: users" apps/api/src/auth.ts && grep -q "session: sessions" apps/api/src/auth.ts && grep -q "account: accounts" apps/api/src/auth.ts && grep -q "verification: verifications" apps/api/src/auth.ts && ! grep -q 'import \* as schema from "@openwhispr/data/schema"' apps/api/src/auth.ts && pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts</antomated></automated>
  </verify>
  <done>
    - apps/api/src/auth.ts no longer contains `import * as schema`.
    - apps/api/src/auth.ts contains named imports of all four pluralized drizzle tables.
    - apps/api/src/auth.ts contains the explicit `schema: { user: users, session: sessions, account: accounts, verification: verifications }` map.
    - Plan 01's `auth-schema-mapping.test.ts` reports 3 passed / 0 failed.
    - Full API-package test suite: no new failures vs the pre-Plan-03 baseline.
  </done>
  <acceptance_criteria>
    - `grep -c "user: users\|session: sessions\|account: accounts\|verification: verifications" apps/api/src/auth.ts` returns 4.
    - `grep -c "import \* as schema" apps/api/src/auth.ts` returns 0.
    - Vitest exit code 0 on `auth-schema-mapping.test.ts` (3 GREEN).
    - Vitest exit code 0 on full `pnpm -F @openwhispr/api test --run` (no regressions).
    - TS compiles: `pnpm -F @openwhispr/api typecheck` (or `pnpm -F @openwhispr/api build`) succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Coverage gate ≥ 90% on the changed surface (TDD-01b)</name>
  <files>apps/api/src/auth.ts</files>
  <read_first>
    - apps/api/vitest.config.ts (or root vitest.config.ts — to confirm coverage tool + thresholds)
  </read_first>
  <action>
    PROJECT.md TDD-01b requires ≥ 90% coverage on the diff-touched surface in this phase.

    1. Run coverage on the API package, narrowed to auth.ts:
       `pnpm -F @openwhispr/api test --run --coverage 2>&1 | tee /tmp/02.5-03-task2-coverage.log`

    2. Inspect the coverage report (text-summary or lcov) for `src/auth.ts`. The Plan 01 unit test exercises the `database: drizzleAdapter(...)` block end-to-end (via the captured-args mock), and the existing `auth-trusted-origins.test.ts` exercises the `trustedOrigins` filter and the `readOidcProviders()` branch. Together this should achieve ≥ 90% on the lines changed in Task 1.

    3. If line coverage on `auth.ts` lines 19-30 (imports) + 136-148 (database block) is < 90%:
       - Add a fourth test to `auth-schema-mapping.test.ts`: assert that `buildAuth({ db: ..., email: ... })` returns an object with the expected `options` shape — exercises lines 96-184 of auth.ts.
       - Re-run coverage.

    4. Capture the coverage table for SUMMARY:
       `pnpm -F @openwhispr/api test --run --coverage --reporter=verbose 2>&1 | grep -A 2 "src/auth.ts"`
  </action>
  <verify>
    <automated>pnpm -F @openwhispr/api test --run --coverage 2>&1 | tee /tmp/02.5-03-task2-coverage.log | grep -E "auth\.ts" | awk -F'[|]' '{ for(i=1;i<=NF;i++) if($i ~ /[0-9]+\.[0-9]+/) { gsub(/[^0-9.]/,"",$i); if($i+0 >= 90) { found=1 } } } END { exit(found?0:1) }'</automated>
  </verify>
  <done>
    - Coverage report shows ≥ 90% line coverage on `apps/api/src/auth.ts`.
    - Coverage log captured at /tmp/02.5-03-task2-coverage.log for SUMMARY.
  </done>
  <acceptance_criteria>
    - The verify command exits 0 (≥ 90% on auth.ts).
    - If branch coverage on auth.ts is also reported and < 80%, document the gap in the commit message body and either backfill (preferred) or escalate to Plan 05 SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `git diff apps/api/src/auth.ts` shows exactly the two edits (named imports + explicit schema map). No other lines changed.
2. `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts` → 3 passed.
3. `pnpm -F @openwhispr/api test --run` → no regressions.
4. `pnpm -F @openwhispr/api test --run --coverage | grep auth.ts` → line% ≥ 90.

Single atomic commit:
```
feat(02.5-03): explicit Better Auth ↔ drizzle schema mapping (D-01)

apps/api/src/auth.ts:
- Replace `import * as schema` with named imports of users/sessions/accounts/verifications.
- Pass explicit `schema: { user, session, account, verification }` to drizzleAdapter.

Turns Plan 01 unit test 3 RED → 3 GREEN.
Coverage on auth.ts: NN.N% (≥ 90% per TDD-01b).

No drizzle export renames — keeps blast radius zero (RLS lint, TENANT_SCOPED_TABLES,
every other consumer untouched).
```
</verification>

<success_criteria>
- auth.ts modified per the two literal edits above.
- Plan 01 unit test GREEN.
- No regressions in API-package test suite.
- ≥ 90% line coverage on auth.ts.
- Combined with Plan 02 (tenant defaults), this should make the contract-test signup succeed (verified in Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-03-SUMMARY.md`.
</output>
