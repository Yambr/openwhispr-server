# Phase 02.5 — Reverse-patch evidence (Plan 05)

**HEAD at experiment time:** `f4fab70`
**Date:** 2026-05-09T22:28Z–22:30Z
**Operator:** Plan 02.5-05 executor
**Methodology:** for each scenario — back up file, edit in-place to revert one piece of the fix, re-run the corresponding RED test, capture output, restore from backup, verify clean diff. No commits made during experiments.

The three experiments below independently prove that each piece of the Phase 02.5 fix is load-bearing: removing any one piece re-creates a specific test failure that is closed by the corresponding GREEN plan. (Scenario 4 — the live `make contract-test` signup — was already documented in `02.6-SUMMARY.md` and is referenced rather than re-run here.)

---

## Experiment 1 — Revert Plan 03 (auth.ts schema map)

**Reverts:** `apps/api/src/auth.ts` named-import block + explicit `schema: { user, session, account, verification }` map → restored to pre-Plan-03 wildcard `import * as schema` + `schema` shorthand.

**Diff applied (temporary, then reverted):**

```diff
-import { accounts, sessions, users, verifications } from "@openwhispr/data/schema";
+import * as schema from "@openwhispr/data/schema";
@@
-    database: drizzleAdapter(db, {
-      provider: "pg",
-      schema: {
-        user: users,
-        session: sessions,
-        account: accounts,
-        verification: verifications,
-      },
-    }),
+    database: drizzleAdapter(db, {
+      provider: "pg",
+      schema,
+    }),
```

**Command:** `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts`

**Captured /tmp/02.5-05-rev1.log (key excerpts):**

```
 FAIL  src/__tests__/auth-schema-mapping.test.ts > buildAuth schema-key mapping (D-01) > passes schema with Better Auth canonical keys user/session/account/verification
 FAIL  src/__tests__/auth-schema-mapping.test.ts > buildAuth schema-key mapping (D-01) > maps each canonical key to the matching drizzle table by reference identity
 FAIL  src/__tests__/auth-schema-mapping.test.ts > buildAuth schema-key mapping (D-01) > does not leak the pluralized drizzle export names into the schema arg

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯
 ❯ src/__tests__/auth-schema-mapping.test.ts:57:35
     55|   it("maps each canonical key to the matching drizzle table by referen…
     56|     // captured by the previous test's buildAuth call (mocks persist a…
     57|     expect(captured.schema!.user).toBe(users);
       |                                   ^

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯
 ❯ src/__tests__/auth-schema-mapping.test.ts:65:31
     63|   it("does not leak the pluralized drizzle export names into the schem…
     64|     const keys = new Set(Object.keys(captured.schema!));
     65|     expect(keys.has("users")).toBe(false);
       |                               ^

 Test Files  1 failed (1)
      Tests  3 failed (3)
   Duration  471ms
```

**Result:** 3/3 tests RED — exact failure mode reproduced. The pluralized drizzle exports leak into the schema arg (`captured.schema.users` populated, `captured.schema.user` undefined), Better Auth would resolve `model "user"` lookup against this object as `undefined` — i.e. the canonical CONTEXT.md error string `[# Drizzle Adapter]: The model "user" was not found in the schema object`. Restored.

**Live-stack corroboration (cross-reference, not re-run):** `02.5-04-CONTRACT-TEST-RESULT.md` documents that pre-Plan-03 the seed step crashed at exactly this lookup; post-Plan-03 the same suite progresses past the schema lookup into `findOne` (where the unrelated wrapper-leak surfaced — closed by Phase 02.6).

**Restore verification:** `git diff --stat apps/api/src/auth.ts` empty; subsequent re-run of the unit test would return 3/3 GREEN (Plan 03 SUMMARY witnesses 3 PASS at this HEAD).

---

## Experiment 2 — Revert Plan 02 ALTER ROLE only (keep column DEFAULTs)

**Reverts:** `packages/data/migrations/0003_better_auth_tenant_defaults.sql` `DO $$ ... ALTER ROLE openwhispr_app SET app.tenant_id ... END $$;` block commented out; column DEFAULTs preserved.

**Diff applied (temporary, then reverted):**

```diff
-DO $$
-BEGIN
-  IF EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'openwhispr_app') THEN
-    EXECUTE $sql$ALTER ROLE openwhispr_app SET app.tenant_id TO '00000000-0000-0000-0000-000000000000'$sql$;
-  END IF;
-END $$;
+-- EXPERIMENT 2 (REVERTED ALTER ROLE BLOCK):
+-- (block commented; replaced with a no-op SELECT 1 to keep the breakpoint)
+SELECT 1;
```

Column DEFAULTs (`ALTER TABLE … SET DEFAULT current_setting('app.tenant_id', true)::uuid`) untouched.

**Command:** `pnpm -F @openwhispr/data test --run src/__tests__/0003_better_auth_tenant_defaults.test.ts`

**Captured /tmp/02.5-05-rev2.log (key excerpts):**

```
 FAIL  src/__tests__/0003_better_auth_tenant_defaults.test.ts > migration 0003_better_auth_tenant_defaults > applies ALTER ROLE openwhispr_app SET app.tenant_id

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/5]⎯
- ArrayContaining [
-   "app.tenant_id=00000000-0000-0000-0000-000000000000",
- ]
+ []

 ❯ src/__tests__/0003_better_auth_tenant_defaults.test.ts:48:42
     46|         `SELECT rolconfig FROM pg_roles WHERE rolname = 'openwhispr_ap…

 FAIL  > INSERT INTO users without tenant_id picks up default-tenant via column DEFAULT
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO sessions without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO account without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO verification without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "verification"

 Test Files  1 failed (1)
      Tests  5 failed (5)
   Duration  4.02s
```

**Result:** 5/5 tests RED. Test 1 (the direct ALTER-ROLE assertion) fails because `pg_roles.rolconfig` is empty — no role default. Tests 2–5 cascade-fail with `new row violates row-level security policy` because, although the column DEFAULT expression `current_setting('app.tenant_id', true)::uuid` still evaluates, with no role default and no per-request `SET LOCAL` the GUC returns the empty string, the `::uuid` cast yields NULL, the FORCE-RLS `USING/WITH CHECK (tenant_id = current_setting(...))` clause rejects the INSERT. This proves the ALTER ROLE is independently load-bearing — the column DEFAULT alone is insufficient. Restored.

**Restore verification:** `cp /tmp/02.5-05-mig0003-backup.sql packages/data/migrations/0003_better_auth_tenant_defaults.sql`; `git diff --stat` empty.

---

## Experiment 3 — Revert Plan 02 column DEFAULTs only (keep ALTER ROLE)

**Reverts:** the four `ALTER TABLE … SET DEFAULT current_setting('app.tenant_id', true)::uuid` lines commented out; ALTER ROLE preserved.

**Diff applied (temporary, then reverted):**

```diff
-ALTER TABLE "users"        ALTER COLUMN "tenant_id" SET DEFAULT current_setting('app.tenant_id', true)::uuid;
---> statement-breakpoint
-ALTER TABLE "sessions"     ALTER COLUMN "tenant_id" SET DEFAULT current_setting('app.tenant_id', true)::uuid;
---> statement-breakpoint
-ALTER TABLE "account"      ALTER COLUMN "tenant_id" SET DEFAULT current_setting('app.tenant_id', true)::uuid;
---> statement-breakpoint
-ALTER TABLE "verification" ALTER COLUMN "tenant_id" SET DEFAULT current_setting('app.tenant_id', true)::uuid;
+-- EXPERIMENT 3 (REVERTED COLUMN DEFAULTs):
+-- (4× ALTER COLUMN commented; replaced with no-op SELECT 1)
+SELECT 1;
```

ALTER ROLE block untouched.

**Command:** `pnpm -F @openwhispr/data test --run src/__tests__/0003_better_auth_tenant_defaults.test.ts`

**Captured /tmp/02.5-05-rev3.log (key excerpts):**

```
 ✓ migration 0003_better_auth_tenant_defaults > applies ALTER ROLE openwhispr_app SET app.tenant_id
 FAIL  > INSERT INTO users without tenant_id picks up default-tenant via column DEFAULT
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO sessions without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO account without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "users"
 FAIL  > INSERT INTO verification without tenant_id picks up default-tenant
error: new row violates row-level security policy for table "verification"

 Test Files  1 failed (1)
      Tests  4 failed | 1 passed (5)
   Duration  4.04s
```

**Result:** 4/5 RED, 1/5 GREEN. The ALTER-ROLE assertion (test 1) PASSES — `pg_roles.rolconfig` correctly contains `app.tenant_id=00000000-…-000`. Tests 2–5 fail with the same RLS violation: even though the GUC IS bound at session time, Postgres does NOT auto-bind GUCs to columns without an explicit `DEFAULT` expression. Bare `INSERT INTO users (email) VALUES (…)` supplies NULL for `tenant_id`, the FORCE-RLS WITH CHECK rejects it. This proves the column DEFAULTs are independently load-bearing — the role default alone is insufficient. Restored.

**Restore verification:** second `cp` from the backup; `git diff --stat` empty.

---

## Experiment 4 (cross-reference, not re-run) — Revert Phase 02.6 entrypoint destructure

**Reverts:** `apps/api/src/index.ts:229–233` `const { db } = makeAppDb();` → `const db = makeAppDb();` (restore wrapper-leak with `as never` casts).

**Status:** Already independently captured by Phase 02.6's `entrypoint-db-shape.test.ts` reverse-patch run; documented verbatim in `02.6-SUMMARY.md` § "RED → GREEN sequence" step 3. Re-cited here for completeness:

```
× destructures makeAppDb() ... AssertionError: expected 'undefined' to be 'function'
× the entrypoint's single destructured db variable ... expected { db: {…}, pool: {…} } to be { select: [Function], … }
Test Files  1 failed (1) / Tests  2 failed (2)
```

This was the third and final cascade defect that prevented `make contract-test` seed from succeeding even after the Phase 02.5 schema-map + tenant-default fixes landed. Closed by `450ce23`.

---

## Final state

```
$ git diff --stat -- apps/api/src/auth.ts packages/data/migrations/0003_better_auth_tenant_defaults.sql
(empty — clean restore)

$ git rev-parse --short HEAD
f4fab70
```

All three experiments executed, each with a captured failure log, each demonstrating that the corresponding piece of the Phase 02.5 fix is independently load-bearing. CONTEXT.md acceptance gate item 5 satisfied.
