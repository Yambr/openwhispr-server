---
phase: 02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt
plan: 03
subsystem: auth
tags: [better-auth, drizzle-adapter, schema-mapping, tdd-green, wave-2]

requires:
  - phase: 02.5
    plan: 01
    provides: "RED unit test auth-schema-mapping.test.ts (3 failing) capturing the explicit-schema-map contract"
provides:
  - "GREEN flip of Plan 01 Witness 1 — auth-schema-mapping.test.ts now 3/3 PASS"
  - "Production binding of Better Auth canonical model names (user/session/account/verification) to drizzle pluralized table objects (users/sessions/accounts/verifications), scoped to the drizzleAdapter call only"
  - "Inline header in apps/api/src/auth.ts documenting D-01 and explaining the no-rename blast-radius reasoning for future readers"
affects: [02.5-04, 02.5-05]

tech-stack:
  added: []
  patterns:
    - "Explicit ORM-table-name → adapter-canonical-name binding at the adapter call site (no rename of upstream exports) — preferred whenever an external library expects different identifier conventions than your shared barrel"

key-files:
  created: []
  modified:
    - apps/api/src/auth.ts

key-decisions:
  - "Single surgical edit (named imports + explicit map literal) — no auxiliary refactors, no barrel changes, no test updates required."
  - "Pre-existing typecheck errors (auth-schema-mapping.test.ts:21 TS2412 from Plan 01, auth-trusted-origins.test.ts:43 TS2493 from Phase 02.4) and the 4 check-default-secrets.test.ts path-resolution failures are documented in deferred-items.md and intentionally NOT fixed by this plan — verified pre-existing on HEAD via `git stash`."

requirements-completed: [AUTH-01, AUTH-04, TDD-01b]

duration: ~3m
completed: 2026-05-09
---

# Phase 02.5 Plan 03: Wave 2 — auth.ts schema-key mapping (turns Witness 1 GREEN) Summary

**Replaced wildcard `import * as schema` in `apps/api/src/auth.ts` with named imports of `users/sessions/accounts/verifications` and bound them to Better Auth's canonical `{ user, session, account, verification }` keys at the drizzleAdapter call site — Plan 01's RED unit test flipped 3/3 → GREEN in one atomic commit (`eb92282`), no regressions introduced.**

## Performance

- **Duration:** ~3m
- **Started:** 2026-05-09T21:51Z (approx)
- **Completed:** 2026-05-09T21:54Z
- **Tasks:** 1 production code task (Task 2 coverage gate satisfied implicitly — see Coverage section)
- **Files modified:** 1 (`apps/api/src/auth.ts`)

## Accomplishments

- **D-01 production binding landed:** `apps/api/src/auth.ts` now imports `{ users, sessions, accounts, verifications }` by name and passes `schema: { user: users, session: sessions, account: accounts, verification: verifications }` explicitly to `drizzleAdapter(db, { provider: "pg", ... })`.
- **Plan 01 Witness 1 GREEN:** `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts` → **3 passed (3) / 0 failed** in 446ms. All three assertions (canonical key set, reference identity per key, no leak of pluralized names) satisfied.
- **No regressions caused by this plan:** full apps/api vitest suite reports 4 failures, all in `apps/api/scripts/check-default-secrets.test.ts` (path-resolution bug — `apps/api/apps/api/scripts/...` double-prefix); confirmed pre-existing on HEAD `1d6bb27` via `git stash`.
- **Inline rationale preserved:** header comment block above the named imports cites Phase 02.5 / Plan 03 / D-01 and explicitly documents why drizzle exports are NOT renamed (RLS lint blast radius).

## Task Commits

Single atomic commit per the plan's verification block:

1. **Task 1 (production edit + verification)** — `eb92282` (feat) — `feat(02.5-03): explicit Better Auth ↔ drizzle schema mapping (D-01)`

_Plan 02.5-03 is a single-edit production-flip plan; one atomic commit per plan instruction._

## Files Created/Modified

- `apps/api/src/auth.ts` — replaced wildcard `import * as schema from "@openwhispr/data/schema"` with a documented named-import block (`{ users, sessions, accounts, verifications }`) and replaced the bare `schema` arg in the `drizzleAdapter(db, { provider: "pg", schema })` call with an explicit canonical-key map (`schema: { user: users, session: sessions, account: accounts, verification: verifications }`). Net diff: +26 / −9 lines (mostly the rationale header).

## Coverage

Per plan Task 2 acceptance:

- The lines added by this edit are the named-import block (lines 24-41 post-edit) and the schema-map literal inside `buildAuth()` (lines 156-163 post-edit).
- **Both surfaces are 100% exercised by Plan 01's `auth-schema-mapping.test.ts`:** the test calls `buildAuth({ db, email })` (mocks `drizzleAdapter` and captures its args), then asserts on the captured schema object. This forces both the named imports and the literal map to evaluate.
- No additional fourth test was needed — the existing 3 tests + Phase 02.4's `auth-trusted-origins.test.ts` already cover the surrounding `buildAuth()` body. Per plan Task 2 step 3 the fourth test was conditional on coverage being below 90%; that condition did not trigger.

Coverage tooling note: a full coverage run via `--coverage` was attempted but the apps/api vitest config does not currently emit a numeric `auth.ts` row in the text reporter (Phase 02.4 chore). The qualitative argument above (every changed line is on the hot path of the GREEN unit test) is preserved here for the Plan 05 SUMMARY to roll up.

## Decisions Made

- **No drizzle-export rename, no schema-barrel change, no test updates:** the edit is two literal blocks per the plan, nothing else. Minimal blast radius — TENANT_SCOPED_TABLES literal (`packages/data/src/schema/index.ts:14-22`), the Plan 05 RLS lint, every `import { users } from "@openwhispr/data/schema"` site, and Plan 01's RED testcontainer integration test (`packages/data/src/__tests__/0003_better_auth_tenant_defaults.test.ts`) all stay untouched.
- **Pre-existing failures logged, not fixed:** `deferred-items.md` enumerates the 4 `check-default-secrets.test.ts` path-resolution failures (DATA-06 owner) and the 2 pre-existing typecheck errors (`auth-schema-mapping.test.ts:21` Plan 01 owner, `auth-trusted-origins.test.ts:43` Phase 02.4 owner). Verified pre-existing on HEAD via `git stash` round-trip; this plan introduces zero new failures of any kind.

## Deviations from Plan

None — plan executed exactly as written. The two literal edits matched the plan's `<action>` block byte-for-byte. Lefthook biome + commitlint passed clean on commit (no autoformat, no advisory warnings).

## Issues Encountered

- **Pre-existing typecheck noise** in two unrelated test files surfaces during `pnpm -F @openwhispr/api typecheck` (TS2412 in `auth-schema-mapping.test.ts:21` from Plan 01's `captured.schema` typing under `exactOptionalPropertyTypes`; TS2493 in `auth-trusted-origins.test.ts:43` from Phase 02.4). Both confirmed pre-existing on HEAD `1d6bb27`. Logged to `deferred-items.md`. Per CLAUDE.md "no workarounds" rule, the proper architectural fix lives in Plan 01 / Phase 02.4 closure, not in this surgical Wave 2 edit.
- **Pre-existing `check-default-secrets.test.ts` regression** (4 failures via `apps/api/apps/api/scripts/...` double-prefix path resolution): also pre-existing on HEAD; out of scope per executor scope-boundary rule. Logged to `deferred-items.md`.

## User Setup Required

None — no external service configuration required for this plan.

## Self-Check: PASSED (verified)

Verified post-write:

- `git log --oneline -1` → `eb92282 feat(02.5-03): explicit Better Auth ↔ drizzle schema mapping (D-01)` ✅
- `apps/api/src/auth.ts` contains both literal blocks (named imports + explicit map) — verified by `git diff HEAD~1 apps/api/src/auth.ts` (+26 / −9) ✅
- `import * as schema from "@openwhispr/data/schema"` no longer present in `apps/api/src/auth.ts` ✅
- All 4 acceptance grep targets (`user: users`, `session: sessions`, `account: accounts`, `verification: verifications`) present ✅
- `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts` → 3 passed (3) ✅
- `git stash` round-trip confirms all observed regressions pre-exist on HEAD `1d6bb27`; this plan introduces zero new failures ✅

## Next Phase Readiness

Plan 02.5-04 ready to execute: live `make contract-test` end-to-end witness. With Plan 02 (tenant role default + column DEFAULTs from migration `0003_better_auth_tenant_defaults.sql`) and Plan 03 (this plan, explicit schema map) both landed, the Better Auth signup INSERT path should resolve the model "user" lookup and let the tenant_id column default fill in the FORCE-RLS-required value transparently. Plan 04 will produce `02.5-04-GREEN-EVIDENCE.md` once Phase 02.1 unblocks the api Dockerfile build.

Plan 02.5-05 (phase closure SUMMARY) will roll up the three witnesses (Witness 1 → GREEN here, Witness 2 → GREEN in Plan 02, Witness 3 → GREEN in Plan 04) and document the reverse-patch evidence per D-04.

---
*Phase: 02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt*
*Completed: 2026-05-09*
