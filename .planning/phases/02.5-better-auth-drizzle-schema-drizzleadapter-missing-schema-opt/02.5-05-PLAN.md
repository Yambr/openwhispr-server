---
phase: 02.5
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md
  - .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md
  - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md
autonomous: true
requirements: [TDD-01, TDD-01b, CONTRACT-01]
must_haves:
  truths:
    - "A phase SUMMARY (02.5-SUMMARY.md) exists and traces every locked decision (D-01 through D-06) to the plan + commit + test evidence."
    - "A reverse-patch evidence document (02.5-REVERSE-PATCH-EVIDENCE.md) records three independent revert experiments, each demonstrating that removing one piece of the fix re-creates a specific test failure."
    - "If Plan 04 status was GREEN: 02-HUMAN-UAT.md Item 1 is flipped to ✅ with the captured contract-test evidence cited."
    - "If Plan 04 status was PARTIAL or BLOCKED: 02-HUMAN-UAT.md Item 1 stays unflipped, the SUMMARY documents what blocks it, and STATE.md flags the next phase needed (e.g. Phase 02.1)."
  artifacts:
    - path: ".planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md"
      provides: "Phase 02.5 end-to-end summary, decision traceability, coverage table, follow-up items"
    - path: ".planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md"
      provides: "3-scenario reverse-patch experiment log proving each piece of the fix is load-bearing"
  key_links:
    - from: "02.5-SUMMARY.md decision matrix"
      to: "Plans 01..04 + their commits"
      via: "explicit table mapping D-01..D-06 → plan/commit/test"
      pattern: "D-0[1-6].*02\\.5-0[1-4]"
    - from: "02-HUMAN-UAT.md Item 1"
      to: "02.5-04-CONTRACT-TEST-RESULT.md GREEN witness"
      via: "result: pass + cite of evidence file"
      pattern: "result: pass"
---

<objective>
Phase-closure plan. Produces the SUMMARY, runs the three reverse-patch experiments per CONTEXT.md acceptance gate item 5, and (conditionally) flips 02-HUMAN-UAT.md Item 1 — the Phase 2 acceptance gate that has been blocked since Phase 02.

No new feature code. This plan is documentation + reverse-patch verification + UAT bookkeeping.

Purpose: close Phase 02.5 cleanly so the project can advance to Phase 3 with full traceability and zero hand-waving.

Output:
- 02.5-SUMMARY.md
- 02.5-REVERSE-PATCH-EVIDENCE.md
- 02-HUMAN-UAT.md Item 1 flipped (if GREEN) or status-noted (if not)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-CONTEXT.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-01-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-02-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-03-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md

<interfaces>
02-HUMAN-UAT.md Item 1 current shape:
```
### 1. Run `make contract-test` against a live docker-compose stack
expected: All 8 conformance test files pass ...
result: blocked
notes: |
  Auto-execution attempted 2026-05-09T11:55Z; failed at `docker compose up --wait` ...
```

To flip on GREEN, change `result: blocked` → `result: pass` and append a new note line:
```
  2026-05-09T<HH:MM>Z — Phase 02.5 closed: schema mapping (Plan 03) + tenant default
  binding (Plan 02) landed; `make contract-test` runs end-to-end. Evidence:
  .planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three reverse-patch experiments</name>
  <files>.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/02.5-.../02.5-CONTEXT.md (Acceptance gate item 5: "Reverse-patch evidence: removing the schema mapping breaks the contract test (red); removing the column DEFAULT breaks the tenant-default-binding test (red)")
    - apps/api/src/auth.ts (post-Plan-03 state — to know what to revert)
    - packages/data/migrations/0003_better_auth_tenant_defaults.sql (post-Plan-02 state — to know what to revert)
  </read_first>
  <action>
    Per CONTEXT acceptance gate item 5, run THREE independent reverse-patch experiments. Each experiment: revert one specific piece, re-run the test that should now fail, capture the failure, then restore the file. Use a scratch git worktree or `git stash` to keep the main tree clean.

    Each experiment requires NO commits — they are temporary reverts that get undone immediately. Capture stdout/stderr only.

    **Experiment 1 — Revert Plan 03 (schema map):**
    1. `git stash` the current working tree (should be clean post-Plan-04, but defensive).
    2. Edit `apps/api/src/auth.ts`: replace the named-import block + explicit schema map with the pre-Plan-03 state — `import * as schema from "@openwhispr/data/schema"` and `database: drizzleAdapter(db, { provider: "pg", schema })`.
    3. Run `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts 2>&1 | tee /tmp/02.5-05-rev1.log` — MUST FAIL (3 RED).
    4. Optional but recommended: rebuild the api image and re-run `make contract-test` to confirm seed re-fails with `model "user" was not found`. This is the canonical proof from CONTEXT acceptance gate.
    5. `git checkout apps/api/src/auth.ts` to restore.

    **Experiment 2 — Revert Plan 02 ALTER ROLE only (keep column DEFAULTs):**
    1. Edit `packages/data/migrations/0003_better_auth_tenant_defaults.sql`: comment out the entire `DO $$ BEGIN ... ALTER ROLE openwhispr_app SET app.tenant_id ... END $$;` block (lines containing the ALTER ROLE).
    2. Drop the testcontainer DB volume: this isn't a normal scenario — testcontainers spins fresh containers per test run, so the next vitest run will pick up the modified migration automatically.
    3. Run `pnpm -F @openwhispr/data test --run src/__tests__/0003_better_auth_tenant_defaults.test.ts 2>&1 | tee /tmp/02.5-05-rev2.log` — the ALTER ROLE assertion test MUST FAIL; the four INSERT tests MAY also fail because `current_setting('app.tenant_id', true)` returns empty string with no role default, casting to uuid fails.
    4. `git checkout packages/data/migrations/0003_better_auth_tenant_defaults.sql` to restore.

    **Experiment 3 — Revert Plan 02 column DEFAULTs only (keep ALTER ROLE):**
    1. Edit `packages/data/migrations/0003_better_auth_tenant_defaults.sql`: comment out the four `ALTER TABLE ... ALTER COLUMN tenant_id SET DEFAULT ...` lines.
    2. Run `pnpm -F @openwhispr/data test --run src/__tests__/0003_better_auth_tenant_defaults.test.ts 2>&1 | tee /tmp/02.5-05-rev3.log` — the four INSERT tests MUST FAIL with `null value in column "tenant_id" of relation ... violates not-null constraint` (the role default sets the GUC but Postgres doesn't auto-bind GUC to columns without an explicit DEFAULT).
    3. `git checkout packages/data/migrations/0003_better_auth_tenant_defaults.sql` to restore.

    Final state: `git status` MUST be clean (or only contain the new evidence file from Task 2 below — handle ordering carefully).

    Write `.planning/phases/02.5-.../02.5-REVERSE-PATCH-EVIDENCE.md` containing:
    - Heading + HEAD SHA at experiment time
    - Experiment 1 section: revert action, expected failure, captured /tmp/02.5-05-rev1.log contents (full)
    - Experiment 2 section: same shape
    - Experiment 3 section: same shape
    - Final assertion that `git diff --stat` post-restore is empty (proves clean restore).
  </action>
  <verify>
    <automated>test -s .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md && grep -c "## Experiment" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md | grep -q '^3$' && grep -qE "(model \"user\" was not found|null value in column \"tenant_id\")" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-REVERSE-PATCH-EVIDENCE.md && git diff --stat -- apps/api/src/auth.ts packages/data/migrations/0003_better_auth_tenant_defaults.sql | grep -qE '^$' || true</automated>
  </verify>
  <done>
    - Three experiments executed, each with captured failure log.
    - Evidence file contains all three sections + their captured logs.
    - All reverted files restored — `git status` shows no modifications to apps/api/src/auth.ts or packages/data/migrations/0003_better_auth_tenant_defaults.sql.
  </done>
  <acceptance_criteria>
    - `grep -c "^## Experiment" .planning/phases/02.5-.../02.5-REVERSE-PATCH-EVIDENCE.md` returns 3.
    - At least one of the three failure modes named in CONTEXT acceptance gate item 5 is captured verbatim in the evidence file.
    - Post-restore vitest run on both test files (auth-schema-mapping + 0003_better_auth_tenant_defaults) is GREEN — confirms restore is clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write 02.5-SUMMARY.md with full decision traceability</name>
  <files>.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md</files>
  <read_first>
    - All Plan 01-04 SUMMARYs (if execute-plan created them per its convention) in .planning/phases/02.5-.../
    - 02.5-CONTEXT.md (the locked decisions D-01..D-06)
    - 02.5-04-CONTRACT-TEST-RESULT.md (the GREEN witness)
    - 02.5-REVERSE-PATCH-EVIDENCE.md (just written in Task 1)
  </read_first>
  <action>
    Write `.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md` with this LITERAL structure (fill in the actual values from prior plans):

    ```markdown
    # Phase 02.5 — SUMMARY

    **Status:** <CLOSED-GREEN | CLOSED-PARTIAL | OPEN-BLOCKED>
    **Plans landed:** 5 (01 RED tests, 02 migration 0003, 03 auth.ts schema map, 04 contract-test verification, 05 SUMMARY+evidence)
    **Commits:** <list short SHAs of all 5 commits>
    **Duration:** <plan time spans>

    ## Outcome

    <One paragraph: did `make contract-test` end-to-end pass? Did 02-HUMAN-UAT Item 1 flip? Are there any residual blockers (Phase 02.1, etc.)?>

    ## Decision Traceability

    | Decision | Description | Implemented in | Test evidence | Reverse-patch witness |
    |----------|-------------|----------------|---------------|------------------------|
    | D-01 | Explicit Better Auth ↔ drizzle schema-key map | Plan 03 (apps/api/src/auth.ts) | apps/api/src/__tests__/auth-schema-mapping.test.ts (3 GREEN) | Experiment 1 in REVERSE-PATCH-EVIDENCE.md |
    | D-02 | ALTER ROLE openwhispr_app SET app.tenant_id default | Plan 02 (migration 0003) | 0003_better_auth_tenant_defaults.test.ts test 1 (GREEN) | Experiment 2 |
    | D-03 | Column DEFAULT current_setting('app.tenant_id', true)::uuid on 4 BA tables | Plan 02 (migration 0003) | 0003_better_auth_tenant_defaults.test.ts tests 2-5 (GREEN) | Experiment 3 |
    | D-04 | TDD discipline (RED before GREEN) | Plan 01 (tests) precedes Plans 02/03 (production) in commit history | All Plan 01 tests started RED, captured in /tmp/02.5-01-task*-red.log | N/A — process check |
    | D-05 | New migration 0003 (NOT 0002 — that slot is occupied by oauth_state) + journal append | Plan 02 | jq '.entries[3].tag == "0003_better_auth_tenant_defaults"' on _journal.json | N/A — schema check |
    | D-06 | Out-of-scope items deferred (true multi-tenant per-request SET LOCAL, OIDC E2E, etc.) | N/A — explicit non-implementation | N/A | Documented below |

    ## GUC Naming Correction

    CONTEXT.md D-02/D-03 referenced GUC `app.current_tenant`. Production reality
    (RLS policies in 0000_initial.sql / 0001_better_auth.sql / 0002_oauth_state.sql)
    uses `app.tenant_id`. Plans 02-03 implemented against `app.tenant_id` to
    align with existing FORCE RLS — the locked decision intent (role default +
    column DEFAULT bound to GUC) was preserved at full strength. CONTEXT.md
    should be amended to read `app.tenant_id` in D-02/D-03 (defer to next
    Phase 2 follow-up if desired).

    ## Coverage

    - apps/api/src/auth.ts: <NN.N>% lines (≥ 90% per TDD-01b ✅)
    - packages/data/migrations/0003_better_auth_tenant_defaults.sql: covered by 5 testcontainer assertions (≥ 90% statement coverage by inspection ✅)
    - Aggregate phase touched-surface coverage: <NN.N>% (≥ 90% per TDD-01b ✅ | ❌ — escalate if below)

    ## Deferred (D-06 explicit non-goals)

    - True multi-tenant per-request SET LOCAL Fastify hook (deferred to a future phase when end-user signup picks tenant).
    - Better Auth session.user / account.userId tenant inheritance verification (RLS already enforces via FK chain).
    - Multi-default-tenant testing (v1 single-tenant only).
    - OIDC end-to-end (Phase 02 Plan 05 wires OIDC redirect; this phase only fixed email/password).
    - Reformatting CONTEXT.md to use canonical GUC name `app.tenant_id` (cosmetic).

    ## Follow-ups

    <If contract-test status was PARTIAL: list any conformance test files that still fail and why; recommend the next phase.>
    <If status was BLOCKED: name the blocker (Phase 02.1 Dockerfile, etc.) and the path forward.>

    ## Acceptance Gate Verification (per CONTEXT.md)

    | Gate item | Status | Evidence |
    |-----------|--------|----------|
    | 1. `pnpm test --coverage` ≥ 90% on touched surface | <PASS|FAIL> | Plan 03 Task 2 coverage log |
    | 2. `make contract-test` runs end-to-end without auth errors | <PASS|PARTIAL|BLOCKED> | 02.5-04-CONTRACT-TEST-RESULT.md |
    | 3. 02-HUMAN-UAT.md Item 1 flippable | <YES|NO — reason> | Task 3 below |
    | 4. No production-side regressions; full vitest green | <PASS|FAIL> | Plan 02 Task 2 + Plan 03 Task 1 regression logs |
    | 5. Reverse-patch evidence (3 scenarios) | PASS | 02.5-REVERSE-PATCH-EVIDENCE.md |
    ```

    Fill in every angle-bracket placeholder with the actual value from prior plans' artefacts. Do NOT leave placeholders in the committed file.
  </action>
  <verify>
    <automated>test -s .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md && grep -q "Decision Traceability" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md && grep -qE "D-0[1-6]" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md && ! grep -q "<NN" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md && ! grep -qE "<(YES|NO|PASS|FAIL)" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-SUMMARY.md</automated>
  </verify>
  <done>
    - 02.5-SUMMARY.md exists with all sections.
    - All angle-bracket placeholders replaced with real values.
    - All six decisions D-01..D-06 traced.
    - Coverage numbers cited.
    - Acceptance gate table filled in.
  </done>
  <acceptance_criteria>
    - `grep -c "^| D-0" .planning/phases/02.5-.../02.5-SUMMARY.md` returns 6 (one row per decision).
    - File contains either "CLOSED-GREEN", "CLOSED-PARTIAL", or "OPEN-BLOCKED" status.
    - File references the REVERSE-PATCH-EVIDENCE.md and 02.5-04-CONTRACT-TEST-RESULT.md by path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Conditionally flip 02-HUMAN-UAT.md Item 1</name>
  <files>.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md (current Item 1 — `result: blocked`)
    - .planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md (Task 1's evidence — determines flip vs no-flip)
  </read_first>
  <action>
    Read 02.5-04-CONTRACT-TEST-RESULT.md and check the Status header.

    **If Status: GREEN** — flip Item 1:
    1. Edit `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md`:
       - Change `result: blocked` (under `### 1. Run \`make contract-test\` ...`) to `result: pass`
       - Append a new note paragraph at the bottom of Item 1's `notes:` block:
         ```
         2026-05-09T<HH:MM>Z — Phase 02.5 closed: schema mapping (Plan 03) +
         tenant default binding (Plan 02) landed; `make contract-test` runs
         end-to-end. Evidence: .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md.
         ```
    2. Update the `## Summary` block: increment `passed` from 0 to 1, decrement `blocked` from 1 to 0.
    3. Update the `updated:` frontmatter timestamp to today's ISO-8601.

    **If Status: PARTIAL or BLOCKED** — do NOT flip:
    1. Edit `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` to append a note paragraph to Item 1's `notes:` describing what's still blocking the flip:
       ```
       2026-05-09T<HH:MM>Z — Phase 02.5 attempted; schema mapping + tenant defaults
       landed but `make contract-test` is <PARTIAL|BLOCKED> due to <reason>.
       Evidence: .planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md.
       Item 1 remains blocked. Next phase: <e.g. Phase 02.1 Dockerfile fix>.
       ```
    2. Leave `result: blocked` unchanged.
    3. Update `updated:` timestamp.

    In either branch, also update `.planning/STATE.md`:
    - Mark Phase 02.5 as DONE (all 5 plans landed) in `Current Position`.
    - Update `Last session stopped at:` to mention Phase 02.5 closure.
    - Update the progress bar `[X][X][X]...` if Phase 2 is fully unblocked (only flip if Item 1 went to pass).
  </action>
  <verify>
    <automated>UAT=.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md; grep -q "Phase 02.5" "$UAT" && grep -qE "result: (pass|blocked)" "$UAT" && grep -q "02.5" .planning/STATE.md</automated>
  </verify>
  <done>
    - 02-HUMAN-UAT.md Item 1 is either flipped to `result: pass` (if GREEN) or has a new note explaining why it stays blocked (if PARTIAL/BLOCKED).
    - STATE.md reflects Phase 02.5 closure.
  </done>
  <acceptance_criteria>
    - The decision (flip or no-flip) is consistent with 02.5-04-CONTRACT-TEST-RESULT.md Status header.
    - 02-HUMAN-UAT.md `## Summary` counts are arithmetically consistent (passed + pending + blocked + skipped + issues = total).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Three reverse-patch experiments executed and captured.
2. SUMMARY exists, fully populated, all D-XX traced.
3. 02-HUMAN-UAT.md Item 1 is either flipped (GREEN) or annotated (not GREEN) — consistent with Plan 04 evidence.
4. STATE.md updated.

Single atomic commit:
```
docs(02.5-05): phase summary + reverse-patch evidence + HUMAN-UAT bookkeeping

- 02.5-SUMMARY.md: full decision traceability (D-01..D-06), coverage, acceptance gate.
- 02.5-REVERSE-PATCH-EVIDENCE.md: 3 experiments proving each piece is load-bearing.
- 02-HUMAN-UAT.md Item 1: <flipped to pass | annotated with residual blocker>.
- STATE.md: Phase 02.5 closed.

Acceptance gate per CONTEXT.md item 5 satisfied.
```
</verification>

<success_criteria>
- Phase 02.5 has a complete paper trail: CONTEXT → 5 plans → 5 SUMMARYs → phase SUMMARY → reverse-patch evidence → UAT bookkeeping.
- Every locked decision (D-01..D-06) is traceable to a plan, a commit, and a test.
- 02-HUMAN-UAT.md Item 1 reflects reality (pass if GREEN, blocked-with-reason if not).
- STATE.md current; the project knows what to do next.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-05-SUMMARY.md`.
</output>
