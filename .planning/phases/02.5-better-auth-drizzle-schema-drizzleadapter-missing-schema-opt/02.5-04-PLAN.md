---
phase: 02.5
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md
autonomous: true
requirements: [CONTRACT-01, AUTH-01, AUTH-04]
must_haves:
  truths:
    - "`make contract-test` runs end-to-end with no Better Auth schema-mismatch error."
    - "All 5 conformance fixtures (fixture@conformance.test, verified@conformance.test, pending@conformance.test, rotation-test@local, poll@conformance.test) are seeded — confirmed via `pg_users` count or `seed-conformance` stdout."
    - "The conformance contract-test suite (8 test files: conventions, check-user, verification-status, delete-account, health, oauth-redirect, token-rotation, cookie-host) reports its prior pass/fail breakdown — at minimum, the runs that previously failed only because seed couldn't complete now have a chance to execute and either pass or surface their own (separate) defects."
    - "Full execution log is captured to .planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md as the GREEN witness for the SUMMARY reverse-patch evidence (Plan 05)."
  artifacts:
    - path: ".planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md"
      provides: "Verbatim output of `docker compose --profile contract-test run --rm seed` and `pnpm -F @openwhispr/contract-tests test --run` after Plans 02+03 land"
  key_links:
    - from: "Plan 02 migration 0003 + Plan 03 auth.ts schema map"
      to: "make contract-test seed step (signup of 5 fixture users)"
      via: "Better Auth INSERT INTO users path now resolves model + tenant_id"
      pattern: "seed: conformance fixtures complete"
---

## Engineering Discipline (constitutional, non-negotiable)

> All work in this plan MUST conform to `.planning/DISCIPLINE.md` and PROJECT.md § Engineering Discipline.
>
> Hard rules:
> 1. **Strict TDD** — RED -> GREEN -> REFACTOR; tests in the SAME atomic commit as code.
> 2. **Coverage >= 90/90/90/90** on the diff (lines / branches / functions / statements).
> 3. **E2E mandatory** — if this plan touches a wire surface, an `tests/e2e/` test boots `docker compose` and round-trips it.
> 4. **No mocks of internal logic** — mocks only at process / network boundaries.
> 5. **Real services** — testcontainers for DB-touching code; verification refuses to pass while Docker is down.
>
> The gsd-verifier agent enforces these and will report `gaps_found` if any rule is violated.


<objective>
Verification gate. After Plans 02 and 03 land, the conformance signup must succeed end-to-end. This plan executes the canonical `make contract-test` flow against a freshly-built api image, captures the full output, and produces the GREEN witness that Plan 05 references in the phase SUMMARY.

No new code in this plan — pure verification + evidence capture.

Purpose: convert the Phase 02.5 acceptance gate (CONTEXT.md Acceptance gate item 2: "`make contract-test` runs end-to-end without auth errors") from "claim" to "captured artefact".

Output:
- `.planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md` containing:
  - HEAD git SHA at run time
  - `docker compose ps` snapshot before run
  - `docker compose --profile contract-test run --rm seed` full stdout/stderr
  - `pnpm -F @openwhispr/contract-tests test --run` full stdout/stderr (or the container-run equivalent invoked by the Makefile)
  - Test summary (passed/failed/skipped counts per file)
- If the seed succeeds but some conformance tests fail for OTHER reasons (e.g. Phase 06.1 obs stack, MinIO, Phase 02.1 pnpm — see STATE.md blockers), document each failure cleanly so Plan 05 can decide whether to flip 02-HUMAN-UAT.md Item 1 fully or partially.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-CONTEXT.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-01-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-02-PLAN.md
@.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-03-PLAN.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md
@Makefile
@docker-compose.yml

<interfaces>
Makefile contract-test target (lines 61-69 — verbatim):
```make
contract-test:
	docker compose --profile default --profile contract-test up -d --wait
	@docker compose --profile contract-test run --rm seed ; \
	  ... \
	  pnpm -F @openwhispr/contract-tests test --run ; \
	  ...
```

Compose seed service (docker-compose.yml lines 362-392) — reuses the openwhispr-api
image and runs `node /app/packages/data/dist/seed-conformance.cjs` against the
internal api+postgres network.

Known external blockers (per STATE.md):
- Phase 02.1 Dockerfile pnpm v10 `ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE` may still
  block api image build. If so, the `up --wait` step will hang/fail before seed
  even runs. Document this verbatim and stop — Plan 05 will note 02-HUMAN-UAT
  Item 1 cannot be flipped until Phase 02.1 lands.
- Phase 06.1 tempo+mimir minimal configs may crash the obs stack — those services
  are NOT required for contract-test signup; if obs services crash but api+postgres
  +pgbouncer+seed succeed, that's still a valid GREEN for THIS phase's gate.
- MinIO image pin defect was closed in Phase 01.1 (per STATE.md commit f6eda58).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `make contract-test` end-to-end and capture full output</name>
  <files>.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md</files>
  <read_first>
    - Makefile (full file — confirm contract-test target hasn't changed)
    - .planning/phases/02.5-.../02.5-01-RED-EVIDENCE.md (the RED baseline this Plan inverts)
  </read_first>
  <action>
    Execute the canonical contract-test flow and capture every step. NO code changes in this task — pure verification.

    Steps:

    1. Capture pre-run state:
       ```sh
       SHA=$(git rev-parse --short HEAD)
       docker compose ps > /tmp/02.5-04-ps-before.log 2>&1 || true
       ```

    2. Build the api image fresh to pick up the auth.ts change from Plan 03:
       ```sh
       docker compose --profile default --profile contract-test build api 2>&1 | tee /tmp/02.5-04-build.log
       ```
       Expected: build succeeds. If build fails with `ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE` or similar, this is the documented Phase 02.1 blocker — STOP, write the result file with `## Status: BLOCKED on Phase 02.1` and the verbatim build error, and exit non-zero.

    3. Run `make contract-test`:
       ```sh
       make contract-test 2>&1 | tee /tmp/02.5-04-make.log
       echo "MAKE_EXIT=$?" >> /tmp/02.5-04-make.log
       ```

    4. Capture post-run docker state:
       ```sh
       docker compose ps > /tmp/02.5-04-ps-after.log 2>&1 || true
       docker compose logs --tail=200 api postgres pgbouncer migrate seed > /tmp/02.5-04-svclogs.log 2>&1 || true
       ```

    5. Tear down:
       ```sh
       docker compose --profile default --profile contract-test down 2>&1 | tee /tmp/02.5-04-down.log
       ```

    6. Write `.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md` with this LITERAL structure:

       ```markdown
       # Phase 02.5 / Plan 04 — Contract-test result

       **Run at HEAD:** <git rev-parse --short HEAD>
       **Date:** <ISO-8601 timestamp>
       **Status:** GREEN | PARTIAL | BLOCKED

       ## Summary

       - Plans 01 + 02 + 03 landed.
       - `make contract-test` outcome: <PASS | FAIL with reason>
       - Seed step: <succeeded | failed with error>
       - Conformance suite: <N passed / M failed / K skipped, broken down by file>

       ## Pre-run state
       <contents of /tmp/02.5-04-ps-before.log>

       ## Build
       <last 100 lines of /tmp/02.5-04-build.log>

       ## `make contract-test` output
       <full /tmp/02.5-04-make.log — keep it complete; this is the canonical evidence>

       ## Post-run state
       <contents of /tmp/02.5-04-ps-after.log>

       ## Service logs (tail 200)
       <contents of /tmp/02.5-04-svclogs.log>

       ## Reverse-patch anchor (for Plan 05)

       The contract-test signup that passes here is the third reverse-patch
       witness for D-01 + D-02 + D-03:
       - Reverting Plan 03's auth.ts schema map → seed fails with
         `[# Drizzle Adapter]: The model "user" was not found in the schema object`.
       - Reverting Plan 02's migration 0003 ALTER ROLE → seed fails with an
         RLS / NOT-NULL error on the first user INSERT.
       - Reverting Plan 02's column DEFAULTs → same as above.

       Plan 05 captures the actual reverse-patch run.
       ```

       Be exhaustive — paste the full log contents, not summaries. Plan 05 mines this file for the SUMMARY.
  </action>
  <verify>
    <automated>test -s .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md && grep -qE "^\*\*Status:\*\* (GREEN|PARTIAL|BLOCKED)" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md && grep -qE "(seed: conformance fixtures complete|## Status: BLOCKED|model \"user\" was not found)" .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-CONTRACT-TEST-RESULT.md</automated>
  </verify>
  <done>
    - Result file exists, non-empty, includes a clear Status header (GREEN / PARTIAL / BLOCKED).
    - Full `make contract-test` stdout/stderr captured verbatim.
    - Service logs captured for post-mortem on any failures.
    - Compose stack torn down (no orphaned containers running on the host).
  </done>
  <acceptance_criteria>
    - `head -10` of the result file shows the HEAD SHA, date, status.
    - The `## make contract-test output` section is non-empty and contains either:
      - GREEN path: `seed: conformance fixtures complete` (from seed-conformance stdout) AND a vitest summary line like `Test Files  N passed`, OR
      - PARTIAL path: seed succeeded but some conformance tests failed — each failure documented with its file + reason.
      - BLOCKED path: explicit reason (e.g. Phase 02.1 build defect) with the verbatim docker-compose error.
    - File is committed to git so Plan 05 can reference it.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md with Phase 02.5 progress</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — esp. "Current Position" and "Last session stopped at" sections)
    - .planning/phases/02.5-.../02.5-04-CONTRACT-TEST-RESULT.md (just written in Task 1)
  </read_first>
  <action>
    Update `.planning/STATE.md` to reflect Phase 02.5 progress. Make these targeted edits (preserve all other content):

    1. Update the `last_updated` frontmatter to today's ISO-8601 timestamp.

    2. Update the `Current Position` table:
       - `Phase`: `02.5 — Better Auth drizzle schema (IN PROGRESS — Plans 01-04 done)` (or DONE if Plan 05 is the only thing left, but at this Plan 04 boundary, "in progress" is correct)
       - `Plan`: `02.5-04 complete (contract-test result captured)`
       - `Status`: depending on Task 1's result — `GREEN — conformance signup passes; awaiting Plan 05 SUMMARY` OR `PARTIAL — <reason>` OR `BLOCKED on Phase 02.1`
       - `Next action`: `Run /gsd-execute-phase 02.5 to land Plan 05 (SUMMARY + reverse-patch evidence + 02-HUMAN-UAT flip)`

    3. Append to the `Roadmap Evolution` section a new bullet:
       ```
       - Phase 02.5 Plans 01-04 landed (commits <sha>, <sha>, <sha>, <sha>):
         RED tests → migration 0003 (tenant default binding) + auth.ts schema map → contract-test
         <GREEN|PARTIAL|BLOCKED>. 02-HUMAN-UAT.md Item 1 flippable: <yes|no — reason>.
       ```

    4. If Status is GREEN, update the progress bar from `[X][X][ ]...` to reflect Phase 02 fully closing once Plan 05 lands. (Leave for Plan 05 if more comfortable.)
  </action>
  <verify>
    <automated>grep -q "02.5" .planning/STATE.md && grep -qE "Phase.*02\.5" .planning/STATE.md && grep -qE "(GREEN|PARTIAL|BLOCKED)" .planning/STATE.md</automated>
  </verify>
  <done>
    - STATE.md updated with current Plan 04 status and next-action pointer.
    - Roadmap Evolution bullet added describing the 02.5 cascade.
  </done>
  <acceptance_criteria>
    - `grep -A 5 "Current Position" .planning/STATE.md` shows Phase 02.5 as the current phase.
    - The status field reflects the Plan 04 outcome (GREEN/PARTIAL/BLOCKED).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Result file exists with explicit Status header and full make output.
2. STATE.md reflects 02.5 progress.
3. No production code modified in this plan — verification + evidence capture only.

Single atomic commit:
```
chore(02.5-04): full contract-test pass — capture GREEN witness

- 02.5-04-CONTRACT-TEST-RESULT.md: verbatim `make contract-test` run after
  Plans 02+03 landed. Status: <GREEN|PARTIAL|BLOCKED — reason>.
- STATE.md updated: phase 02.5 in progress, Plans 01-04 done, Plan 05 next.

Reverse-patch anchors recorded for Plan 05 SUMMARY:
- Schema map revert → seed fails with "model 'user' was not found"
- ALTER ROLE revert → seed fails with NOT NULL / RLS violation on first INSERT
- Column DEFAULT revert → same NOT NULL / RLS violation
```
</verification>

<success_criteria>
- Verifiable artefact (result file) committed.
- STATE.md current.
- Plan 05 has a single source of truth to mine for the SUMMARY (the result file).
- If status is BLOCKED, the blocker is named explicitly with a path forward.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-04-SUMMARY.md`.
</output>
