---
phase: 02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt
plan: 01
subsystem: testing
tags: [tdd, vitest, better-auth, drizzle-adapter, postgres, rls, testcontainers]

requires:
  - phase: 02
    provides: apps/api/src/auth.ts buildAuth() factory + 0001_better_auth.sql tables/policies
  - phase: 01
    provides: packages/data testcontainer harness + DEFAULT_TENANT_ID + FORCE RLS on tenant-scoped tables
provides:
  - "RED unit test asserting buildAuth() passes drizzleAdapter an explicit Better Auth canonical-key schema map"
  - "RED testcontainer integration test asserting migration 0003 binds default-tenant via ALTER ROLE + column DEFAULT"
  - "Captured RED-EVIDENCE.md anchoring all three TDD witnesses for the phase (D-04 compliance)"
affects: [02.5-02, 02.5-03, 02.5-04, 02.5-05]

tech-stack:
  added: []
  patterns:
    - "Reverse-patch evidence anchors: every RED test docstring includes Source-of-record + Reverts blocks for Plan 05 SUMMARY"
    - "Vitest vi.mock pattern for capturing drizzleAdapter args via captured-arg sentinel object"
    - "Reuse of bootMigratedPostgres() testcontainer harness for migration-coverage tests prior to migration landing"

key-files:
  created:
    - apps/api/src/__tests__/auth-schema-mapping.test.ts
    - packages/data/src/__tests__/0003_better_auth_tenant_defaults.test.ts
    - .planning/phases/02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt/02.5-01-RED-EVIDENCE.md
  modified: []

key-decisions:
  - "GUC name correction: production reality uses app.tenant_id (canonical across 0000/0001/0002 RLS); CONTEXT.md mention of app.current_tenant is a documentation typo. Plans 02+ implement against app.tenant_id."
  - "Stage only Plan-01 deliverables in the atomic commit (not the unrelated 02.5 plan/context files that exist untracked); preserves task_commit_protocol's 'never git add -A' rule."
  - "Witness 3 (live contract-test signup) deferred to Plan 04 GREEN-EVIDENCE.md because local stack-up is blocked by missing .env + Phase 02.1 Dockerfile defect. Witnesses 1 + 2 are sufficient for D-04 TDD compliance; canonical Better Auth error preserved verbatim from CONTEXT.md."

patterns-established:
  - "Per-RED-test docstring: header line cites the upcoming production commit/migration; trailing Reverts block names the exact change that re-reds it (reverse-patch anchor for SUMMARY)."
  - "Test files committed in same atomic commit as the RED-EVIDENCE.md witness file — single commit, single message body, three artifacts."

requirements-completed: [TDD-01, TDD-01b]

duration: 4m 15s
completed: 2026-05-09
---

# Phase 02.5 Plan 01: RED baseline lock — Better Auth schema mapping + tenant default binding Summary

**Three RED witnesses landed in one atomic commit (`567c0fb`): unit test asserts the missing explicit drizzleAdapter schema map (3 failed), testcontainer integration test asserts the missing migration 0003 role-default + column DEFAULT (5 failed, 4 of them caught by FORCE RLS on insert), and a markdown evidence file pins the canonical Better Auth `model "user" was not found` error for D-04 TDD compliance.**

## Performance

- **Duration:** ~4m 15s
- **Started:** 2026-05-09T18:45:00Z (approx)
- **Completed:** 2026-05-09T18:49:15Z
- **Tasks:** 3
- **Files modified:** 3 (all newly created)

## Accomplishments

- 3 RED unit tests in `apps/api/src/__tests__/auth-schema-mapping.test.ts` — all 3 fail against current `auth.ts:139` (passes the entire pluralized barrel `[TENANT_SCOPED_TABLES, accounts, auditLog, oauthState, sessions, tenants, usageLedger, users, verifications]` instead of `[account, session, user, verification]`).
- 5 RED testcontainer integration tests in `packages/data/src/__tests__/0003_better_auth_tenant_defaults.test.ts` — all 5 fail: 1 because `pg_roles.rolconfig` is empty (no `ALTER ROLE SET app.tenant_id`), 4 because bare INSERTs without `tenant_id` fail FORCE RLS with `new row violates row-level security policy for table "users" / "verification"`.
- `02.5-01-RED-EVIDENCE.md` captures all three witnesses plus the canonical Better Auth error string from CONTEXT.md, anchored to HEAD `a9a4da7`.
- Zero production source files modified (`git diff --name-only -- apps/ packages/` returned empty before commit; verified per acceptance gate).

## Task Commits

Single atomic commit per the plan's verification block:

1. **Task 1 + 2 + 3 combined** — `567c0fb` (test) — `test(02.5-01): add RED tests for better-auth schema mapping + tenant default binding`

_Plan 02.5-01 is a pure RED-test-lock plan; no separate task commits — single atomic commit per plan instruction (verification §374-388)._

## Files Created/Modified

- `apps/api/src/__tests__/auth-schema-mapping.test.ts` (70 lines) — 3 RED unit tests for D-01 (explicit Better Auth ↔ drizzle key mapping). Uses `vi.mock("better-auth/adapters/drizzle")` to capture the schema arg and assert keys + reference identity.
- `packages/data/src/__tests__/0003_better_auth_tenant_defaults.test.ts` (98 lines) — 5 RED testcontainer integration tests for D-02 + D-03. Uses `bootMigratedPostgres()` and asserts (a) `pg_roles.rolconfig` contains `app.tenant_id=00000000-...-000`, (b) bare INSERTs into `users/sessions/account/verification` resolve to default-tenant via column DEFAULT.
- `.planning/phases/02.5-.../02.5-01-RED-EVIDENCE.md` (160 lines) — three-witness RED baseline pinned to commit SHA + canonical error string.

## Decisions Made

- **GUC name correction (`app.tenant_id`, not `app.current_tenant`):** the CONTEXT.md draft uses `app.current_tenant` in D-02/D-03; production reality across `0000_initial.sql`, `0001_better_auth.sql`, `0002_oauth_state.sql` uses `app.tenant_id`. Plans 02+ implement against `app.tenant_id` to align with existing FORCE RLS policies. Locked-decision intent (role default + column DEFAULT) preserved at full strength.
- **Witness 3 documented as "Stack-up blocked", not skipped:** local docker compose cannot start because `.env` is absent and the api image cannot build until Phase 02.1's pnpm v10 Dockerfile defect is fixed (STATE.md tracked). Per the plan's Task 3 fallback, the canonical Better Auth error is preserved verbatim from CONTEXT.md and substantiated by Witness 1's schema-keys assertion. Plan 04 will re-attempt live `make contract-test` and emit a `02.5-04-GREEN-EVIDENCE.md` companion.
- **Atomic commit scope:** staged only the three Plan-01 deliverables (two test files + RED-EVIDENCE.md), not the unrelated `.planning/phases/02.5-.../02.5-0[1-5]-PLAN.md`/`02.5-CONTEXT.md` files that were untracked. Those belong to the prior planning commits and will land separately. Preserves the "never `git add -A`" task_commit_protocol rule.

## Deviations from Plan

None — plan executed exactly as written. Lefthook auto-formatted the two test files at commit time (Biome import-sort + 2 whitespace tweaks); semantic content preserved, all 8 tests still RED-by-design. Biome flagged 6 advisory `noNonNullAssertion` warnings on `captured.schema!.X` patterns; commit passed because the pre-commit hook treated them as warnings, not errors. Non-null assertions retained because the alternative (`captured.schema?.X`) would silently pass when `schema` is undefined, defeating the test's purpose.

## Issues Encountered

- **Local docker compose stack-up blocked** by `Failed to load /Users/nick/openwhispr-server/.env: open /Users/nick/openwhispr-server/.env: no such file or directory`. Repo ships `.env.example` only. Compounded by the open Phase 02.1 Dockerfile defect (`ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE`) which would prevent the `api` image from building even with a valid `.env`. Documented in RED-EVIDENCE.md `## Stack-up blocked` per Task 3 fallback. Witnesses 1 + 2 are sufficient for D-04; Plan 04 picks up the live conformance witness once Phase 02.1 unblocks.

## User Setup Required

None — no external service configuration required for this plan.

## Self-Check: PASSED

Verified post-write:

- `git log --oneline -1` → `567c0fb test(02.5-01): add RED tests...` ✅
- `apps/api/src/__tests__/auth-schema-mapping.test.ts` exists (70 lines tracked in commit) ✅
- `packages/data/src/__tests__/0003_better_auth_tenant_defaults.test.ts` exists (98 lines tracked in commit) ✅
- `.planning/phases/02.5-.../02.5-01-RED-EVIDENCE.md` exists (160 lines tracked in commit) ✅
- All four acceptance grep checks pass (OK1 OK2 OK3 OK4) ✅
- `git diff --name-only -- apps/ packages/` returned empty before commit (no production source modified) ✅

## Next Phase Readiness

Plan 02.5-02 ready to execute: `packages/data/migrations/0003_better_auth_tenant_defaults.sql` (ALTER ROLE openwhispr_app SET app.tenant_id + ALTER TABLE × 4 with column DEFAULT current_setting('app.tenant_id', true)::uuid). Verification: `pnpm -F @openwhispr/data test --run src/__tests__/0003_better_auth_tenant_defaults.test.ts` flips 5 RED → 5 GREEN.

Plan 02.5-03 ready: `apps/api/src/auth.ts` swap `import * as schema` → named imports + explicit `schema: { user, session, account, verification }`. Verification: `pnpm -F @openwhispr/api test --run src/__tests__/auth-schema-mapping.test.ts` flips 3 RED → 3 GREEN.

Plan 02.5-04 deferred until Phase 02.1 lands (Dockerfile pnpm v10 fix unblocks `docker compose up api` → `make contract-test` end-to-end).

---
*Phase: 02.5-better-auth-drizzle-schema-drizzleadapter-missing-schema-opt*
*Completed: 2026-05-09*
