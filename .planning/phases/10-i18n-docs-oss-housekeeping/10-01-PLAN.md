---
phase: 10
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/src/i18n.ts
  - apps/api/src/index.ts
  - apps/api/src/error-handler.ts
  - apps/api/src/errors.ts
  - apps/api/src/auth.ts
  - apps/api/src/__tests__/error-handler-i18n.test.ts
  - apps/api/src/__tests__/i18n-emission-completeness.test.ts
  - apps/api/src/lib/audit.ts
  - apps/api/src/__tests__/audit-cyrillic-guard.test.ts
  - apps/worker/package.json
  - apps/worker/src/lib/template-renderer.ts
  - apps/worker/src/lib/__tests__/template-renderer.test.ts
  - apps/worker/src/jobs/email-delivery.ts
  - apps/worker/src/index.ts
  - apps/worker/src/__tests__/email-template-completeness.test.ts
  - packages/i18n/package.json
  - packages/i18n/src/index.ts
  - packages/i18n/src/plural-rules.test.ts
  - packages/i18n/__tests__/locale-coverage.test.ts
  - packages/i18n/locales/en/errors.json
  - packages/i18n/locales/ru/errors.json
  - packages/i18n/locales/en/email.json
  - packages/i18n/locales/ru/email.json
  - packages/i18n/locales/en/audit.json
  - packages/i18n/locales/ru/audit.json
  - packages/data/src/schema/users.ts
  - packages/data/src/migrations/00XX_users_locale.sql
  - docker-compose.yml
  - .env.example
  - .github/workflows/ci.yml
autonomous: true
requirements: [I18N-01, I18N-02, TEST-I18N-01]
tags: [i18n, server, api, worker, i18next, icu, fastify]

must_haves:
  truths:
    - "GET /api/anything-401 with Accept-Language: ru returns {error: <ru text>}"
    - "Worker renders email_verification with locale=ru producing Cyrillic subject + body"
    - "i18next init loads from LOCALES_DIR env var when set, baked-in resources otherwise"
    - "CI fails when packages/i18n/locales/en/X.json gains a key not present in ru/X.json"
    - "CI fails when source contains `reply.send({error:` outside allowlisted bootstrap paths"
    - "users.locale column exists, defaults to 'en', is set from Accept-Language at signup"
    - "CLDR ru boundaries 0/1/2/5/11/21/22/25/101/105 render correct plural category"
  artifacts:
    - path: "apps/api/src/i18n.ts"
      exports: ["initI18n"]
    - path: "packages/i18n/locales/en/errors.json"
      contains: "auth.unauthorized"
    - path: "packages/i18n/locales/ru/errors.json"
      contains: "auth.unauthorized"
    - path: "apps/worker/src/lib/template-renderer.ts"
      exports: ["TemplateRenderer", "createTemplateRenderer"]
    - path: "packages/data/src/migrations/00XX_users_locale.sql"
      contains: "ADD COLUMN locale"
  key_links:
    - from: "apps/api/src/error-handler.ts"
      to: "packages/i18n/locales/{en,ru}/errors.json"
      via: "req.i18n.t(`errors.${err.code}`)"
      pattern: "req\\.i18n\\.t\\("
    - from: "apps/worker/src/jobs/email-delivery.ts"
      to: "apps/worker/src/lib/template-renderer.ts"
      via: "renderer.render({template_id, locale, variables})"
      pattern: "renderer\\.render"
    - from: "apps/api/src/auth.ts"
      to: "apps/worker/src/jobs/email-delivery.ts"
      via: "enqueueEmailDelivery({locale: user.locale})"
      pattern: "enqueueEmailDelivery"
---

# Plan 10-01: Server i18n (API + Worker)

<objective>
Stand up the server-side i18next surface (API + Worker) with i18next-icu, i18next-http-middleware, and i18next-fs-backend. Convert remaining inline `reply.send({error:...})` sites to typed throws carrying a stable `err.code`. Translate at the single `setErrorHandler` chokepoint. Implement the worker `TemplateRenderer` for 3 production email templates. Add `users.locale` column + Better Auth integration so verification emails respect user locale. Lock the TEST-I18N-01 gate (key-completeness diff + emission-source-walk).

Purpose: Satisfies I18N-01 (i18next+ICU+CLDR, Accept-Language negotiation), I18N-02 (operator-override volume), and TEST-I18N-01 (CI completeness gate).
Output: Translated error envelopes, localized emails, ru locale resources for all server-emitted strings, CI gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-RESEARCH.md
@CLAUDE.md
@apps/api/src/error-handler.ts
@apps/api/src/errors.ts
@apps/api/src/auth.ts
@apps/worker/src/jobs/email-delivery.ts
@apps/worker/src/index.ts
@packages/i18n/src/index.ts
@packages/data/src/schema/users.ts
@tools/lint-english.ts

<interfaces>
From apps/worker/src/jobs/email-delivery.ts (verified at lines 30-62):

```ts
export const emailDeliverySchema = z.object({
  tenant_id: z.string().uuid(),
  to: z.string().email(),
  template_id: z.string().min(1),
  locale: z.enum(["en", "ru"]).default("en"),
  variables: z.record(z.string(), z.unknown()).default({}),
  request_id: z.string().uuid(),
});

export interface TemplateRenderer {
  render(input: {
    template_id: string;
    locale: "en" | "ru";
    variables: Record<string, unknown>;
  }): Promise<{ subject: string; text: string; html?: string }>;
}
```

From apps/api/src/errors.ts (existing typed-error classes — extend each with `readonly code: string` field):
- `AuthError`, `NotFoundError`, `ValidationError`, `ServiceUnavailableError`, `RateLimitError`

i18next key namespace:
- `errors.auth.unauthorized`, `errors.auth.session_expired`
- `errors.not_found.conversation`, `errors.not_found.transcription`, `errors.not_found.folder`, `errors.not_found.note`, `errors.not_found.api_key`
- `errors.validation.metadata_too_large` (vars: limit_bytes), `errors.validation.batch_too_large` (vars: limit, plural ICU), `errors.validation.uuid_required`, `errors.validation.query_empty`
- `errors.upstream.<provider>_failure`, `errors.upstream.reasoning_failure`
- `errors.rate_limit.exceeded`
- `errors.internal`
- `errors.oauth.unsupported_provider`, `errors.oauth.invalid_state`, `errors.oauth.state_consumed`, `errors.oauth.state_expired`, `errors.oauth.missing_state_or_code`, `errors.oauth.callback_not_configured`, `errors.oauth.idp_error`
- `errors.realtime.streams_invalid`

Email templates (3 production):
- `email_verification` (vars: name, verification_url)
- `password_reset` (vars: name, reset_url, expires_minutes)
- `account_deletion_confirmation` (vars: name, deleted_at)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — i18n test scaffolding (locale-coverage, emission-completeness, error-handler-i18n, plural-rules, template-renderer, email-template-completeness, audit-cyrillic-guard)</name>
  <files>packages/i18n/__tests__/locale-coverage.test.ts, packages/i18n/src/plural-rules.test.ts, apps/api/src/__tests__/error-handler-i18n.test.ts, apps/api/src/__tests__/i18n-emission-completeness.test.ts, apps/api/src/__tests__/audit-cyrillic-guard.test.ts, apps/worker/src/lib/__tests__/template-renderer.test.ts, apps/worker/src/__tests__/email-template-completeness.test.ts, packages/i18n/package.json</files>
  <behavior>
    - locale-coverage: walk packages/i18n/locales/en/*.json; for each en file, ru/<same>.json must exist with identical dotted-key set. Output `MISSING ru: <file>:<dotted.key>` on drift.
    - plural-rules: instantiate i18next + ICU with ru; assert categories one/few/many for boundaries 0,1,2,3,4,5,11,12,21,22,25,101,105 via test fixture `packages/i18n/locales/{en,ru}/__test_plurals__.json`.
    - error-handler-i18n: extend existing error-handler test; build Fastify with i18next plugin; throw `new AuthError("unauthorized")`; with Accept-Language: ru, response body matches ru bundle text for `errors.auth.unauthorized`; with Accept-Language: en, matches en text; unknown locale falls back to en.
    - i18n-emission-completeness: TS compiler API walk of apps/api/src/**/*.ts; collect every `throw new <TypedError>(<string-literal>)` and assert the literal (treated as `err.code`) exists in packages/i18n/locales/en/errors.json AND ru/errors.json. Forbid `reply.send({error: ...})` outside allowlist (`apps/api/src/healthz.ts` and `apps/api/src/index.ts` bootstrap only).
    - audit-cyrillic-guard: call recordAudit() with a payload containing Cyrillic codepoints; assert it throws ProgrammerError before INSERT.
    - template-renderer: render `email_verification` with locale=en and locale=ru; assert non-empty subject/text; assert ru subject contains Cyrillic; snapshot both locales for all 3 templates.
    - email-template-completeness: enumerate TemplateRenderer.knownTemplateIds × [en,ru]; assert every (id, locale) renders non-empty subject+text.
    All tests MUST fail at this point (RED) because production code does not yet exist.
  </behavior>
  <action>Create the seven test files above with full RED test bodies. Add devDependencies to package.json files: `i18next@^26.1.0`, `i18next-icu@^2.4.3`, `i18next-fs-backend@^2.6.0`, `i18next-http-middleware@^3.9.6`. Add a `__test_plurals__.json` fixture per locale containing one ICU message `{count, plural, one {# элемент} few {# элемента} many {# элементов} other {# элементов}}` (ru) and English mirror. Do NOT write production code yet. Commit per D-13 atomic discipline.</action>
  <verify>
    <automated>pnpm -F @openwhispr/i18n test 2>&1 | grep -q "fail" && pnpm -F @openwhispr/api test --filter "i18n|error-handler-i18n|audit-cyrillic" 2>&1 | grep -q "fail" && pnpm -F @openwhispr/worker test --filter "template-renderer|email-template-completeness" 2>&1 | grep -q "fail"</automated>
  </verify>
  <done>All 7 tests exist, run, and FAIL with the expected error messages. Atomic commit: `test(10-01): add red i18n + emission-completeness + template-renderer tests`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — packages/i18n with locales (errors.json + email.json + audit.json en+ru) and plural-rules pass</name>
  <files>packages/i18n/src/index.ts, packages/i18n/locales/en/errors.json, packages/i18n/locales/ru/errors.json, packages/i18n/locales/en/email.json, packages/i18n/locales/ru/email.json, packages/i18n/locales/en/audit.json, packages/i18n/locales/ru/audit.json, packages/i18n/locales/en/__test_plurals__.json, packages/i18n/locales/ru/__test_plurals__.json</files>
  <behavior>
    - errors.json keys cover every code listed in 10-01 interfaces block (~30 unique strings)
    - email.json keys: `email.email_verification.{subject,text,html}`, same for `password_reset` and `account_deletion_confirmation`
    - audit.json keys mirror the 18 AUDIT_LOG_ACTIONS codes (rendered web-side; server includes both locales for parity)
    - All ru bundles use proper CLDR plurals where {count} appears; English uses ICU `{count, plural, one {} other {}}` shape
    - packages/i18n/src/index.ts exports `getLocalesDir()` (returns `LOCALES_DIR` env or default repo path) and `BAKED_LOCALES_PATH` constant
  </behavior>
  <action>Write all 8 JSON resource files with full key sets. Translations: produce native Russian per Russian Yandex/grammar conventions; English source from research §i18n-string-site-inventory. Use ICU MessageFormat for any plural or variable interpolation (`{name}`, NOT `{{name}}`). Keep audit-log keys English in source (the `key` strings) — only `value` strings translate. Update `tools/lint-english.ts` allowlist if needed (already covers `packages/i18n/locales/**`).</action>
  <verify>
    <automated>pnpm -F @openwhispr/i18n test</automated>
  </verify>
  <done>locale-coverage and plural-rules tests GREEN. Atomic commit: `feat(10-01): add server i18n bundles (errors/email/audit en+ru) with cldr plurals`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — Fastify i18next bootstrap + error-handler translation + typed-error code field</name>
  <files>apps/api/src/i18n.ts, apps/api/src/index.ts, apps/api/src/error-handler.ts, apps/api/src/errors.ts, apps/api/package.json</files>
  <behavior>
    - apps/api/src/i18n.ts exports `initI18n()` that constructs the i18next instance (ICU + fs-backend + LanguageDetector). loadPath uses `LOCALES_DIR` env when set, else `packages/i18n/locales/{{lng}}/{{ns}}.json` resolved relative to package root.
    - apps/api/src/index.ts registers `i18nextMiddleware.plugin` with the initialized i18next AFTER routes are constructed but BEFORE setErrorHandler is wired (or in matching order required by i18next-http-middleware).
    - apps/api/src/errors.ts: each typed-error class adds `readonly code: string` constructor field; defaults preserve current behavior (`AuthError("unauthorized")` → code `"auth.unauthorized"`).
    - apps/api/src/error-handler.ts: each branch consults `req.i18n.t(\`errors.${err.code}\`, err.vars ?? {})` and uses the result as the envelope `error` message. Fallback: if `req.i18n` is missing (test bootstrap), use English literal so existing tests still parse.
    - Convert all 89 inline `reply.send({error:"..."})` sites to throws of the appropriate typed error with stable codes (per the interfaces block mapping). This is the bulk of the diff.
    - Preserve byte-for-byte wire shape (status code, envelope keys) per BACKEND_SPEC; only the `error` string changes when Accept-Language is non-en.
  </behavior>
  <action>Wire i18next bootstrap and the error-handler translation per D-13. Codemod-style refactor the 89 inline sites — each `reply.code(401).send({error:"unauthorized"})` becomes `throw new AuthError("unauthorized")` etc. Per Russian decision (D-13), do not invent new error classes; map each unique English string to one of the existing 5 typed classes plus a stable code key. Wire `err.vars` carrier for ICU variable interpolation (used by `batch_too_large` + `metadata_too_large`).</action>
  <verify>
    <automated>pnpm -F @openwhispr/api test --filter "error-handler-i18n|i18n-emission-completeness" && pnpm -F @openwhispr/api typecheck</automated>
  </verify>
  <done>error-handler-i18n and i18n-emission-completeness GREEN. Atomic commit: `feat(10-01): wire fastify i18next + translate envelopes at setErrorHandler chokepoint`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: GREEN — Worker TemplateRenderer with 3 templates + Better Auth verification hook locale wiring</name>
  <files>apps/worker/src/lib/template-renderer.ts, apps/worker/src/jobs/email-delivery.ts, apps/worker/src/index.ts, apps/worker/package.json, apps/api/src/auth.ts, packages/data/src/schema/users.ts, packages/data/src/migrations/00XX_users_locale.sql</files>
  <behavior>
    - createTemplateRenderer({ i18next, localesDir }) returns object with `render({template_id, locale, variables})` and `knownTemplateIds` const array.
    - render uses `i18next.getFixedT(locale, "email")` to render `email.${template_id}.subject` + `.text` + optional `.html` with ICU interpolation of `variables`.
    - Unknown template_id throws an `UnknownTemplateError` (with template_id in message) — caught by BullMQ job consumer and marks job failed for DLQ.
    - apps/worker/src/index.ts replaces noopRenderer with createTemplateRenderer using the shared i18next instance (init via packages/i18n loader).
    - packages/data: add `locale text not null default 'en'` to users table; emit migration SQL with up + down; Better Auth `additionalFields.locale` config.
    - apps/api/src/auth.ts: `sendVerificationEmail` hook now enqueues BullMQ `email-delivery` job with `template_id: "email_verification"`, `locale: user.locale ?? acceptLanguage`, `variables: {name, verification_url}` instead of calling email.send() inline.
    - Same hook pattern for `sendResetPassword` and `account_deletion_confirmation` (the latter fires from existing DELETE /api/auth/delete-account success path).
  </behavior>
  <action>Implement the renderer + Better Auth wiring per the interfaces block. Add the users.locale migration via drizzle-kit and write the raw SQL alongside (project convention). At signup time, persist `req.headers['accept-language']`-derived locale (parse via accept-language-parser, fallback en). Ensure the SAME i18next instance bootstrap is used by api/i18n.ts and worker/lib/template-renderer.ts (factor shared init into packages/i18n/src/init.ts if hygiene demands).</action>
  <verify>
    <automated>pnpm -F @openwhispr/worker test --filter "template-renderer|email-template-completeness" && pnpm -F @openwhispr/api test --filter "auth" && pnpm -F @openwhispr/data test</automated>
  </verify>
  <done>template-renderer and email-template-completeness GREEN; users.locale migration applies cleanly forward+backward; Better Auth verification email enqueues a locale-aware job. Atomic commit: `feat(10-01): worker template renderer + users.locale + locale-aware verification email`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: GREEN — Audit Cyrillic guard + operator-override docker-compose volume + .env.example</name>
  <files>apps/api/src/lib/audit.ts, docker-compose.yml, .env.example</files>
  <behavior>
    - audit.ts: recordAudit() scans the stringified payload for Cyrillic codepoints `/[Ѐ-ӿ]/u`; throws `ProgrammerError("audit payload must be english per ADR-0003")` if hit. Mirrors lint-english regex.
    - docker-compose.yml: `api` and `worker` services gain `volumes: - ./locales:/etc/openwhispr/locales:ro`. New env var `LOCALES_DIR=/etc/openwhispr/locales` documented as commented-out default.
    - .env.example: add `LOCALES_DIR=` (empty by default) with explanatory comment block referencing `docs/i18n.md`.
  </behavior>
  <action>Implement the runtime guard with the test fixture from Task 1. Add compose volumes ONLY to api/worker services (NOT to web — web bundles locales statically). Confirm the volume mount does not collide with existing volumes.</action>
  <verify>
    <automated>pnpm -F @openwhispr/api test --filter "audit-cyrillic-guard" && docker compose config -q</automated>
  </verify>
  <done>audit-cyrillic-guard GREEN; compose config valid. Atomic commit: `feat(10-01): audit cyrillic guard + i18n operator-override volume`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: CI gate — test:i18n-completeness step + coverage gate</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <behavior>
    - Root package.json gains script `test:i18n-completeness` → `pnpm -F @openwhispr/i18n test && pnpm -F @openwhispr/api test --filter i18n-emission-completeness`.
    - ci.yml adds a step `Run i18n completeness gate` running that script on every PR.
    - Coverage gate: ensure existing vitest config on apps/api and apps/worker enforces 90/90/90/90 on the new modules (no exclusion of new files from coverage).
  </behavior>
  <action>Wire the CI step; verify coverage gate is enforced on changed paths via the existing `coverage:check` invocation. Surface any sub-90 axis in the workflow output.</action>
  <verify>
    <automated>act -j ci -W .github/workflows/ci.yml 2>/dev/null || pnpm test:i18n-completeness</automated>
  </verify>
  <done>CI workflow has the new step; coverage ≥ 90/90/90/90 on diff for all files touched in 10-01. Atomic commit: `ci(10-01): add i18n-completeness gate to pr workflow`.</done>
</task>

</tasks>

<verification>
- All 6 task verifications green
- `pnpm -r test` green
- `pnpm typecheck` green
- `pnpm lint:english` green (no Cyrillic outside allowlisted locale paths)
- Coverage ≥ 90/90/90/90 on changed TS files in apps/api + apps/worker + packages/i18n
</verification>

<success_criteria>
- API translates error envelopes via Accept-Language for all converted sites
- Worker renders 3 email templates in en+ru via real i18next (no mocks)
- TEST-I18N-01 gate exists in CI and is green for committed state
- users.locale column live; Better Auth verification email is locale-aware
- Operator can mount `./locales` over baked-in resources without code change
</success_criteria>

<output>
Create `.planning/phases/10-i18n-docs-oss-housekeeping/10-01-SUMMARY.md` summarizing artifacts, decisions, and any deferred items.
</output>
