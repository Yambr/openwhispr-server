---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - tools/spdx-header.ts
  - tools/__tests__/spdx-header.test.ts
  - .github/workflows/spdx.yml
  - .github/CODEOWNERS
  - .github/ISSUE_TEMPLATE/bug.yml
  - .github/ISSUE_TEMPLATE/feature.yml
  - .github/ISSUE_TEMPLATE/question.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - CODE_OF_CONDUCT.md
  - CONTRIBUTING.md
  - SECURITY.md
  - docs/adrs/0004-apache-2-0-licensing.md
  - docs/adrs/0005-stack-node-fastify-better-auth-drizzle-pg-pgbouncer-valkey-bullmq.md
  - docs/adrs/0006-wire-compatibility-with-upstream-backend-spec.md
  - docs/adrs/0007-multi-tenancy-via-rls-with-default-tenant.md
  - docs/adrs/0008-litellm-as-ai-plane-abstraction.md
  - docs/adrs/0009-better-auth-email-password-and-oidc-plugin.md
  - docs/adrs/0010-i18n-runtime-i18next-icu-en-ru-operator-overridable.md
  - docs/adrs/0011-strict-tdd-github-actions-ci-max-automation.md
  - package.json
autonomous: true
requirements: [DOCS-07, DOCS-08]
tags: [oss, license, spdx, codeowners, issue-templates, code-of-conduct, adrs]

must_haves:
  truths:
    - "Every .ts/.tsx source file under apps/ packages/ tools/ has 'SPDX-License-Identifier: Apache-2.0' on line 1 (or line 2 if a shebang exists on line 1)"
    - "CI workflow .github/workflows/spdx.yml runs on every PR and fails when a new TS file is missing the SPDX header"
    - ".github/CODEOWNERS maps default ownership"
    - ".github/ISSUE_TEMPLATE/{bug,feature,question}.yml exist plus config.yml disabling blank issues"
    - "CODE_OF_CONDUCT.md is Contributor Covenant 2.1 verbatim (audit confirms current file matches)"
    - "CONTRIBUTING.md mentions SPDX header convention and i18n contribution rules"
    - "SECURITY.md has response SLA detail and cross-links docs/security.md"
    - "8 new ADRs (0004-0011) exist in Nygard format under docs/adrs/"
  artifacts:
    - path: "tools/spdx-header.ts"
      provides: "Codemod that inserts SPDX header into TS files + audit subcommand"
    - path: ".github/workflows/spdx.yml"
      provides: "CI gate"
    - path: ".github/CODEOWNERS"
      provides: "Default ownership"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1"
    - path: "docs/adrs/0004-apache-2-0-licensing.md"
      provides: "ADR-0004 (license already swapped to Apache-2.0 in bd81d82; ADR records the decision)"
    - path: "docs/adrs/0011-strict-tdd-github-actions-ci-max-automation.md"
      provides: "ADR-0011"
  key_links:
    - from: ".github/workflows/spdx.yml"
      to: "tools/spdx-header.ts"
      via: "pnpm spdx:check"
      pattern: "spdx:check"
    - from: "CONTRIBUTING.md"
      to: "tools/spdx-header.ts"
      via: "License header section pointing to `pnpm spdx:fix`"
      pattern: "spdx:fix"
---

# Plan 10-04: OSS Housekeeping — SPDX, CODEOWNERS, Issue Templates, CoC 2.1, ADRs 0004-0011

<objective>
Complete the OSS housekeeping surface for DOCS-07 and DOCS-08. LICENSE + NOTICE are already shipped pre-planner in commit `bd81d82` (Apache-2.0). This plan adds SPDX short-form license headers to every `.ts`/`.tsx` source file via a codemod with CI gate, drops `.github/CODEOWNERS`, drops three ISSUE_TEMPLATE forms + config.yml, audits the existing Contributor Covenant 2.1 CODE_OF_CONDUCT.md, extends CONTRIBUTING.md (SPDX + i18n notes) and SECURITY.md (response SLA + threat-model cross-link), and writes ADRs 0004 through 0011 covering all eight mandatory Key Decisions from PROJECT.md.

Purpose: Closes DOCS-07 (housekeeping minus LICENSE which is already done) and DOCS-08 (ADRs for every Key Decision).
Output: SPDX coverage with CI gate, GitHub housekeeping files, 8 ADRs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-RESEARCH.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-01-SUMMARY.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-02-SUMMARY.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-03-SUMMARY.md
@docs/adrs/0000-template.md
@docs/adrs/0001-pnpm-workspaces-monorepo.md
@docs/adrs/0002-vitest-and-stryker-for-coverage-and-mutation.md
@docs/adrs/0003-english-only-source-artifacts.md
@CONTRIBUTING.md
@SECURITY.md
@CODE_OF_CONDUCT.md
@LICENSE
@NOTICE
@tools/lint-english.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — SPDX header codemod tests (audit + insert + skip-existing + shebang-safety)</name>
  <files>tools/__tests__/spdx-header.test.ts</files>
  <behavior>
    - audit mode: given a fixture dir with 3 files (one with header, one without, one with shebang + no header), reports the two missing files
    - insert mode: idempotent — running twice leaves files unchanged; first run inserts `// SPDX-License-Identifier: Apache-2.0` on line 1 (or line 2 if line 1 is a shebang)
    - skips JSON, `node_modules`, `dist`, `**/migrations/*.generated.*`, and `.next`
    - preserves trailing newline, biome formatting (no stray blank line after insertion)
    - throws if invoked on a binary file
    Tests MUST fail (RED).
  </behavior>
  <action>Write tools/__tests__/spdx-header.test.ts with vitest fixtures under `tools/__tests__/fixtures/spdx/`. Cover the four cases above. Use temp directories for in-place insertion idempotence checks.</action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/spdx-header.test.ts 2>&1 | grep -q "FAIL\\|✗"</automated>
  </verify>
  <done>Tests fail with expected errors. Atomic commit: `test(10-04): add red spdx-header codemod tests`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — tools/spdx-header.ts codemod + package.json scripts + CI workflow</name>
  <files>tools/spdx-header.ts, package.json, .github/workflows/spdx.yml</files>
  <behavior>
    - tools/spdx-header.ts: CLI with `audit` (exit 1 if any TS/TSX file under apps/ packages/ tools/ is missing the header) and `fix` (insert headers idempotently) subcommands
    - Respects shebang lines (`#!/...`) by inserting header on line 2
    - Excludes JSON, node_modules, dist, .next, **/*.generated.*, packages/i18n/locales/**
    - package.json gains `"spdx:check": "tsx tools/spdx-header.ts audit"` and `"spdx:fix": "tsx tools/spdx-header.ts fix"`
    - .github/workflows/spdx.yml: runs `pnpm spdx:check` on every PR
    - Run `pnpm spdx:fix` once to add headers to all existing files (large diff but mechanical)
  </behavior>
  <action>Implement the codemod. Use TypeScript Compiler API or a plain file walk (the latter is simpler and adequate). After implementation, run `pnpm spdx:fix` to bulk-insert headers. Confirm `pnpm lint:english` stays green (SPDX line is ASCII English). Confirm `pnpm typecheck` stays green. Confirm biome formatting unchanged.</action>
  <verify>
    <automated>pnpm vitest run tools/__tests__/spdx-header.test.ts && pnpm spdx:check && pnpm lint:english && pnpm typecheck</automated>
  </verify>
  <done>Codemod tests GREEN; every existing .ts/.tsx file under apps/ packages/ tools/ has the header; CI workflow committed. Atomic commit: `feat(10-04): spdx header codemod + ci gate + bulk insert across ts/tsx`.</done>
</task>

<task type="auto">
  <name>Task 3: .github housekeeping — CODEOWNERS + ISSUE_TEMPLATE + config.yml</name>
  <files>.github/CODEOWNERS, .github/ISSUE_TEMPLATE/bug.yml, .github/ISSUE_TEMPLATE/feature.yml, .github/ISSUE_TEMPLATE/question.yml, .github/ISSUE_TEMPLATE/config.yml</files>
  <action>
    - **CODEOWNERS**: default owner `* @nick` (or repo-owner handle resolved from `git config user.name` — use the literal `@nick` for now since user is the sole maintainer). Pattern lines for `apps/api/` `apps/web/` `apps/worker/` `packages/data/` `docs/` `.github/` all mapping to the same default.
    - **bug.yml**: GitHub issue form (YAML) with fields: title, what-happened, expected, version (compose/helm), logs, env (self-host/cloud)
    - **feature.yml**: form with: title, motivation, proposed-solution, alternatives, requirement-id (free-text reference into REQUIREMENTS.md)
    - **question.yml**: form with: question, what-you-tried, doc-checked (link)
    - **config.yml**: `blank_issues_enabled: false` + `contact_links` redirecting security reports to the email channel from SECURITY.md
  </action>
  <verify>
    <automated>test -f .github/CODEOWNERS && test -f .github/ISSUE_TEMPLATE/bug.yml && test -f .github/ISSUE_TEMPLATE/feature.yml && test -f .github/ISSUE_TEMPLATE/question.yml && test -f .github/ISSUE_TEMPLATE/config.yml && python3 -c "import yaml; [yaml.safe_load(open(f)) for f in ['.github/ISSUE_TEMPLATE/bug.yml','.github/ISSUE_TEMPLATE/feature.yml','.github/ISSUE_TEMPLATE/question.yml','.github/ISSUE_TEMPLATE/config.yml']]"</automated>
  </verify>
  <done>Five files exist and YAML-parse. Atomic commit: `chore(10-04): add codeowners + issue templates + config`.</done>
</task>

<task type="auto">
  <name>Task 4: Audit CODE_OF_CONDUCT.md + extend CONTRIBUTING.md + SECURITY.md</name>
  <files>CODE_OF_CONDUCT.md, CONTRIBUTING.md, SECURITY.md</files>
  <action>
    - **CODE_OF_CONDUCT.md** audit: existing file is already Contributor Covenant 2.1 per research §OSS housekeeping (verified). If short of 2.1 verbatim text, replace with the full canonical 2.1 text from contributor-covenant.org. Confirm contact email points to a real address (or `security@openwhispr.io` if domain TBD — match the address used in SECURITY.md).
    - **CONTRIBUTING.md** extension (current 67 lines; target ~110):
      - Add "License headers" section: every new TS file must carry `// SPDX-License-Identifier: Apache-2.0`; run `pnpm spdx:fix` to add automatically; CI gate enforces
      - Add "Translations" section: how to add ru-side translations; what NOT to translate (audit_log payloads, ADRs, code, docs); link to docs/i18n.md
      - Add "Strict TDD" section: tests precede production code; coverage ≥ 90/90/90/90 on diff
      - Reaffirm English-only source rule (link to ADR-0003)
    - **SECURITY.md** extension (current 34 lines; target ~80):
      - Add response SLA: acknowledge within 72h, status update within 7d, fix or mitigation within 30d for critical
      - Cross-link `docs/security.md` for posture
      - Threat-model summary table: top T-IDs from Phase 6 audit + Phase 10
      - Disclosure process (coordinated)
  </action>
  <verify>
    <automated>grep -q "Contributor Covenant" CODE_OF_CONDUCT.md && grep -qi "version 2.1\\|2\\.1\\.0" CODE_OF_CONDUCT.md && test $(wc -l < CONTRIBUTING.md) -ge 105 && grep -q "spdx:fix\\|SPDX" CONTRIBUTING.md && grep -q "docs/i18n.md" CONTRIBUTING.md && test $(wc -l < SECURITY.md) -ge 70 && grep -q "72h\\|72 hours" SECURITY.md && pnpm lint:english CODE_OF_CONDUCT.md CONTRIBUTING.md SECURITY.md</automated>
  </verify>
  <done>All three files updated; lint-english green. Atomic commit: `docs(10-04): audit code-of-conduct + extend contributing/security with spdx + i18n + sla`.</done>
</task>

<task type="auto">
  <name>Task 5: Write ADRs 0004-0007 (Apache-2.0, Stack, Wire-compat, Multi-tenancy)</name>
  <files>docs/adrs/0004-apache-2-0-licensing.md, docs/adrs/0005-stack-node-fastify-better-auth-drizzle-pg-pgbouncer-valkey-bullmq.md, docs/adrs/0006-wire-compatibility-with-upstream-backend-spec.md, docs/adrs/0007-multi-tenancy-via-rls-with-default-tenant.md</files>
  <action>
    For each ADR, follow the Nygard format from `docs/adrs/0000-template.md` (Context / Decision / Status / Consequences). Decision text and alternatives table per research §ADR list. Status: Accepted (with date 2026-05-13).

    - **0004 Apache-2.0 Licensing**: decision text + alternatives (MIT, BSD-3, AGPL) + note that LICENSE + NOTICE were shipped in commit bd81d82
    - **0005 Stack**: codify Node 24 + Fastify 5 + Better Auth + Drizzle + PG 17 + PgBouncer 1.23+ + Valkey 7.4/8 + BullMQ; alternatives (Express, Prisma, TypeORM, Lucia, MySQL, PG 16/18, Redis) with rejection rationale
    - **0006 Wire-compat with upstream BACKEND_SPEC**: byte-for-byte; v2-deferred endpoints stub 503; alternative: diverge to "cleaner" v1 (rejected — desktop client canonical)
    - **0007 Multi-tenancy via RLS, single "default" tenant v1**: schema retains tenant_id on every row; v1 ships one tenant; alternatives (schema-per-tenant, separate DBs, single-tenant rewrite) with rejection rationale

    Each ADR ~80-120 lines.
  </action>
  <verify>
    <automated>for f in docs/adrs/0004-*.md docs/adrs/0005-*.md docs/adrs/0006-*.md docs/adrs/0007-*.md; do grep -q "## Context" "$f" && grep -q "## Decision" "$f" && grep -q "## Consequences" "$f"; done && pnpm lint:english docs/adrs/0004-*.md docs/adrs/0005-*.md docs/adrs/0006-*.md docs/adrs/0007-*.md</automated>
  </verify>
  <done>4 ADRs in Nygard format, all lint-english green. Atomic commit: `docs(10-04): adrs 0004 apache-2-0 + 0005 stack + 0006 wire-compat + 0007 multi-tenancy`.</done>
</task>

<task type="auto">
  <name>Task 6: Write ADRs 0008-0011 (LiteLLM, Better Auth, i18n, TDD+CI)</name>
  <files>docs/adrs/0008-litellm-as-ai-plane-abstraction.md, docs/adrs/0009-better-auth-email-password-and-oidc-plugin.md, docs/adrs/0010-i18n-runtime-i18next-icu-en-ru-operator-overridable.md, docs/adrs/0011-strict-tdd-github-actions-ci-max-automation.md</files>
  <action>
    Same Nygard format as Task 5.

    - **0008 LiteLLM as AI plane**: bundle in default compose; env-override `LITELLM_BASE_URL` for corporate; no parallel multi-LLM abstraction; alternatives (custom abstraction, Helicone, Portkey) rejected
    - **0009 Better Auth + email-password + OIDC plugin**: first-class email-password; OIDC plug-in for Google/Azure/Okta/generic; alternatives (Lucia — no OAuth-Provider plugin, NextAuth — Pages-Router-flavored, roll-your-own — rejected per CLAUDE.md)
    - **0010 i18n runtime — i18next + ICU, en+ru, operator-overridable**: server uses i18next + i18next-icu + i18next-http-middleware; web uses i18next + react-i18next + ICU; locales under packages/i18n/locales and apps/web/src/locales; LOCALES_DIR operator override; alternatives (node-polyglot — no ICU, next-intl — RSC fragile, FormatJS direct — heavier) rejected. Reference Phase 10 plans 10-01 and 10-02.
    - **0011 Strict TDD + GitHub Actions CI + max automation**: tests precede production code on every phase; CI runs unit + integration + contract + e2e + load + security + i18n on every PR; no human QA; alternatives (pragmatic TDD, GitLab CI, Buildkite) rejected

    Each ADR ~80-120 lines.
  </action>
  <verify>
    <automated>for f in docs/adrs/0008-*.md docs/adrs/0009-*.md docs/adrs/0010-*.md docs/adrs/0011-*.md; do grep -q "## Context" "$f" && grep -q "## Decision" "$f" && grep -q "## Consequences" "$f"; done && pnpm lint:english docs/adrs/0008-*.md docs/adrs/0009-*.md docs/adrs/0010-*.md docs/adrs/0011-*.md && ls docs/adrs/ | grep -E "^00(04|05|06|07|08|09|10|11)-" | wc -l | grep -q "^8$"</automated>
  </verify>
  <done>4 more ADRs shipped; 8 total new ADRs (0004-0011) present. Atomic commit: `docs(10-04): adrs 0008 litellm + 0009 better-auth + 0010 i18n + 0011 tdd-ci`.</done>
</task>

</tasks>

<verification>
- All 6 task verifications green
- `pnpm spdx:check` green (every TS file has the header)
- `pnpm lint:english` green
- `pnpm -r test` green (no regressions from header insertion)
- 8 ADRs (0004-0011) exist in Nygard format under docs/adrs/
- All 5 .github files (CODEOWNERS + 4 ISSUE_TEMPLATE) parse cleanly
</verification>

<success_criteria>
- DOCS-07 closed: SPDX headers everywhere, CODEOWNERS, ISSUE_TEMPLATE, CoC 2.1 audited, CONTRIBUTING + SECURITY extended (LICENSE + NOTICE shipped pre-planner)
- DOCS-08 closed: 8 ADRs (0004-0011) cover every mandatory Key Decision from PROJECT.md
- CI gate prevents new TS files from landing without SPDX header
</success_criteria>

<output>
Create `.planning/phases/10-i18n-docs-oss-housekeeping/10-04-SUMMARY.md`.
</output>
