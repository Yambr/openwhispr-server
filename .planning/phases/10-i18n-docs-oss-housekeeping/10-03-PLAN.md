---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - README.md
  - docs/architecture.md
  - docs/i18n.md
  - docs/security.md
  - docs/operations.md
  - docs/auth.md
  - docs/wire-contract.md
  - docs/litellm-target-spec.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06]
tags: [docs, oss, architecture, security, i18n, mermaid]

must_haves:
  truths:
    - "README quickstart gets a fresh clone to first authenticated /api/transcribe call in < 5 min"
    - "docs/architecture.md contains 3 mermaid sequence diagrams for the hot paths"
    - "docs/i18n.md explains operator-override mechanism (LOCALES_DIR mount)"
    - "docs/security.md documents SSRF gate, secret loading, log scrubbing, audit-log threat model"
    - "docs/operations.md has upgrade/scale/restore runbooks + i18n volume operator instructions"
    - "docs/auth.md, docs/wire-contract.md, docs/litellm-target-spec.md are audited and cross-linked from README"
    - "All docs pass markdown linter and lint-english"
  artifacts:
    - path: "docs/architecture.md"
      provides: "System decomposition + three hot-path mermaid sequence diagrams + tenant isolation diagram + BullMQ topology"
      min_lines: 350
    - path: "docs/i18n.md"
      provides: "Operator-facing locale guide: negotiation chain, file layout, adding locales, override mechanism, CLDR quick reference"
      min_lines: 180
    - path: "docs/security.md"
      provides: "Security posture: SSRF, secrets, log scrubbing, rate limits, audit-log threat model"
      min_lines: 220
    - path: "README.md"
      provides: "Quickstart < 5 min, languages badge, license badge (Apache-2.0), doc index"
      min_lines: 180
  key_links:
    - from: "README.md"
      to: "docs/architecture.md, docs/operations.md, docs/i18n.md, docs/security.md"
      via: "## Documentation table-of-contents section"
      pattern: "docs/(architecture|operations|i18n|security)\\.md"
    - from: "docs/i18n.md"
      to: "10-01 LOCALES_DIR env var"
      via: "operator-override section"
      pattern: "LOCALES_DIR"
---

# Plan 10-03: Docs Suite (DOCS-01..06 + new docs)

<objective>
Ship the OSS documentation surface for OpenWhispr Server. Write three new docs from scratch (`architecture.md`, `i18n.md`, `security.md`). Extend `README.md` for the < 5 min quickstart per DOCS-01. Audit and extend the three existing docs (`operations.md`, `auth.md`, `wire-contract.md`, `litellm-target-spec.md`) for currency post Phases 5-9 + i18n.

Purpose: Satisfies DOCS-01 through DOCS-06. Makes the project navigable for a first community contributor and a corporate-self-host operator.
Output: Eight markdown files (3 new + 5 extended) cross-linked through README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-RESEARCH.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-01-PLAN.md
@.planning/phases/10-i18n-docs-oss-housekeeping/10-01-SUMMARY.md
@README.md
@docs/operations.md
@docs/auth.md
@docs/wire-contract.md
@docs/litellm-target-spec.md
@docs/conventions.md
@docs/observability.md
@docs/self-hosting.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/architecture.md from scratch</name>
  <files>docs/architecture.md</files>
  <action>
    Compose docs/architecture.md (target ~400 lines) covering:
    1. **Overview & component decomposition** — API (Fastify 5), Worker (BullMQ), Web (Next.js 15), Postgres 17, PgBouncer 1.23+, Valkey/Redis 7.4, LiteLLM 1.83.7+, MinIO, Traefik 3; how they map to docker-compose services and Helm chart pods.
    2. **Three hot-path mermaid sequence diagrams**:
       a) `POST /api/transcribe` — desktop client → Traefik → Fastify multipart → LiteLLM upstream → response envelope (cite `apps/api/src/routes/transcribe.ts`)
       b) `WSS /v1/realtime` on :8443 — Traefik :8443 → Fastify upgrade → OpenAI Realtime reverse proxy (cite Phase 4)
       c) `POST /api/agent/stream` — Bearer auth → Fastify route → NDJSON line-flush back to client (cite Phase 4)
    3. **Tenant isolation diagram** — RLS chokepoint, PgBouncer transaction-mode, `tenant_context` setting (cite Phase 1 / Plan 04 RLS work)
    4. **BullMQ topology** — 8 queues (transcribe-overflow, email-delivery, audit-overflow, virtual-key-rotation, etc.) with consumer concurrency and DLQ wiring
    5. **Cross-links** to operations.md, security.md, wire-contract.md, auth.md
    Use mermaid sequenceDiagram + flowchart blocks. All identifiers in English (file paths, queue names) per ADR-0003.
  </action>
  <verify>
    <automated>test $(wc -l < docs/architecture.md) -ge 350 && grep -c "sequenceDiagram\\|flowchart" docs/architecture.md | awk '$1 >= 4 {exit 0} {exit 1}' && pnpm lint:english docs/architecture.md</automated>
  </verify>
  <done>File exists, ≥350 lines, contains ≥4 mermaid blocks, lint-english green. Atomic commit: `docs(10-03): write docs/architecture.md with three hot-path mermaid diagrams`.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/i18n.md from scratch</name>
  <files>docs/i18n.md</files>
  <action>
    Compose docs/i18n.md (target ~200 lines) covering:
    1. **Supported locales** — en (default), ru; future locale roadmap (defer RTL/CJK)
    2. **Locale negotiation chain** — API (Accept-Language → i18next-http-middleware), Web (NEXT_LOCALE cookie → Edge middleware → x-locale header → RSC layout)
    3. **Resource file layout** — `packages/i18n/locales/{lng}/{ns}.json` (server) vs `apps/web/src/locales/{lng}/{ns}.json` (web bundles statically); why two homes
    4. **How to add a new locale** — concrete recipe: create directory, copy en bundle, translate, add to `supportedLngs` in apps/api/src/i18n.ts and apps/web/src/lib/i18n.ts, add to TEST-I18N-01 coverage list, ship via PR
    5. **Operator-override mechanism (I18N-02)** — `LOCALES_DIR` env, docker-compose volume mount `./locales:/etc/openwhispr/locales:ro`, K8s ConfigMap mount, validation at boot
    6. **CLDR plural quick reference** — Russian one/few/many/other categories with boundary table (cite RESEARCH.md plural section)
    7. **Constitutional rule** — source artifacts English-only; Cyrillic only allowed under allowlisted paths (link to ADR-0003); audit_log payloads NEVER translated
    8. **Email template locale source** — users.locale column, set at signup from Accept-Language, user-changeable via web settings
    9. **Troubleshooting** — common failures: missing key falls to en, ICU MessageFormat syntax errors, Edge runtime caveats
  </action>
  <verify>
    <automated>test $(wc -l < docs/i18n.md) -ge 180 && grep -q "LOCALES_DIR" docs/i18n.md && grep -q "one/few/many/other\\|one.*few.*many.*other" docs/i18n.md && pnpm lint:english docs/i18n.md</automated>
  </verify>
  <done>File exists, ≥180 lines, covers required sections, lint-english green. Atomic commit: `docs(10-03): write docs/i18n.md operator locale guide`.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/security.md from scratch</name>
  <files>docs/security.md</files>
  <action>
    Compose docs/security.md (target ~250 lines) covering:
    1. **Posture summary** — HTTPS only, no plaintext, Apache-2.0 patent grant, English source rule
    2. **SSRF gate** — outbound-request allowlist; how it's enforced (Phase 6 audit work — cite specific file paths); test invariants
    3. **Secret loading conventions** — env-only at v1; SOPS-encrypted .env (operator option); never log secrets; pino redact paths table
    4. **Log scrubbing** — pino redact configuration; key-based redaction works across locales; explicit assertion that Cyrillic does not bypass redact
    5. **Rate-limiting topology** — Valkey-backed; per-tenant + per-IP limits; bypass tokens (none in v1)
    6. **Audit log threat model** — T-audit-loss, T-bearer-leak, T-cyrillic-injection (resolved by 10-01 Cyrillic guard); link to `packages/data/src/schema/audit_log.ts` for the 18-action enum
    7. **Phase 6 audit summary** — reference verification + UAT artifacts
    8. **Reporting** — link to SECURITY.md (separate file is the public report channel)
    9. **Threat model registry** — table of T-IDs across phases (security.md is the consolidated home)
  </action>
  <verify>
    <automated>test $(wc -l < docs/security.md) -ge 220 && grep -q "SSRF\\|ssrf" docs/security.md && grep -q "redact" docs/security.md && pnpm lint:english docs/security.md</automated>
  </verify>
  <done>File exists, ≥220 lines, lint-english green. Atomic commit: `docs(10-03): write docs/security.md posture and threat-model index`.</done>
</task>

<task type="auto">
  <name>Task 4: Extend README.md + extend docs/operations.md</name>
  <files>README.md, docs/operations.md</files>
  <action>
    **README.md** extension (target ~200 lines):
    - Quickstart that gets a fresh clone to first authenticated `/api/transcribe` in < 5 min: `git clone`, `cp .env.example .env`, fill API keys (OpenRouter / OpenAI), `docker compose up -d`, register-and-verify via curl, transcribe a sample WAV via curl
    - Add badges: Apache-2.0 license, languages (en, ru), CI status, coverage
    - "Documentation" table of contents linking to: architecture.md, operations.md, i18n.md, security.md, auth.md, wire-contract.md, litellm-target-spec.md, conventions.md, self-hosting.md, observability.md, oidc-operator-config.md, channel-scheme-override.md, storage.md, litellm-mock-mode.md
    - "Contributing" pointer to CONTRIBUTING.md + ADR list
    - "License" section: Apache-2.0, with NOTICE pointer

    **docs/operations.md** extension (current 588 lines; target ~750 lines):
    - **Upgrade runbook** — Helm chart `helm upgrade` flow (Phase 9 chart name + values diff); docker-compose `docker compose pull && docker compose up -d` flow; DB migration handling (drizzle-kit migrate during deploy hook); rollback recipe
    - **Scale runbook** — API replicas (HPA on CPU + p95 latency), worker replicas (HPA on Redis queue depth, queue-name-by-queue-name), CloudNativePG read-replica count tuning
    - **Restore drill** — full restore from MinIO WAL archive + base backup; verification checklist; RTO/RPO targets (cite Phase 8 SLO publication)
    - **i18n operator section** — mount `./locales:/etc/openwhispr/locales:ro`, set `LOCALES_DIR`, validation steps, troubleshooting
  </action>
  <verify>
    <automated>test $(wc -l < README.md) -ge 180 && test $(wc -l < docs/operations.md) -ge 700 && grep -q "Apache-2.0\\|Apache 2.0" README.md && grep -q "LOCALES_DIR" docs/operations.md && pnpm lint:english README.md docs/operations.md</automated>
  </verify>
  <done>README ≥180 lines with full quickstart; operations.md ≥700 lines with upgrade/scale/restore/i18n sections. Atomic commit: `docs(10-03): extend readme quickstart + operations.md upgrade/scale/restore/i18n runbooks`.</done>
</task>

<task type="auto">
  <name>Task 5: Audit + extend docs/auth.md, docs/wire-contract.md, docs/litellm-target-spec.md</name>
  <files>docs/auth.md, docs/wire-contract.md, docs/litellm-target-spec.md</files>
  <action>
    **docs/auth.md** audit (current 234 lines):
    - Confirm OIDC provider plug-in instructions still align with Phase 2 implementation
    - Cross-link `docs/oidc-operator-config.md` and `docs/channel-scheme-override.md`
    - Add: users.locale column documentation (added in 10-01)
    - Add: email locale source flow (Accept-Language → users.locale → email-delivery job locale)
    - Target +30 lines

    **docs/wire-contract.md** audit (current 217 lines):
    - List v2-deferred endpoints (Stripe / referrals) explicitly with rationale
    - Confirm post-Phase-5 CRUD routes (WIRE-22..29) are documented
    - Add: error envelope translation note (English byte-for-byte unless Accept-Language drives ru)
    - Target +40 lines

    **docs/litellm-target-spec.md** audit (current 240 lines):
    - Confirm corporate-override section is current after Phase 5-8 changes
    - Cross-reference Phase 9 Helm chart's LiteLLM override values
    - Target +20 lines
  </action>
  <verify>
    <automated>test $(wc -l < docs/auth.md) -ge 260 && test $(wc -l < docs/wire-contract.md) -ge 250 && test $(wc -l < docs/litellm-target-spec.md) -ge 250 && pnpm lint:english docs/auth.md docs/wire-contract.md docs/litellm-target-spec.md</automated>
  </verify>
  <done>All three docs extended and audited. Atomic commit: `docs(10-03): audit and extend auth/wire-contract/litellm-target-spec for phase 5-9 + i18n`.</done>
</task>

</tasks>

<verification>
- All 5 task verifications green
- `pnpm lint:english` green across all touched docs
- README documentation index links resolve (no 404s in repo)
</verification>

<success_criteria>
- 3 new docs (architecture, i18n, security) shipped per DOCS-02 + research
- README quickstart proven < 5 min in dry run
- 5 existing docs audited and extended
- All cross-links present
</success_criteria>

<output>
Create `.planning/phases/10-i18n-docs-oss-housekeeping/10-03-SUMMARY.md`.
</output>
