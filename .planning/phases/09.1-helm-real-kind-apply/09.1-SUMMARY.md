---
phase: 09.1
title: Phase 09.1 — Real kind Apply (Live Chart Validation)
status: complete
closed_at: 2026-05-13
parent_phase: 09
findings_total: 34
findings_fixed: 33
findings_deferred: 1
helm_unittest: 109
commits: 17
---

# Phase 09.1 — Real kind Apply: Summary

**CLOSED 2026-05-13.** First-ever live `helm install` of `charts/openwhispr/` against a real Kubernetes cluster (kind v0.31). 33 finding's fixed inline with TDD-discipline; 1 deferred to Phase 09.2. **Storage + application tier reaches `1/1 Running` on all 9 pods + migrate Job Complete in 59 seconds.**

## Goal restatement

> An operator on a fresh kind v0.31 cluster runs `examples/kind-bootstrap.sh && helm dependency update && helm install openwhispr ./charts/openwhispr -f values-kind.yaml --wait` and lands on a fully GREEN deploy in <10 min.

## Outcome

| Success criterion | Status |
| --- | --- |
| kind cluster + CNPG + cert-manager + Traefik + metrics-server installed; 6 CRDs Established | ✅ |
| `helm dependency update` resolves Bitnami sub-charts via OCI | ✅ |
| `helm install --wait --timeout 8m` exits 0 | ✅ |
| All 11 pods 1/1 Running, 0 restarts (api/web/worker/litellm/postgres/pooler×2/valkey/minio/minio-console + migrate Complete) | ✅ |
| `helm-unittest` 109/109 (+3 new assertions from 09.1 RED→GREEN cycles) | ✅ |
| `helm test` first-launch SLO probe reaches api in <40ms | ⚠️ Partial (signup 403 — deferred F35) |
| Every fix carries RED→GREEN tests + atomic commit | ✅ |
| `09.1-FINDINGS.md` documents every bug surfaced + fix commit hash | ✅ |

## Findings inventory (34)

Each finding: symptom → root cause → fix (file + RED test) → commit ref. Full text in `09.1-FINDINGS.md`.

| # | Subject | Severity | Status |
| --- | --- | --- | --- |
| F1 | kind host port 8443 collides with running compose stack | LO | fixed |
| F2 | Traefik entrypoint name `websecure-realtime` > 15 chars (k8s ContainerPort) | HI | fixed |
| F3→F10 | Pre-install Helm hook deadlock against CNPG Cluster CR (`pre-upgrade` exclusivity trap) | BLOCKING | fixed |
| F4 | CNPG ≥1.22 rejects `shared_preload_libraries` in `parameters` | HI | fixed |
| F5→F19 | Distroless `registry.k8s.io/kubectl` has no `/bin/sh` for poll loop | HI | fixed (alpine/kubectl) |
| F6 | Bitnami images moved to `bitnamilegacy/*` (Secure Images migration 2024) | HI | fixed |
| F11 | LiteLLM args hardcoded (mock has no CLI flags) | MED | fixed (`.Values.litellm.args`) |
| F12 | migrate.ts reads `DATABASE_URL_OWNER`, chart sent `DATABASE_URL` | HI | fixed |
| F13 | CNPG bootstrap secret `<fullname>-pg-owner` referenced but never created | BLOCKING | fixed |
| F14 | `openwhispr_owner` lacks CREATEDB; litellm DB create denied | HI | fixed (postInitSQL) |
| F15 | `openwhispr_owner` lacks BYPASSRLS; migration RLS-backfill INSERT denied | HI | fixed |
| F16 | `openwhispr_app` runtime role not created (CNPG `managed.roles` block missing) | BLOCKING | fixed |
| F17 | api/worker DATABASE_URL used owner (BYPASSRLS) — RLS bypassed at runtime | HI | fixed |
| F18 | partman schema GRANT USAGE insufficient — needs full ownership | HI | fixed |
| F20 | partman.create_parent() needs CREATE on schema (DDL for child partitions) | HI | fixed |
| F21 | Traefik admin :9000 not exposed; api preflight init timed out | LO | fixed (preflight off in kind) |
| F22 | App ENTRYPOINT validator demands 15 secrets; chart provided 8 | BLOCKING | fixed |
| F23 | Migrations call ALTER ROLE openwhispr_app from owner — needs membership | HI | fixed |
| F24 | CNPG postInitSQL DO-block `$$` consumed as shell `$$` (PID) | BLOCKING | fixed (`$$$$`) |
| F25 | PG 17 ALTER ROLE needs CREATEROLE + ADMIN OPTION | HI | fixed |
| F26 | PG 17 ALTER ROLE SET parameter requires `GRANT SET ON PARAMETER` | HI | fixed |
| F27 | web missing DATABASE_URL (Next.js makeAppDb() at module init) | BLOCKING | fixed |
| F28 | api/worker missing VALKEY_URL (alias for REDIS_URL) | MED | fixed |
| F29 | F28 missed web Deployment | MED | fixed |
| F30 | OTel exporter logspam without Collector sidecar | LO | fixed (`OTEL_SDK_DISABLED`) |
| F31 | web liveness probe targets `/` (404; Next.js app has no root page) | HI | fixed (`/api/health`) |
| F32 | worker has no HTTP server; httpGet probe was fiction | HI | fixed (exec probe) |
| F33 | mock-litellm only has `/health/liveliness`; chart readiness used `/health/readiness` | MED | fixed |
| F34 | testProbe.target default targets ingress hostname (Pod loopback) | MED | fixed (kind overlay) |
| B1 (review) | F22 didn't update `externalsecret.yaml` — ESO path broken | BLOCKING | fixed |
| B2 (review) | secret-presence-probe still listed 8 keys after F22 | HI | fixed |
| F35 | Better Auth signup 403 in helm test (Origin/IP forwarding) | HI | **DEFERRED to 09.2** |

## Architectural insights validated by live apply

Issues that helm-unittest could not surface offline (admission webhook validation, shell escaping in CNPG postInit, distroless image semantics, runtime probe behavior, role + privilege chain in PG 17, sub-chart image registry migrations) — all caught and fixed.

## Commit chain (17 commits)

`4fd5bf6` … `75bd879` on `main`. Each commit atomic per finding cluster, with RED→GREEN helm-unittest assertion pair where templates changed. Lefthook commitlint + english-only checks pass on every commit.

## Phase 09.2 backlog (recorded for next phase)

1. **F35**: probe Better Auth integration (admin-token signup endpoint or X-Forwarded-For trusted source).
2. **Advisor Option C**: replace per-role `ALTER ROLE openwhispr_app SET app.tenant_id` with per-transaction `SET LOCAL` (drops F23 cross-role coupling entirely).
3. **Reviewer H1**: ingress_test.yaml `equal: wsrealtime` is tautological — replace with regex against 15-char k8s ContainerPort name spec.
4. **Reviewer H3**: per-revision migrate Job creates new Job on every `helm upgrade` (even no-op) — adds 600s gate to app rollout.
5. **F21 follow-up**: amend `examples/kind-bootstrap.sh` to enable Traefik admin :9000, flip `preflightCheck=true` back on.
6. **F32 follow-up**: production hardening — add thin Fastify probe-only hook to worker bundle so HTTP probes work.

## Artifacts

- `.planning/phases/09.1-helm-real-kind-apply/`
  - `09.1-PLAN.md`
  - `09.1-FINDINGS.md` (full text per finding)
  - `09.1-REVIEW.md` (code reviewer output)
  - `09.1-SUMMARY.md` (this file)
- `charts/openwhispr/examples/`
  - `kind-config.yaml`
  - `kind-bootstrap.sh`
  - `values-kind.yaml`
