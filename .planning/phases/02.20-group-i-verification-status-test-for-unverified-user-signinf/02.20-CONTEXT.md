---
phase: 02.20
status: locked
date: 2026-05-10
mode: research-first
---

# Phase 02.20 — CONTEXT

## Goal

Close cascade tail Group I: `verification-status > cookie + unverified` test (1 contract test) needs a session cookie for `pending@conformance.test` but BA blocks unverified sign-in by design. Use Option C from advisor research — helper flips `email_verified=true` via owner pool, signs in to get real BA cookie, flips back to `false` in try/finally. Exploits empirically-verified BA behavior: `getSession` does NOT re-check `emailVerified` (only enforced at sign-in/sign-up entry).

## Locked Decisions (Recommended Option C)

### D-01 — `signInFixture` verified:false branch (helper-only)

`packages/contract-tests/src/helpers/sign-in-fixture.ts`:
- When `opts?.verified === false`:
  1. Open pg owner pool via DATABASE_URL_OWNER (mirror `seed/conformance.ts:95-105`)
  2. UPDATE users SET email_verified=true WHERE lower(email)=lower($1)
  3. POST /api/auth/sign-in/email (will succeed now)
  4. **try/finally:** UPDATE users SET email_verified=false WHERE lower(email)=lower($1) — guaranteed revert even if sign-in fails
  5. Close pool
  6. Return JarFetch with the BA-issued cookie

Production code unchanged. requireEmailVerification:true posture preserved (sign-in for unverified user STILL fails for any caller without owner-pool access — i.e., any external client).

### D-02 — TDD

1. Failing test FIRST: `packages/contract-tests/src/helpers/__tests__/sign-in-fixture-unverified.test.ts`
   - Mock pg.Pool to capture UPDATE statements
   - Mock fetch to return successful BA sign-in response
   - Call signInFixture("pending@x.com", {verified:false})
   - Assert: 2 UPDATE calls (true then false), in correct order
   - Assert: pool.end() called exactly once
   - Assert: revert UPDATE called even if fetch throws (try/finally semantics)
2. Reverse-patch: revert verified:false branch → test red

### D-03 — Production-safety verification

In 02.20 SUMMARY:
- Document that owner pool access requires DATABASE_URL_OWNER which is contract-test-runner-internal only
- Document that `requireEmailVerification:true` is preserved for ALL non-test callers
- Confirm no production code changed

### D-04 — Out of scope

- Test-only mint-session API endpoint (Option B) — rejected per advisor (perpetual gate-audit burden)
- Direct-DB session forge with HMAC cookie signing (Option A) — rejected per advisor (BA upgrade brittleness)
- Disabling requireEmailVerification globally — would invalidate the production contract

## Files modified

- `packages/contract-tests/src/helpers/sign-in-fixture.ts` — verified:false branch with try/finally
- `packages/contract-tests/src/helpers/__tests__/sign-in-fixture-unverified.test.ts` (new)

## Acceptance gate

1. New test RED before, GREEN after
2. Re-run E2E: `verification-status > cookie + unverified` test PASSES (last Group I; remaining 3-4 are Group C carries)
3. Existing `verification-status > cookie + verified` test still GREEN (no regression)
4. Single atomic commit
5. Reverse-patch evidence in SUMMARY
