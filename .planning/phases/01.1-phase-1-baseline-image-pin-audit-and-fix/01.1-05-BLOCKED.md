---
plan: 01.1-05
status: blocked
blocker: phase-02.1
date: 2026-05-09
---

# 01.1-05 — BLOCKED on Phase 02.1

## Status

Plan 01.1-05 (re-prove stack boots clean + re-run blocked Phase 02 contract test + flip `02-HUMAN-UAT.md` Item 1) is **blocked**, not failed. The MinIO image-pin defect this phase was created to fix **is proven closed**:

- All 11 baseline images pull cleanly via `docker compose --profile default pull`
- New MinIO pin `RELEASE.2025-09-07T16-13-09Z` (commit `f6eda58`) returns 200 from registry
- Old future-dated pin `RELEASE.2026-03-25T00-00-00Z` is gone
- `scripts/verify-images.sh` exits 0 against the corrected `docker-compose.yml`

## Blocker

`docker compose --profile default up --wait` exits 17 due to a **distinct, pre-existing defect** in `apps/api/Dockerfile` (introduced in Phase 02), not in any artifact this phase touched:

```
#17 [migrate builder 13/14] RUN pnpm --filter @openwhispr/api --prod deploy /out
[ERR_PNPM_DEPLOY_NONINJECTED_WORKSPACE]
By default, starting from pnpm v10, we only deploy from workspaces
that have "inject-workspace-packages=true" set
```

The `migrate` and `api` images cannot build, so the stack cannot come up, so D-08(a) (full stack smoke) and D-08(b) (`make contract-test` re-run) cannot complete.

## Why blocked, not failed

This defect was **always there**, it just used to be hidden behind the MinIO pull failure (which aborted compose-up earlier). Phase 01.1 fixed the MinIO defect cleanly; the Dockerfile defect now becomes the next blocker in the chain. That is correct, expected behavior — fixing one defect frequently uncovers the next one.

Phase 01.1's scope is image pins only. Editing `apps/api/Dockerfile` from inside Phase 01.1 would:
- Cross phase boundaries (the file belongs to Phase 02)
- Muddy the audit trail of what each phase fixed
- Risk a workaround fix (`--legacy` flag) instead of a proper architectural one

## Resolution path

Phase **02.1** will fix the Dockerfile properly (NOT with `--legacy` — see decision discussion required there; candidate approaches are `inject-workspace-packages: true` in `pnpm-workspace.yaml`, OR a multi-stage Dockerfile that does not use `pnpm deploy` at all). Once 02.1 closes, re-run `/gsd-execute-phase 01.1 --wave 3` to complete Plan 01.1-05.

## What was NOT done

- No commit
- No file modifications anywhere in the repo
- `02-HUMAN-UAT.md` Item 1 NOT flipped — remains `failed` until Plan 01.1-05 actually re-runs and confirms contract-test passes against a live, healthy stack

## Diagnostics archived

The Wave 3 executor agent captured full `docker compose up --wait` output to `/tmp/compose-full.log` during its run; that log has since been recycled by the OS but the failure mode is fully reproducible by anyone running `docker compose --profile default up --wait` on this commit.
