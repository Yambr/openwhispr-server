---
phase: 01.1
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/self-tests/verify-images.test.ts
autonomous: true
requirements_addressed: [SC-02a, SC-02b]
requirements: [SC-02a, SC-02b]
user_setup: []

must_haves:
  truths:
    - "A failing Vitest self-test exists that asserts verify-images.sh exits non-zero when fed a fixture compose containing a missing image"
    - "A failing Vitest self-test exists that asserts verify-images.sh exits non-zero when fed a future-dated RELEASE.YYYY-MM-DD tag (no network call)"
    - "A failing Vitest self-test exists that asserts verify-images.sh rejects image strings containing shell-metacharacters (T-01.1-01 input regex guard)"
  artifacts:
    - path: "tests/self-tests/verify-images.test.ts"
      provides: "RED self-tests for verify-images.sh covering SC-02a, SC-02b, and T-01.1-01"
      min_lines: 70
      contains: "FUTURE-DATED"
  key_links:
    - from: "tests/self-tests/verify-images.test.ts"
      to: "scripts/verify-images.sh"
      via: "execFileSync('bash', [SCRIPT], { env: { COMPOSE_FILE: <fixture> } })"
      pattern: "COMPOSE_FILE"
---

<objective>
Write a failing Vitest self-test (`tests/self-tests/verify-images.test.ts`) that pins down the contract of the not-yet-existent `scripts/verify-images.sh`. This is the RED step of TDD per project CLAUDE.md.

Purpose: Lock in three behaviours BEFORE writing the script — (1) missing-image detection (SC-02a), (2) cheap local future-dated tag detection without network (SC-02b), (3) shell-injection input guard (T-01.1-01 from RESEARCH.md §Security Domain).

Output: One Vitest self-test file with three failing test cases. Atomic commit `test(01.1): RED — verify-images self-test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-VALIDATION.md
@tests/self-tests/refuse-default-secrets.test.ts

<interfaces>
<!-- The script-under-test does not exist yet; these are the contracts the test pins down. -->

Script API (Plan 02 will implement; this plan locks the contract):
- Invocation: `bash scripts/verify-images.sh`
- Input source: env var `COMPOSE_FILE` (default: `docker-compose.yml`)
- Stdout: `OK <image>` per healthy image
- Stderr: `MISSING <image>` per non-existent image; `FUTURE-DATED <image> (tag date YYYY-MM-DD > today YYYY-MM-DD)` per future-dated RELEASE tag; `INVALID-IMAGE <image>` per image string failing the safety regex
- Exit code: number of failed images (0 == clean)

Test fixture pattern (mirror refuse-default-secrets.test.ts):
- `mkdtempSync(join(tmpdir(), "verify-images-"))`
- `writeFileSync(join(dir, "docker-compose.yml"), <synthetic compose>)`
- `execFileSync("bash", [SCRIPT], { env: { ...process.env, COMPOSE_FILE: composePath }, stdio: ["ignore", "pipe", "pipe"] })`
- catch and inspect `e.status`, `e.stdout`, `e.stderr`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED self-test for verify-images.sh covering missing/future-dated/shell-injection</name>
  <files>tests/self-tests/verify-images.test.ts</files>
  <read_first>
    - tests/self-tests/refuse-default-secrets.test.ts (mirror this file's setupRepoRoot + execFileSync + try/catch pattern)
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md §Code Examples (`tests/self-tests/verify-images.test.ts` skeleton) AND §Security Domain (T-01.1-01 input regex guard)
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-VALIDATION.md (Per-Task Verification Map row 01.1-01-01)
  </read_first>
  <behavior>
    Three describe/it blocks, one per SC/threat:

    1. SC-02a "exits non-zero with offending image in stderr when image is missing"
       - mkdtempSync → write fixture compose with two services: one image `nonexistent/image-xyz:does-not-exist-${Date.now()}` (cache-buster) + one image `hello-world:latest` (real, used to assert stdout OK line)
       - run script with COMPOSE_FILE=fixture path
       - assert exit code > 0
       - assert stderr matches /nonexistent\/image-xyz:does-not-exist/
       - assert stdout matches /OK hello-world:latest/
       - timeout: 30_000 ms (one real network call to hello-world:latest)

    2. SC-02b "exits non-zero on a future-dated RELEASE tag without network call"
       - fixture compose contains only `image: minio/minio:RELEASE.2099-01-01T00-00-00Z`
       - assert exit code > 0
       - assert stderr matches /FUTURE-DATED/
       - assert stderr matches /RELEASE\.2099-01-01T00-00-00Z/
       - assert stderr names both the future tag-date AND today's date, e.g.:
         `expect(stderr).toMatch(/2099-01-01/); expect(stderr).toMatch(new RegExp(new Date().toISOString().slice(0,10)));`
         (Plan 02's GREEN script emits the FUTURE-DATED message in the form `(tag date YYYY-MM-DD > today YYYY-MM-DD)`)
       - timeout: 10_000 ms (NO network call expected — this is the cheap pre-check)

    3. T-01.1-01 "rejects image strings containing shell metacharacters before invoking docker"
       - fixture compose contains `image: bad;rm-rf-/:tag` (or another shell-metachar example: backtick, $, semicolon, pipe, ampersand)
       - assert exit code > 0
       - assert stderr matches /INVALID-IMAGE/ (or similar guard sentinel — Plan 02 will surface the exact wording; this test pins down that SOMETHING refuses, exit non-zero, stderr names the bad image)
       - assert stderr contains the offending image substring `bad;rm-rf-/`
       - timeout: 5_000 ms (no network call should ever be attempted)

    Each test must clean up its mkdtempSync directory in a finally block (mirror refuse-default-secrets.test.ts).
  </behavior>
  <action>
    Create `tests/self-tests/verify-images.test.ts` with the structure described in `<behavior>`.

    Boilerplate at top (copy/paste from refuse-default-secrets.test.ts and adapt):
    - `import { execFileSync } from "node:child_process";`
    - `import { mkdtempSync, rmSync, writeFileSync } from "node:fs";`
    - `import { tmpdir } from "node:os";`
    - `import { join } from "node:path";`
    - `import { describe, expect, it } from "vitest";`
    - `const SCRIPT = join(process.cwd(), "scripts", "verify-images.sh");`
    - Helper `runVerify(composePath: string): { code: number; stdout: string; stderr: string }` mirroring `runBootstrap` from refuse-default-secrets.test.ts (try/catch on execFileSync; pull `e.status`, `e.stdout`, `e.stderr`).

    DO NOT create `scripts/verify-images.sh` in this plan — it stays missing so the tests fail RED. Plan 02 creates the script (GREEN).

    Atomic commit at end of task: `test(01.1): RED — verify-images self-test`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/self-tests/verify-images.test.ts 2>&1 | grep -E "(FAIL|3 failed|3 (failed|skipped))" ; echo "tests must FAIL at this point (RED step)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/self-tests/verify-images.test.ts` returns 0
    - `grep -c "describe\|it(" tests/self-tests/verify-images.test.ts` returns >= 4 (1 describe + 3 it OR 3 describe + 3 it)
    - `grep -E "FUTURE-DATED" tests/self-tests/verify-images.test.ts` returns at least one match
    - `grep -E "INVALID-IMAGE|shell.?meta|metachar" tests/self-tests/verify-images.test.ts` returns at least one match (T-01.1-01 coverage)
    - `grep -E "COMPOSE_FILE" tests/self-tests/verify-images.test.ts` returns at least one match
    - `grep -E "mkdtempSync" tests/self-tests/verify-images.test.ts` returns at least one match (no writes to real repo)
    - `grep -E "hello-world:latest" tests/self-tests/verify-images.test.ts` returns at least one match
    - `pnpm vitest run tests/self-tests/verify-images.test.ts` exits non-zero (RED — script does not exist yet)
    - The run output contains "ENOENT" or "no such file" or "Cannot find" referencing `scripts/verify-images.sh` (proving tests are exercising the missing script, not green-by-accident)
    - Single atomic commit on the branch with subject `test(01.1): RED — verify-images self-test`; commit touches ONLY `tests/self-tests/verify-images.test.ts`
  </acceptance_criteria>
  <done>The self-test file exists with 3 failing test cases (missing-image, future-dated, shell-injection guard); `pnpm vitest run tests/self-tests/verify-images.test.ts` exits non-zero because `scripts/verify-images.sh` does not yet exist; single commit `test(01.1): RED — verify-images self-test` landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→docker buildx CLI | image strings parsed from `docker-compose.yml` are passed to `docker buildx imagetools inspect <image>`. The compose file is repo-controlled and code-reviewed, but a malicious PR could attempt to land a crafted image string with shell metacharacters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | scripts/verify-images.sh (Plan 02; this plan tests the contract) | mitigate | This plan locks down the contract: the self-test asserts `INVALID-IMAGE` rejection on shell-metachar input. Plan 02 implements the regex `^[a-zA-Z0-9._/:@-]+$` guard inside verify-images.sh. |
| T-01.1-02 | Denial of Service | GHA verify-images workflow | n/a here | Mitigated in Plan 03 (paths filter + timeout-minutes). Not under test in this plan. |

</threat_model>

<verification>
- `pnpm vitest run tests/self-tests/verify-images.test.ts` exits non-zero (3 failing tests)
- `git log --oneline -1` shows `test(01.1): RED — verify-images self-test`
- `git show --stat HEAD` shows ONLY `tests/self-tests/verify-images.test.ts` modified
</verification>

<success_criteria>
RED step landed: failing self-test file committed; downstream Plan 02 has a concrete contract to implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-01-SUMMARY.md`
</output>
