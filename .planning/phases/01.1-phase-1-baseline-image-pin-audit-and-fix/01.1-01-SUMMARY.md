---
plan: 01.1-01
status: complete
date: 2026-05-09
commits:
  - 2e66215
---

# 01.1-01 — RED: verify-images self-test

## What was built

Three Vitest self-tests in `tests/self-tests/verify-images.test.ts` (137 lines) that pin down the contract of `scripts/verify-images.sh`:

- **SC-02a** — missing-image detection (exit non-zero, stderr names image, stdout shows OK for healthy `hello-world:latest`)
- **SC-02b** — cheap local future-dated `RELEASE.YYYY-MM-DD` detection (no network; emits `FUTURE-DATED` plus tag date and today)
- **T-01.1-01** — shell-metachar input regex guard (`INVALID-IMAGE` sentinel; rejects `bad;rm-rf-/:tag`)

Pattern mirrors `refuse-default-secrets.test.ts`: `mkdtempSync` per case, `COMPOSE_FILE` env override, `finally rmSync` cleanup, `execFileSync` with status/stdout/stderr capture.

## Outcome

RED: tests fail with ENOENT on `scripts/verify-images.sh` (script intentionally missing — Plan 01.1-02 implements GREEN).

## Key files

- `tests/self-tests/verify-images.test.ts` — created (137 lines)

## Commits

- `2e66215` — `test(01.1): RED — verify-images self-test`

## Notes

Worktree was reset from initial commit `9f2de60` to expected base `dad72d1` before execution. SUMMARY.md was authored by the executor but not committed inside the worktree before merge — reconstructed by orchestrator from completion notification (commits and test contract verified against on-disk file).
