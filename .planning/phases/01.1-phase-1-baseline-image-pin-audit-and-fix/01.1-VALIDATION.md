---
phase: 01.1
slug: phase-1-baseline-image-pin-audit-and-fix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4 (already pinned via Phase 0; multi-package via pnpm workspace) |
| **Config file** | `vitest.config.ts` at repo root |
| **Quick run command** | `pnpm vitest run tests/self-tests/verify-images.test.ts` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~5s quick, ~60s full |

---

## Sampling Rate

- **After every task commit:** Run `pnpm vitest run tests/self-tests/verify-images.test.ts`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** Full suite green AND both D-08 proofs (local `compose up --wait` + `make contract-test`) green
- **Max feedback latency:** 5 seconds per-task; 60 seconds full

---

## Per-Task Verification Map

(Filled by planner. Anchors below; each plan task references one.)

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | SC-02a/b | T-01.1-01 (input regex guard) | script rejects shell-meta in image strings; exits non-zero on missing/future tags | unit | `pnpm vitest run tests/self-tests/verify-images.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-02-01 | 02 | 1 | SC-02a/b/c | T-01.1-01 | verify-images.sh implements RED tests | unit | `pnpm vitest run tests/self-tests/verify-images.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-02-02 | 02 | 1 | SC-02c | — | `make verify-images` invokes script | smoke | `make verify-images` | ❌ W0 | ⬜ pending |
| 01.1-03-01 | 03 | 2 | SC-03 | T-01.1-02 (DoS via too many pins) | GHA workflow exists with paths filter + timeout | static | `test -f .github/workflows/verify-images.yml` | ❌ W0 | ⬜ pending |
| 01.1-03-02 | 03 | 2 | SC-03 | — | branch-protection JSON has `verify-images / verify-images` context | self-test | `pnpm vitest run tests/self-tests/branch-protection-contexts.test.ts` | ✅ existing | ⬜ pending |
| 01.1-04-01 | 04 | 3 | SC-01 | — | every `image:` in compose verifies clean | integration | `bash scripts/verify-images.sh` (exit 0) | ❌ W0 | ⬜ pending |
| 01.1-05-01 | 05 | 4 | SC-04 | — | full stack reaches healthy | integration | `docker compose --profile default up --wait && docker compose down -v` | ✅ existing | ⬜ pending |
| 01.1-05-02 | 05 | 4 | SC-05 | — | contract-test passes; UAT Item 1 flipped | integration | `make contract-test` | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/self-tests/verify-images.test.ts` — RED stubs for SC-02a + SC-02b
- [ ] `scripts/verify-images.sh` — script under test (created GREEN)
- [ ] `Makefile` `verify-images` target
- [ ] `.github/workflows/verify-images.yml`
- [ ] `scripts/branch-protection.json` entry: `verify-images / verify-images`
- [ ] `docker-compose.yml` defective pins replaced (at minimum line 97 MinIO; PgBouncer line 53 likely also)

*Framework + harness already in place from Phase 0/1; no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Operator-driven `docker compose --profile default up --wait` against real Docker Hub | SC-04 | Requires Docker daemon + network; not part of `pnpm test` matrix | `docker compose --profile default up --wait` → expect every service healthy → `docker compose down -v` |
| Re-run blocked Phase 02 contract-test against fixed stack | SC-05 | Requires live compose stack from manual SC-04 step | `make contract-test` → expect exit 0 → edit `02-HUMAN-UAT.md` Item 1 `result: passed` with commit hash + date note |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s per-task; < 60s full suite
- [ ] `nyquist_compliant: true` set in frontmatter (after planner fills task IDs and they're verified mappable)

**Approval:** pending
