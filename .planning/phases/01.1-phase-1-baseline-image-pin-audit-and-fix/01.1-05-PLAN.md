---
phase: 01.1
plan: 05
type: execute
wave: 3
depends_on: ["01.1-03", "01.1-04"]
files_modified:
  - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md
autonomous: false
requirements_addressed: [SC-04, SC-05]
requirements: [SC-04, SC-05]
user_setup: []

must_haves:
  truths:
    - "`docker compose --profile default up --wait` reaches healthy across every default-profile service against the post-Plan-04 docker-compose.yml (D-08(a))"
    - "`make contract-test` exits 0 against the post-Plan-04 stack (D-08(b))"
    - "02-HUMAN-UAT.md Item 1 has been edited inline: result changed from `blocked` to `passed`; notes contain a link to the Plan 04 commit SHA and today's date"
    - "Summary counters in 02-HUMAN-UAT.md are updated: passed +1, blocked -1"
  artifacts:
    - path: ".planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md"
      provides: "UAT record updated to reflect Phase 01.1 closure of the MinIO defect"
      contains: "result: passed"
  key_links:
    - from: ".planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md"
      to: "Phase 01.1 Plan 04 commit"
      via: "commit SHA cited in notes block"
      pattern: "[a-f0-9]{7,40}"
---

<objective>
Re-prove the full stack now boots clean (D-08(a)) and the Phase 02 contract-test that was blocked on the MinIO defect now passes (D-08(b)). Then update `02-HUMAN-UAT.md` Item 1 in place per CONTEXT.md D-08(b).

Purpose: This is the phase exit gate per D-09 — Phase 01.1 is not done until both proofs pass.

Output: Verified-healthy stack + verified-passing contract-test + edited UAT file. Atomic commit `test(01.1): re-verify stack + flip Phase 02 UAT Item 1`. NOT autonomous: requires Docker daemon + network egress to pull all 11 images and execute the full contract-test stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md
@.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md
@Makefile
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local stack smoke — `docker compose --profile default up --wait` (D-08(a))</name>
  <files></files>
  <read_first>
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md §decisions D-08(a)
    - docker-compose.yml (note: default profile = postgres, pgbouncer, valkey, minio, traefik, otel-collector, loki, tempo, mimir, grafana, migrate, api)
  </read_first>
  <action>
    Pre-flight: ensure `.env` exists. If not, run `tools/bootstrap.sh --ci` (mirrors the contract-test job in ci.yml).

    Run sequence (capture full output to a temp file for the commit body if any failure investigation is needed):

    1. `docker compose --profile default pull` (pre-pull all images so the timeout in step 2 isn't dominated by network).
    2. `docker compose --profile default up -d --wait` — `--wait` blocks until every service reaches healthy or one fails. Default `--wait-timeout` is 60s; bump to 180s with `--wait-timeout 180` since first-launch with cold caches can be slower.
    3. On success: `docker compose ps --format json` to capture state for the SUMMARY. Then `docker compose down -v` to tear down (D-08(a) explicitly requires teardown).
    4. On failure: capture `docker compose logs --no-color > /tmp/compose-logs-$(date +%s).txt`, then `docker compose down -v`, then surface the failure to the user as a CHECKPOINT (do NOT proceed to Task 2).

    If everything healthy: continue to Task 2.
  </action>
  <verify>
    <automated>docker compose --profile default up -d --wait --wait-timeout 180; rc=$?; docker compose down -v; test "$rc" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose --profile default up -d --wait --wait-timeout 180` exits 0
    - All default-profile services reach `healthy` (verifiable via `docker compose ps` showing State == running and Health == healthy for postgres, pgbouncer, valkey, minio, traefik, otel-collector, loki, tempo, mimir, grafana, api; migrate exits 0 i.e. State == exited (0))
    - `docker compose down -v` cleanup runs at the end (idempotent — fine if it returns warnings about absent containers)
    - No image pull errors in the output (proves Plan 04 pin fixes resolve)
  </acceptance_criteria>
  <done>D-08(a) proof captured; stack tears down cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run `make contract-test` (D-08(b))</name>
  <files></files>
  <read_first>
    - Makefile §contract-test target (it brings up `default` + `contract-test` profiles, seeds, runs the suite, tears down)
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md (Item 1 — the blocked entry)
  </read_first>
  <action>
    1. Run `make contract-test`. The Makefile target itself handles `up --wait`, seeding, suite execution, and `docker compose down -v` regardless of pass/fail.

    2. Capture the exit code (`$?` immediately after).

    3. Capture the head SHA (Plan 04's `fix(01.1): correct future-dated/missing image pins` commit) for citation in Task 3:
       ```bash
       git log --oneline --grep="^fix(01.1): correct future-dated" -1
       ```

    4. If `make contract-test` exits non-zero: surface as CHECKPOINT — Phase 01.1 is not complete; do NOT edit the UAT file. The failure is informative (likely a Phase 02 wiring issue distinct from the MinIO pin defect; route it back to the user for triage).

    5. If `make contract-test` exits 0: continue to Task 3.
  </action>
  <verify>
    <automated>make contract-test</automated>
  </verify>
  <acceptance_criteria>
    - `make contract-test` exits 0
    - The output contains "Test Files" and "Tests" Vitest summary lines from the @openwhispr/contract-tests package
    - All 8 conformance test files green (conventions, check-user, verification-status, delete-account, health, oauth-redirect, token-rotation, cookie-host) — per the expected from 02-HUMAN-UAT.md Item 1
    - `docker compose down -v` ran (Makefile target enforces; verify by `docker compose ps -a` returning empty)
  </acceptance_criteria>
  <done>D-08(b) proof captured; SHA of Plan 04 commit recorded for use in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Flip Phase 02 UAT Item 1 from `blocked` to `passed`</name>
  <files>.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md (entire file — note YAML-ish frontmatter at top, Tests section with Item 1, Summary block, Notes block)
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md §Architecture Patterns §UAT update mechanics (exact format guidance)
  </read_first>
  <action>
    Edit `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` in place:

    1. **Frontmatter:** update `updated:` to today's UTC timestamp (use ISO-8601 format `YYYY-MM-DDTHH:MM:SSZ`). Leave `status: partial`, `phase`, `source`, `started` UNCHANGED.

    2. **Item 1 (`### 1. Run \`make contract-test\` against a live docker-compose stack`):**
       - Change `result: blocked` → `result: passed`
       - Replace the `notes:` block with:
         ```yaml
         notes: |
           Re-run on <today's date YYYY-MM-DD> after Phase 01.1 corrected the
           MinIO image pin (commit <SHORT-SHA from Task 2>). All 8 conformance
           test files green; docker compose --profile default up --wait
           healthy across all services (D-08(a)); make contract-test exit 0
           (D-08(b)). Original blocked-on note retained in git history.
         ```
       - Use the actual short SHA captured in Task 2, not a placeholder.

    3. **Summary block:** update counters:
       - Was: `total: 4, passed: 0, issues: 0, pending: 3, skipped: 0, blocked: 1`
       - Now: `total: 4, passed: 1, issues: 0, pending: 3, skipped: 0, blocked: 0`

    4. **Notes block (bottom of file):** APPEND (do not overwrite the existing 2026-05-09 entry) a new bullet:
       ```
       - <today's date YYYY-MM-DDTHH:MMZ> — Item 1 (`make contract-test`) re-executed
         after Phase 01.1 fixed the MinIO baseline image pin (commit <SHORT-SHA>).
         All 8 conformance test files green; default profile stack reaches healthy.
         Status: passed.
       ```

    5. Do NOT touch Items 2, 3, 4 — they remain pending.

    6. Commit subject: `test(01.1): re-verify stack + flip Phase 02 UAT Item 1`. Commit body should reference D-08(a)+D-08(b) and the SHA of Plan 04. Commit touches ONLY `.planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md`.
  </action>
  <verify>
    <automated>grep -E "^result: passed$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md && grep -E "^blocked: 0$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md && grep -E "^passed: 1$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^result: passed$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns at least 1
    - `grep -c "^result: blocked$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns 0 (no item still blocked)
    - `grep -E "^passed: 1$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns one match
    - `grep -E "^blocked: 0$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns one match
    - `grep -E "^pending: 3$" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns one match (unchanged)
    - `grep -E "Phase 01.1" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns at least one match (links the closure to this phase)
    - `grep -E "commit [a-f0-9]{7,40}" .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns at least one match (cites Plan 04 SHA, not a placeholder)
    - **D-08(a) proof present in notes:** `grep -cE 'D-08\(a\)|compose.*--wait|all services healthy' .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns >= 1 (the notes block must record the local stack-up healthy proof, not just the SHA)
    - **D-08(b) proof present in notes:** `grep -cE 'D-08\(b\)|contract-test.*pass|make contract-test' .planning/phases/02-auth-wire-api-skeleton-conformance-harness/02-HUMAN-UAT.md` returns >= 1 (the notes block must record the contract-test green proof, not just the SHA)
    - The frontmatter `updated:` field reflects today's UTC date
    - Single commit on the branch with subject `test(01.1): re-verify stack + flip Phase 02 UAT Item 1`; commit touches ONLY the UAT file
  </acceptance_criteria>
  <done>Phase 02 UAT Item 1 flipped to passed with the Phase 01.1 commit cited; phase exit gate D-09 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → Docker Hub | full image pulls for 11 services + ad-hoc fixture builds. No new attack surface beyond what Plan 04 introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | scripts/verify-images.sh | mitigate (in Plan 02) | Inherited; no new exposure. |
| T-01.1-02 | DoS | local invocation | accept | Two stack-ups (one for D-08(a), one inside `make contract-test`) — bounded; not a CI concern. |
</threat_model>

<verification>
- `docker compose --profile default up --wait` exits 0 (Task 1)
- `make contract-test` exits 0 (Task 2)
- 02-HUMAN-UAT.md Item 1 reflects passed status with SHA citation (Task 3)
- `git log --oneline -1` shows `test(01.1): re-verify stack + flip Phase 02 UAT Item 1`
</verification>

<success_criteria>
SC-04 + SC-05 satisfied. D-08(a) and D-08(b) both green. D-09 phase exit gate cleared. Phase 01.1 is COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-05-SUMMARY.md`
</output>
