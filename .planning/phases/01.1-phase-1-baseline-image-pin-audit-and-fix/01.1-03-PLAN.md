---
phase: 01.1
plan: 03
type: execute
wave: 2
depends_on: ["01.1-02"]
files_modified:
  - .github/workflows/verify-images.yml
  - scripts/branch-protection.json
autonomous: true
requirements_addressed: [SC-03]
requirements: [SC-03]
user_setup: []

must_haves:
  truths:
    - "A new GHA workflow .github/workflows/verify-images.yml exists with a top-level paths filter on docker-compose.yml + scripts/verify-images.sh + the workflow file itself"
    - "The workflow has timeout-minutes: 5 (T-01.1-02 DoS mitigation)"
    - "The workflow runs `bash scripts/verify-images.sh` (or `make verify-images`) on ubuntu-24.04 with SHA-pinned harden-runner + actions/checkout"
    - "scripts/branch-protection.json contains the `verify-images` context (job name)"
    - "The existing branch-protection-contexts self-test passes (proves JSON↔workflow link)"
  artifacts:
    - path: ".github/workflows/verify-images.yml"
      provides: "Path-filtered CI guard preventing recurrence of bad image pins (D-07, T-01.1-02)"
      contains: "paths:"
    - path: "scripts/branch-protection.json"
      provides: "Required-check entry forcing verify-images to be a merge gate"
      contains: "verify-images"
  key_links:
    - from: "scripts/branch-protection.json"
      to: ".github/workflows/verify-images.yml"
      via: "context name == job name == 'verify-images'"
      pattern: "verify-images"
    - from: ".github/workflows/verify-images.yml"
      to: "scripts/verify-images.sh"
      via: "run: bash scripts/verify-images.sh"
      pattern: "bash scripts/verify-images.sh"
---

<objective>
Add a path-filtered GitHub Actions workflow that runs `verify-images` on every PR touching `docker-compose.yml`, and wire it as a required check via `scripts/branch-protection.json`.

Purpose: Recurrence guard (CONTEXT.md D-07) + DoS mitigation (T-01.1-02 — paths filter + timeout-minutes bound the blast radius). Required-check status mirrors the existing `contract-test` job pattern.

Output: New workflow file + updated branch-protection JSON. The existing `branch-protection-contexts.test.ts` self-test will validate the JSON↔workflow consistency automatically. Atomic commit `ci(01.1): verify-images workflow + branch-protection context`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md
@.github/workflows/ci.yml
@scripts/branch-protection.json
@tests/self-tests/branch-protection-contexts.test.ts

<interfaces>
<!-- Existing CI patterns to mirror -->

From .github/workflows/ci.yml (contract-test job — model for SHA-pinned third-party actions):
```yaml
- uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450  # v2.19.1
  with: { egress-policy: audit }
- uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5
```

From scripts/branch-protection.json (existing required contexts list):
```json
{
  "required_status_checks": {
    "strict": true,
    "contexts": ["lint", "typecheck", "test", "mutation-quick", "lint-english",
                 "pr-checklist", "gitleaks", "trivy-fs", "codeql", "license-scan",
                 "harness-self-check", "lint-rls", "test-migration", "contract-test"]
  },
  ...
}
```

From tests/self-tests/branch-protection-contexts.test.ts (the self-test that auto-validates the link):
- Reads scripts/branch-protection.json contexts
- Walks .github/workflows/*.yml/*.yaml, collects every top-level `jobs.<name>:` key
- Asserts every required context name corresponds to an actual job name
- Therefore: the workflow's top-level job key MUST be `verify-images` (single token, no slash)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/verify-images.yml with paths filter, timeout, and SHA-pinned actions</name>
  <files>.github/workflows/verify-images.yml</files>
  <read_first>
    - .github/workflows/ci.yml (mirror harden-runner SHA pin, actions/checkout SHA pin, ubuntu-24.04 runner — copy SHAs verbatim from the contract-test job)
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md §Code Examples (`.github/workflows/verify-images.yml` skeleton) AND §Security Domain (T-01.1-02)
    - .planning/STATE.md "Third-party GHA actions SHA-pinned with version-tag comments (Trivy 2026-03-19 incident response)" — locked policy
  </read_first>
  <action>
    Create `.github/workflows/verify-images.yml` with the following exact structure:

    ```yaml
    name: verify-images

    on:
      pull_request:
        paths:
          - 'docker-compose.yml'
          - 'scripts/verify-images.sh'
          - '.github/workflows/verify-images.yml'
      push:
        branches: [main]
        paths:
          - 'docker-compose.yml'
          - 'scripts/verify-images.sh'
          - '.github/workflows/verify-images.yml'

    permissions:
      contents: read

    jobs:
      verify-images:
        runs-on: ubuntu-24.04
        timeout-minutes: 5
        steps:
          - uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450  # v2.19.1
            with: { egress-policy: audit }
          - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5
          - name: Run image-pin verifier
            run: bash scripts/verify-images.sh
    ```

    Notes:
    - Top-level job name MUST be `verify-images` (single token) so it matches the branch-protection context exactly per the existing `branch-protection-contexts.test.ts` self-test logic (line 23-36 collects job names directly).
    - `timeout-minutes: 5` mitigates T-01.1-02 (RESEARCH.md §Security Domain).
    - `paths:` filter at workflow trigger level mitigates T-01.1-02 by skipping the network call when compose-file isn't touched.
    - `egress-policy: audit` (NOT block) — the workflow needs network egress to Docker Hub.
    - Action SHAs MUST match the contract-test job in ci.yml verbatim. If the executor needs to pin a newer SHA, leave a `# v<x.y.z>` comment per Trivy 2026-03-19 policy.
  </action>
  <verify>
    <automated>test -f .github/workflows/verify-images.yml && grep -c "verify-images" .github/workflows/verify-images.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .github/workflows/verify-images.yml` returns 0
    - `grep -E "^name: verify-images$" .github/workflows/verify-images.yml` returns one match
    - `grep -E "^  verify-images:$" .github/workflows/verify-images.yml` returns one match (top-level job key)
    - `grep -E "timeout-minutes: 5" .github/workflows/verify-images.yml` returns one match
    - `grep -E "paths:" .github/workflows/verify-images.yml` returns at least 2 matches (one in pull_request, one in push)
    - `grep -E "docker-compose.yml" .github/workflows/verify-images.yml` returns at least 2 matches
    - `grep -E "step-security/harden-runner@[a-f0-9]{40}" .github/workflows/verify-images.yml` returns one match (SHA-pinned)
    - `grep -E "actions/checkout@[a-f0-9]{40}" .github/workflows/verify-images.yml` returns one match (SHA-pinned)
    - `grep -E "bash scripts/verify-images.sh" .github/workflows/verify-images.yml` returns one match
    - YAML parses cleanly: `pnpm exec node -e "console.log(require('yaml').parse(require('fs').readFileSync('.github/workflows/verify-images.yml','utf8')).jobs['verify-images'].steps.length)"` outputs `3`
  </acceptance_criteria>
  <done>verify-images.yml workflow exists; SHA-pinned third-party actions; paths filter limits run scope; timeout bounds DoS exposure.</done>
</task>

<task type="auto">
  <name>Task 2: Add `verify-images` to scripts/branch-protection.json required contexts</name>
  <files>scripts/branch-protection.json</files>
  <read_first>
    - scripts/branch-protection.json (current contexts list — append, do not reorder)
    - tests/self-tests/branch-protection-contexts.test.ts (note: collectJobs() walks every workflow file's top-level jobs.<name>; context name MUST equal job name == "verify-images" — single token)
  </read_first>
  <action>
    Edit `scripts/branch-protection.json` and append `"verify-images"` to the `required_status_checks.contexts` array, after the existing `"contract-test"` entry. Preserve JSON formatting (4-space indent, comma placement).

    Result should look like:
    ```json
        "contexts": [
          "lint",
          "typecheck",
          "test",
          "mutation-quick",
          "lint-english",
          "pr-checklist",
          "gitleaks",
          "trivy-fs",
          "codeql",
          "license-scan",
          "harness-self-check",
          "lint-rls",
          "test-migration",
          "contract-test",
          "verify-images"
        ]
    ```

    Do NOT touch any other field. Do NOT remove `enforce_admins`, `required_pull_request_reviews`, `restrictions`, etc.
  </action>
  <verify>
    <automated>pnpm vitest run tests/self-tests/branch-protection-contexts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const j=JSON.parse(require('fs').readFileSync('scripts/branch-protection.json','utf8')); process.exit(j.required_status_checks.contexts.includes('verify-images')?0:1)"` exits 0
    - `node -e "const j=JSON.parse(require('fs').readFileSync('scripts/branch-protection.json','utf8')); process.exit(j.required_status_checks.contexts.length===15?0:1)"` exits 0 (was 14, now 15)
    - `node -e "JSON.parse(require('fs').readFileSync('scripts/branch-protection.json','utf8'))"` exits 0 (valid JSON)
    - `pnpm vitest run tests/self-tests/branch-protection-contexts.test.ts` exits 0 (the existing self-test now finds the verify-images job because Task 1 created it with the matching name)
    - Single combined commit subject: `ci(01.1): verify-images workflow + branch-protection context`; commit touches `.github/workflows/verify-images.yml` AND `scripts/branch-protection.json` only
  </acceptance_criteria>
  <done>verify-images is wired as a required check; the existing branch-protection-contexts self-test validates the link automatically; commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub PR → GHA runner | untrusted PR contributors can trigger workflow runs by touching `docker-compose.yml`. The workflow has read-only `permissions: contents: read` and no secrets exposure. |
| GHA runner → Docker Hub | anonymous registry inspect; rate-limited shared bucket. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | scripts/verify-images.sh | mitigate (in Plan 02) | This workflow inherits the input regex guard from Plan 02. No additional mitigation needed at the CI layer — the script self-protects. |
| T-01.1-02 | Denial of Service | GHA verify-images workflow | mitigate | (a) `paths:` filter restricts trigger to compose/script/workflow file changes only — typical PRs don't touch these. (b) `timeout-minutes: 5` caps single-run wallclock — even pathological inputs can't burn unbounded runner minutes. (c) `permissions: contents: read` minimises blast radius if the script is ever subverted. |
| T-01.1-03 | Tampering (supply chain) | third-party GHA actions | mitigate | All third-party actions SHA-pinned per STATE.md / Trivy 2026-03-19 policy. Comments preserve human-readable version. |
</threat_model>

<verification>
- `pnpm vitest run tests/self-tests/branch-protection-contexts.test.ts` exits 0
- `pnpm vitest run tests/self-tests/` exits 0 (full self-test suite still green)
- The new workflow YAML parses without errors
</verification>

<success_criteria>
SC-03 satisfied: workflow exists, path-filtered, time-bounded, SHA-pinned, wired as a required check, and the existing self-test validates the JSON↔workflow consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-03-SUMMARY.md`
</output>
