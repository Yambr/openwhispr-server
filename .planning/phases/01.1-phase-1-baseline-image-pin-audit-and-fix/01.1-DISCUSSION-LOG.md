# Phase 01.1: Phase 1 baseline image-pin audit and fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 01.1-phase-1-baseline-image-pin-audit-and-fix
**Areas discussed:** Audit scope, Replacement pin policy, Recurrence guard, Re-verification approach

---

## Gray Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Audit scope | Just MinIO vs full audit of all 11 pins | ✓ |
| Replacement pin policy | latest stable / latest known-good / Phase 1 intent | (rolled into deep-dive) |
| Recurrence guard (CI check) | script + CI / renovate / skip | (rolled into deep-dive) |
| Re-verification approach | local up --wait / contract-test re-run / both | (rolled into deep-dive) |

User initially selected only "Audit scope" but the deep-dive batch carried the other three as the necessary follow-ups (they cannot be left ambiguous for the planner).

---

## Audit scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full audit of all 11 pins | Verify every `image:` exists + non-future-dated; fix all defects | ✓ |
| MinIO only + spot-check obvious dates | Surgical fix + glance at other date-shaped tags | |
| MinIO only | Lowest blast radius; only the confirmed defect | |

**User's choice:** Full audit of all 11 pins.
**Notes:** Same root-cause class likely produced sibling defects.

## Replacement pin policy

| Option | Description | Selected |
|--------|-------------|----------|
| Latest stable as of today | Highest non-prerelease tag currently published | ✓ |
| Match Phase 1 CONTEXT intent | Re-pin to whatever fits Phase 1 D-02 intent today | |
| Latest known-good prior to bad pin date | Conservative — minimizes drift | |

**User's choice:** Latest stable as of today.
**Notes:** Captured guard in D-04 — if "latest stable" would cross a major-version intent line from Phase 1 (e.g., MinIO drops the date scheme, Postgres jumps to 18), executor must STOP and surface, not silently bump.

## Recurrence guard (CI check)

| Option | Description | Selected |
|--------|-------------|----------|
| `make verify-images` script + CI job | Bash script over `docker manifest inspect`, required CI check | ✓ |
| Skip — one-time fix only | Trust the audit; no recurring guard | |
| Document a manual checklist | `operations.md` text only, no automation | |

**User's choice:** `make verify-images` script + CI job.
**Notes:** Project rule is max test automation, no human QA — manual checklist not viable.

## Re-verification approach

| Option | Description | Selected |
|--------|-------------|----------|
| Local `up --wait` + re-run blocked Phase 2 contract-test | Both proofs | ✓ |
| Local `up --wait` only | Pulls + healthchecks; leave UAT to Phase 2 cycle | |
| Re-run Phase 2 contract-test only | Implicit proof via passing tests | |

**User's choice:** Both.
**Notes:** Phase 01.1 owns the unblock — the Phase 02 HUMAN-UAT entry must flip to `passed` as a success criterion of this phase.

## Wrap

| Option | Description | Selected |
|--------|-------------|----------|
| Write CONTEXT.md | Scope tightly bounded; ready to plan | ✓ |
| One more question (commit hygiene / dependent configs) | Add a follow-up | |

**User's choice:** Write CONTEXT.md. Commit hygiene captured anyway as D-10 since it's a near-zero-cost decision.

## Claude's Discretion

- Output format of `verify-images.sh` beyond the per-image OK/MISSING line and exit code.
- `docker manifest inspect` vs `docker buildx imagetools inspect` — both satisfy D-05.
- Exact `Makefile` integration style (matches whatever pattern already exists in the file).

## Deferred Ideas

- Renovate / Dependabot for ongoing image bumps.
- Major-version bumps of any service.
- Registry-API HEAD check (no Docker daemon required).
