---
phase: 01.1
plan: 04
type: execute
wave: 2
depends_on: ["01.1-02"]
files_modified:
  - docker-compose.yml
autonomous: false
requirements_addressed: [SC-01]
requirements: [SC-01]
user_setup: []

must_haves:
  truths:
    - "Every `image:` line in docker-compose.yml verifies clean: registry inspect succeeds AND no future-dated date suffix"
    - "MinIO pin replaced with the latest stable RELEASE tag currently published (verified at execution time, not hardcoded)"
    - "Pins that already verify clean were NOT changed (D-03)"
    - "No replacement crosses a major-version intent line (D-04); if it would, the executor halted and surfaced to the user"
    - "MinIO upstream archive status (2026-04-25) is documented in the commit body (RESEARCH.md §Sources MinIO note)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Phase 1 compose stack with corrected image pins"
      contains: "image: minio/minio:RELEASE."
  key_links:
    - from: "scripts/verify-images.sh"
      to: "docker-compose.yml"
      via: "exit 0 when run against the fixed file"
      pattern: "OK"
---

<objective>
Run the verifier from Plan 02 against the real `docker-compose.yml`, enumerate every defect, and fix each per CONTEXT.md D-02/D-03/D-04. Land the corrections as a SINGLE atomic commit (D-10) with an audit summary in the body listing every audited image and its before/after state.

Purpose: Close SC-01 (every pin verifies clean) so Plan 05 can prove the stack boots and the Phase 02 contract-test unblocks.

Output: Edited `docker-compose.yml` (one or more `image:` lines updated, others UNCHANGED). Atomic commit `fix(01.1): correct future-dated/missing image pins`. NOT autonomous: requires Docker daemon + network egress to upstream registries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md
@.planning/phases/01-core-infra-multi-tenant-data/01-CONTEXT.md
@docker-compose.yml
@scripts/verify-images.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 11 image pins and fix every defect found</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (entire file — note image lines at 30, 53, 82, 97, 114, 136, 157, 170, 183, 196, 289)
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-CONTEXT.md §decisions D-01 through D-04 and D-10
    - .planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-RESEARCH.md §Per-image audit (table of expected current-state-of-the-world per image as of 2026-05-09)
    - .planning/phases/01-core-infra-multi-tenant-data/01-CONTEXT.md §decisions D-02 (major-version intent: postgres 17.x, valkey 8.x, traefik v3.x, edoburu/pgbouncer 1.23+, minio dated RELEASE scheme, otel-collector 0.1XX.x, grafana 11.x / loki 3.x / tempo 2.x / mimir 2.x — anything else means halt-and-surface)
  </read_first>
  <action>
    Procedure (do NOT shortcut — D-01 says full audit):

    1. **Enumerate defects.** Run `bash scripts/verify-images.sh` against the real `docker-compose.yml`. Capture stdout (`OK <image>` lines) AND stderr (`MISSING <image>` / `FUTURE-DATED <image>` lines). Save the raw output for the commit body.

    2. **For each `OK` image:** DO NOTHING (D-03 — do not chase versions).

    3. **For each `MISSING` or `FUTURE-DATED` image:** resolve a replacement tag using the procedure below, then edit `docker-compose.yml` to swap the image line in place. Preserve all surrounding YAML (indentation, profiles, environment, healthcheck — touch ONLY the value after `image:`).

       Replacement tag resolution procedure (apply per defective image):

       a. Identify the image's major-version intent from Phase 1 D-02 (e.g., MinIO is "dated RELEASE.YYYY-MM-DD scheme"; PgBouncer is "1.23+"; Postgres is "17.x-alpine"; Traefik is "v3.x"; Valkey is "8.x"; Loki is "3.x"; Tempo is "2.x"; Mimir is "2.x"; Grafana is "11.x"; OTel Collector contrib is "0.1XX.x"; Mailpit is "v1.x"). Read the entry from the cross-references in CONTEXT.md / RESEARCH.md if uncertain.

       b. Query Docker Hub (or upstream registry) for the latest stable tag matching that intent. Two methods:
          - Preferred: `docker buildx imagetools inspect <repo>:<floating-major-tag>` — if the floating tag exists, it resolves to the latest patch; use that floating tag verbatim.
          - Fallback: query Docker Hub HTTP API: `curl -s "https://hub.docker.com/v2/repositories/<namespace>/<name>/tags/?page_size=100&ordering=last_updated" | jq -r '.results[].name' | grep -E '<intent regex>' | head -1`

       c. Validate the candidate: `docker buildx imagetools inspect <candidate>` must exit 0.

       d. Validate D-04 (major-version intent preserved). For example:
          - Postgres candidate must match `^17\.` or `^17-`. NOT `^18`.
          - MinIO candidate must match `^RELEASE\.\d{4}-\d{2}-\d{2}T` AND date <= today.
          - PgBouncer candidate must match `^v?1\.2[3-9]` (1.23+).
          - Traefik candidate must match `^v3\.`.
          - Valkey candidate must match `^8\.` or `^8-`.
          - Grafana candidate must match `^11\.`.
          - Loki candidate must match `^3\.`.
          - Tempo candidate must match `^2\.`.
          - Mimir candidate must match `^2\.`.
          - OTel Collector contrib candidate must match `^0\.1\d{2}\.`.
          - Mailpit candidate must match `^v1\.`.

       e. **HALT-AND-SURFACE** if no candidate satisfies (a)-(d). Stop the task, do not silently bump majors, return `## CHECKPOINT REACHED` with the image name, intent, and the candidates considered. The user makes the call.

       f. Edit `docker-compose.yml` and replace the `image:` value in place. Make NO other edits.

    4. **Re-verify.** Run `bash scripts/verify-images.sh` against the modified `docker-compose.yml`. It MUST exit 0 (every image OK). If not, repeat step 3 for any remaining defect.

    5. **Document MinIO archive status (RESEARCH.md §Sources, "MinIO upstream archived 2026-04-25").** Even though it does not change Phase 01.1 actions per D-03/D-04, the commit body MUST mention it as a footnote — it is load-bearing context for the next operator who reads `git log` looking for "why this pin." Do NOT touch STATE.md or any other file in this commit (D-10 atomic-commit: `git show --stat HEAD` must list ONLY `docker-compose.yml`).

    6. **Single atomic commit (D-10).** Commit subject: `fix(01.1): correct future-dated/missing image pins`. Body MUST contain:

       ```
       Audit of all 11 image pins in docker-compose.yml (D-01).

       Before / after:
       | # | service        | before                                       | after                                        | reason                |
       |---|----------------|----------------------------------------------|----------------------------------------------|------------------------|
       | 1 | postgres       | postgres:17.5-alpine                         | <unchanged | new>                            | OK | DEFECT: ...      |
       | 2 | pgbouncer      | edoburu/pgbouncer:1.23.1                     | <unchanged | new>                            | ...                    |
       | 3 | valkey         | valkey/valkey:8.1-alpine                     | <unchanged | new>                            | ...                    |
       | 4 | minio          | minio/minio:RELEASE.2026-03-25T00-00-00Z     | minio/minio:RELEASE.2025-09-07T16-13-09Z     | future-dated; replaced |
       | 5 | traefik        | traefik:v3.6                                 | <unchanged | new>                            | ...                    |
       | 6 | otel-collector | otel/.../otel-collector-contrib:0.151.0      | <unchanged | new>                            | ...                    |
       | 7 | loki           | grafana/loki:3.5.0                           | <unchanged | new>                            | ...                    |
       | 8 | tempo          | grafana/tempo:2.8.0                          | <unchanged | new>                            | ...                    |
       | 9 | mimir          | grafana/mimir:2.16.0                         | <unchanged | new>                            | ...                    |
       | 10| grafana        | grafana/grafana:11.6.0                       | <unchanged | new>                            | ...                    |
       | 11| mailpit        | axllent/mailpit:v1.29                        | <unchanged | new>                            | ...                    |

       Verifier output: bash scripts/verify-images.sh exits 0 against the post-fix
       docker-compose.yml.

       Footnote: the upstream MinIO repository was archived on 2026-04-25
       (per https://hub.docker.com/r/minio/minio/tags). RELEASE.2025-09-07T16-13-09Z
       remains the latest stable tag; no new MinIO releases are coming. Per
       Phase 01.1 D-03 (do not chase versions) and D-04 (do not cross major
       intent), Phase 01.1 pins to the last good tag and surfaces this for a
       future "MinIO replacement evaluation" phase.
       ```

       Use the EXACT actual before/after values discovered in step 1 — do not paste from CONTEXT.md or RESEARCH.md verbatim if they differ from runtime reality.

    7. The commit MUST touch ONLY `docker-compose.yml`. No other files.
  </action>
  <verify>
    <automated>bash scripts/verify-images.sh; rc=$?; echo "verifier exit code: $rc"; test "$rc" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify-images.sh` exits 0 against the modified `docker-compose.yml`
    - `grep -E '^\s*image: minio/minio:RELEASE\.2026-' docker-compose.yml` returns ZERO matches (the future-dated pin is gone)
    - `grep -E '^\s*image: minio/minio:RELEASE\.\d{4}-\d{2}-\d{2}T' docker-compose.yml` returns exactly 1 match AND the date portion is `<=` today (use `awk -F'[:.]' '{print}'` or similar to extract and compare)
    - `grep -cE '^\s*image:' docker-compose.yml` returns exactly 11 (no images added or removed)
    - Single commit on the branch with subject `fix(01.1): correct future-dated/missing image pins`; `git show --stat HEAD` lists ONLY `docker-compose.yml`
    - `git show HEAD --format=%B | grep -c "Before / after"` returns at least 1 (audit table present in body)
    - `git show HEAD --format=%B | grep -ciE "minio.*archiv"` returns at least 1 (MinIO archive footnote present)
    - For every image line in docker-compose.yml: `docker buildx imagetools inspect <image>` exits 0 (already covered by verifier exit 0, but assert manually for any executor doing a sanity check)
    - No image string contains shell metacharacters (verifier already enforces; double-check by reading the diff)
    - **D-04 enforcement:** every row of the audit table in the commit body uses one of the literal `reason` values: `OK`, `replaced (within intent)`, or `escalated to user`. Verify with `git log -1 --format=%B | grep -E '\| (OK|replaced \(within intent\)|escalated to user) \|'` returning at least one match per non-header row.
    - **D-04 STOP semantics:** if the audit finds ANY image whose only viable replacement would cross a major-version intent line (i.e. would warrant `escalated to user` as the reason), the executor MUST NOT commit. Instead, surface a CHECKPOINT to the user enumerating the image, the intent line from Phase 1 D-02, and the candidates considered. Resume only with explicit user direction.
  </acceptance_criteria>
  <done>Every `image:` line in docker-compose.yml verifies clean; the future-dated MinIO pin is replaced with a real, present-day-or-earlier RELEASE tag; the commit body documents the full audit + MinIO archive context; single atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→Docker Hub | image-tag identity is trusted by name; this phase does NOT add SHA-pinning (out of scope per CONTEXT.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-04 | Tampering (supply chain) | docker-compose.yml image identity | accept (deferred) | Typosquat / publisher-identity verification is explicitly OUT OF SCOPE per CONTEXT.md (Deferred Ideas). Image SHA-pinning (`image@sha256:...`) is recorded as a deferred future-phase item. Phase 01.1 only verifies tag existence and date-shape, not publisher identity. |
| T-01.1-05 | DoS (registry rate limit during audit) | local audit invocation | accept | 11 anonymous Docker Hub inspects per audit run is well under the 100/6h anonymous bucket. If executor hits `toomanyrequests`, retry after 6h or authenticate (manual override). |

</threat_model>

<verification>
- `bash scripts/verify-images.sh` exits 0
- `git log --oneline -1` shows `fix(01.1): correct future-dated/missing image pins`
- `git show --stat HEAD` shows ONLY `docker-compose.yml`
- The verifier output captured in step 1 matches what the audit table in the commit body claims
</verification>

<success_criteria>
SC-01 satisfied: every image pin verifies clean; the seed defect (MinIO future-dated) is fixed; any sibling defects discovered are also fixed; major-version intent preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-baseline-image-pin-audit-and-fix/01.1-04-SUMMARY.md`
</output>
