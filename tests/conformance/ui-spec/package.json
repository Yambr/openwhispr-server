{
  "name": "@openwhispr/conformance-ui-spec",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 12 UICONF-05 axe baseline (real Chromium against e2e-cjm compose stack). Lives outside the main test suites so its boot lifecycle (bootStack/tearStack from tests/e2e-cjm/support/compose-harness) does not collide with vitest workers.",
  "scripts": {
    "test:axe": "playwright test --config playwright.config.ts"
  }
}
