{
  "name": "@openwhispr/e2e",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "description": "Host-side end-to-end suite (DISCIPLINE rule 3 back-fill). Runs against the live docker-compose stack with the hermetic mock-LiteLLM contract profile by default; extend via env to target real-key compose profiles.",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "test:hermetic": "E2E=1 vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "testcontainers": "^11.14.0",
    "tough-cookie": "5.1.2",
    "ws": "^8.21.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "@types/node": "25.6.2",
    "vitest": "4.1.8"
  }
}
