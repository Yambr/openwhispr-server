# Build artifacts
dist/
build/
*.tsbuildinfo

# Next.js (apps/web)
.next/
apps/web/.next/
apps/web/out/

# Dependencies
node_modules/
.pnpm-store/

# Environment / secrets
.env
.env.*
!.env.example
!.env.e2e.example
!.env.embedded.example
!.env.slim.example
!.env.full.example
*.key
*.pem

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Coverage
coverage/
.nyc_output/

# Logs
*.log
logs/

# Test artifacts
.testcontainers/
playwright-report/
test-results/

# Mutation testing
.stryker-tmp/
reports/

# Tooling
.turbo/

# Bootstrap-generated artifacts (Plan 01-02)
/.env
/backups/*
!/backups/.gitignore
/keys/*
!/keys/.gitkeep
!/keys/backup.age.pub
.claude/worktrees/

# Bootstrap-generated TLS chain (Phase 02.7 / 02.22) — root-ca + leaf are
# minted by tools/bootstrap.sh on first run. Never commit cert material.
compose/traefik/certs/*.crt
compose/traefik/certs/*.srl
!compose/traefik/certs/.gitkeep

# Local-only reference doc (content migrated to docs/litellm-target-spec.md)
/speaches-audio.md
Open wispr server.zip
.debug/

# Phase 13 / Plan 01 — e2e-cjm harness artifacts (generated bddgen specs,
# playwright traces, and the Makefile sidecar state file).
tests/e2e-cjm/.bdd-gen/
tests/e2e-cjm/test-results/
tests/e2e-cjm/.scratch/
.e2e-cjm-user-was-running
# Phase 69 / Plan 06 — `make e2e-cjm-dump-logs` per-service compose log capture
# (CI `if: failure()` upload-artifact source; regenerated each run).
compose-logs/

# Phase 15-04 / HI-03 — history-scrub.sh per-invocation state dir.
# Contains workdir.path + protection-rollback.json scoped to a single
# RUN_ID under .scrub-state/<RUN_ID>/. Never checked in.
.scrub-state/

# Test evidence (local-only ephemeral, Quick 260527-pj6)
.test-evidence/
