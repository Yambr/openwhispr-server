{{/*
Intentionally empty (Phase 09 Plan 05, A6 decision).

The compose stack's PgBouncer pattern required a hand-rolled `userlist.txt`
Secret regenerated at boot from SCRAM hashes (Phase 01.2 admin-password fix).
The CNPG Pooler CRD makes that pattern obsolete: CNPG generates and rotates
the pooler's SCRAM credentials natively against the Cluster's managed roles.

This file exists as a marker so:
  (a) the plan's files_modified manifest is satisfied,
  (b) future maintainers searching for `userlist` find this explainer,
  (c) reverting to a hand-rolled Secret in some future regression is an
      explicit code change rather than a silent omission.

Pitfall #10 carry-over (09-RESEARCH.md): the Phase 08.1 admin password fix
becomes built-in. Do NOT add an initContainer that mutates pooler credentials.
*/}}
