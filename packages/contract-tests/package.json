{
  "name": "@openwhispr/contract-tests",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts"
  },
  "comment:files": "Phase 68 / Plan 68-01 — REVIEW byok HIGH HI-01/02/05. Explicit allowlist so the published tarball ships ONLY the consumer-facing schema/error surface. Test files (*.test.ts) and the privileged test-only helpers (helpers/sign-in-fixture.ts carries FIXTURE_PASSWORD + an owner-pool email_verified DB flip) are excluded via the `!` negations. The bundled audio fixture (HI-05) is included so the multipart helper resolves a package-local path.",
  "files": [
    "src/index.ts",
    "src/schemas.ts",
    "src/errors.ts",
    "src/env.ts",
    "src/negative-matrix.ts",
    "src/helpers/cookie-jar.ts",
    "src/helpers/http.ts",
    "src/helpers/multipart.ts",
    "src/helpers/streaming.ts",
    "fixtures/audio/sample-1s.wav",
    "!**/*.test.ts",
    "!src/helpers/sign-in-fixture.ts"
  ],
  "scripts": {
    "test": "vitest run",
    "test:run": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@openwhispr/wire-schemas": "workspace:*",
    "pg": "8.22.0",
    "tough-cookie": "5.1.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "@types/ws": "^8.18.1",
    "vitest": "4.1.10",
    "ws": "^8.21.0"
  }
}
