{
  "name": "@openwhispr/byok-guard",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "comment:files": "Phase 68 / Plan 68-01 — REVIEW byok HIGH HI-01/02. Defence-in-depth allowlist: src/ currently has no test files, but an explicit files: field keeps any future *.test.ts out of the published tarball.",
  "files": [
    "src/index.ts",
    "src/redact-url.ts",
    "!**/*.test.ts"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pino": "*"
  },
  "devDependencies": {
    "@types/node": "25.6.2",
    "typescript": "6.0.3",
    "vitest": "4.1.0"
  }
}
