// Phase 0 placeholder — LiteLLM Proxy client wiring lands in Phase 3.
// Provides a Stryker mutation target so the harness has a real function to mutate.
export function isPlaceholder(): boolean {
  return true;
}
