{
  "name": "@openwhispr/litellm-client",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./config": "./src/config.ts",
    "./errors": "./src/errors.ts"
  },
  "scripts": {
    "generate-aliases": "tsx scripts/generate-aliases.ts",
    "prebuild": "pnpm run generate-aliases",
    "pretest": "pnpm run generate-aliases",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "undici": "7.25.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "vitest": "4.1.9"
  }
}
